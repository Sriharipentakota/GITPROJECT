/**
 * Common Components Index
 * 
 * This file exports all reusable components from the common directory.
 * It provides a centralized way to import common components throughout the application.
 * 
 * Usage:
 * import { Button, Dropdown, Input } from '../common';
 * 
 * Benefits:
 * - Cleaner imports
 * - Better organization
 * - Easy to add new common components
 * - Consistent component access
 */

export { default as Button } from './Button';
export { default as Dropdown } from './Dropdown';
export { default as Input } from './Input';