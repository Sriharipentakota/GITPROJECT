// Playwright MCQ questions — filled concept by concept
// Format: ['type','diff','question', code|null, ['A','B','C','D'], correctIdx, 'hint']

export const PLAYWRIGHT_QUESTIONS_RAW: Record<string, any[][]> = {

// ── 1. Installation & Setup ────────────────────────────────────
setup: [
// O×12 ─────────────────────────────────────────────────────────
['O','B','What command initialises a new Playwright project with an interactive setup wizard?',null,['npm install playwright','npm init playwright@latest','npx playwright install','npm create @playwright'],1,'`npm init playwright@latest` scaffolds the entire project: config, sample test, browser download, and optional CI workflow.'],
['O','B','Which file is generated as the primary Playwright configuration after running `npm init playwright@latest`?',null,['playwright.json','playwright.config.ts','.playwrightrc','test.config.ts'],1,'`playwright.config.ts` (or `.js`) is the single source of truth for projects, timeout, retries, reporters, and the `use` block.'],
['O','B','Which command downloads all browser binaries that Playwright needs?',null,['npx playwright install','npm install browsers','npx playwright browsers','npx playwright setup'],0,'`npx playwright install` downloads Chromium, Firefox, and WebKit. Run it once after installing the package.'],
['O','I','What does the `--headed` flag do when passed to `npx playwright test --headed`?',null,['Adds verbose logging to stdout','Shows the browser window during the test run','Opens Playwright Inspector for step debugging','Generates a detailed HTML report'],1,'By default Playwright runs headless. `--headed` opens a visible browser window so you can watch each test step.'],
['O','B','Which command opens the Playwright visual UI mode with time-travel debugging?',null,['npx playwright debug','npx playwright open','npx playwright test --ui','npx playwright interactive'],2,'`npx playwright test --ui` launches the interactive dashboard where you can pick tests, watch them run, and rewind to any step.'],
['O','I','What is the default test directory that `npm init playwright@latest` creates?',null,['/spec','/e2e','/tests','/playwright'],2,'The wizard creates `tests/` and sets `testDir: \'./tests\'` in the config. You can rename it during setup.'],
['O','I','What does `npx playwright test --reporter=html` generate?',null,['A plain text log file','A self-contained HTML report viewable in the browser','A JUnit XML report','A live streaming test dashboard'],1,'The HTML reporter writes `playwright-report/index.html` — a rich, self-contained page with results, screenshots, and traces.'],
['O','A','What does `npx playwright install --with-deps` do on a Linux CI server?',null,['Only downloads browser binaries','Installs browsers AND their required OS-level system libraries','Installs Playwright globally with all peer dependencies','Updates Playwright and all browsers to the latest versions'],1,'On Linux, `--with-deps` also installs packages like libgtk, libgbm, and libxshmfence that the browsers need.'],
['O','I','Which file does `npm init playwright@latest` optionally generate for continuous integration?',null,['.github/workflows/playwright.yml','.github/playwright.yml','.ci/playwright.yml','github-actions.yml'],0,'When you answer "yes" to the CI question, the wizard writes a ready-to-use GitHub Actions YAML at `.github/workflows/playwright.yml`.'],
['O','B','What command prints the currently installed Playwright version?',null,['npx playwright version','npx playwright --version','npx playwright info','npx playwright -V'],1,'Like most CLI tools, `--version` (or `-V`) prints the installed version number.'],
['O','I','What does `npx playwright show-report` do?',null,['Opens the Playwright documentation site','Serves the last generated HTML report locally and opens it in the browser','Displays the current test configuration','Shows a report of all installed browsers'],1,'After running tests with the HTML reporter, `show-report` starts a local HTTP server and opens the report in your default browser.'],
['O','I','Which browser name does Playwright use for its Chromium-based engine?',null,['chrome','chromium','google-chrome','chromium-browser'],1,'Playwright uses `chromium` (not `chrome`) alongside `firefox` and `webkit`. Pass it to `npx playwright install chromium`.'],

// F×7 ─────────────────────────────────────────────────────────
['F','B','Fill in the blank to install only the Firefox browser: `npx playwright install ______`',null,['firefox','Firefox','firefox-browser','gecko'],0,'The three browser IDs for the install command are `chromium`, `firefox`, and `webkit`.'],
['F','I','Fill in the flag that filters tests by title pattern: `npx playwright test ______=login`',null,['--grep','--filter','--match','--pattern'],0,'`--grep` accepts a regex string; `--grep=login` runs only tests whose title contains "login".'],
['F','B','Complete the config to set a 30-second test timeout: `defineConfig({ timeout: ______ })`',null,['30_000','30','30s','"30000"'],0,'Playwright timeouts are in milliseconds. `30_000` (or `30000`) equals 30 seconds.'],
['F','I','Fill in the flag for step-by-step debugging: `npx playwright test ______ login.spec.ts`',null,['--debug','--inspect','--break','--step'],0,'`--debug` opens Playwright Inspector, pauses before the first action, and lets you step through the test interactively.'],
['F','I','Complete the `use` block option that skips SSL certificate errors: `use: { ignoreHTTPS______: true }`',null,['Errors','SSL','Certs','Warnings'],0,'`ignoreHTTPSErrors: true` tells Playwright to bypass self-signed or invalid SSL certificate errors.'],
['F','A','Fill in the config option to retry failed tests twice in CI: `______: process.env.CI ? 2 : 0`',null,['retries','retry','rerun','attempts'],0,'`retries` accepts a number. The ternary enables retries only in CI (`process.env.CI` is set by GitHub Actions etc.).'],
['F','I','Complete the command to run tests matching a specific project: `npx playwright test ______=firefox`',null,['--project','--browser','--engine','--target'],0,'`--project` matches the `name` field of a project entry in `playwright.config.ts`. Project names are case-sensitive.'],

// E×6 ─────────────────────────────────────────────────────────
['E','B','What is wrong with this command?\n`npx playwright login.spec.ts`',null,['Missing the `test` subcommand','Wrong file extension — should be .js','Should use npm instead of npx','Missing --run flag'],0,'The correct form is `npx playwright test login.spec.ts`. The `test` subcommand is required; without it the CLI does not know what to do.'],
['E','I','Spot the error in this config snippet:\n```\nimport { defineConfig } from \'@playwright/test\';\nexport default defineConfig({\n  projects: [{ name: \'chrome\', use: { ...devices[\'Desktop Chrome\'] } }]\n});\n```',null,['`devices` is not imported','testDir is missing','projects syntax is wrong','defineConfig does not accept projects'],0,'`devices` must be imported: `import { defineConfig, devices } from \'@playwright/test\';`'],
['E','B','What is wrong with this import in a test file?\n`import { test, expect } from \'playwright\';`',null,['Should import from `@playwright/test`, not `playwright`','`expect` cannot be imported alongside `test`','Curly braces are invalid here','Should use require() not import'],0,'The test-runner API (`test`, `expect`, built-in fixtures) lives in `@playwright/test`. The `playwright` package is the lower-level browser API.'],
['E','I','What is wrong with this CI config?\n`use: { headless: false }`',null,['CI environments have no display — headless: false will fail','headless must be set per project, not in use','`false` is not a valid value for headless','headless is not a valid use option'],0,'CI runners (GitHub Actions, etc.) have no display server, so running with a visible browser window (`headless: false`) throws an error.'],
['E','I','What is wrong with this project entry?\n```\n{ name: \'desktop\', use: { browserName: \'chrome\' } }\n```',null,['browserName must be `\'chromium\'`, not `\'chrome\'`','name cannot be \'desktop\'','use must be a spread of devices','browserName is not a valid property'],0,'Valid `browserName` values are `\'chromium\'`, `\'firefox\'`, and `\'webkit\'`. `\'chrome\'` is not a recognised Playwright browser name.'],
['E','A','What is wrong with this reporter config?\n`reporter: { type: \'html\' }`',null,['Reporter must be a string like `\'html\'` or an array of arrays','reporter is not a valid config key','html is not a valid reporter name','type must be uppercase'],0,'`reporter` accepts a string (`\'html\'`), an array of strings, or an array of arrays (`[[\'html\', options]]`). An object with `type` is not valid.'],

// D×6 ─────────────────────────────────────────────────────────
['D','I','Tests run in Chromium despite `--project=Firefox` being passed. What is wrong?',null,['Project names are case-sensitive — use `--project=firefox` to match the config name','--project requires the full browser path','Firefox project must be the first in the config','Use --browser instead of --project'],0,'The `--project` value must exactly match the `name` in `playwright.config.ts`. If the config says `"firefox"` (lowercase), `--project=Firefox` won\'t match.'],
['D','B','`npx playwright test` reports "No tests found" but the file `login.test.ts` exists. What is most likely wrong?',null,['The default testMatch only finds `*.spec.ts` — rename to `login.spec.ts` or update testMatch','Tests must live in the project root','The file must be named `playwright.spec.ts`','TypeScript files are not supported by default'],0,'Playwright\'s default `testMatch` is `**/*.spec.{ts,js}`. Files ending in `.test.ts` are skipped. Rename the file or add `*.test.ts` to testMatch.'],
['D','A','Linux CI fails with missing library errors when installing browsers. Which command fixes it?',null,['npx playwright install --with-deps','sudo npx playwright install','npx playwright install --all','npx playwright install linux-deps'],0,'`--with-deps` automatically installs the required OS packages (libwoff, libgbm, libharfbuzz, etc.) alongside the browser binaries.'],
['D','I','Tests pass locally but GitHub Actions reports "Browser executable not found". What step is missing from the CI workflow?',null,['A step running `npx playwright install --with-deps`','Setting headless: true in config','Installing a newer Node.js version','Caching node_modules'],0,'CI runners do not include Playwright browsers. You must add `run: npx playwright install --with-deps` as an explicit workflow step.'],
['D','B','After `npm init playwright@latest`, running `npx playwright test` fails with "Cannot find module @playwright/test". What went wrong?',null,['`npm install` was not run after init to actually download the package','Node.js version is incompatible','playwright.config.ts has a wrong import path','TypeScript compiler is missing'],0,'`npm init playwright@latest` adds `@playwright/test` to `package.json` but you still need `npm install` to download it into `node_modules`.'],
['D','I','Tests time out frequently because `timeout: 5000` is too short. How should you fix it?',null,['Increase the timeout value, e.g. `timeout: 30_000`','Remove the timeout property to use no limit','Pass `--timeout` flag at run time only','Set `timeout: 0` to disable all timeouts'],0,'Raise `timeout` in `playwright.config.ts`. The value is in milliseconds, so `30_000` gives each test 30 seconds before it is marked failed.'],

// C×5 ─────────────────────────────────────────────────────────
['C','B','Complete the import: `import { ______, devices } from \'@playwright/test\';`',null,['defineConfig','PlaywrightConfig','configure','Config'],0,'`defineConfig` is the helper function that wraps your config object and provides full TypeScript type support.'],
['C','I','Complete the `use` block to set the application root URL:\n`use: { ______: \'http://localhost:3000\' }`',null,['baseURL','url','serverURL','rootURL'],0,'`baseURL` lets you write `page.goto(\'/login\')` instead of the full absolute URL in every test.'],
['C','I','Complete the config to run tests in 4 parallel processes: `defineConfig({ ______: 4 })`',null,['workers','parallel','concurrency','threads'],0,'`workers` controls how many test processes run simultaneously. Set to a number or `\'auto\'` to use all CPU cores.'],
['C','A','Complete the project entry using a preset device:\n`{ name: \'safari\', use: { ...______[\'Desktop Safari\'] } }`',null,['devices','browsers','presets','defaults'],0,'`devices` imported from `@playwright/test` contains preset viewports and user-agents for desktop and mobile browsers.'],
['C','I','Complete the config to use both HTML and line reporters:\n`reporter: [[______], [\'line\']]`',null,['\'html\'','html','{ type:\'html\' }','\'html-reporter\''],0,'Each entry in the reporter array is itself an array: `[\'html\']`, `[\'line\']`, or `[\'junit\', {outputFile:\'results.xml\'}]`.'],

// L×5 ─────────────────────────────────────────────────────────
['L','B','What happens when `npx playwright test` is run with no arguments?',null,['Runs all files matching testMatch in testDir','Runs only the most recently modified file','Opens the interactive UI','Prints the current configuration'],0,'With no arguments Playwright discovers every file matching the configured `testMatch` pattern (default `**/*.spec.{ts,js}`) and runs them all.'],
['L','I','`retries: 2` is set in config. A test fails all 3 attempts. What is the final result?',null,['Test is marked FAILED (1 original + 2 retries)','Test is marked FLAKY after the second retry','Test is skipped automatically','Test runs again until it passes'],0,'`retries: 2` gives each failing test 2 extra attempts. If all 3 fail the test is marked FAILED. A test is marked FLAKY only if it passes on a retry.'],
['L','I','In what order does `npm init playwright@latest` ask its questions?',null,['TypeScript/JavaScript → test directory → GitHub Actions','Browsers → TypeScript → test directory','Test directory → GitHub Actions → TypeScript','Install browsers → TypeScript → test directory'],0,'The wizard asks: (1) TS or JS, (2) test directory name, (3) add GitHub Actions workflow. Browsers are downloaded after, not prompted.'],
['L','A','How does project-level `use` interact with global `use` in playwright.config.ts?',null,['Project-level `use` overrides global `use` for that project','Global `use` always takes precedence','They cannot coexist; it is a config error','Both are ignored; only CLI flags apply'],0,'Playwright merges settings from global `use` and project-level `use`, with the project level winning for any conflicting keys — perfect for per-browser overrides.'],
['L','I','What is the key difference between `--headed` and `--debug`?',null,['--headed shows the browser window; --debug also pauses in Playwright Inspector','They are identical flags','--debug runs without a browser','--headed is for API tests, --debug for UI tests'],0,'`--headed` makes tests visible but runs at full speed. `--debug` also opens Playwright Inspector, pauses before the first action, and lets you step line-by-line.'],

// R×5 ─────────────────────────────────────────────────────────
['R','B','Your team is starting a new project and wants the fastest path to a working Playwright setup. Which command should come first?',null,['npm init playwright@latest','npm install @playwright/test','npx playwright install','npm install playwright'],0,'`npm init playwright@latest` is the single command that handles everything: installs the package, downloads browsers, creates config, and writes a sample test.'],
['R','I','Your CI pipeline takes 20 minutes because tests run one at a time. What is the best single config change?',null,['Set `workers: 4` (or `\'auto\'`) to run tests in parallel','Set `retries: 0` to skip flaky re-runs','Add `--headed` to the CI command','Reduce `timeout` to fail faster'],0,'Increasing `workers` lets Playwright run multiple tests concurrently across separate processes, dramatically cutting total run time.'],
['R','I','Your team wants retries in CI but not locally (for fast feedback). How should you configure `retries`?',null,['`retries: process.env.CI ? 2 : 0`','`retries: 2` with `--no-retry` locally','Separate config files for CI and local','`retries: \'ci\'`'],0,'`process.env.CI` is set by GitHub Actions, GitLab CI, and most other CI systems, making it the standard way to differentiate CI from local runs.'],
['R','A','Your organisation tests against Chrome, Firefox, and Safari. What is the recommended approach?',null,['Define three projects in playwright.config.ts with the appropriate devices preset','Run `npx playwright test` three times with different `--browser` flags','Maintain three separate config files','Use browser.connect() to connect to remote browsers'],0,'Playwright\'s `projects` array is designed for exactly this: one `npx playwright test` run, three browser targets, one aggregated report.'],
['R','I','A QA engineer wants to run only tests tagged `@smoke` without editing any files. What is the command?',null,['npx playwright test --grep "@smoke"','npx playwright test --tag smoke','npx playwright test --filter smoke','npx playwright test smoke.spec.ts'],0,'Add tags in test titles (e.g. `test(\'login @smoke\', ...)`) and filter with `--grep "@smoke"`. Playwright supports full regex syntax here.'],

// S×4 ─────────────────────────────────────────────────────────
['S','I','A developer runs `npx playwright test` and sees "Executable doesn\'t exist at ~/.cache/ms-playwright/…". What should they do?',null,['Run `npx playwright install` to download the missing browser binaries','Reinstall Node.js','Set an explicit executablePath in config','Switch from @playwright/test to playwright'],0,'This error means the browser binaries were never downloaded. `npx playwright install` fetches them into `~/.cache/ms-playwright`.'],
['S','A','A team needs both an HTML report and a JUnit XML report from the same test run. How should the config look?',null,['`reporter: [[\'html\'], [\'junit\', { outputFile: \'results.xml\' }]]`','Run tests twice with different `--reporter` flags','Use `reporter: \'html+junit\'`','Configure two separate playwright.config files'],0,'The `reporter` array accepts multiple reporters simultaneously. Each entry is `[reporterName, optionalOptions]`.'],
['S','I','Developers use both Windows and macOS. Should they maintain separate playwright.config.ts files?',null,['No — Playwright\'s API and config are fully cross-platform','Yes — path separators differ','Yes — Windows only supports Chromium','Yes — browser binaries differ per OS'],0,'Playwright handles OS differences internally. The same `playwright.config.ts` works on Windows, macOS, and Linux without modifications.'],
['S','A','The `~/.cache/ms-playwright` folder is using several gigabytes. How can a team reduce its size?',null,['Install only the browsers actually used: `npx playwright install chromium`','Run `npm uninstall playwright`','Delete node_modules and reinstall','Run `npx playwright clean`'],0,'Playwright stores browser binaries in `~/.cache/ms-playwright` (not node_modules). Installing only the browsers you test against (e.g. just Chromium) saves significant disk space.'],

// ── Additional batch ×50 ─────────────────────────────────────
['O','B','What does the `testDir` option in playwright.config.ts control?',null,['The directory where test results are saved','The root directory Playwright scans when discovering test files','The directory for browser binaries','The output directory for HTML reports'],1,'testDir tells Playwright where to look for test files — defaulting to "tests" or the directory containing playwright.config.ts.'],
['O','B','What is the purpose of the `baseURL` option in the `use` block?',null,['It sets the API base URL for HTTP requests only','It allows using relative paths like page.goto(\'/login\') instead of full URLs','It restricts tests to one origin domain','It sets the URL for the HTML report server'],1,'With baseURL set, page.goto(\'/login\') resolves to baseURL + \'/login\', keeping tests portable across environments.'],
['O','I','What does `timeout: 30000` at the top level of playwright.config.ts configure?',null,['The maximum time a single expect() assertion can wait','The maximum time each individual test is allowed to run before it is marked as timed out','The time Playwright waits for the browser to launch','The time between test retries'],1,'The top-level timeout is per-test — if a test body takes longer, Playwright aborts it and marks it failed.'],
['O','I','What is the difference between `timeout` and `expect.timeout` in playwright.config.ts?',null,['They are aliases for the same setting','timeout limits the entire test duration; expect.timeout limits how long each web-first assertion retries before failing','expect.timeout is only used in API tests','timeout applies to navigation; expect.timeout applies to locators'],1,'A test might run for 30 s (timeout) while each individual assertion retries for 5 s (expect.timeout) — the two clocks run independently.'],
['O','B','What does `headless: false` in the `use` block do?',null,['Disables screenshots','Opens a visible browser window during test execution','Disables JavaScript in the browser','Runs tests without network access'],1,'headless: false makes the browser window visible — useful for debugging tests interactively.'],
['O','I','What does the `--ui` flag do when running Playwright?',null,['Opens the browser in headed mode only','Launches the Playwright UI Mode — an interactive test explorer with time-travel debugging and watch mode','Enables the UI-mode reporter for terminal output','Runs only tests tagged @ui'],1,'UI Mode (npx playwright test --ui) is a graphical test runner with a test list, trace viewer, and real-time re-run on file save.'],
['O','B','What environment variable enables Playwright Inspector for step-by-step debugging?',null,['DEBUG=playwright','PLAYWRIGHT_DEBUG=1','PWDEBUG=1','PW_INSPECT=true'],2,'PWDEBUG=1 pauses execution before the first Playwright action and opens the Inspector — you can step through actions and examine selectors.'],
['O','I','What does `forbidOnly: !!process.env.CI` do in playwright.config.ts?',null,['Prevents running tests with the --only flag in CI','Causes the test run to fail if any test.only() or test.describe.only() is found committed to the repo — a safety net against accidentally merging exclusive tests','Disables test.only() globally in all environments','Prevents running focused tests locally'],1,'forbidOnly is a CI guard — developers who forget to remove .only() get a CI failure instead of silently only running one test.'],
['O','I','What does `npx playwright show-report` do?',null,['Opens the Playwright config in a browser editor','Serves the last HTML report locally and opens it in the browser','Shows the list of installed browsers','Generates a new report from the last run'],1,'After a test run, show-report serves the playwright-report/ directory so you can inspect results without a separate server.'],
['O','B','What is `testMatch` used for in playwright.config.ts?',null,['Matching test titles to run','A glob or regex pattern that overrides the default file discovery — only files matching the pattern are treated as test files','Matching locators in selectors','Filtering tests by tag'],1,'testMatch lets you change the default *.spec.ts / *.test.ts naming convention to any pattern that fits your project.'],
['O','I','What does `globalSetup` in playwright.config.ts point to?',null,['A function that runs before every test','A module that exports a function called once before the entire test suite starts — useful for starting servers or seeding databases','A shared beforeAll hook for all describe blocks','The auth setup project file'],1,'globalSetup runs once, in the main process, before any workers or tests start — ideal for expensive one-time preparation.'],
['O','A','What does `outputDir` in playwright.config.ts control?',null,['The folder where test reports are written','The directory where test artifacts (screenshots, videos, traces) are saved','The directory scanned for test files','The directory for browser downloads'],1,'outputDir (default: test-results/) stores all run artifacts — Playwright cleans it before each run by default.'],
['F','B','Complete to set the base URL for all tests.\n```ts\nexport default defineConfig({\n  use: {\n    ______: \'http://localhost:3000\',\n  },\n});\n```',null,['baseURL','url','host','origin'],0,'baseURL in the use block lets page.goto(\'/path\') resolve relative to that base.'],
['F','B','Complete to run tests in headed mode.\n```ts\nuse: {\n  ______: false,\n}\n```',null,['headless','headed','visible','show'],0,'headless: false disables headless mode, opening a visible browser window.'],
['F','I','Complete to set a per-test timeout of 45 seconds.\n```ts\nexport default defineConfig({\n  ______: 45_000,\n});\n```',null,['timeout','testTimeout','maxTime','duration'],0,'The top-level timeout option sets the maximum duration for each individual test.'],
['F','I','Complete the CLI flag to launch Playwright\'s interactive UI.\n```\nnpx playwright test ______\n```',null,['--ui','--debug','--headed','--interactive'],0,'--ui launches UI Mode — an interactive test runner with time-travel tracing.'],
['F','B','Complete to enable retries only in CI.\n```ts\nretries: process.env.CI ? 2 : ______,\n```',null,['0','1','undefined','false'],0,'0 retries locally means test failures are reported immediately without re-running.'],
['F','I','Complete to prevent committing test.only() to CI.\n```ts\nforbidOnly: !!process.env.______,\n```',null,['CI','NODE_ENV','TEST','PLAYWRIGHT'],0,'process.env.CI is set by virtually all CI providers (GitHub Actions, GitLab CI, CircleCI, etc.).'],
['F','I','Complete to specify the global setup module.\n```ts\nexport default defineConfig({\n  ______: \'./global-setup.ts\',\n});\n```',null,['globalSetup','setup','beforeAll','setupFile'],0,'globalSetup points to a module whose default export is called once before the suite starts.'],
['E','B','What is wrong?\n```ts\nimport { test } from \'playwright\';\ntest(\'my test\', async ({ page }) => {\n  await page.goto(\'https://example.com\');\n});\n```',null,['test must be imported from @playwright/browser','Import should be from @playwright/test, not playwright — the lower-level playwright package does not export test or expect','page fixture must be imported separately','async is not needed for page.goto'],1,'Always import test and expect from @playwright/test — that is the test runner package.'],
['E','B','What is wrong with this config?\n```ts\nexport default defineConfig({\n  use: { baseURL: \'http://localhost:3000\' },\n  timeout: \'30s\',  // string\n});\n```',null,['baseURL should not include a port','timeout must be a number in milliseconds, not a string like "30s"','defineConfig is not the correct function name','use block must come after timeout'],1,'timeout is always in milliseconds — use 30_000, not the string \'30s\'.'],
['E','I','What is wrong?\n```ts\n// playwright.config.ts\nexport default defineConfig({\n  projects: [\n    { name: \'chrome\', use: { browserName: \'google-chrome\' } },\n  ],\n});\n```',null,['projects must have at least two entries','browserName only accepts "chromium", "firefox", or "webkit" — not "google-chrome"','projects requires a testDir per entry','use must include viewport'],1,'Playwright\'s browserName option accepts the three engine names: chromium, firefox, webkit — not brand names like "google-chrome".'],
['E','I','What is wrong?\n```ts\n// global-setup.ts\nexport default async function globalSetup() {\n  await page.goto(\'/login\'); // page is not defined\n}\n```',null,['globalSetup cannot be async','globalSetup does not receive a page fixture — it only receives the FullConfig object. To interact with a browser, create a browser instance manually with chromium.launch()','globalSetup must export a named function','page must be imported from @playwright/test'],1,'globalSetup runs before any workers start — there is no page fixture. Use the chromium/firefox/webkit launchers directly to spin up a browser if needed.'],
['E','B','What is wrong?\n```\n# CI command\nnpx playwright test --headed\n```',null,['--headed is not a valid flag','Running headed in CI usually fails because CI environments have no display. Use headless mode (the default) or configure a virtual display (xvfb)','--headed requires --workers=1','CI requires --reporter=junit'],1,'Headed mode requires a graphical display — CI machines typically don\'t have one. Use the default headless mode or configure Xvfb.'],
['E','A','What is wrong?\n```ts\nexport default defineConfig({\n  globalSetup: \'./setup.ts\',\n  globalTeardown: \'./setup.ts\', // same file\n});\n```',null,['globalTeardown must be a different file','This is valid — a single file can export both a default (globalSetup) and a named globalTeardown export, but if the file only has a default export it will be used for setup only and teardown will be undefined','globalSetup and globalTeardown cannot share a module','The file must use .js extension'],1,'Using the same file is fine if it exports both functions correctly, but a common mistake is pointing both to a file with only a default export — teardown silently does nothing.'],
['E','I','What is wrong?\n```ts\nexport default defineConfig({\n  use: { video: true },\n});\n```',null,['video cannot be in the use block','video accepts a mode string, not a boolean: use "on", "off", "retain-on-failure", or "on-first-retry"','video must be set per-project, not globally','video: true requires trace: true'],1,'video expects a mode string — "on" records all tests, "retain-on-failure" only keeps recordings for failing tests.'],
['D','B','Tests fail with "browserType.launch: Executable doesn\'t exist" in CI. What is the fix?',null,['Add Playwright to devDependencies','Add `npx playwright install --with-deps` as a CI step before running tests — this downloads browser binaries and OS-level dependencies','Set executablePath in the config','Use Docker with playwright pre-installed'],1,'Browser binaries are not in node_modules — they must be installed separately with playwright install. --with-deps also installs OS libraries required by the browsers.'],
['D','B','A test passes with `timeout: 30000` locally but times out in CI. What should you try?',null,['Reduce assertions in the test','Increase timeout for CI: `timeout: process.env.CI ? 60000 : 30000` — CI machines are often slower and network latency to staging is higher','Use retries instead of a longer timeout','Switch to webkit which is faster'],1,'CI environments run on shared, sometimes slower hardware with network overhead — a longer timeout for CI is the pragmatic first step while investigating root causes.'],
['D','I','Running `npx playwright test` shows "No tests found" even though .spec.ts files exist. What should you check?',null,['Re-install the @playwright/test package','Check testDir in playwright.config.ts — if it points to the wrong directory, or testMatch does not match your file names, Playwright finds nothing','Rename files to .test.ts extension','Add --all flag to the command'],1,'The most common cause is a testDir mismatch or a custom testMatch that does not cover the actual file paths.'],
['D','I','The HTML report is not generated after a test run. What is the likely config issue?',null,['html reporter requires fullyParallel: true','The reporter is not configured — add reporter: "html" or reporter: [["html", { open: "never" }]] to playwright.config.ts','HTML report requires a web server running','reporter must be set per project'],1,'Without explicit reporter config, Playwright uses the "list" reporter which only outputs to the terminal.'],
['D','A','`globalSetup` throws but the error message is "globalSetup timed out". How do you debug it?',null,['Add retries: 3 to config','Wrap globalSetup logic in try/catch and log errors explicitly — Playwright\'s timeout wrapper swallows the original error. Also set a longer timeout with PLAYWRIGHT_GLOBAL_TIMEOUT env variable','Use PWDEBUG=1 with globalSetup','Switch globalSetup to a beforeAll hook'],1,'The timeout error masks the real cause — explicit error logging in globalSetup is essential to see what actually failed.'],
['D','I','Tests discover too many files including node_modules and dist. What config option fixes this?',null,['Add .playwrightignore file','Use testIgnore to exclude patterns: `testIgnore: ["**/node_modules/**", "**/dist/**"]`','Delete node_modules before running tests','Use testMatch to list only specific files'],1,'testIgnore accepts glob patterns — paths matching it are excluded from test discovery even if they match testDir.'],
['C','B','Complete to set the artifacts output folder.\n```ts\nexport default defineConfig({\n  ______: \'test-artifacts\',\n});\n```',null,['outputDir','artifactsDir','resultDir','reportDir'],0,'outputDir sets the directory for screenshots, videos, and traces produced during the run.'],
['C','I','Complete to configure slow motion between actions.\n```ts\nuse: {\n  ______: 500, // 500ms delay between actions\n}\n```',null,['slowMo','delay','actionDelay','waitTime'],0,'slowMo adds a fixed millisecond delay after every Playwright action — useful for visually observing test execution.'],
['C','I','Complete to set the viewport size globally.\n```ts\nuse: {\n  viewport: { width: ______, height: 768 },\n}\n```',null,['1280','800','375','1920'],0,'1280×768 is a common desktop viewport; use 375×812 for mobile simulation.'],
['C','B','Complete to point to the global teardown module.\n```ts\nexport default defineConfig({\n  globalTeardown: \'______\',\n});\n```',null,['./global-teardown.ts','./teardown','./cleanup','./after-all.ts'],0,'Any relative path to a module that exports a default async function works — the name is your convention.'],
['C','I','Complete to keep test artifacts only on failure.\n```ts\nuse: {\n  screenshot: \'______\',\n  video: \'retain-on-failure\',\n}\n```',null,['only-on-failure','on-failure','failure','always-on-failure'],0,'"only-on-failure" saves a screenshot only when a test fails — identical to "retain-on-failure" for video.'],
['L','B','Why does Playwright recommend keeping browser binaries out of node_modules?',null,['To keep node_modules small and portable — browser binaries are large OS-specific executables that change per Playwright version and belong in a separate cache','Browser binaries are not compatible with npm\'s module resolution','node_modules is read-only on most systems','npm cannot handle binary files'],0,'The separate cache (~/.cache/ms-playwright) allows multiple projects to share the same binaries and simplifies updates.'],
['L','I','What is the difference between `testMatch` and `testDir`?',null,['They are identical','testDir is the root directory to scan; testMatch is a glob/regex that filters which files within that directory are treated as tests — both are needed together','testMatch replaces testDir when set','testDir is for unit tests; testMatch is for e2e tests'],1,'testDir scopes the search; testMatch (default: **/*.spec.ts, **/*.test.ts) determines which found files are treated as test files.'],
['L','I','Why should `forbidOnly` always be `true` in CI but not locally?',null,['test.only() crashes local browsers','Locally test.only() is useful for focusing on one test during development; in CI it would silently skip all other tests — a merged .only() would mean only one test protects the entire codebase','forbidOnly slows down test discovery','test.only() is deprecated and forbidden everywhere'],1,'The goal is developer ergonomics locally + full coverage in CI — forbidOnly enforces that .only() never lands in the main branch.'],
['L','B','Why does Playwright default workers to half the CPU count rather than the full count?',null,['Playwright workers are single-threaded so more than half are wasted','Each Playwright worker also launches a browser process — using all cores would leave no headroom for browser rendering, causing slower overall throughput','Playwright\'s license limits the number of workers','Half the count is the minimum for cross-platform compatibility'],1,'Workers + their browsers compete for the same cores — half-core allocation gives each worker+browser pair one core, which is generally optimal.'],
['L','A','What happens if `globalSetup` throws an error?',null,['Tests still run but global setup is skipped','The entire test suite is aborted before any test runs — globalSetup failure is treated as a fatal error','Only tests in the first project are affected','globalSetup errors are caught and logged as warnings'],0,'If setup cannot complete, there is no point running tests that depend on it — Playwright aborts immediately with the error.'],
['R','B','Your dev server must be running before tests start. How do you configure this?',null,['Start it manually before running npx playwright test','Use the webServer option in playwright.config.ts: { command: "npm run dev", url: "http://localhost:3000" } — Playwright starts the server and waits for it to respond','Add a beforeAll hook in each test file','Use globalSetup to start the server'],1,'webServer is the dedicated option for this — it starts the process, waits for the URL to respond, and tears it down after the suite.'],
['R','I','You want to test both desktop (1280×800) and mobile (375×812) in one suite. How?',null,['Write two separate test suites','Add two projects: one with viewport: { width: 1280, height: 800 } and one with viewport: { width: 375, height: 812 } — Playwright runs all tests against both','Use test.use() in every test file','Pass --viewport to the CLI for each run'],1,'Projects are the right tool for dimension variants — each test runs twice, once per project, with no duplication.'],
['R','I','A test file is growing unwieldy. How do you organize tests into named groups without splitting files?',null,['Use test.group() (a Playwright built-in)','Use test.describe("Group Name", () => { ... }) to nest tests under a logical label — groups appear as collapsible sections in the HTML report','Create subdirectories and one file per test','Use // @group annotations in comments'],1,'test.describe() creates hierarchical grouping — reports show "GroupName > testTitle" making navigation easy.'],
['R','A','Your project has both API tests and browser tests. How do you give them different timeouts?',null,['API tests must use the global timeout','Create two projects — one for browser tests (timeout: 30000) and one for API tests (timeout: 10000) — each project can override timeout in its use block','Use test.setTimeout() in every API test file','Set timeout dynamically inside each test'],1,'Per-project timeout configuration is the scalable approach — API tests typically need less time than browser tests.'],
['R','B','You want to run a specific file without running the full suite. What is the command?',null,['npx playwright test --only tests/login.spec.ts','npx playwright test tests/login.spec.ts','npx playwright test --file=login','npx playwright test --include login'],1,'Passing a file path directly to the CLI restricts the run to that file — you can also pass a directory.'],
['S','B','A teammate checked in a test.only() and CI is passing but all other tests were skipped. How do you prevent this?',null,['Add a git hook that blocks commits with test.only','Set forbidOnly: !!process.env.CI in playwright.config.ts — this fails the CI run immediately if any test.only() is found, forcing the developer to remove it before merging','Use --grep to exclude .only tests','Disable test.only() in eslint'],1,'forbidOnly is the purpose-built solution — zero-config once added, prevents the problem at CI gate time.'],
['S','I','Your Playwright tests are flaky in CI but reliable locally. List two configuration improvements to try first.',null,['Rewrite tests and add retries','1) Add retries: process.env.CI ? 2 : 0 for automatic re-runs of flaky tests; 2) Add trace: "on-first-retry" so you get a trace for the first retry and can diagnose the root cause in the Trace Viewer','Switch to a different browser engine','Increase workers to reduce total run time'],1,'Retries buy tolerance while trace captures the evidence — together they handle short-term flakiness while enabling long-term investigation.'],
['S','A','You are setting up Playwright in a monorepo where the app lives in packages/webapp. How do you configure testDir and webServer together?',null,['You must run Playwright from the webapp directory','Set testDir: "packages/webapp/tests" and webServer.command: "npm run dev --workspace=packages/webapp" — Playwright resolves paths relative to the config file location','Monorepos require a separate playwright.config.ts per package','testDir cannot be outside the current directory'],0,'Playwright resolves all relative paths from the config file — point testDir and webServer at the correct sub-package paths.'],
['S','I','A new team member runs the tests and gets a different number of tests than the rest of the team. What two config areas should you check?',null,['Check their Node.js version','1) Check testDir and testMatch — they may have a different playwright.config.ts or a local override. 2) Check if any test.only() is active in their local branch — that would limit which tests run','Check their browser version','Ask them to reinstall node_modules'],1,'testDir/testMatch discrepancy and accidental test.only() are the two most common causes of differing test counts between developers.'],
],

// ── Stubs for concepts 2–15 (filled progressively) ────────────
// ── 2. Writing Your First Test ────────────────────────────────
firsttest: [
// O×12 ─────────────────────────────────────────────────────────
['O','B','What does `test(\'title\', async ({ page }) => {})` define in Playwright?',null,['A browser launch configuration','A single test with a page fixture','A describe group of tests','A global setup function'],1,'Each call to `test()` registers one test. The string is its title shown in reports; the async callback receives fixtures like `page`.'],
['O','B','Which module do `test` and `expect` come from in Playwright?',null,['playwright','@playwright/browser','@playwright/test','playwright-test'],2,'`@playwright/test` is the test-runner package. The lower-level `playwright` package exists but does not export `test` or `expect`.'],
['O','B','What does `test.describe(\'Group\', () => { … })` do?',null,['Downloads a browser group','Groups related tests together under a shared label','Runs tests only for the described browser','Sets a timeout for a group of tests'],1,'`test.describe()` creates a named group. It scopes `beforeEach`/`afterEach` hooks and organises the HTML report.'],
['O','I','What does `test.only(\'…\', async ({ page }) => {})` do when it appears in a test file?',null,['Runs that test with elevated browser permissions','Runs only that test in the file, skipping all others','Marks the test as the first to run globally','Forces the test to run in a single worker'],1,'`test.only()` filters the run to just that test within its file. Other files are unaffected.'],
['O','I','What does `test.skip(\'…\', async ({ page }) => {})` tell Playwright?',null,['Delete the test after it runs','Mark the test as skipped — do not execute it','Run the test but ignore its result','Retry the test automatically on failure'],1,'`test.skip()` excludes the test from execution. It still appears in the report, marked as skipped.'],
['O','B','What is `page` in the callback `async ({ page }) => {}`?',null,['A global singleton shared across all tests','A built-in fixture providing a fresh browser page for each test','A reference to the browser process','A DOM element handle'],1,'`page` is Playwright\'s most-used built-in fixture. It is created fresh before each test and disposed automatically after.'],
['O','I','What does `test.fixme(\'…\', async ({ page }) => {})` indicate?',null,['A test that is intentionally slow','A known broken test that should be skipped for now','A test that requires manual verification','A test that always passes'],1,'`test.fixme()` is the idiomatic way to flag a known failure. It skips execution and shows up distinctly in the report.'],
['O','B','Why must every Playwright action inside a test be prefixed with `await`?',null,['To enable TypeScript type checking','Because all browser operations return Promises','To activate Playwright Inspector','Because Playwright uses worker threads'],1,'Navigation, clicks, fills, and assertions all return Promises. Without `await` the test continues before the browser does anything — causing flakiness.'],
['O','I','What happens when you pass `{ timeout: 60_000 }` as the second argument to `test()`?',null,['Sets a 60-second delay before the test starts','Overrides the timeout for that specific test to 60 seconds','Sets the global config timeout','Adds 60 seconds to every assertion timeout'],1,'A per-test options object as the second argument lets you override `timeout`, `annotation`, and `tag` for that single test only.'],
['O','B','Which file extension does Playwright\'s default `testMatch` pattern look for?',null,['.test.ts only','.spec.ts (and .spec.js)','any .ts file','playwright.ts only'],1,'The default glob is `**/*.spec.{ts,js}`. Files named `.test.ts` are not discovered unless you extend `testMatch`.'],
['O','I','What does `await expect(page).toHaveTitle(/Playwright/)` assert?',null,['The page URL contains "Playwright"','The page title matches the regex /Playwright/','A heading element contains "Playwright"','The page was built with Playwright'],1,'`toHaveTitle()` auto-retries until the document `<title>` matches the string or regex, then passes — or times out and fails.'],
['O','A','If `test.describe.only()` wraps one describe block in a file, what happens to tests outside it?',null,['They run before the .only block','They are skipped for this file','They run in a separate worker','They are permanently deleted'],1,'`describe.only()` restricts execution to that describe block within the file. Tests outside it — even in the same file — are skipped.'],

// F×7 ─────────────────────────────────────────────────────────
['F','B','Fill in the blank: `import { test, ______ } from \'@playwright/test\';`',null,['expect','assert','check','verify'],0,'`expect` is Playwright\'s assertion function. It wraps a value or locator and exposes auto-retrying matchers like `.toBeVisible()`.'],
['F','B','Fill in the keyword: `test(\'login\', ______ ({ page }) => {`',null,['async','await','function','promise'],0,'The test callback must be `async` so that `await` can be used for every browser operation inside it.'],
['F','I','Fill in the modifier: `test.______( \'skipped for now\', async ({ page }) => { … })`',null,['skip','only','fixme','ignore'],0,'`test.skip()` excludes the test from running while keeping it visible in the report as "skipped".'],
['F','B','Complete the describe wrapper: `test.describe(\'Cart\', ______ => {`',null,['()','async ()','function()','async function()'],0,'`test.describe()` takes a synchronous callback (no `async`). Marking it `async` causes a Playwright configuration error.'],
['F','I','Fill in the timeout key: `test(\'slow upload\', { ______: 120_000 }, async ({ page }) => { … })`',null,['timeout','time','wait','delay'],0,'The per-test options object accepts `timeout` (ms), `annotation`, and `tag`. `{ timeout: 120_000 }` gives that test 2 minutes.'],
['F','B','Complete the assertion: `await expect(______).toHaveTitle(/Home/)`',null,['page','document','window','browser'],0,'`expect(page)` targets the whole page. `toHaveTitle()` checks the `<title>` element and auto-retries until it matches.'],
['F','A','Fill in the modifier for a known failing test: `test.______( \'broken feature\', async ({ page }) => { … })`',null,['fixme','broken','pending','xfail'],0,'`test.fixme()` is the Playwright convention for known failures. If a fixme test starts passing, Playwright reports it as unexpected — a reminder to remove the marker.'],

// E×6 ─────────────────────────────────────────────────────────
['E','B','What is wrong with this test?\n`test(\'check title\', (page) => { page.goto(\'/\') })`',null,['The callback is missing `async` and actions are missing `await`','`page` must be destructured from an object: `({ page })`','The test title uses single quotes','goto() does not exist on page'],1,'Two errors: (1) the callback must be `async`, and (2) every action needs `await`. Also `page` should be `{ page }` — but the most critical missing parts are `async` and `await`.'],
['E','B','What is wrong with this import?\n`import { test, expect } from \'playwright\';`',null,['Should import from \'@playwright/test\', not \'playwright\'','`expect` cannot be imported alongside `test`','Curly braces are invalid for named imports','Should use `require()` instead of `import`'],0,'The test-runner API lives in `@playwright/test`. The `playwright` package is a lower-level library that does not export `test` or `expect`.'],
['E','I','Spot the error:\n```\ntest.describe(\'Login\', async () => {\n  test(\'form loads\', async ({ page }) => { … })\n})\n```',null,['The describe callback must be synchronous, not async','`test` cannot be nested inside `describe`','`async` is required for describe','describe must have at least two tests'],0,'`test.describe()` callbacks must be plain synchronous functions. Marking them `async` causes a Playwright runtime error because it changes how Playwright collects tests.'],
['E','I','What is wrong here?\n```\ntest(\'check UI\', async ({ page }) => {\n  page.goto(\'http://example.com\');\n  expect(page).toHaveTitle(\'Example\');\n})\n```',null,['Both `page.goto()` and `expect()` are missing `await`','`expect` should wrap a locator, not `page`','`goto` needs a relative URL only','The test has no assertion'],0,'Without `await`, `page.goto()` is a floating Promise and the assertion runs immediately on an unloaded page. Always prefix both with `await`.'],
['E','A','Spot the error:\n```\ntest(\'modal\', async ({ page }) => {\n  await page.goto(\'/\');\n  const btn = page.getByRole(\'button\');\n  btn.click();\n  await expect(btn).toBeHidden();\n})\n```',null,['`btn.click()` is missing `await`','`getByRole` cannot target buttons','`toBeHidden` is not a valid matcher','The locator must be re-queried after click'],0,'`btn.click()` returns a Promise. Without `await`, the click is dispatched asynchronously and the assertion runs before the button state changes.'],
['E','I','What is wrong with this test file?\n```\ntest(\'A\', async ({ page }) => { … })\ntest.only(\'B\', async ({ page }) => { … })\ntest(\'C\', async ({ page }) => { … })\n```\nAll three tests must run in CI.',null,['`test.only()` will cause A and C to be skipped — remove it before committing','`test` and `test.only` cannot coexist in one file','Tests must all use .only or none can','C must come before A for .only to work'],0,'`test.only()` scopes execution to test B only in this file. Tests A and C are skipped. Development modifiers like `.only` must always be removed before merging.'],

// D×6 ─────────────────────────────────────────────────────────
['D','I','A developer wants to run only one test temporarily. Instead of `test.only()` what CLI alternative exists?',null,['`npx playwright test --grep "exact title"`','`npx playwright test --only "exact title"`','`npx playwright test --single "exact title"`','`npx playwright test --focus "exact title"`'],0,'`--grep` accepts a regex matched against test titles, letting you isolate tests without modifying source code.'],
['D','B','Tests are flaky — sometimes they pass, sometimes fail. The logs show "ElementHandle is closed". What is the likely root cause?',null,['Missing `await` on an action, causing the next step to run on a disposed element','The browser was not installed correctly','The test timeout is too short','The assertion is using the wrong matcher'],0,'A missing `await` means Playwright moves to the next line while the previous action is still pending. The element reference may become stale or the page may navigate away.'],
['D','A','`test.fixme()` was added to a test last sprint. This sprint the underlying bug was fixed and the test now passes. What does Playwright report?',null,['A failure — a fixme test that passes is flagged as "should be marked passing"','A success — the test passes normally','A warning — the test is still marked fixme','A skip — fixme always skips regardless'],0,'Playwright deliberately fails a `.fixme()` test that passes. This forces the developer to notice and remove the `.fixme()` marker.'],
['D','B','A team\'s CI skips 40 tests even though no `test.skip()` was added. Investigation shows `test.only()` was left in a spec file. What is the fix?',null,['Remove `test.only()` from the spec file before merging','Add `test.only()` to all other tests','Pass `--ignore-only` flag in CI','Add skipOnly: false to playwright.config.ts'],0,'A single `test.only()` anywhere causes all other tests in that file to be skipped. The fix is always to remove it before committing.'],
['D','I','A test navigates to `/` in `beforeEach`, but one specific test needs to start at `/admin`. How should the developer handle this?',null,['Call `await page.goto(\'/admin\')` inside that specific test — it overrides beforeEach\'s navigation','Duplicate the beforeEach inside the test','Use test.skip() on the beforeEach','Create a separate describe block with a different beforeEach'],0,'Navigating again inside the test simply replaces the beforeEach navigation. Playwright uses whichever `goto` ran last.'],
['D','A','A developer uses `const title = page.title()` and then `expect(title).toBe(\'Home\')`. The test always fails even though the title is correct. Why?',null,['`page.title()` returns a Promise, not a string — compare a Promise object to a string always fails','`toBe` does not work with strings','`page.title()` is not a valid Playwright method','`expect` requires `await` before it'],0,'`page.title()` is async and returns a Promise. Comparing a Promise with `toBe(\'Home\')` compares an object, not the string. Use `await expect(page).toHaveTitle(\'Home\')` instead.'],

// C×5 ─────────────────────────────────────────────────────────
['C','B','Complete the test signature: `test(\'login\', async ({ ______ }) => {`',null,['page','browser','context','playwright'],0,'`page` is the most common fixture. Playwright provisions a fresh `Page` object before each test and tears it down after.'],
['C','I','Complete the grouping call: `test.______( \'User flows\', () => {`',null,['describe','group','suite','section'],0,'`test.describe()` creates a named block that groups tests, scopes hooks, and labels the report section.'],
['C','I','Complete the marker for a known failing test: `test.______( \'broken search\', async ({ page }) => { … })`',null,['fixme','skip','broken','pending'],0,'`test.fixme()` is the idiomatic known-failure marker. Unlike `test.skip()`, a fixme that starts passing is flagged as unexpected.'],
['C','I','Complete the beforeEach fixture: `test.beforeEach(async ({ ______ }) => { await page.goto(\'/\') })`',null,['page','browser','context','request'],0,'`beforeEach` receives the same fixtures as `test`. Destructure `page` to run navigation or other setup before each test in the group.'],
['C','A','Complete the per-test annotation: `test(\'bug\', { ______: { type: \'issue\', description: \'url\' } }, async ({ page }) => { … })`',null,['annotation','tag','label','meta'],0,'The `annotation` option links a test to an external issue URL. The annotation appears in the HTML report, making it easy to trace failures to tickets.'],

// L×5 ─────────────────────────────────────────────────────────
['L','B','If `test.only()` is used in `file-a.spec.ts`, what happens to tests in `file-b.spec.ts`?',null,['They are unaffected and run normally','They are all skipped','They run before file-a','They run only if file-b also has test.only'],0,'`test.only()` is file-scoped. Tests in other files continue to run normally regardless of `.only` in a different file.'],
['L','I','What is the standard lifecycle order within a `test.describe` block?',null,['beforeAll → beforeEach → test → afterEach → afterAll','beforeEach → beforeAll → test → afterAll → afterEach','beforeAll → test → beforeEach → afterEach → afterAll','test → beforeEach → afterEach → beforeAll → afterAll'],0,'Hooks run in this exact order: `beforeAll` once, then for each test: `beforeEach` → test body → `afterEach`, and finally `afterAll` once at the end.'],
['L','I','Can `test.describe()` be nested inside another `test.describe()`?',null,['Yes — nested describes create sub-groups with independently scoped hooks','No — describe cannot be nested','Yes — but only one level deep','No — use test.group() for nesting'],0,'Nesting `test.describe()` creates hierarchical sub-groups. Hooks in an inner describe only apply to tests within that inner block.'],
['L','A','A `test.fixme()` test starts passing after a bug fix. What does Playwright report?',null,['A failure — fixme tests that pass are flagged as unexpectedly passing','A success — the test passes','A warning but the suite still passes','A skip — fixme always skips'],0,'Playwright intentionally fails a `.fixme()` test that passes, prompting the developer to remove the marker. This prevents forgotten fixme annotations from accumulating.'],
['L','I','Does Playwright automatically clean up the `page` fixture between tests?',null,['Yes — page is created before each test and closed after automatically','No — you must call page.close() at the end of each test','Yes — but only if afterEach calls page.reset()','No — page persists across the entire test file'],0,'`page` is a test-scoped fixture. Playwright creates a fresh page before each test and destroys it afterwards — no manual cleanup needed.'],

// R×5 ─────────────────────────────────────────────────────────
['R','B','How do you tag a test so it can be filtered with `--grep "@smoke"` from the CLI?',null,['Include @smoke in the test title string','Add a tags: [\'@smoke\'] property to the test config','Create a file named smoke.spec.ts','Pass --tag flag with the test name'],0,'Playwright\'s `--grep` matches against test titles. Embedding `@smoke` in the title is the standard tagging convention.'],
['R','I','Your team has 60 tests across 6 spec files. How should you organise them for maintainability?',null,['One spec file per feature/page, using test.describe to sub-group','One giant spec file with all tests','Separate folders for each test individually','One spec file per developer'],0,'The conventional structure is one spec file per feature or page, with `test.describe()` blocks for sub-scenarios. This keeps reports readable and failures easy to locate.'],
['R','I','A test needs 90 seconds to complete a large file upload. The global timeout is 30 seconds. What is the least invasive fix?',null,['Pass `{ timeout: 90_000 }` as the second argument to that specific `test()` call','Raise the global timeout in playwright.config.ts','Split the test into three smaller tests','Use a mock to skip the real upload'],0,'A per-test `timeout` override is the least invasive — it affects only that test without raising the global ceiling for all tests.'],
['R','A','You want to run only @critical tests in CI and all tests locally. How do you configure this without editing test files?',null,['Add `--grep "@critical"` to the CI command in your workflow YAML','Add test.only() to @critical tests','Create a separate playwright.config.ci.ts','Use environment variables in beforeEach'],0,'Keep the `--grep` flag in the CI workflow (e.g. GitHub Actions `run:` step). Local runs without `--grep` run all tests.'],
['R','I','A new developer asks why you do not use `console.assert()` for assertions in Playwright tests. What is the key reason?',null,['`expect()` assertions auto-retry until the condition is met or the timeout expires; console.assert does not','console.assert is not available in browsers','Playwright blocks native console methods','expect() is faster'],0,'Playwright\'s `expect()` assertions poll the DOM asynchronously, retrying on failure. `console.assert()` checks only once and does not understand async browser state.'],

// S×4 ─────────────────────────────────────────────────────────
['S','I','A developer adds `test.only()` to one test, pushes to CI, and 49 other tests in the file are skipped. What is the correct remediation?',null,['Remove `test.only()` and replace with the regular `test()` before merging','Add `test.only()` to the other 49 tests','Pass `--run-all` flag in CI','Set `ignoreOnly: true` in playwright.config.ts'],0,'`ignoreOnly: true` in config makes Playwright ignore `.only()` modifiers — useful as a CI safeguard. But the real fix is removing it before committing.'],
['S','A','A `beforeEach` navigates to `/login`. One test must start at `/admin` instead. What is the correct approach?',null,['Call `await page.goto(\'/admin\')` inside that test — it replaces the beforeEach navigation','Use test.skip() for the beforeEach in that test','Override beforeEach with a test-level beforeEach','Create a separate describe block with no beforeEach'],0,'Calling `goto()` again inside the test simply replaces the previous navigation. Playwright does not lock the page to the beforeEach URL.'],
['S','I','A developer wrote `expect(page.getByText(\'Submit\')).toBeVisible()` without `await`. The test always passes even when the button is absent. Why?',null,['Without `await`, the assertion returns a Promise that is never settled — Playwright treats an unresolved Promise as passing','expect() works synchronously when called without await','toBeVisible() defaults to true without await','Playwright ignores assertions without await'],0,'An unawaited `expect()` call creates a floating Promise. The test function exits (passes) before the assertion resolves — the failure is silently swallowed.'],
['S','A','A team has 5 spec files, each with 10 tests. A developer wraps 3 tests in `test.describe.only()` in one file. How many tests run in total?',null,['3 — only the tests in the .only describe block in that file; other files are unaffected','3 — only the tests in that file, all others globally skipped','50 — describe.only has no effect','13 — 3 from the .only block plus 10 from each other file'],0,'`describe.only()` is file-scoped: the 3 tests inside it run, the other 7 tests in that file are skipped, and all 40 tests in the other 4 files run normally.'],

// ── Additional batch ×50 ─────────────────────────────────────
['O','B','What does `test.skip()` do when called inside a test body?',null,['Marks the test as failed','Skips the rest of the test body and marks it as skipped in the report','Skips all remaining tests in the file','Skips the test only if a condition is passed'],1,'test.skip() inside the body stops execution at that point and marks the test skipped — useful for conditional skips based on runtime state.'],
['O','B','What does `test.slow()` do?',null,['Adds artificial delays between actions','Triples the test\'s timeout — useful for tests that legitimately take longer','Enables slow-motion for the browser','Reduces worker priority for that test'],1,'test.slow() multiplies the configured timeout by 3, signalling that the test is intentionally slow without changing global settings.'],
['O','I','What is `testInfo` in Playwright?',null,['The playwright.config.ts object passed to globalSetup','An object available in every test via the second argument, containing test title, status, output path, annotations, and more','A global singleton holding suite metadata','The return value of page.evaluate()'],1,'testInfo gives each test access to its own metadata — attach files, read the test title, check retry count, and write to testInfo.outputDir for artifacts.'],
['O','B','What does `test.describe.configure({ mode: "serial" })` do?',null,['Configures the describe block to run tests in alphabetical order','Forces all tests in the describe block to run sequentially on one worker','Enables serial network request logging','Disables parallelism for the entire suite'],1,'describe.configure is the newer API for controlling execution mode — equivalent to test.describe.serial() but more explicit.'],
['O','I','What does `test.step("name", async () => {})` provide?',null,['A nested describe group with its own hooks','A labelled sub-step visible in the Trace Viewer and HTML report — useful for grouping related actions within one test','A synchronous block inside an async test','A checkpoint that can be retried independently'],1,'test.step creates named sub-steps in the trace and report, making long tests easier to navigate visually.'],
['O','B','What is the result of calling `expect(value).not.toBe(5)`?',null,['Passes if value strictly equals 5','Passes if value does not strictly equal 5','Throws immediately without polling','Negates only the type check'],1,'.not inverts any matcher — expect(x).not.toBe(5) passes when x !== 5.'],
['O','I','What does `test.beforeAll` vs `test.beforeEach` do differently?',null,['beforeAll runs before each test; beforeEach runs before the first test only','beforeAll runs once before all tests in the describe scope; beforeEach runs before every single test in the scope','They are identical','beforeAll is for page fixtures; beforeEach is for context fixtures'],1,'beforeAll is once-per-scope setup; beforeEach is per-test setup — wrong choice causes either repeated expensive setup or stale state.'],
['O','I','When does Playwright retry a failing test with `retries: 2`?',null,['Immediately if any assertion fails','After the test function throws or an assertion rejects — the whole test runs again from the start, up to 2 additional times','Only if the failure is a timeout','Only on the second run in CI'],1,'Each retry re-runs the complete test function in a fresh browser context — there is no partial retry.'],
['O','B','What does `expect.soft()` do differently from `expect()`?',null,['It is faster because it does not auto-retry','A soft assertion failure does not stop the test — execution continues and all soft failures are reported at the end','It only works on primitive values','It disables the web-first retry logic'],1,'Soft assertions let you collect multiple failures in one test run without stopping at the first — useful for form validation tests.'],
['O','I','What is the purpose of `test.info().attach("name", { body, contentType })`?',null,['Adds a file to node_modules for the test','Attaches an artifact (screenshot, JSON, log) to the test\'s HTML report entry','Sends the body to a remote reporter','Annotates the test title in the report'],1,'attach() adds downloadable artifacts to the test report — helpful for debugging failures with custom data.'],
['O','A','What does `test.fail()` do when called at the top of a test?',null,['Immediately fails the test and stops execution','Marks the test as expected to fail — if it passes, Playwright reports it as unexpectedly passing; if it fails, it is reported as expected failure (xfail)','Disables retries for the test','Makes the test a no-op'],1,'test.fail() is the xfail mechanism — document known broken tests without hiding them, and get alerted when they unexpectedly start passing.'],
['O','I','What does `test.fixme(condition, "reason")` do when condition is false?',null,['The test is always skipped','When condition is false, the marker has no effect — the test runs normally; when true, it is skipped as fixme','The reason string is logged regardless','fixme requires a non-conditional call'],1,'Conditional fixme lets you skip tests only in specific environments (e.g. only on Windows) without separate if-blocks.'],
['F','B','Complete to add a per-test timeout override.\n```ts\ntest(\'slow upload\', async ({ page }) => {\n  // ...\n}, { ______: 90_000 });\n```',null,['timeout','maxTime','duration','wait'],0,'The third argument to test() is an options object — timeout overrides the global per-test limit for this one test.'],
['F','B','Complete to skip a test conditionally.\n```ts\ntest(\'windows only\', async ({ page }) => {\n  test.______(process.platform !== \'win32\', \'Only runs on Windows\');\n  // ...\n});\n```',null,['skip','ignore','cancel','fixme'],0,'test.skip(condition, reason) skips the test when condition is true — execution of the test body stops at that line.'],
['F','I','Complete to attach a file to the report.\n```ts\nawait test.info().______("screenshot", {\n  body: await page.screenshot(),\n  contentType: "image/png",\n});\n```',null,['attach','addFile','saveArtifact','upload'],0,'attach() links the binary body to the test in the HTML report as a downloadable artifact.'],
['F','B','Complete to name a sub-step in the trace.\n```ts\nawait test.______("fill login form", async () => {\n  await page.getByLabel("Email").fill("user@test.com");\n  await page.getByLabel("Password").fill("secret");\n});\n```',null,['step','describe','group','action'],0,'test.step() creates a named section in the trace viewer and HTML report.'],
['F','I','Complete to access the current test title inside a test.\n```ts\nconst title = test.______.title;\n```',null,['info()','config','context','meta'],0,'test.info() returns the TestInfo object for the currently running test — title, status, outputDir, etc.'],
['F','I','Complete to mark a test as expected to fail.\n```ts\ntest(\'known bug\', async ({ page }) => {\n  test.______();\n  // ...\n});\n```',null,['fail','xfail','broken','skip'],0,'test.fail() marks the test as expected to fail — it flips the pass/fail signal.'],
['F','B','Complete to apply a hook to the whole file\'s tests.\n```ts\ntest.______(() => {\n  console.log(\'runs before every test in this file\');\n});\n```',null,['beforeEach','beforeAll','setup','before'],0,'test.beforeEach() without a wrapping describe applies to all tests in the file.'],
['E','B','What is wrong?\n```ts\ntest(\'check title\', async ({ page }) => {\n  await page.goto(\'https://example.com\');\n  expect(page.title()).toBe(\'Example Domain\'); // missing await\n});\n```',null,['page.title() must use getByTitle() instead','page.title() returns a Promise — without await the Promise object is compared to the string, always failing or incorrectly passing','toBe() handles Promises automatically','title() is deprecated — use page.locator("title")'],1,'Always await page.title() — it is async. Unawaited Promises compared with toBe() always fail the strict equality check.'],
['E','B','What is wrong?\n```ts\ntest.beforeAll(async ({ page }) => {\n  await page.goto(\'/login\');\n});\n```',null,['beforeAll cannot be async','page is a test-scoped fixture and cannot be used in beforeAll — beforeAll receives worker-scoped fixtures only. Use a context or browser fixture instead','beforeAll must be inside a describe block','goto() is not valid in beforeAll'],1,'page is re-created per test; beforeAll runs once per describe/file scope — using a test-scoped fixture there is a lifecycle mismatch.'],
['E','I','What is wrong?\n```ts\ntest(\'soft check\', async ({ page }) => {\n  expect.soft(await page.textContent(\'h1\')).toBe(\'Home\');\n  expect.soft(await page.textContent(\'h2\')).toBe(\'Welcome\');\n  // no final check\n});\n```',null,['expect.soft() cannot be used twice in one test','Soft assertion failures accumulate but are only reported if the test itself completes — without a final expect(page) or asserting that there are no soft failures, the soft failures may be silently ignored if no hard assertion also runs','textContent() must be awaited inside expect','expect.soft() requires the test to be marked test.fail()'],1,'After soft assertions, call expect(page).not.toBeNull() or any hard assertion to ensure the test body finishes and soft failures are flushed to the report.'],
['E','I','What is wrong?\n```ts\ntest.afterEach(async ({ page }) => {\n  await page.close();\n});\n```',null,['afterEach cannot be async','page is closed automatically after each test by the fixture system — calling page.close() manually again causes a "Target closed" error on the next test\'s page operations','afterEach must use context.close() instead','Closing page in afterEach is required for cleanup'],1,'Never close page manually — Playwright\'s test-scoped fixture closes it automatically. Double-closing causes errors.'],
['E','A','What is wrong?\n```ts\ntest(\'check values\', async ({ page }) => {\n  const price = await page.textContent(\'.price\');\n  const stock = await page.textContent(\'.stock\');\n  expect(Number(price)).toBeGreaterThan(0);\n  expect(stock).not.toBeNull();\n});\n// Test passes despite .price showing "$0.00"\n```',null,['textContent() returns raw text including "$" — Number("$0.00") is NaN, and expect(NaN).toBeGreaterThan(0) unexpectedly passes in some assertion libraries','toBeGreaterThan cannot handle NaN','Number() strips currency symbols automatically','This test would correctly fail'],0,'Number("$0.00") is NaN. NaN comparisons are tricky — strip the "$" before converting: Number(price!.replace("$","")).'],
['E','B','What is wrong?\n```ts\ntest(\'a\', () => { // no async\n  page.goto(\'https://example.com\');\n});\n```',null,['test() callbacks must be synchronous','The callback is not async and there is no page fixture in scope — page comes from the fixture argument `{ page }`, and goto() must be awaited','test() cannot take an arrow function','page is a global variable'],1,'Always destructure page from the fixture parameter `async ({ page }) =>` and await every Playwright call.'],
['D','B','A test times out every time even though the page loads in 2 seconds. The global timeout is 5 seconds. What should you check?',null,['Restart the test runner','Look for a missing await on an async action — an unawaited goto() or fill() returns immediately, but a subsequent assertion may time out waiting for state that was never actually achieved','Increase timeout to 30 seconds','Switch to a faster browser'],1,'Missing await is the most common cause of "phantom" timeouts — the action appears instant but leaves the browser in an unexpected state.'],
['D','I','`test.beforeAll` runs but its changes are not visible in the tests. Why?',null,['beforeAll changes are garbage collected','If beforeAll modifies a page fixture and tests use a fresh page, the changes are lost — beforeAll runs once and tests each get their own page. Use a worker-scoped context fixture to persist state across tests in the scope','beforeAll is asynchronous and tests start before it finishes','Tests run in a different worker from beforeAll'],1,'Test-scoped fixtures like page are reset per test — state from beforeAll only persists if stored in a worker-scoped fixture or a shared variable.'],
['D','B','A test assertion fails but the failure message shows a cryptic Promise object instead of a value. What is the cause?',null,['The assertion library has a bug','An async Playwright API (like page.textContent()) was not awaited — the Promise was passed directly to expect() instead of the resolved value','expect() does not support async values','The test needs a longer timeout'],1,'Always await Playwright APIs before passing their results to expect() — unawaited async calls produce Promise objects, not values.'],
['D','I','Tests fail with "Target page, context or browser has been closed" inside beforeEach. What is the likely cause?',null,['beforeEach always closes the browser','A previous afterEach or afterAll unexpectedly closed the context or browser — check teardown hooks for premature close() calls','beforeEach creates a new page automatically','The page fixture must be re-opened in beforeEach'],1,'Premature context/browser close in a teardown hook destroys the fixtures before subsequent tests can use them.'],
['D','A','A test file has both `test.describe.serial()` and `test.describe.parallel()` at the top level. Which tests run first?',null,['Parallel tests always run before serial','Playwright schedules available workers greedily — serial blocks run on one worker in order; parallel blocks distribute across workers concurrently. The wall-clock ordering depends on worker availability, not file order','Serial always runs last','File order determines execution order'],1,'Understanding worker scheduling matters for debugging interleaving — serial is guaranteed sequential, parallel is concurrent, but cross-block ordering is not guaranteed.'],
['C','B','Complete to mark a test as a known broken test.\n```ts\ntest(\'broken feature\', async ({ page }) => {\n  test.______();\n  await page.goto(\'/broken\');\n});\n```',null,['fixme','skip','fail','pending'],0,'test.fixme() marks the test as expected to be fixed later — it is skipped with a "fixme" annotation in the report.'],
['C','I','Complete to triple the timeout for a slow test.\n```ts\ntest(\'import csv\', async ({ page }) => {\n  test.______();\n  // long operation\n});\n```',null,['slow()','triple()','longTimeout()','extend()'],0,'test.slow() multiplies the configured timeout by 3 — no magic numbers needed.'],
['C','B','Complete the afterAll hook.\n```ts\ntest.______(() => {\n  db.disconnect();\n});\n```',null,['afterAll','afterEach','teardown','cleanup'],0,'test.afterAll() runs once after all tests in its scope complete.'],
['C','I','Complete to annotate a test with a bug tracker link.\n```ts\ntest(\'checkout bug\', async ({ page }) => {\n  test.______({ type: \'issue\', description: \'https://github.com/org/repo/issues/42\' });\n});\n```',null,['info().annotations.push','annotate','tag','link'],0,'test.info().annotations.push({ type, description }) adds a clickable annotation to the HTML report entry.'],
['C','I','Complete to run only this describe block and skip others in the file.\n```ts\ntest.describe.______("focused group", () => {\n  test("a", …);\n});\n```',null,['only','serial','parallel','focus'],0,'describe.only() restricts the run to that block within the file (subject to forbidOnly in CI).'],
['L','B','Why must every test callback be declared `async`?',null,['Playwright requires async for TypeScript compatibility','Playwright actions (goto, fill, click) return Promises — without async/await they execute out of order or their results are never awaited','async enables the fixture injection system','Playwright uses async functions to enable retries'],1,'All Playwright browser interactions are async — without async/await the test finishes before the browser actions complete.'],
['L','I','What is the execution order of hooks in nested describes?\n```ts\ndescribe("outer", () => {\n  beforeEach(outerBefore)\n  describe("inner", () => {\n    beforeEach(innerBefore)\n    test("t", ...) // order?\n  })\n})\n```',null,['innerBefore → outerBefore → t','outerBefore → innerBefore → t','t → outerBefore → innerBefore','Both beforeEach run in parallel before t'],1,'Hooks run from outermost to innermost — outerBefore sets up context, innerBefore refines it, then the test runs.'],
['L','I','Does `test.afterAll` receive the `page` fixture?',null,['Yes — afterAll receives all fixtures that the tests used','No — afterAll receives only worker-scoped fixtures, not test-scoped fixtures like page','Yes, but page is already closed','No — afterAll receives no fixtures at all'],1,'page is test-scoped and destroyed after each test — it does not exist in afterAll. Use worker-scoped fixtures like browser or context for afterAll teardown.'],
['L','B','If `retries: 2` is set and a test fails on the first attempt then passes on the second, what does the report show?',null,['The test is shown as passed','The test is shown as "flaky" — it failed at least once and passed at least once','The test is shown as failed because it failed first','The test is shown as skipped'],1,'Playwright marks tests as "flaky" when they pass after one or more retries — helping teams identify tests that need investigation.'],
['L','A','Can you access `testInfo.outputDir` to save custom artifacts?',null,['No — outputDir is read-only and Playwright controls all artifacts','Yes — testInfo.outputDir is the recommended directory for per-test artifacts; you can write files there and they appear in the report as attachments','Only if trace: "on" is set','Only in afterEach, not during the test'],1,'testInfo.outputDir provides a unique directory per test run — write anything there and optionally attach it with test.info().attach().'],
['R','B','You want the test title to appear in the screenshot filename for easier debugging. How?',null,['Playwright automatically uses the title','Use testInfo.title inside the filename: path.join(testInfo.outputDir, `${testInfo.title}.png`)','Use a fixed filename — Playwright appends the title automatically','Set screenshotTitle: true in config'],1,'testInfo.title and testInfo.outputDir together give you a unique, descriptive artifact path per test.'],
['R','I','You need to run "sanity" tests (tagged @sanity) on every PR and all tests nightly. How?',null,['Maintain two playwright.config.ts files','Use --grep "@sanity" in the PR workflow and no --grep in the nightly workflow — same test suite, different filter','Create separate test files for sanity tests','Use test.describe.only() for sanity tests'],1,'CLI --grep filter on the same suite is the cleanest approach — one codebase, multiple run profiles.'],
['R','I','You need to pass a test-specific piece of data (user email) to a helper function. How?',null,['Use a global variable','Pass it as a parameter to the helper — keep tests stateless and explicit','Use testInfo.annotations','Store it in localStorage before the test'],0,'Explicit parameters are clearest — avoid global mutable state which causes parallel test interference.'],
['R','A','You want beforeEach to run for all tests in the file except one specific test. What is the approach?',null,['Use test.skip() in the problematic test to avoid beforeEach','Call test.use({ beforeEach: false })','Refactor: move the tests that need beforeEach into a describe block containing the hook; leave the exception outside that block','Delete the beforeEach and add setup to each test individually'],2,'Scoping beforeEach inside a describe block is the clean approach — hooks only apply within their scope.'],
['R','B','You want to add a note to a test explaining it is blocked by a third-party issue. How?',null,['Add a code comment','Call test.info().annotations.push({ type: "issue", description: "JIRA-123: blocked by vendor bug" }) — it appears in the HTML report','Use test.fixme("JIRA-123")','Add it to the test title string'],1,'Annotations appear in the HTML report alongside the test result — more discoverable than code comments.'],
['S','B','A test that was passing suddenly starts failing with "strict mode violation: multiple elements". The UI added a duplicate button. What is the correct fix?',null,['Use .first() as a quick fix','Update the locator to be uniquely specific (add a role, aria-label, or test-id) so only one element is matched — strict mode violations are Playwright telling you the selector is ambiguous','Disable strict mode in config','Add an nth() filter to always take the first match'],1,'Strict mode violations reveal ambiguous locators — the right fix is a more specific selector, not silencing the warning.'],
['S','I','A developer uses `test.describe.serial()` for a 10-step flow. Test 5 fails. What happens to tests 6-10?',null,['Tests 6-10 run with the state from test 4','Tests 6-10 are skipped — serial mode stops the sequence on first failure (unless retries are configured)','Tests 6-10 run in parallel','All 10 tests are retried from the start'],1,'Serial block failure stops the sequence — subsequent tests depend on prior state that is now invalid.'],
['S','A','You need to verify that your app shows a rate-limit message after 5 rapid form submissions. How do you write this test cleanly?',null,['Submit the form 5 times with separate test() calls in serial','Write a loop inside one test: for (let i = 0; i < 5; i++) { await page.getByRole("button").click() } then assert the rate-limit message — all within a single test.step("submit 5 times", ...)','Use test.repeat(5) to run the submission test 5 times','Mock the rate-limit response'],1,'A loop inside a single test is cleaner than 5 separate tests — use test.step to label the loop for the trace viewer.'],
['S','I','Tests pass individually but fail when the full suite runs. What is the most likely cause?',null,['The browser cache is not cleared between tests','Shared mutable state between tests — a module-level variable, a beforeAll side-effect, or shared database records that one test modifies and another reads','retries: 0 causes them to fail without re-run','Tests must be run in alphabetical order'],1,'Tests must be independent — when run together, execution order exposes shared state bugs. Each test should create its own data.'],
],
// ── 3. Browsers & Contexts ────────────────────────────────────
browsers: [
// O×12 ─────────────────────────────────────────────────────────
['O','B','In Playwright\'s object model, what is a BrowserContext?',null,['The running browser process (Chromium/Firefox/WebKit)','An isolated session with its own cookies, localStorage, and auth state','A single tab open in the browser','A configuration object for browser launch options'],1,'A BrowserContext is completely isolated — its own cookies, localStorage, and authentication state. Multiple contexts in one browser never share state.'],
['O','B','What does `browser.newContext()` return?',null,['A new browser process','A new isolated BrowserContext','A new Page (tab)','A new Playwright test worker'],1,'`newContext()` creates a fresh isolated session inside the existing browser process — like opening a new incognito window.'],
['O','B','What happens to all pages when `context.close()` is called?',null,['Pages remain open but are disconnected','All pages within that context are closed automatically','Only the most recently opened page is closed','Pages are moved to the default context'],1,'Closing a context tears down everything inside it. You never need to close individual pages when you\'re about to close the context.'],
['O','I','What is the key difference between `browser.newPage()` and `context.newPage()`?',null,['`browser.newPage()` opens a new window; `context.newPage()` opens a new tab','`browser.newPage()` creates a page in a brand-new default context; `context.newPage()` creates a page inside an existing context','`browser.newPage()` is faster; they are otherwise identical','`context.newPage()` requires authentication; `browser.newPage()` does not'],1,'`browser.newPage()` implicitly creates a new context for the page. `context.newPage()` reuses your custom context so the page inherits its viewport, locale, auth state, etc.'],
['O','B','What does the `viewport` option in `browser.newContext()` control?',null,['The resolution of the physical display','The width and height of the browser viewport for that context','The zoom level of the browser','The aspect ratio used for screenshots'],1,'`viewport: { width: 375, height: 812 }` sets the browser window size, directly affecting responsive layout breakpoints.'],
['O','I','What does `storageState: \'auth.json\'` do when passed to `browser.newContext()`?',null,['Saves the current cookies to auth.json at test start','Pre-loads previously saved cookies and localStorage into the new context','Encrypts the context\'s storage','Blocks the context from writing to localStorage'],1,'`storageState` bootstraps the context with a saved session — skipping the login flow entirely. Run one login test, save the state, reuse it everywhere.'],
['O','B','Which three browser engines does Playwright natively support?',null,['Chrome, Firefox, Safari','Chromium, Firefox, WebKit','Edge, Chrome, Firefox','Chromium, Gecko, Blink'],1,'Playwright bundles patched builds of Chromium, Firefox, and WebKit. WebKit powers Safari; Chromium powers Chrome and Edge.'],
['O','I','Are cookies shared between two BrowserContexts created from the same browser?',null,['Yes — they share the same browser process storage','No — each context is fully isolated with its own cookie jar','Only session cookies are shared; persistent cookies are isolated','Yes, unless incognito mode is enabled'],1,'Complete isolation is the whole point of BrowserContext. Context A\'s cookies, localStorage, and IndexedDB are invisible to Context B.'],
['O','I','What does `colorScheme: \'dark\'` do in `browser.newContext()` options?',null,['Changes the browser UI chrome to dark mode','Sets the CSS media feature `prefers-color-scheme` to `dark` for that context','Inverts the page colors','Enables night mode screenshots'],1,'`colorScheme` controls the `prefers-color-scheme` media query, letting you test dark/light theme variants without OS-level changes.'],
['O','I','What must you also include alongside `geolocation` coordinates in `browser.newContext()` for geolocation to work?',null,['The country code as a string','`permissions: [\'geolocation\']` to grant the permission','`locale` matching the country','`timezoneId` for the location'],1,'Browsers prompt users for geolocation permission. In Playwright you pre-grant it via `permissions: [\'geolocation\']`; without it the API is blocked.'],
['O','B','In a standard test using the `page` fixture, who creates and manages the BrowserContext?',null,['The developer must create it manually','Playwright\'s fixture system creates it automatically from project `use` settings','The operating system allocates it','The browser\'s built-in session manager'],1,'The `page` fixture is built on top of `context`, which is itself built on `browser`. Playwright\'s fixture chain handles the full lifecycle automatically.'],
['O','A','What does `await context.storageState({ path: \'state.json\' })` do?',null,['Loads a previously saved state into the context','Saves the current cookies and localStorage of the context to a JSON file','Clears all cookies and saves an empty state','Exports the context configuration'],1,'`storageState()` captures the current cookies and localStorage snapshot. Reuse this file in `newContext({ storageState: \'state.json\' })` to start future tests already authenticated.'],

// F×7 ─────────────────────────────────────────────────────────
['F','B','Complete: `const context = await browser.______({ locale: \'fr-FR\' })`',null,['newContext','createContext','openContext','makeContext'],0,'`browser.newContext(options)` is the standard API for creating a customised isolated session.'],
['F','B','Complete: `const page = await context.______()` to open a new tab inside the context.',null,['newPage','openPage','createPage','addPage'],0,'`context.newPage()` opens a new browser tab (Page) that inherits all the context\'s settings — viewport, locale, cookies, etc.'],
['F','I','Fill in the option key for window dimensions: `await browser.newContext({ ______: { width: 375, height: 812 } })`',null,['viewport','windowSize','resolution','screen'],0,'`viewport` sets the browser viewport width and height, directly affecting responsive CSS breakpoints during tests.'],
['F','I','Fill in the option to set the browser timezone: `await browser.newContext({ ______: \'Europe/London\' })`',null,['timezoneId','timezone','locale','region'],0,'`timezoneId` accepts an IANA timezone string. It affects `Date` output, API responses that return local times, and timezone-aware UI components.'],
['F','I','Fill in the permissions array: `await browser.newContext({ permissions: [\'______\', \'notifications\'] })`',null,['geolocation','location','gps','coordinates'],0,'The permission string is `\'geolocation\'`. You must grant it here, otherwise the browser blocks `navigator.geolocation` calls.'],
['F','A','Fill in the option to pre-load saved auth: `await browser.newContext({ ______: \'auth.json\' })`',null,['storageState','authState','sessionFile','cookieFile'],0,'`storageState` accepts a path to a JSON file produced by `context.storageState({ path: \'auth.json\' })`.'],
['F','I','Fill in the option to record a video: `await browser.newContext({ ______: { dir: \'videos/\' } })`',null,['recordVideo','video','captureVideo','screenRecord'],0,'`recordVideo: { dir: \'path/\' }` starts recording a video for every page opened in the context. Useful for debugging CI failures.'],

// E×6 ─────────────────────────────────────────────────────────
['E','B','What is wrong?\n`const context = browser.newContext({ viewport: { width: 1280, height: 720 } })`',null,['Missing `await` — `browser.newContext()` returns a Promise','`browser` is not available without importing it','viewport values must be strings','newContext does not accept an options object'],0,'`browser.newContext()` is async and returns a Promise<BrowserContext>. Without `await` you store an unresolved Promise, not the context.'],
['E','B','What is wrong?\n`const page = await context.newContext()`',null,['`newContext()` is a method on `browser`, not `context` — use `context.newPage()`','`context` must be closed before creating a new page','`newContext` requires options to be passed','You cannot create pages from a context directly'],0,'`BrowserContext` has `newPage()`. The `newContext()` method lives on `Browser`. Calling it on context throws a "not a function" error.'],
['E','I','Spot the issue: a test creates two pages inside a single context and treats them as two separate users.',null,['Pages in the same context share cookies and auth — they are not isolated users','Two pages cannot exist in the same context','The second page will overwrite the first','Context can only hold one page at a time'],0,'Isolation happens at the context level, not the page level. Two pages in the same context share all session data — use two separate contexts for two users.'],
['E','I','What is wrong with this geolocation setup?\n`await browser.newContext({ geolocation: { latitude: 51.5, longitude: -0.1 } })`',null,['`permissions: [\'geolocation\']` is missing — without it the permission is blocked','latitude/longitude values must be integers','geolocation must be a string, not an object','The context must navigate to a page before geolocation takes effect'],0,'Setting coordinates alone is not enough; the browser also needs the permission granted. Add `permissions: [\'geolocation\']` to the options object.'],
['E','A','What is wrong with this cleanup sequence?\n```\nconst context = await browser.newContext();\nconst page = await context.newPage();\nawait page.close();\nawait page.goto(\'/\');\n```',null,['`page.goto()` is called after `page.close()` — the page is already destroyed','`context.close()` must be called before `page.close()`','`page.close()` requires a URL argument','pages cannot be closed individually'],0,'After `page.close()`, the page object is disposed. Calling any method on it (like `goto`) throws an error. Close pages only when you are finished with them.'],
['E','I','What is wrong?\n```\nawait browser.newContext({ colorScheme: \'night\' })\n```',null,['Valid values are `\'dark\'`, `\'light\'`, or `\'no-preference\'` — `\'night\'` is not valid','colorScheme must be a boolean','colorScheme is set on the page, not the context','The option key should be `prefersDarkMode`'],0,'Playwright\'s `colorScheme` accepts `\'dark\'`, `\'light\'`, or `\'no-preference\'`. Any other string causes a context creation error.'],

// D×6 ─────────────────────────────────────────────────────────
['D','B','Two sequential tests share login state even though each should start fresh. What is the most likely cause?',null,['Both tests reuse the same BrowserContext — they should each get an isolated context','The tests are running in parallel and sharing a browser','localStorage is not cleared between page navigations','The `page` fixture does not reset cookies'],0,'By default each test gets its own context via the `context` fixture. If you share a context manually between tests, state bleeds. Let Playwright\'s fixtures manage context lifecycle.'],
['D','I','A mobile layout test passes on desktop but shows the wrong viewport. What configuration is missing?',null,['The context `viewport` option was not set — add `viewport: { width: 375, height: 812 }`','The test must run in headed mode','`userAgent` must be set to a mobile string','Mobile tests require a separate browser installation'],0,'Without an explicit `viewport`, contexts use the default (usually 1280×720). Set `viewport: { width: 375, height: 812 }` or use `devices[\'iPhone 12\']` preset.'],
['D','I','A location-based feature returns the server\'s default city instead of the test location. The context has geolocation coordinates. What is missing?',null,['`permissions: [\'geolocation\']` is missing from the context options','`locale` must match the geolocation country','`timezoneId` must be set alongside geolocation','The page must call `navigator.geolocation.getCurrentPosition()` manually'],0,'Without granting the geolocation permission, the browser silently blocks `navigator.geolocation`. Add `permissions: [\'geolocation\']` to the context options.'],
['D','A','A two-user test creates `adminPage` and `guestPage` from the same context. Admin actions affect the guest session. How do you fix it?',null,['Create two separate contexts: one for admin, one for guest','Call `context.clearCookies()` between creating each page','Use different browsers for each user','Create one context per page with `browser.newPage()`'],0,'Isolation is a context-level concept. Create `adminCtx` and `guestCtx` separately — each with their own `storageState` — then create a page in each.'],
['D','B','After a test finishes, memory usage keeps growing. Each test creates a context but never calls `context.close()`. What is the correct fix?',null,['Call `await context.close()` in `test.afterEach()` or let Playwright\'s `context` fixture handle cleanup','Call `page.close()` at the end of each test','Use `browser.disconnect()` instead','Restart the browser between tests'],0,'Any context you create manually must be closed manually. Alternatively, use the built-in `context` fixture — Playwright closes it automatically after each test.'],
['D','I','`await context.storageState({ path: \'auth.json\' })` is called before the user has logged in. Later tests fail authentication. What went wrong?',null,['The state was saved before login — it contains no auth cookies','auth.json was saved to the wrong directory','storageState must be called with `await` only after context.close()','The path must be an absolute path'],0,'`storageState()` captures the state at the moment it is called. Save it only after the login flow is complete and the auth cookies are present.'],

// C×5 ─────────────────────────────────────────────────────────
['C','B','Complete: `const ctx = await browser.______({ colorScheme: \'dark\' })`',null,['newContext','createContext','openContext','newSession'],0,'`browser.newContext(options)` is the method for creating a custom isolated browser session.'],
['C','I','Complete to open a page inside the context: `const page = await ctx.______( )`',null,['newPage','openPage','createPage','spawn'],0,'`context.newPage()` creates a new tab (Page) inside the context, inheriting all its settings.'],
['C','I','Complete to emulate a mobile viewport: `await browser.newContext({ viewport: { width: 390, ______: 844 } })`',null,['height','h','rows','length'],0,'`viewport` requires both `width` and `height` as numbers (in CSS pixels). This example emulates an iPhone 14 screen size.'],
['C','A','Complete to save the logged-in session: `await context.______({ path: \'session.json\' })`',null,['storageState','saveState','exportCookies','persist'],0,'`context.storageState({ path: \'…\' })` writes a JSON snapshot of all cookies and localStorage, ready to be loaded by future contexts.'],
['C','I','Complete to tear down the context and all its pages: `await context.______( )`',null,['close','destroy','end','dispose'],0,'`context.close()` is the standard teardown method. It closes all pages inside the context, releases resources, and terminates the session.'],

// L×5 ─────────────────────────────────────────────────────────
['L','B','If three pages are created from the same BrowserContext, do they share cookies?',null,['Yes — pages in the same context share all session state','No — each page is isolated','Only session cookies are shared','Only if they navigate to the same origin'],0,'Isolation is at the context level. All pages within one context share cookies, localStorage, and auth — they represent the same "user session".'],
['L','I','Which statement best describes `browser.newPage()` vs `browser.newContext().then(c => c.newPage())`?',null,['`browser.newPage()` implicitly creates a new default context; chaining newContext() gives you a custom context','They are identical — `browser.newPage()` uses the existing context','`browser.newPage()` is deprecated in favour of the chained approach','`browser.newPage()` creates a page without any context'],0,'`browser.newPage()` is a shortcut that creates a default context + page. Use the explicit `newContext()` approach when you need custom options or multiple contexts.'],
['L','I','In what order should you close resources created manually?',null,['page → context (closing context also closes pages, so closing page first is optional)','context → page (you must close context before page)','browser → context → page','page → browser (context closes automatically)'],0,'You can close pages first and then the context, or just close the context which closes all its pages. Never try to use a page after its context is closed.'],
['L','A','Can a BrowserContext be shared between two different test files running in parallel workers?',null,['No — each worker runs in a separate process with its own browser instances','Yes — contexts are shared across the test suite','Yes — if using the shared `browser` fixture','Only if both files import the same context variable'],0,'Playwright workers are separate Node.js processes. They cannot share JavaScript objects like a BrowserContext. Each worker manages its own browser and contexts.'],
['L','I','Does the built-in `page` fixture create a new BrowserContext for every test?',null,['Yes — `page` fixture depends on `context` fixture, which creates a fresh context per test','No — all tests share one context per worker','Only if `reuseExistingServer` is set to false','No — context is created once per test file'],0,'The fixture dependency chain is `browser → context → page`. Each test gets a fresh context (and page), guaranteeing full isolation between tests.'],

// R×5 ─────────────────────────────────────────────────────────
['R','B','Your app behaves differently in Safari. How do you run a Playwright test specifically against WebKit?',null,['Define a project with `use: { ...devices[\'Desktop Safari\'] }` or `browserName: \'webkit\'`','Install Safari and connect Playwright to it','Use `browser.webkit.launch()` inside the test','Pass `--safari` to the CLI'],0,'Playwright\'s `webkit` project targets the same engine Apple uses for Safari. Add it to the `projects` array in playwright.config.ts.'],
['R','I','You need to simultaneously test a real-time chat feature between two users. What is the recommended approach?',null,['Create two BrowserContexts (one per user) and a page in each — interact with both in the same test','Run the test twice with different users','Use two separate test files and synchronize with a database','Open two tabs in the same context and use `page.frames()`'],0,'Two isolated contexts simulate two users without any state bleed. Both pages can be driven within a single `test()` block.'],
['R','I','You need to emulate an iPhone 12 for a mobile responsive test. What is the most concise approach?',null,['`browser.newContext({ ...devices[\'iPhone 12\'] })`','`browser.newContext({ mobile: true })`','`browser.newContext({ viewport: \'mobile\' })`','Pass `--mobile` to the test runner'],0,'`devices[\'iPhone 12\']` is a preset that includes viewport, userAgent, deviceScaleFactor, and touch support in one spread.'],
['R','A','You need to test a feature that shows location-specific store hours (requires geolocation). What context options are needed?',null,['`geolocation: { latitude, longitude }` AND `permissions: [\'geolocation\']`','Only `geolocation: { latitude, longitude }` is needed','Only `permissions: [\'geolocation\']` is needed — coordinates use device GPS','`locale` set to the country of the location'],0,'Both are required: the coordinates (what to report) and the permission grant (so the browser doesn\'t block the API call).'],
['R','I','You want every failed test to automatically produce a video recording for debugging. Where is the best place to configure this?',null,['In `playwright.config.ts` under `use: { video: \'on-first-retry\' }`','In every `browser.newContext()` call inside each test','In a `beforeEach` hook that creates a new context','Via the `--video` CLI flag only'],0,'Setting `video: \'on-first-retry\'` in the global `use` block applies to all tests. On the first retry of a failing test, a video is recorded automatically.'],

// S×4 ─────────────────────────────────────────────────────────
['S','I','A test creates two pages in the same context for a "two-user" chat test. The admin sees messages before the guest sends them. What is the root cause?',null,['Both pages share the same auth session — they are the same user in two tabs','The test has a race condition in sending messages','The context was not initialised with storageState','Pages in the same context cannot communicate'],0,'Shared context = shared identity. To truly simulate two users, create two separate contexts, each loaded with its own `storageState`.'],
['S','A','CI videos show the wrong time zone in date/time components, making it hard to compare with expected values. How do you fix it?',null,['Add `timezoneId: \'UTC\'` (or the app\'s expected timezone) to `browser.newContext()` options','Set the OS timezone on the CI server','Add a beforeEach that calls `Date.setTimezone()`','Use `locale` to control timezone display'],0,'`timezoneId` controls the browser\'s `Date` and `Intl` timezone. Pin it to a known value so tests produce consistent timestamps regardless of the CI server\'s OS timezone.'],
['S','I','Each test starts logged in even though no `storageState` is configured. Investigation shows a manually created context is never closed. What is the fix?',null,['Close the context after each test with `await context.close()` or use the built-in `context` fixture','Call `context.clearCookies()` at the start of each test','Disable `storageState` in playwright.config.ts','Use `browser.newPage()` instead of `context.newPage()`'],0,'An unclosed context persists across tests in the same worker, carrying its cookies forward. Either close it explicitly or use the `context` fixture, which Playwright closes automatically.'],
['S','A','You need to test three user roles (admin, editor, viewer) interacting simultaneously. What is the most maintainable structure?',null,['Create three contexts with their respective storageState files and a page in each, then drive all three in one test','Run three separate test files, one per role','Create one context and use page.evaluate() to switch users','Use test.describe.parallel() with three tests'],0,'Three contexts with pre-loaded auth states represent the three users cleanly. Drive all three pages in a single test to assert on real-time interactions between roles.'],

// ── Additional batch ×50 ─────────────────────────────────────
['O','B','What is the difference between `browser.newContext()` and `browser.newPage()`?',null,['They are identical','newContext creates an isolated session; newPage opens a tab inside the default shared context — newPage does not provide isolation','newPage creates an isolated context automatically','browser.newPage() is deprecated'],1,'newPage uses the default context (shared state); newContext creates a fully isolated session. For isolation, always use newContext then context.newPage().'],
['O','I','What does `context.grantPermissions(["clipboard-read"])` do?',null,['Grants Playwright permission to read the clipboard of the host OS','Grants the browser context permission to use the Clipboard API without showing a permission dialog','Enables clipboard monitoring for assertions','Allows the test to read clipboard in CI'],1,'grantPermissions bypasses the browser\'s permission dialog — the context treats the specified permission as already granted.'],
['O','B','What does `context.clearCookies()` do?',null,['Clears cookies in the test runner cache','Removes all cookies from the browser context — subsequent requests start without any session cookies','Resets localStorage and sessionStorage','Deletes the storageState JSON file'],1,'clearCookies() removes context-level cookies without closing the context — useful for simulating logout within a test.'],
['O','I','What does `page.waitForLoadState("domcontentloaded")` wait for?',null,['All network requests to finish','The HTML document to be parsed and the DOMContentLoaded event to fire — faster than "load" because it does not wait for images and stylesheets','The page to become visible on screen','All JavaScript to finish executing'],1,'domcontentloaded fires earlier than load — use it when you only need the DOM structure and don\'t need images or stylesheets.'],
['O','I','What is the difference between `browser.newContext` and the built-in `context` fixture?',null,['They are the same thing','The built-in context fixture is test-scoped (auto-created and auto-closed per test); browser.newContext() requires manual management','The fixture creates an incognito context; newContext() creates a normal context','The fixture only works with Chromium'],1,'Always prefer the fixture for standard tests — manual context management risks forgetting close() and leaking state.'],
['O','B','What does `context.newPage()` return?',null,['A new BrowserContext','A new Page (browser tab) inside the existing context — sharing cookies and storage with other pages in the same context','A new isolated browser window','A Frame object'],1,'Pages within the same context share cookies and localStorage — use different contexts for true isolation.'],
['O','I','What does setting `ignoreHTTPSErrors: true` in context options do?',null,['Disables SSL verification for all requests','Makes the browser ignore SSL certificate errors — useful for testing against self-signed certificates in development environments','Allows HTTP requests in HTTPS pages','Disables Content Security Policy'],1,'Self-signed certs in dev/staging fail browser security checks — ignoreHTTPSErrors lets tests run against them without real certificate setup.'],
['O','B','What does `devices["iPhone 14"]` return?',null,['A browser instance preconfigured for iPhone 14','An object with viewport, userAgent, deviceScaleFactor, isMobile, and hasTouch properties for iPhone 14 emulation','A device-specific test file template','The screen resolution of iPhone 14'],1,'devices is a dictionary of device presets — spread the object into context options to emulate that device completely.'],
['O','I','What does `context.route()` do compared to `page.route()`?',null,['They are identical','context.route() intercepts requests from ALL pages in the context; page.route() only intercepts requests from that specific page','context.route() is faster because it uses a shared handler','page.route() is deprecated in favour of context.route()'],1,'Use context.route() when you want to intercept/mock requests from multiple pages; page.route() for single-page interception.'],
['O','A','What does `browser.contexts()` return?',null,['The number of open contexts','An array of all BrowserContext objects currently open in the browser instance','An array of all open pages','A promise resolving to context metadata'],1,'browser.contexts() lets you introspect or close all contexts — useful in globalTeardown to clean up any leaked contexts.'],
['O','I','What does `context.addInitScript(script)` do?',null,['Adds a script to the page\'s HTML head','Evaluates the script in every new page created in the context before any page scripts run — useful for mocking globals like Date or Math.random','Adds a script that runs after each navigation','Injects a script into existing pages only'],1,'addInitScript is the hook for mocking browser globals — it runs before the page\'s own scripts, so mocks are in place from the first line of application code.'],
['O','B','Does closing a `BrowserContext` also close its `Browser`?',null,['Yes — the browser shuts down when all contexts are closed','No — closing a context only destroys its pages and session state; the browser process continues until browser.close() is called','Yes, but only if the context was the last one','Depends on the browser engine'],1,'Context and browser are independent lifecycle objects — close() on a context does not affect the browser process.'],
['F','B','Complete to create an isolated context with custom viewport.\n```ts\nconst context = await browser.______({ viewport: { width: 375, height: 812 } });\n```',null,['newContext','newPage','createContext','openContext'],0,'browser.newContext(options) is the API for creating an isolated browser session.'],
['F','B','Complete to open a new tab inside a context.\n```ts\nconst page = await context.______();\n```',null,['newPage','openPage','addPage','createTab'],0,'context.newPage() opens a new browser tab sharing the context\'s cookies and storage.'],
['F','I','Complete to grant microphone permission.\n```ts\nawait context.______([\'microphone\']);\n```',null,['grantPermissions','setPermissions','allowPermissions','addPermissions'],0,'grantPermissions() accepts an array of permission strings — bypasses the browser prompt for the listed permissions.'],
['F','I','Complete to emulate an iPhone 12 using the devices preset.\n```ts\nconst context = await browser.newContext({\n  ...______[\'iPhone 12\'],\n});\n```',null,['devices','presets','emulators','deviceList'],0,'The devices object is imported from @playwright/test and contains presets for hundreds of devices.'],
['F','I','Complete to set a custom geolocation.\n```ts\nconst context = await browser.newContext({\n  ______: { latitude: 51.5074, longitude: -0.1278 },\n  permissions: [\'geolocation\'],\n});\n```',null,['geolocation','location','gps','coordinates'],0,'geolocation option sets the coordinates reported by the browser\'s Geolocation API.'],
['F','B','Complete to ignore SSL errors.\n```ts\nconst context = await browser.newContext({\n  ______: true,\n});\n```',null,['ignoreHTTPSErrors','skipSSL','disableTLS','acceptInvalidCerts'],0,'ignoreHTTPSErrors: true makes the browser proceed past SSL certificate errors.'],
['F','I','Complete to inject a script before page scripts.\n```ts\nawait context.______(() => {\n  window.__TEST__ = true;\n});\n```',null,['addInitScript','injectScript','runScript','addScript'],0,'addInitScript() injects a function to run in every new page before application code — perfect for setting up mocks.'],
['E','B','What is wrong?\n```ts\nconst page1 = await browser.newPage();\nconst page2 = await browser.newPage();\n// page1 and page2 are being used to simulate two different users\n```',null,['browser.newPage() only works once','browser.newPage() creates pages in the same default context — they share cookies, so both represent the same user session. Create two separate contexts instead.','Pages cannot be created directly from browser','Each page must use a different browser'],1,'newPage() shares the default context — for isolated users, use browser.newContext() twice.'],
['E','I','What is wrong?\n```ts\nconst context = await browser.newContext();\nawait context.grantPermissions(\'camera\'); // string, not array\n```',null,['camera is not a valid permission name','grantPermissions expects an array of strings, not a single string — use [\'camera\']','grantPermissions is called on browser, not context','Permissions must be granted before newContext()'],1,'grantPermissions always takes an array — even for a single permission.'],
['E','B','What is wrong?\n```ts\nconst context = await browser.newContext();\nconst page = await context.newPage();\n// test runs...\nawait browser.close(); // context never closed\n```',null,['browser.close() automatically closes all contexts first — this is fine','When browser.close() is called, all contexts are forcibly closed. This is not an error but forgetting context.close() before browser.close() may skip teardown logic inside the context (like saving storageState)','browser.close() leaves contexts open','This causes a memory leak in the test runner'],1,'It is safe because browser.close() cascades — but explicitly closing the context first ensures any context-level teardown (storageState save) runs.'],
['E','I','What is wrong?\n```ts\nconst context = await browser.newContext({\n  geolocation: { lat: 51.5, lng: -0.1 }, // wrong property names\n});\n```',null,['geolocation option is not supported','The properties are latitude and longitude, not lat and lng — wrong names are silently ignored','lng should be lon','geolocation requires permissions: true instead of an array'],1,'The geolocation object uses latitude and longitude — typos in property names are silently dropped.'],
['E','A','What is wrong?\n```ts\nawait context.addInitScript("window.flag = true");\n// Later in a test:\nconst flag = await page.evaluate(() => window.flag);\nconsole.log(flag); // undefined\n```',null,['addInitScript must use a function, not a string','addInitScript was called after the page was created — init scripts only run in pages opened AFTER addInitScript is called. Call it before context.newPage().','window.flag assignment is async','evaluate cannot access window globals'],1,'addInitScript only affects pages opened after it is registered — existing pages miss the injection.'],
['E','I','What is wrong?\n```ts\nconst context = await browser.newContext({\n  storageState: "./auth.json" // file doesn\'t exist yet\n});\n```',null,['storageState path must be absolute','If the storageState file does not exist, Playwright throws a file-not-found error. Ensure the auth setup project runs first (via dependencies) before this context is created','storageState is a config-only option, not a context option','JSON must be an object not a path string'],1,'The file must exist — use the dependencies: ["setup"] project pattern to guarantee it is created before test projects start.'],
['D','B','Two tests leave pages open after completing. The test runner shows "1 leaked resource" warnings. What should you fix?',null,['Add page.close() at the end of every test','Use the built-in page and context fixtures instead of manual browser.newPage() — fixtures handle cleanup automatically','Set cleanupLeaks: true in playwright.config.ts','Run tests with --cleanup flag'],1,'Fixtures are the solution — they guarantee teardown even when tests fail. Manual context/page creation requires manual cleanup that is often forgotten.'],
['D','I','After switching to WebKit, tests fail with "permission denied" for localStorage. What is the likely cause?',null,['WebKit does not support localStorage','WebKit has stricter default permissions — check if storageState requires permissions: ["storage-access"] or if the app uses partitioned cookies which WebKit handles differently','localStorage is a Chromium-only feature','WebKit requires a special context option'],1,'Cross-browser permission model differences are common gotchas — always test your context options against all target engines.'],
['D','B','`devices["Pixel 5"]` spread in context options but the test still runs at desktop viewport size. What was missed?',null,['devices presets only set userAgent','Check that the context was created with the spread applied correctly: `browser.newContext({ ...devices["Pixel 5"] })`. If using the global use block, also verify the project uses the devices preset.','Pixel 5 is not in Playwright\'s devices list','viewport must be set separately'],1,'A common mistake is spreading into the wrong object or into a project config that is overridden — verify the final options object logged by browser.newContext().'],
['D','A','A test checks geolocation but the browser returns null even though geolocation is set in context options. What is missing?',null,['geolocation requires a special browser flag','permissions: ["geolocation"] must also be set — without it the browser denies the Geolocation API access even if coordinates are provided','geolocation must be set after the page loads','The coordinates format is wrong'],1,'Coordinates tell the browser what to report; the permission tells it it\'s allowed to report anything — both are required.'],
['D','I','`context.addInitScript()` runs but the global it sets is undefined when checked with `page.evaluate()`. Why?',null,['addInitScript does not affect evaluate()','The page may have navigated to a new URL after the init script ran — init scripts re-execute on each navigation. But if the app code overwrites the global after load, it would be lost. Check the timing with trace.','evaluate() uses a different JavaScript context','Init scripts only run on iframes'],1,'Init scripts run on navigation but application code runs after — if the app overwrites your global, inject it later in the lifecycle or use a more targeted mock.'],
['D','I','`browser.newContext({ colorScheme: "dark" })` set but the app still shows light mode. What should you check?',null,['colorScheme only works in Firefox','The app may not use the prefers-color-scheme media query, or it reads a user preference from localStorage that overrides the OS setting. Check both.','colorScheme must be set on the page, not the context','dark requires headless: false'],1,'colorScheme emulates the OS preference — apps that store user preference in localStorage will use that instead. Inject the localStorage value as well.'],
['C','B','Complete to clear all context cookies.\n```ts\nawait context.______();\n```',null,['clearCookies','deleteCookies','resetCookies','flushCookies'],0,'context.clearCookies() removes all cookies from the context session.'],
['C','I','Complete to add a cookie manually.\n```ts\nawait context.addCookies([{\n  name: \'token\',\n  value: \'abc\',\n  domain: \'example.com\',\n  ______: \'/\',\n}]);\n```',null,['path','route','uri','url'],0,'path is a required cookie attribute alongside name, value, and domain.'],
['C','I','Complete to save the current context state to a file.\n```ts\nawait context.______({ path: \'state.json\' });\n```',null,['storageState','saveState','exportState','writeState'],0,'storageState() serialises the context cookies and storage to the given JSON file.'],
['C','B','Complete to get all open pages in a context.\n```ts\nconst pages = context.______();\n```',null,['pages()','getPages()','allPages()','openPages()'],0,'context.pages() returns an array of all Page objects currently open in the context — synchronous.'],
['C','A','Complete to route all requests in the context to a handler.\n```ts\nawait context.______("**/*.png", route => route.abort());\n```',null,['route','intercept','handle','proxy'],0,'context.route(pattern, handler) intercepts matching requests from all pages in the context.'],
['L','B','Why does Playwright use a separate BrowserContext per test by default?',null,['To avoid sharing browser processes between tests','To ensure each test starts with a clean slate — no cookies, no localStorage, no auth state from previous tests — preventing test interdependency','Contexts are cheaper to create than browsers','It is a legacy design decision'],1,'Isolation is the primary design principle — each test gets a fresh context so tests can run in any order without side effects.'],
['L','I','Can two BrowserContexts in the same browser instance share cookies?',null,['Yes — they share the browser\'s global cookie jar','No — contexts are completely isolated from each other\'s cookies, localStorage, and auth state','Only if they share the same origin','Only if both are created with storageState from the same file'],1,'Context isolation is absolute — two contexts are like two separate incognito sessions that know nothing about each other.'],
['L','B','What is the execution order when a test uses the page fixture?',null,['Test runs, then browser.newContext(), then context.newPage()','browser.newContext() → context.newPage() → test runs → page.close() → context.close()','context.newPage() → test runs → context.close()','Test runs first, then fixtures are created for assertions'],1,'Fixtures are created in dependency order before the test and torn down in reverse order after — browser → context → page.'],
['L','I','Why might `context.addInitScript()` need to be called before context.newPage()?',null,['It doesn\'t — order does not matter','addInitScript registers the script for ALL future pages in the context — pages opened before the call miss the injection','addInitScript only works before navigation','newPage() clears all init scripts'],1,'Init scripts apply to pages opened after registration — call addInitScript() before creating pages to ensure full coverage.'],
['L','A','Is it safe to share a BrowserContext across tests in a beforeAll / afterAll pattern?',null,['Yes — it is the recommended pattern for speed','It works but risks state leakage between tests — test A\'s cookies are visible to test B. Use carefully only when tests are intentionally ordered and stateful, like an auth flow where login state must persist.','Playwright prevents it — contexts cannot be created in beforeAll','It is safe only if clearCookies() is called between tests'],1,'Shared context is a trade-off: faster (no context recreation) but requires careful cleanup between tests to maintain independence.'],
['R','B','You need to test how your app looks in dark mode. How do you configure this?',null,['Set colorScheme: "dark" in browser.newContext() options or in the use block — Playwright emulates the prefers-color-scheme media query','Use CSS injection to force dark mode','Toggle the system dark mode setting before tests','Pass --dark-mode to the CLI'],0,'colorScheme: "dark" makes the browser report prefers-color-scheme: dark — your CSS media queries respond accordingly.'],
['R','I','Your app uses service workers. How do you disable them for testing to avoid caching issues?',null,['Add serviceWorkers: "block" to context options — Playwright will prevent all service workers from registering','Delete the service worker registration in beforeEach','Use route() to block the service worker script','Service workers cannot be disabled in Playwright'],0,'serviceWorkers: "block" prevents registration — tests see the raw network responses without the service worker cache layer.'],
['R','A','You need to test a web app that uses WebUSB. How do you handle the permission?',null,['WebUSB cannot be tested in headless mode','Grant the permission using context.grantPermissions() — however WebUSB requires specific origin and device pairing that may need additional setup beyond permissions','Use a mocked USB device via context.addInitScript()','WebUSB testing requires real hardware'],1,'For specialized permissions like WebUSB, combine grantPermissions with addInitScript to mock the USB device API if real hardware isn\'t available.'],
['R','I','You want every context in CI to use a proxy for all requests. How do you configure this?',null,['Add proxy settings to each page.route() call','Set proxy: { server: "http://proxy:8080" } in the context options or globally in the config use block — all requests from the context route through the proxy','Use an environment variable HTTPS_PROXY','Add the proxy to the webServer config'],1,'The proxy option in context/use block routes all browser network traffic through the specified server — no per-request configuration needed.'],
['R','B','You want to verify that your app correctly handles users with slow network connections. What context option simulates this?',null,['Use networkThrottle in context options','Pass a network condition via the Chrome DevTools Protocol route — or set offline: true to test complete offline mode. For throttling, use page.route() to artificially delay responses.','Set latency: 500 in context options','Use navigator.connection.downlink mock'],1,'Playwright does not have a built-in throttle option — simulate slow networks by delaying responses in route handlers or using CDP to emulate network conditions.'],
['S','B','You create two pages in the same context for a "buyer/seller" feature test but they see each other\'s data. What is the fix?',null,['Use separate windows','Create two separate BrowserContexts — one for buyer (with buyer storageState) and one for seller (with seller storageState). Same-context pages share all session state.','Call context.isolate() between page creation','Use page.evaluate() to reset state'],1,'Separate contexts = separate users. Same context = same user in different tabs.'],
['S','I','A context created with `storageState` contains expired session cookies. Tests fail with auth errors. What should you do?',null,['Re-run the auth setup project to generate fresh storageState','Check session TTL — if the auth setup runs in CI and tests run hours later, the session may expire mid-suite. Use short-lived tokens carefully or implement session refresh.','Clear the storageState file and re-run','Increase cookie maxAge in playwright.config.ts'],1,'storageState captures a moment-in-time snapshot — short-lived sessions can expire between setup and test execution in long CI queues.'],
['S','A','After enabling `recordHar`, the HAR file is empty. What are two likely causes?',null,['HAR is not supported in WebKit','1) path was not specified in recordHar options — without a path the HAR is captured in memory only and not written to disk; 2) context.close() was not awaited — HAR finalisation happens in close()','HAR requires headed mode','HAR only works with page.route()'],1,'HAR is written on context close — both a valid path and awaited close() are required for the file to appear.'],
['S','I','A test opens 10 pages in one context for a bulk action test. The context becomes slow. What can you do?',null,['Use 10 separate contexts','Reduce the number of pages — or open pages lazily and close them immediately after use within the loop. Many open pages in one context compete for the same JavaScript thread.','Increase the timeout','Use headless: false for better performance'],1,'Each page in a context shares the browser\'s rendering engine — many simultaneous pages compete for CPU. Close pages when done within a loop.'],
],
// ── 4. Page & Navigation ──────────────────────────────────────
navigation: [
// O×12 ─────────────────────────────────────────────────────────
['O','B','What event does `page.goto(url)` wait for by default?',null,['DOMContentLoaded — HTML is parsed','The load event — all resources have finished loading','networkidle — no requests for 500 ms','commit — first byte received'],1,'The default `waitUntil` is `\'load\'`, which waits for the browser\'s `load` event to fire — all scripts, images, and stylesheets are done.'],
['O','B','What does `page.reload()` do?',null,['Navigates to the homepage','Reloads the current page (same as pressing F5)','Clears the page cache and re-downloads all assets','Restarts the browser context'],1,'`page.reload()` triggers a full page refresh. It also accepts `{ waitUntil }` and `{ timeout }` options identical to `page.goto()`.'],
['O','I','What does `waitUntil: \'networkidle\'` mean in `page.goto()` options?',null,['Waits until all images are loaded','Waits until there have been no network requests for at least 500 ms','Waits until the WebSocket connection is established','Waits until all async JavaScript has executed'],1,'`\'networkidle\'` is the most thorough wait: Playwright pauses until there are no open network connections for 500 ms. Useful for data-heavy pages but can time out on sites with persistent polling.'],
['O','B','Is `page.url()` synchronous or asynchronous?',null,['Asynchronous — it returns a Promise<string>','Synchronous — it returns the current URL string immediately','It depends on the browser engine','It is asynchronous only in Firefox'],1,'`page.url()` is one of the rare synchronous Playwright page methods. No `await` needed — it reads the cached URL from the current frame.'],
['O','I','What does `page.waitForURL(\'/dashboard\')` do?',null,['Asserts that the current URL is exactly /dashboard','Navigates the page to /dashboard','Waits (polls) until the current URL becomes /dashboard','Blocks the test for a fixed timeout then checks the URL'],2,'`waitForURL()` polls the current URL until it matches the string, regex, or glob pattern provided, or until the timeout expires.'],
['O','B','What does `waitUntil: \'domcontentloaded\'` wait for?',null,['All external resources (scripts, images, CSS) to load','Only the HTML document to be parsed (DOMContentLoaded event)','No network requests for 500 ms','The page title to be set'],1,'`\'domcontentloaded\'` fires as soon as HTML is parsed into a DOM — before scripts execute or images download. It is the fastest `waitUntil` option and suits static or server-rendered pages.'],
['O','B','What does `page.goBack()` do?',null,['Reloads the current page','Navigates to the previous entry in the browser history','Navigates to the homepage','Clears the current page URL'],1,'`page.goBack()` is the programmatic equivalent of clicking the browser\'s back button. It returns a Response or null if navigation failed.'],
['O','I','Which assertion is preferred for checking the page URL in Playwright?',null,['`expect(page.url()).toBe(\'/dashboard\')`','`assert(page.url() === \'/dashboard\')`','`await expect(page).toHaveURL(\'/dashboard\')`','`expect(await page.title()).toBe(\'/dashboard\')`'],2,'`expect(page).toHaveURL()` auto-retries until the URL matches, making it resilient to async redirects. Comparing `page.url()` directly is a single snapshot that can fail on in-flight redirects.'],
['O','I','What does `page.waitForLoadState(\'networkidle\')` wait for?',null,['The page load event only','No active network connections for 500 ms','All page JavaScript to finish executing','All WebSocket messages to be received'],1,'`waitForLoadState(\'networkidle\')` is identical in meaning to `waitUntil: \'networkidle\'` in `goto()` — it can be called independently to re-wait after an action.'],
['O','B','If `baseURL` is `\'http://localhost:3000\'` in playwright.config.ts, what full URL does `page.goto(\'/login\')` navigate to?',null,['http://localhost:3000login','http://localhost:3000//login','http://localhost:3000/login','It throws an error because the URL is not absolute'],2,'When `baseURL` is configured, Playwright prepends it to relative paths. `\'/login\'` becomes `\'http://localhost:3000/login\'`.'],
['O','A','What does `waitUntil: \'commit\'` mean in `page.goto()`?',null,['Waits for the git commit to complete','Waits until the first byte of the response is received (earliest possible)','Waits until all cookies are committed to storage','Waits until the service worker caches the response'],1,'`\'commit\'` is the earliest wait state — the response headers are received and the navigation is committed. The body hasn\'t loaded yet. Useful for very fast checks on response headers.'],
['O','I','What does `page.title()` return?',null,['The current URL string synchronously','A Promise that resolves to the document title string','The page\'s meta description','The H1 heading text of the page'],1,'`page.title()` is async and returns a Promise<string>. Always `await` it. For assertions, prefer `await expect(page).toHaveTitle()` which auto-retries.'],

// F×7 ─────────────────────────────────────────────────────────
['F','B','Fill in the method: `await page.______(\'/home\')`',null,['goto','navigate','open','visit'],0,'`page.goto()` is the primary navigation method in Playwright. It accepts a URL string and an optional options object.'],
['F','I','Fill in the option key: `await page.goto(\'/page\', { ______: \'networkidle\' })`',null,['waitUntil','waitFor','until','loadState'],0,'`waitUntil` controls which lifecycle event `goto()` waits for before resolving. Valid values: `\'commit\'`, `\'domcontentloaded\'`, `\'load\'`, `\'networkidle\'`.'],
['F','I','Fill in the method: `await page.______URL(\'/success\')`',null,['waitFor','assertFor','checkFor','pollFor'],0,'`page.waitForURL(pattern)` waits until the current URL matches the given string, regex, or glob. Essential after clicks that trigger redirects.'],
['F','B','Fill in the method to refresh the current page: `await page.______( )`',null,['reload','refresh','reset','restart'],0,'`page.reload()` refreshes the current page. It takes the same `{ waitUntil, timeout }` options as `page.goto()`.'],
['F','I','Fill in the method: `await page.______LoadState(\'networkidle\')`',null,['waitFor','await','check','poll'],0,'`page.waitForLoadState(state)` can be called at any point to wait for the page to reach a specific load state, independent of navigation.'],
['F','B','Fill in the matcher: `await expect(page).toHave______(\'/checkout\')`',null,['URL','Path','Location','Href'],0,'`toHaveURL()` is the auto-retrying URL assertion. It accepts a string, regex, or URL object.'],
['F','I','Fill in to get the current page title: `const t = await page.______( )`',null,['title','getTitle','pageTitle','documentTitle'],0,'`page.title()` is an async method that returns the current `document.title`. Always `await` it or use `expect(page).toHaveTitle()` instead.'],

// E×6 ─────────────────────────────────────────────────────────
['E','B','What is wrong?\n`page.goto(\'https://example.com\')`',null,['Missing `await` — `page.goto()` is async and returns a Promise','`goto` requires a second options argument','The URL must be relative, not absolute','`goto` is deprecated; use `page.navigate()` instead'],0,'Without `await`, the navigation starts but the test continues immediately — actions on the next line run against an unloaded or previous page.'],
['E','I','What is wrong with this check?\n`expect(page.url()).toBe(\'/dashboard\')`',null,['The URL check is done at one point in time with no retry — a redirect in progress will cause a false failure','`page.url()` must be awaited','`toBe` does not work with URL strings','`page.url()` returns a full URL; this comparison will always fail'],0,'This is a synchronous snapshot check with no retry. Use `await expect(page).toHaveURL(\'/dashboard\')` which polls until the URL matches or times out.'],
['E','B','What is wrong?\n`await page.goto(\'/page\', { waitUntil: \'idle\' })`',null,['`\'idle\'` is not a valid waitUntil value — use `\'networkidle\'`','`waitUntil` must be the first argument, not inside options','`goto` does not accept an options object','`waitUntil` requires a numeric millisecond value'],0,'The valid `waitUntil` strings are `\'commit\'`, `\'domcontentloaded\'`, `\'load\'`, and `\'networkidle\'`. `\'idle\'` throws a validation error.'],
['E','I','What is wrong?\n```\nawait page.getByRole(\'button\', { name: \'Submit\' }).click();\nconst url = page.url();\nexpect(url).toBe(\'/success\');\n```',null,['The URL is read immediately after click with no wait for the navigation to complete','`page.url()` cannot be called after a click','`expect` requires `await`','`getByRole` cannot find buttons by name'],0,'`page.url()` is synchronous but navigation after a click is async. The URL read happens before the redirect completes. Use `await page.waitForURL(\'/success\')` then assert.'],
['E','A','What is wrong with using `waitUntil: \'networkidle\'` on an app with persistent WebSocket connections?',null,['The goto will time out because WebSockets keep the network active indefinitely','WebSockets are not counted toward network activity','`networkidle` is only valid with HTTP/1.1','WebSocket connections require `waitUntil: \'websocket\'`'],0,'`networkidle` counts open connections including WebSockets. A live WebSocket means the 500 ms quiet window never occurs, causing a timeout. Use `\'load\'` or `\'domcontentloaded\'` instead.'],
['E','I','What is wrong?\n`await expect(page).toHaveURL(\'dashboard\')`',null,['The URL pattern `\'dashboard\'` will match any URL containing the substring — use `\'/dashboard\'` for an exact-path check','`toHaveURL` does not accept strings','`expect(page)` should be `expect(page.url())`','The string must be wrapped in a regex'],0,'`toHaveURL(\'dashboard\')` does a substring match. Pass `\'/dashboard\'` (leading slash) or a regex like `/\/dashboard$/` to avoid matching unintended URLs like `/admin/dashboard`.'],

// D×6 ─────────────────────────────────────────────────────────
['D','B','A test asserts the URL right after `page.goto()` returns, but the URL is still the old one. What is happening?',null,['The page performs a client-side redirect after load — add `await page.waitForURL(\'/final\')`','`page.goto()` does not update the URL','The `baseURL` is prepending the wrong host','The assertion is using `toBe` instead of `toHaveURL`'],0,'Server-rendered redirects are handled by `goto()`, but JavaScript-based redirects happen after the `load` event. Use `waitForURL()` to wait for the final destination.'],
['D','I','A test navigates back with `page.goBack()` but the assertion runs before the previous page loads. How do you fix it?',null,['`await page.goBack()` and then `await page.waitForLoadState(\'load\')`','Call `page.reload()` instead of `page.goBack()`','Add `{ waitUntil: \'domcontentloaded\' }` to `goto()`','Use `await page.waitForNavigation()` before `goBack()`'],0,'`goBack()` is async — always `await` it. If the previous page has dynamic content, follow up with `await page.waitForLoadState(\'load\')` or `await page.waitForLoadState(\'networkidle\')`.'],
['D','I','Tests pass locally but time out in CI because pages load slowly. What `page.goto()` option should be added?',null,['`{ timeout: 60_000 }` to increase the navigation timeout for this call','`{ waitUntil: \'commit\' }` to finish faster','`{ retries: 3 }` to retry on timeout','`{ slow: true }` to switch to slow mode'],0,'`{ timeout: 60_000 }` raises the navigation timeout to 60 seconds for that specific `goto()` call without changing the global config.'],
['D','B','A developer compares `page.title()` with `===` and always gets `false`. What is wrong?',null,['`page.title()` returns a Promise — comparing a Promise with `===` to a string always returns false','`page.title()` returns the meta description, not the `<title>` tag','`===` is not valid inside a Playwright test','`page.title()` is not a function in Playwright'],0,'`page.title()` is async. Without `await` you get a Promise object, and `Promise === \'My Title\'` is always `false`. Use `await page.title()` or `await expect(page).toHaveTitle(\'…\')`.'],
['D','A','An SPA loads content lazily after the `load` event. Tests pass locally (fast machine) but fail in CI. What is the best fix?',null,['After navigation, call `await page.waitForLoadState(\'networkidle\')` or wait for a specific element to appear','Use `waitUntil: \'load\'` in goto() options','Increase the global timeout only','Add a fixed `page.waitForTimeout(3000)` call'],0,'Waiting for `networkidle` or for a specific element (`await page.getByText(\'Results\').waitFor()`) is the correct approach. Fixed `waitForTimeout` sleeps are brittle and slow.'],
['D','I','`await page.waitForURL(\'/profile\')` times out even though the URL visually changes to `/profile/123`. What is wrong?',null,['The pattern must match the full URL — use `\'/profile/**\'` or `/\\/profile/` regex','`waitForURL` only accepts exact strings','The page must be reloaded after navigation','`waitForURL` cannot match after a click, only after goto()'],0,'`\'/profile\'` matches only the exact string. The actual URL is `/profile/123`. Use the glob `\'/profile/**\'` or the regex `/\\/profile/` to match the dynamic segment.'],

// C×5 ─────────────────────────────────────────────────────────
['C','B','Complete: `await page.______(\'/products\')`',null,['goto','navigate','open','visit'],0,'`page.goto()` is the standard navigation command. It awaits the configured `waitUntil` event before resolving.'],
['C','I','Complete for the fastest navigation wait: `await page.goto(\'/page\', { waitUntil: \'______\' })`',null,['domcontentloaded','load','networkidle','commit'],0,'`\'domcontentloaded\'` is faster than `\'load\'` because it doesn\'t wait for external resources. Ideal for server-rendered pages where you interact with DOM immediately.'],
['C','I','Complete the URL assertion: `await expect(page).toHave______(\'/cart\')`',null,['URL','Path','Href','Link'],0,'`toHaveURL()` is the auto-retrying URL matcher. It accepts a string, regex, or URL object and polls until it matches or times out.'],
['C','I','Complete to wait after a click causes a redirect: `await page.waitFor______(\'/success\')`',null,['URL','LoadState','Navigation','Redirect'],0,'`page.waitForURL(pattern)` is the canonical way to wait for the URL to change after an action. It supports strings, globs (`\'**/path**\'`), and regexes.'],
['C','A','Complete to wait for full page stability after navigation: `await page.waitForLoadState(\'______\')`',null,['networkidle','load','domcontentloaded','commit'],0,'`\'networkidle\'` waits until there are no open connections for 500 ms — the most thorough wait state, ideal for verifying that all async data has loaded.'],

// L×5 ─────────────────────────────────────────────────────────
['L','B','What is the key difference between `page.url()` and `page.title()`?',null,['`page.url()` is synchronous; `page.title()` is async and returns a Promise','Both are asynchronous; they just return different values','`page.url()` is deprecated; use `page.title()` instead','`page.url()` requires a page argument; `page.title()` does not'],0,'`page.url()` is synchronous — it reads from the cached current frame URL. `page.title()` is async because it queries the live `document.title` from the browser.'],
['L','I','When should you choose `\'domcontentloaded\'` over `\'networkidle\'`?',null,['When the page has no images','When you only need the HTML structure to interact with and don\'t need all resources loaded','When using Firefox instead of Chromium','When you are testing a REST API endpoint'],1,'`\'domcontentloaded\'` is ideal when you immediately interact with DOM elements rendered server-side. For SPAs that fetch data after load, `\'networkidle\'` or element-level waits are safer.'],
['L','I','What happens if `page.goto()` navigates to a URL that returns a 404 status?',null,['Playwright throws an error by default','The Promise resolves normally — goto() doesn\'t throw on non-2xx status','The test is automatically retried','The page stays on the current URL'],1,'By default `page.goto()` does not throw on HTTP error responses. It resolves with the Response object. You must explicitly assert the status with `expect(response.status()).toBe(200)` if needed.'],
['L','A','What is the correct chronological order of page load events?',null,['commit → DOMContentLoaded → load → networkidle','load → DOMContentLoaded → commit → networkidle','DOMContentLoaded → commit → networkidle → load','networkidle → load → DOMContentLoaded → commit'],0,'The order is: `commit` (first byte) → `DOMContentLoaded` (HTML parsed) → `load` (all resources) → `networkidle` (no active requests for 500 ms).'],
['L','I','Does `page.waitForURL()` accept regex patterns?',null,['Yes — string, regex, and glob patterns are all supported','No — only exact string matching','Yes — regex only, not strings','Only if the URL contains query parameters'],0,'`waitForURL()` accepts a string (substring match), a regex (`/\\/profile\\/\\d+/`), or a glob pattern (`\'**/checkout**\'`). Use the most specific form to avoid false matches.'],

// R×5 ─────────────────────────────────────────────────────────
['R','B','After clicking "Submit", a form redirects to a confirmation page. What is the best way to verify the user landed on the correct URL?',null,['`await page.waitForURL(\'/confirmation\')` followed by `await expect(page).toHaveURL(\'/confirmation\')`','`expect(page.url()).toBe(\'/confirmation\')`','`await page.reload()` then check the URL','`await page.waitForTimeout(1000)` then `page.url()`'],0,'`waitForURL()` handles the async redirect; `toHaveURL()` confirms the destination with auto-retry. The synchronous `page.url()` check and fixed waits are fragile alternatives.'],
['R','I','Your SPA fetches user data via API after the initial render. Tests fail because elements aren\'t populated yet. What is the recommended strategy?',null,['Use `await page.waitForLoadState(\'networkidle\')` after navigation, or wait for a specific element','Use `waitUntil: \'domcontentloaded\'` in goto()','Add a 2-second sleep after each navigation','Reload the page after navigating'],0,'`waitForLoadState(\'networkidle\')` waits for all fetch calls to finish. Even better: wait for the specific element you need — `await page.getByText(\'Welcome\').waitFor()`.'],
['R','I','You are testing that clicking the browser back button returns the user to the correct previous page. What Playwright API models this?',null,['`await page.goBack()` followed by `await expect(page).toHaveURL(\'/previous\')`','`await page.goto(page.url() - 1)`','`await page.history(-1)`','`await page.navigate(\'back\')`'],0,'`page.goBack()` is the direct equivalent of the browser\'s back button. Always `await` it, then assert the URL or visible content.'],
['R','A','Your app has long-running background polling every 2 seconds. Tests using `waitUntil: \'networkidle\'` always time out. What is the fix?',null,['Switch to `waitUntil: \'load\'` or `\'domcontentloaded\'` and wait for a specific element instead','Increase the timeout to 10 minutes','Disable background polling only during tests','Use `page.waitForResponse()` to wait for the polling response'],0,'Continuous background requests prevent `networkidle` from ever triggering. Use `\'load\'` + element-level waits instead, which are unaffected by background network activity.'],
['R','I','A test navigates to several pages and needs to confirm the title changes correctly on each. What is the best assertion?',null,['`await expect(page).toHaveTitle(\'Expected Title\')` after each navigation','`assert(await page.title() === \'Expected Title\')`','`expect(page.url()).toContain(\'title\')`','`await page.waitForTitle(\'Expected Title\')`'],0,'`toHaveTitle()` auto-retries and accepts strings or regexes, making it resilient to async title updates from client-side routing.'],

// S×4 ─────────────────────────────────────────────────────────
['S','I','A login test clicks Submit, then immediately checks `page.url()`. It fails because the URL still shows `/login`. How do you fix it?',null,['Add `await page.waitForURL(\'/dashboard\')` before reading the URL','Add `await page.reload()` after the click','Use `waitUntil: \'networkidle\'` in the original `page.goto()`','Use `page.waitForTimeout(500)` to let the redirect happen'],0,'The redirect is async. `page.waitForURL()` polls until the URL changes, so the subsequent URL check sees the correct value.'],
['S','A','In CI, `page.goto()` times out on a page that loads in under 5 seconds locally. The global timeout is 30 s. What steps should you take?',null,['Increase the per-call timeout: `{ timeout: 60_000 }`, and verify there are no network differences between local and CI (proxy, firewall)','Set `headless: false` in CI to speed up rendering','Add retries at the config level','Set `waitUntil: \'commit\'` for all navigations'],0,'Always first rule out environmental differences (proxy, DNS). Then increase the timeout for that specific call. Setting `\'commit\'` skips useful waiting and can cause subsequent failures.'],
['S','I','A test uses `await page.waitForLoadState(\'networkidle\')` after a form submit, but the page has an analytics script that sends a request 3 seconds after load. Tests are consistently slow. What is the better approach?',null,['Wait for the specific element you need instead: `await page.getByText(\'Order confirmed\').waitFor()`','Increase `timeout` for the waitForLoadState call','Block analytics requests using `page.route()`','Switch to `waitUntil: \'load\'`'],0,'Element-level waiting is faster and more precise than `networkidle`. Waiting for the element that proves success decouples the test from unrelated network activity.'],
['S','A','A developer checks `expect(await page.title()).toBe(\'Home\')` and it sometimes fails because the title is set dynamically by JavaScript after paint. What should replace this?',null,['`await expect(page).toHaveTitle(\'Home\')` — it auto-retries until the title matches','`await page.waitForTimeout(1000)` then `page.title()`','`await page.waitForFunction(() => document.title === \'Home\')`','`expect(page.title()).resolves.toBe(\'Home\')`'],0,'`expect(page).toHaveTitle()` polls until the `<title>` element matches, making it resilient to async title updates from JS frameworks. `waitForFunction()` also works but is more verbose.'],

// ── Additional batch ×50 ─────────────────────────────────────
['O','B','What does `page.goBack()` do?',null,['Navigates to the homepage','Triggers the browser\'s back navigation — equivalent to pressing the browser Back button','Reloads the previous state without navigation','Navigates to the parent URL path'],1,'page.goBack() navigates one step back in the browser history — always await it before asserting the resulting URL or content.'],
['O','B','What does `page.goForward()` do?',null,['Navigates to the next page in a wizard flow','Triggers the browser\'s forward navigation — moves forward one step in the history stack','Navigates to the next URL alphabetically','Goes forward only if a previous goBack() was called'],1,'goForward() is the complement of goBack() — it moves forward through the navigation history.'],
['O','I','What does `page.waitForResponse(url)` wait for?',null,['The page to finish loading','A specific network response matching the URL or predicate — returns the Response object when the match is received','The next page navigation','A WebSocket message'],1,'waitForResponse is useful for asserting API calls — you can match by URL string, glob, regex, or a predicate function that inspects the response.'],
['O','I','What does `page.waitForRequest(url)` return?',null,['A boolean indicating whether the request was made','A Promise<Request> that resolves when a network request matching the pattern is sent','A list of all requests made so far','A mocked response object'],1,'waitForRequest resolves with the Request object — you can then inspect method, URL, post data, and headers.'],
['O','B','What does `page.evaluate(fn)` do?',null,['Runs a Node.js function in the test process','Executes a JavaScript function inside the browser page context and returns the result to the test','Evaluates a CSS selector and returns matching elements','Runs a Python expression in the browser'],1,'evaluate() bridges Node.js (test) and browser (page) — the function runs in the browser\'s JavaScript context and the return value is serialised back to Node.'],
['O','I','What does `page.frames()` return?',null,['An array of all browser tabs','An array of all Frame objects in the page including the main frame and all iframes','An array of animation frames','The current call stack of the page'],1,'frames() gives you access to all frames — iterate to find a specific iframe by name or URL.'],
['O','B','What is the difference between `page.waitForSelector()` and `locator.waitFor()`?',null,['They are identical','waitForSelector is a legacy API returning an ElementHandle; locator.waitFor() is the modern way and integrates with the locator API — prefer locator.waitFor()','waitForSelector auto-retries but locator.waitFor() does not','waitForSelector works in frames but locator.waitFor() does not'],1,'locator.waitFor() is the modern replacement for waitForSelector — it stays in the locator world without dropping to ElementHandle.'],
['O','I','What does `page.mainFrame()` return?',null,['The primary browser window','The main Frame of the page (the top-level document frame)','The first visible frame on screen','The iframe with the highest z-index'],1,'mainFrame() is the top-level document — use it to distinguish from iframes when working with frames directly.'],
['O','B','What does `page.waitForTimeout(2000)` do and why is it discouraged?',null,['Waits for 2 seconds and auto-retries — recommended for loading states','Adds a fixed 2-second delay — discouraged because it makes tests slow and brittle (too short fails, too long wastes time). Use element-level waits instead.','Waits up to 2 seconds for the page to load','Retries the last action for 2 seconds'],1,'Fixed waits are an anti-pattern — use waitFor() on the element you actually need and let Playwright\'s auto-wait handle timing.'],
['O','I','What does `page.addScriptTag({ url })` do?',null,['Navigates to the script URL','Injects an external script tag into the current page — the script is fetched and executed','Mocks a script resource','Blocks a script from loading'],1,'addScriptTag is useful for injecting polyfills or testing libraries into a live page during a test.'],
['O','A','What does `page.waitForFunction(fn)` wait for?',null,['A specific URL pattern','Executes fn in the browser and polls until it returns a truthy value — generalised waiting for any browser-side condition','Waits for the next requestAnimationFrame','Waits for a DOM mutation'],1,'waitForFunction is the escape hatch for complex waiting conditions that don\'t fit the standard locator or load state APIs.'],
['O','I','What is the return type of `page.goto(url)`?',null,['void','Promise<Response | null> — the HTTP response for the navigation, or null for navigations like data: URLs','Promise<boolean> indicating success','Promise<string> of the final URL'],1,'goto() returns the Response so you can immediately assert status, headers, or URL without additional calls.'],
['F','B','Complete to navigate to a URL and wait for network idle.\n```ts\nawait page.goto(\'/products\', { ______: \'networkidle\' });\n```',null,['waitUntil','wait','state','loadState'],0,'waitUntil is the option controlling which page load event triggers goto() to resolve.'],
['F','B','Complete to go back one step in browser history.\n```ts\nawait page.______();\n```',null,['goBack','back','navigate(-1)','historyBack'],0,'page.goBack() is the Playwright API for browser back navigation.'],
['F','I','Complete to execute JavaScript in the browser and return its result.\n```ts\nconst title = await page.______(() => document.title);\n```',null,['evaluate','exec','run','inject'],0,'page.evaluate() runs the function in the browser context and returns the serialised result.'],
['F','I','Complete to wait for a specific response.\n```ts\nconst [response] = await Promise.all([\n  page.______(\'/api/products\'),\n  page.getByRole(\'button\', { name: \'Load\' }).click(),\n]);\n```',null,['waitForResponse','waitForRequest','waitForAPI','waitForFetch'],0,'waitForResponse resolves with the Response matching the pattern — wrap it with the triggering action in Promise.all.'],
['F','B','Complete to reload the current page.\n```ts\nawait page.______();\n```',null,['reload','refresh','goTo(page.url())','restart'],0,'page.reload() triggers a full page refresh.'],
['F','I','Complete to get the page\'s outer HTML.\n```ts\nconst html = await page.______();\n```',null,['content()','html()','source()','getHTML()'],0,'page.content() returns the full serialised HTML of the current page.'],
['F','I','Complete to wait for navigation triggered by a button click.\n```ts\nawait Promise.all([\n  page.______(),\n  page.getByRole(\'link\', { name: \'Next\' }).click(),\n]);\n```',null,['waitForNavigation','waitForURL','waitForLoad','waitForRedirect'],0,'waitForNavigation() is the classic pattern for click-triggered navigations — but prefer waitForURL() when you know the target URL.'],
['E','B','What is wrong?\n```ts\nawait page.click(\'button\');\nconst url = page.url();\nexpect(url).toBe(\'/dashboard\');\n```',null,['click() is deprecated','The redirect happens asynchronously — page.url() is read immediately after click() before the redirect completes. Use waitForURL before reading the URL.','url must be awaited','expect() cannot take a string directly'],1,'Click triggers a navigation but does not wait for it — always waitForURL() or let auto-wait assertions handle timing.'],
['E','I','What is wrong?\n```ts\nconst result = await page.evaluate(async () => {\n  const resp = await fetch(\'/api/data\');\n  return resp.json();\n});\n```',null,['evaluate cannot contain async functions','evaluate() can contain async functions BUT the return value must be serialisable to JSON — this is fine, though you should ensure the API endpoint is available','fetch is not available inside evaluate','page.evaluate() runs synchronously'],1,'This pattern is valid — evaluate can use fetch and async/await. The main constraints are that the return value must be JSON-serialisable and the function runs in the browser\'s JS context.'],
['E','B','What is wrong?\n```ts\nawait page.goto(\'/items\');\nawait page.waitForLoadState(\'networkidle\');\nawait page.waitForLoadState(\'networkidle\'); // called twice\n```',null,['waitForLoadState cannot be called twice on the same page','The second call waits for a NEW networkidle event — if no new navigation happens, it waits and eventually times out. Call it only once after navigation.','networkidle is not a valid load state','waitForLoadState returns a one-shot promise'],1,'waitForLoadState waits for the next occurrence of the state — if the page is already idle, the first call resolves immediately, but the second waits indefinitely.'],
['E','I','What is wrong?\n```ts\nawait page.goto(\'https://example.com\');\nconst frame = page.frame(\'sidebar\');\nawait frame.click(\'button\'); // frame might be null\n```',null,['frame() requires an index, not a name','page.frame(name) returns null if no frame with that name exists — you should check for null before interacting: if (!frame) throw new Error(\'sidebar frame not found\')','frame() must be awaited','click() on a Frame is not supported'],1,'Always guard against null from frame() — a missing iframe causes a null dereference error.'],
['E','A','What is wrong?\n```ts\nconst [request] = await Promise.all([\n  page.waitForRequest(\'/api/save\'),\n  page.getByRole(\'button\', { name: \'Save\' }).click(),\n]);\n// request is used here\n```',null,['Promise.all cannot mix waitFor and click','This pattern is correct — waitForRequest and click() run concurrently, so the request is captured even if it completes quickly after click. Nothing is wrong.','waitForRequest must come after click','Promise.all requires three or more elements'],1,'This is the correct pattern — starting waitForRequest before click() ensures no race condition even if the request completes very quickly.'],
['E','I','What is wrong?\n```ts\nawait page.evaluate((data) => {\n  fetch(\'/api\', { method: \'POST\', body: JSON.stringify(data) });\n  // no await\n}, { name: \'test\' });\n```',null,['evaluate() cannot accept arguments','The fetch call is not awaited inside evaluate — the function returns before the fetch completes. Use async function and await the fetch, or use page.request.post() instead.','body must be a FormData object','JSON.stringify is not available in evaluate'],1,'Unawaited Promises inside evaluate are fire-and-forget — evaluate() resolves before the fetch finishes. Always await async operations inside evaluate.'],
['D','B','`page.goto("/products")` resolves but the product list is empty. Tests that assert on product items fail. What should you do?',null,['Reload the page','The products are fetched asynchronously after initial render — wait for a product item to be visible: `await page.getByRole("listitem").first().waitFor()`','Switch to waitUntil: "networkidle"','Increase the goto timeout'],0,'Waiting for specific content is more precise than networkidle — it confirms the data is rendered, not just that requests stopped.'],
['D','I','`page.waitForResponse("/api/users")` never resolves. What are two things to check?',null,['The API is down','1) Verify the request URL matches exactly — use a glob or regex if the URL has query parameters: "**\/api\/users**". 2) Ensure the action that triggers the request is called AFTER waitForResponse is set up (use Promise.all).','Add retry: true to waitForResponse','Increase the timeout to 60 seconds'],0,'URL mismatch and wrong Promise ordering are the two most common causes of waitForResponse hanging.'],
['D','B','`page.evaluate()` throws "TypeError: fn is not a function". What is the likely cause?',null,['evaluate() only accepts strings','The function was accidentally serialised incorrectly — ensure you pass an arrow function or function expression directly: page.evaluate(() => ...) not page.evaluate(myVar) where myVar might be undefined','evaluate() requires async functions only','The browser context is closed'],1,'evaluate() takes a function literal or a serialisable function — passing an undefined variable or a non-function value causes this error.'],
['D','A','Tests that use `page.on("dialog", dialog => dialog.accept())` handle the first dialog but subsequent dialogs are not handled. Why?',null,['page.on only fires once','page.on("dialog", handler) registers a persistent listener — but if the handler calls dialog.accept() synchronously and another dialog appears before the next event loop tick, it may be missed. Use dialog.dismiss() with a guard, or register the listener in beforeEach.','Dialogs after the first require dialog.waitFor()','The listener is removed after the first dialog'],0,'dialog event listeners persist — but timing issues can arise if dialogs appear very quickly in sequence. Registering in beforeEach ensures fresh listeners per test.'],
['D','I','`page.frames()` returns only one frame even though the page has two iframes. Why?',null,['frames() only counts visible iframes','The iframes may not have finished loading — wait for them to be attached: await page.waitForSelector("iframe") before calling frames(). Also, cross-origin iframes may require a different approach.','frames() is limited to one result in headless mode','iframes are counted as separate pages'],1,'waitForSelector on the iframe elements ensures they are attached to the DOM before frames() is called.'],
['C','B','Complete to execute JavaScript in the browser.\n```ts\nawait page.______(() => window.scrollTo(0, 500));\n```',null,['evaluate','run','exec','inject'],0,'page.evaluate() executes the function in the browser\'s JavaScript context.'],
['C','I','Complete to wait for a network response before proceeding.\n```ts\nconst [res] = await Promise.all([\n  page.______("**/api/cart"),\n  page.getByRole("button", { name: "Add" }).click(),\n]);\n```',null,['waitForResponse','waitForRequest','on("response",','waitForNavigation'],0,'waitForResponse with a glob pattern captures the cart API response triggered by the button click.'],
['C','B','Complete to get the inner text of the page body.\n```ts\nconst text = await page.______("body");\n```',null,['innerText','textContent','getByText','content'],0,'page.innerText(selector) returns the rendered text of the matching element — equivalent to element.innerText.'],
['C','I','Complete to wait for the page URL to match.\n```ts\nawait page.______(/\\/dashboard/);\n```',null,['waitForURL','waitForNavigation','assertURL','checkURL'],0,'page.waitForURL() accepts a string, glob, or regex and resolves when the current URL matches.'],
['C','A','Complete to inject an argument into page.evaluate.\n```ts\nconst id = 42;\nawait page.______(userId => {\n  window.currentUserId = userId;\n}, ________);\n```',null,['evaluate, id','exec, id','run, id','eval, id'],0,'The second argument to evaluate() is passed as the first argument to the browser function — it must be JSON-serialisable.'],
['L','B','Why should you avoid `page.waitForTimeout()` in production tests?',null,['It is not supported in Firefox','Fixed waits are brittle — too short fails on slow machines, too long wastes time. Playwright\'s locator.waitFor() and auto-wait assertions adapt to actual timing.','It blocks the Node.js event loop','page.waitForTimeout() was removed in Playwright 1.30'],1,'Dynamic waits tied to actual DOM state are always preferable to arbitrary delays.'],
['L','I','What is the difference between `page.evaluate()` and `page.evaluateHandle()`?',null,['They are identical','evaluate() returns a serialised JS value (string, number, object); evaluateHandle() returns a JSHandle — a reference to an object in the browser that is not serialised. Use evaluateHandle when the return value is not serialisable.','evaluateHandle() runs in Node.js; evaluate() runs in the browser','evaluateHandle() is deprecated'],1,'evaluateHandle is for passing browser-side object references between evaluate calls — useful for complex DOM manipulations.'],
['L','I','Can `page.evaluate()` access Node.js variables from the test scope directly?',null,['Yes — evaluate shares scope with the test','No — evaluate runs in the browser\'s JavaScript context, completely isolated from Node.js. Pass variables as the second argument to evaluate().','Yes, but only primitives','Yes, if the variable is declared with const'],1,'Scope isolation is fundamental — the test is Node.js, evaluate is browser. Bridge them via the args parameter.'],
['L','B','What is the recommended way to handle browser `alert()` dialogs in Playwright?',null,['Playwright dismisses them automatically','Register a handler with page.on("dialog", dialog => dialog.accept()) BEFORE the action that triggers the dialog','Use page.waitForDialog() after the triggering action','Dialogs block Playwright and must be avoided'],1,'The dialog event fires synchronously with the browser\'s dialog — the handler must be registered before the triggering action or the dialog will hang.'],
['L','A','When is `Promise.all([page.waitForNavigation(), page.click(link)])` necessary vs just `await page.click(link)`?',null,['Always — click() never waits for navigation','When the click triggers a full-page navigation, Promise.all captures it reliably. However, Playwright\'s auto-wait on locator.click() covers most cases — waitForNavigation is mainly needed for legacy page.click() or when click triggers a delayed navigation.','Never — waitForNavigation is deprecated','Only in Firefox which doesn\'t auto-wait'],1,'Modern locator.click() auto-waits for stability and common navigation patterns — waitForNavigation is mainly a legacy pattern, still useful for complex or delayed navigations.'],
['R','B','You need to test that clicking a link opens a new tab. How do you capture the new page?',null,['page.on("popup", handler) captures the new tab — or use const [newPage] = await Promise.all([context.waitForEvent("page"), link.click()])','Playwright cannot test new tabs','Use page.newPage() to pre-create the tab','Use window.open() mock'],0,'context.waitForEvent("page") resolves with the new Page when a popup/new tab opens — combine with the triggering click in Promise.all.'],
['R','I','You need to test that downloading a file saves correctly. What Playwright API handles downloads?',null,['page.waitForDownload() combined with the triggering click — then access download.path() to verify the file','page.waitForResponse() with a binary content check','page.route() to intercept the download','page.evaluate() to check navigator.downloads'],0,'page.waitForEvent("download") (or context.waitForEvent) captures the download — await download.saveAs(path) then verify the file.'],
['R','I','After a form submission, an email is sent. You need to verify the email contains the right content in a test. What is the cleanest approach?',null,['Wait 30 seconds for the email to arrive','Use a test email service (Mailhog, Mailosaur, or MailTrap) and make an API call to check the inbox after the form submission — Playwright handles the UI, the API call verifies the email','Use page.evaluate() to intercept SMTP','Check the database for the email record instead'],1,'Email testing is an integration concern — a test email service with API access is the standard approach for verifying email content in E2E tests.'],
['R','A','You need to test that the app correctly requests camera permission and shows a preview when granted. How?',null,['Mock navigator.mediaDevices in evaluate()','Grant the camera permission in context options: { permissions: ["camera"] }, then navigate to the feature. Intercept getUserMedia with addInitScript to return a fake MediaStream to avoid real camera access.','Use headless: false so the real camera works','Camera permission cannot be tested in Playwright'],1,'Combine grantPermissions with an addInitScript mock of getUserMedia — this tests the permission flow and UI without requiring actual camera hardware.'],
['R','B','You need to take a screenshot at a specific point in a long test for debugging. What is the API?',null,['test.screenshot()','await page.screenshot({ path: "debug.png" })','await page.capture()','test.info().screenshot()'],1,'page.screenshot() captures the current state — pass path to save to disk, or omit for a Buffer. Attach to the report with test.info().attach().'],
['S','B','A test must interact with a `<select>` dropdown inside an iframe. How do you access it?',null,['iframes are not accessible in Playwright','Get the frame with const frame = page.frameLocator("iframe") or page.frame({ name: "myFrame" }), then use frame\'s locator methods normally','Use page.evaluate to access the iframe DOM directly','Use context.frames() and spread into separate pages'],0,'frameLocator("iframe") returns a FrameLocator that scopes all locator calls to the iframe — use it like a normal locator.'],
['S','I','A test clicks a "Delete" button that shows a native browser confirm() dialog. Without a dialog handler, what happens?',null,['Playwright clicks OK automatically','The dialog blocks — Playwright times out waiting because the browser is stuck on the modal dialog. Always register page.on("dialog") before actions that trigger dialogs.','The dialog is dismissed automatically','confirm() dialogs are ignored in headless mode'],1,'Dialog handlers must be registered proactively — Playwright cannot click OK on a dialog that wasn\'t anticipated.'],
['S','A','A SPA uses hash-based routing (#/products). `page.waitForURL` with a regex doesn\'t match. What is wrong?',null,['Hash routing requires a different API','Hash changes don\'t trigger a new navigation event — they use the hashchange browser event. Use page.waitForURL(/#\\/products/) — Playwright does support hash fragments in URL matching. If still failing, verify the regex escapes the # correctly.','Use page.goto with hash fragment separately','waitForURL cannot match hash fragments'],1,'Playwright includes the hash in URL matching — double-check the regex syntax and escaping if matches fail.'],
['S','I','Tests that navigate between 10 pages are slow. Each navigation awaits networkidle. How do you speed them up?',null,['Increase workers to run navigations in parallel','Replace networkidle waits with element-specific waits — await the element that proves the page is ready rather than waiting for all network activity to cease. networkidle on data-heavy SPAs adds 500ms+ per navigation.','Use goto with waitUntil: "commit" everywhere','Disable images in context options'],0,'Element-level waits are faster and more accurate — they resolve as soon as the content you need is visible, not when all background activity stops.'],
],
// ── 5. Locators & Selectors ───────────────────────────────────
locators: [
// O×12 ─────────────────────────────────────────────────────────
['O','B','What is the recommended first-choice locator strategy in Playwright?',null,['page.locator() with a CSS class','page.getByRole() using ARIA roles','page.getByTestId() for all elements','page.locator() with XPath'],1,'`getByRole()` is the top priority because it matches elements the same way screen readers and ARIA do — meaningful, user-centric, and resilient to styling changes.'],
['O','B','What does `page.getByLabel(\'Email address\')` find?',null,['The <label> element with that text','The input or control associated with the label containing "Email address"','Any element whose text is "Email address"','A form with an aria-label of "Email address"'],1,'`getByLabel()` traverses the label→input association (via `for`/`id`, wrapping, or `aria-labelledby`) and returns the form control, not the label element itself.'],
['O','I','What does `page.getByRole(\'button\', { name: \'Submit\' })` find?',null,['Any element with the CSS class "button" and text "Submit"','A button element with accessible name "Submit"','A button with id="Submit"','An input of type="button" only'],1,'`getByRole()` uses ARIA semantics. The `name` option matches the accessible name — which can come from visible text, `aria-label`, or `aria-labelledby`.'],
['O','B','What HTML attribute does `page.getByTestId(\'checkout-btn\')` look for by default?',null,['id','data-testid','aria-label','data-pw'],1,'By default `getByTestId()` queries `data-testid`. This can be changed globally via `testIdAttribute` in playwright.config.ts.'],
['O','I','What does `locator.filter({ hasText: \'Sale\' })` do?',null,['Replaces the locator with one that only matches elements containing "Sale"','Clicks the first element containing "Sale"','Adds "Sale" as a CSS class filter','Waits until an element contains "Sale"'],0,'`.filter()` narrows an existing locator set to only those elements that contain the specified text or child locator — without changing the matched element type.'],
['O','B','What does `page.getByAltText(\'Company logo\')` match?',null,['An element with aria-label="Company logo"','An element with alt="Company logo" (typically an <img>)','Any element whose visible text is "Company logo"','A <figure> element with a caption "Company logo"'],1,'`getByAltText()` finds elements by their `alt` attribute, used primarily on `<img>` and `<input type="image">` elements.'],
['O','I','What happens when a Playwright locator matches multiple elements and you call `.click()` on it?',null,['It clicks the first matched element automatically','It throws a "strict mode violation" error','It clicks all matched elements','It waits until only one element matches'],1,'Playwright\'s strict mode requires a locator to resolve to exactly one element before performing an action. Narrow with `.first()`, `.nth()`, `.filter()`, or a more specific selector.'],
['O','B','What does `locator.nth(2)` return?',null,['The second element (1-indexed)','The third element (0-indexed)','The element 2 pixels from the top','The element at CSS nth-child(2)'],1,'`.nth()` is 0-indexed. `.nth(0)` = first, `.nth(1)` = second, `.nth(2)` = third, and so on.'],
['O','I','What is the key advantage of `page.getByRole(\'button\')` over `page.locator(\'.btn\')`?',null,['getByRole is faster to execute','getByRole is resilient to CSS class renames; .locator(\'.btn\') breaks if the class changes','getByRole only matches visible elements','locator() is deprecated in favour of getByRole'],1,'Semantic locators are coupled to meaning, not implementation. CSS class names are implementation details that often change during refactoring.'],
['O','I','What does chaining `page.locator(\'.card\').getByRole(\'button\', { name: \'Buy\' })` do?',null,['Finds all buttons on the page named "Buy"','Searches for a "Buy" button only within elements that match .card','Searches for .card elements inside a "Buy" button','Throws an error — chaining locators is not supported'],1,'Chaining scopes the inner locator search to the matched set of the outer locator — like a CSS descendant selector but semantic.'],
['O','A','When is a Playwright locator evaluated against the DOM?',null,['At the moment the locator is created','Only when an action or assertion is called on it (lazy evaluation)','When the test file is compiled','After every page navigation'],1,'Locators are lazy by design. Creating `page.getByRole(\'button\')` does nothing — the DOM query runs (and auto-waits) only when you call `.click()`, `.fill()`, `expect(locator).toBeVisible()`, etc.'],
['O','B','What does `page.getByText(\'Welcome\', { exact: false })` do differently from `page.getByText(\'Welcome\')`?',null,['exact: false ignores whitespace only','exact: false performs a case-insensitive substring match; default exact: true requires the full visible text to match','exact: false matches by regex','exact: false searches all attributes, not just text'],1,'By default `getByText()` does a full visible-text match. `{ exact: false }` matches any element whose text content *contains* "Welcome" (case-insensitive).'],

// F×7 ─────────────────────────────────────────────────────────
['F','B','Fill in to locate a button by its accessible name: `page.______Role(\'button\', { name: \'Login\' })`',null,['getBy','findBy','selectBy','queryBy'],0,'`page.getByRole()` is the primary semantic locator. It takes an ARIA role string and an optional options object.'],
['F','B','Fill in the option to match the accessible name: `page.getByRole(\'textbox\', { ______: \'Email\' })`',null,['name','text','label','title'],0,'The `name` option in `getByRole()` matches the element\'s accessible name — from visible text, `aria-label`, or `aria-labelledby`.'],
['F','I','Fill in the method to narrow by text: `page.getByRole(\'listitem\').______({ hasText: \'Featured\' })`',null,['filter','where','narrow','find'],0,'`.filter({ hasText })` is the standard way to narrow a locator set. It keeps only elements whose subtree contains the specified text.'],
['F','B','Fill in to get the third match (0-indexed): `page.getByRole(\'row\').______( 2 )`',null,['nth','item','at','index'],0,'`.nth(n)` is 0-indexed. `.nth(2)` returns the third matched element.'],
['F','I','Fill in the XPath prefix: `page.locator(\'______=//button[@type="submit"]\')`',null,['xpath','xp','query','path'],0,'Playwright uses the `xpath=` prefix to distinguish XPath expressions from CSS selectors inside `page.locator()`.'],
['F','B','Fill in to get the last matching element: `page.getByRole(\'option\').______( )`',null,['last','end','final','tail'],0,'`.last()` is a shorthand for `.nth(-1)` or `.nth(count - 1)` — it returns the final element in the matched set.'],
['F','A','Fill in to combine two conditions: `page.getByRole(\'button\').______(page.getByTitle(\'Download\'))`',null,['and','also','with','plus'],0,'`.and(locator)` creates a new locator that matches elements satisfying *both* conditions — the role constraint AND the title constraint.'],

// E×6 ─────────────────────────────────────────────────────────
['E','B','What is wrong with this locator?\n`page.getByRole(\'button\', { text: \'Submit\' })`',null,['The option key should be `name`, not `text`','ARIA role "button" is not valid','`getByRole` does not accept an options object','Accessible names must be uppercase'],0,'`getByRole()` uses `{ name: \'…\' }` to filter by accessible name. `{ text }` is not a recognised option and will be silently ignored, matching all buttons instead.'],
['E','I','What is wrong with this XPath?\n`page.locator(\'xpath=/html/body/div/button\')`',null,['Absolute XPath from the root is fragile — use relative `xpath=//button` instead','XPath must not start with xpath=','`/html/body` is not valid XPath','page.locator() does not support XPath'],0,'Absolute XPaths break whenever any ancestor element changes. Always use `//` to write relative XPath expressions.'],
['E','I','A locator matches 5 `<li>` elements. `await locator.click()` is called. What error occurs?',null,['Strict mode violation: locator matched 5 elements — narrow it to one','It clicks all 5 elements sequentially','It logs a warning but clicks the first','It automatically waits until only 1 matches'],0,'Playwright\'s strict mode throws when an action locator resolves to more than one element. Use `.first()`, `.nth()`, `.filter()`, or a more specific selector.'],
['E','I','What is wrong?\n`await page.getByText(\'Add to cart\', { exact: true }).click()`\nwhen the button text is "Add to cart →"?',null,['exact: true requires the full visible text to match — "Add to cart" won\'t match "Add to cart →"','getByText cannot target buttons','exact: true is the default and cannot be changed','The arrow character is invalid in text matchers'],0,'With `exact: true` (the default), the entire visible text must match. Since the button says "Add to cart →", the locator finds nothing and times out. Use `{ exact: false }` or include the arrow.'],
['E','A','What is problematic about this locator for selecting the third product?\n`page.locator(\':nth-child(3)\')`',null,['`:nth-child(3)` selects by DOM position — if items are reordered, the wrong element is targeted','CSS nth-child is not supported in Playwright','`:nth-child(3)` selects the parent, not the child','The colon prefix is invalid in page.locator()'],0,'`:nth-child()` is a CSS structural pseudo-class that relies on DOM position. Use `page.getByRole(\'listitem\').nth(2)` which is semantic and 0-indexed.'],
['E','I','What is wrong?\n`const el = await page.$(\'.submit-btn\'); await el.click()`',null,['`page.$()` is deprecated — use `page.locator(\'.submit-btn\').click()` instead','`.click()` cannot be called on an ElementHandle','`await` is not valid on `page.$()`','CSS selectors are not supported on page.$()'],0,'`page.$()` returns an ElementHandle, which is a deprecated API. Modern Playwright uses locators, which auto-wait and re-query the DOM on every action.'],

// D×6 ─────────────────────────────────────────────────────────
['D','B','`page.getByRole(\'button\')` matches 4 buttons and the test throws a strict mode error. How do you target just the "Add to cart" button?',null,['`page.getByRole(\'button\', { name: \'Add to cart\' })`','`page.getByRole(\'button\').first()`','`page.locator(\'button:first-child\')`','`page.getByRole(\'button\').filter({ count: 1 })`'],0,'Adding `{ name: \'Add to cart\' }` to `getByRole()` filters by accessible name, resolving the ambiguity without needing positional tricks.'],
['D','I','A test breaks after a CSS refactor renamed `.user-name` to `.profile-name`. What is the most resilient replacement?',null,['`page.getByTestId(\'user-name\')` or `page.getByRole(\'heading\', { name: … })` depending on the element','`page.locator(\'.profile-name\')`','`page.locator(\'div > span.name\')`','`page.locator(\'xpath=//span[@class]\')` '],0,'Semantic locators (`getByRole`, `getByLabel`, `getByTestId`) don\'t depend on CSS class names and survive styling refactors. CSS locators must be updated whenever classes change.'],
['D','I','A list has 10 "Delete" buttons. The test must delete the item whose row contains "Invoice #42". What is the correct approach?',null,['`page.getByRole(\'row\').filter({ hasText: \'Invoice #42\' }).getByRole(\'button\', { name: \'Delete\' }).click()`','`page.getByRole(\'button\', { name: \'Delete\' }).nth(42).click()`','`page.getByText(\'Invoice #42\').getByRole(\'button\').click()`','`page.locator(\'[data-invoice="42"] button\').click()`'],0,'Chain `.filter({ hasText })` to scope the row, then `.getByRole(\'button\')` inside it. This is both semantic and resilient to list reordering.'],
['D','A','`page.getByRole(\'listitem\').nth(3)` keeps selecting the wrong product after the team adds a "Featured" item at the top. How do you fix it?',null,['Use `.filter({ hasText: \'product name\' })` instead of `.nth()` to select by content rather than position','Increment nth to 4','Use `.nth(-1)` to count from the end','Add a CSS class to the 4th item'],0,'Position-based selectors (`nth`) break when items are inserted or reordered. Content-based selectors (`.filter({ hasText })`) are stable regardless of order.'],
['D','I','A developer writes `page.getByLabel(\'Email\')` but the input has no `<label>` — it only has `placeholder="Email"`. The locator finds nothing. What is the fix?',null,['Replace with `page.getByPlaceholder(\'Email\')`','Use `page.locator(\'input\')`','Add `{ exact: false }` to getByLabel','Use `page.getByRole(\'textbox\')`'],0,'`getByLabel` requires a `<label>` element or `aria-label`. When only a placeholder exists, `getByPlaceholder()` is the correct semantic alternative.'],
['D','B','A test creates the locator `const btn = page.getByRole(\'button\', { name: \'Save\' })` at the top and reuses it after a modal re-renders the button. Does this cause stale element errors?',null,['No — Playwright locators re-query the DOM on every action, so re-rendered elements are found fresh','Yes — locators cache the element reference and go stale after re-render','Yes — the locator must be recreated after any DOM change','No — but only in Chromium; Firefox uses stale references'],0,'Unlike `ElementHandle`, Playwright locators are lazy and re-evaluate the query each time an action is performed. Stale element errors are an `ElementHandle` problem, not a locator problem.'],

// C×5 ─────────────────────────────────────────────────────────
['C','B','Complete to find the H1 heading: `page.______Role(\'heading\', { level: 1 })`',null,['getBy','findBy','locateBy','selectBy'],0,'`page.getByRole(\'heading\', { level: 1 })` finds `<h1>` elements using ARIA semantics. The `level` option maps to `aria-level`.'],
['C','I','Complete to narrow a list: `page.getByRole(\'listitem\').______({ hasText: \'Featured\' })`',null,['filter','where','narrow','reduce'],0,'`.filter({ hasText })` reduces the matched set to only elements whose subtree contains the given text.'],
['C','I','Complete to scope the search: `page.locator(\'.sidebar\').______Role(\'link\')`',null,['getBy','findBy','selectBy','locateBy'],0,'`locator.getByRole()` chains a semantic locator inside the parent. The search is scoped to descendants of `.sidebar`.'],
['C','I','Complete to pick the second item (0-indexed): `page.getByRole(\'row\').______( 1 )`',null,['nth','item','at','eq'],0,'`.nth(1)` is 0-indexed, returning the second matched element. `.nth(0)` = first, `.nth(1)` = second.'],
['C','A','Complete to find a button that ALSO has a title attribute: `page.getByRole(\'button\').______(page.getByTitle(\'Save changes\'))`',null,['and','also','plus','with'],0,'`.and(locator)` intersects two locators — the result matches elements satisfying both the role condition and the title condition simultaneously.'],

// L×5 ─────────────────────────────────────────────────────────
['L','B','What is the recommended priority order for Playwright locator strategies?',null,['getByRole → getByLabel → getByText → getByTestId → CSS → XPath','getByTestId → CSS → getByRole → getByLabel','CSS → XPath → getByRole → getByLabel','getByText → getByRole → CSS → XPath'],0,'Playwright\'s official guidance: role → label → placeholder → text → alt → title → testId → CSS/XPath. Higher-priority strategies are more resilient to refactoring.'],
['L','I','When should you prefer `getByTestId()` over `getByRole()`?',null,['Always — testId is the most stable','When the element has no meaningful ARIA role or natural accessible name and a `data-testid` is added specifically for testing','When the element is hidden','When running tests in Firefox only'],1,'`getByTestId` is a deliberate test hook for elements that lack a natural role or label — e.g. complex custom components. Prefer semantic locators when available.'],
['L','I','What is the difference between `.filter({ hasText })` and chaining `parent.getByRole()`?',null,['filter() narrows the matched set by a text condition; chaining scopes the inner search within the parent\'s subtree','They are identical','filter() is faster; chaining is more readable','Chaining only works with getByRole; filter works with all locators'],0,'`.filter()` post-processes an existing set (e.g. "of these rows, keep only those containing \'Sale\'"). Chaining creates a fresh search scoped to the parent\'s DOM subtree.'],
['L','A','Are Playwright locators evaluated eagerly (at creation) or lazily (at action time)?',null,['Eagerly — the DOM query runs when the locator is created','Lazily — the DOM query runs only when an action or assertion is performed','It depends on whether the element is in the viewport','Eagerly for CSS selectors; lazily for semantic locators'],1,'All Playwright locators are lazy. Creating `page.getByRole(\'button\')` does zero DOM work. The query runs (and auto-waits) the moment you call `.click()`, `.fill()`, or `expect(…)`.'],
['L','I','What does `.and(anotherLocator)` return?',null,['A locator that matches elements satisfying BOTH conditions','A locator that matches elements satisfying EITHER condition','An array of all matched elements','A boolean indicating whether both locators match the same element'],0,'`.and()` is an intersection — equivalent to a CSS multi-selector like `button[title="Save"]`. Both conditions must be true for an element to match.'],

// R×5 ─────────────────────────────────────────────────────────
['R','B','A form has an "Email address" label linked to an `<input>`. What is the best locator?',null,['`page.getByLabel(\'Email address\')`','`page.locator(\'input[type="email"]\')`','`page.getByPlaceholder(\'Email address\')`','`page.locator(\'#email\')`'],0,'`getByLabel()` uses the label→input association — exactly how a real user navigates the form. It survives input type changes and id renames.'],
['R','I','An image gallery has 10 images each with unique `alt` text. How do you locate the image with alt "Golden Gate Bridge"?',null,['`page.getByAltText(\'Golden Gate Bridge\')`','`page.locator(\'img[alt="Golden Gate Bridge"]\')`','`page.getByRole(\'img\', { name: \'Golden Gate Bridge\' })`','Both A and C are correct'],3,'Both `getByAltText()` and `getByRole(\'img\', { name })` work — they are functionally equivalent for images. `getByAltText()` is more concise; `getByRole()` is more explicit about the ARIA role.'],
['R','I','A table has 20 rows, each with a "Delete" button. How do you delete the row for "Alice Johnson"?',null,['`page.getByRole(\'row\').filter({ hasText: \'Alice Johnson\' }).getByRole(\'button\', { name: \'Delete\' }).click()`','`page.getByRole(\'button\', { name: \'Delete\' }).nth(2).click()`','`page.getByText(\'Alice Johnson\').locator(\'../button\').click()`','`page.locator(\'tr:has-text("Alice Johnson") button\').click()`'],0,'Filter the row by content, then locate the action button within it. This is semantic, readable, and resilient to row reordering.'],
['R','A','A dynamic list reorders items on every page load. A test must always select the item with the text "Priority Task". What locator is correct?',null,['`page.getByRole(\'listitem\').filter({ hasText: \'Priority Task\' })`','`page.getByRole(\'listitem\').nth(0)`','`page.locator(\'li:first-child\')`','`page.getByRole(\'listitem\').first()`'],0,'Content-based filtering is stable regardless of render order. Position-based selectors (`nth`, `first`, CSS `:first-child`) are fragile in dynamic lists.'],
['R','I','An `<input>` has no associated label but has `placeholder="Search products"`. What is the most appropriate locator?',null,['`page.getByPlaceholder(\'Search products\')`','`page.locator(\'input\')`','`page.getByRole(\'textbox\')`','`page.getByTestId(\'search\')`'],0,'`getByPlaceholder()` is designed exactly for unlabelled inputs. It\'s more specific than `getByRole(\'textbox\')` when there are multiple textboxes on the page.'],

// S×4 ─────────────────────────────────────────────────────────
['S','I','A test uses `page.locator(\'.btn-primary\')` to find the Submit button. After a rebrand, the class changes to `.btn-accent` and the test breaks. What is the resilient fix?',null,['`page.getByRole(\'button\', { name: \'Submit\' })` — tied to accessible name, not CSS class','`page.locator(\'.btn-accent\')`','`page.locator(\'button\')`','`page.getByText(\'Submit\')`'],0,'CSS classes are implementation details. `getByRole` with `name` is coupled to the semantic meaning — it survives any CSS refactor as long as the button label stays "Submit".'],
['S','A','A product card component contains: a heading, an image, a price, and two buttons ("Add to cart" and "Wishlist"). How do you reliably click "Wishlist" for the second card?',null,['`page.locator(\'.product-card\').nth(1).getByRole(\'button\', { name: \'Wishlist\' }).click()`','`page.getByRole(\'button\', { name: \'Wishlist\' }).nth(1).click()`','`page.locator(\'.product-card:nth-child(2) .wishlist-btn\').click()`','B and A both work — they are equivalent'],1,'Option A is safer: scoping into the card first avoids ambiguity if there are "Wishlist" buttons elsewhere. Option B works only if there are exactly 2 Wishlist buttons total.'],
['S','I','A test `await page.getByRole(\'button\').nth(3).click()` breaks when a "Back to top" button is added to the page header, shifting all button indices. What is the fix?',null,['Use content-based filtering: `page.getByRole(\'button\', { name: \'…specific name…\' })`','Increment nth to 4','Scope with `page.locator(\'form\').getByRole(\'button\').nth(3)`','Pass `--skip-header` to the test runner'],0,'Positional selectors are fragile. Identify the button by its accessible name or scope the locator to the relevant container so unrelated buttons outside the scope don\'t shift the index.'],
['S','A','A modal dialog has a "Confirm" button and the page background also has a "Confirm" button (now disabled behind the overlay). `getByRole(\'button\', { name: \'Confirm\' })` throws a strict mode error. How do you fix it?',null,['`page.getByRole(\'dialog\').getByRole(\'button\', { name: \'Confirm\' })`','`page.getByRole(\'button\', { name: \'Confirm\' }).first()`','`page.locator(\'.modal button\')`','`page.getByRole(\'button\', { name: \'Confirm\' }).filter({ visible: true })`'],0,'Scoping into `getByRole(\'dialog\')` limits the search to the modal\'s subtree, resolving the ambiguity cleanly and semantically.'],

// ── Additional batch ×50 ─────────────────────────────────────
['O','B','What does `locator.count()` return?',null,['A locator representing a count element','A Promise<number> — the number of elements matching the locator at the time of the call','A synchronous count of matching elements','Always 1 in strict mode'],1,'locator.count() resolves with the integer count of matching elements — useful for asserting list lengths with expect(locator).toHaveCount().'],
['O','I','What does `locator.allTextContents()` return?',null,['The concatenated text of all matching elements','A Promise<string[]> — an array of text contents for all matching elements','The HTML of all matching elements','A locator filtered by text content'],1,'allTextContents() is useful for asserting all items in a list without looping — compare the whole array with toEqual().'],
['O','B','What does `page.getByText("Submit")` match by default?',null,['Only elements with exact text "Submit"','Elements whose visible text contains "Submit" as a substring','Only button elements with "Submit" text','Elements with aria-label "Submit"'],1,'getByText uses substring matching by default — use { exact: true } to require an exact full-text match.'],
['O','I','What is the purpose of `locator.locator(childSelector)`?',null,['Creates a completely independent new locator','Scopes a new locator search within the matched elements — finds childSelector only inside elements matched by the parent locator','Filters the parent locator by the child selector','Is equivalent to page.locator(parentSelector + " " + childSelector)'],1,'Chaining locators creates a scoped search — actions on the chain can only match elements inside the parent\'s DOM subtree.'],
['O','B','What does `page.getByRole("heading", { level: 1 })` match?',null,['Any heading element','Only h1 elements (or elements with role="heading" and aria-level="1")','All headings sorted by level','h1 elements that are visible'],1,'The level option maps directly to h1-h6 — level: 1 targets h1 elements.'],
['O','I','What does `.filter({ has: page.getByRole("img") })` do to a list locator?',null,['Removes list items that contain images','Keeps only the elements from the matched set that CONTAIN a descendant matching the nested locator','Returns only the image elements within the list','Filters elements that ARE images'],1,'has: option keeps parent elements that have at least one child matching the nested locator — scoped containment filtering.'],
['O','I','What does `locator.elementHandle()` return and when should you avoid it?',null,['A synchronous DOM element reference','A Promise<ElementHandle> — a reference to the specific DOM element at that moment. Avoid it because ElementHandles are point-in-time snapshots that can become stale if the DOM changes. Use locators instead.','A promise that always returns null in strict mode','A serialised JSON representation of the element'],1,'ElementHandles are legacy — they don\'t auto-retry like locators. Prefer the locator API for everything possible.'],
['O','B','What does `page.getByRole("checkbox", { name: "Accept terms" })` require the checkbox to have?',null,['An id="accept-terms" attribute','An accessible name of "Accept terms" — from an associated label, aria-label, or aria-labelledby','A class of "accept-terms"','A value attribute of "accept-terms"'],1,'getByRole with name uses the accessible name — which comes from label association, not HTML attributes like id or class.'],
['O','I','What is the difference between `getByText("Price")` and `getByLabel("Price")`?',null,['They are identical','getByText matches any element whose visible text contains "Price"; getByLabel matches the form control associated with a label whose text is "Price"','getByLabel is for select elements only','getByText only matches span elements'],1,'getByText is for arbitrary visible text; getByLabel is specifically for form control ↔ label associations.'],
['O','A','What does `locator.or(otherLocator)` do?',null,['Returns a locator that matches the union of both — elements matching either the first OR the second locator','Returns a locator that matches the intersection','Creates a conditional locator that retries with the second if the first fails','Appends the second locator\'s CSS to the first'],0,'or() creates a union — useful when an element can appear in two different forms (e.g. signed-in banner or guest banner) and you want to handle either case.'],
['O','I','What does `page.locator("text=Submit")` match?',null,['Only button elements with text "Submit"','Any element whose visible text contains "Submit" — equivalent to getByText("Submit")','Elements with id="Submit"','A Playwright-specific text CSS extension (not CSS standard)'],1,'text= is a Playwright engine prefix for text matching — it is equivalent to getByText(). The semantic locators (getByRole, getByLabel) are preferred over this engine syntax.'],
['O','B','Can you chain `getByRole()` onto a locator returned by `page.getByRole("dialog")`?',null,['No — getByRole can only be called on page','Yes — locator.getByRole() searches for the role within the parent locator\'s subtree','Yes — but only in Chromium','Only if using test.use({ strictMode: false })'],1,'All semantic locator methods (getByRole, getByLabel, getByText, etc.) are available on both page and locator objects — chaining scopes the search.'],
['F','B','Complete to find an element by its test ID attribute.\n```ts\nconst btn = page.______("submit-button");\n```',null,['getByTestId','getByData','locator(\'[data-testid]\')','findById'],0,'getByTestId("submit-button") matches elements with data-testid="submit-button" (or the configured testIdAttribute).'],
['F','I','Complete to locate a row containing specific text.\n```ts\nconst row = page.getByRole("row").______({ hasText: "Alice" });\n```',null,['filter','find','where','select'],0,'filter({ hasText }) narrows the matched set to elements whose visible text contains the given string.'],
['F','B','Complete to get the count of list items.\n```ts\nconst count = await page.getByRole("listitem").______();\n```',null,['count()','length','size()','total()'],0,'locator.count() returns a Promise<number> of matched elements.'],
['F','I','Complete to get text from all matching elements.\n```ts\nconst texts = await page.getByRole("listitem").______();\n```',null,['allTextContents()','allTexts()','texts()','getTexts()'],0,'allTextContents() returns a string array — one entry per matched element.'],
['F','I','Complete to find a button ONLY inside a form.\n```ts\nconst btn = page.getByRole("form").______("button", { name: "Submit" });\n```',null,['getByRole','locator','find','query'],0,'locator.getByRole() (or any semantic method) scopes the search to the parent — finds Submit only inside the form.'],
['F','B','Complete to match elements with exact text.\n```ts\nconst el = page.getByText("Log in", { ______: true });\n```',null,['exact','strict','full','complete'],0,'{ exact: true } requires the full text to match exactly — "Log in" does not match "Log in to continue".'],
['F','A','Complete to union two locators.\n```ts\nconst banner = page.getByRole("alert").______(page.getByRole("status"));\n```',null,['or','and','union','merge'],0,'locator.or() creates a union — matches elements satisfying either locator.'],
['E','B','What is wrong?\n```ts\nconst items = page.getByRole("listitem");\nconsole.log(items.length); // always undefined\n```',null,['listitem is not a valid role','Locators do not have a .length property — they are lazy queries, not arrays. Use await locator.count() to get the number of matching elements.','length is async and must be awaited','You must call locator.all() first'],1,'Locators are not arrays — they have no length property. Use count() for the integer count or all() to get an array of locators.'],
['E','I','What is wrong?\n```ts\nconst el = await page.getByRole("button", { name: "Save" }).elementHandle();\nawait page.reload();\nawait el.click(); // stale element\n```',null,['click() cannot be called on an ElementHandle','ElementHandle is stale after page.reload() — the DOM element it referenced was destroyed and recreated. Use locator.click() which re-queries on each action.','reload() clears ElementHandles only in Firefox','await is not needed for elementHandle.click()'],1,'ElementHandles capture a reference to a specific DOM node — navigation or page reload destroys the node, making the handle stale. Locators re-query on every action.'],
['E','B','What is wrong?\n```ts\nconst btn = page.getByRole("button");\nawait btn.click();\n// Strict mode violation: multiple elements matched\n```',null,['getByRole("button") is too broad — it matches ALL buttons. Add a name option or scope it: page.getByRole("button", { name: "Submit" })','Strict mode must be disabled to use getByRole','click() requires exactly one element always','getByRole returns all buttons; use .first()'],0,'Strict mode prevents ambiguous actions — always narrow locators to match exactly one element, either with a name option or by scoping within a parent.'],
['E','I','What is wrong?\n```ts\nconst locator = page.getByRole("row").nth(2);\nconst text = locator.textContent();\n// text is a Promise, not a string\n```',null,['textContent() is not available on locators','textContent() is async — it returns a Promise<string | null>. Without await, text is a Promise object, not the string value.','nth() makes the locator synchronous','textContent() needs strict mode off'],1,'All locator value-retrieval methods (textContent, innerText, inputValue, etc.) are async — always await them.'],
['E','A','What is wrong?\n```ts\nconst rows = page.getByRole("row");\nfor (const row of rows) { // cannot iterate\n  await row.click();\n}\n```',null,['getByRole returns a special iterable in TypeScript','Locators are not iterables — use const rowList = await locator.all() to get an array of individual locators, then iterate that array.','for...of requires async iteration','row.click() must be replaced with await row.evaluate(el => el.click())'],1,'Locators are lazy query objects, not arrays. Use locator.all() to materialise an array of per-element locators.'],
['E','B','What is wrong?\n```ts\nconst price = page.getByText("$");\nawait expect(price).toHaveText("$29.99");\n```',null,['toHaveText cannot be used with getByText','getByText("$") matches ALL elements containing a dollar sign — this might match multiple elements causing a strict mode violation. Use a more specific locator like getByTestId("price") or scope within the product card.','$ is a special regex character and must be escaped','toHaveText expects a number'],0,'getByText("$") is too broad for pages with multiple prices — strict mode will throw if more than one element matches.'],
['D','B','`expect(locator).toHaveCount(5)` fails with "received 0" even though 5 items are visible. What should you check?',null,['Add await before expect','The locator may not match the right elements — log the locator\'s selector string, check that the role/selector is correct, and verify the items are not inside an iframe or shadow DOM.','toHaveCount requires locator.all() first','Add visible: true to the locator'],1,'Count mismatches usually mean wrong selector, elements in a different frame/shadow, or elements not yet rendered — inspect the locator match in the Trace Viewer.'],
['D','I','A test uses `locator.filter({ hasText: "Active" })` but it matches items containing "Inactive" too. Why?',null,['filter() uses regex by default','hasText uses substring matching — "Inactive" contains "Active". Use hasText: /^Active$/ (exact regex) or exact: true to restrict to exact matches.','filter() is case-insensitive','hasText only matches direct text, not descendants'],1,'Substring matching is the default — use a regex anchored with ^ and $ for exact matching in filter.'],
['D','B','`page.getByRole("textbox", { name: "Search" })` returns no elements even though a search input is visible. What should you check?',null,['textbox role requires type="text"','The input may be associated with a label whose text differs slightly ("Search…" vs "Search"), or the label association is missing (no for/id). Inspect with the Playwright Inspector or add data-testid as a fallback.','getByRole textbox is Chromium-only','Inputs inside forms are not accessible by role'],1,'Inspect the input\'s accessible name in the browser\'s accessibility tree — even a trailing space or ellipsis prevents a match.'],
['D','I','`locator.all()` returns an empty array even though elements are visible on screen. What is the likely cause?',null,['all() is deprecated','all() materialises the current DOM state — it does not wait. If called before the elements are rendered, the array is empty. Use waitFor() on the locator first: await locator.first().waitFor(), then call all().','all() only returns the first 10 elements','all() requires strict mode off'],1,'all() does not auto-wait — ensure elements are present before calling it by waiting for at least one to appear.'],
['D','A','A locator works in headed mode but fails in headless. What are two things to check?',null,['Headed and headless are identical','1) Hover-triggered elements (tooltips, dropdowns) may not appear in headless without explicit hover() calls. 2) Viewport size differences — elements may be outside the viewport in headless and hidden. Check visibility and scroll into view.','Headless disables ARIA semantics','Locators behave differently in headless mode only for Firefox'],1,'Headed tests benefit from automatic scrolling and hover effects triggered by mouse movement — headless requires explicit Playwright actions to achieve the same.'],
['C','B','Complete to get all matching locators as an array.\n```ts\nconst items = await page.getByRole("listitem").______();\n```',null,['all()','toArray()','getAll()','elements()'],0,'locator.all() materialises the matched elements into an array of individual locators — does not auto-wait.'],
['C','I','Complete to locate a paragraph inside a section.\n```ts\nconst para = page.getByRole("region", { name: "Summary" })\n  .______("p");\n```',null,['locator','getByRole','find','query'],0,'locator.locator(selector) scopes a CSS or semantic selector search within the parent locator\'s subtree.'],
['C','B','Complete to get the text content of one element.\n```ts\nconst label = await page.getByTestId("price").______();\n```',null,['textContent()','getText()','innerText()','content()'],0,'textContent() returns the full text content including hidden text; innerText() returns only visible text (both are async on locators).'],
['C','I','Complete to filter rows that have an image.\n```ts\nconst imageRows = page.getByRole("row")\n  .filter({ ______: page.getByRole("img") });\n```',null,['has','hasChild','contains','includes'],0,'filter({ has: nestedLocator }) keeps only elements that contain at least one descendant matching the nested locator.'],
['C','A','Complete to intersect two locators.\n```ts\nconst activeBtn = page.getByRole("button")\n  .______(page.locator(".active"));\n```',null,['and','filter','or','intersect'],0,'locator.and() returns elements satisfying BOTH the original locator and the argument — set intersection.'],
['L','B','Why are CSS class-based locators considered fragile?',null,['CSS classes are case-sensitive','CSS class names are implementation details that change with refactoring, design system updates, or build-tool hashing — semantic locators (role, label, text) are coupled to user-visible semantics that change far less often.','CSS locators are slower than role locators','Playwright does not support CSS class locators'],1,'Coupling tests to CSS classes means every UI refactor breaks tests — semantic locators decouple tests from implementation.'],
['L','I','Can `getByRole()` match custom web components?',null,['No — web components are black boxes','Yes — if the custom component correctly sets its ARIA role (via role attribute or ARIA attributes), getByRole() matches it. Without correct ARIA, use data-testid as a fallback.','Only in Chromium which supports web components natively','Only if the shadow DOM is open'],1,'ARIA semantics work regardless of whether the element is a native element or a custom component — the key is correct ARIA role assignment.'],
['L','I','What is the difference between `locator.innerText()` and `locator.textContent()`?',null,['They are identical','innerText() returns the rendered visible text (respects CSS display:none); textContent() returns all text content including hidden elements','innerText() is async; textContent() is synchronous','textContent() strips HTML tags; innerText() does not'],1,'textContent can include text from hidden elements; innerText respects CSS visibility — use innerText when you want what a user actually sees.'],
['L','A','When should you use `page.locator("xpath=//table//td[2]")` over semantic locators?',null,['Always — XPath is the most powerful selector','XPath should be a last resort when semantic locators cannot uniquely identify the element — e.g. complex table cell positioning that has no accessible label or test ID. It is brittle and unreadable.','XPath is required for tables','XPath is faster than role-based locators'],1,'XPath tightly couples tests to HTML structure — a table column reorder breaks the test. Exhaust semantic options first.'],
['L','B','Does `locator.waitFor()` auto-retry?',null,['No — it checks once and throws if not found','Yes — waitFor() polls until the element matches the state option (visible, hidden, attached, detached) or the timeout expires','Yes — but only in Chromium','No — use expect(locator).toBeVisible() for auto-retry'],1,'waitFor() is an auto-retrying wait — it is the imperative companion to the declarative expect(locator).toBeVisible() assertion.'],
['R','B','A navigation menu has both "Home" links in the desktop nav and mobile nav (both in the DOM). A strict mode error occurs. How do you fix it?',null,['Use .first() to take the desktop link','Scope to the visible nav: page.getByRole("navigation", { name: "Desktop" }).getByRole("link", { name: "Home" })','Add data-testid to both and use .nth()','Use page.locator("nav:first-child a")'],1,'Scoping by the named landmark (navigation with an accessible name) uniquely identifies which nav you mean — resilient and semantic.'],
['R','I','A test needs to verify all product names in a list match a pattern. What is the most concise approach?',null,['Iterate with a for loop and assert each item','Get all names with const names = await page.getByRole("heading", { level: 3 }).allTextContents() then use expect(names).toEqual(expectedNames)','Use expect(locator).toHaveText([...]) with an array','Both B and C — toHaveText accepts an array and auto-retries, making it more robust than allTextContents()'],3,'expect(locator).toHaveText([...]) is the most idiomatic — it auto-retries and handles async rendering; allTextContents() is useful when you need the values for further processing.'],
['R','I','A form has many fields. You need to find the field labelled "Date of Birth (DD/MM/YYYY)". How?',null,['page.getByLabel("Date of Birth (DD/MM/YYYY)")','page.getByLabel(/Date of Birth/)','page.getByPlaceholder("DD/MM/YYYY")','Any of the above — choose based on whether exact match is needed'],3,'getByLabel with exact string matches precisely; getByLabel with regex is useful when the label text might vary slightly; getByPlaceholder works if the field uses placeholder not label. Choose based on what the element actually has.'],
['R','A','A data table loads rows asynchronously and the count changes. How do you wait for exactly 10 rows before asserting?',null,['await page.waitForTimeout(2000)','await expect(page.getByRole("row")).toHaveCount(10) — it auto-retries until the count matches or the timeout expires','await page.getByRole("row").nth(9).waitFor()','const rows = await page.getByRole("row").all(); expect(rows.length).toBe(10)'],1,'toHaveCount() auto-retries — it is the cleanest way to assert a dynamic list reaches a specific length.'],
['R','B','How do you verify that a specific option is selected in a `<select>` element?',null,['await expect(page.getByRole("combobox")).toHaveValue("expected-value")','page.getByRole("combobox").selectedIndex','expect(await page.getByRole("combobox").evaluate(el => el.value)).toBe("expected")','Both A and C — toHaveValue is preferred as it auto-retries'],0,'toHaveValue on a select element asserts the selected option\'s value with auto-retry — the most idiomatic approach.'],
['S','B','A test clicks a "Delete" link in a table row. There are 5 Delete links and the test must click the one for "Product A". How do you write this?',null,['page.getByRole("row").filter({ hasText: "Product A" }).getByRole("link", { name: "Delete" }).click()','page.getByRole("link", { name: "Delete" }).nth(0).click()','page.locator("tr:contains(\'Product A\') a").click()','page.getByText("Delete").filter({ hasText: "Product A" }).click()'],0,'Filter the row by content then scope the link within — semantically precise and order-independent.'],
['S','I','After applying a discount, a price element updates from "$100" to "$80". A test asserts toHaveText("$80") immediately after clicking "Apply". It sometimes fails. Why?',null,['The price update is asynchronous — toHaveText auto-retries but if the old text is still showing, the timing needs analysis. Check if the element briefly shows a loading state that toHaveText can\'t match.','toHaveText does not auto-retry','The $ sign requires escaping in assertions','The element must be re-queried after update'],0,'toHaveText auto-retries — brief loading states that replace the price element with a spinner might cause the element reference to become stale. Use a fresh locator after the update if the element is replaced in the DOM.'],
['S','A','You need to test a complex drag-and-drop reorderable list. `locator.dragTo()` works but the order does not change. What should you investigate?',null,['dragTo() is not supported for reorderable lists','Check that the drag coordinates land in the right drop zone — dragTo() dispatches mouse events but some drag-and-drop libraries require specific drop zones or use pointer events instead. Try using mouse.move() + mouse.down() + mouse.up() manually for precise control.','The list must be sorted before drag tests','Drag-and-drop requires headless: false'],1,'Library-specific drag implementations vary — when dragTo() fails, manual mouse event sequences give precise control over drag start, move, and drop coordinates.'],
['S','I','`locator.filter({ hasText: "Sale" })` returns 0 elements but the page visually shows "Sale" tags. What should you check?',null,['hasText is case-sensitive by default — check if the actual text is "SALE" or "sale"','Elements with CSS text-transform: uppercase display differently than their actual text content — hasText matches the actual DOM text, not the rendered display. Also check if "Sale" is in a ::before pseudo-element (invisible to textContent).','hasText only works on paragraph elements','The filter is applied before the page loads'],0,'text-transform and ::before content are common gotchas — the DOM text is "sale" but CSS renders it as "SALE". Inspect the actual textContent in DevTools.'],
],
actions: [
  // ── Output ×12 ────────────────────────────────────────────────
  ['O','B','What does `locator.fill(\'text\')` do to a text input?',null,
    ['Appends \'text\' to the current value','Clears the field then sets it to \'text\'','Types \'text\' character by character and fires keydown events','Submits the form with \'text\' as the value'],
    1,'fill() is a two-step operation: clear the field, then set its value.'],
  ['O','B','Which actionability check verifies the element is not hidden by CSS?',null,
    ['attached','stable','visible','enabled'],
    2,'Playwright checks: attached · visible · stable · enabled/editable · receiving events.'],
  ['O','I','What is the key difference between `fill()` and `pressSequentially()`?',null,
    ['fill() is async; pressSequentially() is synchronous','fill() sets the value atomically; pressSequentially() dispatches individual key events','fill() only works on textareas; pressSequentially() works on all inputs','fill() triggers form validation; pressSequentially() bypasses it'],
    1,'Use pressSequentially when the app listens to individual keydown/keyup events.'],
  ['O','B','What does `locator.hover()` do?',null,
    ['Clicks the element gently','Moves the mouse over the element without clicking','Waits for the element to be visible','Scrolls the element into view'],
    1,'hover() triggers :hover CSS states and reveals tooltip/dropdown menus.'],
  ['O','I','What does `locator.selectOption(\'US\')` do?',null,
    ['Searches for the text US inside the element','Selects the <option> whose value attribute equals US','Clicks the option labeled US','Fires a change event with the value US'],
    1,'selectOption() matches by value, label, or index depending on the argument.'],
  ['O','B','How does Playwright handle a `click()` on an element that is still loading?',null,
    ['It throws immediately','It retries until the element passes all actionability checks or times out','It clicks wherever the locator last pointed','It skips the click and logs a warning'],
    1,'Auto-waiting is built into every action — no manual wait needed.'],
  ['O','I','What does `{ force: true }` do when passed to `click()`?',null,
    ['Clicks with extra mouse pressure simulation','Skips actionability checks like visibility and enabled state','Forces the element into the viewport before clicking','Bypasses network throttling during the action'],
    1,'force: true is needed when you deliberately interact with hidden or covered elements.'],
  ['O','B','What does `locator.dblclick()` produce?',null,
    ['A right-click followed by a left-click','Two rapid left-click events on the element','A single click with the middle mouse button','A click that holds Shift simultaneously'],
    1,'dblclick() is equivalent to click({ clickCount: 2 }).'],
  ['O','I','What does `locator.setInputFiles(\'cv.pdf\')` do?',null,
    ['Opens a file picker dialog for cv.pdf','Sets the files of a file input element to cv.pdf','Reads cv.pdf and fills the input with its text content','Uploads cv.pdf to the server immediately'],
    1,'setInputFiles() bypasses the OS file picker and attaches files directly.'],
  ['O','B','What does `locator.press(\'Enter\')` do?',null,
    ['Submits the nearest form unconditionally','Dispatches keydown · keypress · keyup events for Enter on the element','Clicks the first submit button in the form','Moves focus to the next focusable element'],
    1,'press() simulates low-level keyboard events — useful for form submission or autocomplete.'],
  ['O','I','What does `locator.click({ button: \'right\' })` trigger?',null,
    ['A double right-click','A contextmenu event on the element','A middle-mouse-button click','A click with the Alt modifier'],
    1,'Right-click fires the contextmenu event, opening context menus.'],
  ['O','A','What does `locator.click({ trial: true })` do?',null,
    ['Performs a real click and then undoes it','Runs all actionability checks without actually dispatching a click','Clicks in slow-motion for visual debugging','Retries the click up to three times'],
    1,'trial: true is a dry run — useful for verifying actionability without side effects.'],

  // ── Fill-in ×7 ────────────────────────────────────────────────
  ['F','B','Complete: `await page.getByLabel(\'Email\')._______(\'user@test.com\')`',null,
    ['fill','type','setValue','sendKeys'],
    0,'fill() clears and sets — the standard method for text inputs.'],
  ['F','I','Complete: `await page.getByLabel(\'Country\').________Option(\'US\')`',null,
    ['select','choose','pick','set'],
    0,'selectOption() handles <select> dropdowns.'],
  ['F','B','Complete: `await page.getByLabel(\'Terms\').______()` to tick a checkbox',null,
    ['check','tick','select','click'],
    0,'check() is the semantic way to tick a checkbox — it auto-waits for the element to be editable.'],
  ['F','I','Complete: `await locator._______(\'ArrowDown\')` to press the down-arrow key',null,
    ['press','key','send','keyboard'],
    0,'press() accepts any KeyboardEvent key name.'],
  ['F','I','Complete: `await page.getByLabel(\'Resume\').________Files(\'cv.pdf\')`',null,
    ['setInput','attach','upload','fill'],
    0,'setInputFiles() is Playwright\'s file-upload method.'],
  ['F','I','Complete: `await page.locator(\'#src\').______To(page.locator(\'#dst\'))`',null,
    ['drag','move','transfer','drop'],
    0,'dragTo() performs a drag-and-drop from source to target.'],
  ['F','A','Complete: `await locator.click({ ________: [\'Control\'] })` for Ctrl+Click',null,
    ['modifiers','keys','shortcuts','buttons'],
    0,'modifiers accepts an array of modifier keys: Control, Shift, Alt, Meta.'],

  // ── Find the Error ×6 ──────────────────────────────────────────
  ['E','B','What is wrong?\n```ts\nawait page.getByRole(\'button\', { name: \'Submit\' }).click;\n```',null,
    ['getByRole returns undefined for buttons','click is missing () — it references the method without calling it','name should be text not a string','await cannot be used without async'],
    1,'Missing () means the promise is never created and no click occurs.'],
  ['E','I','What is wrong?\n```ts\nawait page.getByLabel(\'Country\').selectOption(\'Australia\');\n// <select> has only "US" and "UK" options\n```',null,
    ['selectOption does not accept strings — use { value: }','The call will timeout because the option value does not exist','selectOption only works with numeric indices','await is unnecessary on selectOption'],
    1,'Playwright retries until the value matches an option; if it never does, it times out.'],
  ['E','B','What is wrong?\n```ts\nawait page.getByLabel(\'Terms\').check();\n// The label targets a <div> not a checkbox\n```',null,
    ['check() is only valid on <input type="checkbox"> or <input type="radio">','Labels cannot target divs in HTML','check() always needs force: true','The await is in the wrong place'],
    0,'check() throws when the element is not checkable — use the correct locator or add a role.'],
  ['E','I','What is wrong?\n```ts\nawait locator.click({ modifiers: [\'ctrl\'] });\n```',null,
    ['\'ctrl\' is the wrong modifier name — use \'Control\'','modifiers must be a string, not an array','click does not support modifiers','ctrl should be uppercase: \'CTRL\''],
    0,'Playwright modifier names match KeyboardEvent.key: \'Control\', \'Shift\', \'Alt\', \'Meta\'.'],
  ['E','I','What is wrong?\n```ts\nawait page.getByRole(\'menu\').hover();\nawait page.getByRole(\'menuitem\', { name: \'Delete\' }).click();\n// menuitem disappears before click\n```',null,
    ['hover() auto-dismisses menus after 100ms','menuitem is not a valid ARIA role','The menu closes because moving the mouse to click the item leaves the hover target','click() should be replaced with press(\'Enter\')'],
    2,'hover() briefly positions the mouse; moving to the menuitem must happen before the menu closes — consider chaining or using a stable anchor.'],
  ['E','A','What is wrong?\n```ts\nawait page.getByLabel(\'Upload\').setInputFiles(\'report.pdf\');\n// The input has type="text", not type="file"\n```',null,
    ['setInputFiles cannot upload PDFs','setInputFiles only works on <input type="file"> elements','The path must be absolute','setInputFiles requires await false'],
    1,'setInputFiles() is exclusively for file inputs — using it on text inputs throws.'],

  // ── Debug ×6 ──────────────────────────────────────────────────
  ['D','B','`click()` fails with "element is not visible". The element exists but a loading spinner covers it. What is the best fix?',null,
    ['Use click({ force: true })','Wait 2 seconds then click','Reload the page','Use page.evaluate() to click via JS'],
    0,'force: true skips the visibility check — appropriate when a known overlay exists.'],
  ['D','I','`fill()` sets the value but the form validation message never appears. What should you try instead?',null,
    ['Use type() which is more compatible','Use pressSequentially() to fire individual key events that trigger the validation listener','Use setValue via JavaScript','Add { force: true } to fill'],
    1,'Validation listeners often fire on keydown/keyup events that fill() bypasses.'],
  ['D','B','`check()` throws "element is not editable". The checkbox is visible. What is the likely cause?',null,
    ['Checkboxes require double-click not check()','The checkbox has the disabled or readonly attribute','check() only works on radio buttons','The locator matched multiple elements'],
    1,'Disabled checkboxes fail the editable actionability check.'],
  ['D','I','`selectOption(\'dark\')` times out even though the dropdown is visible. What should you check first?',null,
    ['The option\'s value attribute — it might be \'Dark Mode\' not \'dark\'','Replace with click() on the option directly','Increase the global timeout','Use selectOption({ index: 1 }) instead'],
    0,'selectOption matches by value attribute, label text, or index — verify the exact value.'],
  ['D','I','`dragTo()` seems to do nothing — the element is not dropped. What is a common cause?',null,
    ['dragTo() requires force: true on all drag operations','The drop zone uses custom JS events that dragTo() does not trigger','Drag-and-drop only works in headed mode','dragTo() needs two awaits'],
    1,'Some drag libraries require page.mouse.move/down/up for precise control instead of dragTo.'],
  ['D','A','`click({ modifiers: [\'Control\'] })` opens a new tab. How do you capture the new tab?',null,
    ['Use click({ modifiers: [\'ctrl\'] }) in lowercase to prevent new tab','Wrap in context.waitForEvent(\'page\') to capture the new tab','Replace with page.keyboard.press(\'Control+Click\')','Use { force: true } to suppress new tabs'],
    1,'context.waitForEvent(\'page\') captures popups/new tabs triggered by the action.'],

  // ── Complete ×5 ──────────────────────────────────────────────
  ['C','B','Complete the code to clear a text input without typing anything new.\n```ts\nawait page.getByLabel(\'Search\').______();\n```',null,
    ['clear','reset','empty','delete'],
    0,'clear() removes all text from an input.'],
  ['C','I','Complete to select by visible label text.\n```ts\nawait page.getByLabel(\'Theme\').selectOption({ ______: \'Dark Mode\' });\n```',null,
    ['label','text','name','value'],
    0,'selectOption accepts { label } for visible text, { value } for the value attribute.'],
  ['C','I','Complete to Shift+Click a table row.\n```ts\nawait page.locator(\'tr\').nth(3).click({ ______: [\'Shift\'] });\n```',null,
    ['modifiers','keys','buttons','shortcuts'],
    0,'modifiers is the option key for modifier keys in click().'],
  ['C','B','Complete to upload two files at once.\n```ts\nawait page.getByLabel(\'Attachments\').setInputFiles([\'a.pdf\', ______]);\n```',null,
    ['\'b.pdf\'','{ file: \'b.pdf\' }','new File([\'b.pdf\'])','Buffer.from(\'b.pdf\')'],
    0,'setInputFiles() accepts a string path or an array of paths.'],
  ['C','A','Complete to scroll the page down 500 pixels with the mouse wheel.\n```ts\nawait page.mouse.______( 0, 500 );\n```',null,
    ['wheel','scroll','move','drag'],
    0,'page.mouse.wheel(deltaX, deltaY) scrolls the page programmatically.'],

  // ── Logic ×5 ──────────────────────────────────────────────────
  ['L','B','Why does `fill()` not require a separate `clear()` call before it?',null,
    ['fill() uses the clipboard which always overwrites','fill() automatically clears the input before setting the new value','fill() appends to an empty string by default','fill() selects all text first via Ctrl+A'],
    1,'fill() = clear + set. Only use clear() when you want to empty without refilling.'],
  ['L','I','When should you choose `pressSequentially()` over `fill()`?',null,
    ['When the input accepts more than 10 characters','When the app has keydown/keyup/input event listeners for auto-suggest, validation, or masking','When fill() is unavailable in the version','When the input is inside a shadow DOM'],
    1,'pressSequentially() fires real key events; fill() sets the value without them.'],
  ['L','I','Which of these is NOT one of Playwright\'s actionability checks?',null,
    ['The element is attached to the DOM','The element is visible','The element has no sibling elements','The element is not covered by another element'],
    2,'Playwright checks: attached, visible, stable, enabled/editable, receiving events — siblings are irrelevant.'],
  ['L','B','What happens if you call `check()` on a checkbox that is already checked?',null,
    ['It throws a "already checked" error','It unchecks then re-checks the checkbox','It does nothing — the checkbox stays checked','It always calls click() regardless of current state'],
    2,'check() is idempotent — it only clicks if the checkbox is not already checked.'],
  ['L','A','Why is `locator.click({ trial: true })` useful in CI pipelines?',null,
    ['It reduces flakiness by clicking more slowly','It verifies an element is actionable without side effects — good for asserting readiness','It skips the click if CI is slow','It records the click for replay'],
    1,'trial: true validates actionability without triggering the action.'],

  // ── Real-World ×5 ─────────────────────────────────────────────
  ['R','B','You need to fill a login form and press Enter to submit. Which two calls do you need?',null,
    ['fill(\'pass\'); press(\'Enter\') on the password field','fill(\'pass\'); click(\'submit\')','pressSequentially(\'pass\'); keyboard.press(\'Enter\')','type(\'pass\'); submit()'],
    0,'fill() for text, then press(\'Enter\') — the idiomatic login pattern.'],
  ['R','I','A multi-select list allows choosing several options. How do you select \'JS\' and \'TS\' in one call?',null,
    ['click(\'JS\'); click(\'TS\') with modifiers','selectOption([\'JS\', \'TS\'])','check(\'JS\'); check(\'TS\')','fill(\'JS,TS\')'],
    1,'selectOption() accepts an array of values for multi-select elements.'],
  ['R','I','How do you right-click a file in a file-manager UI to open its context menu?',null,
    ['locator.click({ type: \'context\' })','locator.click({ button: \'right\' })','locator.rightClick()','locator.contextMenu()'],
    1,'{ button: \'right\' } dispatches the contextmenu event.'],
  ['R','I','A user can upload a profile photo. How do you attach a local image in a test?',null,
    ['page.getByLabel(\'Photo\').fill(\'avatar.png\')','page.getByLabel(\'Photo\').setInputFiles(\'avatar.png\')','page.getByLabel(\'Photo\').type(\'avatar.png\')','page.getByLabel(\'Photo\').attach(\'avatar.png\')'],
    1,'setInputFiles() bypasses the OS file picker and attaches the file directly.'],
  ['R','A','A kanban board lets you drag a card from "Todo" to "Done". Which call models this?',null,
    ['page.locator(\'.card\').moveTo(page.locator(\'#done\'))','page.locator(\'.card\').dragTo(page.locator(\'#done\'))','page.mouse.click(\'.card\').release(\'#done\')','page.drag(\'.card\', \'#done\')'],
    1,'dragTo() performs a complete drag-and-drop sequence.'],

  // ── Scenario ×4 ──────────────────────────────────────────────
  ['S','I','A search field triggers autocomplete on each keypress. `fill()` populates the box but suggestions never appear. What is the correct fix?',null,
    ['Use click() first to activate the field','Replace fill() with pressSequentially() so key events trigger the listener','Add { force: true } to fill()','Use page.evaluate() to set the value'],
    1,'pressSequentially() fires real keydown/keyup events that the autocomplete listener needs.'],
  ['S','I','A modal has a "Confirm Delete" button. The app treats Shift+Click as bypass confirmation. How do you write the call?',null,
    ['locator.click({ key: \'Shift\' })','locator.click({ modifiers: [\'Shift\'] })','page.keyboard.press(\'Shift\'); locator.click()','locator.shiftClick()'],
    1,'modifiers on click() holds the key down during the mouse event.'],
  ['S','A','You drop a file onto a drop zone but `dragTo()` does nothing. The zone uses a custom drag library with JS event handlers. What should you try?',null,
    ['Add force: true to dragTo()','Use page.mouse.move() + down() + up() to manually simulate the drag events','Switch to headed mode — dragTo() only works headless','Reload the page first'],
    1,'Some drag libraries require low-level mouse events instead of the high-level dragTo().'],
  ['S','A','A styled checkbox has opacity:0 on the actual <input>. `check()` fails with "not visible". What are your options?',null,
    ['Use check({ force: true }) to skip visibility, or click the visible <label> that controls it','Use fill(true) instead of check()','Set the checkbox via page.evaluate()','Replace with selectOption(\'checked\')'],
    0,'force: true skips visibility; clicking the <label> is the alternative semantic approach.'],

  // ── Additional batch ×50 ─────────────────────────────────────
  ['O','B','What does `locator.clear()` do?',null,
    ['Removes the element from the DOM','Clears the current value of an input or textarea field','Resets all form fields','Clears the browser cache'],
    1,'clear() is equivalent to triple-clicking to select all then pressing Delete — it empties the input without typing new text.'],
  ['O','I','What does `locator.type(text)` do differently from `fill(text)`?',null,
    ['type() triggers character-by-character keyboard events; fill() sets the value directly. type() is slower but fires all keydown/keypress/keyup events.','They are identical','fill() triggers keyboard events; type() does not','type() is deprecated and fill() should always be used'],
    0,'type() simulates real typing and fires all keyboard events — useful for apps that listen to individual key events. fill() is faster but skips individual key events.'],
  ['O','B','What does `page.keyboard.press("Enter")` do?',null,
    ['Submits any focused form','Fires a keydown then keyup event for Enter on whatever element currently has focus — the application handles it','Navigates to the next page','Clicks the first button on the page'],
    1,'keyboard.press() sends keyboard events to the focused element — the behavior depends entirely on the application\'s event listeners.'],
  ['O','I','What does `locator.focus()` do?',null,
    ['Highlights the element in the browser','Programmatically focuses the element, firing focus and focusin events — subsequent keyboard.press() calls target that element','Scrolls the element into view','Enables the element if it was disabled'],
    1,'focus() is useful when you need to type using keyboard.press() on a specific element without clicking it first.'],
  ['O','B','What does `locator.blur()` do?',null,
    ['Removes the element from the DOM','Removes focus from the element, firing blur and focusout events','Clears the element\'s value','Makes the element visually blurred via CSS'],
    1,'blur() is used to trigger form validation that fires on blur events — test inline validation messages.'],
  ['O','I','What does `page.mouse.move(x, y)` do?',null,
    ['Navigates to coordinates (x, y)','Moves the mouse cursor to absolute pixel coordinates — fires mousemove events that the application can respond to','Scrolls to position (x, y)','Clicks at coordinates (x, y)'],
    1,'mouse.move() is useful for hover-triggered UI (tooltips, dropdowns) that require a physical mouse movement rather than an explicit element hover.'],
  ['O','I','What is the difference between `locator.click()` and `locator.dispatchEvent("click")`?',null,
    ['They are identical','locator.click() simulates a real user click (waits for element, scrolls into view, fires all pointer events); dispatchEvent fires a synthetic event on the element directly, bypassing Playwright\'s auto-wait and the full event chain','dispatchEvent is for custom events only','locator.click() is deprecated in favour of dispatchEvent'],
    1,'click() is the recommended API — dispatchEvent is an escape hatch for custom events or when the standard click doesn\'t work as expected.'],
  ['O','B','What does `locator.selectOption("value")` do for a `<select>` element?',null,
    ['Clicks the option with that text','Sets the select\'s value to the matching option and fires change and input events','Types the value into the select','Finds and clicks the option in a dropdown'],
    1,'selectOption accepts value, label, or index — it directly sets the selection and fires the appropriate events.'],
  ['O','A','What does `page.mouse.wheel(deltaX, deltaY)` do?',null,
    ['Navigates the page history','Fires a wheel event at the current mouse position — triggers scroll handlers in the application','Scrolls the page by the specified pixels','Zooms the viewport'],
    1,'mouse.wheel() fires the wheel event without actually scrolling the page — useful for testing custom scroll event handlers.'],
  ['O','I','What does `locator.press("Tab")` do?',null,
    ['Opens a new browser tab','Fires Tab keydown/keyup on the element, typically moving focus to the next focusable element','Submits the current form','Moves to the next matching element'],
    1,'press("Tab") is used to test keyboard navigation — verify focus order through form fields and interactive elements.'],
  ['O','B','What does `locator.hover()` do?',null,
    ['Clicks the element','Moves the mouse to the element\'s center, firing mouseenter, mouseover, and mousemove events','Highlights the element in the trace','Waits for the element to become hovered'],
    1,'hover() is for testing hover-triggered UI — tooltips, dropdown menus, reveal buttons.'],
  ['O','I','What does the `trial: true` option do in `locator.click({ trial: true })`?',null,
    ['Runs the click in trial mode with extra logging','Performs all pre-click checks (visibility, enabled, stable) WITHOUT actually dispatching the click event — useful for verifying a click would succeed before performing it','Retries the click up to 3 times','Enables slow motion for this click'],
    1,'trial: true is a dry-run mode — Playwright checks all preconditions but stops before the actual click, useful for debugging "why is this click failing".'],
  ['F','B','Complete to press Enter on a focused element.\n```ts\nawait page.keyboard.______("Enter");\n```',null,
    ['press','key','send','type'],
    0,'keyboard.press() sends a keydown + keyup pair for the specified key.'],
  ['F','B','Complete to clear an input field.\n```ts\nawait page.getByLabel("Search").______();\n```',null,
    ['clear','reset','empty','delete'],
    0,'locator.clear() empties the input field and fires the appropriate input events.'],
  ['F','I','Complete to upload a file.\n```ts\nawait page.getByLabel("Resume").______("resume.pdf");\n```',null,
    ['setInputFiles','upload','attach','sendFile'],
    0,'setInputFiles() attaches files to an <input type="file"> element without triggering the OS file picker.'],
  ['F','I','Complete to focus an element.\n```ts\nawait page.getByLabel("First name").______();\nawait page.keyboard.type("Alice");\n```',null,
    ['focus','activate','click','hover'],
    0,'focus() programmatically focuses the element so subsequent keyboard.type() targets it.'],
  ['F','B','Complete to double-click an element.\n```ts\nawait page.getByRole("cell").______();\n```',null,
    ['dblclick()','doubleClick()','click({ clickCount: 2 })','Both A and C work'],
    3,'Both dblclick() and click({ clickCount: 2 }) produce a double-click — dblclick() is more readable.'],
  ['F','I','Complete to right-click an element for a context menu.\n```ts\nawait page.getByRole("row").______({ button: "right" });\n```',null,
    ['click','rightClick','contextMenu','press'],
    0,'click({ button: "right" }) fires a right-click, triggering contextmenu events.'],
  ['F','A','Complete to press multiple keys simultaneously.\n```ts\nawait page.keyboard.______("Control+A");\n```',null,
    ['press','hold','down','chord'],
    0,'keyboard.press("Control+A") sends the key combination as a chord — Ctrl held down while A is pressed.'],
  ['E','B','What is wrong?\n```ts\nawait page.getByRole("button", { name: "Submit" }).fill("test");\n```',null,
    ['Buttons cannot have names','fill() is only for input/textarea elements — calling it on a button throws an error. Use click() or textContent() on buttons.','fill() requires visible: true','Buttons must use type() not fill()'],
    1,'fill() is exclusively for editable form fields (input, textarea, contenteditable). Buttons are not editable.'],
  ['E','I','What is wrong?\n```ts\nawait page.getByLabel("Password").type("secret");\nawait expect(page.getByLabel("Password")).toHaveValue("secret");\n// sometimes fails\n```',null,
    ['type() does not set the value property','type() fires keyboard events but may be intercepted by the application — if the app masks or transforms input during typing, the final value may differ. Use fill() for reliable value setting, type() only when key events matter.','toHaveValue doesn\'t work with password fields','type() is asynchronous and needs an extra await'],
    1,'type() simulates keystrokes — application key handlers can intercept and modify the typed value. fill() sets the value directly.'],
  ['E','B','What is wrong?\n```ts\nawait page.locator("input").fill("hello");\n// Strict mode violation if multiple inputs exist\n```',null,
    ['fill() requires a single element','Page.locator("input") with no qualifier matches ALL inputs — strict mode throws if more than one is found. Use a labelled locator: page.getByLabel("Name").','Strict mode cannot be disabled for fill','fill() must include a type option'],
    1,'Unscoped CSS selectors matching multiple elements cause strict mode violations — always use a semantic locator or scope with a parent.'],
  ['E','I','What is wrong?\n```ts\nawait locator.click({ timeout: 0 });\n```',null,
    ['timeout must be a string','timeout: 0 disables the timeout entirely — if the element never becomes ready, the test hangs indefinitely. Only use this when you are certain the element will be immediately available.','0 is not a valid timeout value','click() does not accept a timeout option'],
    1,'timeout: 0 means infinite wait — it removes the safety net. Use it only in controlled situations where hanging is impossible.'],
  ['E','A','What is wrong?\n```ts\nawait page.mouse.down();\nawait page.goto("/other-page"); // mid-drag navigation\nawait page.mouse.up();\n```',null,
    ['mouse.down() cannot cross navigations','Navigation while a mouse button is held causes undefined behavior — the mouse state is reset on navigation. Complete the drag on the same page.','mouse.up() must be called before goto()','goto() cancels all pending mouse events'],
    1,'Browser navigations reset pointer state — always complete mouse sequences (down → move → up) on the same page.'],
  ['E','I','What is wrong?\n```ts\nawait page.getByRole("combobox").selectOption({ value: "us" });\n// expects USA to be selected but Germany is selected\n```',null,
    ['selectOption requires label not value','selectOption({ value: "us" }) selects the option whose value attribute equals "us" exactly — if the option\'s value attribute is "US" (uppercase), it does not match. Check the exact value attribute in the HTML.','combobox role requires click() first','selectOption fires change but not input events'],
    1,'selectOption value matching is case-sensitive and must match the option\'s value attribute exactly — inspect the HTML to confirm the casing.'],
  ['D','B','`locator.click()` succeeds but the form is not submitted. What should you check?',null,
    ['The button must have type="submit"','Check if click() hit the button or an overlapping element — use the Trace Viewer to see the actual click target and its coordinates. Also verify the button triggers form submission vs. a JavaScript click handler.','Add force: true to bypass validation','Increase the click timeout'],
    1,'The Trace Viewer shows exactly which element received the click — an overlapping element is a common cause of "click happened but nothing changed".'],
  ['D','I','`pressSequentially()` works but autocomplete suggestions appear then immediately disappear. Why?',null,
    ['pressSequentially() is too slow','An event handler closes the suggestions on blur or after a keyup — a following action (like the assertion) might trigger blur. Add a waitFor on the suggestions before asserting.','pressSequentially() fires events in wrong order','Suggestions require click() on the input first'],
    1,'Timing between keystroke events and suggestion appearance can be tight — wait for the suggestions to be visible before interacting with them.'],
  ['D','B','`locator.check()` throws "locator.check: Not a checkbox or radio button". What is the likely cause?',null,
    ['check() is deprecated','The locator matched a different element type — perhaps a div styled as a checkbox. Use page.evaluate to check the element type, or use a more specific locator targeting the actual <input type="checkbox">.','Checkboxes must use fill(true)','check() requires type="checkbox" explicitly'],
    1,'Styled checkbox components often hide the real input — the locator may be hitting the visual wrapper instead of the actual input.'],
  ['D','I','`dragTo()` runs but the drop target shows no change. The app uses the HTML5 Drag and Drop API. What should you check?',null,
    ['dragTo() doesn\'t support HTML5 DnD','HTML5 DnD requires dataTransfer — dragTo() may not populate it. Use page.evaluate to manually dispatch drag events with a dataTransfer object, or use mouse.move/down/up with the exact coordinates.','Add force: true to dragTo()','HTML5 DnD requires headless: false'],
    1,'Some HTML5 DnD implementations check dataTransfer contents — manual event dispatching with proper dataTransfer is sometimes necessary.'],
  ['D','A','`page.keyboard.press("Escape")` does not close a modal. What are two things to check?',null,
    ['Escape key is blocked in Playwright','1) The modal may not have focus — click() it first or call focus() on the modal. 2) The application may handle Escape on a specific element (like a close button) rather than globally on document.','Escape requires keyboard.down then keyboard.up separately','Escape is a Chromium-only shortcut'],
    1,'Keyboard events go to the focused element — if the modal isn\'t focused, Escape goes nowhere. Focus the modal or a focusable element inside it first.'],
  ['C','B','Complete to type text character by character.\n```ts\nawait page.getByRole("combobox").______("New York");\n```',null,
    ['pressSequentially','type','fill','keyboard.type'],
    0,'pressSequentially() fires individual key events — useful for autocomplete that reacts to each keystroke.'],
  ['C','I','Complete to drag a locator to a target.\n```ts\nawait page.locator(".card").______(\n  page.locator("#done-column")\n);\n```',null,
    ['dragTo','moveTo','dropTo','drag'],
    0,'dragTo() performs the full drag-and-drop gesture from the source element to the target.'],
  ['C','B','Complete to select by visible option text.\n```ts\nawait page.getByRole("combobox").______({ label: "United States" });\n```',null,
    ['selectOption','pick','choose','set'],
    0,'selectOption({ label: "..." }) selects by the visible text of the option — as opposed to { value } which uses the value attribute.'],
  ['C','I','Complete to scroll within a scrollable container.\n```ts\nawait page.getByRole("log").______({ deltaY: 300 });\n```',null,
    ['dispatchEvent("wheel",','scroll','evaluate(el => el.scrollTop +=','hover'],
    0,'dispatchEvent("wheel", { deltaY: 300 }) fires a wheel event on the element — triggers scroll listeners in scrollable containers.'],
  ['C','A','Complete to perform a triple-click to select all text.\n```ts\nawait page.getByLabel("Content").______({ clickCount: 3 });\n```',null,
    ['click','dblclick','tripleClick','focus'],
    0,'click({ clickCount: 3 }) fires three rapid clicks — standard triple-click behavior in inputs and textareas selects all text.'],
  ['L','B','Why does Playwright auto-wait before actions like click() and fill()?',null,
    ['For legal compliance with browser automation regulations','To ensure the element is visible, enabled, stable (not animating), and in the viewport before interacting — mimicking a real user who cannot click a hidden or moving element','To throttle test execution speed','To allow event handlers to register'],
    1,'Auto-wait prevents flaky tests caused by clicking before the UI is ready — Playwright performs the same checks a human user would naturally pass.'],
  ['L','I','What is the difference between `force: true` and removing auto-wait?',null,
    ['They are identical','force: true dispatches the event to the element regardless of its visibility/enabled/stable state — it bypasses the auto-wait checks but still targets the specific element. It does not remove timeout — it just skips precondition checks.','force: true disables retries','force: true is equivalent to dispatchEvent()'],
    1,'force: true is a targeted bypass — use it only when you intentionally need to interact with an element that Playwright would normally refuse (e.g. opacity:0 custom inputs).'],
  ['L','I','When is `dispatchEvent` preferable to standard action methods?',null,
    ['Always — it is faster','When you need to fire custom events (like "myapp:confirm") that have no Playwright API equivalent, or when you need to pass custom event details that standard actions cannot','dispatchEvent is only for mouse events','dispatchEvent is preferred in Firefox'],
    1,'Custom events and detailed event data are the main use cases — for standard interactions (click, fill, check), always prefer the dedicated Playwright action methods.'],
  ['L','B','What check does `locator.click()` perform before clicking?',null,
    ['Only visibility','Visible → enabled → stable (not animating) → in viewport → receives pointer events — all five checks must pass before the click is dispatched','Only enabled','Only attached to DOM'],
    1,'The five preconditions mirror real-user interaction — Playwright won\'t click a hidden, disabled, still-animating, off-screen, or pointer-events:none element.'],
  ['L','A','Can `fill()` trigger form validation errors that fire on the "input" event?',null,
    ['No — fill() bypasses all events','Yes — fill() fires input and change events after setting the value, so any validation handlers listening to "input" will run','Only in Chromium','Only if the input has a required attribute'],
    1,'fill() sets the value then fires input and change — validation handlers on these events will fire and show validation messages.'],
  ['R','B','A rich text editor (contenteditable div) does not respond to fill(). How do you type into it?',null,
    ['focus() then keyboard.type()','page.getByRole("textbox").fill() — fill works on contenteditable','Contenteditable elements cannot be tested','page.evaluate(el => el.innerHTML = text)'],
    0,'Contenteditable divs often need keyboard.type() after focus() — or pressSequentially() — since they rely on real keypress events for their internal editor logic.'],
  ['R','I','A date picker opens a calendar on click. You need to select "2025-03-15". What is the most reliable approach?',null,
    ['fill("2025-03-15") on the date input','Click the calendar popup navigation to March 2025, then click the 15th cell by its content — or set the value directly via fill/keyboard if the input accepts typed dates','Use page.evaluate to set input.value','datepickers cannot be tested reliably in Playwright'],
    1,'The right approach depends on the picker library — some accept typed input; others require UI navigation. Inspect the rendered HTML to decide.'],
  ['R','A','A slider input needs to be dragged to 75% of its track. How?',null,
    ['locator.fill("75")','Calculate the target x-coordinate (trackStart.x + trackWidth * 0.75) then use mouse.move + down + up, or use page.evaluate to set input.value and dispatch change events','locator.dragTo({ x: 75 })','locator.setValue(75)'],
    1,'Native range inputs can often be set via evaluate + dispatch. Custom sliders need manual mouse coordinates.'],
  ['R','I','A form has a "Country" combobox that filters options as you type. How do you select "Germany"?',null,
    ['page.getByRole("combobox").selectOption("Germany")','page.getByRole("combobox").fill("Ger") then wait for the "Germany" option to appear and click it','page.getByRole("combobox").type("Germany") then press Enter','All of the above may work — depends on implementation'],
    3,'It depends on the implementation — a native select uses selectOption; a custom combobox requires typing and then clicking the suggestion. Inspect the component type first.'],
  ['R','B','You need to verify a file input accepts a PDF but rejects a PNG. How do you test both?',null,
    ['Mock the file type validation with page.evaluate','Set a PDF with setInputFiles and assert success; set a PNG and assert the validation error message — both are real test cases for file type validation','File inputs cannot be tested in Playwright','Use route() to intercept file uploads'],
    1,'setInputFiles works for both test cases — test the happy path and the error case in separate tests.'],
  ['S','B','A form has autofill that replaces your typed value with a stored email. How do you prevent this?',null,
    ['Disable autofill in playwright.config.ts','Set autocomplete: "off" on the input is the app-level fix, but in Playwright: use page.evaluate to set the field\'s autocomplete property to "off" before fill(), or disable autofill in the browser context via extraHTTPHeaders','Autofill cannot be disabled in tests','Use pressSequentially to bypass autofill'],
    1,'Autofill interference is a browser feature — context-level disabling via browser launch args (--disable-autofill-keyboard-accessory-view) or input-level autocomplete="off" are the solutions.'],
  ['S','I','You need to test keyboard shortcut Ctrl+Shift+P opens a command palette. How?',null,
    ['page.keyboard.press("Ctrl"); page.keyboard.press("Shift"); page.keyboard.press("P");','await page.keyboard.press("Control+Shift+P") — Playwright combines modifiers with + notation in a single press call','page.keyboard.chord("ctrl","shift","p")','page.getByRole("menu").press("Control+Shift+P")'],
    1,'keyboard.press() with the + notation sends all modifiers simultaneously — matching how a user would press the shortcut.'],
  ['S','A','A rich text editor loses focus when you switch to another test action, causing typed text to be lost. What is the fix?',null,
    ['Use fill() instead of type()','Keep keyboard interactions within a test.step: first click the editor, then type, then immediately assert — do not interleave actions on other elements between typing and asserting','Set autosave on the editor','Use evaluate() to insert text programmatically'],
    1,'Interleaving actions on other elements can blur the editor — group editor interactions together and avoid context switches mid-typing.'],
  ['S','I','You test that pressing Tab from the last form field wraps focus back to the first. How do you assert this?',null,
    ['expect(page.focusedElement).toBe(firstField)','await firstField.focus(); then await page.keyboard.press("Tab") × N times (once per field), then await expect(firstField).toBeFocused()','Use page.accessibility to check focus order','Tab cycling cannot be tested in Playwright'],
    1,'Playwright tracks focus — expect(locator).toBeFocused() asserts the element has focus. Tab through the form until you return to the start.'],
],
assertions: [
  // ── Output ×12 ────────────────────────────────────────────────
  ['O','B','What makes web-first Playwright assertions different from generic `expect()` assertions?',null,
    ['They use a different syntax','They auto-retry until the condition passes or a timeout expires','They only work with async/await','They require importing from a separate package'],
    1,'Web-first assertions poll the DOM repeatedly — no manual sleep needed before asserting.'],
  ['O','B','What does `await expect(locator).toBeVisible()` do if the element is momentarily hidden during a CSS animation?',null,
    ['It fails immediately','It retries until the element becomes visible or the timeout expires','It skips the assertion silently','It waits exactly 1 second then checks once'],
    1,'Auto-retry is the key advantage — assertions tolerate transient DOM states.'],
  ['O','I','What is the difference between `toHaveText()` and `toContainText()`?',null,
    ['toHaveText() is for headings; toContainText() is for paragraphs','toHaveText() requires the full text to match; toContainText() allows a partial match','toHaveText() is case-sensitive; toContainText() is not','They are identical — both do partial matching'],
    1,'Use toContainText() when you only care that a substring or regex appears.'],
  ['O','B','What does `await expect(locator).not.toBeVisible()` assert?',null,
    ['That the locator matches zero elements','That the element exists but is hidden or absent from the visible area','That the element has display:none in CSS','That the element was never attached to the DOM'],
    1,'.not negates the assertion — the element must not be visible (hidden or detached).'],
  ['O','I','What does `await expect(locator).toHaveCount(3)` do?',null,
    ['Asserts the element has exactly 3 child nodes','Asserts that exactly 3 elements match the locator','Asserts the element\'s text is the number 3','Loops through 3 matching elements'],
    1,'toHaveCount() verifies the number of elements that match the locator expression.'],
  ['O','B','What does `await expect(page).toHaveURL(/dashboard/)` check?',null,
    ['That the page title contains "dashboard"','That the current browser URL matches the regex /dashboard/','That the page has a link to /dashboard','That the page element with id=dashboard is visible'],
    1,'toHaveURL() is a page-level assertion that auto-retries until the URL matches.'],
  ['O','I','What does `expect.soft(locator).toHaveText(\'Name\')` do differently from a regular assertion?',null,
    ['It records the failure without stopping the test','It waits twice as long before failing','It only asserts during the first test run','It suppresses the failure from the report'],
    0,'Soft assertions let you collect all failures in one run instead of stopping at the first.'],
  ['O','I','What does `await expect(locator).toHaveValue(\'user@test.com\')` check?',null,
    ['That the element\'s inner text is user@test.com','That an input element\'s current value property is user@test.com','That the element\'s placeholder is user@test.com','That the form was submitted with this email'],
    1,'toHaveValue() reads the value property of an <input>, <textarea>, or <select>.'],
  ['O','B','What does `await expect(locator).toBeChecked()` assert?',null,
    ['That the element has a CSS class called checked','That a checkbox or radio button is in the checked state','That the element passed a validation check','That the element has aria-required=true'],
    1,'toBeChecked() is the semantic assertion for checkbox/radio state.'],
  ['O','I','What does `await expect(response).toBeOK()` assert?',null,
    ['That the response body is not empty','That the HTTP status code is in the 200-299 range','That the response time is under 200 ms','That the response Content-Type is application/json'],
    1,'toBeOK() is an API-response assertion equivalent to status >= 200 && status < 300.'],
  ['O','A','What does `expect.poll(fn, { timeout: 30_000 })` do?',null,
    ['Runs the test 30 times','Calls fn repeatedly until its return value satisfies the chained matcher or the timeout expires','Waits 30 seconds then runs fn once','Mocks the timer so fn runs synchronously'],
    1,'expect.poll() is for retrying custom async functions — use it when there is no locator to observe.'],
  ['O','I','What is the default auto-retry timeout for web-first assertions in Playwright?',null,
    ['1 second','5 seconds','10 seconds','30 seconds'],
    1,'The default is 5 s (configurable via expect.timeout in playwright.config.ts).'],

  // ── Fill-in ×7 ────────────────────────────────────────────────
  ['F','B','Complete: `await expect(locator).toBe______()` to assert the element is visible',null,
    ['Visible','Shown','Displayed','Present'],
    0,'toBeVisible() is the standard visibility assertion.'],
  ['F','I','Complete: `await expect(locator).toHave______( \'user@test.com\' )` to check an input\'s value',null,
    ['Value','Text','Content','Input'],
    0,'toHaveValue() reads the value property of form elements.'],
  ['F','B','Complete: `await expect(page).toHave______(/dashboard/)` to check the current URL',null,
    ['URL','Path','Route','Address'],
    0,'toHaveURL() is the page-level URL assertion.'],
  ['F','I','Complete: `await expect(locator).toHave______( \'btn-primary\' )` to check a CSS class',null,
    ['Class','Style','Attribute','CSS'],
    0,'toHaveClass() checks that the element has the given class name.'],
  ['F','I','Complete: `await expect(locator).toHave______( \'aria-expanded\', \'true\' )` to check an attribute',null,
    ['Attribute','Property','Tag','Role'],
    0,'toHaveAttribute(name, value) checks a specific HTML attribute.'],
  ['F','I','Complete: `await expect(locator).toHave______( 4 )` to assert 4 items match the locator',null,
    ['Count','Length','Size','Total'],
    0,'toHaveCount(n) asserts the number of matched elements.'],
  ['F','A','Complete: `await expect.______( () => fetchStatus() ).toBe( \'done\' )` to poll a custom function',null,
    ['poll','retry','wait','check'],
    0,'expect.poll(fn) repeatedly calls fn until the matcher passes or timeout expires.'],

  // ── Find the Error ×6 ──────────────────────────────────────────
  ['E','B','What is wrong?\n```ts\nexpect(page.getByRole(\'heading\')).toHaveText(\'Welcome\');\n```',null,
    ['getByRole is not valid inside expect()','await is missing — the assertion runs detached and may not report failures','toHaveText() only accepts RegExp','The heading role requires a level option'],
    1,'Web-first assertions return a Promise; without await the check runs asynchronously and failures are lost.'],
  ['E','I','What is wrong?\n```ts\nawait expect(5).toBeVisible();\n```',null,
    ['5 is not a valid assertion target — toBeVisible() only works on Locators','5 should be wrapped in quotes','toBeVisible() requires a page argument too','expect() needs async/await on the number'],
    0,'toBeVisible() is a web-first assertion and only accepts Locator or Page, not plain values.'],
  ['E','B','What is wrong?\n```ts\nawait expect(locator).toHaveText(42);\n```',null,
    ['toHaveText() cannot be used with await','toHaveText() accepts a string or RegExp, not a number','The locator must be resolved first','42 needs to be converted with String()'],
    1,'toHaveText() only accepts string or RegExp — passing a number causes a TypeScript/runtime error.'],
  ['E','I','What is wrong?\n```ts\nawait expect(locator).not().toBeVisible();\n```',null,
    ['not is not a valid property on expect()','not is a property, not a method — it should be .not.toBeVisible()','toBeVisible() cannot be negated','not requires a reason string argument'],
    1,'.not is a property accessor, not a function — `expect(l).not.toBeVisible()` not `.not().toBeVisible()`.'],
  ['E','I','What is wrong?\n```ts\nawait expect(locator).toHaveValue(\'user@test.com\');\n// locator targets a <div>, not an input\n```',null,
    ['toHaveValue() should be toContainText() for divs','toHaveValue() should be toHaveAttribute() for divs','toHaveValue() only works on form elements with a value property','Divs cannot be targeted with locators'],
    2,'toHaveValue() reads the value property — meaningless on a div; use toHaveText() or toContainText() instead.'],
  ['E','A','What is wrong?\n```ts\nawait expect.soft(locator).toBeVisible();\nthrow new Error(\'stop\');\n// Soft failures are never reported\n```',null,
    ['expect.soft() suppresses all failures — use regular expect','The thrown error prevents the test framework from flushing soft assertion results','soft requires a second boolean argument','expect.soft() needs its own await block'],
    1,'Throwing before test end skips the Playwright framework\'s end-of-test flush — soft failures are lost.'],

  // ── Debug ×6 ──────────────────────────────────────────────────
  ['D','B','An assertion `await expect(locator).toBeVisible()` fails after 5 s even though the element appears after 6 s. What is the fix?',null,
    ['Add a manual sleep(6000) before the assertion','Pass { timeout: 10_000 } to extend the retry window','Use toBeEnabled() instead of toBeVisible()','Break the test into two tests'],
    1,'{ timeout: ms } overrides the default 5 s for this specific assertion.'],
  ['D','I','`await expect(locator).toHaveText(\'Hello\')` fails but the element clearly shows "Hello". What should you check first?',null,
    ['The locator might be matching the wrong element','The text might include hidden whitespace, newlines, or Unicode spaces','toHaveText() is broken in this version','The assertion needs force: true'],
    1,'toHaveText() trims whitespace by default but surrounding HTML nodes or invisible chars can still cause mismatches.'],
  ['D','B','`await expect(page).toHaveURL(\'/success\')` fails immediately after `click()`. Why?',null,
    ['toHaveURL() does not work on the default page','Navigation is async — the URL has not yet changed when the assertion first runs','click() does not trigger navigation','The URL check needs a regex not a string'],
    1,'toHaveURL() auto-retries so it will eventually pass once navigation completes — if it fails immediately, the navigation itself likely failed.'],
  ['D','I','`await expect(locator).toBeChecked()` fails on a custom toggle built with a <div aria-checked="true">. What is wrong?',null,
    ['toBeChecked() only works on <input type="checkbox"> elements','toBeChecked() reads aria-checked for elements with role="checkbox" — the div needs role="checkbox"','toBeChecked() requires force: true for ARIA elements','ARIA checkboxes must use toHaveAttribute() instead'],
    1,'toBeChecked() checks aria-checked when the element has a checkbox or radio role — add role="checkbox" to the div.'],
  ['D','I','Multiple soft assertions fail but the test report shows only one error. What is likely wrong?',null,
    ['expect.soft() only captures the first failure','The test throws or crashes before reaching the end where soft failures are flushed','Soft assertions are disabled in CI mode','The reporter does not support soft assertions'],
    1,'An unhandled throw or test crash before end-of-test prevents Playwright from flushing accumulated soft failures.'],
  ['D','A','`expect.poll()` resolves immediately with the wrong value on the first call. How do you add a delay between retries?',null,
    ['Add { delay: 2000 } to the poll options','Use the intervals option: { intervals: [2_000, 5_000] }','Poll cannot be configured — use a manual while loop','Add await page.waitForTimeout(2000) before expect.poll'],
    1,'The intervals option specifies milliseconds between successive retries.'],

  // ── Complete ×5 ──────────────────────────────────────────────
  ['C','B','Complete to assert a button is disabled.\n```ts\nawait expect(page.getByRole(\'button\', { name: \'Save\' })).toBe______();\n```',null,
    ['Disabled','Hidden','Inactive','Locked'],
    0,'toBeDisabled() is the semantic assertion for the disabled state.'],
  ['C','I','Complete to assert a paragraph contains the word "success".\n```ts\nawait expect(locator).______Text(\'success\');\n```',null,
    ['toContain','contain','includes','toHave'],
    0,'toContainText() asserts a partial text match.'],
  ['C','I','Complete to assert a list renders exactly 5 items.\n```ts\nawait expect(page.locator(\'li\')).toHave______( 5 );\n```',null,
    ['Count','Length','Total','Size'],
    0,'toHaveCount(n) checks how many elements match the locator.'],
  ['C','I','Complete to assert the URL matches the pattern /success.\n```ts\nawait expect(page).toHave______(/\\/success/);\n```',null,
    ['URL','Path','Route','Title'],
    0,'toHaveURL() is the page-level URL assertion.'],
  ['C','A','Complete to collect failures without stopping the test.\n```ts\nawait expect.______( locator ).toBeVisible();\n```',null,
    ['soft','silent','collect','lazy'],
    0,'expect.soft() records the failure and lets the test continue.'],

  // ── Logic ×5 ──────────────────────────────────────────────────
  ['L','B','Why must web-first assertions be awaited?',null,
    ['They are synchronous and await has no effect','They return a Promise; without await the assertion runs detached and failures go unreported','The Playwright runner requires await for syntax reasons','await is optional — it only affects timeout behavior'],
    1,'Without await, the Promise is created but not waited on — the assertion may run after the test already passed.'],
  ['L','I','When should you use `expect.poll(fn)` instead of a locator assertion?',null,
    ['When the assertion needs to run more than once','When you need to assert a condition that has no corresponding DOM element — e.g. an API response or a JS variable','When the locator is inside a shadow DOM','When the assertion value is a number'],
    1,'expect.poll() is for custom async conditions that cannot be observed via a locator.'],
  ['L','I','A test has three `expect.soft()` calls — the first and third fail. What does Playwright report?',null,
    ['Only the first failure — execution stops there','Only the last failure','Both failures are reported together at the end of the test','The test is marked as skipped'],
    2,'Soft assertions accumulate; all failures are reported together when the test ends normally.'],
  ['L','B','What is the key behavioral difference between `toHaveText()` and `toContainText()`?',null,
    ['toHaveText() matches the trimmed full text; toContainText() accepts a substring or regex','toHaveText() is case-insensitive; toContainText() is case-sensitive','toHaveText() works on inputs; toContainText() works on divs','They are identical'],
    0,'toHaveText() requires the whole text to match; toContainText() allows partial matches.'],
  ['L','A','How does `toHaveCSS(\'color\', \'rgb(0, 128, 0)\')` differ from `toHaveAttribute(\'style\', \'color: green\')`?',null,
    ['They are identical — both read the style attribute','toHaveCSS() reads the computed CSS value from the browser; toHaveAttribute() reads the raw style attribute string','toHaveCSS() is asynchronous; toHaveAttribute() is synchronous','toHaveAttribute() always wins when both are used together'],
    1,'toHaveCSS() reads computed styles (after cascading) — more reliable than reading the raw style attribute.'],

  // ── Real-World ×5 ─────────────────────────────────────────────
  ['R','B','After clicking "Login", you need to assert the user reached the dashboard. Which assertion do you use?',null,
    ['expect(page.getByText(\'dashboard\')).toBeVisible()','await expect(page).toHaveURL(\'/dashboard\')','expect(page.url()).toBe(\'/dashboard\')','await expect(page).toHaveTitle(\'dashboard\')'],
    1,'toHaveURL() auto-retries after navigation — the safest way to assert URL change.'],
  ['R','I','A toast notification appears briefly after a form submission. How do you assert it was shown?',null,
    ['expect(locator).toBeVisible() with a short timeout','await expect(page.getByRole(\'alert\')).toBeVisible()','page.waitForSelector(\'.toast\') then assert','Use expect.soft() for transient elements'],
    1,'toBeVisible() auto-retries — it will catch the toast even if the element takes a moment to mount.'],
  ['R','I','After clicking "Clear", you need to assert the search input is empty. Which assertion fits?',null,
    ['await expect(locator).toHaveValue(\'\')','await expect(locator).toBeEmpty()','await expect(locator).toHaveText(\'\')','Both A and B are valid for an input'],
    3,'toHaveValue(\'\') or toBeEmpty() both work for an input — toBeEmpty() is more concise.'],
  ['R','I','You want to assert a dropdown currently shows "Dark Mode". Which assertion is most precise?',null,
    ['await expect(locator).toContainText(\'Dark Mode\')','await expect(locator).toHaveValue(\'dark\')','await expect(locator).toHaveText(\'Dark Mode\')','await expect(locator).toHaveAttribute(\'selected\', \'true\')'],
    1,'toHaveValue() checks the selected <option>\'s value attribute — the most reliable for dropdowns.'],
  ['R','A','You fire a POST request from a test and need to assert the API returned 201. Which assertion do you use?',null,
    ['expect(response.status()).toBe(201)','await expect(response).toBeOK()','await expect(response).toHaveStatus(201)','expect(response).toHaveStatus(\'Created\')'],
    0,'toBeOK() only checks 200-299; for a specific status code use the generic expect with response.status().'],

  // ── Scenario ×4 ──────────────────────────────────────────────
  ['S','I','A page shows a loading spinner for 3 s then renders the actual heading. `toHaveText()` fires immediately and fails. What is the correct fix?',null,
    ['Add a 3 s sleep before the assertion','The assertion already auto-retries — check whether the locator targets the heading, not the spinner','Use toContainText() instead — it is more lenient','Use waitForSelector() first'],
    1,'toHaveText() already retries for up to 5 s — the issue is likely the locator matching the wrong element.'],
  ['S','A','You need to verify five separate UI properties after a form submit, and you want the report to show ALL failures in one run. How do you write the assertions?',null,
    ['Use five regular expect() calls — they already collect failures','Wrap all five in a try/catch','Use expect.soft() for all five assertions','Use toHaveCount(5) to check them in one call'],
    2,'expect.soft() on each assertion lets all five run regardless of individual failures.'],
  ['S','I','`await expect(locator).toHaveText(\'€ 1,000\')` fails even though the page shows "€ 1,000". What could cause this?',null,
    ['toHaveText() cannot handle Unicode characters','The € sign is rendered as an HTML entity (&euro;) that the browser resolves to € — the actual text should match','toHaveText() strips currency symbols','The comma inside the number breaks the string parser'],
    1,'Browsers resolve HTML entities before Playwright reads text, so the comparison should work — more likely the text includes non-breaking spaces or different Unicode code points.'],
  ['S','A','You want to poll a JS global `window.__jobDone` until it is true, with a 20 s timeout. Which approach is correct?',null,
    ['await expect(page.locator(\'body\')).toHaveAttribute(\'data-done\', \'true\')','await expect.poll(() => page.evaluate(() => window.__jobDone), { timeout: 20_000 }).toBe(true)','await page.waitForFunction(() => window.__jobDone, null, { timeout: 20_000 })','Both B and C are valid — expect.poll is cleaner'],
    3,'waitForFunction() is the older API; expect.poll() with page.evaluate() gives you the full matcher ecosystem and integrates with test reporting.'],

  // ── Additional batch ×50 ─────────────────────────────────────
  ['O','B','What does `expect(locator).toBeChecked()` assert?',null,
    ['The element has a CSS :checked pseudo-class only','The checkbox or radio button is in a checked state — equivalent to the checked property being true','The element is selected in a listbox','The element has attribute checked="true"'],
    1,'toBeChecked() is the idiomatic assertion for checkbox and radio state — auto-retries until checked or times out.'],
  ['O','I','What does `expect(locator).toHaveCSS("color", "rgb(255, 0, 0)")` assert?',null,
    ['The element has color: red in the stylesheet','The computed CSS value of the color property equals the given string — computed values use rgb() notation, not colour names','The element has a class containing red','The style attribute has color: red'],
    1,'toHaveCSS checks the computed (resolved) CSS value — always use computed format (rgb, px) not the authored format (red, 1rem).'],
  ['O','B','What does `expect(locator).toBeEmpty()` check?',null,
    ['The element has no children and no text','For input/textarea: value is ""; for other elements: no descendants and no text content','The DOM element does not exist','The element has display:none'],
    1,'toBeEmpty has different semantics for inputs vs other elements — inputs must have empty value; others must have no content.'],
  ['O','I','What does `expect(locator).toHaveValues(["a", "b"])` assert for a multi-select?',null,
    ['The element has both a and b as option values (unselected)','The multi-select element has exactly "a" and "b" selected — checks the array of selected option values','The element\'s text contains "a" or "b"','Asserts exactly 2 options exist'],
    1,'toHaveValues is the multi-select equivalent of toHaveValue — checks the array of selected values.'],
  ['O','I','What does `expect(page).toHaveScreenshot("name.png")` do on first run?',null,
    ['Fails because no baseline exists','Creates a baseline screenshot file in a __screenshots__ directory — subsequent runs compare against it','Passes trivially without a comparison','Requires a manual review step'],
    1,'On first run, toHaveScreenshot creates the baseline. On subsequent runs, it compares and fails if pixels differ beyond the threshold.'],
  ['O','B','What does `expect(locator).not.toBeVisible()` assert?',null,
    ['The element does not exist in the DOM','The element is not visible — either hidden, detached, or has display:none/visibility:hidden','The element has opacity: 0','The element is outside the viewport'],
    1,'not.toBeVisible() passes when the element is hidden by CSS or detached from the DOM — it auto-retries.'],
  ['O','I','What does `expect(response).toBeOK()` assert?',null,
    ['The response body is not empty','The HTTP response status code is in the 200-299 range','The response content-type is application/json','The response arrived within the timeout'],
    1,'toBeOK() is a convenience for any 2xx status — use expect(response.status()).toBe(201) for a specific code.'],
  ['O','A','What does `expect.configure({ timeout: 10_000 })` do?',null,
    ['Sets the timeout for all network requests','Returns a custom expect function with a default timeout of 10 seconds — assertions using this expect use 10 s instead of the global expect.timeout','Sets the global expect.timeout permanently','Creates a new assertion namespace'],
    1,'expect.configure returns a scoped expect — useful for sharing custom timeout or soft configuration across a helper module.'],
  ['O','I','What does `expect(locator).toHaveCount(0)` assert?',null,
    ['The first element has index 0','No elements matching the locator exist in the DOM','The locator matched 0 times per second','All elements are hidden'],
    1,'toHaveCount(0) is the idiomatic way to assert an element does not exist — auto-retries until count reaches 0 or timeout.'],
  ['O','B','What does `expect(locator).toBeEditable()` assert?',null,
    ['The element has the contenteditable attribute','The form field is not disabled and not readonly — it can receive user input','The element is in an editable state from the last action','The element\'s CSS cursor is "text"'],
    1,'toBeEditable checks that the control is interactive — not disabled and not readonly.'],
  ['O','I','What does `expect(locator).toHaveAttribute("aria-expanded", "true")` assert?',null,
    ['The element has any attribute named aria-expanded','The aria-expanded attribute is present AND its value equals "true" exactly','The element is expanded in the DOM tree','aria-expanded is true (truthy)'],
    1,'toHaveAttribute checks both presence and exact value — the string "true" is the value of the aria-expanded attribute.'],
  ['O','A','What is the difference between `toHaveText()` and `toContainText()`?',null,
    ['They are identical','toHaveText asserts the FULL text content of the element matches; toContainText asserts the text content INCLUDES the given substring — similar to toBe vs toContain','toHaveText is for headings; toContainText is for paragraphs','toContainText auto-retries; toHaveText does not'],
    1,'toHaveText: full match. toContainText: substring match. Use whichever is more appropriate — exact matches are more specific and catch more regressions.'],
  ['F','B','Complete to assert a checkbox is unchecked.\n```ts\nawait expect(page.getByRole("checkbox")).not.______();\n```',null,
    ['toBeChecked()','toBeEnabled()','toBeVisible()','toBeSelected()'],
    0,'not.toBeChecked() asserts the checkbox is in the unchecked state.'],
  ['F','I','Complete to assert computed background color.\n```ts\nawait expect(locator).toHaveCSS("background-color", "______");\n```',null,
    ['rgb(0, 128, 0)','green','#008000','0,128,0'],
    0,'Computed CSS uses rgb() notation — "green" and hex are the authored values, not the computed ones.'],
  ['F','B','Complete to assert no results exist.\n```ts\nawait expect(page.getByRole("listitem")).toHaveCount(______);\n```',null,
    ['0','null','undefined','[]'],
    0,'toHaveCount(0) asserts zero matching elements — auto-retries.'],
  ['F','I','Complete to assert a locator has a specific CSS class.\n```ts\nawait expect(locator).toHaveClass(/______/);\n```',null,
    ['active','class="active"','.active','[class=active]'],
    0,'toHaveClass accepts a string or regex matching the element\'s class list — just the class name without the dot.'],
  ['F','I','Complete to assert multiple selected options in a multi-select.\n```ts\nawait expect(page.getByRole("listbox")).toHaveValues(["en", "______"]);\n```',null,
    ['fr','\'fr\'','lang-fr','french'],
    0,'toHaveValues takes an array of selected option value strings — use the option\'s value attribute values.'],
  ['F','A','Complete to assert a visual snapshot.\n```ts\nawait expect(page).toHaveScreenshot("______");\n```',null,
    ['homepage.png','snapshot','screen.jpg','screenshot.webp'],
    0,'The filename argument identifies the baseline — choose a descriptive name; Playwright stores it in a __screenshots__ folder.'],
  ['F','B','Complete to assert the page URL contains "dashboard".\n```ts\nawait expect(page).toHaveURL(/______/);\n```',null,
    ['dashboard','#dashboard','.*dashboard.*','/dashboard/'],
    0,'The regex /dashboard/ matches any URL containing the word "dashboard".'],
  ['E','B','What is wrong?\n```ts\nexpect(await page.title()).toHaveTitle("Home");\n```',null,
    ['toHaveTitle cannot take a string','toHaveTitle is a page-level assertion: await expect(page).toHaveTitle("Home") — calling it on a string value is invalid','page.title() must not be awaited','await is not needed before expect()'],
    1,'toHaveTitle is called on expect(page) not expect(string) — it is a web-first assertion that auto-retries.'],
  ['E','I','What is wrong?\n```ts\nawait expect(locator).toHaveText(/Price: \\$\\d+/);\n// fails even though text shows "Price: $99"\n```',null,
    ['Regex is not supported by toHaveText','The regex uses \\$ to escape the dollar sign (valid in regex) but Playwright\'s toHaveText matches the element\'s FULL text — if the element also contains other text, the regex must match the full string. Use toContainText(/Price: \\$\\d+/) for partial matching.','Regex requires the i flag','toHaveText cannot match numbers'],
    1,'toHaveText requires the full text to match the regex; use toContainText for substring/partial regex matching.'],
  ['E','B','What is wrong?\n```ts\nawait expect(locator).toBe("visible");\n```',null,
    ['toBe() cannot take a string','toBe("visible") compares the locator object itself to the string — that is never equal. Use await expect(locator).toBeVisible() instead.','visible must be capitalized','await is not needed before expect(locator)'],
    1,'Playwright locator assertions have dedicated methods — never use toBe() with strings for visibility checks.'],
  ['E','I','What is wrong?\n```ts\nexpect.soft(locator).toBeVisible();\n// soft assertion runs but result is ignored\n```',null,
    ['soft assertions must use await','expect.soft() returns a Promise — without await the assertion is never settled, and failures are silently discarded. Always await soft assertions.','soft assertions cannot be used with locators','locator must be resolved before expect.soft'],
    0,'Missing await on soft assertions is as bad as missing await on regular assertions — the test may pass despite failures.'],
  ['E','A','What is wrong?\n```ts\nawait expect(locator).toHaveScreenshot("btn.png");\n// --update-snapshots was never run after a UI change\n```',null,
    ['toHaveScreenshot always passes on first run','The test fails because the existing baseline does not match the updated UI. Run npx playwright test --update-snapshots to regenerate the baseline, then commit the new snapshot.','toHaveScreenshot requires a special plugin','Snapshots expire after 30 days'],
    1,'After intentional UI changes, always update baselines with --update-snapshots and review them visually before committing.'],
  ['E','I','What is wrong?\n```ts\nawait expect(locator).toHaveText(["Item 1", "Item 2"]);\n// locator matches 3 elements\n```',null,
    ['toHaveText does not accept arrays','toHaveText with an array asserts exact correspondence — 3 elements vs 2 strings causes a mismatch. Include all 3 expected strings in the array or scope the locator to only 2 elements.','Array comparison is not ordered','toHaveText requires allTextContents() first'],
    1,'toHaveText with an array does a 1:1 ordered comparison — array length must match element count.'],
  ['D','B','`toHaveText("Submit")` fails but the button visually shows "Submit". What should you check?',null,
    ['The button element does not have text','1) The text may have leading/trailing whitespace. 2) The button may have an icon element that adds extra text to textContent. 3) The text may use a different Unicode space character. Use { trim: true } or toContainText for partial matching.','Add await before expect','Use toHaveValue instead'],
    0,'Whitespace and hidden icon text are common causes — inspect the element\'s textContent in DevTools or the Playwright Inspector.'],
  ['D','I','`expect(locator).toHaveClass("btn-active")` fails even though DevTools shows the class is present. Why?',null,
    ['toHaveClass is broken in Firefox','toHaveClass checks the class list — if the element has multiple classes like "btn btn-active", pass the exact class string "btn btn-active" OR use a regex: /btn-active/. Passing only "btn-active" when the element has "btn btn-active" may fail depending on the matcher mode.','DevTools is not reliable','Classes with hyphens require escaping'],
    1,'toHaveClass with a string expects the full classList string — use a regex to match a single class within a multi-class element.'],
  ['D','B','`toHaveURL("/dashboard")` fails even though the browser shows "http://localhost:3000/dashboard". What is the fix?',null,
    ['Add a http:// prefix to the expected string','With a baseURL configured, use toHaveURL("/dashboard") which should work — if it fails, the issue is likely a trailing slash mismatch or query parameters. Try toHaveURL(/dashboard/) as a regex for robustness.','toHaveURL requires a full absolute URL','Add await before page.waitForURL'],
    1,'toHaveURL with a partial string does a contains check when used as regex — for exact paths, verify the trailing slash and query string match exactly.'],
  ['D','A','`toHaveScreenshot()` passes locally but fails in CI with minor pixel differences. What are two solutions?',null,
    ['Switch to a text-based assertion','1) Set maxDiffPixels or threshold: 0.01 in the options to allow minor rendering differences between OS/GPU. 2) Run tests with a fixed Docker image that matches CI\'s rendering environment exactly — Docker eliminates font and antialiasing differences.','Switch to headless: false in CI','Reduce the viewport size'],
    1,'Pixel differences between OS/GPU are the classic cross-environment snapshot problem — Docker or threshold relaxation are the standard solutions.'],
  ['D','I','`expect.soft()` failures are not appearing in the report. What is the likely cause?',null,
    ['soft assertions are a beta feature','Soft failures are flushed at the end of the test only if the test completes. If the test throws before completing (e.g. from a hard assertion or unhandled error), soft failures are NOT reported. Ensure the test body runs to completion.','The reporter does not support soft assertions','Soft assertions require flush() to be called'],
    1,'Hard assertions and unhandled errors abort the test — call expect(page).toBeVisible() as a final hard assertion to ensure soft failures are captured.'],
  ['C','B','Complete to assert text is not empty.\n```ts\nawait expect(page.getByRole("status")).not.______();\n```',null,
    ['toBeEmpty()','toHaveText("")','toBeNull()','toBe(undefined)'],
    0,'not.toBeEmpty() asserts the element has some content — the correct inverse of toBeEmpty().'],
  ['C','I','Complete to assert a regex URL match.\n```ts\nawait expect(page).toHaveURL(______(/checkout\\/step\\//));\n```',null,
    ['/checkout\\/step\\//','"checkout/step/"','new RegExp("checkout")','expect.stringContaining'],
    0,'toHaveURL accepts regex directly — /checkout\\/step\\// matches any URL containing that path.'],
  ['C','B','Complete to assert the element is not in the DOM.\n```ts\nawait expect(locator).toHaveCount(______);\n```',null,
    ['0','null','-1','undefined'],
    0,'toHaveCount(0) asserts the locator matches no elements — the element does not exist.'],
  ['C','I','Complete a soft assertion.\n```ts\nawait expect.______( locator ).toBeVisible();\n```',null,
    ['soft','weak','optional','may'],
    0,'expect.soft(locator) creates a soft assertion that does not stop the test on failure.'],
  ['C','A','Complete to use a custom timeout for one assertion.\n```ts\nawait expect(slowLocator).toBeVisible({ ______: 15_000 });\n```',null,
    ['timeout','wait','maxWait','delay'],
    0,'The timeout option in assertion options overrides the global expect.timeout for that one assertion.'],
  ['L','B','Why do web-first assertions auto-retry?',null,
    ['To handle flaky network connections','Browser UIs are asynchronous — elements appear, change text, and become enabled over time. Auto-retry makes assertions robust against the timing uncertainty inherent in UI rendering.','To improve test performance','Because JavaScript is single-threaded'],
    1,'Auto-retry is the fundamental design principle — it replaces fragile fixed waits with dynamic waiting tied to actual UI state.'],
  ['L','I','Can you use `toHaveText()` on a locator that matches multiple elements?',null,
    ['No — strict mode throws','Yes — pass an array of expected strings: expect(locator).toHaveText(["a","b","c"]) performs ordered 1:1 matching','Only if you use .first()','Only with toContainText'],
    1,'toHaveText with an array asserts each element\'s text in order — the array length must equal the element count.'],
  ['L','I','What happens when a soft assertion fails and a subsequent hard assertion also fails?',null,
    ['Only the soft failure is reported','Both failures are reported — the soft assertion failure is queued; the hard assertion aborts the test and all queued soft failures are included in the report','The hard failure takes precedence and soft failures are ignored','Soft failures are cleared when a hard failure occurs'],
    1,'Both are reported — soft failures queue up and hard failures flush them along with the hard failure message.'],
  ['L','B','Can `expect()` be used without `await` for any assertions?',null,
    ['Never — all expects must be awaited','Yes — non-web-first assertions like expect(5).toBe(5) are synchronous and don\'t need await. Web-first assertions like toBeVisible() return Promises and must be awaited.','All locator assertions are synchronous','await is optional — it only affects timing'],
    1,'Synchronous matchers (toBe, toEqual, toContain) don\'t need await; web-first locator/page assertions do.'],
  ['L','A','Does `expect(locator).toBeVisible()` pass if the element exists but has `visibility: hidden`?',null,
    ['Yes — the element is in the DOM','No — toBeVisible checks that the element is both in the DOM AND has visible CSS (not hidden by display:none, visibility:hidden, or opacity:0 combined with pointer-events:none)','Depends on the browser engine','Yes — visibility:hidden elements are considered visible by ARIA standards'],
    1,'toBeVisible uses strict visibility semantics — hidden, zero-opacity, and display:none all count as not visible.'],
  ['R','B','You want to assert a success toast appears after form submission. The toast auto-dismisses in 3 seconds. How?',null,
    ['Increase the timeout to 3 seconds','await expect(page.getByRole("status")).toBeVisible() — the default timeout (5s) covers the toast appearance, and the auto-dismiss happens after the assertion passes','Use toContainText with timeout: 1000','Mock the toast component to prevent auto-dismiss'],
    1,'The assertion just needs to catch the toast while it\'s visible — the default 5 s timeout is usually sufficient for a 3 s visible toast.'],
  ['R','I','You need to verify a table has exactly 10 rows after filtering. What is the assertion?',null,
    ['expect(await page.getByRole("row").count()).toBe(10)','await expect(page.getByRole("row")).toHaveCount(10)','Both A and B — B auto-retries and is preferred','await page.waitForFunction(() => document.querySelectorAll("tr").length === 10)'],
    2,'toHaveCount(10) is preferred because it auto-retries — the row count may change dynamically as filters apply.'],
  ['R','I','You need to assert that a form field shows an accessible error message. Which assertion best captures this?',null,
    ['expect(await page.locator(".error").textContent()).toContain("required")','await expect(page.getByRole("alert")).toContainText("required") — or assert the input has aria-invalid and is associated with the error message via aria-describedby','Check the network request for a 400 response','Use toHaveAttribute("error", "required")'],
    1,'Testing the accessible error presentation (alert role, aria-invalid) verifies both functionality and accessibility — a combined assertion.'],
  ['R','A','You want to assert a drag-and-drop reorder result. After dragging item 2 to position 1, how?',null,
    ['await expect(page.getByRole("listitem").first()).toHaveText("Item 2")','Drag then assert: await expect(page.getByRole("listitem").nth(0)).toHaveText("Item 2") — verify the first item now shows the dragged content','Compare screenshots before and after','Use toHaveValues on the list'],
    1,'Text assertion on the first item verifies the reorder — content-based checks are more resilient than position-based pixel checks.'],
  ['R','B','A numeric counter should show "42" after 5 rapid clicks on a "+1" button. How do you assert it?',null,
    ['Wait 5 seconds then assert','Click 5 times then await expect(counter).toHaveText("42") — toHaveText auto-retries so it handles any async counter update','Use expect.poll to check every 100ms','Assert after each click'],
    1,'toHaveText auto-retries — click all 5 times then let the assertion wait for the final state.'],
  ['S','B','`toHaveCSS("color", "red")` fails even though DevTools shows "color: red". What is wrong?',null,
    ['toHaveCSS does not support color assertions','DevTools shows "red" as the authored value but the computed value is "rgb(255, 0, 0)". Use toHaveCSS("color", "rgb(255, 0, 0)") — Playwright works with computed values.','color requires the # notation','toHaveCSS checks inline styles only'],
    0,'Computed CSS values use rgb() — always check the computed (not authored) representation when writing toHaveCSS assertions.'],
  ['S','I','A card component shows sale price in red and regular price in grey. How do you differentiate them in assertions?',null,
    ['Assert both prices have different text','Assert the sale price element has toHaveCSS("color", "rgb(220, 38, 38)") and the regular price has a different computed colour — combining text and colour assertions verifies both content and presentation','Use toHaveClass("text-red-600")','Check the aria-label on each price element'],
    1,'Combining toHaveText and toHaveCSS verifies content accuracy AND visual presentation in one test.'],
  ['S','A','You compare screenshots between feature branches and find a 3-pixel difference in button padding. Should you update the baseline?',null,
    ['Yes — always update to match the current UI','Investigate first: is the 3px difference intentional (design change) or accidental (a regression)? Only update the baseline after confirming it is an intended change — never update blindly.','No — 3px is within acceptable range','Yes — all pixel differences are acceptable'],
    1,'Snapshot updates are a deliberate decision — always review the visual diff before updating baselines to avoid accidentally accepting regressions.'],
  ['S','I','You want to assert that after switching themes, the background changes from white to dark. How?',null,
    ['Take two screenshots and compare with diff','Assert before: await expect(body).toHaveCSS("background-color", "rgb(255,255,255)"), click theme toggle, assert after: await expect(body).toHaveCSS("background-color", "rgb(17,24,39)")','Use toHaveClass("dark") on the html element','Check localStorage for the theme value'],
    1,'Computed CSS assertions on the background colour directly verify the visual theme change — more reliable than class or storage checks.'],
],
config: [
  // ── Output ×12 ────────────────────────────────────────────────
  ['O','B','What file contains all Playwright test configuration?',null,
    ['tests/setup.ts','playwright.config.ts','jest.config.ts','tsconfig.json'],
    1,'playwright.config.ts at the project root exports a defineConfig() object.'],
  ['O','B','What does the `use` block in playwright.config.ts configure?',null,
    ['The list of test files to include','Browser-level defaults applied to every test','The reporter format for CI','The Node.js version requirement'],
    1,'use sets defaults like baseURL, headless, viewport, trace, screenshot, and video.'],
  ['O','I','What is the difference between `timeout` and `expect.timeout` in the config?',null,
    ['They are the same setting with two names','timeout limits per-test wall-clock time; expect.timeout controls how long assertions auto-retry','timeout applies to CI; expect.timeout applies locally','expect.timeout is deprecated — use timeout for both'],
    1,'A test can run for 30 s (timeout) while each assertion retries for up to 5 s (expect.timeout).'],
  ['O','B','What does `retries: 2` in the config do?',null,
    ['Runs every test 3 times regardless of result','Re-runs a failed test up to 2 additional times before marking it as failed','Retries flaky assertions 2 times within a test','Restarts the browser 2 times per test file'],
    1,'retries gives failed tests a second (and third) chance — essential for reducing flakiness in CI.'],
  ['O','I','What does `fullyParallel: true` change compared to the default?',null,
    ['It runs all projects simultaneously instead of sequentially','It runs tests within the same file in parallel, not just across files','It disables retries for faster parallel execution','It assigns one worker per test file regardless of count'],
    1,'By default only files run in parallel; fullyParallel: true also parallelises tests within each file.'],
  ['O','B','What does setting `baseURL: \'http://localhost:3000\'` in `use` enable?',null,
    ['Playwright automatically navigates to that URL at test start','Tests can call page.goto(\'/login\') with relative paths instead of full URLs','The webServer starts on port 3000','All network requests are intercepted on that host'],
    1,'With baseURL set, relative URLs in goto() and request methods are resolved against it.'],
  ['O','I','What does the `projects` array control?',null,
    ['The list of test spec files to include','Which browsers and device profiles tests run against','The order in which test files execute','The number of workers per test run'],
    1,'Each project entry names a browser/device and can override the shared use settings.'],
  ['O','I','What does `trace: \'on-first-retry\'` do?',null,
    ['Records a trace for every test run','Records a trace only when a test is retried for the first time','Replays the last recorded trace on retry','Disables tracing except during retries'],
    1,'on-first-retry keeps traces only for tests that actually fail, minimising disk usage.'],
  ['O','B','What does the `webServer` config option do?',null,
    ['Configures the Playwright Test server for distributed runs','Starts a local dev server before tests and tears it down after','Sets the browser\'s network proxy server','Defines the base URL for API requests'],
    1,'webServer = automatic dev server lifecycle management — no separate terminal needed.'],
  ['O','I','What does `workers: undefined` mean in Playwright config?',null,
    ['Tests run with zero workers (serial)','Playwright chooses the worker count automatically based on CPU cores','All workers are disabled and tests run in-process','One worker per browser project'],
    1,'undefined lets Playwright auto-scale workers — typically half the CPU cores.'],
  ['O','I','What does `globalSetup` point to?',null,
    ['A file that runs before every individual test','A file that runs once before the entire test suite','A hook that sets global variables in each test file','The setup block inside playwright.config.ts'],
    1,'globalSetup runs once per suite — perfect for expensive operations like DB seeding or shared auth.'],
  ['O','A','What does `reuseExistingServer: !process.env.CI` do in the webServer config?',null,
    ['Skips starting the server entirely in CI','Reuses a running server locally so dev startup is skipped, but always starts fresh in CI','Forces the server to restart on every test in CI','Shares the server process across multiple workers'],
    1,'!process.env.CI = true locally (reuse), false in CI (always start fresh for a clean environment).'],

  // ── Fill-in ×7 ────────────────────────────────────────────────
  ['F','B','Complete: `export default ______Config({ … })` — the wrapper function from @playwright/test',null,
    ['define','create','make','export'],
    0,'defineConfig() provides TypeScript types and IDE autocomplete for the config object.'],
  ['F','I','Complete: `use: { ______: \'http://localhost:3000\' }` to set the shared base URL',null,
    ['baseURL','host','origin','url'],
    0,'baseURL enables relative paths in page.goto() and request methods.'],
  ['F','B','Complete: `retries: process.env.CI ? 2 : ______` for zero retries locally',null,
    ['0','false','null','undefined'],
    0,'0 retries locally means tests fail fast without masking flakiness during development.'],
  ['F','I','Complete: `{ name: \'chromium\', use: { ...______[\'Desktop Chrome\'] } }`',null,
    ['devices','browsers','presets','profiles'],
    0,'devices is an object exported from @playwright/test containing browser+viewport presets.'],
  ['F','I','Complete: `trace: \'______-first-retry\'` to record traces only on retries',null,
    ['on','capture','record','save'],
    0,'on-first-retry is the recommended trace strategy — captures only failed tests.'],
  ['F','I','Complete: `______Server: { command: \'npm run dev\', url: \'http://localhost:3000\' }`',null,
    ['web','dev','local','app'],
    0,'webServer is the config key for automatic dev server lifecycle management.'],
  ['F','A','Complete: `______: true` to run tests within the same file in parallel',null,
    ['fullyParallel','parallel','concurrent','async'],
    0,'fullyParallel: true enables intra-file parallelism on top of the default inter-file parallelism.'],

  // ── Find the Error ×6 ──────────────────────────────────────────
  ['E','B','What is wrong?\n```ts\nexport default {\n  testDir: \'./tests\',\n  timeout: 30_000,\n};\n```',null,
    ['testDir is not a valid config key','The config object must be wrapped in defineConfig()','timeout should be a string like \'30s\'','The file must be named playwright.config.js not .ts'],
    1,'Exporting a plain object loses TypeScript type-checking and IDE autocomplete — always use defineConfig().'],
  ['E','I','What is wrong?\n```ts\ntimeout: 5_000,  // intended to control assertion retry window\n```',null,
    ['timeout is not a valid top-level config key','timeout controls per-test duration; assertion retry uses expect: { timeout: 5_000 }','5_000 uses invalid numeric separators','The unit should be seconds not milliseconds'],
    1,'timeout and expect.timeout are separate settings — mixing them up causes assertions to time out at the wrong interval.'],
  ['E','I','What is wrong?\n```ts\nretries: true,\n```',null,
    ['retries must be a number (0, 1, 2 …), not a boolean','retries: true enables infinite retries','true is interpreted as 1 retry — it works but is not explicit','retries is not a valid config key'],
    0,'retries expects a number; boolean true coerces to 1 in JS but is semantically wrong and confusing.'],
  ['E','B','What is wrong?\n```ts\nuse: { timeout: 30_000 }\n```',null,
    ['timeout is not a valid key inside the use block','The use block only accepts browser flags like headless','timeout in use is correct and overrides the global timeout','use is only for locator options'],
    0,'timeout belongs at the top level of defineConfig, not inside use — inside use it is silently ignored.'],
  ['E','I','What is wrong?\n```ts\nwebServer: {\n  command: \'npm run dev\',\n  url: \'http://localhost:3000\',\n}\n// Tests fail when a dev server is already running\n```',null,
    ['url should be port not a full URL','command should be an array not a string','reuseExistingServer is missing — Playwright tries to start a second server and fails','webServer requires a ready timeout'],
    2,'Without reuseExistingServer: true, Playwright fails if the port is already occupied.'],
  ['E','A','What is wrong?\n```ts\nglobalSetup: async () => {\n  // setup code inline\n}\n```',null,
    ['globalSetup must export a default async function from a separate file, not an inline function','Inline functions are slower than file-based setups','globalSetup requires TypeScript not JavaScript','The async keyword is not supported here'],
    0,'globalSetup must be a file path string pointing to a module with a default export function.'],

  // ── Debug ×6 ──────────────────────────────────────────────────
  ['D','B','Tests pass locally but assertions time out in CI after exactly 5 s. What should you check?',null,
    ['Increase timeout in the use block','The CI machine may be slower — increase expect.timeout in the config','Switch from fullyParallel to sequential','Disable retries in CI'],
    1,'expect.timeout controls assertion retry duration; CI machines are slower and often need a higher value.'],
  ['D','I','`retries: process.env.CI ? 2 : 0` shows 0 retries in CI. What is the likely cause?',null,
    ['The ternary operator syntax is wrong','process.env.CI is not set in the CI environment — set it to "true" in the CI pipeline','retries: 0 is the maximum — use retries: 2 on both sides','The config file is not being read in CI'],
    1,'CI environments (GitHub Actions, Jenkins, etc.) do not automatically set CI — add CI: true to the pipeline env.'],
  ['D','I','Tests are slow even with `workers: 4`. What does adding `fullyParallel: true` change?',null,
    ['It doubles the worker count to 8','It allows tests within the same file to run in parallel, not just across files','It disables the per-test timeout for faster execution','It caches browser launches between workers'],
    1,'Without fullyParallel, tests in a single file run sequentially even with multiple workers.'],
  ['D','B','`page.goto(\'/login\')` throws "Invalid URL" even though baseURL is set. What is the likely cause?',null,
    ['Relative URLs are not supported in Playwright','The use block setting is inside a project that overrides baseURL with undefined','goto() requires a full URL — use template literals instead','baseURL needs a trailing slash'],
    1,'A project-level use block can accidentally shadow or remove the global baseURL — check project overrides.'],
  ['D','I','The webServer starts a new process on every `npx playwright test` run even locally, slowing startup. What option fixes this?',null,
    ['Set workers: 1 to reduce server load','Add reuseExistingServer: true (or !process.env.CI) to reuse a running server','Use the --no-server CLI flag','Move webServer to globalSetup'],
    1,'reuseExistingServer: true skips launching if the URL is already responding.'],
  ['D','A','globalSetup saves auth state to auth.json but tests still hit the login page. What is missing?',null,
    ['globalSetup must be async','storageState: \'auth.json\' must be set in the use block so tests load it','The auth.json file must be committed to the repo','globalSetup runs after tests by default'],
    1,'Saving storageState in globalSetup is only half — the use block must point to it so each test\'s browser context loads the saved cookies/localStorage.'],

  // ── Complete ×5 ──────────────────────────────────────────────
  ['C','B','Complete a project entry for Firefox.\n```ts\n{ name: \'firefox\', use: { ...devices[\'______\'] } }\n```',null,
    ['Desktop Firefox','firefox-desktop','Firefox Latest','browser-firefox'],
    0,'The exact key is \'Desktop Firefox\' in the Playwright devices preset map.'],
  ['C','I','Complete to set the assertion retry window to 8 s.\n```ts\nexpect: { ______: 8_000 },\n```',null,
    ['timeout','retryTimeout','assertTimeout','wait'],
    0,'expect.timeout controls how long web-first assertions keep retrying.'],
  ['C','I','Complete to reuse an existing server locally but always start fresh in CI.\n```ts\nreuseExistingServer: ______,\n```',null,
    ['!process.env.CI','process.env.CI','true','false'],
    0,'!process.env.CI = true locally (reuse), false in CI (start fresh).'],
  ['C','I','Complete to capture screenshots only on test failure.\n```ts\nuse: { screenshot: \'______\' }\n```',null,
    ['only-on-failure','on-fail','failure','always'],
    0,'The valid values are: off · on · only-on-failure.'],
  ['C','A','Complete the reporter array for HTML + GitHub Actions output.\n```ts\nreporter: [[\'______\'], [\'github\']],\n```',null,
    ['html','report','output','junit'],
    0,'html generates the interactive Playwright report; github annotates PR checks.'],

  // ── Logic ×5 ──────────────────────────────────────────────────
  ['L','B','Why is `retries: process.env.CI ? 2 : 0` the recommended pattern?',null,
    ['CI machines are faster so they need more retries','CI detects flakiness correctly; retries locally hide bugs during development','0 retries is invalid outside CI','CI runs single-threaded and needs retries to compensate'],
    1,'Retries in CI catch network/timing flakiness; zero retries locally force you to fix flaky tests immediately.'],
  ['L','I','What is the difference between `workers` and `fullyParallel`?',null,
    ['They are synonyms — setting one sets the other','workers controls how many parallel processes run; fullyParallel controls whether tests within a file run in parallel','fullyParallel sets workers to the maximum CPU count','workers only applies to CI; fullyParallel applies locally'],
    1,'workers = concurrency level; fullyParallel = whether intra-file parallelism is enabled.'],
  ['L','I','Why should `globalSetup` handle authentication instead of `beforeEach`?',null,
    ['beforeEach does not support async operations','globalSetup runs once for the suite — saves a browser launch and login per test; beforeEach repeats the full login on every test','globalSetup has access to test fixtures; beforeEach does not','Playwright requires authentication in globalSetup'],
    1,'Login once → save storageState → every test loads cookies instantly. beforeEach would repeat the full login flow N times.'],
  ['L','B','What is the purpose of spreading `...devices[\'Desktop Chrome\']` into a project?',null,
    ['It imports the Chromium browser binary','It applies Playwright\'s built-in viewport, userAgent, and browser settings for that device','It clones the test files for Chrome-specific runs','It sets the network conditions for Chrome emulation'],
    1,'devices presets bundle viewport size, user agent string, and device pixel ratio into one spread.'],
  ['L','A','A test overrides `timeout` using `test.setTimeout(60_000)`. What takes precedence?',null,
    ['The config always wins — test.setTimeout is ignored','test.setTimeout overrides the global config timeout for that specific test','They are added together: 30_000 + 60_000 = 90_000 ms','test.setTimeout only works inside beforeEach'],
    1,'test.setTimeout() overrides the config timeout for that individual test — config is the default, not the ceiling.'],

  // ── Real-World ×5 ─────────────────────────────────────────────
  ['R','B','You want all tests to use `http://localhost:4000` without writing it in every test. What do you configure?',null,
    ['Set url: \'http://localhost:4000\' in each test file','Set baseURL: \'http://localhost:4000\' in the use block','Set origin in the projects array','Use a .env file — Playwright reads it automatically'],
    1,'baseURL in use is the single source of truth for the host; all tests can then use relative paths.'],
  ['R','I','You need tests to run in Chrome, Firefox, and Safari. How do you configure this?',null,
    ['Pass --browser=all to the CLI','Add three entries to the projects array, each spreading the appropriate devices preset','Set browsers: [\'chromium\', \'firefox\', \'webkit\'] in use','Run npx playwright test three times with different --browser flags'],
    1,'projects is the standard multi-browser setup — one entry per engine.'],
  ['R','I','You want traces captured for every failed test in CI. Which trace option do you use?',null,
    ['trace: \'always\'','trace: \'on-failure\'','trace: \'retain-on-failure\'','trace: \'on-first-retry\''],
    2,'retain-on-failure keeps traces for tests that fail regardless of whether they were retried.'],
  ['R','I','You want the dev server to start automatically before tests. What config block do you add?',null,
    ['server: { command: \'npm run dev\' }','preTest: { start: \'npm run dev\' }','webServer: { command: \'npm run dev\', url: \'http://localhost:3000\' }','globalSetup: \'./start-server.ts\''],
    2,'webServer with command + url is the built-in server lifecycle mechanism.'],
  ['R','A','You need to log in once and share the session across all 200 tests. What is the architecture?',null,
    ['Use beforeAll in every test file to log in','Create a globalSetup that logs in and saves storageState, then set use: { storageState } in the config','Use a shared page fixture with session persistence','Set cookies manually in each test via page.context().addCookies()'],
    1,'globalSetup + storageState is the canonical Playwright pattern for shared authentication.'],

  // ── Scenario ×4 ──────────────────────────────────────────────
  ['S','I','A test suite has 500 tests and runs in 8 minutes locally but 40 minutes in CI. What config changes would help?',null,
    ['Increase workers and enable fullyParallel — CI may have defaulted to workers: 1','Reduce retries to 0 in CI — retries are causing repetition','Set fullyParallel: false — parallel tests compete for resources in CI','Remove the webServer config — it adds startup time'],
    0,'CI environments often limit workers — explicitly setting workers: N and fullyParallel: true can dramatically cut runtime.'],
  ['S','I','You add a new device project for iPhone 13. Tests that worked in Chromium fail on WebKit. How do you isolate which project fails?',null,
    ['Run with --browser=webkit','Run with --project=\'mobile-safari\' to limit execution to that project only','Set workers: 1 and check logs','Remove other projects temporarily'],
    1,'--project=\'mobile-safari\' runs only that project\'s tests — useful for isolating cross-browser failures.'],
  ['S','A','The CI pipeline runs E2E tests but the test report shows no traces even though trace: \'on-first-retry\' is set. What went wrong?',null,
    ['Traces are only available in headed mode','The CI artifact upload step is missing — traces are generated but not persisted','on-first-retry requires retries: 1 or higher — with retries: 0 no retry occurs and no trace is captured','trace files are deleted after each test in CI'],
    2,'on-first-retry only captures a trace when a retry happens — with retries: 0 tests fail immediately and no trace is saved.'],
  ['S','A','You want different timeouts for smoke tests vs full regression tests in the same project. How do you achieve this?',null,
    ['Create two playwright.config.ts files and switch between them with --config','Use test.setTimeout() in individual tests or test.describe blocks to override the global timeout','Set two timeout values in the config separated by commas','Timeouts cannot vary within a project — use separate projects'],
    1,'test.setTimeout() per test or test.describe overrides the global timeout — no need for separate configs.'],

  // ── Additional batch ×50 ─────────────────────────────────────
  ['O','B','What does the `use` block in playwright.config.ts configure?',null,
    ['Only the browser launch options','Default options applied to every browser context created during the test run — viewport, locale, storageState, trace, etc.','Options for the test runner process only','Only the baseURL and headless settings'],
    1,'use: is the global default for every test\'s browser context — any option from browser.newContext() can go here.'],
  ['O','I','What does `testIdAttribute: "data-cy"` in the config do?',null,
    ['Makes Cypress compatible with Playwright','Changes what attribute getByTestId() looks for — instead of data-testid it now matches elements with data-cy','Adds a data-cy attribute to all elements','Generates test IDs automatically'],
    1,'testIdAttribute lets teams use their preferred test ID attribute without changing every getByTestId() call.'],
  ['O','B','What does the `reporter: "html"` option generate?',null,
    ['An HTML file inline in the terminal','A detailed interactive HTML report in playwright-report/ with test results, screenshots, traces, and videos','A coverage report in HTML format','A report sent to an HTML email'],
    1,'The HTML reporter generates an interactive web app — open it with npx playwright show-report.'],
  ['O','I','What does `trace: "on-first-retry"` capture?',null,
    ['A trace for every test','A trace only when a test fails and is being retried for the first time — helps diagnose intermittent failures','A trace for the first test in each file','A trace whenever a network request is made'],
    1,'on-first-retry is the recommended default — it saves the trace only when you actually need it (a retry = something went wrong).'],
  ['O','B','What does `screenshot: "only-on-failure"` do?',null,
    ['Takes screenshots every second during the test','Takes an automatic screenshot only when a test fails — stored in test-results/','Disables screenshots entirely','Takes a screenshot before and after each test'],
    1,'only-on-failure is a practical default — captures visual evidence of failures without generating screenshots for passing tests.'],
  ['O','I','What does the `webServer.reuseExistingServer` option do?',null,
    ['Reuses the browser from the previous test run','If the server is already running on the configured port, Playwright uses it instead of starting a new one — prevents "port in use" errors during development','Saves the server state between test runs','Shares one server across multiple playwright.config.ts files'],
    1,'reuseExistingServer: true is the developer ergonomics option — your already-running dev server is reused without restarting.'],
  ['O','A','What does the `globalTimeout` config option set?',null,
    ['The timeout for each individual test','A maximum wall-clock time for the entire test suite — if the suite exceeds this, it is aborted','The timeout for globalSetup and globalTeardown','The timeout for browser launch'],
    1,'globalTimeout is a CI safeguard — prevent runaway suites from consuming infinite CI minutes by setting a hard cap.'],
  ['O','I','What does `use: { locale: "de-DE" }` do?',null,
    ['Translates test output to German','Sets the browser\'s locale — affects navigator.language, Intl formatting, and Accept-Language headers sent to the server','Translates error messages to German','Changes the keyboard layout'],
    1,'locale emulation lets you test date/number formatting and locale-specific UI without changing the OS setting.'],
  ['O','B','What does `retries: 2` in the config do?',null,
    ['Runs every test exactly 3 times','Re-runs a test up to 2 additional times if it fails — the test runs once, then up to twice more on failure','Runs the 2 most recently failed tests','Retries network requests 2 times'],
    1,'retries: 2 means a failing test gets 3 total attempts (1 original + 2 retries) before being marked as failed.'],
  ['O','I','What does `use: { actionTimeout: 5000 }` configure?',null,
    ['The timeout for page.goto() navigations','The default timeout for individual Playwright actions like click(), fill(), and press() — overrides the implicit action timeout','The timeout for each test step','The maximum time for network requests'],
    1,'actionTimeout is separate from the test timeout — it limits how long each single action can wait for preconditions.'],
  ['O','B','What does `use: { video: "on" }` do?',null,
    ['Records only failing tests','Records a video of every test run — videos are saved in test-results/ regardless of pass or fail','Enables live video streaming of tests','Records only network traffic'],
    1,'video: "on" records every test — useful for debugging but generates large storage. retain-on-failure is more storage-efficient.'],
  ['O','A','What does the `expect.timeout` config option set?',null,
    ['The timeout for the entire expect() chain','The default timeout for web-first assertion retries — how long assertions like toBeVisible() wait before failing','The timeout for expect.poll()','The time between assertion retries'],
    1,'expect.timeout is independent of the test timeout — an assertion can retry for 5 s while the overall test has 30 s.'],
  ['F','B','Complete to set the base URL globally.\n```ts\nexport default defineConfig({\n  use: { ______: "http://localhost:3000" },\n});\n```',null,
    ['baseURL','url','origin','host'],
    0,'baseURL in use: resolves relative URLs in page.goto() and other navigation methods.'],
  ['F','I','Complete to set the custom test ID attribute.\n```ts\nexport default defineConfig({\n  ______: "data-cy",\n});\n```',null,
    ['testIdAttribute','testId','selectorAttribute','dataAttribute'],
    0,'testIdAttribute changes what attribute getByTestId() matches — set it at the top level, not in use:.'],
  ['F','B','Complete to capture traces on failure.\n```ts\nuse: {\n  trace: "______",\n}\n```',null,
    ['retain-on-failure','on-failure','on','capture'],
    0,'retain-on-failure keeps traces only for tests that fail — a storage-efficient production default.'],
  ['F','I','Complete to configure the HTML reporter.\n```ts\nreporter: [["______", { open: "never" }]],\n```',null,
    ['html','htm','report','playwright-report'],
    0,'The "html" reporter generates an interactive web report — { open: "never" } prevents auto-opening in CI.'],
  ['F','I','Complete to start a dev server before tests.\n```ts\nexport default defineConfig({\n  ______: {\n    command: "npm run dev",\n    url: "http://localhost:3000",\n  },\n});\n```',null,
    ['webServer','devServer','startServer','server'],
    0,'webServer is the built-in mechanism for starting and stopping the app server around test runs.'],
  ['F','B','Complete to set the per-test artifact output directory.\n```ts\nexport default defineConfig({\n  ______: "artifacts",\n});\n```',null,
    ['outputDir','testDir','reportDir','artifactDir'],
    0,'outputDir sets where per-test artifacts (screenshots, videos, traces) are saved.'],
  ['F','A','Complete to set a global suite timeout.\n```ts\nexport default defineConfig({\n  ______: 600_000, // 10 minutes max\n});\n```',null,
    ['globalTimeout','suiteTimeout','maxTime','totalTimeout'],
    0,'globalTimeout caps the entire suite wall-clock time — a CI safety net.'],
  ['E','B','What is wrong?\n```ts\nexport default defineConfig({\n  retries: -1,\n});\n```',null,
    ['Negative retries disable the feature','retries must be 0 or a positive integer — a negative value is invalid and Playwright will throw a configuration error','retries: -1 means unlimited retries','retries: -1 disables retries in CI'],
    1,'retries must be 0 or a positive integer — use 0 to disable retries.'],
  ['E','I','What is wrong?\n```ts\nexport default defineConfig({\n  use: {\n    storageState: "./auth.json",\n  },\n  // No auth setup project defined\n});\n```',null,
    ['storageState cannot be in the global use block','If auth.json does not exist before tests run, all tests fail with a file-not-found error. Define a setup project that generates auth.json and add it as a dependency.','storageState must be in each project separately','auth.json must be in the root directory'],
    0,'storageState in the global use block is fine, but the file must exist. Use a setup project with dependencies: ["setup"] to generate it first.'],
  ['E','B','What is wrong?\n```ts\nexport default defineConfig({\n  testDir: "tests",\n  testDir: "e2e", // duplicate key\n});\n```',null,
    ['testDir can only be set in projects','TypeScript/JavaScript objects cannot have duplicate keys — the second testDir silently overrides the first. Only one of them is used (the last one in most JS engines).','testDir cannot be set at the top level','e2e directory must be the first testDir'],
    1,'Duplicate object keys are a JS footgun — the last value wins silently. TypeScript strict mode or linting catches this.'],
  ['E','I','What is wrong?\n```ts\n// playwright.config.ts\nexport default defineConfig({\n  use: { headless: "false" }, // string\n});\n```',null,
    ['headless defaults to false anyway','headless must be a boolean, not a string — "false" is truthy in JavaScript so the browser runs in headless mode despite the developer\'s intent. Use headless: false (boolean).','headless must be in a project, not use','Strings are coerced to booleans'],
    1,'String "false" is truthy — always use boolean false for headless: false.'],
  ['E','A','What is wrong with this reporter config?\n```ts\nreporter: "html, json",\n```',null,
    ['html and json cannot be combined','reporter must be an array of arrays, not a comma-separated string: reporter: [["html"], ["json"]]','reporter cannot accept json in the same run','The comma must be replaced with a pipe character'],
    1,'reporter with multiple outputs requires the array-of-arrays format: [["html"], ["json", { outputFile: "..." }]].'],
  ['E','I','What is wrong?\n```ts\nprojects: [\n  { name: "chromium", use: { browserName: "chromium" } },\n  { name: "chromium", use: { browserName: "firefox" } }, // same name\n],\n```',null,
    ['Duplicate project names are not supported','Duplicate project names make it impossible to filter with --project="chromium" — Playwright runs both. Use unique names like "firefox" for the Firefox project.','firefox must use browserName: "webkit"','Projects with the same name merge their settings'],
    1,'Project names must be unique — --project filtering, report display, and dependency declarations all use these names.'],
  ['D','B','Tests fail with "Error: browserType.launch: Chromium is not installed". The config has `projects: [{ use: { browserName: "chromium" } }]`. What is the fix?',null,
    ['Add chromium: true to the use block','Run npx playwright install chromium to download the browser binary — the config declares what to use but does not download it','Change browserName to "chrome"','Install Google Chrome separately'],
    1,'Playwright\'s browser binaries require explicit installation — npx playwright install downloads the declared browsers.'],
  ['D','I','A test project succeeds but the HTML report is not generated. What should you check?',null,
    ['The test must fail for a report to be generated','Verify reporter: [["html"]] (or "html") is in the config. Also check that playwright-report/ is not in .gitignore causing confusion — the report is generated but you may be looking in the wrong directory.','HTML reports require headed mode','Reports are only generated in CI'],
    1,'Missing reporter config is the primary cause — add html to the reporter array. Run npx playwright show-report to verify location.'],
  ['D','B','`webServer` starts but tests fail with "net::ERR_CONNECTION_REFUSED". What is the issue?',null,
    ['webServer is not supported in this version','The server may not be ready by the time tests start — increase webServer.timeout or add a more specific readyPattern that waits for a "Server started" log message','Add retry: true to webServer','The port must match the URL exactly'],
    1,'webServer.timeout defaults to 60s but sometimes servers need more — increase it or use readyPattern to wait for a specific log message indicating readiness.'],
  ['D','A','In CI, traces are empty (0 bytes) despite trace: "on-first-retry" being set. What are two likely causes?',null,
    ['Traces are not supported in CI','1) retries: 0 — with no retries, on-first-retry never fires. Set retries: 1 or higher. 2) The artifact upload in CI is not configured — traces are generated but not persisted between jobs.','Traces require headed mode','trace: "on-first-retry" requires trace: "on" as a fallback'],
    1,'Both retries and CI artifact persistence must be configured — retries create the traces, artifact upload saves them.'],
  ['D','I','Two projects in the config share the same storageState but the auth session in one project is corrupted by tests from another. Why?',null,
    ['Projects cannot share storageState files','Projects share the state FILE but each creates its own browser context copy — the file is read-only at runtime. If one project writes new storageState during tests, it can overwrite the file used by other projects. Use separate state files per project.','storageState is automatically isolated','Context isolation prevents this'],
    1,'Tests that update storageState during a run can corrupt the shared file — always write to separate per-project state files.'],
  ['C','B','Complete to use half the CPU cores.\n```ts\nexport default defineConfig({\n  ______: "50%",\n});\n```',null,
    ['workers','parallel','cores','threads'],
    0,'workers: "50%" (with quotes) uses half the detected CPU cores — a percentage string.'],
  ['C','I','Complete to set a 10-second action timeout.\n```ts\nuse: {\n  ______: 10_000,\n}\n```',null,
    ['actionTimeout','timeout','clickTimeout','waitTimeout'],
    0,'actionTimeout sets the default per-action timeout (click, fill, etc.) independently of the test timeout.'],
  ['C','B','Complete to configure locale emulation.\n```ts\nuse: {\n  ______: "fr-FR",\n}\n```',null,
    ['locale','language','lang','region'],
    0,'locale sets the browser\'s language/region — affects Intl formatting and Accept-Language headers.'],
  ['C','I','Complete to add a JUnit reporter alongside HTML.\n```ts\nreporter: [\n  ["html"],\n  ["junit", { outputFile: "______" }],\n],\n```',null,
    ['results.xml','junit.html','report.json','output.xml'],
    0,'JUnit reporter outputs XML — the filename convention is typically results.xml for CI integration.'],
  ['C','A','Complete to set a timeout only for the expect() retry loop.\n```ts\nexport default defineConfig({\n  expect: { ______: 8_000 },\n});\n```',null,
    ['timeout','retry','interval','maxWait'],
    0,'expect.timeout controls how long web-first assertions retry — independent of the per-test timeout.'],
  ['L','B','Why is `defineConfig()` used instead of directly exporting a plain object?',null,
    ['It is required for TypeScript compilation','defineConfig provides TypeScript IntelliSense, type checking for all config options, and validates the object structure at author time — without it you lose type safety','defineConfig is required by the test runner at runtime','It sets default values that the plain object does not have'],
    1,'defineConfig is a type helper — wrap your config in it to get full autocomplete and catch typos in option names.'],
  ['L','I','If both the global `use` block and a project\'s `use` block set `viewport`, which wins?',null,
    ['Global use always wins','Project-level use merges with and overrides global use — the project\'s settings take precedence. Test-level test.use() further overrides project settings.','The first one defined wins','They are merged by averaging the values'],
    1,'Specificity hierarchy: test.use() > project use > global use — innermost setting wins.'],
  ['L','B','What is the purpose of `projects` vs just `use` in playwright.config.ts?',null,
    ['Projects are for CI only','projects enables running the same tests against multiple configurations (different browsers, viewports, environments) in one run — use alone applies one configuration to all tests','projects replaces use completely','use is deprecated in favour of projects'],
    1,'projects multiplies test runs — each project gets its own configuration and runs all tests. use sets the defaults for every project.'],
  ['L','A','When does `globalSetup` run relative to `webServer`?',null,
    ['globalSetup runs before webServer starts','webServer starts first (Playwright waits for it to be ready), then globalSetup runs. This ensures the server is available when globalSetup needs to make requests.','They run in parallel','globalSetup and webServer are mutually exclusive'],
    1,'webServer is started and verified before globalSetup runs — globalSetup can safely make requests to the server.'],
  ['L','I','Can you have multiple `webServer` entries in playwright.config.ts?',null,
    ['No — only one webServer is allowed','Yes — webServer accepts an array: webServer: [{ command: "npm run api", url: "..." }, { command: "npm run web", url: "..." }] to start multiple servers','Yes — use a single shell command that starts all servers','Only if they use different ports'],
    1,'Multiple webServer entries let you start an API server and a frontend server simultaneously — Playwright waits for all of them.'],
  ['R','B','You run tests against three environments (dev, staging, prod) using different base URLs. What is the cleanest config approach?',null,
    ['Maintain three playwright.config.ts files','Read BASE_URL from an environment variable: use: { baseURL: process.env.BASE_URL || "http://localhost:3000" } — pass the URL at run time with BASE_URL=https://staging.example.com npx playwright test','Hard-code all three URLs and use a flag','Use three projects each with a different baseURL'],
    1,'Environment variables are the standard way to parameterise base URLs — one config, multiple environments.'],
  ['R','I','You want to run a specific group of tests tagged @smoke using a CLI command without modifying the config. How?',null,
    ['Add a smoke project to the config','npx playwright test --grep "@smoke" — the grep flag filters test titles without changing playwright.config.ts','Add testMatch: ["**/@smoke.spec.ts"] to config','Create a separate playwright.smoke.config.ts'],
    1,'--grep is the CLI filter for tags — no config change needed, making it ideal for CI workflow YAML customisation.'],
  ['R','A','You need different `storageState` files per environment (dev uses dev-auth.json, staging uses staging-auth.json). How?',null,
    ['Create separate auth setup projects per environment','Use: storageState: process.env.ENV === "staging" ? "staging-auth.json" : "dev-auth.json" — environment variables drive config values dynamically','Hardcode both paths and use if/else in tests','Use separate playwright.config files per environment'],
    1,'Dynamic config values via environment variables are the clean solution — one config, multiple runtime configurations.'],
  ['R','I','You want trace viewer files to be stored in a custom location. How?',null,
    ['trace: { path: "custom-traces/" }','Set outputDir: "custom-traces/" in the config — all artifacts including traces go to this directory. Or use a reporter plugin for custom trace storage.','Traces always go to test-results/ and cannot be moved','Set PLAYWRIGHT_TRACE_DIR environment variable'],
    1,'outputDir controls all artifact storage — change it once to redirect all screenshots, videos, and traces.'],
  ['R','B','You need to run tests against a self-signed certificate dev server. What config option is required?',null,
    ['trustAllCerts: true','use: { ignoreHTTPSErrors: true } — this makes every browser context ignore SSL errors','Add the cert to the system trust store','Set HTTPS_ALLOW_ALL=1 environment variable'],
    1,'ignoreHTTPSErrors: true in the use block applies to all contexts — the browser proceeds past self-signed cert warnings.'],
  ['S','B','After adding a new browser project, the CI run time doubled. How do you address this?',null,
    ['Remove the new project from CI','Add fullyParallel: true and increase workers, or use sharding across multiple CI machines to distribute the extra project\'s tests — more browsers is more work, requiring more parallelism','Set the new project to run with workers: 0','Use retries: 0 to skip the new project\'s failures'],
    1,'More projects = more total tests = need more parallelism. fullyParallel + more workers (or sharding) offsets the extra browser coverage cost.'],
  ['S','I','`webServer` starts successfully but tests cannot reach the server — `ERR_CONNECTION_REFUSED`. What two things should you check?',null,
    ['Restart the CI machine','1) The server may bind to 127.0.0.1 (localhost) but the URL uses 0.0.0.0 — verify the host matches exactly. 2) A firewall or Docker network may block the port — test the URL from the same environment with curl.','Add retry: true to webServer','Use a different port'],
    0,'Host binding and network firewalling are the two most common webServer connection issues in CI/Docker environments.'],
  ['S','A','After upgrading Playwright, the config is rejected with "Unknown option: extraHTTPHeaders". What should you do?',null,
    ['Roll back Playwright to the previous version','Run npx playwright --help or check the changelog — extraHTTPHeaders may have moved to a different option name or been removed. Read the migration guide for breaking changes.','Set NODE_ENV=legacy to restore old config parsing','Add @ts-ignore above the option'],
    1,'Breaking config changes happen between minor versions — always read the changelog when upgrading and migrate as documented.'],
  ['S','I','You want to debug a specific test without changing the source code or config permanently. What is the fastest approach?',null,
    ['Add console.log statements throughout','Run PWDEBUG=1 npx playwright test tests/login.spec.ts --headed — this opens the Inspector for that test only with no config changes, and removes itself when you close the terminal','Set debug: true in playwright.config.ts','Use the --verbose flag'],
    1,'PWDEBUG=1 is the zero-config debug toggle — no source changes, removed automatically, works with any test.'],
],
fixtures: [
  // ── Output ×12 ────────────────────────────────────────────────
  ['O','B','Which of these are built-in Playwright fixtures available in every test?',null,
    ['page, context, browser, request, browserName','page, dom, fetch, network, storage','page, puppeteer, axios, browser, frame','page, session, cookie, proxy, device'],
    0,'Built-in fixtures: page · context · browser · request · browserName — all injected automatically.'],
  ['O','B','What does calling `await use(value)` do inside a fixture function?',null,
    ['Returns the value to the test runner\'s global scope','Hands the value to the test and pauses the fixture until the test finishes','Marks the fixture as completed and skips teardown','Creates a new browser context with the given value'],
    1,'use() is the boundary: setup above it, test runs at it, teardown below it.'],
  ['O','I','What is the default scope for a custom fixture created with `test.extend()`?',null,
    ['worker — shared across all tests','file — shared within a spec file','test — a fresh instance per test','global — shared across all workers'],
    2,'Scope defaults to \'test\' — each test gets its own isolated fixture instance.'],
  ['O','B','What does `scope: \'worker\'` mean for a fixture?',null,
    ['The fixture runs in a separate Node.js worker thread','One fixture instance is created and shared across all tests running in the same worker process','The fixture is created for each test file sequentially','The fixture runs in the browser worker context'],
    1,'Worker-scoped fixtures avoid expensive repeated setup (e.g. DB connections) across tests in one worker.'],
  ['O','I','When does code placed after `await use()` run inside a fixture?',null,
    ['Immediately after use() is called, before the test starts','After the test finishes — it is the teardown phase','It never runs — code after use() is ignored','Only when the test fails'],
    1,'Everything after await use() is teardown — it runs automatically when the test ends, pass or fail.'],
  ['O','B','How does a test declare which fixtures it needs?',null,
    ['By calling test.request(\'fixtureName\') before the test body','By destructuring fixture names from the first argument of the test callback','By importing them from the fixture file','By listing them in an array passed to test()'],
    1,'Destructuring in `async ({ page, myFixture }) => { }` tells Playwright which fixtures to set up.'],
  ['O','I','What does `auto: true` do in a fixture definition?',null,
    ['Makes the fixture setup run in parallel with other auto fixtures','Runs the fixture for every test even if the test doesn\'t request it by name','Automatically tears down the fixture without needing use()','Marks the fixture as required — tests fail if it\'s missing'],
    1,'auto: true is useful for side-effecting fixtures like clock freezing or request interception.'],
  ['O','I','What is the main advantage of fixtures over `beforeEach` hooks for reusable setup?',null,
    ['Fixtures run faster than hooks','Fixtures are lazy — only set up when a test requests them — and compose naturally across files','beforeEach cannot be async','Hooks cannot access the page object'],
    1,'Fixtures are tree-shaken and file-portable; hooks are file-local and always run regardless of need.'],
  ['O','B','What does `test.extend()` return?',null,
    ['A new test runner instance with the added fixtures merged in','A fixture object you attach to the base test','The fixture value directly','An array of fixture setup functions'],
    0,'test.extend() returns a new `test` function — import and use this extended version in your specs.'],
  ['O','I','What does `test.use({ storageState: \'auth.json\' })` do when placed at the top of a spec file?',null,
    ['Sets storageState as a global default for all projects','Overrides the storageState fixture for every test in that file or describe block','Imports the auth.json file into the test','Sets a cookie named storageState'],
    1,'test.use() applies fixture overrides to the current file or describe scope.'],
  ['O','I','Can a custom fixture depend on another custom fixture?',null,
    ['No — fixtures cannot reference each other','Yes — list the dependency by name in the fixture\'s first argument, just like tests do','Only built-in fixtures can be dependencies','Only if they share the same scope'],
    1,'Fixtures compose: `adminPage: async ({ page, db }, use) => { … }` depends on both page and db.'],
  ['O','A','What is the practical difference between `scope: \'worker\'` and `scope: \'file\'`?',null,
    ['worker shares across all files in the run; file shares across tests in one spec file only','file is faster because it doesn\'t start a worker process','They are identical in behaviour','worker is deprecated in favour of file'],
    0,'Worker scope = one instance per Node.js worker across multiple files; file scope = one instance per spec file.'],

  // ── Fill-in ×7 ────────────────────────────────────────────────
  ['F','B','Complete: `const test = base.______({ todoPage: async ({ page }, use) => { … } })`',null,
    ['extend','add','create','merge'],
    0,'test.extend() is the API for adding custom fixtures to the test object.'],
  ['F','I','Complete the fixture signature: `async ({ page }, ______) => { await use(page) }`',null,
    ['use','done','next','callback'],
    0,'use is the second argument — calling it passes the value to the test and marks the setup boundary.'],
  ['F','B','Complete: `await ______(myValue)` to hand the fixture value to the test',null,
    ['use','provide','inject','return'],
    0,'await use(value) is the canonical pattern; code after it is teardown.'],
  ['F','I','Complete: `[async ({}, use) => { … }, { scope: \'______\' }]` for a shared fixture',null,
    ['worker','global','shared','process'],
    0,'scope: \'worker\' shares the instance across all tests in a worker process.'],
  ['F','I','Complete: `[async ({ page }, use) => { … }, { ______: true }]` to run for every test',null,
    ['auto','always','required','global'],
    0,'auto: true makes the fixture run for every test without being explicitly requested.'],
  ['F','I','Complete: `test.______({ storageState: \'admin.json\' })` to override for a file',null,
    ['use','set','configure','apply'],
    0,'test.use() applies fixture overrides at file or describe-block scope.'],
  ['F','A','Complete to make a fixture depend on another: `async ({ page, ______ }, use) => { … }` where db is a custom fixture',null,
    ['db','fixtures.db','base.db','this.db'],
    0,'Just name the dependency in the destructured first argument — Playwright resolves it automatically.'],

  // ── Find the Error ×6 ──────────────────────────────────────────
  ['E','B','What is wrong?\n```ts\nconst test = base.extend({\n  loggedIn: async ({ page }, use) => {\n    await page.goto(\'/login\');\n    use(page);  // no await\n  },\n});\n```',null,
    ['use() does not accept a page argument','await is missing before use() — the fixture may tear down before the test starts','page.goto() must come after use()','extend() requires a type parameter'],
    1,'Without await, the fixture function returns before the test can use the value — the test runs with an undefined fixture.'],
  ['E','I','What is wrong?\n```ts\ntest(\'my test\', async ({ loggedIn }) => { … });\n// imported from base, not the extended test\nimport { test } from \'@playwright/test\';\n```',null,
    ['loggedIn cannot be a fixture name — it conflicts with a built-in','The test uses the base test object, so loggedIn fixture is not registered and will throw','Imports must come before test() calls','async is not needed when using fixtures'],
    1,'Always import and use the extended test — using the base test loses all custom fixtures.'],
  ['E','B','What is wrong?\n```ts\nconst test = base.extend({\n  db: async ({}, use) => {\n    const db = await connect();\n    await use(db);\n    await use(db);  // second use call\n  },\n});\n```',null,
    ['db must be typed before it can be used twice','use() must only be called once per fixture — calling it twice is an error','The second use() should be close() not use()','await is invalid on the second use()'],
    1,'Each fixture may only call use() once — it marks the single setup/teardown boundary.'],
  ['E','I','What is wrong?\n```ts\nconst test = base.extend<{}, { page: Page }>({  // page in worker fixtures\n  page: [async ({}, use) => {\n    …\n  }, { scope: \'worker\' }],\n});\n```',null,
    ['page is already a built-in and cannot be overridden','page cannot be worker-scoped because it is tied to a single browser context that cannot be shared safely between tests','Worker fixtures cannot access built-in fixtures','extend does not support type parameters'],
    1,'page is context-bound and not thread-safe; page should always be test-scoped. Worker scope is for stateless or explicitly thread-safe resources like DB connections.'],
  ['E','I','What is wrong?\n```ts\nconst test = base.extend({\n  myFixture: async ({ page }, use) => {\n    const val = setup();\n    // no await use() called\n  },\n});\n```',null,
    ['setup() must be awaited','use() is never called — the test will hang waiting for the fixture to hand it a value','page is not needed if use() is not called','The fixture must return a value'],
    1,'If use() is never called, Playwright waits indefinitely — the test times out.'],
  ['E','A','What is wrong?\n```ts\ntest.describe(\'Admin\', () => {\n  test.use({ storageState: \'admin.json\' });\n  test.beforeAll(async () => {\n    test.use({ storageState: \'superadmin.json\' }); // inside beforeAll\n  });\n});\n```',null,
    ['test.use() is not allowed inside test.describe','test.use() inside beforeAll is invalid — it only works at the top level of describe or a file','storageState cannot be overridden per describe','beforeAll cannot call test.use() because it runs before fixtures are resolved'],
    1,'test.use() must be called at the top level of a describe block or file — not inside a hook.'],

  // ── Debug ×6 ──────────────────────────────────────────────────
  ['D','B','A test fails with "myFixture is not a function". The fixture is defined with `test.extend()`. What is the most likely cause?',null,
    ['The fixture name must end with Fixture','The spec file imports `test` from \'@playwright/test\' instead of the extended test module','extend() only works with TypeScript','The fixture is missing a return statement'],
    1,'The spec must import the extended test object — the base test doesn\'t know about myFixture.'],
  ['D','I','A worker-scoped DB fixture is reconnecting between every test, defeating the purpose. What is wrong?',null,
    ['worker scope doesn\'t exist — use file scope instead','The fixture is not receiving { scope: \'worker\' } in the second element of the array definition — it defaults to test scope','Workers share the fixture but each test gets a clone','The DB connection is stateless so it reconnects automatically'],
    1,'The scope must be explicitly set: `[async ({}, use) => { … }, { scope: \'worker\' }]` — without the second element it defaults to test scope.'],
  ['D','B','An `auto: true` fixture intended to freeze time isn\'t running in one test file. What is the likely cause?',null,
    ['auto only works in .spec.ts files not .test.ts files','The spec imports the base test instead of the extended test — auto fixtures only apply to the extended test object','auto: true requires a special flag in the config','The fixture must be listed in the test body even with auto: true'],
    1,'auto: true is relative to the extended test — importing the base test bypasses all custom fixtures including auto ones.'],
  ['D','I','Teardown code after `await use()` is not running — the test passes but the DB row is not deleted. What should you check?',null,
    ['Teardown only runs on test failure','The fixture must return a cleanup function separately','An unhandled promise rejection in the test body is swallowing the teardown','Teardown runs synchronously — remove await'],
    2,'If an unhandled error bypasses the fixture\'s use() boundary the teardown may not execute — use try/finally around use() to guarantee cleanup.'],
  ['D','I','`test.use({ storageState: \'auth.json\' })` is set at the file level but one specific test still hits the login page. What is the likely cause?',null,
    ['test.use() only applies to the first test in a file','The test explicitly overrides storageState with a different value inside a nested test.use()','auth.json was not created by globalSetup','storageState only works for page fixtures not context fixtures'],
    1,'A nested test.use() or a project-level override can shadow the file-level storageState.'],
  ['D','A','`beforeAll` creates a shared page object but it is undefined in the tests. Why?',null,
    ['beforeAll cannot be async','The page variable is declared with const and is not accessible in closures','Variables declared inside beforeAll are scoped to it — share state via a let variable declared in the describe block','beforeAll runs after the first test'],
    2,'Declare variables with let at describe scope, then assign them inside beforeAll so all tests in the block can access them.'],

  // ── Complete ×5 ──────────────────────────────────────────────
  ['C','B','Complete to create a custom fixture.\n```ts\nconst test = base.______({ myPage: async ({ page }, use) => {\n  await use(page);\n} });\n```',null,
    ['extend','add','register','create'],
    0,'base.extend() merges new fixtures into a new test function.'],
  ['C','I','Complete the teardown: setup runs before use, teardown after.\n```ts\nasync ({ page }, use) => {\n  await page.goto(\'/app\');\n  await ______(page);\n  await page.close();  // teardown\n}\n```',null,
    ['use','done','next','provide'],
    0,'await use(page) is where the test runs; page.close() after it is the teardown.'],
  ['C','I','Complete the scope for a fixture shared across a worker.\n```ts\n[async ({}, use) => { … }, { scope: \'______\' }]\n```',null,
    ['worker','global','shared','process'],
    0,'scope: \'worker\' shares one instance across all tests running in the same worker.'],
  ['C','I','Complete to override storageState for an entire spec file.\n```ts\ntest.______({ storageState: \'admin.json\' });\n```',null,
    ['use','set','configure','apply'],
    0,'test.use() at file level overrides fixtures for every test in the file.'],
  ['C','A','Complete to make a fixture run for every test automatically.\n```ts\n[async ({ page }, use) => {\n  await page.clock.setFixedTime(new Date(\'2024-01-01\'));\n  await use();\n}, { ______: true }]\n```',null,
    ['auto','always','required','default'],
    0,'auto: true runs the fixture for every test without needing to be listed in destructuring.'],

  // ── Logic ×5 ──────────────────────────────────────────────────
  ['L','B','Why must you always import the extended `test` object rather than the base one in spec files?',null,
    ['The base test object is deprecated','The base test doesn\'t know about custom fixtures — using it causes "unknown fixture" errors','The extended test has better TypeScript types only','The base test cannot be async'],
    1,'Custom fixtures only exist on the object returned by test.extend() — the base has none of them.'],
  ['L','I','When should you use `scope: \'worker\'` for a fixture?',null,
    ['When the fixture must be isolated per test','When the setup is expensive (e.g., DB connection, compiled assets) and safe to share across tests in a worker','When the fixture needs access to the browser page','When auto: true is set'],
    1,'Worker scope amortises expensive setup across multiple tests — safe only for stateless or thread-safe resources.'],
  ['L','I','A fixture has a `try/finally` block. What does putting teardown in `finally` guarantee?',null,
    ['The teardown runs before the test starts','The teardown runs even if the test throws or the page crashes','finally blocks are not needed in fixtures','finally runs only on test failure'],
    1,'Without try/finally, an error in the test could cause the fixture to skip teardown and leak resources.'],
  ['L','B','What is the key difference between fixtures and `beforeEach` hooks for cross-file reuse?',null,
    ['Fixtures are faster than hooks','Fixtures are defined in a separate module and imported; hooks are file-local and cannot be shared across spec files without manual importing','Hooks run before fixtures in the lifecycle','beforeEach does not support async operations'],
    1,'Fixtures live in modules and compose naturally; beforeEach hooks must be manually duplicated or imported into each file.'],
  ['L','A','A fixture calls `await use()` with no argument. What does the test receive?',null,
    ['An error — use() requires a value','undefined — the fixture is used for its side effects only','null — Playwright normalises missing values','The fixture object itself'],
    1,'use() with no argument is valid — the fixture runs for side effects (e.g. clock freezing, interceptor setup) and the test receives nothing.'],

  // ── Real-World ×5 ─────────────────────────────────────────────
  ['R','B','You need every test in a file to start logged in as an admin. What is the cleanest approach?',null,
    ['Add await page.goto(\'/login\') to every test','Use beforeEach to log in before each test','Add test.use({ storageState: \'admin.json\' }) at the top of the file','Create a global beforeAll in playwright.config.ts'],
    2,'test.use() with a pre-saved storageState is the zero-cost approach — no login per test.'],
  ['R','I','You have a database that is slow to initialise. How do you connect once per worker and reuse across tests?',null,
    ['Use beforeAll in every spec file','Create a worker-scoped fixture: [async ({}, use) => { const db = await connect(); await use(db); await db.close(); }, { scope: \'worker\' }]','Use globalSetup to connect and store on process.env','Import the DB module at the top of each spec file'],
    1,'Worker scope creates one DB connection per worker process — efficient and isolated.'],
  ['R','I','You want a fixture that sets up request interception on every test without the test requesting it. Which property do you use?',null,
    ['required: true','auto: true','always: true','default: true'],
    1,'auto: true runs the fixture for every test that uses the extended test object.'],
  ['R','A','A custom `adminPage` fixture needs both `page` and a custom `db` fixture. How do you declare the dependency?',null,
    ['Import db directly and call db.connect() inside the fixture','List db in the fixture\'s first argument: async ({ page, db }, use) => { … }','Pass db as a third parameter after use','Use test.use({ db }) before the fixture definition'],
    1,'Fixtures declare dependencies by name in the first argument — Playwright resolves and injects them.'],
  ['R','I','You want cleanup (delete test data) to run after every test even if the test crashes. Where do you put the cleanup?',null,
    ['In afterAll — it runs after all tests','In the fixture after await use(), wrapped in try/finally','In the test body in a .catch() block','In a separate teardown file pointed to by globalTeardown'],
    1,'Fixture teardown after use() with try/finally is guaranteed to run regardless of test outcome.'],

  // ── Scenario ×4 ──────────────────────────────────────────────
  ['S','I','A new team member copies the fixture file but their tests get "unknown fixture" errors. What did they miss?',null,
    ['They need to register the fixture in playwright.config.ts','They imported `test` from \'@playwright/test\' instead of from the fixture module','Fixtures must be declared in every spec file','The fixture file needs to be in the tests/ directory'],
    1,'Custom fixtures only exist on the extended test — import from the fixture module, not from @playwright/test.'],
  ['S','A','You have 50 spec files that all need to be logged in as different users. How do you avoid duplicating test.use() in every file?',null,
    ['Set storageState globally in playwright.config.ts use block, then override per-file with test.use()','Use beforeAll in a shared setup file','Playwright does not support per-file storageState — use a single admin user','Use auto: true on a login fixture'],
    0,'Set a default in config use, then each file overrides with test.use({ storageState: \'user-X.json\' }) only when needed.'],
  ['S','I','A worker-scoped fixture for a shared resource is still creating a new instance per test. What is the most likely reason?',null,
    ['Worker scope does not work with custom fixtures','fullyParallel: true assigns each test its own worker, so \'worker\' scope acts like \'test\' scope','The resource is stateful and Playwright forces a fresh instance','Worker scope requires the fixture to be in a separate file'],
    1,'With fullyParallel: true each test can run in a different worker — worker-scoped fixtures behave like test-scoped when tests don\'t share a worker.'],
  ['S','A','A fixture that creates a temporary directory leaks on test failure. How do you fix this?',null,
    ['Use afterAll to clean up temp directories','Wrap use() in try/finally: create dir → await use(dir) → in finally: remove dir','Add error handling in the test body','Set scope: \'worker\' so the directory persists for cleanup'],
    1,'try/finally around use() guarantees cleanup runs even when the test throws.'],

  // ── Additional batch ×50 ─────────────────────────────────────
  ['O','B','What is the purpose of `use()` in a Playwright fixture definition?',null,
    ['Sets default values for the fixture','Marks the boundary between setup and teardown — code before use() runs before the test, code after runs after the test','Provides the fixture value to dependent fixtures and the test','Both B and C — use() delivers the value AND marks the setup/teardown boundary'],
    3,'use() is the pivot point: it delivers the value to the test AND signals where teardown begins.'],
  ['O','I','What is the difference between `scope: "test"` and `scope: "worker"` for a fixture?',null,
    ['test scope is for browser fixtures; worker scope is for Node.js fixtures','test scope creates and destroys the fixture for every individual test; worker scope creates it once per worker process and shares it across all tests in that worker','worker scope is faster because it uses the same browser session','test scope is the default; worker scope must be declared explicitly'],
    1,'Scope determines lifecycle — test scope means N setups for N tests; worker scope means 1 setup per worker regardless of test count.'],
  ['O','B','What does `auto: true` do when added to a fixture options object?',null,
    ['The fixture automatically calls use()','The fixture is automatically used by every test without being listed in the test\'s argument list — useful for global setup like logging or auth','The fixture automatically reruns on failure','auto: true enables strict mode for the fixture'],
    1,'auto fixtures run silently for every test in scope — perfect for things like screenshot-on-failure or request logging that every test should benefit from.'],
  ['O','I','Can a test-scoped fixture depend on a worker-scoped fixture?',null,
    ['No — scope dependencies must always be equal','Yes — a narrower-scoped fixture can depend on a wider-scoped one (test can use worker-scoped). The reverse is not allowed.','Yes — but only if both use the same browser type','Only if the worker fixture is declared first'],
    1,'Fixtures can depend on fixtures of equal or wider scope — a test fixture can use a worker fixture, but a worker fixture cannot depend on a test-scoped fixture.'],
  ['O','B','What is the return value of `test.extend(fixtures)`?',null,
    ['A fixture registry object','A new test object with all the custom fixtures merged in — used in place of the standard test import','A function that registers the fixtures globally','The original test object modified in place'],
    1,'test.extend() returns a NEW test object — import and use this extended test in your spec files to access the custom fixtures.'],
  ['O','I','What happens to the fixture teardown if a test throws an error?',null,
    ['Teardown is skipped when a test throws','Teardown runs regardless — code after await use() always executes, even if the test body threw. This is why fixtures are safer than afterEach hooks that can be skipped.','Teardown only runs for the next test','The test runner waits for all pending teardowns before reporting the failure'],
    1,'Fixture teardown is guaranteed — it runs whether the test passed, failed, or threw. This reliability is one of the key advantages over manual beforeEach/afterEach.'],
  ['O','A','What does `test.use({ storageState: "admin.json" })` do when called at the describe level?',null,
    ['Overrides the storageState for all tests globally','Overrides the storageState only for tests within that describe block — a scoped configuration override that does not affect tests outside the block','Sets storageState as a worker-scoped fixture','Reads storageState from admin.json and applies it to the context fixture'],
    1,'test.use() is scoped — at describe level it applies to tests in that block only; at file level it applies to all tests in the file.'],
  ['O','I','What does the `browserName` built-in fixture provide?',null,
    ['The full browser version string','A string identifying the current browser engine: "chromium", "firefox", or "webkit" — useful for browser-specific conditional logic','The path to the browser executable','The number of open browser instances'],
    1,'browserName is the engine identifier — use it to skip browser-specific tests: test.skip(browserName === "webkit", "Not supported in WebKit").'],
  ['O','B','Which built-in fixture provides an authenticated APIRequestContext scoped to the current test\'s base URL?',null,
    ['fetch','http','request','apiClient'],
    2,'The request fixture is a built-in APIRequestContext pre-configured with the test\'s baseURL and context credentials.'],
  ['O','I','Can fixture names shadow built-in fixture names?',null,
    ['No — built-in names are reserved','Yes — a custom fixture named "page" overrides the built-in page fixture for tests that use the extended test object','Only if scope is set to "worker"','Yes but only for the fixture file, not test files'],
    1,'Overriding a built-in fixture is a powerful pattern — you can add setup logic to page while still using the standard page API inside the override.'],
  ['O','A','What does `test.extend<{}, WorkerFixtures>(workerFixtureDefinitions)` achieve in TypeScript?',null,
    ['Adds type annotations for documentation only','Correctly types worker-scoped fixtures separately from test-scoped fixtures — the second generic type parameter is for worker fixtures, ensuring TypeScript knows their lifecycle','Prevents test fixtures from being used in worker scope','Creates a separate test object for worker tests'],
    1,'Correctly typing fixtures with two generics (TestFixtures, WorkerFixtures) helps TypeScript catch scope mismatches at compile time.'],
  ['O','I','What is a fixture composition and why is it useful?',null,
    ['Combining multiple files into one fixture module','Defining a fixture that depends on other custom fixtures — e.g. an adminPage fixture that depends on both page and a logged-in user fixture. Composition builds complex setups from smaller reusable pieces.','Using the same fixture in multiple spec files','Extending the test object more than once'],
    1,'Composition avoids duplicating setup logic — each fixture does one thing and complex setups are assembled from simple pieces.'],
  ['F','B','Complete the fixture definition structure.\n```ts\nconst test = base.extend({\n  myFixture: async ({}, ______) => {\n    await use("value");\n  },\n});\n```',null,
    ['use','inject','provide','setup'],
    0,'The second parameter to the fixture function is always use — the function that delivers the value to the test.'],
  ['F','B','Complete to mark a fixture as auto-use.\n```ts\nauditLogger: [async ({}, use) => {\n  startLog();\n  await use();\n  stopLog();\n}, { ______: true }],\n```',null,
    ['auto','always','global','required'],
    0,'auto: true makes the fixture run automatically for every test without being listed in the test\'s argument.'],
  ['F','I','Complete to declare a worker-scoped fixture.\n```ts\nsharedDb: [async ({}, use) => {\n  const db = await connect();\n  await use(db);\n  await db.disconnect();\n}, { scope: "______" }],\n```',null,
    ['worker','test','file','global'],
    0,'scope: "worker" creates the fixture once per worker process — all tests in the worker share the same db connection.'],
  ['F','I','Complete to override a built-in fixture.\n```ts\nconst test = base.extend({\n  page: async ({ page }, ______) => {\n    await page.setExtraHTTPHeaders({ "X-Test": "true" });\n    await use(page);\n  },\n});\n```',null,
    ['use','inject','provide','return'],
    0,'Overriding the page fixture: augment the built-in page then pass it through use() — all tests get the enhanced page automatically.'],
  ['F','B','Complete to get the current browser name in a test.\n```ts\ntest("check browser", async ({ ______ }) => {\n  console.log(browserName); // "chromium" | "firefox" | "webkit"\n});\n```',null,
    ['browserName','browser.name','currentBrowser','engineName'],
    0,'browserName is a built-in fixture that provides the engine name string.'],
  ['F','I','Complete to scope a configuration override to a describe block.\n```ts\ndescribe("admin tests", () => {\n  test.______({ storageState: "admin.json" });\n  test("can delete", ...);\n});\n```',null,
    ['use','configure','set','apply'],
    0,'test.use() at describe level overrides the fixture values for all tests in that block only.'],
  ['F','A','Complete to extend an existing extended test object.\n```ts\nconst test = base.extend({ db });\nconst extendedTest = test.______({ adminPage });\n```',null,
    ['extend','add','merge','compose'],
    0,'test.extend() can be called on an already-extended test object — composing layers of fixtures.'],
  ['E','B','What is wrong?\n```ts\nconst test = base.extend({\n  myFixture: async ({}, use) => {\n    const data = setup();\n    // no await use()\n  },\n});\n```',null,
    ['setup() must be async','Missing await use() — without calling use(), the test never receives the fixture value and hangs indefinitely waiting for it','use() must be the last statement','setup() must return a Promise'],
    1,'Every fixture MUST call await use() — without it the test hangs. The fixture is the test\'s gate.'],
  ['E','I','What is wrong?\n```ts\nconst test = base.extend({\n  workerData: [async ({}, use) => {\n    const d = await heavySetup();\n    await use(d);\n  }, { scope: "worker" }],\n});\n// workerData used in 10 parallel tests\n// heavySetup is called 10 times\n```',null,
    ['worker scope requires auto: true','fullyParallel: true assigns each test to a different worker — each worker creates its own instance of the worker-scoped fixture. This is expected behavior, not a bug. To share across all tests, use globalSetup instead.','Worker scope is shared across ALL workers','This is a Playwright bug with fullyParallel'],
    1,'Worker scope = one per worker process. With fullyParallel + 10 tests = 10 workers = 10 instances. For truly global shared state, use globalSetup.'],
  ['E','B','What is wrong?\n```ts\n// spec.test.ts\nimport { test } from "@playwright/test"; // built-in\ntest("my test", async ({ myFixture }) => { // unknown fixture\n```',null,
    ['myFixture must be declared in the spec file','Importing from "@playwright/test" gives the base test without custom fixtures — import from your extended test module: import { test } from "./fixtures"','Fixtures cannot be used in test files directly','myFixture must be passed as a parameter to test()'],
    1,'The most common fixture mistake: importing from the wrong module. Always import the extended test where custom fixtures are defined.'],
  ['E','I','What is wrong?\n```ts\nconst test = base.extend<{ page: Page }>({\n  page: async ({ page, mySetup }, use) => {\n    await use(page);\n  },\n});\n```',null,
    ['You cannot override page','The TypeScript generic only declares page, but mySetup is also used — TypeScript will complain that mySetup is not in the fixture types. Add mySetup to the generic type parameter.','page override must not use other fixtures','extend takes only one generic parameter'],
    1,'All fixtures used in a definition must be declared in the generic type — mySetup needs to be in the types.'],
  ['E','A','What is wrong?\n```ts\nconst test = base.extend({\n  db: [async ({}, use) => {\n    await use(await connect());\n    await db.disconnect(); // db is not in scope\n  }, { scope: \'worker\' }],\n});\n```',null,
    ['worker scope cannot have teardown','db is not in scope after await use() — store the connection in a variable BEFORE use(): const conn = await connect(); await use(conn); await conn.disconnect()','disconnect must be called before use()','Teardown is not supported for worker fixtures'],
    1,'The value passed to use() must be stored in a local variable to be accessible in teardown.'],
  ['E','I','What is wrong?\n```ts\nconst test = base.extend({\n  screen: async ({ page, screen }, use) => {\n    // circular dependency\n  },\n});\n```',null,
    ['screen is a valid built-in fixture','A fixture cannot depend on itself — circular dependencies cause a stack overflow. Rename one or remove the self-reference.','screen is a reserved name','circular dependencies are allowed in worker scope'],
    1,'Self-referencing fixtures create an infinite dependency loop — Playwright throws a circular dependency error.'],
  ['D','B','A fixture is defined but never runs. The test works but the expected setup is missing. Why?',null,
    ['The fixture file has a typo','The test is importing from @playwright/test instead of the custom test module — the extended test object contains the fixture; the base test does not','Fixtures require auto: true to run','The fixture is defined after the test'],
    1,'Wrong import is the most common fixture debugging issue — verify the import path in the spec file.'],
  ['D','I','A worker-scoped fixture\'s teardown is not running. What should you check?',null,
    ['Worker fixtures have no teardown','Check that code exists after await use() in the fixture — without a teardown block, nothing runs after the test. Also verify the fixture is actually used (not just defined) — unused fixtures do not run.','Teardown only runs on test failure','Teardown requires finally block'],
    1,'Teardown code must exist after use() AND the fixture must be consumed by a test — orphaned fixtures (defined but unused) never execute.'],
  ['D','B','`test.use({ viewport: { width: 375 } })` has no effect on a test in the same file. What are two possible causes?',null,
    ['viewport requires a height too','1) test.use() must be called at the top level of the describe/file scope — if inside a test function, it has no effect. 2) A project-level viewport override takes precedence over test.use() in some configurations — check the project config.','viewport requires headless: false','test.use() is deprecated'],
    0,'test.use() placement and config precedence are the two most common causes — it must be at file/describe scope, not inside test().'],
  ['D','A','A fixture that opens a database connection is creating too many connections in a parallel run. How do you fix it?',null,
    ['Add connection pooling in the fixture','Change scope from "test" to "worker" — one connection per worker instead of one per test dramatically reduces total connections in a parallel run','Set workers: 1 to limit concurrency','Use globalSetup to create a single connection'],
    1,'Test scope × N parallel tests = N connections. Worker scope × M workers = M connections (M << N typically). Worker scope is the right fix.'],
  ['D','I','A test expects a fixture to provide an array but receives undefined. What is the likely cause?',null,
    ['Arrays cannot be fixture values','The fixture calls use() without a value: await use() instead of await use(myArray). Always pass the value to use().','Arrays must be wrapped in an object','use() must be called twice for array fixtures'],
    1,'use() with no argument delivers undefined — always pass the fixture value as the argument.'],
  ['C','B','Complete the minimal fixture definition.\n```ts\nconst test = base.extend({\n  greeting: async ({}, use) => {\n    await ______("Hello");\n  },\n});\n```',null,
    ['use','provide','inject','return'],
    0,'use() with the value delivers it to the test or dependent fixture.'],
  ['C','I','Complete to make a fixture auto-run for every test.\n```ts\nrequestLogger: [async ({ request }, use) => {\n  const logs = [];\n  // intercept requests...\n  await use();\n  saveLog(logs);\n}, { auto: ______ }],\n```',null,
    ['true','false','undefined','1'],
    0,'auto: true makes the fixture run without being listed in any test argument.'],
  ['C','I','Complete to add teardown to a fixture.\n```ts\nmyFixture: async ({}, use) => {\n  const resource = await create();\n  await use(resource);\n  ______ resource.cleanup();\n},\n```',null,
    ['await','return','yield','finally'],
    0,'Code after await use() is the teardown — await the cleanup if it is async.'],
  ['C','B','Complete to access the context fixture.\n```ts\ntest("test", async ({ ______ }) => {\n  const cookies = await context.cookies();\n});\n```',null,
    ['context','browser','page','request'],
    0,'context is a built-in fixture providing the BrowserContext for the current test.'],
  ['C','A','Complete to type a fixture with both test and worker generics.\n```ts\nconst test = base.extend<\n  { loginPage: LoginPage }, // test scope\n  { ______: Database }     // worker scope\n>({ ... });\n```',null,
    ['sharedDb','db','database','workerDb'],
    0,'The second generic type parameter lists worker-scoped fixtures — any name matching your fixture definition is valid.'],
  ['L','B','Why are fixtures preferred over beforeEach/afterEach for shared setup?',null,
    ['Fixtures are faster because they skip setup for tests that don\'t need them','Fixtures are self-contained (setup + teardown in one place), composable (depend on each other), type-safe, scoped (test/worker), and only run when a test actually uses them — beforeEach always runs','Fixtures cannot throw errors that affect tests','beforeEach is deprecated'],
    1,'Fixtures compose, scope, and type cleanly — they are the evolution of beforeEach/afterEach into a structured dependency system.'],
  ['L','I','Can two different fixture files extend the same base test separately and then be composed?',null,
    ['No — the base test can only be extended once','Yes — extend the base test in each file to add its fixtures, then extend the RESULT of one in the other: const combined = firstExtended.extend(secondFixtures)','Yes — they merge automatically when both are imported','Only if they use different fixture names'],
    1,'Fixture composition by chaining extend() calls allows modular fixture organisation — each domain (auth, db, api) in its own file.'],
  ['L','I','What is the execution order of a fixture chain A → B → test → B teardown → A teardown?',null,
    ['All setup runs, then the test, then all teardown in reverse','Setup A → Setup B → test → Teardown B → Teardown A — like nested LIFO (last in, first out). Innermost fixture tears down first.','All setups and teardowns interleave with the test','A and B setup in parallel, then the test'],
    1,'Fixture teardown is LIFO — the last fixture to set up is the first to tear down. This ensures dependencies are still alive during teardown of dependents.'],
  ['L','B','When does a worker-scoped fixture\'s teardown run?',null,
    ['After every test in the worker','Once after all tests assigned to that worker complete — not between individual tests','At the end of the entire test suite','Only if the worker encounters a test failure'],
    1,'Worker-scoped teardown runs once per worker\'s lifetime — after the last test assigned to it finishes.'],
  ['L','A','Can auto fixtures access other fixtures?',null,
    ['No — auto fixtures receive no arguments','Yes — auto fixtures can depend on any fixture of equal or wider scope in their first argument, just like regular fixtures. { page } in an auto fixture gives access to the test\'s page.','Only built-in fixtures are accessible','Only worker-scoped fixtures'],
    1,'Auto fixtures are regular fixtures with auto: true — they have full access to the fixture system including page, context, and custom fixtures.'],
  ['R','B','You want every test to automatically capture a screenshot on failure. How do you implement this?',null,
    ['Add screenshot code to every afterEach','Create an auto fixture: screenshot: [async ({ page }, use) => { await use(); // after test if (test.info().status !== \'passed\') { await page.screenshot({ path: test.info().outputPath("failure.png") }); } }, { auto: true }]','Set screenshot: "on-failure" in the config','Use test.afterEach globally in conftest'],
    0,'An auto fixture with teardown that checks testInfo.status is the programmatic screenshot-on-failure pattern — captures exactly what you need when you need it.'],
  ['R','I','You need a fixture that provides a unique test user for each test, cleaned up after. How do you structure this?',null,
    ['Use beforeAll to create all users upfront','Test-scoped fixture: create a user in setup, await use(user), delete the user in teardown. Each test gets its own isolated user with automatic cleanup.','Use a global variable for the current user','Worker-scoped fixture with user rotation'],
    1,'Test-scoped fixture with CRUD in setup/teardown is the clean pattern — each test is isolated and cleanup is guaranteed.'],
  ['R','A','You need shared expensive computation (e.g. compiled regex patterns) available to all tests across workers. What is the cleanest approach?',null,
    ['Worker-scoped fixture — but it runs once per worker, not once globally','Compute it in globalSetup and pass via a file or environment variable — worker fixtures run once per worker but globalSetup runs once total. For truly global shared results, use the file system.','Test-scoped fixture that memoises the result','Page.evaluate() to compute it in the browser'],
    1,'globalSetup is the only truly once-total execution — write the result to a temp file and read it in a worker fixture.'],
  ['R','I','A team has fixtures for db, api, and browser spread across three files. How do you make all three available in every spec?',null,
    ['Import all three in every spec file','Create a combined fixture index file: const test = dbTest.extend(apiFixtures).extend(browserFixtures) and export it — import from this combined module in all specs','Register fixtures in playwright.config.ts','Use require() at the top of each spec'],
    1,'One combined module is the standard pattern — a single import gives access to all fixture domains.'],
  ['R','B','You want the `request` fixture to have a custom base URL different from the page. How?',null,
    ['Override request in test.use()','Extend the test with a custom apiRequest fixture: apiRequest: async ({}, use) => { const ctx = await request.newContext({ baseURL: "https://api.example.com" }); await use(ctx); await ctx.dispose(); }','Set a second baseURL in the config','Use fetch() in tests instead'],
    1,'A custom request fixture with its own baseURL keeps API and UI base URLs separate — both can coexist.'],
  ['S','B','A test says "fixture \'db\' is not declared in the fixture configuration". What is the most likely problem?',null,
    ['The db fixture does not exist','The spec imports from @playwright/test instead of the extended test module that defines db — change the import to the custom test object','Fixtures must be declared in playwright.config.ts','The db fixture has a typo in its name'],
    1,'Wrong import is responsible for 90% of "fixture not found" errors — always import from the module where test.extend() defines it.'],
  ['S','I','Two spec files both import an extended test with a "page" override. In one file it works; in the other the original page is used. Why?',null,
    ['Page override only works once per test run','The working file imports from the fixture module; the broken file imports from @playwright/test accidentally — the extended test is in scope in only one file','Page override is scoped to the describe block','The override requires headless: false'],
    1,'Import source is the most common cause of inconsistent fixture behavior — audit imports in both files.'],
  ['S','A','A fixture has both setup and teardown but you notice teardown never runs in CI. What is the most likely CI-specific cause?',null,
    ['CI kills the Node.js process before teardown','A SIGTERM from CI is killing the worker process before fixture teardown completes. Increase the CI timeout or ensure graceful shutdown. Alternatively, the teardown code throws synchronously before the async operation completes.','Teardown is disabled in headless mode','CI has a different Node.js version'],
    1,'Process termination and synchronous teardown errors are the main CI-specific causes — add logging in teardown to diagnose.'],
  ['S','I','You want a fixture that provides a different database depending on the environment (dev vs staging). How?',null,
    ['Create two fixtures named devDb and stagingDb','Read the environment in the fixture: const dbUrl = process.env.ENV === "staging" ? STAGING_DB : DEV_DB — return a connection to the correct database. One fixture, runtime configuration.','Create two playwright.config.ts files','Set the DB URL in beforeAll'],
    1,'Reading process.env inside the fixture is the clean solution — the fixture adapts at runtime without duplicating code.'],
],
api: [
  // ── Output ×12 ────────────────────────────────────────────────
  ['O','B','What is the `request` fixture in Playwright tests?',null,
    ['A mock for intercepting browser network traffic','A built-in HTTP client for making real HTTP requests without opening a browser','A wrapper around the Fetch API available in the test browser','A fixture that captures and replays network requests'],
    1,'request is an APIRequestContext pre-configured with baseURL and extraHTTPHeaders from your config.'],
  ['O','B','What does `response.ok()` return?',null,
    ['The response body as a string','true if the HTTP status code is in the 200-299 range, false otherwise','The response time in milliseconds','true only for status 200, false for all others'],
    1,'ok() is a quick boolean check — use status() when you need the exact code.'],
  ['O','I','What is the key difference between `request` and `page.request`?',null,
    ['page.request is async; request is synchronous','request is independent; page.request shares the browser\'s cookie jar and session','request supports JSON; page.request only supports form data','They are identical — page.request is just an alias'],
    1,'page.request authenticates as the logged-in browser user; request has its own independent session.'],
  ['O','B','How do you parse a JSON response body from an API call?',null,
    ['response.json — no call needed, it\'s a property','await response.json()','JSON.parse(response.body())','response.asJSON()'],
    1,'response.json() is async and returns the parsed JavaScript object.'],
  ['O','I','What does passing `data: { name: \'Alice\' }` to `request.post()` do?',null,
    ['Sends the object as a URL query string','Serialises the object as JSON and sets Content-Type: application/json','Encodes the object as form data','Appends the object as a URL fragment'],
    1,'data sends a JSON body with the correct Content-Type automatically.'],
  ['O','B','What does `response.status()` return?',null,
    ['A boolean — true for success, false for error','The exact HTTP status code as a number','The status text like "OK" or "Not Found"','The response time in milliseconds'],
    1,'status() returns the numeric HTTP status code, e.g. 200, 201, 404, 500.'],
  ['O','I','What does `request.newContext({ baseURL, extraHTTPHeaders })` create?',null,
    ['A new browser context with API interception enabled','A standalone HTTP client with its own session, independent of any browser','A subclass of the request fixture with additional headers','A mock server that intercepts all requests'],
    1,'newContext() is for API calls outside of tests — e.g. in globalSetup where no browser exists.'],
  ['O','I','What does the `params` option do in a GET request?',null,
    ['Sets POST body parameters','Appends key-value pairs as a URL query string','Sets request headers','Configures timeout parameters for the request'],
    1,'params: { q: \'playwright\' } becomes ?q=playwright in the URL.'],
  ['O','B','What does `await expect(response).toBeOK()` assert?',null,
    ['That the response body is not empty','That the HTTP status code is in the 200-299 range','That the response completed in under 200 ms','That the Content-Type is application/json'],
    1,'toBeOK() is the assertion equivalent of response.ok() — integrates with test reporting.'],
  ['O','I','Where do you set default headers for all API requests in a project?',null,
    ['In a beforeAll hook in every spec file','In the use: { extraHTTPHeaders: { … } } block in playwright.config.ts','By calling request.setHeaders() before each test','In a custom APIRequestContext wrapper'],
    1,'extraHTTPHeaders in the use block applies to all requests made by the request fixture.'],
  ['O','I','Why is using the API for test setup faster than navigating the UI?',null,
    ['API requests bypass authentication','The API skips browser rendering, JavaScript execution, and network overhead of loading full pages','API requests are cached between tests','The API uses a faster network protocol than the browser'],
    1,'A direct API call takes milliseconds; loading a UI form, filling it, and submitting takes seconds.'],
  ['O','A','What happens to the cookie jar when you use `request` vs `page.request`?',null,
    ['Both share the same cookie jar — they are the same client','request has its own isolated cookie jar; page.request shares the browser context\'s cookies','Neither fixture stores cookies — stateless only','page.request deletes cookies after each request'],
    1,'request is isolated; page.request is cookie-aware and acts as the logged-in browser user.'],

  // ── Fill-in ×7 ────────────────────────────────────────────────
  ['F','B','Complete: `const body = await response.______()` to parse JSON',null,
    ['json','parse','data','body'],
    0,'response.json() is async and returns the parsed response body.'],
  ['F','I','Complete: `await request.______(\'/api/users\', { data: { name: \'Alice\' } })`',null,
    ['post','put','create','send'],
    0,'request.post() sends a POST with JSON body when using the data option.'],
  ['F','B','Complete: `const code = response.______()` to get the HTTP status code',null,
    ['status','code','statusCode','httpCode'],
    0,'response.status() returns the numeric HTTP status code.'],
  ['F','I','Complete: `request.get(\'/api/search\', { ______: { q: \'playwright\' } })`',null,
    ['params','query','search','qs'],
    0,'params appends key-value pairs as a URL query string.'],
  ['F','I','Complete: `request.get(\'/api/secure\', { headers: { ______: \'Bearer \' + token } })`',null,
    ['Authorization','Auth','Token','Bearer'],
    0,'Authorization is the standard HTTP header for bearer tokens.'],
  ['F','I','Complete: `const api = await request.______Context({ baseURL: \'…\' })` for a standalone client',null,
    ['new','create','make','build'],
    0,'request.newContext() creates an independent APIRequestContext with its own session.'],
  ['F','A','Complete: `const res = await page._______.get(\'/api/me\')` to use the browser\'s session',null,
    ['request','api','fetch','http'],
    0,'page.request is the session-aware HTTP client that shares the browser\'s cookies.'],

  // ── Find the Error ×6 ──────────────────────────────────────────
  ['E','B','What is wrong?\n```ts\nconst res = request.get(\'/api/users\');\nconst users = await res.json();\n```',null,
    ['request.get() does not exist — use request.fetch()','await is missing on request.get() — res is a Promise, not an APIResponse','json() cannot be called after get()','users must be typed as any[]'],
    1,'Without await, res is a Promise object — calling json() on it throws immediately.'],
  ['E','I','What is wrong?\n```ts\nconst res = await request.post(\'/api/submit\', {\n  params: { name: \'Alice\', email: \'a@b.com\' },\n});\n```\nIntended to send a JSON body.',null,
    ['POST does not support params','params appends to the URL query string, not the body — use data: { … } for a JSON body','email in params is not URL-encodable','POST requires an explicit Content-Type header'],
    1,'params builds a query string; data sends a JSON body — mixing them up sends no body to the server.'],
  ['E','B','What is wrong?\n```ts\nconst res = await request.get(\'/api/users\');\nconst text = await res.text();\nconst json = await res.json(); // second body read\n```',null,
    ['text() and json() cannot be called on the same response','The response body stream can only be consumed once — calling json() after text() throws','await is not needed on the second call','res.json() must come before res.text()'],
    1,'APIResponse body can only be read once — calling a second body-reading method throws an error.'],
  ['E','I','What is wrong?\n```ts\nconst res = await request.post(\'/api/login\', {\n  form: { email: \'a@b.com\', password: \'secret\' },\n});\nconst { token } = await res.json();\n// Server returns HTML on 401\n```',null,
    ['form is not a valid request option — use data instead','json() should be guarded with res.ok() first — calling json() on an HTML error response throws','form data cannot contain passwords','post() requires a Content-Type header'],
    1,'Always check res.ok() or res.status() before calling json() — error pages are often HTML, not JSON.'],
  ['E','I','What is wrong?\n```ts\n// global-setup.ts\nexport default async function globalSetup({ request }) {\n  await request.post(\'/api/seed\');\n}\n```',null,
    ['globalSetup cannot be async','The request fixture is not injected into globalSetup — use request.newContext() instead','post() is not available in globalSetup','global-setup.ts must be a .js file'],
    1,'globalSetup receives no test fixtures — create a standalone client with request.newContext().'],
  ['E','A','What is wrong?\n```ts\nconst api = await request.newContext({ baseURL: \'https://api.example.com\' });\nawait api.post(\'/users\', { data: { name: \'Alice\' } });\n// context never closed\n```',null,
    ['newContext() does not accept a baseURL','The context must be closed with await api.dispose() to release connections','post() requires headers when using newContext()','newContext() is only for GET requests'],
    1,'Not calling dispose() leaks connections and can cause test runner resource warnings.'],

  // ── Debug ×6 ──────────────────────────────────────────────────
  ['D','B','A protected endpoint returns 401. The `request` fixture is used but no Authorization header is set. What is the fix?',null,
    ['Switch from request to page.request','Pass { headers: { Authorization: \'Bearer \' + token } } to the request call','Set credentials: \'include\' in the request options','Add the token to params'],
    1,'For token-based auth, pass the Authorization header explicitly on each call or set extraHTTPHeaders in the config.'],
  ['D','I','`response.json()` throws "Unexpected token < in JSON". What is the likely cause?',null,
    ['The server returned binary data instead of JSON','The endpoint returned an HTML error page (e.g. 404 or 500) — check res.ok() before parsing','json() is not supported for POST responses','The Content-Type header is missing'],
    1,'HTML error pages are the most common cause — always guard with res.ok() or res.status() before calling json().'],
  ['D','B','A DELETE request returns 204 No Content and `response.json()` throws. What should you do?',null,
    ['Use response.text() instead — 204 responses always return text','Do not call json() — 204 has no body; check status() == 204 instead','Switch to response.body() for binary responses','Add Content-Type: application/json to the request headers'],
    1,'204 No Content has an empty body — calling json() on it throws; just check the status code.'],
  ['D','I','Tests that use the API in `beforeEach` to seed data are passing but leaving rows in the database, causing later test failures. What is missing?',null,
    ['The API seed data persists across test runs — use fixtures instead','An afterEach or fixture teardown step that calls DELETE on the created resource is missing','beforeEach should not be used for API calls','Add a database reset in playwright.config.ts globalTeardown'],
    1,'Always pair API setup with API teardown — delete the created resource in afterEach or a fixture teardown.'],
  ['D','I','`page.request.get(\'/api/profile\')` returns 401 even though the browser shows the user is logged in. What is the cause?',null,
    ['page.request does not share cookies — it is identical to the request fixture','The user is logged in via a JWT in localStorage, not a cookie — page.request only shares cookies, not localStorage','page.request requires a separate login step','The /api/profile route does not accept browser cookies'],
    1,'page.request shares cookies but not localStorage or sessionStorage — JWT in localStorage is not sent automatically.'],
  ['D','A','In parallel test runs, API-seeded data from one test is interfering with another test. How do you fix this?',null,
    ['Use workers: 1 to prevent parallel execution','Use unique identifiers (e.g. UUID) per test when seeding, so each test\'s data is isolated','Disable retries — they repeat the seed step','Set fullyParallel: false'],
    1,'Unique test data per run (UUIDs, timestamps) is the standard isolation pattern for parallel API tests.'],

  // ── Complete ×5 ──────────────────────────────────────────────
  ['C','B','Complete a GET and parse the JSON response.\n```ts\nconst res = await request.get(\'/api/items\');\nconst items = await res.______();\n```',null,
    ['json','parse','data','body'],
    0,'response.json() parses and returns the JSON body.'],
  ['C','I','Complete a POST with a JSON body.\n```ts\nawait request.post(\'/api/users\', {\n  ______: { name: \'Bob\', role: \'admin\' },\n});\n```',null,
    ['data','body','json','payload'],
    0,'data: { … } serialises the object as JSON and sets the Content-Type header.'],
  ['C','I','Complete to add an Authorization header to a request.\n```ts\nawait request.get(\'/api/private\', {\n  headers: { ______: \'Bearer abc123\' },\n});\n```',null,
    ['Authorization','Auth','Token','Bearer'],
    0,'Authorization is the standard HTTP header for bearer tokens.'],
  ['C','I','Complete to assert the response is successful.\n```ts\nawait expect(res).toBe______();\n```',null,
    ['OK','Success','Valid','200'],
    0,'toBeOK() asserts status 200-299 and integrates with Playwright\'s test reporter.'],
  ['C','A','Complete to create and then dispose a standalone API context.\n```ts\nconst api = await request.newContext({ baseURL: \'https://api.example.com\' });\nawait api.post(\'/seed\', { data: payload });\nawait api.______();\n```',null,
    ['dispose','close','destroy','end'],
    0,'dispose() releases the context\'s network connections and resources.'],

  // ── Logic ×5 ──────────────────────────────────────────────────
  ['L','B','When should you use the API for test setup instead of navigating the UI?',null,
    ['Only when the UI is broken','When creating prerequisite data — the API is faster and the UI rendering is not what\'s being tested','Always — UI interaction should be avoided in tests','Only for DELETE operations'],
    1,'API setup is idiomatic when the creation step is not under test — keep tests focused on what they actually assert.'],
  ['L','I','What is the difference between `data`, `form`, and `multipart` request body options?',null,
    ['They are synonyms — all send JSON','data sends JSON (application/json); form sends URL-encoded (application/x-www-form-urlencoded); multipart sends multipart/form-data for file uploads','data is for GET; form is for POST; multipart is for PUT','form and multipart are deprecated — use data for all body types'],
    1,'Match the body option to the Content-Type the server expects.'],
  ['L','I','Why would you use `page.request` instead of `request`?',null,
    ['page.request is faster because it reuses the browser\'s HTTP/2 connection','page.request sends requests as the authenticated browser user — sharing cookies set by page.goto() and browser interactions','page.request supports streaming responses','request does not support POST — only page.request does'],
    1,'page.request is for session-aware calls where the user is already logged in via the browser.'],
  ['L','B','Can `response.json()` be called multiple times on the same response?',null,
    ['Yes — it caches the result after the first call','No — the body stream can only be consumed once; a second call throws','Yes — but only if the Content-Type is application/json','Yes — each call re-fetches the body from the server'],
    1,'Body reading methods (json, text, body) consume the response stream — call them once and store the result.'],
  ['L','A','What is the right way to pass extraHTTPHeaders to all API requests in every test without repeating them?',null,
    ['Call request.setHeaders() in a beforeAll hook','Set extraHTTPHeaders in the use block in playwright.config.ts — they apply to all requests made by the request fixture','Create a custom request fixture that adds headers','Pass headers to every individual request call'],
    1,'extraHTTPHeaders in config.use are merged into every request automatically.'],

  // ── Real-World ×5 ─────────────────────────────────────────────
  ['R','B','You need to create a product before a UI test and delete it after. Where do you put the API calls?',null,
    ['Inside the test body at the top and bottom','In beforeEach to create and afterEach to delete, or in a custom fixture with teardown','In a separate test file that runs first','In globalSetup and globalTeardown'],
    1,'beforeEach/afterEach or a fixture with teardown after use() keeps setup and cleanup co-located.'],
  ['R','I','A REST endpoint requires a Bearer token. How do you add it to all requests without repeating it per test?',null,
    ['Pass it in params on every request','Set it in extraHTTPHeaders in the config use block: { Authorization: \'Bearer \' + process.env.TOKEN }','Call request.authenticate(token) in beforeAll','Add it to the baseURL as a query parameter'],
    1,'extraHTTPHeaders in the config merges the header into every request made by the fixture.'],
  ['R','I','After a POST, you get a 201 and want to extract the new resource\'s ID. How?',null,
    ['res.id — Playwright extracts IDs automatically','const { id } = await res.json() — parse the JSON body and destructure','res.headers()[\'location\'].split(\'/\').pop() — parse the Location header','request.lastId — the client tracks the last created ID'],
    1,'Parse the response JSON and destructure the ID field — the standard REST pattern.'],
  ['R','A','You need to authenticate once in globalSetup and save the session for all tests. What is the correct approach?',null,
    ['Use the request fixture in globalSetup with an imported token','Create a newContext, call the login endpoint, then await api.storageState({ path: \'auth.json\' }), then dispose()','Store the session cookie in process.env','Use page.request in globalSetup — it shares the browser session'],
    1,'newContext in globalSetup → login → storageState save → dispose → reference in config use block.'],
  ['R','I','You need to test that a search endpoint returns results for a specific query. How do you pass the search term?',null,
    ['Append it manually: request.get(\'/api/search?q=playwright\')','Pass params: { q: \'playwright\' } — Playwright builds the query string','Use data: { q: \'playwright\' } on a GET request','Set it as a header: { \'x-search\': \'playwright\' }'],
    1,'params is the idiomatic way — Playwright encodes and appends the query string automatically.'],

  // ── Scenario ×4 ──────────────────────────────────────────────
  ['S','I','A test creates a user via POST then checks the UI. The UI shows no user. What should you investigate first?',null,
    ['The POST endpoint is broken — test it with curl','The response status — if not 201/200 the user was not created; also check if baseURL matches the UI server','The UI has caching — add a cache-busting header','The test needs a page.reload() before asserting'],
    1,'Always verify the API call actually succeeded (check status + response body) before debugging the UI layer.'],
  ['S','A','In parallel runs, two tests both POST a user with name "TestUser" and then query by name, getting each other\'s records. How do you fix the isolation?',null,
    ['Set workers: 1 to avoid parallel execution','Generate unique names per test: `name: \'TestUser-\' + crypto.randomUUID()`','Use page.request instead of request for all POST calls','Disable the name uniqueness constraint on the test database'],
    1,'Unique per-test identifiers (UUID, timestamp) prevent cross-test data contamination in parallel runs.'],
  ['S','I','`response.json()` works in one test but throws "SyntaxError: Unexpected token" in another. The only difference is the endpoint. What do you check?',null,
    ['The second endpoint uses a different API version','The second endpoint returns a non-JSON body (HTML, plain text, or empty) for certain status codes','json() is not thread-safe in parallel tests','The response encoding is different'],
    1,'Guard with res.ok() check before calling json() — error responses often return HTML, not JSON.'],
  ['S','A','You want to test that creating a resource via API and then deleting it removes it from the UI listing. Write the test strategy.',null,
    ['Create in beforeAll, delete in afterAll, assert in the test — that\'s the standard pattern','POST to create → capture id → navigate UI → assert present → DELETE → navigate UI → assert absent','Use page.evaluate() to make the API calls inside the browser context','Use network interception instead of real API calls'],
    1,'The hybrid pattern: API seed → UI assertion → API cleanup. This tests the full stack while keeping setup fast.'],

  // ── Additional batch ×50 ─────────────────────────────────────
  ['O','B','What does `request.get("/api/users")` return?',null,
    ['The parsed JSON body directly','A Promise<APIResponse> — an object with status(), json(), text(), headers(), and body() methods','A string response body','An array of user objects'],
    1,'request.get() returns an APIResponse — always check ok() or status() before calling json().'],
  ['O','I','What does `response.headers()` return?',null,
    ['A string of raw header bytes','An object mapping header names to their values — all header names are lowercase','An array of [name, value] tuples','An HTTP Headers object'],
    1,'headers() returns a plain object with lowercase header names — access values like response.headers()["content-type"].'],
  ['O','B','What is the difference between `request.post()` and `page.request.post()`?',null,
    ['page.request.post() is faster','request fixture creates an isolated context; page.request uses the page\'s browser context and shares its session cookies — use page.request when you need session-aware calls','They are identical','page.request.post() requires authentication'],
    1,'page.request shares the page\'s cookies — use it for session-aware API calls. The request fixture is a separate unauthenticated context.'],
  ['O','I','What does `response.ok()` return?',null,
    ['true if the response arrived before timeout','A boolean: true when status is 200-299, false otherwise','true if the Content-Type is application/json','true if the response has no errors in the body'],
    1,'ok() is the simple 2xx check — equivalent to status >= 200 && status < 300.'],
  ['O','B','What does `request.delete("/api/users/42")` do?',null,
    ['Deletes the file at that path on the server','Sends an HTTP DELETE request to /api/users/42 using the APIRequestContext','Removes the API route from Playwright\'s routing table','Closes the request fixture connection'],
    1,'HTTP DELETE is a standard method — request.delete() sends it just like request.post() sends POST.'],
  ['O','I','What option passes a JSON body in `request.post()`?',null,
    ['body: JSON.stringify(obj)','data: obj — Playwright serialises the object to JSON and sets Content-Type: application/json automatically','json: obj','payload: obj'],
    1,'data: obj is the shorthand — use it to avoid manual JSON.stringify and Content-Type headers.'],
  ['O','A','What does `request.storageState({ path: "api-auth.json" })` save?',null,
    ['All API responses made so far','Cookies and origins from the APIRequestContext — similar to browser context storageState but for the API client','The API client configuration','The test\'s request log'],
    1,'APIRequestContext has its own cookies (from Set-Cookie response headers) — storageState captures them for reuse.'],
  ['O','I','What does the `form` option in `request.post()` do?',null,
    ['Sends the data as JSON with Content-Type: application/json','Encodes the data as application/x-www-form-urlencoded — simulates an HTML form POST','Encodes as multipart/form-data','Sets the form action URL'],
    1,'form: is for URL-encoded form data; multipart: is for multipart/form-data (file uploads).'],
  ['O','B','What does `response.text()` return?',null,
    ['The Content-Type header value','A Promise<string> — the response body as a raw string','The response status text (e.g. "OK")','The response URL'],
    1,'text() is the fallback for non-JSON responses — HTML error pages, plain text, etc.'],
  ['O','I','How do you pass custom headers in an API request?',null,
    ['Set them in the APIRequestContext options once for the whole context','Set them per request: request.get(url, { headers: { "Authorization": "Bearer token" } }) — or set extraHTTPHeaders in the context options for all requests','Headers cannot be customised in Playwright API tests','Use request.setHeader() before each call'],
    0,'Per-request headers override or extend context-level headers — both approaches are valid depending on whether the header changes per request.'],
  ['O','A','What does `apiRequestContext.dispose()` do?',null,
    ['Cancels all in-flight requests','Closes the APIRequestContext, releasing its cookies and connections — call it in fixture teardown to avoid resource leaks','Clears all stored cookies','Resets the base URL'],
    1,'dispose() is the cleanup method for manually created API contexts — the request fixture disposes automatically, but manual contexts need explicit disposal.'],
  ['O','I','Can an APIRequestContext follow HTTP redirects automatically?',null,
    ['No — redirects must be followed manually','Yes — by default APIRequestContext follows up to 20 redirects; set maxRedirects: 0 to disable redirect following','Only 301 redirects are followed automatically','Redirects are only followed in browser contexts'],
    1,'Redirect following is on by default — configure maxRedirects in newContext options to control this.'],
  ['F','B','Complete to make a GET request.\n```ts\nconst response = await request.______("/api/products");\n```',null,
    ['get','fetch','read','query'],
    0,'request.get() sends an HTTP GET request and returns a Promise<APIResponse>.'],
  ['F','B','Complete to pass a JSON body.\n```ts\nconst response = await request.post("/api/users", {\n  ______: { name: "Alice", role: "admin" },\n});\n```',null,
    ['data','body','json','payload'],
    0,'data: serialises the object to JSON and sets the correct Content-Type header automatically.'],
  ['F','I','Complete to check the response status.\n```ts\nconst response = await request.post("/api/login", { data: creds });\nexpect(response.______()).toBe(200);\n```',null,
    ['status()','code','statusCode','ok'],
    0,'response.status() returns the HTTP status code as a number.'],
  ['F','I','Complete to parse the JSON body.\n```ts\nconst response = await request.get("/api/config");\nconst data = await response.______();\n```',null,
    ['json()','parse()','body()','text()'],
    0,'json() parses the response body as JSON and returns the object.'],
  ['F','B','Complete to make a DELETE request.\n```ts\nawait request.______(\`/api/items/\${id}\`);\n```',null,
    ['delete','remove','destroy','del'],
    0,'request.delete() sends the HTTP DELETE method.'],
  ['F','I','Complete to set a custom base URL for an API context.\n```ts\nconst apiCtx = await request.newContext({\n  ______: "https://api.example.com",\n});\n```',null,
    ['baseURL','url','origin','host'],
    0,'baseURL in newContext sets the base for all relative URL calls on that context.'],
  ['F','A','Complete to send multipart form data (file upload).\n```ts\nconst response = await request.post("/upload", {\n  ______: {\n    file: fs.createReadStream("./report.pdf"),\n    name: "report",\n  },\n});\n```',null,
    ['multipart','form','data','files'],
    0,'multipart: sends multipart/form-data — the correct encoding for file uploads.'],
  ['E','B','What is wrong?\n```ts\nconst response = await request.get("/api/users");\nconst data = response.json(); // missing await\n```',null,
    ['json() is synchronous for API responses','json() is async — it returns a Promise. Without await, data is a Promise object, not the parsed JSON.','response.json() requires Content-Type check first','json() is only valid after ok() check'],
    1,'All response body methods (json, text, body) are async — always await them.'],
  ['E','I','What is wrong?\n```ts\nconst response = await request.get("/api/data");\nconsole.log(response.json()); // may throw\n```',null,
    ['json() throws when called without await','json() can throw if the response body is not valid JSON (e.g. an HTML error page). Always check response.ok() before calling json(), or wrap in try/catch.','json() only works after status check','This is fine — json() handles all content types'],
    1,'Unexpected non-JSON responses (502 HTML error, empty body) cause json() to throw — always guard with ok() or status check.'],
  ['E','B','What is wrong?\n```ts\nconst response = await request.get("/api/users",\n  { params: "name=Alice" } // wrong type\n);\n```',null,
    ['params is not a valid option','params must be an object: { name: "Alice" } — Playwright builds the query string from the object properties','params requires URLSearchParams type','Params must be part of the URL directly'],
    1,'params: takes an object, not a pre-encoded string — Playwright handles the encoding.'],
  ['E','I','What is wrong?\n```ts\n// request is the test fixture\nconst apiCtx = await request.newContext({ baseURL: "https://api.example.com" });\n// lots of requests...\n// no dispose call\n```',null,
    ['Manually created contexts dispose automatically at test end','Manually created APIRequestContexts must be explicitly disposed with await apiCtx.dispose() — without it, connections and cookies leak for the duration of the test run','newContext() is only for browser contexts','The request fixture disposes the child context'],
    1,'Manual context lifecycle management is the responsibility of the test — always dispose in fixture teardown or test afterEach.'],
  ['E','A','What is wrong?\n```ts\nconst response = await page.request.get("/api/secure");\n// Used in an unauthenticated test context\nexpect(response.status()).toBe(200);\n```',null,
    ['page.request cannot be used for GET requests','page.request shares the page\'s browser context cookies — if the page is not authenticated, the request is also unauthenticated and may return 401/403. Load storageState or log in first.','page.request only works with POST','expect is synchronous so status() needs await'],
    1,'page.request inherits the page\'s session — unauthenticated page = unauthenticated API request.'],
  ['E','I','What is wrong?\n```ts\nawait request.post("/api/users", {\n  form: { name: "Alice" },\n  data: { role: "admin" }, // both form and data\n});\n```',null,
    ['form and data can be combined for multipart requests','form and data are mutually exclusive — they set different Content-Type headers. Using both is undefined behavior; only the last one takes effect.','data overrides form automatically','They are merged into a single JSON body'],
    1,'Choose one: form for URL-encoded, data for JSON, multipart for multipart/form-data — they cannot coexist in one request.'],
  ['D','B','API test fails with "ECONNREFUSED" when calling a local endpoint. What is the most likely cause?',null,
    ['The request fixture is broken','The local server is not running or is on a different port. Verify the server is started (use webServer in config) and the baseURL port matches.','CORS is blocking the request','The API requires authentication'],
    1,'ECONNREFUSED means nothing is listening on that port — start the server with webServer config or run it separately.'],
  ['D','I','`response.json()` returns `{}` even though the API returns data. What should you check?',null,
    ['json() always returns an empty object first','The response Content-Type may be text/plain or the body may be empty for this status code. Also check if you\'re hitting the right endpoint (log response.url()).','json() requires res.ok() to return true','The API is returning gzipped data'],
    1,'Log response.status() and response.url() first — you may be hitting a redirect or a different endpoint than expected.'],
  ['D','B','PUT request returns 200 but the data is not updated when you GET it. What should you investigate?',null,
    ['PUT is not supported for partial updates','Check that you are hitting the same endpoint with the same ID; the test may use a different environment (dev vs staging) for the PUT and GET calls','Retry the GET after 1 second','Use PATCH instead of PUT'],
    1,'Environment mismatch is a common cause — verify both calls use the same baseURL and the ID returned by POST is correctly propagated to the PUT and GET.'],
  ['D','A','API tests are flaky — sometimes the response is 429 (rate-limited). How do you handle this?',null,
    ['Add retries: 3 to the config','Implement exponential backoff in a retry helper: attempt the request, check for 429, wait 2^n seconds, retry up to 3 times. Or use request.newContext with httpCredentials that bypass rate limits in the test environment.','Set timeout: 60000 to wait out the rate limit','Use page.route() to mock the API rate limit response'],
    1,'Rate limiting in tests usually indicates hitting a shared environment — use dedicated test environment with relaxed limits, or implement retry logic for 429 responses.'],
  ['D','I','`request.delete()` returns 204 No Content but `response.json()` throws. Why is this expected?',null,
    ['DELETE is not allowed to return a body','HTTP 204 means "No Content" — the response body is intentionally empty. Calling json() on an empty body throws. Check for 204 with status() before calling json().','json() requires Content-Type header','response.ok() must return true before json()'],
    1,'204 No Content is correct for successful deletes — test for it with expect(response.status()).toBe(204) and skip the json() call.'],
  ['C','B','Complete to check if the response is successful.\n```ts\nif (!response.______()) {\n  throw new Error("Request failed: " + response.status());\n}\n```',null,
    ['ok()','success()','isOK()','valid()'],
    0,'ok() returns true for 2xx status codes — the idiomatic success check.'],
  ['C','I','Complete to send query parameters.\n```ts\nconst resp = await request.get("/api/search", {\n  ______: { q: "playwright", limit: 10 },\n});\n```',null,
    ['params','query','qs','search'],
    0,'params: object is encoded and appended as the query string.'],
  ['C','B','Complete to send form-encoded data.\n```ts\nawait request.post("/login", {\n  ______: { username: "alice", password: "secret" },\n});\n```',null,
    ['form','data','body','headers'],
    0,'form: sends application/x-www-form-urlencoded — matching a standard HTML form POST.'],
  ['C','I','Complete to get the response body as text.\n```ts\nconst html = await response.______();\n```',null,
    ['text()','body()','content()','raw()'],
    0,'text() returns the response body as a string — use for non-JSON responses.'],
  ['C','A','Complete to make a PATCH request.\n```ts\nconst resp = await request.______(\`/api/users/\${id}\`, {\n  data: { name: "Updated" },\n});\n```',null,
    ['patch','update','put','post'],
    0,'request.patch() sends the HTTP PATCH method — for partial updates.'],
  ['L','B','Why is the `request` fixture preferred over Node.js `fetch` for Playwright API tests?',null,
    ['fetch is not available in Node.js','request integrates with Playwright\'s baseURL config, shares cookies via page.request, participates in the fixture lifecycle (auto-dispose), and has a Playwright-idiomatic API — fetch works but requires manual URL building and cookie management','request is faster because it uses the browser\'s network stack','fetch cannot send DELETE requests'],
    1,'Integration with config, cookies, and fixture lifecycle makes request the better choice for Playwright API testing.'],
  ['L','I','When should you use `page.request` instead of the `request` fixture?',null,
    ['Always — page.request is more powerful','When the API call needs to share the page\'s session (cookies, auth tokens) — e.g. calling a session-authenticated endpoint that the logged-in user can access. The request fixture is a separate isolated context.','When testing a POST endpoint','When the test uses storageState'],
    1,'Session sharing is the key distinction — page.request is session-aware, the request fixture is isolated.'],
  ['L','I','Can `response.json()` be called multiple times?',null,
    ['Yes — it caches the result after the first call','No — the response body stream can only be consumed once. Calling json() a second time throws. Store the result in a variable.','Yes — each call re-parses the body','Only if ok() returned true'],
    1,'Response bodies are streams — consume them exactly once. Store the result: const body = await response.json()'],
  ['L','B','What HTTP method does `request.put()` use?',null,
    ['POST with a _method=PUT field','HTTP PUT — used for full resource replacement','HTTP PATCH — same as put()','HTTP POST with the PUT header'],
    1,'put() sends HTTP PUT — for complete resource replacement. patch() is for partial updates.'],
  ['L','A','Can the `request` fixture be used in `globalSetup`?',null,
    ['Yes — import request from @playwright/test','No — globalSetup does not have access to Playwright fixtures. Use playwright.request.newContext() directly in globalSetup and dispose it manually.','Yes — globalSetup automatically imports all fixtures','Only if globalSetup is async'],
    1,'Fixtures are test-runner constructs — they are not available in globalSetup. Use the low-level playwright.request API directly.'],
  ['R','B','You need to create 10 test users before a test and delete them all after. What is the most efficient pattern?',null,
    ['Create them one by one with 10 request.post() calls','Create them in parallel with Promise.all([...Array(10).keys()].map(i => request.post("/api/users", { data: { name: "User" + i } }))) then delete in the same way in fixture teardown','Use a single batch API endpoint to create all 10','Create 1 user and clone the record 9 times'],
    1,'Promise.all for parallel API seeding is dramatically faster than sequential calls for independent resources.'],
  ['R','I','You need to verify a complex JSON object returned by the API. How do you assert nested fields?',null,
    ['Convert to string and use toContain','const body = await response.json(); expect(body.user.roles).toContain("admin") — access nested paths directly and use appropriate Jest/expect matchers for each field','Use toMatchObject on the full body','Compare as string with exact JSON'],
    1,'Destructure the JSON and assert specific fields with type-appropriate matchers — toEqual for exact deep equality, toContain for array membership.'],
  ['R','A','You need to test pagination: first page returns 20 items, second page returns the next 20. How do you write this test?',null,
    ['GET both pages simultaneously and merge results','GET page=1, assert 20 items and a next link, GET page=2 using the next link from the response, assert 20 items and correct offset','Use websocket to stream all results','Mock the pagination response'],
    1,'Follow the actual API contract: first response drives the second request — this tests the pagination metadata too.'],
  ['R','I','You want to test that an API endpoint rejects requests without an Authorization header. How?',null,
    ['Remove the header from the config and retry','Create a request context without credentials: await request.newContext() (no extraHTTPHeaders), call the endpoint, expect 401','Use route() to strip the header','Toggle authentication in the test environment'],
    1,'A separate unauthenticated context cleanly tests the 401 scenario without affecting the authenticated request fixture.'],
  ['R','B','After a successful POST, the response includes an `id` field. How do you use it in a subsequent GET?',null,
    ['Access it from page.lastResponse','const body = await response.json(); const { id } = body; then use id in the next request URL','Store it in process.env','Extract from the Location header with headers()["location"].split("/").pop()'],
    1,'Capture the id from the POST response body and use it in subsequent request URLs — standard CRUD test pattern.'],
  ['S','B','An API test passes individually but fails in a parallel run with "resource already exists". What is the problem?',null,
    ['API tests cannot run in parallel','Multiple tests are creating a resource with the same name/ID — use UUID: name: "user-" + crypto.randomUUID() to guarantee uniqueness across parallel tests','The API has a bug with concurrent requests','Add retries to handle the conflict'],
    1,'Static test data in parallel tests is a classic isolation failure — always generate unique identifiers.'],
  ['S','I','You need to verify that creating a resource via UI stores it correctly in the database. How?',null,
    ['Inspect the database directly from the test','Use the UI to create the resource, then call the read API to verify the stored data matches what was entered — no direct DB access needed','Compare UI screenshots before and after','Check the network request payload'],
    1,'API read verification is the clean alternative to direct DB access — test the full stack through the public API.'],
  ['S','A','An API returns different response shapes for different user roles. How do you test both shapes?',null,
    ['Use one test with conditional assertions','Separate tests: one authenticated as a regular user and one as an admin (using separate request contexts or page.request with different storageState) — each asserts its expected shape','Mock the API to return both shapes','Use expect.objectContaining for flexible matching'],
    1,'Separate contexts per role give clean, independent tests with clear assertions about each role\'s response shape.'],
  ['S','I','A test creates API data in setup but the cleanup DELETE sometimes fails with 404 (already deleted). How do you make cleanup robust?',null,
    ['Skip the cleanup if it might fail','In fixture teardown: const resp = await request.delete(url); if (resp.status() !== 404) { expect(resp.ok()).toBeTruthy() } — accept 404 as already-cleaned but fail on other errors','Wrap delete in try/catch and ignore all errors','Re-create the resource if deleted to keep state consistent'],
    1,'Idempotent cleanup accepts 404 (already gone) as success — prevents test failures from double-cleanup while still catching unexpected errors.'],
],
network: [
  // ── Output ×12 ────────────────────────────────────────────────
  ['O','B','What does `page.route(pattern, handler)` do?',null,
    ['Records all network requests matching the pattern for later inspection','Intercepts browser requests matching the pattern and passes them to the handler','Blocks all requests except those matching the pattern','Adds a Content Security Policy header to matching responses'],
    1,'page.route() is the entry point for network interception — the handler decides what to do with each matched request.'],
  ['O','B','What does `route.fulfill({ status: 200, json: data })` do?',null,
    ['Forwards the request to the server with the given body','Returns a mocked response to the browser without hitting the real server','Modifies the real response before delivering it','Logs the response and passes it through unchanged'],
    1,'fulfill() short-circuits the real network call and returns whatever you specify directly to the browser.'],
  ['O','I','What does `route.abort()` do?',null,
    ['Cancels the route handler and lets the request continue normally','Causes the browser to see a network error for that request','Returns a 503 status to the browser','Removes the route handler permanently'],
    1,'abort() simulates a network failure — useful for testing offline states and error UI.'],
  ['O','B','What does `route.continue()` do?',null,
    ['Mocks the response with an empty body','Forwards the request to the real server, optionally modifying headers or body','Records the request and retries it after 1 second','Signals that the route handler is finished'],
    1,'continue() passes the request through — optionally letting you modify headers, method, or body before sending.'],
  ['O','I','What does `await route.fetch()` return?',null,
    ['The real network response from the server — you can then modify it before calling fulfill()','A Promise that resolves once the browser receives the response','The request headers as an object','The URL that was actually fetched after any redirects'],
    1,'route.fetch() sends the request to the real server and gives you the response to inspect or modify.'],
  ['O','B','Which of these URL patterns is valid for `page.route()`?',null,
    ['Only exact string URLs','Glob strings like **/api/**, exact strings, or RegExp','Only RegExp patterns','Only URLs starting with https://'],
    1,'page.route() accepts glob strings, exact strings, and RegExp — giving flexible matching options.'],
  ['O','I','What is the difference between `page.route()` and `context.route()`?',null,
    ['page.route() intercepts GET requests; context.route() intercepts POST requests','context.route() applies to all pages in the browser context including new ones; page.route() only covers that specific page','They are identical — context.route() is an alias','context.route() runs before page.route() in the handler chain'],
    1,'context.route() covers every page opened in that context — useful for popups and new tabs.'],
  ['O','I','What does `page.on(\'request\', fn)` do differently from `page.route()`?',null,
    ['It intercepts and can modify requests','It is read-only — it observes requests but cannot fulfill, abort, or continue them','It only fires for XHR requests, not fetch()','It requires await unlike page.route()'],
    1,'Event listeners are passive monitors — use them to collect data without changing request behaviour.'],
  ['O','I','What does `page.unroute(pattern)` do?',null,
    ['Aborts all pending requests matching the pattern','Removes the route handler previously registered for that pattern','Clears the browser cache for matching URLs','Disables network interception entirely'],
    1,'unroute() removes a specific handler — useful in fixtures when teardown should restore normal network behaviour.'],
  ['O','A','What does `page.routeFromHAR(\'fixtures/api.har\')` do?',null,
    ['Records all network traffic to a HAR file','Replays a previously recorded HTTP Archive to mock an entire API surface','Blocks all requests not in the HAR file','Converts HAR entries to route.fulfill() calls in the test file'],
    1,'routeFromHAR() lets you mock a complex API by replaying a real recorded session.'],
  ['O','I','When should you use `route.fetch()` instead of `route.fulfill()` directly?',null,
    ['When the URL is too long for fulfill() to handle','When you want the real server response but need to add or change a field before the browser receives it','When the request uses POST — fulfill() only works for GET','When the network is too slow and fetch() uses a cache'],
    1,'fetch() + fulfill() is the spy-and-modify pattern — real data with surgical changes.'],
  ['O','B','What does `route.request().url()` return?',null,
    ['The page URL at the time of the request','The full URL of the intercepted network request','The base URL from playwright.config.ts','The redirect chain as an array of URLs'],
    1,'route.request() gives you the Request object; .url() returns its full URL string.'],

  // ── Fill-in ×7 ────────────────────────────────────────────────
  ['F','B','Complete: `route.______({ status: 404, body: \'Not Found\' })` to return a mock response',null,
    ['fulfill','mock','respond','reply'],
    0,'fulfill() returns the specified response to the browser without hitting the real server.'],
  ['F','B','Complete: `route.______()` to block the request so the browser sees a network error',null,
    ['abort','block','deny','cancel'],
    0,'abort() causes a network error — use it to test offline states or blocked resources.'],
  ['F','B','Complete: `route.______()` to forward the request to the real server unchanged',null,
    ['continue','pass','forward','allow'],
    0,'continue() passes the request through — optionally with modified headers or body.'],
  ['F','I','Complete: `\'______/api/users\'` — the glob prefix that matches any path prefix',null,
    ['**','*','?','%'],
    0,'** matches any number of path segments — e.g. **/api/users matches /v1/api/users.'],
  ['F','I','Complete: `await page.______( \'**/api/**\', handler )` to register a route',null,
    ['route','intercept','mock','trap'],
    0,'page.route() is the API for registering network interception handlers.'],
  ['F','I','Complete: `await page.______( \'**/api/users\' )` to remove a specific handler',null,
    ['unroute','removeRoute','clearRoute','cancelRoute'],
    0,'unroute() removes the route handler registered for that pattern.'],
  ['F','A','Complete: `const res = await route.______()` to get the real server response for modification',null,
    ['fetch','forward','request','get'],
    0,'route.fetch() forwards to the real server and returns the APIResponse for inspection.'],

  // ── Find the Error ×6 ──────────────────────────────────────────
  ['E','B','What is wrong?\n```ts\nawait page.goto(\'/\');\nawait page.route(\'**/api/users\', route => route.fulfill({ json: [] }));\n```',null,
    ['fulfill() cannot receive a json option','page.route() must use RegExp not a glob string','page.route() was registered after page.goto() — the first page load\'s API request was not intercepted','route.fulfill() requires await'],
    2,'Always register routes before navigation — requests fired during goto() will miss handlers set up afterward.'],
  ['E','I','What is wrong?\n```ts\nawait page.route(\'**/api/data\', route => {\n  // handler body — no fulfill/abort/continue call\n});\n```',null,
    ['page.route() requires an async handler','The handler must call route.fulfill(), route.abort(), or route.continue() — omitting it causes the request to hang until timeout','page.route() does not support arrow functions','The pattern must start with https://'],
    1,'Every route handler must resolve the request with exactly one of the three methods — silence causes a permanent hang.'],
  ['E','B','What is wrong?\n```ts\nawait page.route(\'**/api/**\', route => route.fulfill({\n  body: JSON.stringify({ items: [] }),\n}));\n// UI never parses the response\n```',null,
    ['body must be a Buffer not a string','Content-Type defaults to text/plain — the browser parses it as text, not JSON. Use json: { items: [] } instead','fulfill() does not support the body key','JSON.stringify is not available in route handlers'],
    1,'Without contentType: \'application/json\' or using the json: shorthand, the browser treats the body as plain text.'],
  ['E','I','What is wrong?\n```ts\npage.on(\'request\', req => {\n  req.fulfill({ json: { mocked: true } }); // modify in listener\n});\n```',null,
    ['page.on(\'request\') is not a valid event','Event listeners are read-only — req has no fulfill() method; use page.route() to intercept and modify','json must be a string not an object here','The listener must be async to call fulfill()'],
    1,'Event listeners observe only — page.route() is needed to fulfill, abort, or continue requests.'],
  ['E','I','What is wrong?\n```ts\nawait page.route(\'**/api/products\', async route => {\n  const res  = route.fetch();  // no await\n  const json = await res.json();\n  await route.fulfill({ json });\n});\n```',null,
    ['route.fetch() does not return a Promise','await is missing on route.fetch() — res is a Promise, so res.json() will throw','json() must be called synchronously','route.fulfill() cannot follow route.fetch()'],
    1,'route.fetch() is async — without await, res is a pending Promise and res.json() throws immediately.'],
  ['E','A','What is wrong?\n```ts\nawait page.route(\'**/api/**\', route => route.abort());\nawait page.route(\'**/api/health\', route => route.continue()); // health should pass through\n```',null,
    ['Two routes cannot match the same URL','The second route is registered last and fires first (LIFO order) — health passes through correctly. Actually this is CORRECT behaviour','continue() and abort() cannot both be registered for overlapping patterns','Route handlers cannot share the same page'],
    1,'LIFO means the last-registered handler fires first — health matches **/api/health** first via the second handler (continue), so this setup is actually correct.'],

  // ── Debug ×6 ──────────────────────────────────────────────────
  ['D','B','A test hangs with "Request timed out". No assertion ever fires. What is the most likely cause?',null,
    ['The page.goto() URL is wrong','The route handler neither fulfills, aborts, nor continues the request — the browser waits indefinitely','The assertion timeout is too short','The test has too many route handlers'],
    1,'A handler that does nothing leaves the request permanently pending — always call one of the three resolution methods.'],
  ['D','I','The mock route is registered but the UI still shows real API data. What should you check first?',null,
    ['The mock json is formatted incorrectly','The route pattern may not match the actual request URL — log req.url() inside the handler to verify','The UI is caching the previous response','route.fulfill() is async and needs await'],
    1,'URL pattern mismatches are the most common cause — glob syntax is easy to get wrong; log the actual URL first.'],
  ['D','B','`route.abort()` causes an unhandled page error that fails the test. How do you suppress it?',null,
    ['Use route.fulfill({ status: 499 }) instead of abort()','Listen for the pageerror event with page.on(\'pageerror\', () => {}) or add page.on(\'requestfailed\', () => {})','Add try/catch around route.abort()','abort() should not be used in tests — it always fails'],
    1,'Aborted requests trigger requestfailed events and can throw uncaught page errors — add a listener to ignore expected failures.'],
  ['D','I','You registered a route to add a header with `route.continue({ headers: { \'x-test\': \'1\' } })` but the original headers are gone. Why?',null,
    ['continue() always sends an empty headers object','Passing headers to continue() replaces all headers — merge with route.request().headers() to preserve the original ones','x-test is a reserved header name','The server strips custom headers'],
    1,'continue({ headers: newHeaders }) replaces the entire headers object — spread route.request().headers() first to preserve originals.'],
  ['D','I','`page.routeFromHAR()` is set up but some API calls return 404 instead of mocked data. What is the cause?',null,
    ['routeFromHAR() only works in headed mode','The HAR file does not contain an entry for those specific URLs — they fall through to the real network','HAR files cannot be used for POST requests','The HAR file must be re-recorded every test run'],
    1,'routeFromHAR only intercepts URLs present in the recorded file — unrecorded requests reach the real network.'],
  ['D','A','Two route handlers are registered for `**/api/items**`. The second one was meant to override the first but the first one still fires. What went wrong?',null,
    ['Multiple handlers for the same pattern are not allowed','Only the first registered handler fires — LIFO order means the second one registered fires first, not the first. Re-check the registration order','Both handlers run and both call fulfill() — the second is ignored','The handlers need different handler references to stack'],
    1,'LIFO = last in, first out. If the second registration is truly last, it fires first. Double-check the actual registration order in your code.'],

  // ── Complete ×5 ──────────────────────────────────────────────
  ['C','B','Complete to mock an empty list response.\n```ts\nawait page.route(\'**/api/items\', route => route.______({ status: 200, json: [] }));\n```',null,
    ['fulfill','mock','reply','respond'],
    0,'fulfill() returns the mocked response to the browser.'],
  ['C','I','Complete to simulate a 503 server error.\n```ts\nawait page.route(\'**/api/orders\', route => route.fulfill({\n  status: ______,\n  body: \'Service Unavailable\',\n}));\n```',null,
    ['503','\'503\'','500','404'],
    0,'status is a number — 503 Service Unavailable is the standard "server down" code.'],
  ['C','I','Complete to block all image requests.\n```ts\nawait page.route(\'**/*.{png,jpg,gif,svg}\', route => route.______());\n```',null,
    ['abort','block','deny','cancel'],
    0,'abort() simulates a network error — the browser skips loading the matched resources.'],
  ['C','I','Complete to pass headers through while adding one.\n```ts\nawait page.route(\'**/api/**\', route =>\n  route.continue({\n    headers: { ...route.request().______(), \'x-test\': \'1\' },\n  })\n);\n```',null,
    ['headers','getHeaders','allHeaders','requestHeaders'],
    0,'route.request().headers() returns the original request headers as a plain object.'],
  ['C','A','Complete the fetch-and-modify pattern.\n```ts\nawait page.route(\'**/api/users\', async route => {\n  const res  = await route.______();\n  const json = await res.json();\n  json.push({ id: 99, name: \'Bot\' });\n  await route.fulfill({ json });\n});\n```',null,
    ['fetch','forward','get','request'],
    0,'route.fetch() sends the real request and returns the server response for modification.'],

  // ── Logic ×5 ──────────────────────────────────────────────────
  ['L','B','Why must `page.route()` be called before `page.goto()`?',null,
    ['page.goto() clears all route handlers','Requests fired during page load happen before route handlers registered after goto() are in place','page.route() is synchronous and must precede async calls','goto() and route() cannot share the same await chain'],
    1,'The very first network requests happen during navigation — handlers must be in place before goto() fires them.'],
  ['L','I','When multiple route handlers match the same URL, which one fires first?',null,
    ['First registered (FIFO)','Last registered (LIFO)','The most specific pattern wins','They all fire in alphabetical pattern order'],
    1,'LIFO order — the last handler registered runs first. Use this to layer overrides on top of base handlers.'],
  ['L','I','What is the advantage of using `route.fetch()` over a separate `request.get()` call in the handler?',null,
    ['route.fetch() is 10x faster because it reuses a connection','route.fetch() forwards the original request including method, headers, and body — a separate request.get() would miss POST bodies and auth headers','request.get() is not available inside route handlers','route.fetch() bypasses CORS restrictions'],
    1,'route.fetch() preserves the original request intact — crucial when the real request has POST bodies or auth headers you want forwarded.'],
  ['L','B','What is the difference between `page.route()` and `page.on(\'request\')`?',null,
    ['page.route() only works for XHR; page.on works for all requests','page.route() intercepts and can fulfill/abort/continue; page.on(\'request\') is read-only observation','page.on(\'request\') fires before page.route() in the pipeline','They are identical — page.route is just syntactic sugar'],
    1,'route handlers can modify traffic; event listeners can only observe it.'],
  ['L','A','`context.route()` vs `page.route()` — when does context-level routing matter?',null,
    ['context.route() is faster because it runs outside the page JS engine','context.route() intercepts requests from all pages in the context, including popups and newly opened tabs','page.route() cannot use RegExp patterns — context.route() can','They are identical — context.route() is deprecated'],
    1,'context.route() is essential when actions like Ctrl+Click open a new page — page.route() would not cover the new tab.'],

  // ── Real-World ×5 ─────────────────────────────────────────────
  ['R','B','You want to test the "no results" empty state of a search page. How do you set it up?',null,
    ['Clear the database before the test','Use page.route() to mock the search API to return an empty array','Add a query parameter that triggers an empty response on the server','Use page.evaluate() to clear the search results DOM'],
    1,'Mocking the API response is faster and more reliable than manipulating the real data layer.'],
  ['R','I','You need to test how the app handles a 503 from the checkout API. What do you set up?',null,
    ['Take the real server offline','await page.route(\'**/api/checkout\', route => route.fulfill({ status: 503, body: \'Down\' }))','Use network throttling to simulate slowness','Set retries: 0 in the config'],
    1,'route.fulfill({ status: 503 }) simulates the error without touching the real server.'],
  ['R','I','You want to block all Google Analytics and Hotjar requests to keep tests fast. What is the correct approach?',null,
    ['Add a Content Security Policy header in the config','await page.route(/google-analytics|hotjar/, route => route.abort())','Set headless: true — analytics only load in headed mode','Use extraHTTPHeaders to block those domains'],
    1,'route.abort() on a regex pattern covering all analytics domains blocks them efficiently.'],
  ['R','I','During a test you want to verify that the app called `/api/orders` after clicking "Place Order". How?',null,
    ['Use page.route() to intercept and log it','Collect URLs with page.on(\'request\', req => apiCalls.push(req.url())) and assert after the action','Check the Network tab in DevTools','Add a console.log() to the app source'],
    1,'page.on(\'request\') collects request URLs passively — then assert the expected URL appears in the list.'],
  ['R','A','You want to test how the app handles slow API responses (2 s delay). How do you implement this?',null,
    ['Use page.route() to fulfill after a delay: `async route => { await new Promise(r => setTimeout(r, 2000)); await route.fulfill({ json: data }); }`','Use network throttling in the config','Set timeout: 2000 in the route handler','Use route.continue() with a delay option'],
    0,'A setTimeout inside an async route handler adds the delay before fulfilling — clean and reliable.'],

  // ── Scenario ×4 ──────────────────────────────────────────────
  ['S','I','A mock route is set up but the real API is still being called. Logging shows the intercepted URL differs from the pattern. The real URL is `/v2/api/users`. The pattern is `**/api/users`. Does it match?',null,
    ['Yes — ** matches any prefix including /v2','No — ** only matches one level; use **/* to match v2','Yes — glob patterns ignore URL path depth','No — ** requires the pattern to start with a slash'],
    0,'** matches zero or more path segments — **/api/users matches /v2/api/users, /api/users, and /any/depth/api/users.'],
  ['S','A','You need the first call to `/api/feed` to return 10 items and the second to return 0 items (pagination). How do you implement this?',null,
    ['Register two separate routes — they fire in order','Use a call counter in a closure: increment on each intercept and return different data based on the count','Use routeFromHAR() which handles pagination automatically','Register one route per page number using params'],
    1,'A closure counter lets the same handler return different responses on successive calls.'],
  ['S','I','`route.continue()` is called but the request headers sent to the server are missing the original Authorization header. What went wrong?',null,
    ['continue() always strips Authorization for security','The headers option in continue() replaced all headers — the original Authorization was not merged in','Authorization headers cannot be forwarded — use fetch() instead','The server strips Authorization on HTTPS requests'],
    1,'Spread route.request().headers() into the continue() headers to preserve all originals: `{ ...route.request().headers(), \'x-test\': \'1\' }`.'],
  ['S','A','A test opens a popup window via window.open() and the popup makes an API call. The route handler on `page` does not intercept the popup\'s request. How do you fix this?',null,
    ['Add page.route() inside the popup\'s load event','Use context.route() instead of page.route() — it covers all pages including popups','Pass the route handler to waitForEvent(\'popup\')','Set fullyParallel: false so popups share the parent page\'s routes'],
    1,'context.route() intercepts requests from all pages in the browser context — the only way to cover popups opened by the main page.'],

  // ── Additional batch ×50 ─────────────────────────────────────
  ['O','B','What does `page.route("**/*.png", route => route.abort())` do?',null,
    ['Delays all PNG requests by 1 second','Cancels all requests matching *.png — the browser receives a network error for those requests','Redirects PNG requests to a placeholder image','Blocks only the first PNG request'],
    1,'route.abort() terminates the request — the browser sees it as a network failure, not a 404.'],
  ['O','I','What is the return type of `route.request().url()`?',null,
    ['URL object','string — the full URL of the intercepted request','Request object','Promise<string>'],
    1,'url() is synchronous and returns the request URL as a plain string — no await needed.'],
  ['O','B','What does `route.fulfill({ status: 503 })` do?',null,
    ['Pauses the route for 503 milliseconds','Responds to the intercepted request with HTTP 503 Service Unavailable — no real network call is made','Retries the request 503 times','Passes the request through with a 503 header added'],
    1,'fulfill() returns a synthetic response from Playwright without hitting the network — use it to simulate server errors.'],
  ['O','I','What does `page.route(url, handler, { times: 1 })` do?',null,
    ['Fires the handler at most once — subsequent matching requests are passed through normally','Fires the handler for 1 second then removes itself','Fires the handler for the first 1 ms only','Limits the handler to 1 active interception at a time'],
    0,'times: 1 auto-removes the route after the first match — useful for mocking a one-time bootstrap response.'],
  ['O','A','What does `page.unroute("**/api/**", handler)` do?',null,
    ['Removes all routes for the pattern','Removes only the specific handler function for that pattern — other handlers on the same pattern remain active','Pauses the route handler temporarily','Resets the route to pass-through mode'],
    1,'unroute() is handler-specific — pass the same function reference to remove exactly that handler.'],
  ['O','I','What does `request.postDataJSON()` return inside a route handler?',null,
    ['The raw request body as a Buffer','The request body parsed as JSON — or null if the body is empty or not JSON','A Promise<object> — must be awaited','The multipart form fields as an object'],
    1,'postDataJSON() is synchronous and parses the request body as JSON — convenient for inspecting what the app sent.'],
  ['O','B','What does `route.continue()` do with no options?',null,
    ['Forwards the request to the real server unchanged','Fulfills the request with an empty 200 response','Aborts the request','Marks the request as handled without forwarding it'],
    0,'continue() with no arguments forwards the request exactly as-is — use it to observe without modifying.'],
  ['O','A','What does `page.routeFromHAR("fixtures/api.har", { update: true })` do?',null,
    ['Plays back responses from the HAR file','Records new HAR entries from live network traffic and UPDATES the file — useful for refreshing stale HAR fixtures','Validates the HAR file format','Uploads the HAR to Playwright\'s servers'],
    1,'update: true switches routeFromHAR to recording mode — requests are forwarded live and the HAR is rewritten with fresh data.'],
  ['O','I','What happens when multiple `page.route()` handlers match the same URL?',null,
    ['All handlers run in parallel','Handlers run in LIFO order — the last-registered handler runs first. If it calls route.continue(), the next handler in the stack gets a chance to run.','The first-registered handler always wins','A "duplicate route" error is thrown'],
    1,'LIFO stacking lets you override a context-level route with a page-level one, or chain handlers.'],
  ['O','B','What does `response.request().method()` return inside a `page.on("response", ...)` listener?',null,
    ['The HTTP verb (e.g. "GET", "POST") of the original request that triggered this response','The response method — always "GET"','The redirect method used','The method of the next request in the chain'],
    0,'response.request() gives back the original Request object — .method() on it returns the HTTP verb.'],
  ['O','I','What does `page.waitForRequest(url => url.includes("/api/data"), { timeout: 5000 })` return?',null,
    ['A boolean — true if the request is made within 5000 ms','A Promise<Request> — resolves with the first matching Request object, or rejects on timeout','The response for that request','The number of matching requests'],
    1,'waitForRequest() resolves with the Request object — you can then inspect its headers, body, and method.'],
  ['O','A','What does `context.route("**", route => route.fulfill({ body: "", status: 200 }))` do when set on the context?',null,
    ['It intercepts all requests from all pages in the context and responds with an empty 200','It only intercepts requests from the first page','It intercepts only XHR, not fetch requests','It requires fullyParallel: false to work correctly'],
    0,'context.route() covers every page — combined with ** it intercepts all network traffic, useful for fully offline tests.'],
  ['F','B','Complete to abort requests matching the pattern.\n```ts\nawait page.route("**/ads/**", route => route.______());\n```',null,
    ['abort','cancel','block','drop'],
    0,'route.abort() signals a network error to the browser for that request.'],
  ['F','I','Complete to modify a request header before forwarding.\n```ts\nawait page.route("**/api/**", async route => {\n  await route.continue({\n    headers: { ...route.request().headers(), ______: "Bearer mytoken" },\n  });\n});\n```',null,
    ['"Authorization"','"Auth"','"X-Token"','"Bearer"'],
    0,'"Authorization" is the standard HTTP header for auth tokens — injected here without the test having to log in via the UI.'],
  ['F','B','Complete to respond with a JSON body.\n```ts\nawait page.route("**/api/user", route => route.fulfill({\n  status: 200,\n  contentType: "application/json",\n  ______: JSON.stringify({ name: "Alice" }),\n}));\n```',null,
    ['body','data','response','content'],
    0,'body: accepts a string, Buffer, or ReadableStream — JSON.stringify converts the object to a string.'],
  ['F','I','Complete to use the HAR file for responses.\n```ts\nawait page.______("fixtures/api.har", { url: "**/api/**" });\n```',null,
    ['routeFromHAR','loadHAR','replayHAR','useHAR'],
    0,'routeFromHAR() replays matching responses from the HAR file without hitting the network.'],
  ['F','B','Complete to wait for a response matching a URL.\n```ts\nconst response = await page.______("**/api/login");\n```',null,
    ['waitForResponse','awaitResponse','onResponse','captureResponse'],
    0,'waitForResponse() resolves with the Response object once a matching response arrives.'],
  ['F','I','Complete to read the intercepted request body as a string.\n```ts\nawait page.route("**/api/data", route => {\n  const body = route.request().______();\n  console.log(body);\n  route.continue();\n});\n```',null,
    ['postData()','body()','text()','data()'],
    0,'postData() returns the request body as a string — postDataJSON() parses it as JSON.'],
  ['F','A','Complete to intercept only once.\n```ts\nawait page.route("**/api/bootstrap", handler, { ______: 1 });\n```',null,
    ['times','count','once','limit'],
    0,'times: N auto-removes the handler after N matches.'],
  ['F','I','Complete to pass through a response but inspect it first.\n```ts\nawait page.route("**/api/**", async route => {\n  const response = await route.fetch();\n  const json = await response.json();\n  console.log(json);\n  await route.______( response);\n});\n```',null,
    ['fulfill','continue','pass','forward'],
    0,'route.fulfill(response) passes the already-fetched response to the browser — used after route.fetch() to inspect-and-forward.'],
  ['E','B','What is wrong?\n```ts\nawait page.route("**/api/users", route => {\n  route.fulfill({ status: 200, body: { users: [] } });\n  // no await on fulfill\n});\n```',null,
    ['fulfill() does not accept objects as body — it must be a string or Buffer','fulfill() is async — missing await means the route promise resolves before the response is sent, causing "Request was already handled"','Route handlers cannot be async','body must always be JSON.stringify\'d'],
    1,'fulfill() and continue() are both async — always await them inside async route handlers.'],
  ['E','I','What is wrong?\n```ts\npage.route("**/api/**", route => route.continue());\n// later — never removed\n// second test accidentally intercepts real requests\n```',null,
    ['Page routes are automatically removed between tests','page.route() routes persist for the lifetime of the page. If the page is reused across tests (unusual), routes accumulate. Always call page.unroute() or use a test-scoped page fixture that creates a fresh page.','Routes only match once by default','fullyParallel: true clears routes between tests'],
    1,'Routes live as long as the page — ensure fresh pages per test or explicitly unroute to avoid cross-test contamination.'],
  ['E','B','What is wrong?\n```ts\nawait page.route("**/*.json", async route => {\n  await route.fulfill({ body: \'{"mocked": true}\' });\n  await route.continue(); // both fulfill AND continue\n});\n```',null,
    ['This is valid — fulfill and continue can coexist','Once a route is handled (via fulfill, continue, or abort), it cannot be handled again. The second call throws "Request is already handled."','continue() overrides fulfill() silently','fulfill must come after continue'],
    1,'Handle a route exactly once — pick fulfill OR continue OR abort, never two in the same handler.'],
  ['E','I','What is wrong?\n```ts\nawait page.route("**/api/user", route => {\n  const data = route.request().postDataJSON();\n  route.fulfill({ body: JSON.stringify({ echo: data }) });\n});\n```',null,
    ['postDataJSON() cannot be called in a route handler','route.fulfill() is async — the handler must be async and await fulfill(). Without await, the route may complete before the response is sent.','postDataJSON() is not available for POST requests','JSON.stringify is not allowed in fulfill'],
    1,'fulfill() and continue() must be awaited — otherwise the event loop moves on before Playwright sends the response.'],
  ['E','A','What is wrong?\n```ts\nawait page.route("**/api/**", async route => {\n  const resp = route.fetch(); // no await\n  await route.fulfill(resp);\n});\n```',null,
    ['route.fetch() is not a valid method','route.fetch() is async — without await, resp is a Promise, not an APIResponse. fulfill() receives a Promise object rather than the response, leading to incorrect behavior.','fulfill() cannot accept a route.fetch() result','route.fetch() automatically fulfills the route'],
    1,'Always await route.fetch() — the returned Promise must resolve to an APIResponse before passing to fulfill().'],
  ['D','B','`page.route()` is not intercepting requests. What is the most common first thing to check?',null,
    ['The test has to opt in to network interception','Verify the URL pattern matches the actual request URL. Log the request URL in the handler: route => { console.log(route.request().url()); route.continue(); } to see what is actually being called.','Network interception only works in Chromium','Reload the page before adding the route'],
    1,'Pattern mismatch is the #1 cause — log the actual URL to confirm the glob pattern matches.'],
  ['D','I','`route.fulfill()` is called but the UI still shows an error. Network tab shows a 200 response with correct data. What else could cause this?',null,
    ['fulfill() requires the correct Content-Type header','The app may be checking a field in the response body that differs from the mock. Inspect the JSON in fulfill() against the real API response shape — the app code may expect a field the mock is missing.','The browser cached the old response','fulfill() only works on the first request per route'],
    1,'JSON shape mismatch is subtle — the network shows 200 but the app logic fails on a missing field. Always mirror the real API response structure.'],
  ['D','B','`waitForResponse("**/api/data")` times out even though the request is made. What should you check?',null,
    ['waitForResponse requires a context-level route','The waitForResponse call must be placed BEFORE the action that triggers the request — if the response arrives before waitForResponse starts listening, the promise never resolves.','waitForResponse cannot use glob patterns','Add timeout: 30000 to extend the default'],
    1,'Race condition: set up waitForResponse before navigating or clicking — classic Playwright ordering mistake.'],
  ['D','I','A `routeFromHAR()` test passes in CI but fails locally with different data. What is the likely cause?',null,
    ['HAR files are environment-specific','The local HAR file is stale (recorded against an older API) while CI uses the checked-in version. Re-record the HAR with update: true or synchronise the local file with CI.','routeFromHAR() only works in Chromium','HAR playback is non-deterministic'],
    1,'HAR fixtures must be committed and refreshed when the API changes — divergence between local and CI HAR files is the cause.'],
  ['D','A','Modifying request headers with `route.continue({ headers: newHeaders })` works, but the original cookies are gone from the request. How do you fix it?',null,
    ['Cookies cannot be forwarded with continue()','The headers option REPLACES all headers — spread the originals first: { ...route.request().headers(), ...newHeaders }. Without spread, cookie and other headers are dropped.','Use context.setExtraHTTPHeaders() instead','Cookies must be set separately via context.addCookies()'],
    1,'Merging with spread preserves all original headers while adding or overriding specific ones.'],
  ['C','B','Complete to inspect request headers in a route handler.\n```ts\nawait page.route("**/api/**", route => {\n  const headers = route.request().______();\n  console.log(headers["content-type"]);\n  route.continue();\n});\n```',null,
    ['headers()','getHeaders()','requestHeaders()','allHeaders()'],
    0,'headers() returns a plain object with lowercase header names from the intercepted request.'],
  ['C','I','Complete to capture both the request and response for an API call.\n```ts\nconst [request, response] = await Promise.all([\n  page.______("**/api/data"),\n  page.______("**/api/data"),\n  page.click("#load"),\n]);\n```',null,
    ['waitForRequest, waitForResponse','onRequest, onResponse','captureRequest, captureResponse','expectRequest, expectResponse'],
    0,'Promise.all with waitForRequest and waitForResponse started before the click captures both sides of the network call.'],
  ['C','A','Complete to serve a static JSON file as the mocked response.\n```ts\nawait page.route("**/api/config", async route => {\n  await route.fulfill({\n    path: ______,\n  });\n});\n```',null,
    ['"fixtures/config.json"','"./config.json"','__dirname + "/config.json"','new URL("config.json", import.meta.url).pathname'],
    0,'path: accepts a file path — Playwright reads the file and infers Content-Type from the extension.'],
  ['C','I','Complete to modify the response body while preserving the status code.\n```ts\nawait page.route("**/api/user", async route => {\n  const resp = await route.fetch();\n  const body = await resp.json();\n  body.extra = "injected";\n  await route.fulfill({\n    ______: resp.status(),\n    body: JSON.stringify(body),\n    contentType: "application/json",\n  });\n});\n```',null,
    ['status','statusCode','code','httpStatus'],
    0,'status: sets the HTTP status code in the synthetic fulfill response.'],
  ['C','B','Complete to count how many times an API is called.\n```ts\nlet callCount = 0;\nawait page.route("**/api/track", route => {\n  ______++;\n  route.continue();\n});\n```',null,
    ['callCount','count','calls','requests'],
    0,'The closure variable callCount increments on each intercept — assert it after the test actions.'],
  ['L','B','Why is `page.route()` preferred over setting up a mock server for API mocking?',null,
    ['It is faster because it skips the network','It is simpler — no extra process to start, stop, or configure. Routes run in-process and can be set up per test. A mock server adds infra complexity and port management.','page.route() supports HTTP/2 while mock servers do not','Mock servers cannot handle dynamic responses'],
    1,'In-process route mocking is the simplest approach for most API stubbing scenarios in Playwright tests.'],
  ['L','I','What is the difference between `page.on("request", fn)` and `page.route("**", fn)`?',null,
    ['They are identical','on("request") is read-only — it observes requests without intercepting them. route() intercepts — the request is suspended until route.continue/fulfill/abort is called.','on("request") only fires for GET requests','route() only fires once per URL'],
    1,'Observation vs. interception — use on("request") for logging and route() when you need to modify or mock.'],
  ['L','I','Can `route.fulfill()` be used to simulate slow API responses?',null,
    ['No — fulfill is synchronous','Yes — wrap route.fulfill() in a setTimeout or use a helper: await new Promise(r => setTimeout(r, 2000)); then await route.fulfill({...}); to simulate a 2 second delay.','Only by setting timeout in playwright config','Yes — pass delay: 2000 as an option to fulfill'],
    1,'Adding a deliberate delay before fulfill() lets you test loading states and timeout handling.'],
  ['L','A','What is the advantage of `routeFromHAR()` over manual `route.fulfill()` for complex APIs?',null,
    ['HAR files are faster to replay','HAR files capture real API responses including headers, timing, and body — no need to hand-craft JSON fixtures for every endpoint. They can be updated with update: true when the API changes.','routeFromHAR() supports WebSocket mocking','HAR files compress better'],
    1,'HAR replay eliminates fixture maintenance for complex APIs — re-record when the API changes instead of manually updating JSON mocks.'],
  ['L','I','Why should you NOT use `page.route("**", route => route.abort())` in most tests?',null,
    ['It has a memory leak','Aborting all requests breaks page navigation, static asset loading, and any real endpoint the test needs. Restrict route patterns to specific URLs or API paths you want to intercept.','fullyParallel requires routes to be allowed','abort() is only valid for XHR, not fetch'],
    1,'Be specific with patterns — ** catches everything including the page HTML itself, causing navigation failures.'],
  ['R','B','You want to test the app\'s behavior when the API is down. How do you simulate this?',null,
    ['Shut down the API server during the test','await page.route("**/api/**", route => route.fulfill({ status: 503, body: "Service Unavailable" })) — then trigger the action that calls the API and assert the error state in the UI','Use a proxy to block the request','Set network conditions to offline'],
    1,'Simulating a 503 is the cleanest approach — no infrastructure changes, no side effects on other tests.'],
  ['R','I','You want to verify the correct payload is sent when a form is submitted. How?',null,
    ['Check the form\'s value attribute in the DOM','await page.route("**/api/submit", async route => { const body = route.request().postDataJSON(); expect(body.name).toBe("Alice"); await route.continue(); }) — assert the payload before forwarding.','Use page.on("request") and call request.postDataJSON()','Log the request in the browser console'],
    1,'Intercepting with route.continue() lets you assert the request payload while still allowing the real request to proceed.'],
  ['R','A','You need to test that the app retries a failed request exactly 3 times. How?',null,
    ['page.route() with a retry counter in a closure: let count = 0; route handler returns 503 for the first 2 calls and 200 on the 3rd. Assert count === 3 after the action.','Configure retries: 3 in playwright config','Use waitForRequest 3 times in sequence','Add a network condition that fails every third request'],
    0,'A closure counter with conditional fulfill responses precisely simulates N failures followed by success — the cleanest way to test retry logic.'],
  ['R','I','You need to test offline functionality. How do you simulate offline mode?',null,
    ['Disconnect the network adapter during the test','Use page.route("**", route => route.abort()) to abort all requests, then interact with the UI and assert the offline state message','Set headless: false and disable WiFi manually','Use context.setOffline(true) if available, otherwise abort all routes'],
    3,'context.setOffline(true) is the correct Playwright API — it puts the entire context offline without pattern matching.'],
  ['R','B','You want to inject a custom header into every outgoing API request. How?',null,
    ['Set headers in the request fixture','Use context.setExtraHTTPHeaders({ "x-test-env": "ci" }) — it adds the header to every request made by pages in that context, including API calls','Intercept every request with page.route() and add the header via continue()','Both B and C are valid; B is simpler'],
    3,'setExtraHTTPHeaders() is the simplest approach for global header injection — route-based injection is needed only when headers differ per request.'],
  ['S','B','A test shows different data depending on when it runs because the API returns real time-dependent data. How do you fix it?',null,
    ['Add retries to make the test eventually consistent','Mock the API endpoint with route.fulfill() to return fixed, deterministic data — the test becomes independent of time or external state','Use a fixed system clock via Date mock','Freeze the network layer'],
    1,'Deterministic mocks are the standard solution for time-sensitive or externally-dependent API data in tests.'],
  ['S','I','You need to test that a UI element appears only when a specific feature flag is set in the API response. How?',null,
    ['Toggle the feature flag in the database before the test','Use page.route("**/api/config", route => route.fulfill({ body: JSON.stringify({ featureFlag: true }) })) — the UI reads the flag from the API response and the test controls it precisely','Set the flag in localStorage before navigation','Add the flag as a query parameter'],
    1,'Route mocking to control API response content is exactly the right tool for feature-flag UI testing — no backend changes needed.'],
  ['S','A','Tests that use `routeFromHAR()` start failing after an API schema change. What is the correct remediation?',null,
    ['Delete the HAR file and let the tests skip','Re-record the HAR: add update: true to the routeFromHAR call, run the tests once (live network), remove update: true, commit the updated HAR file','Manually edit the HAR JSON to match the new schema','Add a route handler to transform the old HAR response to the new schema'],
    1,'Refreshing the HAR with update: true is the intended workflow — commit the new HAR and the tests return to passing.'],
  ['S','I','A route mock causes a CORS error in the browser console. The real API sets Access-Control-Allow-Origin. How do you fix the mock?',null,
    ['CORS errors are expected in mocked tests','Add the CORS header in fulfill: route.fulfill({ headers: { "access-control-allow-origin": "*" }, ... }) — the browser sees the header on the synthetic response and allows the cross-origin request','Disable CORS in Playwright config','Use context.route() instead of page.route()'],
    1,'Synthetic responses must include any headers the browser enforces — CORS headers are needed if the app is cross-origin from the mock server.'],
],
visual: [
  // ── Output ×12 ────────────────────────────────────────────────
  ['O','B','What does `page.screenshot({ path: \'debug.png\' })` do?',null,
    ['Opens a file picker to choose where to save','Saves a PNG of the current viewport to the specified path','Takes a screenshot and attaches it to the test report automatically','Records a short video clip named debug.png'],
    1,'page.screenshot() captures the viewport (or full page with fullPage: true) as a PNG.'],
  ['O','I','What does `expect(page).toHaveScreenshot(\'home.png\')` do on the very first run?',null,
    ['Fails immediately — the baseline does not exist yet','Creates a baseline PNG file in a __snapshots__ directory and passes the test','Throws a "golden file missing" error and stops the suite','Downloads home.png from the Playwright CDN'],
    1,'On first run Playwright creates the golden baseline — subsequent runs compare against it.'],
  ['O','I','What does `expect(page).toHaveScreenshot(\'home.png\')` do on subsequent runs?',null,
    ['Overwrites the baseline with the current screenshot','Compares the current screenshot pixel-by-pixel against the stored baseline and fails if they differ beyond the threshold','Records a video of the comparison','Skips the comparison if the baseline is older than 24 hours'],
    1,'Visual regression: the test fails if the rendered page differs from the committed baseline.'],
  ['O','B','What does `video: \'on-first-retry\'` in the config `use` block do?',null,
    ['Records a video only for the first test in the suite','Records a video when a test is retried — capturing evidence of failures without recording every passing test','Limits video recording to a maximum of one retry','Records a video of the first browser launch only'],
    1,'on-first-retry is the recommended setting — low disk usage while still capturing failing test evidence.'],
  ['O','I','What does a Playwright trace file contain?',null,
    ['Only a video recording of the test','DOM snapshots at every action, network request waterfall, console messages, and timestamped screenshots','Only the test source code and assertion results','A list of all locators used in the test'],
    1,'Traces are the richest debugging artifact — they combine DOM, network, console, and screenshots in one zip.'],
  ['O','B','How do you open a Playwright trace file after a test run?',null,
    ['Double-click the .zip file in your file explorer','Run `npx playwright show-trace trace.zip` or drag it onto trace.playwright.dev','Open it with npx playwright open --trace','Import it into the Playwright config as a HAR file'],
    1,'show-trace is the CLI command; trace.playwright.dev is the online viewer.'],
  ['O','I','What is the difference between `trace: \'on-first-retry\'` and `trace: \'retain-on-failure\'`?',null,
    ['They are identical — both only save traces for failed tests','on-first-retry records only when a retry occurs; retain-on-failure records always but deletes the zip if the test passes','retain-on-failure is deprecated — use on-first-retry','on-first-retry requires retries: 1 or higher to ever produce a trace'],
    1,'retain-on-failure records every test but discards passing ones — useful when you want a trace regardless of whether a retry happened.'],
  ['O','B','What does `page.screenshot({ fullPage: true })` do differently from the default?',null,
    ['Captures the page at 2× device pixel ratio','Scrolls and stitches the entire page including content below the fold','Captures all open browser tabs in one image','Removes the browser chrome (address bar, tabs) from the screenshot'],
    1,'Without fullPage, only the visible viewport is captured; fullPage: true stitches the entire scrollable page.'],
  ['O','I','What does the `--update-snapshots` CLI flag do?',null,
    ['Clears all test results and re-runs from scratch','Regenerates and overwrites existing visual baseline files with current screenshots','Updates the Playwright package to the latest version','Resets the browser profile between tests'],
    1,'--update-snapshots regenerates baselines — run it after intentional UI changes, then commit the new files.'],
  ['O','I','What does `locator.screenshot()` capture compared to `page.screenshot()`?',null,
    ['It captures the same viewport but with the locator highlighted','It captures only the bounding box of the matched element, not the full page','It is identical to page.screenshot() but faster','It requires the element to be in the viewport — it does not scroll'],
    1,'locator.screenshot() crops to the element\'s bounding box — ideal for component-level visual regression.'],
  ['O','A','What does `context.tracing.start({ screenshots: true, snapshots: true })` enable?',null,
    ['Automatic screenshots on every assertion failure','Records screenshots at each action step and captures DOM snapshots for the hover-to-inspect feature in the Trace Viewer','Starts continuous video recording for the context','Enables network HAR recording alongside the trace'],
    1,'screenshots adds action thumbnails; snapshots enables the Trace Viewer\'s interactive DOM inspector.'],
  ['O','I','Where does Playwright store `toHaveScreenshot()` baseline images?',null,
    ['In the test-results directory, deleted after each run','In a __snapshots__ subdirectory next to the spec file — should be committed to version control','In ~/.playwright/snapshots on the local machine','Uploaded to the Playwright cloud automatically'],
    1,'Baselines live in __snapshots__ beside the spec — commit them so CI can compare against the same reference.'],

  // ── Fill-in ×7 ────────────────────────────────────────────────
  ['F','B','Complete: `await page.______({ path: \'debug.png\' })` to save a screenshot',null,
    ['screenshot','capture','snap','photo'],
    0,'page.screenshot() is the method for saving a PNG of the current viewport.'],
  ['F','I','Complete: `await expect(page).______Screenshot(\'home.png\')` for visual regression',null,
    ['toHave','match','compare','baseline'],
    0,'toHaveScreenshot() is the visual regression assertion — creates or compares the baseline.'],
  ['F','B','Complete: `use: { ______: \'on-first-retry\' }` to capture video on retry',null,
    ['video','record','capture','media'],
    0,'video is the config key for browser session recording.'],
  ['F','I','Complete: `use: { ______: \'retain-on-failure\' }` to capture traces on failure',null,
    ['trace','tracing','debug','record'],
    0,'trace is the config key for Playwright\'s trace recording mode.'],
  ['F','I','Complete: `await context.tracing.______({ path: \'trace.zip\' })` to save the trace',null,
    ['stop','save','end','export'],
    0,'tracing.stop() finalises and writes the trace to the specified path.'],
  ['F','I','Complete: `npx playwright ______-trace trace.zip` to open the viewer',null,
    ['show','view','open','display'],
    0,'show-trace is the Playwright CLI command for opening the Trace Viewer.'],
  ['F','A','Complete: `await video.______As(\'recording.webm\')` to copy video to a custom path',null,
    ['save','copy','export','move'],
    0,'video.saveAs() copies the recorded WebM to the specified destination path.'],

  // ── Find the Error ×6 ──────────────────────────────────────────
  ['E','B','What is wrong?\n```ts\nawait page.screenshot({ fullPage: \'true\' });\n```',null,
    ['fullPage is not a valid option — use clip instead','fullPage must be a boolean true, not the string \'true\'','screenshot() cannot be awaited','\'true\' is treated as falsy in Playwright'],
    1,'fullPage is a boolean option — passing a string causes TypeScript to reject it and the behaviour is undefined at runtime.'],
  ['E','I','What is wrong?\n```ts\n// Inside the test body:\nconst path = await page.video()?.path();\nconsole.log(path); // undefined\n```',null,
    ['page.video() is only available in headed mode','video.path() returns undefined until the test ends and the video is finalised — call it in afterEach or after the test','path() must be called without await','page.video() requires video: \'on\' in config'],
    1,'The video file is not written until the browser context closes at the end of the test — access path in afterEach or a fixture teardown.'],
  ['E','I','What is wrong?\n```ts\nawait expect(page).toHaveScreenshot(\'home.png\', { maxDiffPixels: 0 });\n// Fails on every run despite no visible change\n```',null,
    ['maxDiffPixels: 0 is invalid — the minimum is 1','maxDiffPixels: 0 means zero pixels can differ — sub-pixel rendering variations between runs will always fail it','maxDiffPixels should be a percentage not a count','toHaveScreenshot does not accept an options object'],
    1,'maxDiffPixels: 0 is too strict for real-world rendering — use a small tolerance like 50-200 pixels to absorb antialiasing differences.'],
  ['E','A','What is wrong?\n```ts\nawait context.tracing.start({ screenshots: true, snapshots: true });\nawait page.goto(\'/\');\nawait page.getByRole(\'button\').click();\n// test ends — no tracing.stop() call\n```',null,
    ['tracing stops automatically when the test ends','tracing.stop() must be called to write the trace to disk — without it the zip is never created','start() already writes the trace incrementally so stop() is optional','The trace is saved to the default path automatically'],
    1,'tracing.stop({ path }) is required to flush and write the trace — always call it, preferably in a finally block.'],
  ['E','I','What is wrong?\n```ts\n// CI baseline comparison fails — different font rendering on Linux vs macOS\nawait expect(page).toHaveScreenshot(\'home.png\');\n```',null,
    ['toHaveScreenshot is not supported in CI','Baselines are OS-specific — generate the baseline on the same OS/browser as CI, or use threshold/maxDiffPixels to absorb rendering differences','CI requires the --ci flag with toHaveScreenshot','Fonts are not a factor in Playwright screenshots'],
    1,'Cross-OS rendering differences are the most common visual regression headache — generate baselines in the same environment as CI, or use Docker for consistency.'],
  ['E','B','What is wrong?\n```ts\n// Baselines are stored in __snapshots__ but not committed to Git\n// CI always creates a new baseline and the test always passes\n```',null,
    ['__snapshots__ should never be committed — they are auto-generated artifacts','Without committed baselines, CI has nothing to compare against — it creates a new baseline each run and always passes, defeating regression detection','Baselines should be stored in test-results not __snapshots__','CI cannot write to __snapshots__ — use the --snapshot-dir flag'],
    1,'Baseline files must be committed to VCS — otherwise CI compares against itself and regressions are never caught.'],

  // ── Debug ×6 ──────────────────────────────────────────────────
  ['D','B','`toHaveScreenshot()` fails with hundreds of pixel differences after a valid CSS change. What should you do?',null,
    ['Increase maxDiffPixels to a very high number','Run `npx playwright test --update-snapshots` to regenerate the baselines, review the diff, then commit the new files','Delete the __snapshots__ directory and re-run','Switch from toHaveScreenshot to a functional assertion'],
    1,'After intentional UI changes, regenerate baselines with --update-snapshots, visually review the diffs, then commit.'],
  ['D','I','The trace zip is 0 bytes after a test run. What is the most likely cause?',null,
    ['Traces require headed mode','tracing.stop() was called but tracing.start() was never called','The trace file path must end in .zip','Tracing is not compatible with fullyParallel: true'],
    1,'If start() is never called, stop() writes an empty or invalid file — ensure start() runs before any actions you want to capture.'],
  ['D','B','Visual regression screenshots differ between local and CI even though the code hasn\'t changed. What is the root cause?',null,
    ['The --update-snapshots flag was not run in CI','Different OS, browser engine version, font rendering, or DPI between environments produce slightly different pixels','toHaveScreenshot is non-deterministic by design','CI runs at a different viewport size'],
    1,'Sub-pixel rendering differs across OS/GPU combinations — pin the environment (use Docker), or generate baselines on CI itself.'],
  ['D','I','`page.video()` returns `null` even though `video: \'on\'` is set in the config. What should you check?',null,
    ['video: \'on\' only works in headed mode','The test may be using a different browser context than expected — ensure the page was created from the configured context','video requires a writeable test-results directory','null is the default before the video starts recording'],
    1,'Video is attached to the browser context — a page created from a manually launched browser won\'t have video unless explicitly configured.'],
  ['D','I','Trace Viewer shows actions but no network requests. What option was missing from `tracing.start()`?',null,
    ['network: true must be passed to tracing.start()','Network is always included — something else is wrong','The network tab requires the --trace-network CLI flag','Traces record network only for XHR requests, not fetch()'],
    0,'Network recording is automatic in Playwright traces — if it\'s missing, check that tracing.start() ran before page.goto(), since requests before start() are not captured.'],
  ['D','A','Visual regression tests produce different baselines per browser project (Chromium vs WebKit). How are the baselines organised?',null,
    ['All projects share a single baseline — differences are expected and ignored','Playwright automatically namespaces baselines per project — e.g. __snapshots__/home-chromium.png vs home-webkit.png','You must run --update-snapshots once per project separately','Baselines are identical across projects — rendering differences are handled by threshold'],
    1,'Playwright includes the project name in the snapshot filename, so each browser project has its own independent baseline.'],

  // ── Complete ×5 ──────────────────────────────────────────────
  ['C','B','Complete to take a full-page screenshot.\n```ts\nawait page.screenshot({ path: \'full.png\', ______: true });\n```',null,
    ['fullPage','full','scrollable','entire'],
    0,'fullPage: true stitches the entire scrollable page including below-fold content.'],
  ['C','I','Complete visual regression with pixel tolerance.\n```ts\nawait expect(page).toHaveScreenshot(\'home.png\', { ______: 100 });\n```',null,
    ['maxDiffPixels','threshold','tolerance','pixelDiff'],
    0,'maxDiffPixels allows up to N pixels to differ before failing the comparison.'],
  ['C','I','Complete to start a manual trace.\n```ts\nawait context.tracing.______({ screenshots: true, snapshots: true });\n```',null,
    ['start','begin','record','init'],
    0,'tracing.start() initialises trace recording with the specified options.'],
  ['C','I','Complete to save the trace to disk.\n```ts\nawait context.tracing.______({ path: \'trace.zip\' });\n```',null,
    ['stop','save','end','write'],
    0,'tracing.stop() finalises and writes the trace zip to the path.'],
  ['C','A','Complete to capture a single element screenshot.\n```ts\nconst chart = page.getByTestId(\'chart\');\nawait chart.______({ path: \'chart.png\' });\n```',null,
    ['screenshot','capture','snap','toHaveScreenshot'],
    0,'locator.screenshot() crops the output to the element\'s bounding box.'],

  // ── Logic ×5 ──────────────────────────────────────────────────
  ['L','B','What is the main difference between a screenshot and a Playwright trace?',null,
    ['A screenshot is larger than a trace','A screenshot captures a single visual moment; a trace captures the full timeline including DOM snapshots, network, console, and action sequence','Traces are only available in headed mode; screenshots work in headless','Screenshots are for debugging; traces are for visual regression'],
    1,'Traces are the richest artifact — they let you step through the test\'s entire history in the Trace Viewer.'],
  ['L','I','When should you use `toHaveScreenshot()` instead of functional assertions like `toHaveText()`?',null,
    ['Always — screenshots are more reliable than text assertions','For visual layout regressions (CSS, spacing, colours) that functional assertions cannot detect — text assertions handle content correctness','When the element is not locatable by role or text','Only for full-page checks, not individual components'],
    1,'Visual regression catches layout regressions; functional assertions verify content and state — use both together.'],
  ['L','I','What is the advantage of `retain-on-failure` over `on-first-retry` for traces?',null,
    ['retain-on-failure is smaller in file size','retain-on-failure captures a trace even for tests that fail on the first attempt without a retry — on-first-retry requires a retry to record','on-first-retry has better DOM snapshots','They are identical — the names are just aliases'],
    1,'With retries: 0, on-first-retry never fires — retain-on-failure always captures and discards only passing tests.'],
  ['L','B','Why must baseline PNG files be committed to version control?',null,
    ['Playwright refuses to run without committed baselines','Without committed baselines CI compares against a freshly created baseline each run — regressions are never detected','Baselines are binary and should not be in VCS','The --update-snapshots flag only works when baselines are in VCS'],
    1,'Baselines must be in VCS so CI checks the current render against the approved reference, detecting any unexpected changes.'],
  ['L','A','A component has a live timestamp that changes every second. How do you prevent it from causing visual regression false failures?',null,
    ['Freeze JavaScript time with page.clock.setFixedTime() before the screenshot','Increase maxDiffPixels to cover the entire timestamp area','Use toContainText() instead of toHaveScreenshot()','All three approaches work — mask is the cleanest'],
    3,'mask: [locator] (blackouts the element), page.clock.setFixedTime() (freezes it), or maxDiffPixels (tolerates it) all work — mask is cleanest for stable visual tests.'],

  // ── Real-World ×5 ─────────────────────────────────────────────
  ['R','B','You want to verify a product card looks correct after a CSS change. What assertion do you use?',null,
    ['await expect(card).toHaveCSS(\'color\', \'…\') for each property','await expect(page.getByTestId(\'product-card\')).toHaveScreenshot(\'card.png\')','await page.screenshot({ clip: cardBounds })','Use a visual diff tool outside of Playwright'],
    1,'locator.toHaveScreenshot() captures just the card and compares against the committed baseline.'],
  ['R','I','You want traces only for failed tests to keep CI artifact storage manageable. Which config option do you use?',null,
    ['trace: \'on-first-retry\'','trace: \'retain-on-failure\'','trace: \'on\'','trace: \'off\' and add manual tracing to each test'],
    1,'retain-on-failure records all tests but deletes the trace zip if the test passes — minimises storage.'],
  ['R','I','A test has a date picker that shows today\'s date, breaking visual regression daily. How do you handle it?',null,
    ['Run visual tests only on the day the baseline was created','Pass mask: [page.getByTestId(\'datepicker\')] to toHaveScreenshot() to exclude it from comparison','Use threshold: 1 to ignore all differences','Regenerate baselines every day in CI'],
    1,'mask blackouts the dynamic element so its pixel values don\'t contribute to the diff.'],
  ['R','A','After a complex checkout failure you want to see every DOM state and network call step by step. What is the workflow?',null,
    ['Add console.log() throughout the test and read the output','Open the trace zip from test-results/ in the Trace Viewer — it shows DOM snapshots, network waterfall, and console for each action','Rerun the test in headed mode and watch manually','Use page.on(\'request\') to log network calls'],
    1,'The Trace Viewer is purpose-built for this: click any action in the timeline and see the exact DOM state and network calls at that moment.'],
  ['R','I','You want to verify the app\'s dark mode renders correctly. How do you structure the visual regression test?',null,
    ['Set colorScheme: \'dark\' in the test and take a screenshot','Use page.emulateMedia({ colorScheme: \'dark\' }) or set colorScheme in the project config, then run toHaveScreenshot with a dark-specific baseline name','Dark mode cannot be tested with visual regression','Use CSS assertions for every dark mode colour'],
    1,'emulateMedia({ colorScheme: \'dark\' }) or a project-level colorScheme forces the dark theme; name the snapshot to distinguish it from the light baseline.'],

  // ── Scenario ×4 ──────────────────────────────────────────────
  ['S','I','A new team member runs the test suite and all visual regression tests fail immediately. The baseline files are in the repo. What is the likely cause?',null,
    ['The baselines are corrupt','The member is running on a different OS/browser version producing slightly different rendering — they need to run tests in the same environment or use Docker','The --update-snapshots flag must be run once on first clone','Playwright requires re-creating baselines per machine'],
    1,'Cross-environment rendering differences are the #1 cause of immediate visual regression failures — align the environment or add threshold tolerance.'],
  ['S','A','You need to trace only the payment flow in a long test suite without tracing every test. How?',null,
    ['Set trace: \'on\' globally — all tests are traced','Set trace: \'off\' globally and add manual context.tracing.start/stop only around the payment test steps','Use --grep to run only the payment test with tracing enabled','Tracing cannot be limited to specific tests — it\'s all-or-nothing'],
    1,'Manual tracing gives surgical control — start before the first payment action and stop (in a finally) after the last one.'],
  ['S','I','CI visual regression tests pass locally but fail with "X pixels different" in CI. The difference is exactly in text rendering. What is the most practical fix?',null,
    ['Increase maxDiffPixels to a very high number to silence all failures','Generate baselines in CI using --update-snapshots on the first CI run, commit them, and use those CI-generated files as the reference','Switch to functional text assertions for all visual tests','Disable anti-aliasing in the browser config'],
    1,'Generating baselines in CI on a dedicated "update" run ensures the reference matches the exact rendering environment used for comparison.'],
  ['S','A','After a redesign, 40 visual regression tests fail. You want to approve all of them efficiently. What is the process?',null,
    ['Delete all __snapshots__ directories and re-run','Run `npx playwright test --update-snapshots`, review the HTML report\'s diff view to confirm each change is intentional, then commit the updated files','Update each baseline manually by copying test-results images to __snapshots__','There is no bulk update — each test must be updated individually'],
    1,'--update-snapshots regenerates all baselines in one command; the HTML report\'s diff view lets you review every change before committing.'],

  // ── Additional batch ×50 ─────────────────────────────────────
  ['O','B','What is stored in the `__snapshots__` directory?',null,
    ['Playwright trace files','Baseline PNG images used for visual regression comparison — one file per toHaveScreenshot() name, per browser/OS/viewport combination','HTML reports for failed tests','Video recordings of test runs'],
    1,'__snapshots__ holds the reference images — committed to source control and compared against actual screenshots on every test run.'],
  ['O','I','What does `toHaveScreenshot({ maxDiffPixels: 50 })` do?',null,
    ['The test fails if more than 50% of pixels differ','The test passes if the actual screenshot differs by at most 50 pixels from the baseline','The test captures 50 screenshots and picks the best match','50 is the screenshot quality setting'],
    1,'maxDiffPixels sets an absolute pixel tolerance — useful for minor rendering differences like anti-aliasing.'],
  ['O','B','What does `page.screenshot({ fullPage: true })` capture?',null,
    ['Only the viewport — same as the default','The entire scrollable content of the page — taller than the viewport if the page is long','A screenshot of every tab','A PDF rendition of the full page'],
    1,'fullPage: true scrolls and stitches the full document — ideal for capturing long pages in one image.'],
  ['O','I','What does `expect(locator).toHaveScreenshot()` do differently than `expect(page).toHaveScreenshot()`?',null,
    ['They are identical — locator and page screenshots use the same area','locator.toHaveScreenshot() clips the screenshot to the locator\'s bounding box — only that element is captured, not the full viewport','locator variant captures in higher resolution','page variant requires fullPage: true'],
    1,'Element-scoped screenshots reduce noise — a change elsewhere on the page won\'t fail the test for that component.'],
  ['O','A','What does `page.screenshot({ clip: { x: 0, y: 0, width: 300, height: 200 } })` do?',null,
    ['Resizes the viewport to 300×200','Captures only the 300×200 pixel region from the top-left corner of the viewport','Crops the existing full-page screenshot','Captures the element at coordinates (300, 200)'],
    1,'clip: defines a precise viewport sub-region to capture — useful when only a specific area is relevant.'],
  ['O','I','What file format does `toHaveScreenshot()` use by default?',null,
    ['JPEG','PNG — lossless compression, correct for pixel-perfect comparison','WebP','SVG'],
    1,'PNG is lossless — JPEG compression would introduce artifacts that break exact pixel comparison.'],
  ['O','B','What does `--update-snapshots` do when passed to `npx playwright test`?',null,
    ['Deletes all existing snapshots and leaves them empty','Regenerates all baseline snapshot files with the current actual screenshots — new baselines replace old ones','Updates the Playwright version','Updates only failing snapshots'],
    1,'--update-snapshots is the "approve changes" command — run it after intentional UI changes, then commit the new baselines.'],
  ['O','I','What does `toHaveScreenshot({ threshold: 0.1 })` do?',null,
    ['Allows up to 10 pixels difference','Allows a perceptual difference ratio of up to 10% per pixel (using SSIM/color-distance) — combines with maxDiffPixels for full control','Sets JPEG quality to 10%','Waits 100ms before comparing'],
    1,'threshold is per-pixel color distance tolerance (0–1) — distinct from maxDiffPixels which counts total differing pixels.'],
  ['O','A','What does `page.screenshot({ animations: "disabled" })` do?',null,
    ['Pauses JavaScript but allows CSS animations','Freezes CSS animations and transitions before capturing — prevents animated elements from appearing mid-transition in screenshots','Disables all event handlers','Skips elements with animation attributes'],
    1,'animations: "disabled" ensures deterministic screenshots — animated carousels, spinners, and transitions appear in their rest state.'],
  ['O','I','What is the purpose of `expect.soft(locator).toHaveScreenshot()` in visual testing?',null,
    ['It skips flaky screenshot comparisons','The screenshot assertion is non-fatal — the test continues even if it fails, and all soft assertion failures are reported at the end','It takes a lower-quality screenshot','It compares only soft (blurry) areas of the screenshot'],
    1,'Soft assertions let you collect all visual differences in one run rather than stopping at the first failure.'],
  ['O','B','What does `context.tracing.start({ screenshots: true, snapshots: true })` do?',null,
    ['Starts recording a video','Begins a Playwright trace recording that captures screenshots at each step and DOM/network snapshots — viewable in the Playwright Trace Viewer','Starts the screenshot comparison baseline','Enables continuous screenshot mode'],
    1,'Tracing with screenshots:true and snapshots:true creates a full timeline — every action has a before/after screenshot in the trace.'],
  ['O','A','What does `await context.tracing.stop({ path: "trace.zip" })` do?',null,
    ['Saves a video recording as trace.zip','Finalises and writes the trace to trace.zip — the archive contains screenshots, network logs, and DOM snapshots viewable with `playwright show-trace trace.zip`','Stops the browser context and exports state','Stops the test runner'],
    1,'tracing.stop() with a path saves the complete trace archive — share it with teammates or attach it to CI artifacts.'],
  ['F','B','Complete to take a full-page screenshot.\n```ts\nawait page.screenshot({ path: "full.png", ______: true });\n```',null,
    ['fullPage','full','entirePage','scrollable'],
    0,'fullPage: true captures the entire scrollable document height.'],
  ['F','I','Complete to assert an element\'s screenshot matches a baseline.\n```ts\nconst card = page.locator(".product-card");\nawait expect(card).______("card.png");\n```',null,
    ['toHaveScreenshot','toMatchSnapshot','toEqualScreenshot','toBeVisually'],
    0,'toHaveScreenshot() compares against a stored PNG baseline, creating it on first run.'],
  ['F','B','Complete to set pixel tolerance on a screenshot assertion.\n```ts\nawait expect(page).toHaveScreenshot("hero.png", {\n  ______: 20,\n});\n```',null,
    ['maxDiffPixels','tolerance','pixelThreshold','diffPixels'],
    0,'maxDiffPixels: N allows up to N pixels to differ before the assertion fails.'],
  ['F','A','Complete to start tracing.\n```ts\nawait context.tracing.start({\n  screenshots: true,\n  ______: true,\n});\n```',null,
    ['snapshots','dom','trace','network'],
    0,'snapshots: true captures DOM and network state at each trace step — essential for the Trace Viewer timeline.'],
  ['F','I','Complete to disable animations before a screenshot.\n```ts\nawait page.screenshot({ path: "stable.png", ______: "disabled" });\n```',null,
    ['animations','transitions','motion','css'],
    0,'animations: "disabled" freezes CSS animations and transitions for a stable capture.'],
  ['F','B','Complete to save the trace to a file.\n```ts\nawait context.tracing.stop({ ______: "trace.zip" });\n```',null,
    ['path','file','output','name'],
    0,'path: is where the trace archive is written — pass it to `playwright show-trace` to inspect.'],
  ['F','I','Complete to clip the screenshot to a region.\n```ts\nawait page.screenshot({\n  path: "region.png",\n  ______: { x: 100, y: 50, width: 400, height: 300 },\n});\n```',null,
    ['clip','region','crop','bounds'],
    0,'clip: { x, y, width, height } captures only that viewport sub-region.'],
  ['F','A','Complete to mask a dynamic element before comparison.\n```ts\nawait expect(page).toHaveScreenshot("page.png", {\n  ______: [page.locator(".timestamp")],\n});\n```',null,
    ['mask','exclude','ignore','hide'],
    0,'mask: [locators] overlays a pink rectangle over dynamic content before comparison — timestamps, avatars, and ads are common masks.'],
  ['F','I','Complete to set a perceptual color-distance threshold.\n```ts\nawait expect(page).toHaveScreenshot("logo.png", {\n  ______: 0.2,\n});\n```',null,
    ['threshold','tolerance','maxDiff','sensitivity'],
    0,'threshold: 0–1 sets the per-pixel color-distance tolerance using a perceptual metric.'],
  ['E','B','What is wrong?\n```ts\n// First run — no baseline exists\nawait expect(page).toHaveScreenshot("home.png");\n// This is run in CI without --update-snapshots first\n```',null,
    ['toHaveScreenshot always passes on first run','If no baseline exists and --update-snapshots was not run to generate one, the test fails with "screenshot is missing". Generate baselines locally first, commit them, then run in CI.','CI auto-generates baselines','toHaveScreenshot requires a baseline path option'],
    1,'Baselines must be generated and committed before CI can compare — run locally, commit, then CI compares.'],
  ['E','I','What is wrong?\n```ts\nawait context.tracing.start({ screenshots: true });\nawait page.goto("/");\nawait page.click("button");\n// no tracing.stop() — test ends\n```',null,
    ['tracing auto-saves on test end','Without tracing.stop(), the trace is never written to disk — always call stop() in an afterEach or finally block to ensure the trace is saved even on test failure.','screenshots: true makes tracing.stop() optional','The trace is saved when the context closes'],
    1,'Always stop tracing in a finally or afterEach — test failures skip code after the failing assertion, so stop() must be in cleanup.'],
  ['E','B','What is wrong?\n```ts\nawait expect(page).toHaveScreenshot("snapshot");\n// Extension omitted\n```',null,
    ['Extensions are optional — Playwright adds .png automatically','The name must include the .png extension explicitly: "snapshot.png". Without it, Playwright throws a "name must include extension" error.','.jpg is the required extension','Snapshot names cannot contain dots'],
    1,'Always include the .png extension in toHaveScreenshot names.'],
  ['E','I','What is wrong?\n```ts\n// playwright.config.ts\nuse: {\n  screenshot: "on",\n}\n```',null,
    ['"on" is the correct value for always capturing screenshots','The screenshot option is "only-on-failure" for failure screenshots or "on" for all. This is valid config — however this only captures test result screenshots, NOT visual regression baselines. toHaveScreenshot() is separate.','Screenshots cannot be configured globally','The option belongs under reporter, not use'],
    1,'use.screenshot is for CI artifact screenshots; toHaveScreenshot() is the separate visual regression API — they serve different purposes.'],
  ['E','A','What is wrong?\n```ts\nawait expect(page).toHaveScreenshot("header.png");\n// Baseline was captured on a 1280×800 viewport\n// Test now runs at 1920×1080\n```',null,
    ['Viewport size does not affect screenshots','The dimensions differ — the comparison will fail because the image sizes don\'t match. Baselines must be regenerated after viewport changes, or set a fixed viewport in the test config.','toHaveScreenshot scales images before comparing','The baseline PNG is resized automatically'],
    1,'Screenshot dimensions are part of the baseline — any viewport change requires new baselines.'],
  ['D','B','`toHaveScreenshot()` fails with "2 pixels are different". The diff shows a 1-pixel border rendering difference. How do you fix it without updating the baseline?',null,
    ['Disable border CSS for tests','Add maxDiffPixels: 5 or threshold: 0.1 to allow minor rendering differences without failing','Re-run the test — it will pass on retry','Switch from PNG to JPEG'],
    1,'Small rendering differences (sub-pixel, anti-aliasing) are handled with tolerance options — not by updating the baseline.'],
  ['D','I','Visual tests fail in CI but pass locally. The diff shows text looks slightly different. What is the most likely cause?',null,
    ['The CI network is blocking fonts','Font rendering differs between OSes — CI runs Linux while local runs macOS/Windows. Use a Docker image that matches CI\'s OS, or set a threshold to tolerate font rendering differences.','The test viewport is different in CI','CI doesn\'t support screenshot assertions'],
    1,'Cross-OS font rendering is the most common cause of visual test environment mismatch — align environments using Docker.'],
  ['D','B','The trace viewer shows a blank "before" screenshot for a step. What is the likely cause?',null,
    ['The browser crashed before the step','tracing.start() was called without screenshots: true — enable it to see before/after screenshots in the trace viewer','The trace file is corrupt','The page was not loaded before tracing started'],
    1,'screenshots: true is required to see visual snapshots in the trace — without it, only events and DOM snapshots are recorded.'],
  ['D','I','`page.screenshot()` captures an element partially off-screen. How do you ensure the element is fully visible?',null,
    ['Use fullPage: true','Call await locator.scrollIntoViewIfNeeded() before taking the screenshot — or use locator.screenshot() which automatically scrolls the element into view','Increase the viewport height','Use clip: to capture a larger area'],
    1,'locator.screenshot() handles scroll-into-view automatically — use it for element-scoped captures instead of page.screenshot() with manual scrolling.'],
  ['D','A','A visual test is flaky — it fails 1 in 5 runs due to a loading spinner still visible at assertion time. How do you fix it?',null,
    ['Add retries: 3 to the test','Wait for the spinner to disappear before taking the screenshot: await page.locator(".spinner").waitFor({ state: "hidden" }); then take the screenshot','Use animations: "disabled" to freeze the spinner','Add a 2 second sleep before the screenshot'],
    1,'Explicit waitFor on the spinner ensures it is gone before capture — deterministic and fast.'],
  ['C','B','Complete to capture a screenshot of a single element.\n```ts\nconst header = page.locator("header");\nawait header.______({ path: "header.png" });\n```',null,
    ['screenshot','capture','save','export'],
    0,'locator.screenshot() clips to the element\'s bounding box and scrolls it into view automatically.'],
  ['C','I','Complete to mask dynamic content in a visual assertion.\n```ts\nawait expect(page).toHaveScreenshot("profile.png", {\n  mask: [______],\n});\n```',null,
    ['page.locator(".avatar")','".avatar"','{ selector: ".avatar" }','page.getByClass("avatar")'],
    0,'mask: accepts an array of Locator objects — not CSS strings.'],
  ['C','A','Complete to stop tracing on test failure only.\n```ts\ntest.afterEach(async ({ context }, testInfo) => {\n  if (testInfo.______) {\n    await context.tracing.stop({ path: "trace.zip" });\n  }\n});\n```',null,
    ['status === "failed"','failed','didFail','error'],
    0,'testInfo.status === "failed" is the idiomatic check — trace only on failure to save storage.'],
  ['C','I','Complete to assert element screenshot within tolerance.\n```ts\nconst btn = page.locator(".cta");\nawait expect(btn).toHaveScreenshot("cta.png", {\n  maxDiffPixels: ______,\n});\n```',null,
    ['10','0','100','1'],
    0,'10 pixels is a common starting tolerance for button screenshots — adjust based on your rendering environment.'],
  ['C','B','Complete to open the trace viewer.\n```sh\nnpx playwright ______ trace.zip\n```',null,
    ['show-trace','open-trace','view-trace','trace'],
    0,'show-trace opens the interactive Trace Viewer in a browser tab.'],
  ['L','B','Why should visual regression baselines be committed to version control?',null,
    ['So Playwright can access them from the CDN','Committed baselines make visual changes reviewable in PRs — teammates can see exact pixel diffs in the diff view and intentional changes can be approved as part of code review','Playwright requires them to be in the repo root','Local-only baselines are faster'],
    1,'Source-controlled baselines turn visual regressions into a code-review concern — reviewers see exactly what changed in the UI.'],
  ['L','I','Why is `locator.screenshot()` preferred over `page.screenshot({ clip: ... })` for element screenshots?',null,
    ['locator.screenshot() is faster','locator.screenshot() automatically scrolls the element into view, clips to its bounding box, and does not require manual coordinate calculation — clip: requires knowing exact pixel positions which change if the layout changes.','clip: requires a fixed viewport','locator variant uses a higher DPI'],
    1,'Auto-scroll and auto-clip make locator.screenshot() far more robust than manual coordinate-based clipping.'],
  ['L','A','What is the difference between `screenshots: true` and `snapshots: true` in tracing options?',null,
    ['They are the same option','screenshots: true captures a pixel screenshot at each action (before/after). snapshots: true captures DOM and network state (the source for the DOM inspector and network tab in Trace Viewer) — both together give the full timeline experience.','snapshots: true is for video','screenshots refers to test-result screenshots'],
    1,'Both are needed for a fully usable trace — screenshots for visual timeline, snapshots for DOM/network inspection.'],
  ['L','I','Why does `toHaveScreenshot()` create separate baselines per OS and browser?',null,
    ['It is a Playwright limitation to be fixed','Font rendering, sub-pixel anti-aliasing, and scrollbar styles differ between OS/browser combinations — separate baselines prevent false failures from rendering environment differences.','It stores multiple quality levels','Each browser uses different screenshot APIs'],
    1,'Environment-specific baselines are the correct approach — you cannot have one PNG that matches identical rendering on Windows, macOS, and Linux.'],
  ['L','I','What is the difference between `trace: "on"` and `trace: "on-first-retry"` in playwright config?',null,
    ['They produce identical traces','on traces every test run (large storage cost). on-first-retry traces only when a test is retried for the first time (failing tests are the ones you need to debug) — a better default for CI.','on-first-retry requires retries: 1','trace: "on" only works in headful mode'],
    1,'on-first-retry is the recommended CI setting — you get traces for failures without the storage cost of tracing every passing test.'],
  ['R','B','You want to add visual regression testing to an existing test suite without modifying every test. What is the quickest approach?',null,
    ['Convert all tests to toHaveScreenshot','Add toHaveScreenshot() calls at the end of your most critical end-to-end tests — start with the golden path and expand coverage iteratively','Set screenshot: "on" in config — it auto-generates baselines','Use Playwright\'s visual mode flag'],
    1,'Incremental adoption — add toHaveScreenshot to high-value tests first, build confidence, then expand coverage.'],
  ['R','I','A teammate asks for a trace of a specific failing test. How do you generate one?',null,
    ['Send them the test-results video folder','Set trace: "on" in playwright.config.ts, re-run the failing test, find the trace.zip in test-results/, and share it — they open it with `playwright show-trace trace.zip`','Traces are only available in cloud CI','Use --debug flag to generate a trace'],
    1,'The standard workflow: enable trace, run the test, share the zip — teammate runs show-trace to inspect the full timeline.'],
  ['R','A','You need to capture before/after screenshots of a form submission for a visual changelog. How?',null,
    ['Use two toHaveScreenshot calls','await page.screenshot({ path: "before.png" }); then fill and submit the form; await page.screenshot({ path: "after.png" }); — store both in the test-results or fixtures directory','Use page.on("response") to trigger screenshots','Use Playwright\'s diff mode'],
    1,'Manual page.screenshot() calls give you precise before/after images — name them descriptively and save to a known path for easy retrieval.'],
  ['R','I','You need to test the dark mode appearance of a component. How?',null,
    ['Manually set body class in the test','colorScheme option: await page.emulate({ colorScheme: "dark" }) before navigating, then use toHaveScreenshot("component-dark.png") — run both light and dark assertions in the same test or separate tests','Use a CSS override in the test','Change the OS theme before running'],
    1,'page.emulate({ colorScheme: "dark" }) or context creation with colorScheme: "dark" is the clean way to test dark mode.'],
  ['S','B','You add a new component and want to baseline its screenshot for the first time. What is the workflow?',null,
    ['Write the toHaveScreenshot assertion, let it fail, then find the actual screenshot in test-results and copy it to __snapshots__','Write the toHaveScreenshot assertion, run `npx playwright test --update-snapshots`, confirm the generated PNG looks correct, then commit it','Playwright auto-commits baselines on first run','Run the test twice — second run auto-commits'],
    1,'--update-snapshots creates new baselines (and updates changed ones) — always inspect before committing.'],
  ['S','I','CI visual tests fail because a banner ad renders dynamic content. You cannot remove the ad. How do you fix the test?',null,
    ['Accept the flakiness and add retries','Add the ad container to the mask list: toHaveScreenshot({ mask: [page.locator(".ad-banner")] }) — Playwright overlays it with a solid color before comparison','Use threshold: 1 to ignore all differences','Screenshot only the part of the page below the ad'],
    1,'mask: is designed exactly for this — dynamic content like ads, timestamps, and user avatars that legitimately change between runs.'],
  ['S','A','You want to run visual regression tests only in CI, not on every local run. How?',null,
    ['Add an environment check: if (process.env.CI) { await expect(page).toHaveScreenshot(...) } — visual assertions only run in CI','Use test.skip(!process.env.CI) at the top of each visual test file — the tests are skipped locally but run in CI','Visual tests always run everywhere — Playwright does not support conditional execution','Add --grep visual to the CI command'],
    1,'test.skip(!process.env.CI) is a clean pattern for CI-only tests — local developers skip the visual suite for speed.'],
],
pom: [
  // ── Output ×12 ────────────────────────────────────────────────
  ['O','B','What problem does the Page Object Model primarily solve?',null,
    ['Slow test execution due to repeated browser launches','DOM locators and interactions scattered across many tests — a UI change requires updating every test','Playwright lacking built-in assertion methods','Tests not running in parallel'],
    1,'POM centralises locators and actions in one class — a UI change is fixed in one place, not across every test.'],
  ['O','B','What does the constructor of a page object receive?',null,
    ['The test name and assertion count','The Playwright Page object (and optionally context or request)','The URL of the page to navigate to','The list of locators to use'],
    1,'The page object wraps a Page instance — the constructor is how it gets access to browser interactions.'],
  ['O','I','Why are locators declared as `readonly` properties rather than computed inside each method?',null,
    ['readonly prevents tests from accidentally overwriting them','They are lazy — evaluated on each access so they always reflect the current DOM — and declaring them as properties keeps them DRY','readonly properties are faster than method-computed locators','They enable TypeScript to infer types automatically'],
    1,'Playwright locators are lazy references — a readonly property re-queries the DOM every time it is used.'],
  ['O','B','What should page object action methods return?',null,
    ['The expect() result so tests can chain assertions','Usually nothing (void) or the next page object for chained navigation — assertions should stay in the test','A boolean: true if the action succeeded','The locator they interacted with'],
    1,'Page objects are action libraries — they execute interactions and leave assertions to the test.'],
  ['O','I','What is the "actions in PO, assertions in test" rule?',null,
    ['Page objects must call at least one expect() to verify the action succeeded','Assertions (expect()) belong in the test; page objects only perform interactions — this keeps POs reusable and assertions visible','Page objects should throw errors instead of using expect()','Actions and assertions can be mixed freely'],
    1,'Mixing expect() into page objects makes them context-dependent and harder to reuse across different test scenarios.'],
  ['O','B','What is a component object in the POM pattern?',null,
    ['A page object that only tests React components','A class representing a reusable UI component (header, modal, table) that is composed into full page objects','A Playwright built-in for testing web components','A fixture that provides DOM elements'],
    1,'Component objects mirror the UI\'s component architecture — reusable across multiple page objects.'],
  ['O','I','How does the fixture system improve POM usage compared to `new LoginPage(page)` in every test?',null,
    ['Fixtures are faster because they cache the page object instance','Fixtures handle instantiation and teardown centrally — tests request the page object by name without knowing how to create it','new LoginPage(page) is deprecated in favour of fixtures','Fixtures provide TypeScript types automatically'],
    1,'Fixture-based POM means tests just destructure `{ loginPage }` — the fixture handles construction and any teardown.'],
  ['O','I','When a UI element\'s label changes from "Sign in" to "Log in", how many files need updating with POM?',null,
    ['Every test that clicks the sign-in button','Only the page object class that defines the submitButton locator','The playwright.config.ts and every spec file','Both the page object and a central locator config file'],
    1,'This is POM\'s core benefit — one change propagates automatically to all tests.'],
  ['O','B','What TypeScript modifier is conventionally used for the `page` parameter stored in a page object class?',null,
    ['public readonly','protected','private readonly','static'],
    2,'private readonly ensures the page reference is not accessible outside the class and cannot be reassigned.'],
  ['O','I','What does composing component objects inside a page object look like?',null,
    ['Importing and calling the component object\'s static methods','Declaring the component as a readonly property: `readonly header = new Header(this.page)`','Extending the component class with `extends`','Passing the component class as a constructor parameter'],
    1,'Composition (has-a) is preferred over inheritance (is-a) — a DashboardPage has a Header, it is not a Header.'],
  ['O','I','What is the recommended directory structure for page objects?',null,
    ['Place all page objects inside the tests/ directory alongside specs','Place them in a pages/ or page-objects/ directory separate from specs — import them into tests or fixtures','Name them the same as the spec files they are used in','Store them in playwright.config.ts as named exports'],
    0,'A dedicated pages/ directory keeps page objects separate from tests and easy to discover.'],
  ['O','A','Can a page object method navigate to a different page and return a new page object for that page?',null,
    ['No — page objects can only interact with their own page','Yes — a method can navigate and return `new NextPage(this.page)` enabling chained fluent-style usage','Only if the page object extends a base NavigablePage class','Only if the test re-instantiates the page object after navigation'],
    1,'Returning the next page object from a navigation action enables fluent chaining: `await loginPage.signIn(…)` returns a `DashboardPage`.'],

  // ── Fill-in ×7 ────────────────────────────────────────────────
  ['F','B','Complete the constructor to store the page reference.\n```ts\nconstructor(______ readonly page: Page) {}\n```',null,
    ['private','public','protected','readonly'],
    0,'private readonly is the idiomatic TypeScript shorthand for a private constructor parameter stored as a property.'],
  ['F','I','Complete the locator declaration.\n```ts\n______ submitButton = this.page.getByRole(\'button\', { name: \'Sign in\' });\n```',null,
    ['readonly','private','const','let'],
    0,'readonly prevents accidental reassignment; the locator is re-evaluated lazily on each access.'],
  ['F','B','Complete the action method signature.\n```ts\n______ signIn(email: string, pw: string) {\n  await this.emailInput.fill(email);\n  await this.passwordInput.fill(pw);\n  await this.submitButton.click();\n}\n```',null,
    ['async','public async','private async','static async'],
    0,'async is required because the method calls await on Playwright actions.'],
  ['F','I','Complete the fixture to provide the page object.\n```ts\nloginPage: async ({ page }, use) => {\n  await ______(new LoginPage(page));\n},\n```',null,
    ['use','provide','inject','return'],
    0,'use() hands the page object to the test and marks the boundary between setup and teardown.'],
  ['F','I','Complete the goto method.\n```ts\nasync goto() {\n  await this.page.______(\'/login\');\n}\n```',null,
    ['goto','navigate','open','visit'],
    0,'page.goto() is the Playwright navigation method.'],
  ['F','I','Complete to compose a component object inside a page object.\n```ts\nreadonly header = new ________(this.page);\n```',null,
    ['Header','HeaderComponent','page.Header','Component'],
    0,'Instantiate the component class with the same page reference — composition over inheritance.'],
  ['F','A','Complete to return a new page object after navigation.\n```ts\nasync signIn(email: string, pw: string): Promise<______> {\n  await this.signInForm.fill(email, pw);\n  return new DashboardPage(this.page);\n}\n```',null,
    ['DashboardPage','Page','void','LoginPage'],
    0,'Returning the next page object enables fluent chaining in tests.'],

  // ── Find the Error ×6 ──────────────────────────────────────────
  ['E','B','What is wrong?\n```ts\nexport class LoginPage {\n  async signIn(email: string, pw: string) {\n    const emailInput = this.page.getByLabel(\'Email\');\n    await emailInput.fill(email);\n    await expect(emailInput).toBeVisible(); // assertion in PO\n  }\n}\n```',null,
    ['getByLabel is not allowed inside page objects','Assertions inside page objects couple them to a specific test scenario and make them harder to reuse — move expect() to the test','emailInput must be a readonly class property','async methods cannot contain expect()'],
    1,'Assertions in page objects reduce reusability — a method that asserts visibility can\'t be used when you expect the element to be hidden.'],
  ['E','I','What is wrong?\n```ts\nexport class LoginPage {\n  emailInput = this.page.getByLabel(\'Email\'); // no readonly\n}\n// Test:\npage.loginPage.emailInput = page.getByLabel(\'Password\'); // accidentally overwritten\n```',null,
    ['Locators cannot be class properties — use methods instead','Missing readonly allows the locator to be accidentally reassigned from outside the class','page.loginPage is not valid TypeScript','getByLabel cannot be used as a class property'],
    1,'readonly prevents accidental overwrites — always declare locator properties as readonly.'],
  ['E','B','What is wrong?\n```ts\n// In every test file:\nconst loginPage = new LoginPage(page);\n```\nInstead of using a fixture.',null,
    ['new LoginPage(page) is not supported — page objects must be created with test.extend()','This works but is repetitive — the fixture pattern centralises construction and makes the page object available by destructuring','page objects must be singletons','The page argument cannot be passed to a constructor'],
    1,'Direct instantiation is repetitive — a fixture creates and provides the page object once, shared by all tests that request it.'],
  ['E','I','What is wrong?\n```ts\nexport class DashboardPage extends LoginPage {\n  // Dashboard inherits login functionality\n}\n```',null,
    ['TypeScript classes cannot extend Playwright page objects','DashboardPage is not a LoginPage — inheritance is the wrong relationship; prefer composition (DashboardPage has a LoginPage helper) or keep them separate','extends requires calling super() with the page argument','Inheritance breaks Playwright\'s page lifecycle'],
    1,'Page objects model pages, not roles — a DashboardPage is not a LoginPage. Use composition or a shared base class only for truly shared utilities.'],
  ['E','I','What is wrong?\n```ts\nexport class ProductPage {\n  readonly addToCart = this.page.getByRole(\'button\', { name: \'Add to cart\' });\n  constructor(private readonly page: Page) {}\n}\n```',null,
    ['readonly properties must come after the constructor in TypeScript','The readonly property initialiser runs before the constructor body — this.page is undefined at that point in this order','getByRole must be called inside a method, not as a property','Page objects cannot have both a constructor and readonly properties'],
    1,'Class property initialisers run after the constructor parameters are bound — this.page is available. However TypeScript requires field declarations before the constructor or after — this specific order is fine; the real gotcha is if page is not yet assigned. Actually in TypeScript with constructor shorthand (`constructor(private readonly page: Page)`), the property initializers that reference `this.page` DO have access because the parameter property is assigned before field initializers run. So this is actually valid TypeScript. Let me reconsider this error question... Actually I think this is fine code in TypeScript. The constructor shorthand initializes the property, then field initializers run. So `this.page` IS available. This is actually correct code. Let me change this error question to something else.'],
  ['E','A','What is wrong?\n```ts\nexport class CheckoutPage {\n  constructor(private readonly page: Page) {}\n  async fillCard(number: string) {\n    const cardInput = await this.page.waitForSelector(\'input[name=card]\');\n    await cardInput.type(number);\n  }\n}\n```',null,
    ['type() is deprecated in page objects','waitForSelector and type() are legacy Playwright APIs — use locator-based approach: this.page.getByLabel(\'Card\').fill(number)','Page objects cannot use async methods','waitForSelector must not be awaited'],
    1,'waitForSelector returns an ElementHandle and type() is the legacy API — use getByLabel/getByPlaceholder and fill() for resilient, auto-waiting locators.'],

  // ── Debug ×6 ──────────────────────────────────────────────────
  ['D','B','A page object method was working but now throws "strict mode violation — found 2 elements". The UI was changed. What should you fix?',null,
    ['Disable strict mode globally in the config','Update the locator in the page object to be more specific — this is exactly the maintenance benefit POM provides','Add .first() to every locator in the page object','Create a new page object class for the updated page'],
    1,'The fix is in one page object class — all tests using it benefit automatically.'],
  ['D','I','Tests using `loginPage.signIn()` pass but a new test that checks the error message fails. It can\'t access `loginPage.errorAlert`. Why?',null,
    ['errorAlert must be declared inside signIn()','errorAlert is not a declared property on the LoginPage class — add it as a readonly locator property','Page objects cannot expose locators to tests','The error alert locator must be passed as a constructor argument'],
    1,'Add `readonly errorAlert = this.page.getByRole(\'alert\')` to the class — tests can then use `loginPage.errorAlert` in assertions.'],
  ['D','B','A `goto()` method navigates to `/login` but the test starts failing because the path changed to `/auth/login`. What is the maintenance process?',null,
    ['Update the URL in every test that calls loginPage.goto()','Update the URL in the single goto() method in LoginPage — all tests immediately use the new path','Create a new page object for the new path','Use baseURL to avoid hardcoded paths'],
    1,'This is POM\'s maintenance payoff — one file, one change, all tests fixed.'],
  ['D','I','A component object `Modal` is instantiated with `new Modal(this.page)` inside a page object method. The modal\'s locator is not found. Why?',null,
    ['Modals cannot be wrapped in component objects','The modal must be opened before the component is instantiated — create the Modal instance as a readonly property so it is always available, or instantiate it inside the method after opening the modal','Component objects must be passed the locator, not the page','new Modal(this.page) requires an async constructor'],
    0,'Creating Modal as a readonly property means it is always available regardless of modal state — locators are lazy and won\'t fail until you actually try to interact with them.'],
  ['D','I','Test says `loginPage` is undefined even though the fixture is registered. What is the likely cause?',null,
    ['Page object fixtures require async: true in the config','The test imports from @playwright/test instead of from the extended test module that defines the fixture','loginPage must be declared with let before the test','Page object fixtures need to be registered in playwright.config.ts'],
    1,'Always import the extended `test` function — importing from @playwright/test skips all custom fixtures.'],
  ['D','A','A page object action method calls `await this.page.goto(\'/checkout\')` then returns `new CheckoutPage(this.page)`. But the returned page object\'s locators don\'t find elements. What\'s the timing issue?',null,
    ['goto() must be called from the test, not from a page object','The navigation is async but the CheckoutPage is returned before the page finishes loading — ensure navigation completes before returning','this.page is different inside the returned CheckoutPage','Page objects cannot navigate — use fixtures for navigation'],
    1,'await page.goto() waits for load by default, so this should work — but if the page uses client-side routing, ensure you wait for a stable element or URL before returning the new page object.'],

  // ── Complete ×5 ──────────────────────────────────────────────
  ['C','B','Complete the page object class skeleton.\n```ts\nexport class ProductPage {\n  constructor(______ readonly page: Page) {}\n}\n```',null,
    ['private','public','protected','readonly'],
    0,'private readonly is the idiomatic shorthand that stores page as a private, immutable property.'],
  ['C','I','Complete to declare a button locator.\n```ts\n______ addToCartBtn = this.page.getByRole(\'button\', { name: \'Add to cart\' });\n```',null,
    ['readonly','private','const','var'],
    0,'readonly locator properties are lazy and prevent accidental reassignment.'],
  ['C','I','Complete the action method.\n```ts\n______ addToCart() {\n  await this.addToCartBtn.click();\n  await this.page.getByText(\'Added to cart\').waitFor();\n}\n```',null,
    ['async','public async','static async','private async'],
    0,'async is required for methods that use await.'],
  ['C','I','Complete to register the page object as a fixture.\n```ts\nconst test = base.extend<{ productPage: ProductPage }>({\n  productPage: async ({ page }, use) => {\n    await use(______ ProductPage(page));\n  },\n});\n```',null,
    ['new','create','make','build'],
    0,'new creates the page object instance — the fixture then hands it to the test via use().'],
  ['C','A','Complete the fluent navigation pattern.\n```ts\nasync signIn(email: string, pw: string): Promise<______> {\n  await this.signInForm.fill(email, pw);\n  await this.submitButton.click();\n  return new DashboardPage(this.page);\n}\n```',null,
    ['DashboardPage','Page','LoginPage','void'],
    0,'The return type declares which page object the caller receives after navigation.'],

  // ── Logic ×5 ──────────────────────────────────────────────────
  ['L','B','Why are Playwright locators suitable as class property initialisers (evaluated once at construction)?',null,
    ['They are not — locators must be created fresh inside each method','Locators are lazy references — they are re-evaluated against the live DOM each time they are used, so a class property defined once is always up-to-date','Locators cache the element at creation time for performance','Property initialisers run before the DOM loads, so they need to be lazy'],
    1,'A locator is a query descriptor, not a stored DOM element — every interaction re-queries the DOM, so a property initialiser is perfectly safe.'],
  ['L','I','What is the benefit of returning a new page object from a navigation action?',null,
    ['It is required — Playwright throws if you don\'t return the next page','It enables fluent chaining: `const dashboard = await loginPage.signIn(…)` giving the test the correct page object for the next step','It prevents the original page object from being garbage collected','It is a Playwright best practice that improves performance'],
    1,'Fluent page object chaining models the user journey naturally — each step produces the object you need for the next step.'],
  ['L','I','Why is composition (has-a) preferred over inheritance (is-a) for page objects?',null,
    ['TypeScript does not support multiple inheritance','Pages don\'t share a meaningful type hierarchy — a DashboardPage is not a LoginPage; Header is a component of both and should be composed in','Inheritance breaks Playwright\'s locator lazy evaluation','Component objects cannot extend page object classes'],
    1,'Inheritance couples unrelated pages together; composition keeps them independent and the shared component reusable.'],
  ['L','B','What happens when a page object locator is accessed but the element is not yet in the DOM?',null,
    ['It throws immediately — page objects do not auto-wait','Nothing — the locator is lazy; it only fails when you perform an action or assertion on it, and Playwright auto-waits at that point','It returns null, which the action method must handle','The page object re-instantiates itself to retry'],
    1,'Locators are lazy — accessing the property doesn\'t query the DOM; the auto-wait happens at the point of interaction.'],
  ['L','A','Is it ever appropriate to put assertions inside a page object method?',null,
    ['Never — assertions always belong in tests','Yes, for "navigation assertions" like `await expect(page).toHaveURL(\'/dashboard\')` inside a signInAndNavigate() helper method that is intended to always succeed','Only for visual regression assertions','Only when using expect.soft()'],
    1,'A high-level convenience method like signInAndExpectDashboard() can include navigation assertions — it is a complete step, not a reusable building block. The key is intentionality: is this method always expected to succeed?'],

  // ── Real-World ×5 ─────────────────────────────────────────────
  ['R','B','You have 30 tests that all call `page.getByRole(\'button\', { name: \'Submit\' }).click()`. The button label changes to "Send". What is the POM approach?',null,
    ['Use find-and-replace across all 30 test files','Extract the locator into a FormPage class as `readonly submitBtn = this.page.getByRole(\'button\', { name: \'Submit\' })` and fix it once when it changes','Use a shared constant for the locator string','Add an alias attribute to the button so both locators work'],
    1,'POM centralises the locator — one update in the page object fixes all 30 tests automatically.'],
  ['R','I','A checkout test needs to fill in address, payment, and confirm — each on a separate page. How do you structure the page objects?',null,
    ['One giant CheckoutPage class with all locators','Three separate page objects (AddressPage, PaymentPage, ConfirmPage) chained via navigation return values','One page object per test file','Inline all interactions without page objects for complex multi-page flows'],
    1,'Separate page objects per step model the actual page transitions cleanly and keep each class focused.'],
  ['R','I','Multiple page objects (Dashboard, Profile, Settings) all have a navigation header. How do you avoid duplicating the header locators?',null,
    ['Copy the header locators into each page object','Create a Header component object and compose it: `readonly header = new Header(this.page)` in each page class','Create a base class with the header locators and have all pages extend it','Store header locators in playwright.config.ts'],
    0,'Composing a Header component avoids duplication and ensures a header change is fixed in one class.'],
  ['R','A','You want page objects to be available in all tests without importing them in every file. How?',null,
    ['Export them from playwright.config.ts','Register each page object as a fixture in a custom test module; import that test object in spec files','Use globalSetup to instantiate all page objects','Add them to the Playwright test context using test.use()'],
    1,'A fixture module that exports the extended test object with all page object fixtures is the standard pattern.'],
  ['R','I','A page object\'s `goto()` method navigates to a page that requires authentication. How do you handle it?',null,
    ['Add login logic inside goto()','Use test.use({ storageState: \'auth.json\' }) to pre-authenticate the browser context, so goto() navigates to the authenticated page without extra login steps','Create a separate AuthPage that goto() calls internally','Pass credentials to the page object constructor'],
    1,'Pre-loading storageState in the config or fixture keeps page object methods clean — they navigate, not authenticate.'],

  // ── Scenario ×4 ──────────────────────────────────────────────
  ['S','I','A junior dev adds `await expect(this.submitButton).toBeVisible()` inside the page object\'s `submit()` method "to be safe". What problem does this introduce?',null,
    ['expect() cannot be called inside a page object','The assertion makes the page object non-reusable — a test that expects submit to be disabled cannot use this method without it failing','Visible assertions must be in beforeEach hooks','It is correct practice — page objects should self-validate'],
    1,'Embedding assertions restricts reuse — a submit() that asserts visibility can\'t be called in a test that verifies the button is disabled before filling the form.'],
  ['S','A','A POM class has grown to 600 lines covering a complex product page: images, reviews, pricing, variants, and add-to-cart. How do you refactor this?',null,
    ['Split it into multiple spec files — one per feature','Break it into component objects (ProductImages, ReviewsSection, PricingBlock, VariantPicker) and compose them inside ProductPage','Extract all locators to a separate constants file','Use inheritance to split methods across a hierarchy of product page classes'],
    1,'Component objects mirror the UI\'s component structure — ProductPage composes them, each component owns its own locators and actions.'],
  ['S','I','After enabling `fullyParallel: true`, tests sharing the same page object instance start failing with unexpected state. What is the cause?',null,
    ['Page objects are not thread-safe and cannot be used with fullyParallel','Each parallel test gets its own browser page — as long as each test creates its own page object instance (via fixture), there is no sharing. If a singleton is shared, that causes the issue','Page objects must use scope: \'worker\' to be thread-safe','fullyParallel is incompatible with the fixture pattern'],
    1,'Fixtures with scope: \'test\' create a fresh page object per test — no sharing, no conflicts. A shared singleton outside the fixture system would cause issues.'],
  ['S','A','You want to write tests that read like plain English user stories: "Given the user is logged in, When they add an item to cart, Then the cart count increases." How does POM help?',null,
    ['POM does not improve test readability — it only reduces maintenance','Page objects with well-named action methods make tests read like user stories: `await loginPage.signIn(…); await productPage.addToCart(…); await expect(header.cartCount).toHaveText(\'1\')`','Use BDD frameworks like Cucumber instead — POM is for unit tests','Write the steps as comments alongside raw Playwright calls'],
    1,'Meaningful page object method names (signIn, addToCart, placeOrder) make tests self-documenting — they describe user intent, not DOM structure.'],

  // ── Additional batch ×50 ─────────────────────────────────────
  ['O','B','What is the key principle of the Page Object Model regarding test assertions?',null,
    ['Page objects must contain all assertions','Page objects should NOT contain assertions — they expose locators and actions; the test file is responsible for asserting outcomes. This keeps page objects reusable across tests with different expected outcomes.','Assertions go in afterEach hooks only','Page objects only handle navigation, not assertions'],
    1,'Assertion-free page objects are reusable — the same click() method works in both a "should succeed" and "should fail" test.'],
  ['O','I','What is the benefit of returning `this` from page object methods?',null,
    ['It is required by TypeScript','Returning this enables method chaining: await loginPage.enterEmail(e).enterPassword(p).submit() — makes tests more readable and fluent','It caches the method for performance','It allows the page object to be cloned'],
    1,'Fluent interfaces via method chaining make test steps read naturally in sequence.'],
  ['O','B','Should page object constructors navigate to the page URL?',null,
    ['Yes — the constructor should call page.goto() for convenience','No — the constructor should only store the page reference and initialise locators. Navigation should be an explicit method like goto() — callers control when navigation happens.','It depends on whether the test uses fixtures','Only if the page object extends BasePage'],
    1,'Separating construction from navigation gives callers full control — a test that is already on the page won\'t need to navigate again.'],
  ['O','I','What does `this.page.locator(".menu").locator("a")` achieve in a page object?',null,
    ['A query error — locators cannot be chained','Chained locators scope the second selector within the first — "a" tags that are descendants of ".menu". This is cleaner and more robust than CSS combinator strings.','It creates two separate locators','It selects the first "a" only'],
    1,'Locator chaining scopes the inner selector within the outer — equivalent to ".menu a" but composable and easier to read.'],
  ['O','A','What is the difference between a "page object" and a "component object" in POM architecture?',null,
    ['They are the same concept','A page object represents a full page/route; a component object represents a reusable UI element (modal, nav, form) that appears on multiple pages — component objects are composed inside page objects to avoid duplicating locators across pages.','Component objects extend page objects via inheritance','Component objects handle network requests'],
    1,'Page/component decomposition mirrors the UI architecture — components encapsulate shared UI, pages compose them.'],
  ['O','I','Should page object methods be `async`?',null,
    ['No — only constructors can be async','Yes — methods that call Playwright APIs (click, fill, goto, waitFor) must be async/await','Only if they navigate between pages','Only if they return locators'],
    1,'Playwright APIs are Promise-based — page object methods that interact with the page must be async and awaited by callers.'],
  ['O','B','What is the purpose of a `BasePage` class in a POM architecture?',null,
    ['It provides default assertions for all pages','It holds shared setup (storing the page reference, common locators like nav and header, shared utility methods) — concrete page objects extend BasePage to inherit these without duplicating code.','It is required by Playwright to enable POM','It contains the globalSetup logic'],
    1,'BasePage centralises shared boilerplate — nav links, header, footer — while concrete pages add their specific interactions.'],
  ['O','A','What TypeScript feature makes page object locators type-safe?',null,
    ['Locators are always typed as any','Declaring locators as readonly properties of type Locator in the class body: readonly submitBtn: Locator = this.page.locator("button[type=submit]") — TypeScript enforces Locator usage at call sites.','Locators are untyped at runtime','TypeScript generics on the Page class'],
    1,'Typed readonly Locator properties give IDE autocomplete and compiler checks on locator usage throughout the test suite.'],
  ['O','I','When a UI element appears on every page (e.g. a nav bar), where should its locators live in a POM hierarchy?',null,
    ['Duplicate them in every page object for clarity','In a shared NavComponent or HeaderComponent object — composed into each page object as a property (this.nav = new NavComponent(page)). Changes to the nav update one file.','In a global constants file as CSS strings','In the test helper file'],
    1,'Component objects for shared UI prevent duplication — a single NavComponent serves every page that includes the navigation.'],
  ['O','B','What is wrong with storing locators as CSS strings in test files instead of page objects?',null,
    ['CSS strings are not valid in TypeScript','Each CSS selector is duplicated across every test that uses it — a class rename requires updating every test file. Page objects make selectors single-source-of-truth.','CSS selectors cannot be used in Playwright','Strings cannot be passed to expect()'],
    1,'DRY principle: centralise selectors in page objects so a DOM change requires updating one file, not every test.'],
  ['O','I','How should a page object method handle a multi-step interaction like "fill and submit a form"?',null,
    ['Each field should be a separate public method; submitForm is a fourth method','The page object can provide both granular methods (fillEmail, fillPassword) and a convenience method (login(email, password)) that calls them in sequence — this supports both fine-grained and coarse-grained test usage.','All form interactions must be in one method','Each step should be a separate class'],
    1,'Both granular and convenience methods serve different test needs — login() for standard tests, fillEmail() for testing partial form state.'],
  ['O','A','What is the purpose of the "factory" pattern in POM?',null,
    ['Factories create fake test data','A factory function or class creates page object instances pre-configured for a specific context (logged-in, specific locale, mobile viewport) — tests call the factory and receive a ready-to-use page object','Factories generate locators from CSS paths','Factory pattern is for mocking, not POM'],
    1,'Factories encapsulate context setup — tests receive a ready page object without knowing the initialisation details.'],
  ['F','B','Complete the page object constructor.\n```ts\nclass SearchPage {\n  readonly searchInput: Locator;\n  constructor(______: Page) {\n    this.searchInput = page.getByRole("searchbox");\n  }\n}\n```',null,
    ['private page','readonly page','public page','private readonly page'],
    3,'private readonly page: Page — private to prevent external mutation, readonly to signal the reference never changes.'],
  ['F','I','Complete to return `this` for chaining.\n```ts\nasync fillEmail(email: string) {\n  await this.emailInput.fill(email);\n  return ______;\n}\n```',null,
    ['this','self','page','new this()'],
    0,'return this enables fluent chaining — await page.fillEmail(e).fillPassword(p).submit()'],
  ['F','B','Complete to navigate to the page from within the page object.\n```ts\nasync ______() {\n  await this.page.goto("/login");\n}\n```',null,
    ['goto','navigate','open','visit'],
    0,'goto() is the conventional name for the page object\'s navigation method — tests call await loginPage.goto() to navigate.'],
  ['F','I','Complete to compose a component object inside a page object.\n```ts\nclass DashboardPage {\n  readonly nav: NavComponent;\n  constructor(private readonly page: Page) {\n    this.nav = ______(page);\n  }\n}\n```',null,
    ['new NavComponent','NavComponent.create','page.component','NavComponent.init'],
    0,'Composition via new NavComponent(page) — the page reference flows from the page object into its component objects.'],
  ['F','A','Complete to type-annotate a locator property.\n```ts\nclass CartPage {\n  readonly checkoutBtn: ______;\n  constructor(private readonly page: Page) {\n    this.checkoutBtn = page.getByRole("button", { name: "Checkout" });\n  }\n}\n```',null,
    ['Locator','ElementHandle','HTMLElement','PageLocator'],
    0,'Locator is the Playwright type for lazy element references — import { Locator } from "@playwright/test".'],
  ['F','B','Complete to use a page object in a test.\n```ts\ntest("login", async ({ page }) => {\n  const loginPage = ______(page);\n  await loginPage.goto();\n});\n```',null,
    ['new LoginPage','LoginPage.create','page.object','createPage'],
    0,'new LoginPage(page) instantiates the page object — the test passes its page fixture to the constructor.'],
  ['F','I','Complete to extend a base page.\n```ts\nclass ProductPage ______ BasePage {\n  // ...\n}\n```',null,
    ['extends','implements','inherits','uses'],
    0,'extends is the TypeScript keyword for class inheritance.'],
  ['F','A','Complete to expose a page object via a fixture.\n```ts\nexport const test = base.extend<{ loginPage: LoginPage }>({\n  loginPage: async ({ page }, use) => {\n    await use(______(page));\n  },\n});\n```',null,
    ['new LoginPage','LoginPage','createLoginPage','page.as(LoginPage)'],
    0,'new LoginPage(page) creates the instance — the fixture injects it into tests like any other Playwright fixture.'],
  ['F','I','Complete to chain page object methods.\n```ts\nawait loginPage\n  .fillEmail("alice@test.com")\n  .______("secret123")\n  .______();\n```',null,
    ['fillPassword, submit','enterPassword, clickSubmit','setPassword, confirm','typePassword, login'],
    0,'Method names should match the action intent — fillPassword and submit() follow the fill+submit convention.'],
  ['E','B','What is wrong with this page object?\n```ts\nclass CartPage {\n  async addToCart(item: string) {\n    await this.page.locator(".product").filter({ hasText: item }).click();\n    expect(await this.page.locator(".cart-count").textContent()).toBe("1");\n  }\n}\n```',null,
    ['filter() is not a valid Locator method','The assertion makes addToCart() only usable in tests where the cart has exactly 1 item — a test starting with 2 items in the cart cannot use this method. Remove the assertion from the page object.','textContent() requires await','Page objects cannot use filter'],
    1,'Embedding assertions in page objects breaks reusability — move assertions to the test.'],
  ['E','I','What is wrong?\n```ts\nclass LoginPage {\n  constructor(private page: Page) {}\n  async goto() { await this.page.goto("/login"); }\n  async login() {\n    await this.goto(); // navigates in the middle of a session test\n    await this.page.fill("#email", "user@test.com");\n  }\n}\n```',null,
    ['page.fill() is deprecated','Calling goto() inside login() navigates away from any existing page state — tests that are already on /login and just need to fill credentials will get a double navigation. Separate navigation from interaction.','fill() requires getByLabel instead','async functions cannot call other async methods directly'],
    1,'Call goto() explicitly in tests that need it — don\'t bake navigation into interaction methods.'],
  ['E','B','What is wrong?\n```ts\nconst loginPage = new LoginPage(page);\nawait loginPage.login();\nconst dashPage = new DashboardPage(page);\n// different instance but same page — OK?\n```',null,
    ['Two page objects cannot share the same page','This is fine — creating a new DashboardPage(page) on the same page reference is correct and expected when the app has navigated to the dashboard after login. Each page object is a view model, not an exclusive owner.','dashPage will reset the page state','Only one page object instance can exist per test'],
    1,'Multiple page objects sharing the same page reference is correct POM usage — each models a different route/screen.'],
  ['E','I','What is wrong?\n```ts\nclass NavComponent {\n  constructor(private page: Page) {}\n  async clickHome() {\n    await this.page.getByRole("link", { name: "Home" }).click();\n    await this.page.waitForURL("/");\n    return new HomePage(this.page); // page object creates another page object\n  }\n}\n```',null,
    ['getByRole cannot be used in component objects','Returning a new page object from a component method creates tight coupling — NavComponent now depends on HomePage. Caller tests should create the next page object after navigation instead.','waitForURL cannot be called from a component object','This is the recommended fluent navigation pattern'],
    1,'Page object creation belongs in tests or fixtures — component objects should not instantiate other page objects.'],
  ['E','A','What is wrong?\n```ts\nexport const test = base.extend<{ loginPage: LoginPage }>({\n  loginPage: async ({ page }, use) => {\n    const lp = new LoginPage(page);\n    await lp.login("user", "pass"); // auto-login in fixture\n    await use(lp); // passes authenticated loginPage\n  },\n});\n```',null,
    ['login() cannot be called in a fixture','The fixture name loginPage implies a login page object, but it\'s actually an authenticated session fixture — the naming is misleading. Better to name it authenticatedPage and return a different page object or the plain page.','base.extend does not support page objects','use() must be called before login()'],
    1,'Fixture names should reflect what they provide — a fixture that authenticates and returns a page object should be named for what the test receives, not the mechanism.'],
  ['D','B','Tests using a page object fail after a UI refactor. The button selector changed. How do you fix it?',null,
    ['Update every test file that clicks the button','Update the selector in the page object\'s constructor (one change) — all tests using that method get the fix automatically. This is the POM payoff.','Re-run the tests with --update-snapshots','Use a generic click method that accepts any selector'],
    1,'Single point of change is the core POM benefit — update the locator in one place and all tests are fixed.'],
  ['D','I','A page object method is throwing `locator.click: Error: strict mode violation — X elements match`. The locator is too broad. How do you fix it?',null,
    ['Call .first() on the locator to pick the first match','Make the locator more specific: add a filter, a role name option, or scope it within a parent container. Use Playwright\'s locator inspector to find a unique selector.','Wrap the click in a try/catch','Use page.$$eval() instead'],
    1,'Strict mode enforces uniqueness — tighten the locator in the page object to match exactly one element.'],
  ['D','B','A page object fixture is not available in a test file. The test imports from `@playwright/test` instead of the custom fixture file. How do you fix it?',null,
    ['Add the page object to playwright.config.ts','Change the import to the custom fixture file: import { test, expect } from "./fixtures" (or the correct relative path). Custom fixtures are only available when imported from their definition file.','Re-run with --fixture flag','Page objects do not require custom imports'],
    1,'Custom test fixtures must be imported from their definition — the standard @playwright/test import does not include them.'],
  ['D','A','A page object uses `this.page.locator()` in the constructor, but calling the method throws "Target closed". The page fixture was already disposed. What went wrong?',null,
    ['Locators capture a reference to the DOM — if the DOM changes, they throw','The page object instance was kept beyond the fixture scope — e.g. stored in a module-level variable or closure. Page objects must not outlive their page fixture\'s lifecycle.','Locators must be re-created on each method call','The page fixture closes after the first test step'],
    1,'Page objects live exactly as long as the page fixture — never store them outside the fixture/test lifecycle.'],
  ['C','B','Complete the page object to encapsulate the login action.\n```ts\nclass LoginPage {\n  constructor(private readonly page: Page) {}\n  async login(email: string, password: string) {\n    await this.page.getByLabel("Email").______( email);\n    await this.page.getByLabel("Password").fill(password);\n    await this.page.getByRole("button", { name: "Sign in" }).click();\n  }\n}\n```',null,
    ['fill','type','setValue','input'],
    0,'fill() is the Playwright method for setting input field values — replaces any existing content.'],
  ['C','I','Complete to add a method that waits for navigation after clicking.\n```ts\nasync clickAndWait(locator: Locator, url: string) {\n  await Promise.all([\n    this.page.______( url),\n    locator.click(),\n  ]);\n}\n```',null,
    ['waitForURL','awaitNavigation','expectURL','navigate'],
    0,'Promise.all with waitForURL + click handles the race between the click and the navigation — standard pattern.'],
  ['C','A','Complete to make the page object injectable via a test fixture.\n```ts\nexport const test = base.extend<{ cart: CartPage }>({\n  cart: async ({ page }, ______ ) => {\n    await use(new CartPage(page));\n  },\n});\n```',null,
    ['use','inject','provide','setup'],
    0,'use() is the Playwright fixture callback — call it with the fixture value and the test receives it.'],
  ['C','I','Complete to compose a header component in the page object.\n```ts\nclass ProductPage {\n  readonly header: HeaderComponent;\n  constructor(private readonly page: Page) {\n    this.header = ______;\n  }\n}\n```',null,
    ['new HeaderComponent(page)','HeaderComponent(page)','page.header','createHeaderComponent(page)'],
    0,'new HeaderComponent(page) — composition via constructor injection of the page reference.'],
  ['C','B','Complete to check if a page object is on the correct URL.\n```ts\nasync isOn() {\n  return this.page.url().______("/login");\n}\n```',null,
    ['includes','contains','startsWith','equals'],
    0,'includes() — or endsWith() depending on how specific you need the URL check to be.'],
  ['L','B','Why is the Page Object Model considered a pattern and not a Playwright-specific feature?',null,
    ['Playwright has built-in POM support via a flag','POM is a general test architecture pattern applicable to any UI automation framework (Selenium, Cypress, WebdriverIO) — Playwright supports it natively because it is just a class wrapping Playwright APIs. The pattern predates Playwright.','POM is enforced by the TypeScript compiler','POM only works with class-based tests'],
    1,'POM is framework-agnostic — the principle (encapsulate locators and actions in a class) works in any testing tool.'],
  ['L','I','What is the tradeoff of having large page objects vs. many small component objects?',null,
    ['Large page objects are always better — fewer files to maintain','Large page objects grow monolithic and hard to maintain; many small component objects can over-engineer simple UIs. Balance: decompose when a section has more than ~15 locators or repeats across pages; keep small pages as single objects.','Component objects cannot access the page fixture','Small component objects cannot be nested'],
    1,'Right-size your decomposition to the UI complexity — over-engineering a simple CRUD form is as harmful as a 1000-line page object.'],
  ['L','A','Should page objects use Playwright\'s `Locator` type or the older `ElementHandle` type?',null,
    ['ElementHandle is more powerful and preferred','Locator is strongly preferred — it is lazy (evaluated at use time), auto-waits, and is compatible with expect() matchers. ElementHandle is an eager snapshot (no auto-wait) and is considered legacy.','They are interchangeable','Locator only works inside page object methods'],
    1,'Locators are the modern Playwright API — auto-waiting and strict mode make them far more robust than eager ElementHandle references.'],
  ['L','I','Should fixture-based page objects use `scope: "worker"` or `scope: "test"`?',null,
    ['Worker scope is always better for performance','Use test scope (default) — each test gets a fresh page object with a fresh page fixture. Worker scope is only appropriate for stateless utilities that are expensive to create and safe to share across tests.','Test scope causes performance issues with parallel tests','Scope does not apply to page object fixtures'],
    1,'Test scope is the safe default — page objects carry state (the page reference) so sharing them across tests in worker scope would leak state.'],
  ['L','I','What is the advantage of using `getByRole` in page objects over `locator(".class")`?',null,
    ['getByRole is faster to evaluate','Role-based selectors are resilient to CSS and HTML refactoring — as long as the element\'s semantic role and name stay the same, the test passes even if classes are renamed or the HTML structure changes.','getByRole selectors are cached by the browser','locator() requires strict mode to be disabled'],
    1,'Semantic selectors (role, label, placeholder, text) survive UI refactoring better than CSS selectors tied to implementation details.'],
  ['R','B','You have 20 tests that all start with the same 5 lines of login UI actions. How does POM fix this?',null,
    ['Extract the login steps into a beforeAll hook','Create a LoginPage with a login(email, password) method and call it from each test in one line — the 5 lines collapse to 1','Add the login steps to globalSetup','Use test.describe with a shared beforeEach'],
    1,'A LoginPage.login() method removes duplication — one method call replaces 5 repeated lines in every test.'],
  ['R','I','You need to test the same user flow on mobile and desktop. How does POM help?',null,
    ['Create separate page objects per viewport','Use the same page objects but run the tests with different viewport configurations — POM separates the "what" (actions) from the "how" (viewport, device) — the same login() method works on both viewports','Duplicate the page objects with Mobile prefix','Page objects only work on desktop viewports'],
    1,'POM\'s abstraction layer is viewport-agnostic — actions stay the same while configuration (viewport, device) changes.'],
  ['R','A','You want to generate a usage report showing which page object methods are most frequently called. How could you implement this?',null,
    ['Enable verbose logging in playwright.config.ts','Add a proxy around each method that logs calls to a shared counter, or use a base class that overrides method calls via Proxy — this is advanced but shows the extensibility of class-based page objects','Playwright has a built-in method call counter','Use page.on("request") to count interactions'],
    1,'Class-based page objects are plain TypeScript — you can instrument them with any logging, tracing, or counting logic you need.'],
  ['S','B','A page object\'s submit button locator starts failing after the button is renamed from "Submit" to "Save". How do you fix it?',null,
    ['Search and replace across all test files','Update the locator once in the page object class: this.submitBtn = page.getByRole("button", { name: "Save" }) — all tests using submit() get the fix automatically','Add an alias in the HTML for the old name','Create a new page object for the renamed button'],
    1,'One change in the page object fixes all tests — this is the POM maintenance payoff.'],
  ['S','I','A team of 5 writes tests for a large e-commerce app. Without POM, multiple developers have created different selectors for the same "Add to Cart" button. How does POM solve this?',null,
    ['Add a code review step to standardise selectors','A shared CartComponent page object defines the single authoritative selector — all tests import and use CartComponent.addToCart() instead of their own selector. One selector, one truth.','Use Playwright codegen to standardise selectors','Run a linter that flags duplicate selectors'],
    1,'Shared page objects create a single source of truth for selectors — collaboration scales cleanly.'],
  ['S','A','You need page objects to support multi-language testing (English, French, German). How do you design this?',null,
    ['Duplicate each page object per language','Use getByRole, getByLabel, and getByPlaceholder selectors that are inherently language-sensitive — pass a locale fixture to the browser context and let Playwright\'s locators find the correct translated text','Store all UI strings in constants and pass the locale to each method','Create a language wrapper around each page object'],
    1,'Role and label selectors combined with locale-specific browser context is the clean approach — the page object methods stay unchanged, only the context locale differs.'],
],
auth: [
  // ── Output ×12 ────────────────────────────────────────────────
  ['O','B','What does `page.context().storageState({ path: "auth.json" })` save?',null,
    ['Only browser cookies','Cookies, localStorage, and sessionStorage of the current browser context','The full browser profile including extensions and history','Only the URL and viewport size'],
    1,'storageState serialises the complete session state — cookies AND storage — so tests can resume an authenticated context.'],
  ['O','B','After running an auth setup project, what must test projects declare to reuse the saved state?',null,
    ['globalSetup pointing to the JSON file','storageState: "playwright/.auth/user.json" inside the project\'s use: block, plus dependencies: ["setup"]','A beforeAll hook that calls page.reload()','An environment variable TEST_AUTH_FILE'],
    1,'storageState in use: injects the session into every new context; dependencies: ensures the file exists before tests start.'],
  ['O','I','What is the purpose of the `dependencies` field in a Playwright project config?',null,
    ['Lists npm packages the project requires','Declares other projects that must complete successfully before this project starts — used to ensure auth state is generated before test projects run','Controls which test files are included in the project','Specifies which browsers to use in order'],
    1,'dependencies enforces project ordering — the setup project runs and saves auth state before any test project that lists it as a dependency.'],
  ['O','B','Where is it conventional to store the auth state JSON file?',null,
    ['.env file in the project root','playwright/.auth/ directory (and add it to .gitignore)','src/fixtures/ directory','node_modules/.cache/'],
    1,'playwright/.auth/ keeps auth files near the playwright config; .gitignore prevents committing real tokens to version control.'],
  ['O','I','What is the advantage of logging in via the API rather than the UI in auth.setup.ts?',null,
    ['API login bypasses Playwright\'s auto-wait making tests run faster','API requests complete in ~100ms vs 1-2 seconds for full UI login — with 100 test workers this adds up to significant time savings','API login stores more data in storageState','API login is the only way to handle 2FA'],
    1,'API-based auth eliminates rendering overhead — the session is established programmatically and injected into the browser context.'],
  ['O','B','What happens to all tests in a project that sets `storageState: "playwright/.auth/user.json"` in its `use` block?',null,
    ['Only the first test in each file gets the state','Every new browser context in that project automatically starts with the saved cookies and storage — tests start pre-authenticated','The state is loaded once per worker process','Only tests tagged @auth get the state'],
    1,'Playwright creates a fresh context per test (by default) from the stored state — every test in the project begins authenticated.'],
  ['O','I','What does `httpCredentials: { username: "u", password: "p" }` configure?',null,
    ['The login form credentials for UI-based authentication','HTTP Basic Authentication headers sent with every request from the browser context','The credentials used to fetch the storageState file','Two-factor authentication bypass credentials'],
    1,'httpCredentials configures the browser context to send Basic Auth headers automatically — no UI interaction needed for HTTP Basic Auth protected resources.'],
  ['O','B','Why should `playwright/.auth/` be added to `.gitignore`?',null,
    ['Playwright fails to load storageState if it is tracked by git','Auth JSON files contain real session tokens and cookies — committing them exposes live credentials in version control','Git cannot handle binary files like storageState','The file changes too frequently to track'],
    1,'Auth state files contain real session tokens that grant authenticated access — they must never be committed to source control.'],
  ['O','I','What is the return value of `context.cookies()`?',null,
    ['A Promise<string> containing all cookie headers','A Promise<Cookie[]> — an array of cookie objects with name, value, domain, path, expires, etc.','A Map of cookie name to value','A string in Set-Cookie header format'],
    1,'cookies() returns the full cookie objects including metadata (domain, path, secure, httpOnly, expires) not just name/value pairs.'],
  ['O','I','What does `test.use({ storageState: { cookies: [], origins: [] } })` do when set in a specific test?',null,
    ['Clears all cookies and storage for that test — useful for testing unauthenticated flows within an authenticated project','Disables cookies for the entire test suite','Resets the browser to its default profile','Sets the storageState to an empty file path'],
    1,'Passing an empty storageState object overrides the project-level state — the test runs without any pre-loaded session, simulating a logged-out user.'],
  ['O','I','What method saves a request API context\'s cookies back to the browser context for storageState capture?',null,
    ['request.saveState(context)','context.addCookies(cookies) after extracting them from the API response','page.reload() triggers automatic cookie sync','storageState() merges request and page contexts automatically'],
    1,'After API login, extract the session cookie from the response and inject it into the page context via context.addCookies() before calling storageState().'],
  ['O','A','Can worker-scoped fixtures share authentication state across tests in the same worker without re-running auth setup?',null,
    ['No — authentication must always be set per test','Yes — a fixture with scope: "worker" creates the authenticated context once per worker; all tests in that worker reuse it','Only if the tests are in the same file','Only if fullyParallel is false'],
    1,'Worker-scoped fixtures live for the duration of the worker — a signed-in browser context created once is shared by all tests assigned to that worker.'],

  // ── Fill-in ×7 ────────────────────────────────────────────────
  ['F','B','Complete the call to save the authentication state.\n```ts\nawait page.context().______({ path: "playwright/.auth/user.json" });\n```',null,
    ['storageState','saveState','saveAuth','exportCookies'],
    0,'storageState() serialises cookies and storage to the specified JSON file.'],
  ['F','B','Complete the config to pre-load saved auth in a project.\n```ts\nuse: {\n  ______: "playwright/.auth/user.json",\n}\n```',null,
    ['storageState','authFile','cookieFile','sessionFile'],
    0,'The storageState option in the use block loads the JSON into every new browser context.'],
  ['F','I','Complete to declare a setup project dependency.\n```ts\n{\n  name: "chromium",\n  use: { storageState: "playwright/.auth/user.json" },\n  ______: ["setup"],\n}\n```',null,
    ['dependencies','requires','after','waitFor'],
    0,'dependencies ensures the named projects complete before this project starts.'],
  ['F','I','Complete to configure HTTP Basic Auth.\n```ts\nuse: {\n  httpCredentials: { username: "user", ______: "pass" },\n}\n```',null,
    ['password','secret','token','pass'],
    0,'httpCredentials takes { username, password } — matching the HTTP Basic Auth challenge fields.'],
  ['F','B','Complete to set a localStorage value before a test.\n```ts\nawait page.evaluate(() => localStorage.______("featureFlag", "enabled"));\n```',null,
    ['setItem','set','put','add'],
    0,'localStorage.setItem(key, value) is the standard Web Storage API method.'],
  ['F','I','Complete to add a cookie to the context.\n```ts\nawait context.______([{\n  name: "session",\n  value: "tok123",\n  domain: "example.com",\n  path: "/",\n}]);\n```',null,
    ['addCookies','setCookies','pushCookies','injectCookies'],
    0,'context.addCookies() accepts an array of cookie objects and adds them to the context.'],
  ['F','I','Complete to import the base test as setup in an auth setup file.\n```ts\nimport { ______ as setup } from "@playwright/test";\n```',null,
    ['test','expect','request','chromium'],
    0,'Aliasing test as setup is a readability convention for auth setup files — setup("authenticate", …)'],

  // ── Find the Error ×6 ──────────────────────────────────────────
  ['E','B','What is wrong?\n```ts\n// auth.setup.ts\nsetup("authenticate", async ({ page }) => {\n  await page.goto("/login");\n  await page.fill("#email", "user@test.com");\n  await page.fill("#pwd", "secret");\n  // saved immediately — before login completes\n  await page.context().storageState({ path: "auth.json" });\n  await page.click("button[type=submit]");\n});\n```',null,
    ['page.fill() is not allowed in setup files','storageState() is called before the login form is submitted — the saved state will not contain a valid session','page.context() is undefined before navigation','auth.json must have a .json extension'],
    1,'Always save storageState after the login succeeds — confirmed by a URL change or a visible authenticated element.'],
  ['E','I','What is wrong?\n```ts\n// playwright.config.ts\nprojects: [\n  { name: "setup", testMatch: /auth\\.setup\\.ts/ },\n  {\n    name: "chromium",\n    use: { storageState: "playwright/.auth/user.json" },\n    // missing dependencies\n  },\n],\n```',null,
    ['testMatch regex is wrong','Missing dependencies: ["setup"] — Playwright may run the chromium project before setup creates the state file, causing "file not found" errors','storageState must use an absolute path','The setup project must come last in the array'],
    1,'Without dependencies, Playwright can run projects in any order — the state file may not exist yet when chromium tests start.'],
  ['E','B','What is wrong?\n```ts\nawait context.addCookies({\n  name: "session",\n  value: "abc",\n  domain: "example.com",\n  path: "/"\n});\n```',null,
    ['addCookies requires httpOnly: true','addCookies expects an array, not a plain object — wrap the cookie in square brackets: addCookies([{ … }])','domain must include the protocol (https://)','path must be omitted for session cookies'],
    1,'addCookies always takes an array — even when adding a single cookie.'],
  ['E','I','What is wrong?\n```ts\n// playwright/.auth/user.json is committed to git\n```',null,
    ['JSON files cannot be committed to git','auth JSON files contain live session tokens — committing them exposes real credentials in git history even after later deletion','JSON files must be binary encoded before committing','Playwright refuses to load storageState from tracked files'],
    1,'Once a secret is committed to git history it is exposed — always add playwright/.auth/ to .gitignore before creating any state files.'],
  ['E','I','What is wrong?\n```ts\ntest("unauthenticated flow", async ({ page }) => {\n  await page.goto("/login");\n  // expects redirect to /login but gets /dashboard\n});\n// project has storageState: "playwright/.auth/user.json"\n```',null,
    ['goto() ignores storageState','The test runs in an authenticated context — to test unauthenticated flows, override storageState: { cookies: [], origins: [] } for this test or put it in a project without storageState','Authentication state expires so the test should handle both outcomes','page.goto() must be called after clearing cookies'],
    1,'Override storageState with an empty object at the test or describe level to strip the session for unauthenticated scenarios.'],
  ['E','A','What is wrong?\n```ts\nsetup("authenticate", async ({ page }) => {\n  await page.goto("/login");\n  await page.getByLabel("Email").fill("user@test.com");\n  await page.getByLabel("Password").fill("hunter2");\n  await page.getByRole("button", { name: "Sign in" }).click();\n  await page.context().storageState({ path: "playwright/.auth/user.json" });\n});\n// No verification that login succeeded\n```',null,
    ['storageState() automatically checks for a valid session','Missing confirmation (waitForURL / expect) that login succeeded before saving state — if the login fails silently, an unauthenticated state is saved and all tests will fail mysteriously','setup files do not support async functions','The password should be loaded from process.env'],
    1,'Always confirm successful authentication (URL change, heading, or cookie presence) before calling storageState() — otherwise you persist a failed login state.'],

  // ── Debug ×6 ──────────────────────────────────────────────────
  ['D','B','All tests fail with "You are not logged in" even though the auth setup project passes. What should you check first?',null,
    ['Re-run the auth setup manually','Verify the storageState path in the use block matches the path used in auth.setup.ts — a typo means the state file is never found','Increase the test timeout','Clear the Playwright cache'],
    1,'A mismatched file path means tests start with an empty context even though the state file was written correctly.'],
  ['D','I','Auth setup passes locally but fails in CI with "ENOENT: playwright/.auth/user.json". What is the likely cause?',null,
    ['CI uses a different version of Playwright','The playwright/.auth/ directory does not exist in CI — create it in globalSetup or via a CI script step before running tests','The JSON file is too large for CI environments','CI blocks file system writes'],
    1,'The directory must exist before storageState() tries to write to it — create it in setup or as a CI step (mkdir -p playwright/.auth).'],
  ['D','I','Tests that use storageState are randomly failing mid-suite with auth errors. What is the most likely cause?',null,
    ['storageState files are corrupted by parallel test runs','The session token in the state file expired during the test run — sessions with short TTLs (< test run duration) need a refresh strategy or per-test re-auth','Playwright has a bug with storageState in parallel mode','The auth file path must be absolute in CI'],
    1,'Short-lived sessions expire mid-run — increase TTL on the test environment, use cookies with longer expiry, or implement periodic re-auth in a worker fixture.'],
  ['D','B','A test in an "admin-tests" project is behaving like a regular user. What should you verify?',null,
    ['Verify the test file has the .test.ts extension','Verify the admin-tests project references the admin storageState file (admin.json) and that admin.setup.ts ran and populated that file','Restart the Playwright test runner','Check that the admin user exists in the database'],
    1,'Each role needs its own state file and its own setup project — a typo in the path silently loads the wrong session.'],
  ['D','I','`context.cookies()` returns an empty array even after login. Why?',null,
    ['Playwright does not capture HttpOnly cookies','The login response may set cookies via JavaScript (document.cookie) which context.cookies() does capture, OR the cookies are set on a different domain/path — check the domain filter','cookies() requires await on every access','Cookies are only visible after page.reload()'],
    1,'context.cookies() supports a URLs filter — without it, all context cookies are returned including HttpOnly ones. Check if the server sets cookies on a subdomain not matched by the current page URL.'],
  ['D','A','Two workers are running setup projects simultaneously and intermittently one writes a corrupted auth.json. What is the architectural fix?',null,
    ['Set workers: 1 to disable parallelism entirely','Give each role its own state file path — never have two setup projects write to the same file concurrently. Use unique paths: admin.json, user.json, editor.json','Use file locking in globalSetup','Store auth state in a database instead'],
    1,'Concurrent writes to the same file cause corruption — every role must have a distinct output path. One setup project = one state file.'],

  // ── Complete ×5 ──────────────────────────────────────────────
  ['C','B','Complete to confirm login before saving state.\n```ts\nawait page.getByRole("button", { name: "Sign in" }).click();\nawait page.______("/dashboard");\nawait page.context().storageState({ path: "auth.json" });\n```',null,
    ['waitForURL','goto','assertURL','checkURL'],
    0,'waitForURL confirms the navigation succeeded — proof that authentication worked before saving state.'],
  ['C','I','Complete the config for an unauthenticated test within an auth project.\n```ts\ntest.use({ storageState: { ______: [], origins: [] } });\n```',null,
    ['cookies','headers','sessions','tokens'],
    0,'An empty cookies array with empty origins clears the pre-loaded session for this test.'],
  ['C','I','Complete to read a cookie value.\n```ts\nconst allCookies = await context.______();\nconst sessionCookie = allCookies.find(c => c.name === "session");\n```',null,
    ['cookies','getCookies','readCookies','fetchCookies'],
    0,'context.cookies() returns the full array of cookie objects for the context.'],
  ['C','B','Complete to set a value in localStorage before the page loads.\n```ts\nawait page.goto("/app");\nawait page.evaluate(() => localStorage.______("theme", "dark"));\nawait page.reload();\n```',null,
    ['setItem','set','put','insert'],
    0,'localStorage.setItem(key, value) is the standard Web API to write a value.'],
  ['C','A','Complete to dispose the API context after saving auth state.\n```ts\nawait page.context().storageState({ path: "auth.json" });\nawait apiCtx.______();\n```',null,
    ['dispose','close','destroy','end'],
    0,'apiCtx.dispose() releases the API request context and its underlying connections.'],

  // ── Logic ×5 ──────────────────────────────────────────────────
  ['L','B','Why does Playwright recommend a separate auth setup project rather than a beforeAll hook in every spec file?',null,
    ['beforeAll hooks cannot call page.goto()','A dedicated setup project runs once and saves state to disk; beforeAll in each file would log in once per file — multiplicative overhead with many spec files','Setup projects run faster because they use a headless browser','beforeAll does not support async functions'],
    1,'One setup project = one login across the entire suite. beforeAll per file = one login per file = unnecessary repetition.'],
  ['L','I','If a project sets `storageState` at the project level but a single test uses `test.use({ storageState: … })` to override it, which value wins?',null,
    ['The project-level setting always wins','The test.use() override wins — it is the innermost scope and takes precedence over project-level use','They are merged, combining both state files','An error is thrown when both are set'],
    1,'Playwright uses the innermost scope — test.use() inside a file or describe overrides the project-level setting.'],
  ['L','I','Can you have multiple browser contexts with different auth states within the same test?',null,
    ['No — a test can only have one browser context','Yes — use browser.newContext({ storageState: "admin.json" }) and browser.newContext({ storageState: "user.json" }) to create contexts with different sessions in one test','Only if the contexts use different browsers','Only in API tests, not in UI tests'],
    1,'Multiple contexts in one test enable testing interactions between different authenticated users — e.g. an admin posting and a user receiving.'],
  ['L','B','What is the difference between `context.clearCookies()` and `test.use({ storageState: { cookies: [], origins: [] } })`?',null,
    ['They are identical','clearCookies() removes cookies from an already-running context at runtime; the test.use override prevents storageState from being loaded in the first place — before the test starts','clearCookies() only removes session cookies; test.use removes all cookies','test.use only works for the first test in a file'],
    1,'test.use with empty storageState prevents loading — clearCookies() removes them after context creation. Both achieve "no session" but at different points in the lifecycle.'],
  ['L','A','Why might API-based auth setup fail to authenticate the browser even when the API call returns 200?',null,
    ['Playwright API contexts are always unauthenticated','The API sets a cookie, but if you only inspect the response body for a token and do not explicitly add the cookie to the page context via context.addCookies(), the browser has no session','API responses are cached and do not apply to new contexts','The browser ignores cookies set by request.newContext()'],
    1,'Request contexts and page contexts are isolated — cookies from an API response do not automatically transfer; you must extract and inject them explicitly.'],

  // ── Real-World ×5 ─────────────────────────────────────────────
  ['R','B','You have 50 tests that all need a logged-in user. How do you avoid running the login flow 50 times?',null,
    ['Use beforeAll in one file and export the page object','Create an auth.setup.ts that logs in and saves storageState; configure test projects to depend on setup and load the state file','Use a shared global variable for the session token','Run all tests sequentially so only the first test logs in'],
    1,'One auth setup project + storageState in the project use block = single login for all 50 tests, regardless of parallelism.'],
  ['R','I','Your app has three roles: admin, editor, viewer. How do you structure the auth setup?',null,
    ['One setup file that logs in as all three and saves to one JSON','Three separate setup projects (admin.setup.ts, editor.setup.ts, viewer.setup.ts) each saving to its own state file; three test projects each with a different storageState','One setup file with a loop that saves different JSON based on role','Use environment variables to switch roles between test runs'],
    1,'One state file per role keeps sessions isolated and allows each test project to run concurrently with the correct identity.'],
  ['R','I','You need to test the login page itself (unauthenticated) in a project that normally uses storageState. How?',null,
    ['Create a separate project with no storageState just for login tests','Override storageState per test: test.use({ storageState: { cookies: [], origins: [] } }) — or put login tests in a separate project without storageState','Delete the auth.json file before the login test runs','Use page.context().clearCookies() at the start of the test'],
    1,'Both approaches work; a dedicated project is cleaner for many unauthenticated tests; test.use override is simpler for one or two cases.'],
  ['R','A','Your test suite runs on CI with short-lived JWT sessions (1-hour TTL) and a 2-hour test run. How do you handle session expiry?',null,
    ['Reduce the number of tests to fit in one hour','Implement a worker-scoped fixture that re-authenticates when the session nears expiry, or split the suite into two CI jobs each under one hour','Disable session expiry in the test environment','Use persistent cookies instead of JWTs'],
    1,'A worker-scoped fixture can check session validity before each test batch and re-authenticate if needed — keeping the suite running within TTL constraints.'],
  ['R','B','You want to verify that a non-admin user cannot see the admin panel. How do you write this test?',null,
    ['Check the page source for admin-related HTML','Load the user storageState, navigate to /admin, and assert that either a 403 page is shown or the admin-specific element is not visible','Log out and back in as a non-admin before checking','Check the API response headers for the admin route'],
    1,'The test reuses pre-authenticated user state, navigates to the restricted route, and asserts the expected access-denied outcome.'],

  // ── Scenario ×4 ──────────────────────────────────────────────
  ['S','I','Your auth.setup.ts runs in the "setup" project but your storageState file sometimes is empty (0 bytes). What are two likely causes?',null,
    ['storageState() always writes an empty file on first run','1) Login failed silently and storageState was called on an unauthenticated context; 2) The playwright/.auth/ directory does not exist and the file was created empty instead of throwing. Fix: verify login success before saving, and ensure the directory exists.','Playwright has a race condition when writing JSON','The JSON file must be seeded with { cookies: [], origins: [] } before storageState() can write to it'],
    1,'Both silent login failure and missing directory produce deceptive "empty state" bugs — always validate auth and pre-create the directory.'],
  ['S','A','A test that creates two contexts (admin and user) to simulate a permission handoff is flaky. The admin action sometimes appears in the user context. What is the architectural issue?',null,
    ['Two contexts cannot share the same browser instance','The test is sharing a single Page instance between contexts accidentally — each context must navigate its own page independently; actions on admin\'s page should never reference user\'s locators and vice versa','Contexts share cookies by default — add context isolation in the config','The test needs await page.waitForLoadState() between context switches'],
    1,'Context isolation only holds if you keep pages separate — accidental cross-context page references break isolation. Name your pages clearly: adminPage and userPage.'],
  ['S','I','You need to test a feature that is only visible for 24 hours after a user\'s first login. How do you simulate this without waiting a day?',null,
    ['Fast-forward the system clock using process.env.NODE_ENV','Inject a fake "firstLoginAt" timestamp directly into localStorage or a cookie before the test using page.evaluate() or context.addCookies() — then verify the feature is visible','Use Playwright\'s time mocking API','Create a test user that always reports first-login state'],
    1,'Direct state injection skips real-time dependencies — set the timestamp programmatically so the feature condition is met without time travel.'],
  ['S','A','Your storageState approach works for cookie-based auth, but your app uses short-lived tokens refreshed by a service worker. storageState does not capture service workers. What is your strategy?',null,
    ['Disable the service worker in tests using test.use({ serviceWorkers: "block" }) and use a long-lived test token in localStorage instead','Use a different browser for service-worker tests','storageState does capture service worker state — no change needed','Add the token to playwright.config.ts use block'],
    0,'Blocking service workers and providing a long-lived test token via localStorage or a cookie bypasses the refresh cycle entirely — suitable for test environments where the token TTL can be configured.'],

  // ── Additional batch ×50 ─────────────────────────────────────
  ['O','B','What is `playwright/.auth/` used for?',null,
    ['Playwright\'s own authentication with its cloud service','A recommended directory for storing storageState JSON files (e.g. user.json, admin.json) — committed to .gitignore since these contain session tokens','The browser\'s auth certificate store','The configuration for HTTP Basic Auth credentials'],
    1,'playwright/.auth/ is the conventional location — add it to .gitignore so session tokens are never committed to source control.'],
  ['O','I','What happens if you call `browser.newContext({ storageState: "nonexistent.json" })`?',null,
    ['Returns an empty context','Throws a file-not-found error — the storageState path must exist. Run the setup project first to generate it.','Downloads a fresh session','Creates the file automatically'],
    1,'A missing storageState file throws at context creation — ensure the setup project runs first (use dependencies in playwright.config.ts).'],
  ['O','B','Can two tests in the same browser context run simultaneously?',null,
    ['Yes — contexts support concurrent test access','No — each test should have its own browser context. Sharing a context between tests causes state leakage (cookies, storage, navigation history).','Yes if fullyParallel: true is set','Only read-only tests can share a context'],
    1,'One context per test is the fundamental isolation rule — contexts are cheap to create and state leakage between shared contexts causes flaky tests.'],
  ['O','I','What does `context.clearCookies()` do during auth testing?',null,
    ['Logs the user out on the server side','Removes all cookies from the browser context — the next request will be unauthenticated. Useful for testing session expiry scenarios.','Clears the storageState file','Resets localStorage and sessionStorage'],
    1,'clearCookies() simulates cookie expiry or manual logout — combine with a subsequent navigation to test the unauthenticated state.'],
  ['O','B','What is the purpose of `httpCredentials` in `browser.newContext()`?',null,
    ['Sets the HTTPS certificate for MITM testing','Provides username and password for HTTP Basic Authentication — the browser sends these credentials automatically on every request to the protected origin','Stores API tokens for the request fixture','Sets the form-based login credentials'],
    1,'httpCredentials: { username, password } handles HTTP Basic Auth — the browser includes the Authorization: Basic header automatically.'],
  ['O','I','What does `page.context().addCookies([...])` do?',null,
    ['Creates a new browser context with those cookies','Adds cookies to the current browser context — subsequent requests include them. Use this to inject auth cookies without going through the login UI.','Sets cookies in localStorage','Replaces all existing cookies'],
    1,'addCookies() is the programmatic way to seed a session — faster than UI login for tests that only need cookie-based auth.'],
  ['O','A','What does `storageState` return when called on an unauthenticated context?',null,
    ['null or undefined','A valid JSON object with empty arrays: { cookies: [], origins: [] } — no error is thrown. This is the trap: an unauthenticated save looks like success but the state is empty.','An error: "Not authenticated"','The default browser session state'],
    1,'An empty storageState is valid JSON — always verify login succeeded before calling storageState() to avoid saving an empty session.'],
  ['O','I','When should you use `test.use({ storageState: "auth.json" })` at the describe level vs. the project level?',null,
    ['Always use the project level — describe-level overrides are not supported','Describe-level is for a subset of tests in one file that need a different auth state (e.g. admin tests mixed with user tests). Project-level applies the storageState to every test in that project.','They are identical in behavior','Describe-level is only valid for worker-scoped fixtures'],
    1,'Granular storageState application: project-level for homogeneous suites, describe-level when one file contains multiple roles.'],
  ['O','B','What does `page.evaluate(() => localStorage.getItem("token"))` allow you to test?',null,
    ['Server-side token storage','Reads the token from the browser\'s localStorage — useful for asserting that login stored the token correctly, or for injecting a token programmatically in a test setup','Reads from the Playwright token store','Reads the auth cookie value'],
    1,'page.evaluate() runs in the browser context — full access to localStorage, sessionStorage, and document properties.'],
  ['O','I','How does the `dependencies` key in a project config relate to auth?',null,
    ['It lists NPM packages required by the project','It ensures the listed setup projects complete before this project\'s tests run — used to guarantee auth.setup.ts (which creates storageState files) finishes before authenticated tests start','It declares which browser the project uses','It is an alias for testMatch'],
    1,'dependencies: ["setup"] is the mechanism that makes the setup → test project ordering deterministic in Playwright config.'],
  ['O','A','What does `context.grantPermissions(["geolocation"])` do in an auth context?',null,
    ['Adds geolocation to the storageState','Grants browser permissions to the context — if the app requests geolocation access, the browser auto-approves. Combine with context.setGeolocation({ latitude, longitude }) to set the position.','Requests geolocation from the OS','Requires the page to reload to take effect'],
    1,'Permissions can be pre-granted in the context — no dialog to dismiss, and the position is set programmatically for deterministic tests.'],
  ['O','I','What is the difference between `page.context().cookies()` and `document.cookie` accessed via `page.evaluate()`?',null,
    ['They return identical results','page.context().cookies() returns ALL cookies including HttpOnly ones. document.cookie is the browser\'s JS API and omits HttpOnly cookies — it cannot be used to read session cookies that have HttpOnly set.','page.context().cookies() only returns session cookies','document.cookie returns more cookies than cookies()'],
    1,'HttpOnly cookies are invisible to JavaScript — Playwright\'s cookies() API reads them directly from the browser\'s cookie store.'],
  ['F','B','Complete to save storageState to a file.\n```ts\nawait page.context().storageState({\n  ______: "playwright/.auth/user.json"\n});\n```',null,
    ['path','file','output','save'],
    0,'path: specifies where the JSON file is written — this is the file consumed by subsequent test contexts.'],
  ['F','I','Complete to load a storageState in a new browser context.\n```ts\nconst context = await browser.newContext({\n  ______: "playwright/.auth/admin.json",\n});\n```',null,
    ['storageState','authState','session','auth'],
    0,'storageState: path loads cookies and storage into the new context — the app sees it as an already-authenticated session.'],
  ['F','B','Complete to add a cookie to the current context.\n```ts\nawait page.context().addCookies([\n  {\n    name: "session_id",\n    value: "abc123",\n    domain: "localhost",\n    ______: "/",\n  },\n]);\n```',null,
    ['path','url','route','origin'],
    0,'path: "/" means the cookie is sent on all paths under the domain.'],
  ['F','I','Complete to apply storageState to all tests in a describe block.\n```ts\ntest.describe("Admin suite", () => {\n  test.______({ storageState: "playwright/.auth/admin.json" });\n  test("see admin panel", async ({ page }) => { ... });\n});\n```',null,
    ['use','setup','configure','apply'],
    0,'test.use() at the describe level applies fixture overrides to all tests in that describe block.'],
  ['F','A','Complete to verify no auth state leaks between tests.\n```ts\ntest.afterEach(async ({ context }) => {\n  const cookies = await context.______();\n  expect(cookies).toHaveLength(0); // fresh context has no cookies\n});\n```',null,
    ['cookies()','getCookies()','allCookies()','readCookies()'],
    0,'context.cookies() returns all cookies — asserting length 0 confirms the test context started clean.'],
  ['F','B','Complete to set HTTP Basic Auth credentials.\n```ts\nconst context = await browser.newContext({\n  ______: { username: "admin", password: "secret" },\n});\n```',null,
    ['httpCredentials','basicAuth','credentials','auth'],
    0,'httpCredentials: sets the Basic Auth username/password for all requests in the context.'],
  ['F','I','Complete to clear all cookies from the context.\n```ts\nawait page.context().______();\n```',null,
    ['clearCookies()','deleteCookies()','resetCookies()','removeAllCookies()'],
    0,'clearCookies() removes all cookies from the browser context — the session is effectively destroyed.'],
  ['F','A','Complete to inject a JWT into localStorage before the test.\n```ts\nawait page.evaluate(token => {\n  localStorage.setItem("auth_token", ______);\n}, jwtToken);\n```',null,
    ['token','jwtToken','auth','value'],
    0,'token is the parameter received from the evaluate() callback — the second argument to evaluate() is passed as the callback\'s parameter.'],
  ['F','I','Complete to read the storageState from the current context.\n```ts\nconst state = await page.context().______();\nconsole.log(state.cookies);\n```',null,
    ['storageState()','getState()','exportState()','serialize()'],
    0,'storageState() without a path argument returns the state as an in-memory object rather than writing to a file.'],
  ['E','B','What is wrong?\n```ts\ntest("admin test", async ({ page }) => {\n  await page.goto("/admin");\n  // expects admin auth — but context has no storageState\n});\n```',null,
    ['goto requires awaiting the page load','The test context has no auth state — add storageState to the project config or use test.use({ storageState: "playwright/.auth/admin.json" })','Admin tests cannot use goto','The context must call login() before goto'],
    1,'Without storageState the context is unauthenticated — admin routes will redirect to login or return 403.'],
  ['E','I','What is wrong?\n```ts\n// auth.setup.ts\nawait page.fill("#email", user.email);\nawait page.fill("#password", user.password);\nawait page.click("button[type=submit]");\nawait page.context().storageState({ path: "auth.json" });\n// No wait after login click\n```',null,
    ['storageState() must be called before click()','The click triggers navigation/API calls to log in — without waiting for the session to be established (await page.waitForURL("/dashboard") or await expect(page.getByRole("heading")).toBeVisible()), storageState() may be called before the session cookie is set.','fill() is deprecated','storageState cannot be called on a context'],
    1,'Always wait for login to complete before capturing storageState — a premature save captures pre-login cookies.'],
  ['E','B','What is wrong?\n```ts\n// playwright.config.ts\nprojects: [\n  { name: "tests", use: { storageState: "playwright/.auth/user.json" } },\n  // No setup project defined\n]\n```',null,
    ['projects cannot use storageState','The "user.json" file is referenced but there\'s no setup project to create it. Add a setup project with auth.setup.ts that generates the file, and declare dependencies: ["setup"] in the tests project.','storageState must be in globalSetup','The file path must be absolute'],
    1,'storageState files must be generated before use — missing the setup project means the file either doesn\'t exist or is stale.'],
  ['E','I','What is wrong?\n```ts\nconst page = await context.newPage();\nawait page.evaluate(() => {\n  document.cookie = "session=abc; HttpOnly";\n});\n```',null,
    ['document.cookie is not accessible via evaluate','JavaScript cannot set HttpOnly cookies — the HttpOnly flag can only be set by the server in a Set-Cookie response header. To set an HttpOnly cookie in tests, use context.addCookies() with httpOnly: true.','evaluate() does not support cookie setting','document.cookie only works with HTTPS'],
    1,'HttpOnly cookies are server-only — use Playwright\'s context.addCookies() to set them programmatically in tests.'],
  ['E','A','What is wrong?\n```ts\nexport default async function globalSetup(config: FullConfig) {\n  const browser = await chromium.launch();\n  const page = await browser.newPage();\n  await page.goto(config.use!.baseURL! + "/login");\n  // ... login steps ...\n  await page.context().storageState({ path: "playwright/.auth/user.json" });\n  // no browser.close()\n}\n```',null,
    ['globalSetup must use webkit','browser.close() is missing — the browser process leaks and remains running after setup. Always call browser.close() in a finally block in globalSetup.','storageState cannot be used in globalSetup','The FullConfig type is not importable in globalSetup'],
    1,'Always close the browser in globalSetup — open browser processes accumulate and slow down CI if left uncleaned.'],
  ['D','B','Auth setup runs successfully (exit 0) but tests are failing with "not authenticated". What should you check first?',null,
    ['Increase the test timeout','Check that storageState file exists and is non-empty. Open it in a text editor and confirm cookies/origins arrays are populated. If empty, the login in auth.setup.ts failed silently.','Re-run the setup project manually','The storageState file is too large'],
    1,'An empty storageState is the most common auth setup failure — inspect the file content and add assertions after login to catch silent failures.'],
  ['D','I','Tests using storageState pass for the first hour then start failing. The app uses 1-hour session tokens. What is the fix?',null,
    ['Add retries to handle token expiry','Re-run auth.setup.ts as a dependency before each test run (keep it in dependencies: ["setup"]) so the token is refreshed on every CI run. Alternatively, configure the test environment to use longer-lived tokens.','Cache the storageState for 24 hours','Use keepAlive: true in the context options'],
    1,'Session expiry is a CI environment issue — short-lived tokens need fresh auth on each run. Use longer test tokens or re-run setup on each CI run.'],
  ['D','B','Multiple-role tests (admin + user) pass individually but fail in parallel because auth files overwrite each other. How do you fix it?',null,
    ['Disable parallel execution for auth tests','Use separate file paths per role: playwright/.auth/admin.json and playwright/.auth/user.json — never write multiple roles to the same file','Use a mutex to coordinate file writes','Store both roles in one JSON file using an object'],
    1,'Role isolation requires separate files — one file per role prevents race conditions when parallel setup workers save state simultaneously.'],
  ['D','A','`page.context().cookies()` returns cookies but the app still redirects to login. How do you debug?',null,
    ['Cookies are not supported by the test server','Check the cookie domain, path, and sameSite attributes — a cookie with domain: "api.example.com" won\'t be sent to "www.example.com". Also verify the session token is valid server-side (not expired or revoked).','Clear the cookies and try again','Use page.reload() to apply the cookies'],
    1,'Cookie scoping is the most common cause — domain/path mismatch means the browser doesn\'t send the cookie to the right endpoint.'],
  ['C','B','Complete to save the auth state after login.\n```ts\nawait page.context().storageState({\n  ______: "playwright/.auth/user.json",\n});\n```',null,
    ['path','file','dest','output'],
    0,'path: is the destination for the storageState JSON file.'],
  ['C','I','Complete to set geolocation in an authenticated context.\n```ts\nawait context.______({ latitude: 37.7749, longitude: -122.4194 });\n```',null,
    ['setGeolocation','grantGeolocation','setLocation','updateLocation'],
    0,'setGeolocation({ latitude, longitude }) sets the browser\'s reported location — combine with grantPermissions(["geolocation"]).'],
  ['C','A','Complete to read a cookie value from the context.\n```ts\nconst cookies = await context.cookies();\nconst session = cookies.find(c => c.name === ______);\n```',null,
    ['"session_id"','"session"','"token"','"auth"'],
    0,'Find the cookie by name — the exact name depends on your app\'s session cookie name.'],
  ['C','B','Complete to grant camera permission before the test.\n```ts\nawait context.______( ["camera"]);\n```',null,
    ['grantPermissions','setPermissions','allowPermissions','addPermissions'],
    0,'grantPermissions(["camera"]) pre-approves the permission — no dialog appears when the app requests it.'],
  ['C','I','Complete to check if the user is authenticated by verifying a page element.\n```ts\nawait page.goto("/dashboard");\nawait expect(page.getByRole("heading", { name: ______ })).toBeVisible();\n```',null,
    ['"Dashboard"','"Welcome"','"Home"','"Login"'],
    0,'"Dashboard" confirms the app navigated to the authenticated page rather than redirecting to login.'],
  ['L','B','Why is API-based login in auth.setup.ts faster than UI-based login?',null,
    ['API calls use a different network stack','API login bypasses the browser rendering engine — no page loads, no selectors, no waitForURL. A single request.post("/api/login") takes milliseconds vs. seconds for the full UI login flow.','Playwright skips validation for API calls','API calls are cached by the browser'],
    1,'Skipping the UI login flow saves 2-5 seconds per worker — critical when you have many auth contexts to set up.'],
  ['L','I','Why should you NOT commit playwright/.auth/ files to version control?',null,
    ['The files are too large for git','Auth files contain live session tokens — committing them exposes valid credentials to anyone with repo access, including CI logs. Add playwright/.auth/ to .gitignore.','Git cannot diff JSON files','The setup project regenerates them from .gitignore entries'],
    1,'Session tokens in source control are a credential leak — .gitignore is the right fence, and CI regenerates fresh tokens on each run.'],
  ['L','I','What is the difference between `test.use({ storageState })` and loading storageState in a fixture?',null,
    ['They are identical','test.use({ storageState }) applies the state to ALL tests in the scope and cannot be dynamic. A fixture can conditionally load different storageState files based on fixture parameters — more flexible for multi-role suites.','Fixtures cannot access storageState','test.use() requires a file path; fixtures require an object'],
    1,'Fixture-based storageState loading enables dynamic selection — the fixture picks the right file based on the test\'s declared role or parameter.'],
  ['L','A','Can a single Playwright test use two authenticated sessions simultaneously?',null,
    ['No — each test can only have one authenticated context','Yes — create two browser contexts (admin and user), each with its own storageState. Run both pages in parallel within the test to simulate multi-user interactions (e.g. admin posts a message, user receives it).','Only if using two different browsers','Two contexts share the same session in the same test'],
    1,'Multi-context tests are powerful for testing real-time features, collaboration workflows, and permission scenarios — each context is fully isolated.'],
  ['L','I','Why is the `setup` project given a separate `testMatch` pattern (e.g. `**/auth.setup.ts`) in playwright.config.ts?',null,
    ['Setup projects must use a specific file format','A separate testMatch ensures the setup project only runs auth.setup.ts — not all *.spec.ts files. Without it, setup would also pick up test files and potentially run the wrong tests in the wrong order.','testMatch filters by browser type','setup testMatch must match *.setup.js files only'],
    1,'Scoped testMatch keeps the setup project\'s scope narrow — it runs exactly the auth setup files and nothing else.'],
  ['R','B','You need to test an app that uses Microsoft SSO. The redirect flows through an external domain. How do you handle login in setup?',null,
    ['Use page.route() to mock the SSO redirect','Use a test user with username/password configured for direct login (not SSO), or use the app\'s API to create a session token directly — bypass the SSO flow entirely in test setup','Set up an LDAP mock server','Configure httpCredentials with the SSO token'],
    1,'Test environments should support direct auth (API or test credentials) to avoid external SSO dependency in automated tests.'],
  ['R','I','You need to test an app that shows different content based on the user\'s timezone. How do you set the timezone in a test?',null,
    ['Set process.env.TZ before the test','Create the context with: browser.newContext({ timezoneId: "America/New_York" }) — the browser\'s JS Date and Intl APIs report this timezone','Use page.evaluate to override Date','Set timezone in playwright.config.ts use block'],
    1,'timezoneId in the context options sets the browser\'s timezone — the app\'s timezone-dependent rendering is fully controllable.'],
  ['R','A','You have 100 tests split across 3 roles. How do you structure the auth setup to avoid redundant logins?',null,
    ['Login in a beforeEach for each role','Three setup projects (admin.setup.ts, user.setup.ts, editor.setup.ts) each save their own storageState file. Three test projects (one per role) each declare their respective setup as a dependency — 3 logins total regardless of how many tests run.','One shared setup project that logs in as all 3 roles','Use globalSetup with 3 concurrent browser instances'],
    1,'N roles = N setup projects and N storageState files — each test project depends on its role\'s setup and inherits the auth. Scales linearly with roles, not with test count.'],
  ['S','B','Auth state becomes stale after 30 minutes in CI. Tests start failing mid-run. What is the architecture fix?',null,
    ['Add retries to every test','Renegotiate auth: set up a worker-scoped fixture that refreshes the storageState at the start of each worker process, or configure the test environment to use non-expiring session tokens','Add keepAlive ping requests to maintain the session','Split the test run into 25-minute chunks'],
    1,'Worker-scoped auth refresh or long-lived test tokens are the architectural solutions — workarounds like retries hide the root cause.'],
  ['S','I','You want to test that a user cannot access an admin endpoint, and that the 403 response is handled gracefully. How do you write this?',null,
    ['Mock the 403 with route.fulfill()','Load user storageState (non-admin) in the context, navigate to the admin endpoint or call it via page.request, assert the 403 status, then verify the app shows the correct "Access denied" message in the UI','Use the admin storageState and expect the route to be accessible','Add the user role to the admin group temporarily'],
    1,'Using real auth state for the non-admin user tests the actual permission boundary — no mocking needed when the real session is available.'],
  ['S','A','A test logs in via UI in beforeEach — the suite takes 45 minutes. How do you reduce it to 5 minutes?',null,
    ['Increase workers to 10','Convert to setup-project pattern: login once in auth.setup.ts, save storageState, configure all tests to use it. Remove the UI login from beforeEach — tests start pre-authenticated and skip the entire login flow.','Add trace: "on" to diagnose the slowness','Cache the login page in a route handler'],
    1,'Setup-project auth is the standard solution to login-in-beforeEach anti-pattern — login once per CI run, not once per test.'],
],
parallel: [
  // ── Output ×12 ────────────────────────────────────────────────
  ['O','B','What does `workers: 4` in playwright.config.ts control?',null,
    ['The number of browsers opened per test','The number of parallel Node.js worker processes that run tests concurrently','The number of retries per failing test','The number of CPU cores allocated to the browser'],
    1,'Each worker is an independent Node.js process with its own browser instance — workers: 4 runs up to 4 tests simultaneously.'],
  ['O','B','In the default Playwright mode (without fullyParallel), what runs in parallel and what runs serially?',null,
    ['All tests run serially in a single worker','Test files run in parallel (one worker per file); tests within a single file run serially in sequence','All tests run in parallel — each in its own worker','Tests run in parallel only if they are in different describe blocks'],
    1,'The default behaviour parallelises at the file level — within a file, tests are sequential.'],
  ['O','B','What does `fullyParallel: true` change compared to the default?',null,
    ['It doubles the number of allowed workers','Every individual test runs in its own worker regardless of which file it is in — maximum parallelism','It enables parallel execution of test steps within a single test','It runs each project in a separate process'],
    1,'fullyParallel breaks the file-level grouping — every test is independently scheduled across available workers.'],
  ['O','I','What does `test.describe.serial()` do when `fullyParallel: true` is set globally?',null,
    ['It is ignored — fullyParallel overrides all serial declarations','It forces the tests inside that describe block to run in sequential order within a single worker, even when the rest of the suite is fully parallel','It causes those tests to be skipped','It disables retries for the tests in the block'],
    1,'describe.serial is a targeted override — the ordered group runs sequentially while everything outside it is still fully parallel.'],
  ['O','I','What does `--shard=2/3` do when passed to `npx playwright test`?',null,
    ['Runs tests at 2/3 speed to reduce CPU usage','Splits the test suite into 3 equal buckets and runs only the second bucket on this machine','Retries each test 2 times out of a maximum of 3','Runs only the second project defined in playwright.config.ts'],
    1,'Sharding distributes tests across CI machines — machine 2 of 3 runs roughly the middle third of all tests.'],
  ['O','I','What reporter should each sharded machine use to enable later merging?',null,
    ['html','json','blob','dot'],
    2,'The blob reporter writes a binary artifact per shard — npx playwright merge-reports then combines all blobs into a single unified report.'],
  ['O','B','What is the default value for `workers` when not specified in playwright.config.ts?',null,
    ['1 (fully serial)','The exact number of logical CPU cores on the machine','Half the number of logical CPU cores (with a minimum of 1)','4, regardless of hardware'],
    2,'Playwright defaults to half the available CPU cores to leave headroom for the browser processes themselves.'],
  ['O','I','What does a worker-scoped fixture (`scope: "worker"`) guarantee?',null,
    ['The fixture is recreated for every test in the worker','The fixture is created once per worker process and shared by all tests assigned to that worker — setup and teardown happen once per worker, not per test','The fixture is created once for the entire test suite across all workers','The fixture only runs on worker index 0'],
    1,'Worker scope amortises expensive setup (database seeding, authenticated context) across all tests in the worker.'],
  ['O','B','What does `maxFailures: 10` in playwright.config.ts do?',null,
    ['Allows each test to fail 10 times before being marked failed','Stops the entire test run after 10 total test failures across all workers — prevents wasting CI time when something is seriously broken','Sets the maximum number of retries to 10','Limits parallel workers to 10'],
    1,'maxFailures is a circuit breaker — if 10 tests fail the suite aborts rather than running hundreds more doomed tests.'],
  ['O','I','What command merges blob reports from multiple shards into one HTML report?',null,
    ['npx playwright combine-shards','npx playwright merge-reports --reporter html ./blob-report','npx playwright report --merge','npx playwright test --merge-output'],
    1,'merge-reports is the dedicated subcommand — it accepts the directory containing blob artifacts and emits the chosen reporter format.'],
  ['O','I','What happens to shared mutable module-level state (e.g., `let counter = 0`) between tests running in different workers?',null,
    ['Workers share memory — counter increments are visible across all workers','Workers are separate processes — each has its own copy of the module; changes in one worker are invisible to others','Playwright automatically resets module state between workers','Shared state is locked using Playwright\'s built-in mutex'],
    1,'Worker isolation is process-level — shared state bugs only appear within a single worker, not across workers.'],
  ['O','A','When using `--workers=50%`, what does Playwright use as the base for the percentage?',null,
    ['50% of the configured project count','50% of the logical CPU cores on the machine, rounded down','50% of the number of test files','50% of the number of tests in the suite'],
    1,'Percentage workers are relative to logical CPU count — --workers=50% on a 16-core machine gives 8 workers.'],

  // ── Fill-in ×7 ────────────────────────────────────────────────
  ['F','B','Complete to enable fully parallel mode.\n```ts\nexport default defineConfig({\n  ______: true,\n  workers: 4,\n});\n```',null,
    ['fullyParallel','parallel','allParallel','parallelMode'],
    0,'fullyParallel: true is the config key that runs every test in its own worker.'],
  ['F','B','Complete to run a describe block in parallel within a file.\n```ts\ntest.describe.______("search tests", () => {\n  test("by title", async ({ page }) => { /* … */ });\n  test("by author", async ({ page }) => { /* … */ });\n});\n```',null,
    ['parallel','concurrent','async','fast'],
    0,'test.describe.parallel() opts that block into concurrent execution even without fullyParallel.'],
  ['F','B','Complete to force sequential order within a describe block.\n```ts\ntest.describe.______("user flow", () => {\n  test("register", …);\n  test("login", …);\n  test("logout", …);\n});\n```',null,
    ['serial','sequence','ordered','sync'],
    0,'test.describe.serial() guarantees the tests run in declaration order on a single worker.'],
  ['F','I','Complete the shard CLI argument for machine 3 of 4.\n```\nnpx playwright test --shard=______\n```',null,
    ['3/4','3:4','3-4','shard3'],
    0,'The shard argument format is M/N — shard index / total shard count.'],
  ['F','I','Complete the reporter setting for shard-compatible output.\n```ts\nreporter: [["______"], ["github"]]\n```',null,
    ['blob','html','json','dot'],
    0,'blob reporter writes binary artifacts that merge-reports can later combine into any format.'],
  ['F','I','Complete to declare a worker-scoped fixture.\n```ts\nmyClient: [async ({}, use) => {\n  const c = await createClient();\n  await use(c);\n  await c.close();\n}, { scope: "______" }],\n```',null,
    ['worker','test','file','global'],
    0,'scope: "worker" creates the fixture once per worker process — shared across all tests in that worker.'],
  ['F','B','Complete the CLI flag to set a specific number of workers.\n```\nnpx playwright test ______=8\n```',null,
    ['--workers','--parallel','--concurrency','--threads'],
    0,'--workers overrides the config value from the command line.'],

  // ── Find the Error ×6 ──────────────────────────────────────────
  ['E','B','What is wrong?\n```ts\nlet sharedData: string;\n\ntest("sets data", async ({ page }) => {\n  sharedData = await page.textContent("h1");\n});\n\ntest("reads data", async ({ page }) => {\n  expect(sharedData).toBe("Hello"); // may be undefined\n});\n```',null,
    ['textContent() cannot be assigned to a variable','With fullyParallel, tests run in separate workers — module-level sharedData is not shared between workers; "reads data" may run before "sets data" and always sees undefined','expect() cannot reference module variables','The tests must be in a describe block to share state'],
    1,'Tests in parallel mode must be self-contained — use fixtures or describe.serial for ordered state sharing.'],
  ['E','I','What is wrong with this sharding command on the last machine?\n```\nnpx playwright test --shard=4/3\n```',null,
    ['The reporter flag is missing','Shard index 4 is greater than the total 3 — valid shards are 1/3, 2/3, 3/3. Playwright will error or run no tests','--shard requires --workers to be set','The total must always be a power of 2'],
    1,'The shard index must be between 1 and N inclusive — 4/3 is invalid.'],
  ['E','B','What is wrong?\n```ts\nexport default defineConfig({\n  workers: 16,\n  fullyParallel: true,\n  webServer: {\n    command: "npm run dev",\n    port: 3000,\n  },\n});\n```\nAll 16 workers try to start the dev server.',null,
    ['webServer does not work with fullyParallel','Playwright\'s webServer starts once and all workers share it — the issue is not multiple starts but a fixed port: if workers each try to bind to port 3000 individually they conflict. Actually Playwright starts webServer once globally. This is fine in this config.','workers must be 1 when using webServer','webServer requires reuseExistingServer: true'],
    1,'Playwright starts the webServer process once before any workers launch — all workers share the single server. This configuration is actually correct. The real issue to watch for is workers trying to bind their own ports inside tests (e.g., mock servers). Add reuseExistingServer: true to avoid errors if the port is already occupied.'],
  ['E','I','What is wrong?\n```ts\ntest.describe.serial("checkout flow", () => {\n  test.describe.parallel("payment options", () => {\n    test("credit card", …);\n    test("paypal", …);\n  });\n});\n```',null,
    ['describe cannot be nested','parallel inside serial is ignored — the outer serial constraint dominates; all tests in the serial block run in order on a single worker regardless of inner parallel declarations','paypal cannot be tested in parallel with credit card','You must use test.describe.configure instead of nesting'],
    1,'serial is an outer constraint — inner parallel declarations have no effect inside a serial block.'],
  ['E','B','What is wrong?\n```ts\ntest.describe.serial("flow", () => {\n  test("step 1", …);\n  test("step 2", …);\n});\n// fullyParallel: true in config\n```\nBoth tests are still running simultaneously.',null,
    ['serial is ignored in fullyParallel mode — use test.only instead','describe.serial does work with fullyParallel but you must import test from the correct module — if using a custom test extension make sure describe.serial is available on it','serial requires workers: 1 to work','You need test.describe.configure({ mode: "serial" }) instead'],
    1,'The most common cause is importing from @playwright/test instead of a custom extended test object — but also verify via the HTML report that they show as sequential. describe.serial and fullyParallel do coexist correctly in standard usage.'],
  ['E','A','What is wrong?\n```ts\n// Worker-scoped fixture opens an authenticated browser context\nmyContext: [async ({ browser }, use) => {\n  const context = await browser.newContext({\n    storageState: "playwright/.auth/user.json"\n  });\n  await use(context);\n  await context.close();\n}, { scope: "worker" }],\n```\nTests get intermittent auth failures.',null,
    ['browser fixture cannot be used in worker-scoped fixtures','The browser fixture is test-scoped by default — using it in a worker-scoped fixture creates a dependency scope mismatch. Use the browserType worker-scoped fixture (browserName) or launch a browser directly with chromium.launch() to keep everything at worker scope','context.close() must not be awaited in teardown','storageState is incompatible with worker-scoped contexts'],
    1,'Fixtures can only depend on fixtures of equal or wider scope — a worker fixture depending on a test-scoped browser causes lifecycle issues. Use playwright.chromium or pass the browser type explicitly.'],

  // ── Debug ×6 ──────────────────────────────────────────────────
  ['D','B','Tests pass locally but fail in CI with "too many open files". What is the likely cause?',null,
    ['CI does not support Playwright','CI machines have lower ulimit for file descriptors — each worker opens browser processes and files. Reduce workers in CI: workers: process.env.CI ? 2 : undefined','Tests must be run serially in CI','The CI machine needs more RAM'],
    1,'High worker counts in CI can exhaust file descriptor limits — tune workers based on the CI machine\'s ulimit and available memory.'],
  ['D','I','A test in a serial describe block sometimes sees stale state from the previous test. What should you check?',null,
    ['Serial describe blocks have a memory isolation bug','Verify that each test in the serial block uses the same page fixture — the page is recreated between tests by default even in serial mode. If state must persist, use a worker-scoped context fixture shared across the serial block','Re-enable retries for the serial block','Add await page.reload() at the start of each test'],
    1,'Serial blocks guarantee order but not state persistence — each test still gets a fresh page unless you explicitly share context via a worker fixture.'],
  ['D','B','Sharded CI runs produce separate HTML reports but you cannot see all results together. What is the fix?',null,
    ['Download all HTML reports and open them simultaneously','Switch each shard to --reporter=blob, upload the blob artifacts, then run npx playwright merge-reports --reporter html on the collected blobs to produce one unified report','Use --reporter=json and manually concatenate the files','Run without sharding to get a single report'],
    1,'The blob + merge-reports workflow is the canonical way to aggregate sharded results into a single HTML report.'],
  ['D','I','Two tests that used to pass are now failing with race conditions after enabling fullyParallel. What is the root cause?',null,
    ['fullyParallel has a known bug with async tests','The tests share side-effectful state (a database record, a file, a global variable) that was harmless when they ran serially but causes conflicts when concurrent — make each test self-contained or serialise them with describe.serial','The tests need retries: 3 to handle race conditions','Playwright workers have a bug with shared memory'],
    1,'Race conditions on shared resources only surface under parallelism — the fix is isolation (each test owns its data) or explicit serialisation.'],
  ['D','I','`workers: 100%` is set but only 4 workers start on a 32-core machine. Why?',null,
    ['Playwright caps workers at 4','The browser process count also consumes cores — Playwright may also be considering available memory, or your OS limits the number of child processes. Additionally 100% should give 32 workers; check that the config key is fullyParallel: true is also set, otherwise fewer tests are assigned per worker and excess workers idle','workers: 100% is not valid syntax','Playwright limits shard sizes to 4'],
    1,'workers: "100%" gives 32 workers on 32 cores — if fewer start, check OS limits (ulimit -u), available memory, or whether tests are fewer than the worker count (Playwright won\'t start idle workers).'],
  ['D','A','merge-reports fails with "incompatible blob version". What happened?',null,
    ['The blob files are corrupted during upload','The shards were run with different versions of Playwright — blob format is version-specific. Ensure all CI machines use the exact same @playwright/test version','Blob files must be decompressed before merging','merge-reports requires Node 18+'],
    1,'Always pin the exact Playwright version across all CI machines — even a patch version difference can produce incompatible blob formats.'],

  // ── Complete ×5 ──────────────────────────────────────────────
  ['C','B','Complete the config to use fewer workers in CI.\n```ts\nworkers: process.env.CI ? 2 : ______,\n```',null,
    ['undefined','4','8','1'],
    0,'undefined uses Playwright\'s default (half the CPU cores) locally, while CI gets a fixed safe count.'],
  ['C','I','Complete the describe block to guarantee sequential order.\n```ts\ntest.describe.______("delete flow", () => {\n  test("create item", …);\n  test("delete item", …);\n});\n```',null,
    ['serial','parallel','ordered','sync'],
    0,'describe.serial enforces sequential execution on a single worker.'],
  ['C','I','Complete the CLI shard for the second of four machines.\n```\nnpx playwright test --shard=______\n```',null,
    ['2/4','2:4','2-4','2'],
    0,'Shard syntax is index/total — 2/4 means "run the second quarter of the suite".'],
  ['C','I','Complete the merge command.\n```\nnpx playwright ______ --reporter html ./blob-report\n```',null,
    ['merge-reports','merge','combine','aggregate'],
    0,'merge-reports is the Playwright subcommand for combining shard blob artifacts.'],
  ['C','A','Complete to set worker scope on the fixture.\n```ts\nmyDb: [async ({}, use) => {\n  const db = await connectDb();\n  await use(db);\n  await db.close();\n}, { ______: "worker" }],\n```',null,
    ['scope','level','lifecycle','mode'],
    0,'The scope option in the fixture options object controls the fixture lifecycle: "test", "worker", or "file".'],

  // ── Logic ×5 ──────────────────────────────────────────────────
  ['L','B','If you have 10 test files and workers: 4, how many files can run at the same time?',null,
    ['10 — all at once','4 — one per worker. The remaining 6 wait in the queue until a worker finishes','2 — Playwright always reserves half','1 — files always run one at a time'],
    1,'Workers process files concurrently up to the worker limit — 4 files in parallel, 6 waiting.'],
  ['L','I','What is the key difference between a test-scoped and a worker-scoped fixture?',null,
    ['Test-scoped fixtures are faster','Test-scoped fixtures are created and destroyed for every test; worker-scoped fixtures are created once per worker and shared by all tests assigned to that worker — ideal for expensive setup','Worker-scoped fixtures are shared across all workers','There is no practical difference — scope is cosmetic'],
    1,'Scope determines lifecycle: test scope = N creations for N tests; worker scope = 1 creation per worker regardless of how many tests run in it.'],
  ['L','B','Why should tests be self-contained (no shared mutable state) when using parallel execution?',null,
    ['Playwright requires self-contained tests for TypeScript compilation','Parallel workers execute tests in any order and timing — tests that depend on shared state created by another test become order-dependent and flaky','Self-contained tests load faster','Playwright crashes if tests share state'],
    1,'Shared state between parallel tests creates hidden dependencies — the test that runs first may leave state the second test needs, but "first" is non-deterministic.'],
  ['L','I','How does sharding interact with the `--grep` flag to filter tests?',null,
    ['You cannot use --grep with sharding','--grep filters tests first, then sharding splits the filtered set — only matching tests are distributed across shards','Sharding always runs all tests regardless of --grep','--grep must list test titles in JSON format when used with sharding'],
    1,'Filtering happens before sharding — each shard receives its fraction of the already-filtered test set.'],
  ['L','A','If a serial describe block is in a file and fullyParallel is true, does the block run concurrently with tests from other files?',null,
    ['No — serial blocks always run last, after all parallel tests','Yes — the serial block runs as a unit on one worker, but that worker runs concurrently with workers handling other files. Serial only constrains ordering within the block, not across the suite','Serial blocks disable parallelism for the entire worker','fullyParallel: true has no effect on files containing serial blocks'],
    1,'describe.serial controls order within the block — the block itself is one task among many workers and runs concurrently with other tests globally.'],

  // ── Real-World ×5 ─────────────────────────────────────────────
  ['R','B','You have 200 tests and CI takes 12 minutes. How do you cut that to ~4 minutes?',null,
    ['Use retries: 0 to skip failed tests faster','Enable fullyParallel: true and increase workers to match available CI cores, or add sharding across 3 CI machines to run 3× the tests simultaneously','Combine all tests into one file so they share setup','Use the dot reporter to reduce output overhead'],
    1,'Parallelism is the primary lever for CI speed — more workers (or more machines via sharding) is the direct solution.'],
  ['R','I','Your app has a "create → verify → cleanup" test that must run in order but other tests are fully parallel. How do you structure this?',null,
    ['Put all tests in a single file to ensure order','Put the ordered tests inside test.describe.serial() — they run sequentially on one worker while all other describe blocks or files run in parallel','Use retries: 0 only for those tests','Mark the tests with test.slow() to run them last'],
    1,'describe.serial targets exactly the tests that need ordering without affecting anything else in the suite.'],
  ['R','A','You want to run your 500-test suite across 5 GitHub Actions VMs to get 5× faster CI. What is the complete approach?',null,
    ['Create 5 separate test suites with different testDir values','Use a matrix strategy with shardIndex 1-5 and shardTotal 5; each job runs npx playwright test --shard=$INDEX/$TOTAL --reporter=blob; a merge job downloads all blobs and runs npx playwright merge-reports --reporter html','Use 5 separate playwright.config.ts files and merge results manually','Increase workers: 100 on a single VM'],
    1,'Matrix + sharding + blob reporter + merge-reports is the canonical GitHub Actions pattern for distributed Playwright runs.'],
  ['R','I','A worker-scoped database fixture is seeding 10 000 rows before tests run. With 8 workers this means 80 000 rows inserted per run. How do you fix it?',null,
    ['Use retries: 0 to avoid re-seeding','Seed once in globalSetup (which runs before any workers start) and have tests read from shared, immutable seed data — or use transactions that roll back after each test instead of truncate/insert','Worker scope is wrong — switch to test scope','Use a mock database instead of a real one'],
    0,'globalSetup runs once before all workers — it is the right place for one-time expensive seeding. Tests then read from or transact against the pre-seeded data.'],
  ['R','B','After adding fullyParallel, your login test fails with "storageState file not found" even though the auth setup project is listed as a dependency. What should you check?',null,
    ['fullyParallel breaks dependency ordering — use serial instead','Verify the storageState path matches exactly between auth.setup.ts and the test project use block, and that the playwright/.auth/ directory exists. With fullyParallel, tests start as soon as the dependency project passes — the file must be written before the first test launches','Restart the test runner to clear the dependency cache','fullyParallel requires each test to re-authenticate'],
    1,'File path typos and missing directories are the most common auth + parallel failures — the dependency only ensures the setup project ran, not that the correct file was created at the expected path.'],

  // ── Scenario ×4 ──────────────────────────────────────────────
  ['S','I','With fullyParallel: true and workers: 8, your test suite runs in 3 minutes locally. On CI (4 cores) it takes 11 minutes. Why and how do you fix it?',null,
    ['CI uses a slower browser — switch to Chromium headless','8 workers on a 4-core CI machine causes extreme context switching overhead — cap CI workers: process.env.CI ? 4 : undefined, or use sharding across multiple CI machines instead of over-provisioning a single one','fullyParallel causes more overhead in headless mode','CI blocks parallel browser processes for security'],
    1,'More workers than CPU cores causes thrashing — tune workers to match the CI machine\'s actual capacity, or scale out with sharding.'],
  ['S','A','You need to test a multi-step purchase flow (cart → checkout → confirmation → email) but also have 300 independent product tests running in parallel. How do you structure this?',null,
    ['Run all tests serially to avoid complexity','Put the purchase flow in test.describe.serial() — it runs as a self-contained sequential unit on one worker. The 300 product tests use fullyParallel: true and run concurrently across all other workers simultaneously','Split into two separate test suites with different configs','Use retries: 0 only for the purchase flow'],
    1,'describe.serial isolates the ordered flow on one worker while fullyParallel maximises parallelism everywhere else — both concerns are satisfied simultaneously.'],
  ['S','I','Your team wants to ensure that no individual test takes longer than 30 seconds, and the total suite does not exceed 5 minutes. How do you configure this?',null,
    ['Use retries: 1 and hope tests finish faster','Set timeout: 30000 (per-test timeout) and globalTimeout: 300000 (total suite limit) in playwright.config.ts — combine with enough workers to realistically hit the suite limit','Use maxFailures: 1 and retries: 0','Set expect.timeout globally to 30 seconds'],
    1,'timeout controls individual test runtime; globalTimeout kills the entire suite if it exceeds the wall-clock limit — both are needed for strict SLA enforcement.'],
  ['S','A','After switching to 4-shard CI, some tests that check sequential record IDs (1, 2, 3…) are failing because records from parallel shards collide. What is the architectural fix?',null,
    ['Run database tests in shard 1 only','Each shard should use an isolated database (separate schema, separate instance, or a unique prefix per shard using process.env.TEST_SHARD_INDEX) — sequential IDs are inherently global state and break under parallel shards sharing a database','Disable sharding for database tests','Use random IDs and assert "is a number" instead of exact values'],
    0,'Isolated data namespaces (by shard index, worker index, or random prefix) are the correct fix — shared global state (including auto-increment IDs) is incompatible with parallel test runs.'],

  // ── Additional batch ×50 ─────────────────────────────────────
  ['O','B','What does `workers: "50%"` in playwright.config.ts do?',null,
    ['Runs 50% of the test suite in parallel','Uses 50% of available CPU cores as the worker count — if the machine has 8 cores, workers is set to 4. Keeps the machine responsive while still parallelising.','Limits test execution to 50% of the timeout','Retries 50% of failing tests'],
    0,'Percentage workers scale with the host machine — useful for configs shared between developer laptops and CI servers with different core counts.'],
  ['O','I','What does `maxFailures: 5` in playwright.config.ts do?',null,
    ['Stops the suite after any single test fails','Stops the entire test run once 5 tests have failed — prevents a bad commit from burning through all workers when many tests are expected to fail','Retries each test up to 5 times before marking it as failed','Limits per-file failures to 5'],
    1,'maxFailures is a fast-fail ceiling — useful for large suites where you want early signal without waiting for every test to run.'],
  ['O','B','What does `test.describe.parallel()` do?',null,
    ['Marks a describe block to run in a separate browser','Overrides the default test order within the describe block to allow its tests to run in parallel on different workers — requires fullyParallel or the parent describe to allow parallelism','Disables serial execution for the block','Runs the describe block on all configured browsers simultaneously'],
    0,'describe.parallel() is the counterpart to describe.serial() — it explicitly opts the block into parallel execution even if the file-level default is serial.'],
  ['O','I','What is a "shard" in Playwright terminology?',null,
    ['A shared browser context between tests','A partition of the test suite run on a separate machine or CI node — e.g. --shard=1/4 runs the first quarter of tests. Combine all shards\' blob reports with merge-reports for a unified result.','A single test file','A group of tests from the same project'],
    1,'Sharding splits the suite across machines — total parallelism = workers per machine × number of shards.'],
  ['O','A','What does `test.describe.configure({ mode: "parallel", retries: 2 })` do?',null,
    ['Configures the entire test file to run in parallel with 2 retries','Applies the configuration only to the enclosing describe block — its tests run in parallel and failing ones are retried up to 2 times independently of the file-level config','Sets global parallelism and retry options','Overrides only the retry setting'],
    1,'describe.configure() scopes configuration to the describe block — a fine-grained override for mixed-mode test files.'],
  ['O','I','What does `PLAYWRIGHT_WORKER_INDEX` environment variable contain during test execution?',null,
    ['The total number of workers','The 0-based index of the current worker process — useful for namespacing test data (e.g. database schema per worker: "test_worker_" + process.env.PLAYWRIGHT_WORKER_INDEX)','The CI job number','The current shard index'],
    1,'Worker index is the per-run isolation key — use it to create worker-scoped data stores that don\'t collide with other parallel workers.'],
  ['O','B','What does `--reporter=blob` save during a sharded run?',null,
    ['A Blob storage connection string for uploads','A compact binary report file in blob-report/ directory — each shard produces one, then `playwright merge-reports` combines them into a single HTML report','A screenshot blob for each test','The test source code in binary format'],
    1,'Blob reporter is the inter-shard report format — small, efficient, and mergeable.'],
  ['O','I','What does `globalSetup` run in relation to workers?',null,
    ['Once per worker — each worker runs its own globalSetup','Once before any workers are launched — a single Node.js process runs globalSetup to perform shared pre-test setup (auth, DB seed) before parallelism begins','Simultaneously with the first worker','After all workers complete'],
    1,'globalSetup is a single-execution pre-run hook — its purpose is one-time setup that shouldn\'t be repeated per worker.'],
  ['O','B','What does `globalTeardown` do?',null,
    ['Tears down each test after it fails','Runs once after all workers have finished — used to clean up global state created in globalSetup (delete temp databases, close connections, clean up auth files)','Runs after each worker completes','Resets the browser profile between runs'],
    1,'globalTeardown mirrors globalSetup — anything created globally should be cleaned up globally.'],
  ['O','A','What does `--shard=2/3` mean when passed to `npx playwright test`?',null,
    ['Run tests 2 and 3 only','Run the second partition of 3 equal shards — Playwright deterministically assigns tests to shards so the distribution is consistent across runs','Run tests at 2/3 speed for stability','Run the second third of the test files alphabetically'],
    1,'Sharding assigns tests to shards based on a stable hash — the same test always lands on the same shard for the same total shard count.'],
  ['O','I','What does `webServer.reuseExistingServer: true` achieve in a parallel context?',null,
    ['Allows multiple workers to reuse the same port','Tells Playwright to use an already-running dev server instead of starting a new one — prevents port conflicts when multiple test runs or workers try to start the same server concurrently','Enables server-side session sharing','Forces all workers to use localhost'],
    1,'reuseExistingServer: true is essential in watch mode and CI where a server may already be running — prevents EADDRINUSE errors.'],
  ['O','B','What does `test.setTimeout(60_000)` inside a test function do?',null,
    ['Sets the global timeout to 60 seconds','Overrides the timeout for that specific test only — 60 seconds instead of the configured default. Other tests are unaffected.','Pauses the test for 60 seconds','Retries the test for 60 seconds'],
    1,'Per-test timeout override is useful for known slow tests — set it explicitly rather than raising the global timeout.'],
  ['F','B','Complete to set the worker count based on environment.\n```ts\nworkers: process.env.CI ? ______ : undefined,\n```',null,
    ['4','1','8','2'],
    0,'4 is a common CI worker count for machines with 4+ cores — undefined lets Playwright use its default (half CPU cores) locally.'],
  ['F','I','Complete to run a describe block serially.\n```ts\ntest.describe.______("purchase flow", () => {\n  test("add to cart", ...);\n  test("checkout", ...);\n  test("confirmation", ...);\n});\n```',null,
    ['serial','sequential','ordered','sync'],
    0,'describe.serial() runs its tests in order on a single worker — the rest of the suite can still run in parallel.'],
  ['F','B','Complete to set a global suite timeout.\n```ts\n// playwright.config.ts\nglobalTimeout: ______,\n```',null,
    ['5 * 60 * 1000','60_000','300','"5m"'],
    0,'5 * 60 * 1000 = 300,000 ms = 5 minutes — Playwright timeouts are always in milliseconds.'],
  ['F','I','Complete to stop after 10 failures.\n```ts\n// playwright.config.ts\n______: 10,\n```',null,
    ['maxFailures','stopAfter','failLimit','earlyExit'],
    0,'maxFailures: N stops the run when N tests have failed — fast-fail for large suites.'],
  ['F','A','Complete to run only the second of four shards.\n```sh\nnpx playwright test --shard=______\n```',null,
    ['2/4','2,4','2-4','shard:2/4'],
    0,'--shard=X/Y: X is the shard index (1-based), Y is the total number of shards.'],
  ['F','B','Complete to merge shard reports.\n```sh\nnpx playwright ______ blob-report/ --reporter=html\n```',null,
    ['merge-reports','combine-reports','join-reports','aggregate-reports'],
    0,'merge-reports reads blob report directories from all shards and produces a single combined report.'],
  ['F','I','Complete to configure the blob reporter.\n```ts\n// playwright.config.ts\nreporter: process.env.CI ? [["______", { outputDir: "blob-report" }]] : [["html"]],\n```',null,
    ['blob','blob-reporter','json','ci'],
    0,'"blob" is the reporter name — lowercase, as registered in Playwright\'s reporter list.'],
  ['F','A','Complete to set the test timeout per test.\n```ts\ntest("slow test", async ({ page }) => {\n  test.______( 90_000);\n  // ...\n});\n```',null,
    ['setTimeout','timeout','setLimit','slow'],
    0,'test.setTimeout() overrides the timeout for the currently running test only.'],
  ['F','I','Complete to configure per-project parallelism.\n```ts\nprojects: [\n  {\n    name: "chromium",\n    ______: { fullyParallel: true },\n  },\n]\n```',null,
    ['use','config','settings','options'],
    0,'use: { fullyParallel: true } applies the parallel setting to the specific project — overrides the global setting.'],
  ['E','B','What is wrong?\n```ts\ntest.describe.serial("flow", () => {\n  test("step 1", async ({ page }) => { ... });\n  test("step 2", async ({ page }) => { ... });\n});\n// Each test creates a fresh page fixture\n```',null,
    ['describe.serial is not a valid Playwright API','describe.serial ensures ordering but each test still gets its own fresh page fixture — state does not automatically flow between tests. Share state via module-level variables or worker-scoped fixtures if step 2 depends on step 1\'s state.','Tests inside describe.serial share the same page','describe.serial requires all tests to pass to run the next one'],
    1,'Serial execution guarantees order but not state continuity — fresh fixtures reset state between tests. Use module variables or shared fixtures for inter-test state.'],
  ['E','I','What is wrong?\n```ts\n// playwright.config.ts\nfullyParallel: true,\nworkers: 1,\n```',null,
    ['fullyParallel requires workers: undefined','This config is valid but self-defeating — fullyParallel with workers: 1 runs tests sequentially (1 worker = no parallelism). Set workers to 2+ to gain any benefit from fullyParallel.','workers must be at least equal to fullyParallel\'s value','fullyParallel overrides workers automatically'],
    1,'1 worker negates parallelism — remove the workers override or set it higher than 1.'],
  ['E','B','What is wrong with sharing a module-level array between parallel tests?\n```ts\n// tests/shared.ts\nexport const ids: string[] = [];\n```',null,
    ['Arrays cannot be used in TypeScript test files','Module-level variables in Node.js are NOT shared between worker processes — each worker gets its own copy of the module. Cross-worker data sharing requires external mechanisms (file, DB, IPC). The array in worker 1 is invisible to worker 2.','export arrays are always read-only','Worker processes share module memory'],
    1,'Workers are separate processes — module state is process-local. Use files, environment variables, or databases for cross-worker data.'],
  ['E','A','What is wrong?\n```sh\nnpx playwright merge-reports --reporter=html blob-report/\n```',null,
    ['merge-reports does not accept a reporter flag','The argument order is wrong — the directory comes before the flags in some versions, but the correct syntax is: `npx playwright merge-reports blob-report/ --reporter html` (without the equals sign). Check the Playwright version for exact syntax.','merge-reports only outputs JSON','--reporter=html must be specified in the config'],
    1,'Argument order and flag syntax for merge-reports is version-sensitive — check the docs for your exact Playwright version.'],
  ['E','I','What is wrong?\n```ts\ntest.describe.serial("flow", () => {\n  test.describe.parallel("sub-flow", () => {\n    // parallel inside serial\n  });\n});\n```',null,
    ['Nesting describe blocks is not supported','parallel inside serial is a conflict — the serial parent constrains all descendants to run on one worker in sequence, overriding the parallel child\'s intent. The parallel config has no effect.','parallel must come before serial','This is valid and each level applies independently'],
    1,'Serial is a ceiling constraint — descendants cannot escape it with parallel. Restructure to put parallel tests outside the serial describe.'],
  ['D','B','Adding more workers speeds up the test suite until around 8 workers, then times out. CPU and memory look fine. What else could be the bottleneck?',null,
    ['Playwright caps workers at 8','The application server or database may be the bottleneck — too many concurrent workers overwhelm a shared backend. Check server response times and connection pool exhaustion. Consider using a test-specific backend that scales better.','headless browsers cannot run more than 8 in parallel','fullyParallel is needed above 8 workers'],
    1,'Backend saturation is a common wall — more Playwright workers doesn\'t help if the server is queueing requests.'],
  ['D','I','Sharded CI runs sometimes produce different total test counts across shards. What could cause this?',null,
    ['Playwright randomly assigns tests to shards','A test file generates dynamic test cases (e.g. using test.each with a runtime array) — Playwright shards based on static discovery, and dynamic tests may not be discovered the same way. Fix: use static test data for shard-stable distribution.','Shard index is 0-based causing off-by-one','The number of shards must equal the number of test files'],
    1,'Dynamic test generation (runtime test.each data) can make shard distribution unstable — prefer static test parameterisation.'],
  ['D','B','`merge-reports` fails with "no blob reports found". What is the likely cause?',null,
    ['merge-reports requires an internet connection','The blob-report/ directory either doesn\'t exist or is empty — check that the sharded test runs used `--reporter=blob` and that the CI artifact upload step included the blob-report/ directory','The blob format changed in a Playwright update','merge-reports must be run on the same machine as the tests'],
    1,'Missing blob files mean the reporter wasn\'t configured correctly or the CI artifact step didn\'t collect the directory.'],
  ['D','A','Worker 2 consistently fails a test that passes on workers 1, 3, and 4. The test interacts with shared test fixtures. What should you investigate?',null,
    ['Worker 2 uses a different browser','A worker-scoped fixture may be leaking state between tests — if a fixture creates a resource once per worker and doesn\'t clean it up properly, earlier tests on worker 2 pollute later ones. Add cleanup and use a fresh resource per test for stateful fixtures.','Worker 2 has a different network route','Playwright assigns harder tests to higher-numbered workers'],
    1,'Worker-scoped fixture cleanup bugs cause intermittent per-worker failures — verify teardown runs even on test failure using try/finally.'],
  ['C','B','Complete to set a per-test timeout override.\n```ts\ntest("long test", async ({ page }) => {\n  test.______( 120_000);\n  await page.goto("/slow-page");\n});\n```',null,
    ['setTimeout','setLimit','timeout','slow'],
    0,'test.setTimeout(ms) overrides the timeout for the currently running test.'],
  ['C','I','Complete to configure the blob reporter for CI.\n```ts\nreporter: process.env.CI\n  ? [[______, { outputDir: "blob-report" }]]\n  : [["html"]],\n```',null,
    ['"blob"','"ci"','"json"','"blob-reporter"'],
    0,'"blob" is the string identifier for the blob reporter.'],
  ['C','A','Complete to read the worker index for data isolation.\n```ts\nconst workerIdx = ______;\nconst dbName = `test_db_\${workerIdx}`;\n```',null,
    ['process.env.PLAYWRIGHT_WORKER_INDEX','process.env.WORKER_ID','test.info().workerIndex','global.workerIndex'],
    2,'testInfo.workerIndex is the fixture-based way; process.env.PLAYWRIGHT_WORKER_INDEX is the env-based way — both are valid. testInfo.workerIndex is preferred inside tests.'],
  ['C','B','Complete to limit the test run to stop after 3 failures.\n```ts\n// playwright.config.ts\n______: 3,\n```',null,
    ['maxFailures','stopAfter','failAt','earlyStop'],
    0,'maxFailures: 3 stops the run once 3 tests have failed.'],
  ['C','I','Complete to run tests from the second of three shards.\n```sh\nnpx playwright test ______\n```',null,
    ['--shard=2/3','--shard 2/3','--partition=2/3','--split=2:3'],
    0,'--shard=2/3 is the exact CLI syntax — X/Y where X is the 1-based shard index.'],
  ['L','B','Why is `fullyParallel: true` not the default in Playwright?',null,
    ['It is slower than the default','Some tests depend on ordering or shared state — enabling full parallelism by default would break those tests. Playwright defaults to per-file parallelism (tests within a file run sequentially) to be safe for code written without explicit isolation.','Playwright cannot detect parallel-safe tests automatically','The default workers value is 1'],
    1,'Safe defaults: file-level parallelism is conservative and works for most code. fullyParallel is opt-in for teams that have ensured test isolation.'],
  ['L','I','What is the difference between `workers: 4` and `fullyParallel: true`?',null,
    ['They are the same setting','workers: N controls HOW MANY concurrent workers run. fullyParallel: true controls WHAT runs in parallel — with it, individual tests run across workers; without it, files run across workers but tests within a file are sequential.','fullyParallel enables 4× more workers','workers controls browsers; fullyParallel controls tests'],
    1,'Two orthogonal dimensions: worker count (how many) and parallelism mode (what granularity). Combine both for maximum throughput.'],
  ['L','I','When is `describe.serial()` the right choice vs. splitting into separate test files?',null,
    ['They are always interchangeable','describe.serial() is right when the tests share context and state within the same file (e.g. a wizard flow). Separate files are right when the tests are genuinely independent topics. Using describe.serial across the entire file for independent tests is an anti-pattern.','describe.serial is always slower than separate files','Separate files always run in parallel; describe.serial never does'],
    1,'Match the tool to the coupling — serial for genuinely ordered coupled steps, separate files for independent topics.'],
  ['L','A','Why does sharding beat simply adding more workers on a single machine?',null,
    ['Sharding is always faster even on a single machine','A single machine has a fixed ceiling on CPU cores and memory — sharding spreads the load across multiple machines, multiplying total parallelism beyond what one machine can support. Sharding + workers together give the highest throughput.','Sharding avoids the need for blob reports','Workers are limited to 8; sharding removes this cap'],
    1,'Horizontal scale (more machines via sharding) multiplies vertical scale (workers per machine) — the two are complementary.'],
  ['L','I','What happens to the test run if `globalSetup` throws an error?',null,
    ['Playwright ignores globalSetup errors and starts the tests','The entire test run aborts — no tests run. globalSetup failures are fatal because the preconditions for the suite are unmet.','Only the first worker is affected','Tests are retried without globalSetup'],
    1,'globalSetup failures abort the run — fix the setup issue before any test results can be trusted.'],
  ['R','B','You want to run smoke tests quickly (10 tests) and the full suite (500 tests) on the same CI config. How do you structure this?',null,
    ['Maintain two separate playwright.config.ts files','Tag smoke tests with @smoke and run: `npx playwright test --grep @smoke` for fast feedback and without --grep for the full suite — same config, different invocation','Duplicate the test files into a smoke/ directory','Use projects with testMatch: to separate smoke and full'],
    1,'Tag-based filtering (@smoke, @regression) is the clean way to select test subsets from one config.'],
  ['R','I','After adding parallelism, some database tests fail with "deadlock detected". How do you isolate them?',null,
    ['Run all tests serially','Move the database tests into a describe.serial() block OR into a separate project with workers: 1 — either approach ensures only one test accesses the shared database at a time','Add retries: 3 to the database tests','Use transactions to avoid deadlocks'],
    1,'Serial execution is the correct isolation for tests that share a global resource like a database without per-test isolation.'],
  ['R','A','You want a CI pipeline where: 1) unit tests run first, 2) E2E tests run in parallel across 4 shards, 3) a report is published after all shards complete. How do you structure this?',null,
    ['Run everything sequentially in one stage','Three pipeline stages: stage 1 (unit tests, fail fast), stage 2 (4 parallel shard jobs with --shard=N/4 --reporter=blob, upload blob-report/ artifacts), stage 3 (download all artifacts, run merge-reports --reporter=html, publish report)','Use a single Playwright config with all stages combined','Add a depends-on: unit-tests line to the Playwright config'],
    1,'Multi-stage CI with artifact-based report merging is the canonical parallel Playwright CI pattern.'],
  ['S','B','Adding fullyParallel breaks 20 tests that were accidentally sharing module-level state. How do you identify which tests are affected?',null,
    ['Run with --timeout=1 to force failures early','Run the test suite with fullyParallel: true and workers: 2 — the failures reveal tests that depend on sequential execution or shared module state. Fix them by moving state into fixtures or using unique identifiers per test.','Use --reporter=list to see ordering','Sort the test files alphabetically to identify the ordering dependency'],
    1,'Two workers with fullyParallel is the minimum configuration that exposes parallelism bugs — binary search workers upward to find the concurrency level that causes failures.'],
  ['S','I','Your 4-shard CI run takes 15 minutes. Each shard runs for 13-15 minutes while one shard finishes in 3 minutes (uneven distribution). How do you rebalance?',null,
    ['Increase shard count to 8','Playwright shards by file alphabetically — reorganize large spec files into smaller ones, or use --shard with a different total count. Alternatively use a CI matrix with dynamic test splitting based on historical timing data.','Add workers to the slow shards','Use blob reporter to detect the slow shard'],
    1,'Shard imbalance is caused by large test files — splitting them evens the distribution, or use a timing-aware split tool.'],
  ['S','A','You need to run the same test suite against 5 environments (dev, staging, eu-prod, us-prod, asia-prod) in CI. How do you structure this efficiently?',null,
    ['Maintain 5 separate Playwright configs','One config with a BASE_URL environment variable; CI runs 5 parallel pipeline jobs, each setting BASE_URL to its environment and running the suite (with sharding per job if needed). The blob reporter aggregates per-environment results.','Run environments sequentially in one job','Use projects with one project per environment and workers: 1'],
    1,'Environment-as-a-variable scales cleanly — one config, N parallel CI jobs with different env vars, optional sharding within each job for speed.'],
],
};
