import{n as e}from"./rolldown-runtime-BpQH8Ho1.js";var t=e({PLAYWRIGHT_QUESTIONS_RAW:()=>n}),n={setup:[[`O`,`B`,`What command initialises a new Playwright project with an interactive setup wizard?`,null,[`npm install playwright`,`npm init playwright@latest`,`npx playwright install`,`npm create @playwright`],1,"`npm init playwright@latest` scaffolds the entire project: config, sample test, browser download, and optional CI workflow."],[`O`,`B`,"Which file is generated as the primary Playwright configuration after running `npm init playwright@latest`?",null,[`playwright.json`,`playwright.config.ts`,`.playwrightrc`,`test.config.ts`],1,"`playwright.config.ts` (or `.js`) is the single source of truth for projects, timeout, retries, reporters, and the `use` block."],[`O`,`B`,`Which command downloads all browser binaries that Playwright needs?`,null,[`npx playwright install`,`npm install browsers`,`npx playwright browsers`,`npx playwright setup`],0,"`npx playwright install` downloads Chromium, Firefox, and WebKit. Run it once after installing the package."],[`O`,`I`,"What does the `--headed` flag do when passed to `npx playwright test --headed`?",null,[`Adds verbose logging to stdout`,`Shows the browser window during the test run`,`Opens Playwright Inspector for step debugging`,`Generates a detailed HTML report`],1,"By default Playwright runs headless. `--headed` opens a visible browser window so you can watch each test step."],[`O`,`B`,`Which command opens the Playwright visual UI mode with time-travel debugging?`,null,[`npx playwright debug`,`npx playwright open`,`npx playwright test --ui`,`npx playwright interactive`],2,"`npx playwright test --ui` launches the interactive dashboard where you can pick tests, watch them run, and rewind to any step."],[`O`,`I`,"What is the default test directory that `npm init playwright@latest` creates?",null,[`/spec`,`/e2e`,`/tests`,`/playwright`],2,"The wizard creates `tests/` and sets `testDir: './tests'` in the config. You can rename it during setup."],[`O`,`I`,"What does `npx playwright test --reporter=html` generate?",null,[`A plain text log file`,`A self-contained HTML report viewable in the browser`,`A JUnit XML report`,`A live streaming test dashboard`],1,"The HTML reporter writes `playwright-report/index.html` — a rich, self-contained page with results, screenshots, and traces."],[`O`,`A`,"What does `npx playwright install --with-deps` do on a Linux CI server?",null,[`Only downloads browser binaries`,`Installs browsers AND their required OS-level system libraries`,`Installs Playwright globally with all peer dependencies`,`Updates Playwright and all browsers to the latest versions`],1,"On Linux, `--with-deps` also installs packages like libgtk, libgbm, and libxshmfence that the browsers need."],[`O`,`I`,"Which file does `npm init playwright@latest` optionally generate for continuous integration?",null,[`.github/workflows/playwright.yml`,`.github/playwright.yml`,`.ci/playwright.yml`,`github-actions.yml`],0,'When you answer "yes" to the CI question, the wizard writes a ready-to-use GitHub Actions YAML at `.github/workflows/playwright.yml`.'],[`O`,`B`,`What command prints the currently installed Playwright version?`,null,[`npx playwright version`,`npx playwright --version`,`npx playwright info`,`npx playwright -V`],1,"Like most CLI tools, `--version` (or `-V`) prints the installed version number."],[`O`,`I`,"What does `npx playwright show-report` do?",null,[`Opens the Playwright documentation site`,`Serves the last generated HTML report locally and opens it in the browser`,`Displays the current test configuration`,`Shows a report of all installed browsers`],1,"After running tests with the HTML reporter, `show-report` starts a local HTTP server and opens the report in your default browser."],[`O`,`I`,`Which browser name does Playwright use for its Chromium-based engine?`,null,[`chrome`,`chromium`,`google-chrome`,`chromium-browser`],1,"Playwright uses `chromium` (not `chrome`) alongside `firefox` and `webkit`. Pass it to `npx playwright install chromium`."],[`F`,`B`,"Fill in the blank to install only the Firefox browser: `npx playwright install ______`",null,[`firefox`,`Firefox`,`firefox-browser`,`gecko`],0,"The three browser IDs for the install command are `chromium`, `firefox`, and `webkit`."],[`F`,`I`,"Fill in the flag that filters tests by title pattern: `npx playwright test ______=login`",null,[`--grep`,`--filter`,`--match`,`--pattern`],0,'`--grep` accepts a regex string; `--grep=login` runs only tests whose title contains "login".'],[`F`,`B`,"Complete the config to set a 30-second test timeout: `defineConfig({ timeout: ______ })`",null,[`30_000`,`30`,`30s`,`"30000"`],0,"Playwright timeouts are in milliseconds. `30_000` (or `30000`) equals 30 seconds."],[`F`,`I`,"Fill in the flag for step-by-step debugging: `npx playwright test ______ login.spec.ts`",null,[`--debug`,`--inspect`,`--break`,`--step`],0,"`--debug` opens Playwright Inspector, pauses before the first action, and lets you step through the test interactively."],[`F`,`I`,"Complete the `use` block option that skips SSL certificate errors: `use: { ignoreHTTPS______: true }`",null,[`Errors`,`SSL`,`Certs`,`Warnings`],0,"`ignoreHTTPSErrors: true` tells Playwright to bypass self-signed or invalid SSL certificate errors."],[`F`,`A`,"Fill in the config option to retry failed tests twice in CI: `______: process.env.CI ? 2 : 0`",null,[`retries`,`retry`,`rerun`,`attempts`],0,"`retries` accepts a number. The ternary enables retries only in CI (`process.env.CI` is set by GitHub Actions etc.)."],[`F`,`I`,"Complete the command to run tests matching a specific project: `npx playwright test ______=firefox`",null,[`--project`,`--browser`,`--engine`,`--target`],0,"`--project` matches the `name` field of a project entry in `playwright.config.ts`. Project names are case-sensitive."],[`E`,`B`,"What is wrong with this command?\n`npx playwright login.spec.ts`",null,["Missing the `test` subcommand",`Wrong file extension — should be .js`,`Should use npm instead of npx`,`Missing --run flag`],0,"The correct form is `npx playwright test login.spec.ts`. The `test` subcommand is required; without it the CLI does not know what to do."],[`E`,`I`,`Spot the error in this config snippet:
\`\`\`
import { defineConfig } from '@playwright/test';
export default defineConfig({
  projects: [{ name: 'chrome', use: { ...devices['Desktop Chrome'] } }]
});
\`\`\``,null,["`devices` is not imported",`testDir is missing`,`projects syntax is wrong`,`defineConfig does not accept projects`],0,"`devices` must be imported: `import { defineConfig, devices } from '@playwright/test';`"],[`E`,`B`,"What is wrong with this import in a test file?\n`import { test, expect } from 'playwright';`",null,["Should import from `@playwright/test`, not `playwright`","`expect` cannot be imported alongside `test`",`Curly braces are invalid here`,`Should use require() not import`],0,"The test-runner API (`test`, `expect`, built-in fixtures) lives in `@playwright/test`. The `playwright` package is the lower-level browser API."],[`E`,`I`,"What is wrong with this CI config?\n`use: { headless: false }`",null,[`CI environments have no display — headless: false will fail`,`headless must be set per project, not in use`,"`false` is not a valid value for headless",`headless is not a valid use option`],0,"CI runners (GitHub Actions, etc.) have no display server, so running with a visible browser window (`headless: false`) throws an error."],[`E`,`I`,"What is wrong with this project entry?\n```\n{ name: 'desktop', use: { browserName: 'chrome' } }\n```",null,["browserName must be `'chromium'`, not `'chrome'`",`name cannot be 'desktop'`,`use must be a spread of devices`,`browserName is not a valid property`],0,"Valid `browserName` values are `'chromium'`, `'firefox'`, and `'webkit'`. `'chrome'` is not a recognised Playwright browser name."],[`E`,`A`,"What is wrong with this reporter config?\n`reporter: { type: 'html' }`",null,["Reporter must be a string like `'html'` or an array of arrays",`reporter is not a valid config key`,`html is not a valid reporter name`,`type must be uppercase`],0,"`reporter` accepts a string (`'html'`), an array of strings, or an array of arrays (`[['html', options]]`). An object with `type` is not valid."],[`D`,`I`,"Tests run in Chromium despite `--project=Firefox` being passed. What is wrong?",null,["Project names are case-sensitive — use `--project=firefox` to match the config name",`--project requires the full browser path`,`Firefox project must be the first in the config`,`Use --browser instead of --project`],0,'The `--project` value must exactly match the `name` in `playwright.config.ts`. If the config says `"firefox"` (lowercase), `--project=Firefox` won\'t match.'],[`D`,`B`,'`npx playwright test` reports "No tests found" but the file `login.test.ts` exists. What is most likely wrong?',null,["The default testMatch only finds `*.spec.ts` — rename to `login.spec.ts` or update testMatch",`Tests must live in the project root`,"The file must be named `playwright.spec.ts`",`TypeScript files are not supported by default`],0,"Playwright's default `testMatch` is `**/*.spec.{ts,js}`. Files ending in `.test.ts` are skipped. Rename the file or add `*.test.ts` to testMatch."],[`D`,`A`,`Linux CI fails with missing library errors when installing browsers. Which command fixes it?`,null,[`npx playwright install --with-deps`,`sudo npx playwright install`,`npx playwright install --all`,`npx playwright install linux-deps`],0,"`--with-deps` automatically installs the required OS packages (libwoff, libgbm, libharfbuzz, etc.) alongside the browser binaries."],[`D`,`I`,`Tests pass locally but GitHub Actions reports "Browser executable not found". What step is missing from the CI workflow?`,null,["A step running `npx playwright install --with-deps`",`Setting headless: true in config`,`Installing a newer Node.js version`,`Caching node_modules`],0,"CI runners do not include Playwright browsers. You must add `run: npx playwright install --with-deps` as an explicit workflow step."],[`D`,`B`,'After `npm init playwright@latest`, running `npx playwright test` fails with "Cannot find module @playwright/test". What went wrong?',null,["`npm install` was not run after init to actually download the package",`Node.js version is incompatible`,`playwright.config.ts has a wrong import path`,`TypeScript compiler is missing`],0,"`npm init playwright@latest` adds `@playwright/test` to `package.json` but you still need `npm install` to download it into `node_modules`."],[`D`,`I`,"Tests time out frequently because `timeout: 5000` is too short. How should you fix it?",null,["Increase the timeout value, e.g. `timeout: 30_000`",`Remove the timeout property to use no limit`,"Pass `--timeout` flag at run time only","Set `timeout: 0` to disable all timeouts"],0,"Raise `timeout` in `playwright.config.ts`. The value is in milliseconds, so `30_000` gives each test 30 seconds before it is marked failed."],[`C`,`B`,"Complete the import: `import { ______, devices } from '@playwright/test';`",null,[`defineConfig`,`PlaywrightConfig`,`configure`,`Config`],0,"`defineConfig` is the helper function that wraps your config object and provides full TypeScript type support."],[`C`,`I`,"Complete the `use` block to set the application root URL:\n`use: { ______: 'http://localhost:3000' }`",null,[`baseURL`,`url`,`serverURL`,`rootURL`],0,"`baseURL` lets you write `page.goto('/login')` instead of the full absolute URL in every test."],[`C`,`I`,"Complete the config to run tests in 4 parallel processes: `defineConfig({ ______: 4 })`",null,[`workers`,`parallel`,`concurrency`,`threads`],0,"`workers` controls how many test processes run simultaneously. Set to a number or `'auto'` to use all CPU cores."],[`C`,`A`,"Complete the project entry using a preset device:\n`{ name: 'safari', use: { ...______['Desktop Safari'] } }`",null,[`devices`,`browsers`,`presets`,`defaults`],0,"`devices` imported from `@playwright/test` contains preset viewports and user-agents for desktop and mobile browsers."],[`C`,`I`,"Complete the config to use both HTML and line reporters:\n`reporter: [[______], ['line']]`",null,[`'html'`,`html`,`{ type:'html' }`,`'html-reporter'`],0,"Each entry in the reporter array is itself an array: `['html']`, `['line']`, or `['junit', {outputFile:'results.xml'}]`."],[`L`,`B`,"What happens when `npx playwright test` is run with no arguments?",null,[`Runs all files matching testMatch in testDir`,`Runs only the most recently modified file`,`Opens the interactive UI`,`Prints the current configuration`],0,"With no arguments Playwright discovers every file matching the configured `testMatch` pattern (default `**/*.spec.{ts,js}`) and runs them all."],[`L`,`I`,"`retries: 2` is set in config. A test fails all 3 attempts. What is the final result?",null,[`Test is marked FAILED (1 original + 2 retries)`,`Test is marked FLAKY after the second retry`,`Test is skipped automatically`,`Test runs again until it passes`],0,"`retries: 2` gives each failing test 2 extra attempts. If all 3 fail the test is marked FAILED. A test is marked FLAKY only if it passes on a retry."],[`L`,`I`,"In what order does `npm init playwright@latest` ask its questions?",null,[`TypeScript/JavaScript → test directory → GitHub Actions`,`Browsers → TypeScript → test directory`,`Test directory → GitHub Actions → TypeScript`,`Install browsers → TypeScript → test directory`],0,`The wizard asks: (1) TS or JS, (2) test directory name, (3) add GitHub Actions workflow. Browsers are downloaded after, not prompted.`],[`L`,`A`,"How does project-level `use` interact with global `use` in playwright.config.ts?",null,["Project-level `use` overrides global `use` for that project","Global `use` always takes precedence",`They cannot coexist; it is a config error`,`Both are ignored; only CLI flags apply`],0,"Playwright merges settings from global `use` and project-level `use`, with the project level winning for any conflicting keys — perfect for per-browser overrides."],[`L`,`I`,"What is the key difference between `--headed` and `--debug`?",null,[`--headed shows the browser window; --debug also pauses in Playwright Inspector`,`They are identical flags`,`--debug runs without a browser`,`--headed is for API tests, --debug for UI tests`],0,"`--headed` makes tests visible but runs at full speed. `--debug` also opens Playwright Inspector, pauses before the first action, and lets you step line-by-line."],[`R`,`B`,`Your team is starting a new project and wants the fastest path to a working Playwright setup. Which command should come first?`,null,[`npm init playwright@latest`,`npm install @playwright/test`,`npx playwright install`,`npm install playwright`],0,"`npm init playwright@latest` is the single command that handles everything: installs the package, downloads browsers, creates config, and writes a sample test."],[`R`,`I`,`Your CI pipeline takes 20 minutes because tests run one at a time. What is the best single config change?`,null,["Set `workers: 4` (or `'auto'`) to run tests in parallel","Set `retries: 0` to skip flaky re-runs","Add `--headed` to the CI command","Reduce `timeout` to fail faster"],0,"Increasing `workers` lets Playwright run multiple tests concurrently across separate processes, dramatically cutting total run time."],[`R`,`I`,"Your team wants retries in CI but not locally (for fast feedback). How should you configure `retries`?",null,["`retries: process.env.CI ? 2 : 0`","`retries: 2` with `--no-retry` locally",`Separate config files for CI and local`,"`retries: 'ci'`"],0,"`process.env.CI` is set by GitHub Actions, GitLab CI, and most other CI systems, making it the standard way to differentiate CI from local runs."],[`R`,`A`,`Your organisation tests against Chrome, Firefox, and Safari. What is the recommended approach?`,null,[`Define three projects in playwright.config.ts with the appropriate devices preset`,"Run `npx playwright test` three times with different `--browser` flags",`Maintain three separate config files`,`Use browser.connect() to connect to remote browsers`],0,"Playwright's `projects` array is designed for exactly this: one `npx playwright test` run, three browser targets, one aggregated report."],[`R`,`I`,"A QA engineer wants to run only tests tagged `@smoke` without editing any files. What is the command?",null,[`npx playwright test --grep "@smoke"`,`npx playwright test --tag smoke`,`npx playwright test --filter smoke`,`npx playwright test smoke.spec.ts`],0,"Add tags in test titles (e.g. `test('login @smoke', ...)`) and filter with `--grep \"@smoke\"`. Playwright supports full regex syntax here."],[`S`,`I`,'A developer runs `npx playwright test` and sees "Executable doesn\'t exist at ~/.cache/ms-playwright/…". What should they do?',null,["Run `npx playwright install` to download the missing browser binaries",`Reinstall Node.js`,`Set an explicit executablePath in config`,`Switch from @playwright/test to playwright`],0,"This error means the browser binaries were never downloaded. `npx playwright install` fetches them into `~/.cache/ms-playwright`."],[`S`,`A`,`A team needs both an HTML report and a JUnit XML report from the same test run. How should the config look?`,null,["`reporter: [['html'], ['junit', { outputFile: 'results.xml' }]]`","Run tests twice with different `--reporter` flags","Use `reporter: 'html+junit'`",`Configure two separate playwright.config files`],0,"The `reporter` array accepts multiple reporters simultaneously. Each entry is `[reporterName, optionalOptions]`."],[`S`,`I`,`Developers use both Windows and macOS. Should they maintain separate playwright.config.ts files?`,null,[`No — Playwright's API and config are fully cross-platform`,`Yes — path separators differ`,`Yes — Windows only supports Chromium`,`Yes — browser binaries differ per OS`],0,"Playwright handles OS differences internally. The same `playwright.config.ts` works on Windows, macOS, and Linux without modifications."],[`S`,`A`,"The `~/.cache/ms-playwright` folder is using several gigabytes. How can a team reduce its size?",null,["Install only the browsers actually used: `npx playwright install chromium`","Run `npm uninstall playwright`",`Delete node_modules and reinstall`,"Run `npx playwright clean`"],0,"Playwright stores browser binaries in `~/.cache/ms-playwright` (not node_modules). Installing only the browsers you test against (e.g. just Chromium) saves significant disk space."],[`O`,`B`,"What does the `testDir` option in playwright.config.ts control?",null,[`The directory where test results are saved`,`The root directory Playwright scans when discovering test files`,`The directory for browser binaries`,`The output directory for HTML reports`],1,`testDir tells Playwright where to look for test files — defaulting to "tests" or the directory containing playwright.config.ts.`],[`O`,`B`,"What is the purpose of the `baseURL` option in the `use` block?",null,[`It sets the API base URL for HTTP requests only`,`It allows using relative paths like page.goto('/login') instead of full URLs`,`It restricts tests to one origin domain`,`It sets the URL for the HTML report server`],1,`With baseURL set, page.goto('/login') resolves to baseURL + '/login', keeping tests portable across environments.`],[`O`,`I`,"What does `timeout: 30000` at the top level of playwright.config.ts configure?",null,[`The maximum time a single expect() assertion can wait`,`The maximum time each individual test is allowed to run before it is marked as timed out`,`The time Playwright waits for the browser to launch`,`The time between test retries`],1,`The top-level timeout is per-test — if a test body takes longer, Playwright aborts it and marks it failed.`],[`O`,`I`,"What is the difference between `timeout` and `expect.timeout` in playwright.config.ts?",null,[`They are aliases for the same setting`,`timeout limits the entire test duration; expect.timeout limits how long each web-first assertion retries before failing`,`expect.timeout is only used in API tests`,`timeout applies to navigation; expect.timeout applies to locators`],1,`A test might run for 30 s (timeout) while each individual assertion retries for 5 s (expect.timeout) — the two clocks run independently.`],[`O`,`B`,"What does `headless: false` in the `use` block do?",null,[`Disables screenshots`,`Opens a visible browser window during test execution`,`Disables JavaScript in the browser`,`Runs tests without network access`],1,`headless: false makes the browser window visible — useful for debugging tests interactively.`],[`O`,`I`,"What does the `--ui` flag do when running Playwright?",null,[`Opens the browser in headed mode only`,`Launches the Playwright UI Mode — an interactive test explorer with time-travel debugging and watch mode`,`Enables the UI-mode reporter for terminal output`,`Runs only tests tagged @ui`],1,`UI Mode (npx playwright test --ui) is a graphical test runner with a test list, trace viewer, and real-time re-run on file save.`],[`O`,`B`,`What environment variable enables Playwright Inspector for step-by-step debugging?`,null,[`DEBUG=playwright`,`PLAYWRIGHT_DEBUG=1`,`PWDEBUG=1`,`PW_INSPECT=true`],2,`PWDEBUG=1 pauses execution before the first Playwright action and opens the Inspector — you can step through actions and examine selectors.`],[`O`,`I`,"What does `forbidOnly: !!process.env.CI` do in playwright.config.ts?",null,[`Prevents running tests with the --only flag in CI`,`Causes the test run to fail if any test.only() or test.describe.only() is found committed to the repo — a safety net against accidentally merging exclusive tests`,`Disables test.only() globally in all environments`,`Prevents running focused tests locally`],1,`forbidOnly is a CI guard — developers who forget to remove .only() get a CI failure instead of silently only running one test.`],[`O`,`B`,"What is `testMatch` used for in playwright.config.ts?",null,[`Matching test titles to run`,`A glob or regex pattern that overrides the default file discovery — only files matching the pattern are treated as test files`,`Matching locators in selectors`,`Filtering tests by tag`],1,`testMatch lets you change the default *.spec.ts / *.test.ts naming convention to any pattern that fits your project.`],[`O`,`I`,"What does `globalSetup` in playwright.config.ts point to?",null,[`A function that runs before every test`,`A module that exports a function called once before the entire test suite starts — useful for starting servers or seeding databases`,`A shared beforeAll hook for all describe blocks`,`The auth setup project file`],1,`globalSetup runs once, in the main process, before any workers or tests start — ideal for expensive one-time preparation.`],[`O`,`A`,"What does `outputDir` in playwright.config.ts control?",null,[`The folder where test reports are written`,`The directory where test artifacts (screenshots, videos, traces) are saved`,`The directory scanned for test files`,`The directory for browser downloads`],1,`outputDir (default: test-results/) stores all run artifacts — Playwright cleans it before each run by default.`],[`F`,`B`,`Complete to set the base URL for all tests.
\`\`\`ts
export default defineConfig({
  use: {
    ______: 'http://localhost:3000',
  },
});
\`\`\``,null,[`baseURL`,`url`,`host`,`origin`],0,`baseURL in the use block lets page.goto('/path') resolve relative to that base.`],[`F`,`B`,"Complete to run tests in headed mode.\n```ts\nuse: {\n  ______: false,\n}\n```",null,[`headless`,`headed`,`visible`,`show`],0,`headless: false disables headless mode, opening a visible browser window.`],[`F`,`I`,"Complete to set a per-test timeout of 45 seconds.\n```ts\nexport default defineConfig({\n  ______: 45_000,\n});\n```",null,[`timeout`,`testTimeout`,`maxTime`,`duration`],0,`The top-level timeout option sets the maximum duration for each individual test.`],[`F`,`I`,"Complete the CLI flag to launch Playwright's interactive UI.\n```\nnpx playwright test ______\n```",null,[`--ui`,`--debug`,`--headed`,`--interactive`],0,`--ui launches UI Mode — an interactive test runner with time-travel tracing.`],[`F`,`B`,"Complete to enable retries only in CI.\n```ts\nretries: process.env.CI ? 2 : ______,\n```",null,[`0`,`1`,`undefined`,`false`],0,`0 retries locally means test failures are reported immediately without re-running.`],[`F`,`I`,"Complete to prevent committing test.only() to CI.\n```ts\nforbidOnly: !!process.env.______,\n```",null,[`CI`,`NODE_ENV`,`TEST`,`PLAYWRIGHT`],0,`process.env.CI is set by virtually all CI providers (GitHub Actions, GitLab CI, CircleCI, etc.).`],[`F`,`I`,"Complete to specify the global setup module.\n```ts\nexport default defineConfig({\n  ______: './global-setup.ts',\n});\n```",null,[`globalSetup`,`setup`,`beforeAll`,`setupFile`],0,`globalSetup points to a module whose default export is called once before the suite starts.`],[`E`,`B`,`What is wrong?
\`\`\`ts
import { test } from 'playwright';
test('my test', async ({ page }) => {
  await page.goto('https://example.com');
});
\`\`\``,null,[`test must be imported from @playwright/browser`,`Import should be from @playwright/test, not playwright — the lower-level playwright package does not export test or expect`,`page fixture must be imported separately`,`async is not needed for page.goto`],1,`Always import test and expect from @playwright/test — that is the test runner package.`],[`E`,`B`,`What is wrong with this config?
\`\`\`ts
export default defineConfig({
  use: { baseURL: 'http://localhost:3000' },
  timeout: '30s',  // string
});
\`\`\``,null,[`baseURL should not include a port`,`timeout must be a number in milliseconds, not a string like "30s"`,`defineConfig is not the correct function name`,`use block must come after timeout`],1,`timeout is always in milliseconds — use 30_000, not the string '30s'.`],[`E`,`I`,`What is wrong?
\`\`\`ts
// playwright.config.ts
export default defineConfig({
  projects: [
    { name: 'chrome', use: { browserName: 'google-chrome' } },
  ],
});
\`\`\``,null,[`projects must have at least two entries`,`browserName only accepts "chromium", "firefox", or "webkit" — not "google-chrome"`,`projects requires a testDir per entry`,`use must include viewport`],1,`Playwright's browserName option accepts the three engine names: chromium, firefox, webkit — not brand names like "google-chrome".`],[`E`,`I`,`What is wrong?
\`\`\`ts
// global-setup.ts
export default async function globalSetup() {
  await page.goto('/login'); // page is not defined
}
\`\`\``,null,[`globalSetup cannot be async`,`globalSetup does not receive a page fixture — it only receives the FullConfig object. To interact with a browser, create a browser instance manually with chromium.launch()`,`globalSetup must export a named function`,`page must be imported from @playwright/test`],1,`globalSetup runs before any workers start — there is no page fixture. Use the chromium/firefox/webkit launchers directly to spin up a browser if needed.`],[`E`,`B`,"What is wrong?\n```\n# CI command\nnpx playwright test --headed\n```",null,[`--headed is not a valid flag`,`Running headed in CI usually fails because CI environments have no display. Use headless mode (the default) or configure a virtual display (xvfb)`,`--headed requires --workers=1`,`CI requires --reporter=junit`],1,`Headed mode requires a graphical display — CI machines typically don't have one. Use the default headless mode or configure Xvfb.`],[`E`,`A`,`What is wrong?
\`\`\`ts
export default defineConfig({
  globalSetup: './setup.ts',
  globalTeardown: './setup.ts', // same file
});
\`\`\``,null,[`globalTeardown must be a different file`,`This is valid — a single file can export both a default (globalSetup) and a named globalTeardown export, but if the file only has a default export it will be used for setup only and teardown will be undefined`,`globalSetup and globalTeardown cannot share a module`,`The file must use .js extension`],1,`Using the same file is fine if it exports both functions correctly, but a common mistake is pointing both to a file with only a default export — teardown silently does nothing.`],[`E`,`I`,"What is wrong?\n```ts\nexport default defineConfig({\n  use: { video: true },\n});\n```",null,[`video cannot be in the use block`,`video accepts a mode string, not a boolean: use "on", "off", "retain-on-failure", or "on-first-retry"`,`video must be set per-project, not globally`,`video: true requires trace: true`],1,`video expects a mode string — "on" records all tests, "retain-on-failure" only keeps recordings for failing tests.`],[`D`,`B`,`Tests fail with "browserType.launch: Executable doesn't exist" in CI. What is the fix?`,null,[`Add Playwright to devDependencies`,"Add `npx playwright install --with-deps` as a CI step before running tests — this downloads browser binaries and OS-level dependencies",`Set executablePath in the config`,`Use Docker with playwright pre-installed`],1,`Browser binaries are not in node_modules — they must be installed separately with playwright install. --with-deps also installs OS libraries required by the browsers.`],[`D`,`B`,"A test passes with `timeout: 30000` locally but times out in CI. What should you try?",null,[`Reduce assertions in the test`,"Increase timeout for CI: `timeout: process.env.CI ? 60000 : 30000` — CI machines are often slower and network latency to staging is higher",`Use retries instead of a longer timeout`,`Switch to webkit which is faster`],1,`CI environments run on shared, sometimes slower hardware with network overhead — a longer timeout for CI is the pragmatic first step while investigating root causes.`],[`D`,`I`,'Running `npx playwright test` shows "No tests found" even though .spec.ts files exist. What should you check?',null,[`Re-install the @playwright/test package`,`Check testDir in playwright.config.ts — if it points to the wrong directory, or testMatch does not match your file names, Playwright finds nothing`,`Rename files to .test.ts extension`,`Add --all flag to the command`],1,`The most common cause is a testDir mismatch or a custom testMatch that does not cover the actual file paths.`],[`D`,`I`,`The HTML report is not generated after a test run. What is the likely config issue?`,null,[`html reporter requires fullyParallel: true`,`The reporter is not configured — add reporter: "html" or reporter: [["html", { open: "never" }]] to playwright.config.ts`,`HTML report requires a web server running`,`reporter must be set per project`],1,`Without explicit reporter config, Playwright uses the "list" reporter which only outputs to the terminal.`],[`D`,`A`,'`globalSetup` throws but the error message is "globalSetup timed out". How do you debug it?',null,[`Add retries: 3 to config`,`Wrap globalSetup logic in try/catch and log errors explicitly — Playwright's timeout wrapper swallows the original error. Also set a longer timeout with PLAYWRIGHT_GLOBAL_TIMEOUT env variable`,`Use PWDEBUG=1 with globalSetup`,`Switch globalSetup to a beforeAll hook`],1,`The timeout error masks the real cause — explicit error logging in globalSetup is essential to see what actually failed.`],[`D`,`I`,`Tests discover too many files including node_modules and dist. What config option fixes this?`,null,[`Add .playwrightignore file`,'Use testIgnore to exclude patterns: `testIgnore: ["**/node_modules/**", "**/dist/**"]`',`Delete node_modules before running tests`,`Use testMatch to list only specific files`],1,`testIgnore accepts glob patterns — paths matching it are excluded from test discovery even if they match testDir.`],[`C`,`B`,"Complete to set the artifacts output folder.\n```ts\nexport default defineConfig({\n  ______: 'test-artifacts',\n});\n```",null,[`outputDir`,`artifactsDir`,`resultDir`,`reportDir`],0,`outputDir sets the directory for screenshots, videos, and traces produced during the run.`],[`C`,`I`,"Complete to configure slow motion between actions.\n```ts\nuse: {\n  ______: 500, // 500ms delay between actions\n}\n```",null,[`slowMo`,`delay`,`actionDelay`,`waitTime`],0,`slowMo adds a fixed millisecond delay after every Playwright action — useful for visually observing test execution.`],[`C`,`I`,"Complete to set the viewport size globally.\n```ts\nuse: {\n  viewport: { width: ______, height: 768 },\n}\n```",null,[`1280`,`800`,`375`,`1920`],0,`1280×768 is a common desktop viewport; use 375×812 for mobile simulation.`],[`C`,`B`,"Complete to point to the global teardown module.\n```ts\nexport default defineConfig({\n  globalTeardown: '______',\n});\n```",null,[`./global-teardown.ts`,`./teardown`,`./cleanup`,`./after-all.ts`],0,`Any relative path to a module that exports a default async function works — the name is your convention.`],[`C`,`I`,`Complete to keep test artifacts only on failure.
\`\`\`ts
use: {
  screenshot: '______',
  video: 'retain-on-failure',
}
\`\`\``,null,[`only-on-failure`,`on-failure`,`failure`,`always-on-failure`],0,`"only-on-failure" saves a screenshot only when a test fails — identical to "retain-on-failure" for video.`],[`L`,`B`,`Why does Playwright recommend keeping browser binaries out of node_modules?`,null,[`To keep node_modules small and portable — browser binaries are large OS-specific executables that change per Playwright version and belong in a separate cache`,`Browser binaries are not compatible with npm's module resolution`,`node_modules is read-only on most systems`,`npm cannot handle binary files`],0,`The separate cache (~/.cache/ms-playwright) allows multiple projects to share the same binaries and simplifies updates.`],[`L`,`I`,"What is the difference between `testMatch` and `testDir`?",null,[`They are identical`,`testDir is the root directory to scan; testMatch is a glob/regex that filters which files within that directory are treated as tests — both are needed together`,`testMatch replaces testDir when set`,`testDir is for unit tests; testMatch is for e2e tests`],1,`testDir scopes the search; testMatch (default: **/*.spec.ts, **/*.test.ts) determines which found files are treated as test files.`],[`L`,`I`,"Why should `forbidOnly` always be `true` in CI but not locally?",null,[`test.only() crashes local browsers`,`Locally test.only() is useful for focusing on one test during development; in CI it would silently skip all other tests — a merged .only() would mean only one test protects the entire codebase`,`forbidOnly slows down test discovery`,`test.only() is deprecated and forbidden everywhere`],1,`The goal is developer ergonomics locally + full coverage in CI — forbidOnly enforces that .only() never lands in the main branch.`],[`L`,`B`,`Why does Playwright default workers to half the CPU count rather than the full count?`,null,[`Playwright workers are single-threaded so more than half are wasted`,`Each Playwright worker also launches a browser process — using all cores would leave no headroom for browser rendering, causing slower overall throughput`,`Playwright's license limits the number of workers`,`Half the count is the minimum for cross-platform compatibility`],1,`Workers + their browsers compete for the same cores — half-core allocation gives each worker+browser pair one core, which is generally optimal.`],[`L`,`A`,"What happens if `globalSetup` throws an error?",null,[`Tests still run but global setup is skipped`,`The entire test suite is aborted before any test runs — globalSetup failure is treated as a fatal error`,`Only tests in the first project are affected`,`globalSetup errors are caught and logged as warnings`],0,`If setup cannot complete, there is no point running tests that depend on it — Playwright aborts immediately with the error.`],[`R`,`B`,`Your dev server must be running before tests start. How do you configure this?`,null,[`Start it manually before running npx playwright test`,`Use the webServer option in playwright.config.ts: { command: "npm run dev", url: "http://localhost:3000" } — Playwright starts the server and waits for it to respond`,`Add a beforeAll hook in each test file`,`Use globalSetup to start the server`],1,`webServer is the dedicated option for this — it starts the process, waits for the URL to respond, and tears it down after the suite.`],[`R`,`I`,`You want to test both desktop (1280×800) and mobile (375×812) in one suite. How?`,null,[`Write two separate test suites`,`Add two projects: one with viewport: { width: 1280, height: 800 } and one with viewport: { width: 375, height: 812 } — Playwright runs all tests against both`,`Use test.use() in every test file`,`Pass --viewport to the CLI for each run`],1,`Projects are the right tool for dimension variants — each test runs twice, once per project, with no duplication.`],[`R`,`I`,`A test file is growing unwieldy. How do you organize tests into named groups without splitting files?`,null,[`Use test.group() (a Playwright built-in)`,`Use test.describe("Group Name", () => { ... }) to nest tests under a logical label — groups appear as collapsible sections in the HTML report`,`Create subdirectories and one file per test`,`Use // @group annotations in comments`],1,`test.describe() creates hierarchical grouping — reports show "GroupName > testTitle" making navigation easy.`],[`R`,`A`,`Your project has both API tests and browser tests. How do you give them different timeouts?`,null,[`API tests must use the global timeout`,`Create two projects — one for browser tests (timeout: 30000) and one for API tests (timeout: 10000) — each project can override timeout in its use block`,`Use test.setTimeout() in every API test file`,`Set timeout dynamically inside each test`],1,`Per-project timeout configuration is the scalable approach — API tests typically need less time than browser tests.`],[`R`,`B`,`You want to run a specific file without running the full suite. What is the command?`,null,[`npx playwright test --only tests/login.spec.ts`,`npx playwright test tests/login.spec.ts`,`npx playwright test --file=login`,`npx playwright test --include login`],1,`Passing a file path directly to the CLI restricts the run to that file — you can also pass a directory.`],[`S`,`B`,`A teammate checked in a test.only() and CI is passing but all other tests were skipped. How do you prevent this?`,null,[`Add a git hook that blocks commits with test.only`,`Set forbidOnly: !!process.env.CI in playwright.config.ts — this fails the CI run immediately if any test.only() is found, forcing the developer to remove it before merging`,`Use --grep to exclude .only tests`,`Disable test.only() in eslint`],1,`forbidOnly is the purpose-built solution — zero-config once added, prevents the problem at CI gate time.`],[`S`,`I`,`Your Playwright tests are flaky in CI but reliable locally. List two configuration improvements to try first.`,null,[`Rewrite tests and add retries`,`1) Add retries: process.env.CI ? 2 : 0 for automatic re-runs of flaky tests; 2) Add trace: "on-first-retry" so you get a trace for the first retry and can diagnose the root cause in the Trace Viewer`,`Switch to a different browser engine`,`Increase workers to reduce total run time`],1,`Retries buy tolerance while trace captures the evidence — together they handle short-term flakiness while enabling long-term investigation.`],[`S`,`A`,`You are setting up Playwright in a monorepo where the app lives in packages/webapp. How do you configure testDir and webServer together?`,null,[`You must run Playwright from the webapp directory`,`Set testDir: "packages/webapp/tests" and webServer.command: "npm run dev --workspace=packages/webapp" — Playwright resolves paths relative to the config file location`,`Monorepos require a separate playwright.config.ts per package`,`testDir cannot be outside the current directory`],0,`Playwright resolves all relative paths from the config file — point testDir and webServer at the correct sub-package paths.`],[`S`,`I`,`A new team member runs the tests and gets a different number of tests than the rest of the team. What two config areas should you check?`,null,[`Check their Node.js version`,`1) Check testDir and testMatch — they may have a different playwright.config.ts or a local override. 2) Check if any test.only() is active in their local branch — that would limit which tests run`,`Check their browser version`,`Ask them to reinstall node_modules`],1,`testDir/testMatch discrepancy and accidental test.only() are the two most common causes of differing test counts between developers.`],[`L`,`A`,`What is the core advantage of Playwright's Trace Viewer over reading console logs or screenshots alone when diagnosing a CI failure?`,null,[`A trace captures a full timeline of DOM snapshots, network activity, console logs, and action-by-action screenshots for the entire test — letting you 'time travel' through exactly what the browser saw at each step`,`Trace Viewer re-runs the test live against the CI server`,`Traces only capture the final failure screenshot, nothing before it`,`Trace Viewer requires a paid Playwright Cloud subscription`],0,"`trace: 'on-first-retry'` (or 'retain-on-failure') captures this rich, replayable timeline specifically for failed runs — invaluable for diagnosing flaky CI-only failures."],[`O`,`I`,`With this config, when is a trace file actually saved to disk?`,`// playwright.config.ts
export default defineConfig({
  use: { trace: 'retain-on-failure' },
});`,[`Only for tests that ultimately fail — passing tests generate no trace, saving disk space and CI artifact upload time`,`For every test, pass or fail`,`Only when running in headed mode`,`Only on the very first test in the suite`],0,`'retain-on-failure' records a trace for every test but discards it if the test passes.`],[`D`,`B`,`A developer wants to pause a test mid-run and manually step through it, inspecting selectors live. What is the quickest way to do this?`,null,["Run the test with `PWDEBUG=1 npx playwright test`, which opens the Playwright Inspector and pauses before the first action, letting you step through and use the selector picker",`Add console.log() after every line and re-run repeatedly`,"Manually insert `await page.waitForTimeout(60000)` everywhere you want to pause",`There is no way to pause mid-test in Playwright; you must use browser DevTools separately`],0,`PWDEBUG=1 is the fastest path to interactive step-through debugging — no code changes needed.`],[`R`,`I`,`A test fails only in CI, never locally, and you don't have direct terminal access to the CI runner. What's the most efficient way to diagnose it without adding print statements and re-running the pipeline repeatedly?`,null,["Configure the CI job to upload the Playwright HTML report and trace/video artifacts on failure, then download and open them locally with `npx playwright show-report` / `npx playwright show-trace`",`SSH into the CI runner and attach a live debugger, which Playwright supports natively`,`Increase console.log verbosity and read raw CI logs line by line as the only option`,`Reproducing the exact CI OS/hardware locally is the only reliable method`],0,`Playwright's HTML report and trace files are portable artifacts — downloading them from CI gives you the same rich debugging experience as running locally.`],[`O`,`I`,"What does this `projects` array accomplish?",`export default defineConfig({
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'Mobile Safari', use: { ...devices['iPhone 13'] } },
  ],
});`,[`Every test file runs twice — once emulating desktop Chrome, once emulating an iPhone 13 in Safari — as two independent test matrix entries`,`It runs tests only on whichever project is listed first`,`It merges both configurations into a single hybrid browser`,`projects only affects reporting labels, not actual execution`],0,"Each entry in `projects` is a full test run configuration — Playwright multiplies your suite across every project by default."],[`L`,`I`,"What is the difference between the global `retries` setting and re-running a flaky test manually?",null,["`retries` tells Playwright to automatically re-run a FAILING test up to N more times within the same run, and reports it as 'flaky' if a retry passes — manual re-runs require a human to notice and trigger them",`They are functionally identical`,`retries only applies to assertion failures, never to timeouts`,`Setting retries disables all other failure reporting`],0,`Automatic retries are a built-in mechanism for surfacing and tolerating occasional flakiness without manual intervention — while still reporting it as "flaky", not silently "passed".`],[`O`,`I`,`What does this reporter configuration produce?`,`export default defineConfig({
  reporter: [['list'], ['html'], ['json', { outputFile: 'results.json' }]],
});`,[`Three simultaneous outputs from the same run: live console output (list), a browsable HTML report, and a machine-readable JSON results file`,`Only the last reporter in the array actually runs`,`Playwright errors because only one reporter is allowed at a time`,`The three reporters run the test suite three separate times`],0,`The reporter option accepts an array specifically so multiple output formats can be generated from a single test run — common for combining human-readable and CI-machine-readable outputs.`],[`O`,`A`,"What does the `webServer` config option achieve?",`export default defineConfig({
  webServer: {
    command: 'npm run start',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
});`,["Playwright automatically starts the app's dev server before running tests (and shuts it down after), waiting until the URL responds — avoiding the need to manually start the app before running `npx playwright test`",`It proxies all test requests through a mock server instead of the real app`,`It only works if the server is already running; Playwright never starts anything`,`reuseExistingServer forces Playwright to always start a brand-new server, even if one is running`],0,"`webServer` is designed exactly for CI convenience — one command runs the whole pipeline: start app, wait for it to be ready, run tests, tear it down."],[`R`,`I`,`Your tests need different API base URLs and credentials depending on whether they run against staging or production-like environments. What is the idiomatic way to manage this in Playwright?`,null,["Read environment variables (e.g., `process.env.BASE_URL`) in playwright.config.ts and set them per CI job or via a local `.env` file (loaded with a package like dotenv) — never hard-code environment-specific values into the config itself",`Maintain a fully separate copy of every test file per environment`,`Hard-code the staging URL directly and manually find-and-replace before each production run`,`Environment differences must be handled entirely inside each individual test file`],0,`Environment variables are the standard mechanism for keeping a single test codebase portable across environments without duplicating logic or config.`],[`D`,`I`,`A visual regression test fails in CI right after a legitimate, intentional design change, and the team wants to accept the new appearance as the new baseline. What command accomplishes this?`,null,["`npx playwright test --update-snapshots` — regenerates the stored baseline screenshots from the current (correct) rendering, which should then be reviewed and committed",`Delete the entire test file and rewrite it from scratch`,`Manually edit the PNG file's binary content by hand`,`Increase maxDiffPixels until the old baseline matches by coincidence`],0,`--update-snapshots is the standard workflow step after confirming a visual diff represents an intentional change, not a regression.`],[`L`,`I`,"What is the difference between the global `timeout` setting and the `expect.timeout` setting in playwright.config.ts?",null,["`timeout` bounds how long an ENTIRE test is allowed to run before being marked failed; `expect.timeout` bounds how long an individual web-first assertion (like toBeVisible()) will keep RETRYING before failing — they operate at different granularities",`They are exact synonyms; either can be used interchangeably`,`expect.timeout applies to the whole test suite, while timeout applies to a single assertion`,`Only one of the two settings can be active at a time`],0,`A single test can have a 30s overall timeout while individual assertions inside it each retry for up to 5s — the two settings answer different questions.`],[`L`,`A`,`Why is sharding (--shard) commonly combined with the blob reporter rather than the default HTML reporter in CI?`,null,[`Blob reports from each shard can be merged into a single unified HTML report afterward, whereas separate HTML reports from each shard cannot be combined into one view`,`The blob reporter runs tests faster than the HTML reporter`,`HTML reporter does not support parallel test execution at all`,`Blob is required because HTML reporter crashes when --shard is used`],0,"Each shard normally only sees a slice of the suite, so its own HTML report would show partial results — `npx playwright merge-reports` combines blob outputs into one complete report."],[`L`,`A`,"Why can setting `fullyParallel: true` sometimes surface test-isolation bugs that were previously hidden?",null,[`Because tests within the same file now run concurrently instead of sequentially, so any shared mutable state between tests in that file is exercised simultaneously instead of one-at-a-time`,`Because fullyParallel disables the context fixture entirely`,`Because it forces every test to share a single page object`,`Because it turns off retries, so flaky failures become permanent`],0,`By default, tests in one file run in order on one worker; fullyParallel breaks that ordering guarantee, which can expose accidental dependencies between tests.`],[`L`,`I`,"What is the practical difference between `globalTimeout` and the per-test `timeout` option in playwright.config.ts?",null,[`globalTimeout caps the total wall-clock duration of the entire test run across all tests and workers; timeout caps how long any single test is allowed to run`,`They are synonyms and either can be used interchangeably`,`globalTimeout only applies to globalSetup, not to tests`,`timeout applies to the whole run; globalTimeout applies to a single test`],0,`globalTimeout is a safety net for the whole run (e.g. abort after 1 hour no matter what), independent of how many individual tests pass or fail.`],[`L`,`A`,"Why might increasing `workers` beyond the number of available CPU cores actually reduce total test suite throughput?",null,[`Each worker launches its own browser process, so once cores are saturated, additional workers compete for the same CPU time, causing context switching overhead that slows every test down`,`Playwright hard-caps effective workers at the core count and silently ignores extras, so there is no effect at all`,`Extra workers automatically disable headless mode, which is slower`,`Playwright charges a per-worker licensing fee that throttles execution above the core count`],0,`Worker count should generally match or stay below available cores — oversubscribing CPU with more browser processes than cores creates contention, not speed.`],[`L`,`I`,"Why does the official Playwright Docker image use tags like `mcr.microsoft.com/playwright:v1.49.0-jammy` with an exact version instead of `:latest`?",null,["The bundled browser binaries must exactly match the `@playwright/test` npm package version installed in your project — a mismatched image could ship browsers incompatible with your test code","Docker Hub does not allow a `latest` tag for Microsoft-published images","The `-jammy` suffix only works with pinned versions, never with latest",`Using latest would require rebuilding the image on every git commit`],0,`Playwright pairs a specific browser build tightly with each release — an image built for a different version can cause obscure protocol mismatches.`],[`L`,`I`,"What risk does running `npm update @playwright/test` introduce if you do not also re-run browser installation afterward?",null,[`The newly updated test runner may expect newer browser binaries than the ones already cached locally, causing launch failures or protocol mismatches`,`npm update automatically breaks your playwright.config.ts syntax`,`It has no risk — @playwright/test and browser binaries are always version-independent`,`It silently reverts your config file to defaults`],0,"Always follow a package update with `npx playwright install` to keep the browser binaries in sync with the new test runner version."],[`L`,`I`,"Why does Playwright allow `workers: '50%'` as a string percentage instead of only accepting an integer?",null,[`It lets teams scale worker count proportionally to whatever CPU count a given CI runner happens to have, without hardcoding a machine-specific number`,`Percentages run faster than integers due to internal rounding`,`String values disable retries automatically`,`It is required syntax for Docker-based CI runners`],0,`A percentage adapts automatically across differently sized CI machines, whereas a hardcoded integer might over- or under-utilize a given runner.`],[`L`,`I`,"Why might a team configure `maxFailures` for CI runs but leave it unset during local development?",null,[`In CI, stopping early after a threshold of failures saves compute time and gives faster feedback on a broken build; locally, developers usually want to see every failure while iterating`,`maxFailures is only supported in CI environments, not on local machines`,`Setting maxFailures disables the HTML reporter`,`Local runs always ignore the maxFailures setting by default`],0,"`maxFailures: 10` aborts the run once 10 tests have failed — ideal for cutting a doomed CI job short, less useful when debugging locally where full visibility helps."],[`L`,`B`,"Why is `npx playwright test --list` useful before committing a change to `testMatch` or `testIgnore`?",null,[`It prints every test that would be discovered and run, without actually executing any of them — letting you verify your pattern change had the intended effect`,`It lists all previously failed tests from the last run only`,`It installs any missing browsers before listing tests`,`It shows only tests currently marked with test.only()`],0,`--list is a fast, side-effect-free way to sanity-check test discovery before actually running (and waiting on) the full suite.`],[`L`,`I`,"Why does `test.describe.configure({ retries: N })` exist when a global `retries` setting is already available in playwright.config.ts?",null,[`It lets you override the retry count for just one describe block — e.g. giving a known-flaky group of tests more retries without inflating the retry budget for the entire suite`,`It is a deprecated alias for the global retries setting with no additional behavior`,`It only works when the global retries setting is 0`,`describe-level retries apply only in headed mode`],0,`Per-describe configuration lets you scope tuning (retries, timeout, mode) to the tests that actually need it, rather than changing behavior suite-wide.`],[`L`,`I`,"Why is the `--repeat-each` CLI flag primarily a diagnostic tool rather than something used in routine CI runs?",null,[`It reruns every matched test multiple times in the same invocation, which is useful for confirming whether a test is genuinely flaky, but multiplies total run time and is too slow for everyday CI`,`It repeats only the last failed test from the previous run`,`It permanently modifies the test file to add duplicate test() calls`,`--repeat-each only works with test.describe.serial() blocks`],0,`Running a suspect test 20 times with --repeat-each quickly reveals intermittent failures that a single run might miss — but doing this for the whole suite every time would be wasteful.`],[`L`,`A`,"Why can the `reportSlowTests` config option help surface application performance regressions, not just slow test code?",null,[`Because a sudden jump in a test's duration often reflects the page or API under test genuinely getting slower, and reportSlowTests highlights the top offenders in the terminal output after every run`,`Because it automatically profiles the browser's JavaScript engine and reports memory leaks`,`Because it disables retries for tests that are flagged as slow`,`Because it deletes slow tests automatically to keep the suite fast`],0,`reportSlowTests({ max, threshold }) is primarily meant to draw attention to tests whose duration creeps up over time — often the first visible sign of a real regression.`],[`L`,`I`,"Why does `preserveOutput` matter for disk usage on long-running, self-hosted CI agents?",null,[`It controls whether artifacts (screenshots, videos, traces) from every test run are kept on disk or only from failing tests, directly affecting how much storage accumulates over repeated runs`,`It controls whether npm caches are preserved between builds`,`It determines whether the browser binaries are preserved after each test run`,`It only affects whether console.log output is preserved in the report`],0,'`preserveOutput: "failures-only"` avoids accumulating gigabytes of passing-test artifacts on agents that run thousands of builds.'],[`L`,`I`,"Why is `--grep-invert` (or config `grepInvert`) useful alongside `--grep`?",null,[`It lets you run every test EXCEPT those matching a pattern — e.g. temporarily excluding a known-flaky tagged subset while still running everything else`,`It inverts the pass/fail result of matched tests`,`It reverses the execution order of matched tests`,`It only works when combined with test.describe.serial()`],0,`grepInvert is the complement of grep — matching becomes exclusion instead of inclusion, handy for quarantining a subset without deleting or skipping it in source.`],[`L`,`A`,"Why do monorepos often need one `playwright.config.ts` per package (run via `--config`) rather than a single shared config at the repo root?",null,[`Each package may have its own testDir, baseURL, webServer command, and dependency setup, so a single config would force awkward compromises or excessive per-project overrides`,`Playwright technically forbids more than one playwright.config.ts existing in the same repository`,`The --config flag only works with exactly one config file per machine, ever`,`A shared config would automatically merge all packages' tests into a single project, ignoring folder boundaries`],0,"Per-package configs keep testDir, webServer, and environment-specific settings clean and independently runnable, using `npx playwright test --config=packages/app/playwright.config.ts`."],[`L`,`I`,"Why does the `metadata` field in defineConfig help when integrating Playwright results with external dashboards or CI systems?",null,[`It attaches arbitrary key-value data (e.g. build number, git commit, environment name) to the run, which custom or third-party reporters can read and surface alongside test results`,`It configures which metadata tags are stripped from screenshots before upload`,`It is required for the HTML reporter to render correctly`,`It controls the metadata embedded in browser binary downloads`],0,`metadata is free-form context you control — reporters and dashboards can pull it to correlate a Playwright run with the exact build or deployment that produced it.`],[`L`,`A`,"Why might `snapshotPathTemplate` need customizing in a project where multiple `projects` entries run the same spec file against different browsers?",null,[`Without customization, snapshot file names can collide or become hard to distinguish per-project; a custom template can embed the project name into the path to keep chromium/firefox/webkit baselines separate and organized`,`snapshotPathTemplate is required only when using visual comparisons on Windows`,`It controls where trace.zip files are written, not screenshots`,`Snapshots are always stored in a single shared file regardless of this setting`],0,`The default template already includes the project name, but teams with unusual folder conventions often need to customize it to match existing snapshot directory layouts.`],[`L`,`I`,"Why is `quiet: true` typically paired with a machine-readable reporter like `json` or `junit` in CI pipelines?",null,[`quiet suppresses stdout/stderr output from the tests themselves, keeping CI logs clean, while the machine-readable reporter still captures full structured results for downstream tooling to parse`,`quiet disables all reporters except junit`,`quiet and json cannot be used separately; they must be combined`,`quiet only affects the HTML reporter's console messages`],0,`You still get complete result data via the structured reporter file — quiet just declutters the live console output during the run.`],[`L`,`A`,"Why does using `channel: 'chrome'` in a project's `use` block typically require an additional install step beyond the default `npx playwright install`?",null,["The default install only downloads Playwright's bundled Chromium build; the real branded Chrome/Edge browsers must be fetched separately with `npx playwright install chrome` (or msedge)",`channel: chrome requires a paid Playwright Cloud license`,`channel automatically triggers the OS package manager to install Chrome, so no Playwright command is needed`,`channel is only usable in headed mode and needs no separate install`],0,`Bundled Chromium and the real branded browsers are downloaded separately — install the specific channel by name if your config references it.`],[`L`,`A`,'Why would a mature test suite enable `--fail-on-flaky-tests` even though a flaky test technically "passes" after a retry?',null,[`A test that only passes after retrying still indicates an underlying reliability problem worth fixing — failing the CI build on flakiness forces the team to address root causes instead of quietly tolerating them forever`,`--fail-on-flaky-tests actually disables retries entirely`,`It is required for the HTML reporter to display flaky test counts`,`It only affects tests tagged with @flaky in their title`],0,`Retries are meant as a short-term tolerance mechanism, not a permanent fix — this flag turns flakiness into a visible, actionable CI failure.`],[`L`,`B`,"Why should `.env` files typically be excluded from version control while a `.env.example` file is committed instead?",null,["`.env` often contains secrets or machine-specific values; `.env.example` documents the expected variable names and format without exposing sensitive data, letting new contributors set up their own local `.env`",`Playwright refuses to load any .env file that is tracked by git`,`.env.example is required for the config to type-check correctly`,`.env files are automatically deleted by npm install`],0,`This is a general Node.js convention, not Playwright-specific, but it directly affects how a team manages baseURL, credentials, and API keys used by tests.`],[`L`,`I`,"Why does Playwright support writing `playwright.config.ts` in both CommonJS (`require`) and ESM (`import`) styles?",null,[`It adapts to whatever module system the surrounding Node.js project already uses (based on package.json "type" field or the .mts/.cts extension), so teams do not need to restructure their whole project just to add Playwright`,`ESM support was added only for browser-side test code, not config files`,`CommonJS support was fully removed in recent Playwright versions`,`The two styles produce functionally different test runners`],0,`Playwright detects and works with your project's existing module system rather than forcing a specific one on the config file.`],[`L`,`I`,"What is the distinction between `use.actionTimeout` and `use.navigationTimeout` in playwright.config.ts?",null,[`actionTimeout bounds how long an individual action like click() or fill() waits before failing; navigationTimeout bounds how long page.goto() or similar navigations are allowed to take — both are narrower than the overall test timeout`,`They are aliases for the same underlying setting`,`actionTimeout only applies to keyboard actions; navigationTimeout applies to mouse actions`,`navigationTimeout replaces the need for a test-level timeout entirely`],0,`These two timeouts let you tune slow-but-expected operations (like a heavy page load) differently from fast interactive actions (like a click), without touching the whole-test timeout.`],[`L`,`A`,"Why might a project prefer a `teardown` project (paired with a `setup` project via `dependencies`) over a `globalTeardown` file for cleaning up test data?",null,[`A teardown project runs with full access to fixtures and browser APIs like a normal test, making it easier to, say, log in and delete data through the UI or API — globalTeardown runs outside the fixture system entirely`,`globalTeardown cannot be asynchronous, but a teardown project can`,`teardown projects run before the tests instead of after`,`globalTeardown only works when retries are disabled`],0,`Project-based setup/teardown pairs get the same rich fixture ecosystem as regular tests, which is often more convenient than the bare-bones globalSetup/globalTeardown API.`],[`L`,`I`,"Why does `testIdAttribute` matter for teams whose HTML markup uses `data-qa` or `data-cy` instead of the Playwright default `data-testid`?",null,["Setting `use: { testIdAttribute: 'data-qa' }` tells `page.getByTestId()` to look for that custom attribute instead of the default, avoiding a rewrite of the app's markup just to match Playwright's convention",`testIdAttribute must always be data-testid; it cannot be changed`,`It only works if data-qa attributes are unique across the entire app`,`Changing testIdAttribute requires a plugin outside core Playwright`],0,`This config setting bridges an existing test-attribute convention (from Cypress migration, for example) with Playwright's getByTestId locator.`],[`O`,`I`,"Why should downloading Playwright's browser binaries typically be a separate, cached CI step from `npm ci`?",null,[`Browser binaries are large and do not change between most commits, so caching them separately (keyed on the Playwright version) avoids re-downloading hundreds of megabytes on every single CI run`,`npm ci automatically fails if browsers are installed in the same step`,`Browser installation must run before Node.js is installed`,`Combining the steps causes package.json to be corrupted`],0,`Separating and caching the install step (keyed by Playwright version) is one of the most effective ways to speed up repeated CI runs.`],[`L`,`A`,"Why does `--ipc=host` matter when running Chromium-based Playwright tests inside a Docker container?",null,["Chromium uses shared memory for inter-process communication, and Docker's small default `/dev/shm` size can cause the browser to crash under normal test load unless a larger shared memory segment is provided (e.g. via --ipc=host or --shm-size)",`--ipc=host is required for Playwright to detect installed browsers inside the container`,`It disables the container's network namespace, which speeds up navigation`,`It has no real effect and is a legacy Docker flag from older Playwright versions`],0,`Insufficient shared memory is one of the most common causes of mysterious Chromium crashes specifically inside containerized environments.`],[`L`,`I`,"Why is it good practice to pin exact versions of `@playwright/test` in package.json rather than a caret range like `^1.49.0`?",null,["A caret range could silently pull in a newer minor version with a different bundled browser build on a fresh `npm install`, causing CI and local environments to diverge unexpectedly",`Caret ranges are not supported at all by the npm registry for this package`,`Exact versions are required for TypeScript type declarations to load`,`Pinning versions disables automatic security patches entirely, which is intentional`],0,`Playwright's API and bundled browsers evolve together — an unplanned upgrade from a caret range can introduce subtle behavior changes across a team without anyone noticing.`],[`L`,`A`,"Why might a team set `PLAYWRIGHT_BROWSERS_PATH=0` when building a CI Docker image?",null,[`It forces browsers to install directly inside node_modules instead of the shared user-level cache, making the browser binaries part of a single self-contained, reproducible Docker layer`,`It disables browser installation entirely, forcing manual downloads`,`It sets the browser cache to expire after zero days`,`It is required for headless mode to function`],0,`Some Docker build strategies prefer everything self-contained within node_modules rather than relying on an external OS-level cache path.`],[`L`,`A`,"Why does merging blob reports from multiple shards (via `npx playwright merge-reports`) produce a materially better result than simply looking at each shard's output separately?",null,[`A merged report reconstructs the full picture of the entire suite — total pass/fail/flaky counts and a single browsable timeline — instead of forcing someone to manually cross-reference four or more partial, disconnected result sets`,`Merging automatically re-runs any failed tests from each shard`,`It is purely cosmetic and provides no additional information beyond individual shard reports`,`Merged reports only work for the json reporter, not html`],0,`Sharding splits work across machines for speed, but humans still need one coherent view of the results — merge-reports reconstructs that single view.`],[`L`,`I`,"Why is `dotenv` typically imported explicitly at the top of `playwright.config.ts` (e.g. `import 'dotenv/config'`) rather than relying on Playwright to auto-load a `.env` file?",null,["Playwright does not automatically read `.env` files itself — that behavior comes entirely from the dotenv package, which must be installed and invoked explicitly before `process.env` values are used in the config",`dotenv is a built-in part of @playwright/test and only needs enabling via a config flag`,`Auto-loading .env files is disabled by default for security and must be re-enabled per project`,`Environment variables can only be read inside test files, never inside the config`],0,"This is a common point of confusion — Playwright reads `process.env` like any Node.js program, but populating it from a `.env` file requires a separate tool like dotenv."],[`L`,`A`,`Why does Playwright shard a test suite by splitting whole test FILES across machines, rather than splitting individual test cases within a file?`,null,[`Keeping all tests from one file together on a single shard preserves file-scoped fixtures, hooks (like beforeAll), and serial/parallel mode semantics that would otherwise break if a describe block's tests were split across separate machines`,`File-level sharding is simply faster to compute at startup`,`Playwright cannot detect individual test boundaries until after they finish running`,`Splitting by individual test is technically impossible in Node.js`],0,`A file often shares beforeAll setup or a serial describe block — splitting those tests across different shards/machines would break that shared context.`],[`L`,`I`,"Why would a team intentionally set `workers: 1` for an entire CI job, accepting slower total run time?",null,[`Certain test suites have unavoidable shared state (e.g. a single seeded database) that makes true parallel execution unsafe; running with one worker sacrifices speed for correctness and easier-to-read sequential logs`,`workers: 1 is required whenever trace: "on" is enabled`,`Single-worker mode is the only way to generate an HTML report`,`It is the default recommended setting for all production CI pipelines`],0,`workers: 1 is a pragmatic (if slower) fallback for suites that are not yet safely parallelizable, rather than the general recommendation.`],[`L`,`I`,"Why is `--headed` almost always combined with `--workers=1` during local interactive debugging?",null,[`With multiple workers, several visible browser windows would open and run simultaneously, making it hard to visually follow any single test — restricting to one worker keeps the visible run sequential and easy to watch`,`--headed silently ignores the --workers flag and always forces one worker anyway`,`Headed mode is incompatible with more than one worker and Playwright will error out otherwise`,`--workers=1 is required to enable the browser's DevTools panel`],0,`This is a practical debugging convention, not a hard technical requirement — but watching four browser windows race each other is rarely useful.`],[`L`,`B`,"Why does the Playwright VS Code extension not eliminate the need for a dedicated `npx playwright test` step in CI?",null,[`The extension is an interactive, local development tool for running/debugging tests from the editor; CI pipelines need a headless, scriptable, machine-triggered execution path that the extension does not provide`,`The extension only supports Firefox, not Chromium or WebKit`,`CI servers cannot have Node.js installed, so the CLI is required instead`,`The extension automatically runs on every git push without any CI configuration`],0,`The extension and the CLI serve complementary purposes — one for developer convenience while writing tests, the other for automated, reproducible pipeline execution.`],[`L`,`A`,"Why does `expect.toHaveScreenshot`'s default `threshold`/`maxDiffPixels` sometimes need adjusting per-project rather than left at global defaults?",null,[`A page with animations, live data, or anti-aliasing differences across OS/CI rendering may need a more lenient threshold to avoid false failures, while a pixel-perfect static component might warrant a stricter one`,`threshold and maxDiffPixels can only be configured once globally and never overridden`,`Playwright automatically tunes these values per test based on machine learning`,`These settings only affect video recordings, not screenshots`],0,`Visual comparison tolerance is inherently content-dependent — one-size-fits-all thresholds either miss real regressions or generate constant false alarms.`],[`L`,`A`,"Why does `test.info().config` give useful information at runtime that simply importing `playwright.config.ts` directly inside a test would not?",null,[`testInfo.config reflects the fully resolved configuration after CLI flag overrides, project merging, and environment-based logic have all been applied — a raw import would only show the static, unresolved file contents`,`Importing the config file directly is technically disallowed by Node.js`,`testInfo.config is always empty unless globalSetup explicitly populates it`,`They return identical data; there is no practical difference`],0,`Command-line flags like --project or --grep, and per-project overrides, all get merged into the resolved config — testInfo.config reflects that final, effective state.`],[`O`,`I`,"What does running `npx playwright test --shard=2/4` do?",null,[`It runs only the tests from the 2nd file discovered`,`It runs roughly one quarter of the test files (the second slice), intended to run alongside three sibling invocations using shards 1/4, 3/4, and 4/4 on separate machines`,`It repeats the test suite 2 out of 4 times`,`It limits the run to 2 out of 4 configured projects`],1,`Sharding splits the full suite into N roughly equal slices by file; each CI machine runs one slice using a different shard index, cutting total wall-clock time.`],[`O`,`I`,`What does this config produce when tests run?`,`export default defineConfig({
  reporter: 'blob',
});`,["A single self-contained ZIP-like report file (blob-report/report.zip) meant to be combined with other shards' blob reports later via `npx playwright merge-reports`, rather than a human-readable report on its own",`A live-streaming report visible in the terminal in real time`,`An error, because blob is not a valid reporter name`,`The same output as the html reporter, just renamed`],0,`The blob reporter is an intermediate format designed specifically for later merging into one combined report after a sharded run.`],[`O`,`A`,`What is the effect of this configuration?`,`export default defineConfig({
  use: {
    actionTimeout: 10_000,
    navigationTimeout: 30_000,
  },
  timeout: 60_000,
});`,[`Each individual action (click, fill) must complete within 10s, each navigation (goto) within 30s, and the entire test body has an overall ceiling of 60s — three independent, nested timing constraints`,`Only the longest of the three values (60s) actually applies; the others are ignored`,`The three timeouts are summed together to form a single 100-second timeout`,`actionTimeout and navigationTimeout override the global timeout entirely`],0,`These are layered constraints — a slow action or navigation can fail early even if the overall 60s test timeout has not been reached yet.`],[`O`,`I`,"What does `fullyParallel: true` change compared to the Playwright default?",`export default defineConfig({
  fullyParallel: true,
});`,[`By default, tests within a single file run sequentially on one worker; fullyParallel makes tests within the SAME file also run concurrently across multiple workers, in addition to different files already running in parallel`,`It has no effect on the file-vs-worker execution model, only on reporter formatting`,`It disables all hooks (beforeEach/afterEach) to avoid conflicts during parallel runs`,`It forces every project to run in a single shared browser context`],0,`Without fullyParallel, one slow file monopolizes one worker for all its tests; with it, even tests inside that file get distributed for maximum concurrency.`],[`O`,`I`,"What does `globalTimeout` do in this config?",`export default defineConfig({
  globalTimeout: 60 * 60 * 1000,
});`,[`It caps the ENTIRE test run (every test, every worker combined) at one hour — if the whole suite has not finished by then, Playwright stops the run regardless of how many tests remain`,`It sets a 1-hour timeout for each individual test`,`It sets the maximum time globalSetup is allowed to run`,`It has no effect unless retries are also configured`],0,`This is a run-wide safety net, distinct from per-test timeout, useful for capping CI job duration regardless of suite size.`],[`O`,`I`,`What is the effect of this config option?`,`export default defineConfig({
  maxFailures: 10,
});`,[`The test run stops immediately once 10 tests have failed, skipping any remaining tests — useful for getting fast feedback on a badly broken build without waiting for the entire suite`,`It retries each failing test up to 10 times before giving up`,`It limits the suite to a maximum of 10 total tests`,`It fails the build only if more than 10% of tests fail`],0,"maxFailures (or the equivalent `--max-failures` CLI flag) is a fail-fast mechanism for large suites in CI."],[`O`,`I`,`What does this config accomplish?`,`import 'dotenv/config';
import { defineConfig } from '@playwright/test';

export default defineConfig({
  use: {
    baseURL: process.env.BASE_URL,
  },
});`,["It loads variables from a local `.env` file into `process.env` before defineConfig runs, allowing `baseURL` to be set per environment without hardcoding it into the config file",`It automatically creates a .env file if one does not exist`,`process.env.BASE_URL is populated by Playwright regardless of the dotenv import`,`It only works when running tests with the --env flag`],0,`The dotenv import must run before process.env is read — placing it at the top of the config file (or importing it first) ensures values are available when defineConfig executes.`],[`O`,`A`,"What does the `testIdAttribute` setting change here?",`export default defineConfig({
  use: {
    testIdAttribute: 'data-qa',
  },
});`,['`page.getByTestId(\'submit\')` will now look for an element with `data-qa="submit"` instead of the Playwright default `data-testid="submit"`',`It renames every data-testid attribute in the rendered HTML to data-qa at runtime`,`It only affects visual snapshot file naming, not locators`,`It has no effect unless combined with a custom fixture`],0,`This is purely a locator-matching preference — it tells getByTestId() which HTML attribute to search for.`],[`O`,`A`,`What does this project configuration accomplish?`,`export default defineConfig({
  projects: [
    { name: 'setup', testMatch: /global\\.setup\\.ts/ },
    { name: 'teardown', testMatch: /global\\.teardown\\.ts/ },
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
      dependencies: ['setup'],
      teardown: 'teardown',
    },
  ],
});`,[`The "setup" project runs first (e.g. to seed data or authenticate), then the "chromium" tests run, and finally the "teardown" project runs afterward to clean up — all with full fixture access like normal tests`,`setup and teardown run in parallel with chromium, with no ordering guarantees`,`dependencies and teardown are ignored unless fullyParallel is also set`,`This configuration is invalid because a project cannot reference another project by name`],0,`dependencies establishes a "must run before" relationship, and teardown links a cleanup project that runs after the dependent project finishes — both execute as regular test-like projects with fixtures.`],[`O`,`I`,`What does this reporter configuration produce during a CI run?`,`export default defineConfig({
  reporter: process.env.CI
    ? [['blob'], ['github']]
    : 'list',
});`,[`In CI, output goes to a mergeable blob report plus GitHub Actions-formatted annotations; locally, a simple live list is printed to the terminal instead`,`It always uses the blob reporter regardless of environment`,`The github reporter only works outside of GitHub Actions`,`It causes an error because reporter cannot be conditional`],0,`Reporter choice is commonly branched on process.env.CI so local runs stay lightweight while CI gets structured, mergeable output plus native annotations.`],[`O`,`A`,`What is the purpose of this config snippet?`,`export default defineConfig({
  reportSlowTests: { max: 5, threshold: 15_000 },
});`,[`After the run, the terminal reporter highlights up to the 5 slowest tests that took longer than 15 seconds, drawing attention to potential performance issues in either the tests or the app under test`,`It automatically marks any test over 15 seconds as failed`,`It limits the suite to a maximum of 5 slow tests, skipping the rest`,`It sets a global test timeout of 15 seconds for exactly 5 tests`],0,`This is purely informational — it does not change pass/fail outcomes, only surfaces which tests are taking unusually long.`],[`O`,`I`,"What does `preserveOutput: 'failures-only'` change about test artifacts?",`export default defineConfig({
  preserveOutput: 'failures-only',
  outputDir: 'test-results',
});`,[`Artifacts (screenshots, traces, videos) are only kept in outputDir for tests that fail; artifacts from passing tests are deleted after the run to save disk space`,`It prevents any artifacts from being written at all, for any test`,`It only affects the HTML report, not raw files on disk`,`It preserves output only when running with --headed`],0,`The alternative default of "always" keeps everything regardless of pass/fail, which can consume significant disk space on large suites run frequently.`],[`O`,`I`,`What does this CLI command accomplish?`,`npx playwright test --grep "@smoke" --grep-invert`,['It runs every test EXCEPT those whose title contains "@smoke" — effectively the opposite of a plain `--grep "@smoke"` run',`It runs only tests tagged @smoke, identically to a plain --grep`,`It is invalid syntax; --grep-invert cannot be combined with --grep`,`It runs @smoke tests twice, once forward and once inverted`],0,`--grep-invert flips the matching logic of whatever pattern --grep was given, turning inclusion into exclusion.`],[`O`,`I`,`What does this describe-level configuration do?`,`test.describe('checkout flow', () => {
  test.describe.configure({ retries: 3, timeout: 90_000 });

  test('completes purchase', async ({ page }) => { /* ... */ });
});`,[`It overrides both retries and timeout specifically for tests inside this describe block, without changing the global config values used by tests elsewhere in the suite`,`It changes the global retries and timeout for the entire test run`,`configure() only accepts a mode option; retries and timeout here are ignored`,`It applies retroactively to tests that already ran in other files`],0,`test.describe.configure() is a scoped override — perfect for a known-flaky or unusually slow group of tests without loosening standards suite-wide.`],[`O`,`B`,"What does `npx playwright test --repeat-each=10 login.spec.ts` do?",null,[`Runs every test in login.spec.ts ten times in a row within this single invocation, useful for confirming whether a suspected test is flaky`,`Runs the file once, but multiplies the reported pass count by 10`,`Retries the file up to 10 times only if it fails`,`Runs the test suite across 10 different browser projects`],0,`Unlike retries (which only re-run on failure), --repeat-each runs the test the specified number of times regardless of outcome, surfacing intermittent failures.`],[`O`,`A`,`What is the purpose of this use-block configuration?`,`export default defineConfig({
  use: {
    launchOptions: {
      slowMo: 50,
      args: ['--disable-gpu'],
    },
  },
});`,[`It passes options directly through to the underlying browserType.launch() call — slowing every action by 50ms and disabling GPU acceleration via a Chromium launch flag`,`launchOptions is ignored inside use and must be set at the top level of defineConfig instead`,`It only affects the first test in each file`,`slowMo and args can only be set via CLI flags, not inside launchOptions`],0,`launchOptions is a passthrough object letting you set any low-level browser launch flag directly from the config's use block.`],[`O`,`I`,`What does this workers setting do on a CI runner with 8 CPU cores?`,`export default defineConfig({
  workers: '50%',
});`,[`Playwright calculates 50% of the available CPU cores at runtime (4 workers on an 8-core machine) rather than using a hardcoded integer, so the same config scales sensibly across differently sized machines`,`It always uses exactly 50 workers regardless of hardware`,`Percentage values are invalid for the workers option and default to 1`,`It halves the timeout for every test instead of changing worker count`],0,`A relative percentage lets the same config file behave sensibly whether it runs on a 2-core laptop or a 32-core CI runner.`],[`O`,`A`,`What does this GitHub Actions step accomplish, run before the test step?`,`- name: Cache Playwright browsers
  uses: actions/cache@v4
  with:
    path: ~/.cache/ms-playwright
    key: playwright-\${{ hashFiles('package-lock.json') }}`,[`It caches the downloaded browser binaries keyed to the lockfile's hash, so subsequent CI runs skip re-downloading hundreds of megabytes of browsers as long as dependencies (and thus the Playwright version) have not changed`,`It caches the actual test results between runs, skipping tests that already passed`,`It permanently installs Chromium into the GitHub-hosted runner's base image`,`The cache key must be a fixed static string, not a hash, for this to work`],0,`Keying the cache on the lockfile hash ensures the cache is invalidated automatically whenever the Playwright version (and thus required browsers) changes.`],[`O`,`A`,`What does this command accomplish after running sharded tests on 4 separate CI machines?`,`npx playwright merge-reports --reporter=html ./all-blob-reports`,[`It combines the individual blob-report outputs collected from all 4 shards (downloaded into all-blob-reports/) into a single unified HTML report reflecting the entire suite's results`,`It re-runs all tests one more time to produce a combined result`,`It merges the reports but only keeps the results from the last shard listed`,`It requires all 4 shards to have passed, or it throws an error`],0,`merge-reports is specifically designed to consume multiple blob-report artifacts (typically downloaded from CI job artifacts) and produce one coherent output in any target reporter format.`],[`O`,`A`,`Why might this Dockerfile line be used when building a custom CI image?`,`ENV PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1`,["It skips downloading browsers during `npm install` in this Docker layer, allowing the browsers to be installed in a separate, more cacheable layer (e.g. by copying from the official Playwright base image) instead",`It permanently disables all browser installation, breaking the image`,`It forces Playwright to use only browsers already present on the host OS`,`It has no effect on Docker builds and is only used for local development`],0,`Skipping the automatic download during npm install lets a Docker build layer browser installation separately for better caching and smaller diffs between builds.`],[`O`,`I`,"What does this top-level `expect` block configure?",`export default defineConfig({
  expect: {
    timeout: 10_000,
    toHaveScreenshot: { maxDiffPixels: 100 },
  },
});`,[`It sets a 10-second retry window for all web-first assertions globally, and configures visual comparison assertions specifically to tolerate up to 100 differing pixels before failing`,`It sets the whole test suite's timeout to 10 seconds and screenshots to a maximum size of 100 pixels`,`toHaveScreenshot options can only be set per-test, so this global setting is ignored`,`It disables all assertions that are not screenshot comparisons`],0,"The top-level `expect` object configures assertion-specific behavior globally, separate from the test-level `timeout` setting."],[`O`,`I`,"What is the effect of running this command in a project already configured with `retries: 0`?",`npx playwright test --retries=2`,[`The CLI flag overrides the config file's retries value for this invocation only, giving each failing test up to 2 additional attempts`,`CLI flags are ignored whenever a config file value is already set`,`It causes an error since retries cannot be changed at the CLI level`,`It sets retries to 2 permanently by rewriting playwright.config.ts`],0,`Most Playwright config options have a corresponding CLI flag that takes precedence for that single run, without needing to edit the config file.`],[`O`,`A`,`What does this project setup achieve when multiple projects share the same spec files?`,`export default defineConfig({
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },
  ],
  snapshotPathTemplate: '{testDir}/__screenshots__/{projectName}/{arg}{ext}',
});`,[`It stores visual snapshot baselines in separate subfolders per project (e.g. __screenshots__/chromium/ and __screenshots__/firefox/), preventing chromium and firefox baselines from overwriting each other`,`It merges all snapshots from every project into one shared baseline file`,`snapshotPathTemplate only affects trace file names, not screenshots`,`It disables snapshot testing for any project other than the first one listed`],0,`Different browser engines often render pages with tiny pixel differences — namespacing snapshots by project avoids false positives from comparing across engines.`],[`O`,`B`,"What does `npx playwright install-deps` do differently from `npx playwright install --with-deps`?",null,[`install-deps only installs the OS-level system libraries needed by the browsers (without downloading the browser binaries themselves) — useful when browsers are already baked into a Docker image but system packages are still missing`,`They are functionally identical; install-deps is just a shorter alias`,`install-deps installs Playwright itself as an npm dependency`,`install-deps only works on Windows, while --with-deps only works on Linux`],0,"install-deps is a standalone subcommand focused purely on OS package installation, distinct from the combined browser+deps behavior of `install --with-deps`."],[`O`,`I`,`What is the outcome of this CI job step ordering?`,`- run: npm ci
- run: npx playwright install --with-deps
- run: npx playwright test --shard=\${{ matrix.shard }}/4`,[`Dependencies are installed, then browsers plus OS libraries are installed, then this particular matrix job runs only its assigned quarter of the test suite — designed to run as 4 parallel matrix jobs total`,`The shard flag is ignored because it was not set in playwright.config.ts`,`This will run the entire suite 4 times regardless of the matrix.shard value`,`Sharding requires a separate playwright.config.ts per shard, so this configuration would fail`],0,`A CI matrix strategy with 4 shard values, each running this same workflow, is the standard way to parallelize a large suite across multiple machines.`],[`O`,`A`,`What does this conditional config selection achieve?`,`const env = process.env.TEST_ENV || 'staging';
const configs = {
  staging: { baseURL: 'https://staging.example.com' },
  production: { baseURL: 'https://example.com' },
};

export default defineConfig({
  use: { ...configs[env] },
});`,[`It lets a single playwright.config.ts serve multiple environments by branching on an environment variable, avoiding duplicated config files for staging vs production`,`It runs tests against both staging and production simultaneously in one invocation`,`TEST_ENV must be set inside the test file itself for this to work, not via CLI or CI`,`defineConfig does not support spreading objects into the use block`],0,`This pattern is common for teams that need the same test suite to target different base URLs (and possibly credentials) depending on the pipeline stage.`],[`O`,`I`,`What is wrong with expecting this snippet alone to load environment variables for CI?`,`// playwright.config.ts
import 'dotenv/config';
export default defineConfig({
  use: { baseURL: process.env.BASE_URL },
});`,["Nothing is wrong for local development, but in CI, secrets are usually injected as real environment variables (via the CI provider's secrets settings) rather than from a `.env` file — dotenv simply finds no file to load there, and process.env.BASE_URL still works fine as long as CI sets it directly",`dotenv/config only works in test files, never in config files`,`This throws an error whenever a .env file is missing`,`baseURL must be a string literal and cannot reference process.env at all`],0,`dotenv silently does nothing if no .env file is found — that is fine in CI as long as the CI system itself injects the same environment variables through its native secrets mechanism.`],[`O`,`A`,`What does this config combination test for, and why might it fail in CI but pass locally?`,`export default defineConfig({
  workers: 8,
  use: { headless: true },
});
// Docker container limited to 2 CPUs, default 64MB /dev/shm`,[`On a resource-constrained container, 8 concurrent Chromium processes can exhaust CPU and shared memory, causing browser crashes or hangs that would not occur on a developer's full-powered local machine — workers should be tuned to the container's actual resource limits`,`headless: true is incompatible with workers greater than 1`,`Docker containers cannot run more than 1 worker under any configuration`,`This configuration is fine everywhere; the described failure must be caused by something else entirely`],0,`workers should reflect real available resources — a config tuned for a powerful laptop can silently overwhelm a constrained CI container.`],[`R`,`A`,`Your suite has grown to 5,000 tests and now takes 40 minutes in a single CI job. Leadership wants it under 10 minutes without buying faster hardware. What is the most effective config-level change?`,null,["Split the run across 4 parallel CI machines using `--shard=N/4`, each running roughly a quarter of the suite, then merge their blob reports into one final HTML report",`Reduce the global timeout to force tests to fail faster`,`Switch the reporter from html to list, which runs faster`,`Disable retries entirely to save time`],0,`Sharding distributes the actual test execution across separate machines running in parallel, directly cutting wall-clock time roughly proportional to the shard count.`],[`R`,`I`,`Your team wants every developer and every CI job to use the exact same browser binary versions, eliminating "works on my machine" bugs caused by mismatched local installs. What is the most robust solution?`,null,[`Standardize on the official Playwright Docker image (pinned to the exact version matching package.json) for both CI and local development containers, so everyone runs identical, reproducible browser binaries`,"Ask each developer to manually check their `npx playwright --version` output before each PR",`Add a pre-commit hook that reinstalls browsers on every commit`,`Rely on npm's lockfile alone; browser binaries are automatically kept in sync by npm`],0,`The lockfile pins the npm package version, but the actual browser binaries live outside node_modules — a shared Docker image guarantees true environment parity.`],[`R`,`A`,`Your monorepo has three Playwright test packages, and visual regression snapshots for the same component are colliding when tests run across projects. What is the best fix?`,null,["Customize `snapshotPathTemplate` (or organize per-package testDir/projects) to namespace snapshot directories by package and/or project name, preventing baseline overwrites",`Disable visual regression testing across the monorepo entirely`,`Rename every test file to have a globally unique name`,`Run each package's tests in strictly sequential CI jobs to avoid file writes overlapping`],0,`The root cause is a shared snapshot namespace — fixing the path template (or per-package config isolation) resolves it without sacrificing parallelism.`],[`R`,`I`,`After introducing 4-way sharding in CI, you now get 4 separate pass/fail summaries and no one can easily tell if the overall build is green. What should you add to the pipeline?`,null,["Configure the blob reporter for each shard, upload each shard's blob-report as a CI artifact, then add a final job that downloads all of them and runs `npx playwright merge-reports` to produce one combined report",`Have each shard email its own separate report to the team`,`Reduce sharding back to a single job to simplify reporting`,`Manually average the four pass rates together as a rough approximation`],0,`A dedicated merge step after all shards complete is the standard pattern for reconstructing one coherent view of a sharded run.`],[`R`,`I`,`Your CI pipeline currently runs the full 3,000-test suite even when a single, obviously broken change would fail hundreds of them. You want faster failure feedback without full sharding infrastructure. What is the simplest change?`,null,["Set `maxFailures` (e.g. to 20) so the run aborts early once enough failures accumulate, giving quick feedback that something is fundamentally broken",`Delete all failing tests from the suite`,`Set retries to a very high number so failures eventually pass`,`Disable the HTML reporter, since it is slowing down the run`],0,`maxFailures is a lightweight, single-config-line way to stop a doomed run early without needing to set up sharding or infrastructure changes.`],[`R`,`A`,"Your app uses `data-qa` attributes for testing (inherited from a previous Cypress suite) instead of `data-testid`. Rewriting the entire application's markup is not feasible right now. How do you adapt Playwright without touching app code?",null,["Set `use: { testIdAttribute: 'data-qa' }` in playwright.config.ts so `getByTestId()` matches the existing attribute convention",`Write a custom locator engine from scratch to replace getByTestId entirely`,'Use only CSS selectors like `[data-qa="submit"]` everywhere instead of getByTestId',`Ask the frontend team to add duplicate data-testid attributes alongside data-qa everywhere`],0,`testIdAttribute is designed exactly for this migration scenario — align Playwright's convention with your existing markup instead of the reverse.`],[`R`,`I`,`Your team wants CI to fail immediately after 10 test failures rather than run all 2,000 tests to completion, saving compute minutes on clearly broken builds — but local runs should show every failure. How do you configure this?`,null,["Set `maxFailures: process.env.CI ? 10 : undefined` in playwright.config.ts, mirroring the common pattern used for retries",`Hardcode maxFailures: 10 globally, affecting both CI and local runs equally`,`Use --grep to skip failing tests in CI only`,`There is no way to make maxFailures environment-conditional`],0,"The same `process.env.CI` ternary pattern used for retries works for maxFailures too — CI gets a fail-fast ceiling, local development sees everything."],[`R`,`A`,`You need the same test suite to run against a staging environment on every pull request and a production-like environment nightly, with different base URLs and credentials, without maintaining two separate config files. What is the cleanest approach?`,null,["Read an environment variable (e.g. `TEST_ENV`) at the top of playwright.config.ts and branch the `use.baseURL` and any credentials accordingly, setting that variable differently in each CI workflow",`Duplicate playwright.config.ts into playwright.staging.config.ts and playwright.prod.config.ts, keeping both manually in sync`,`Hardcode the staging URL and manually find-and-replace before the nightly run`,`Store both environments' data inside every individual test file and pick at runtime`],0,`Branching a single config on an environment variable avoids config duplication and keeps environment differences centralized and easy to audit.`],[`R`,`I`,"A CI runner is provisioned with only 2 vCPUs but the default Playwright config (copied from a teammate's 16-core machine) sets `workers: 12`. Tests are timing out intermittently. What should change?",null,["Lower `workers` to match the runner's actual CPU count (e.g. 2, or `workers: '50%'` for headroom), since oversubscribing CPU with more browser processes than cores causes contention and slowdowns",`Increase the global timeout instead, since the machine is just naturally slower`,`Switch to a faster browser engine to compensate`,`Disable headless mode to reduce rendering overhead`],0,`Worker count should be tuned to the actual execution environment's resources, not copied blindly from a more powerful development machine.`],[`R`,`A`,`A team building a Chrome extension needs their Playwright tests to detect real-world differences between the bundled Chromium build and actual Google Chrome (specific to codec support and branding features). What config change addresses this?`,null,["Set `channel: 'chrome'` in the relevant project's `use` block and separately install it with `npx playwright install chrome`, so tests run against the real branded browser instead of bundled Chromium",`This is not possible; Playwright can only ever test against its bundled Chromium build`,`Set browserName: 'chrome' instead of channel, since channel is deprecated`,`Install Chrome manually and set executablePath to its system install location as the only supported method`],0,`The channel option is specifically designed to target real installed browser builds (Chrome, Edge) rather than Playwright's own bundled Chromium.`],[`S`,`I`,"A team upgrades `@playwright/test` from 1.44 to 1.49 in package.json and merges the change. The next CI run fails immediately with a browser executable not found error, even though local machines that ran `npm install` and then manually reinstalled browsers work fine. What is the root cause and fix?",null,["The CI Docker image (or cached browser install step) is still pinned to the older Playwright version's browsers — the CI pipeline needs to reinstall/re-cache browsers matching the new version, e.g. by re-running `npx playwright install` keyed on the updated lockfile hash",`The upgrade corrupted package-lock.json and it needs to be deleted and regenerated`,`Playwright 1.49 removed support for Docker-based CI entirely`,`The fix is to downgrade back to 1.44 permanently`],0,`A version bump in package.json does not automatically update already-cached browser binaries in CI — the cache key (often the lockfile hash) needs to reflect the new version so it invalidates correctly.`],[`S`,`A`,'A previously stable suite starts showing occasional random failures only when `workers` was recently raised from 4 to 16 to "speed things up" on the same 8-core CI runner. What is the most likely explanation and remedy?',null,[`16 workers on an 8-core machine oversubscribes the CPU, causing resource contention that manifests as timing-sensitive flaky failures — reduce workers back to match or stay under the core count`,`The flakiness is unrelated to workers and must be caused by application code changes`,`Higher worker counts always disable retries automatically`,`The fix is to disable parallelism entirely by setting fullyParallel: false`],0,`Flakiness that correlates directly with a worker-count change is a strong signal of resource contention rather than a genuine application or test bug.`],[`S`,`I`,'After a monorepo restructuring moved test files from `tests/` to `apps/web/e2e/`, running `npx playwright test` from the repo root reports "No tests found," even though the files clearly exist. What is the fix?',null,["Update `testDir` (and/or `testMatch`) in playwright.config.ts to point at the new `apps/web/e2e/` location, since Playwright only discovers files under the configured testDir",`Reinstall @playwright/test, since the restructuring corrupted the package`,`Rename all files back to their exact previous filenames`,`Run npx playwright test --list first to force a re-scan, then run tests normally`],0,`A restructuring that moves test files without updating testDir is one of the most common causes of "no tests found" after a repo reorganization.`],[`S`,`A`,"A new engineer runs the suite locally and gets 340 tests, but CI reports 410 tests for the same commit. Investigation shows their local machine has an extra `playwright.local.config.ts` file picked up by an old alias in their shell profile. What two things should the team check to prevent this recurring for other new hires?",null,["1) Ensure onboarding docs specify the exact `npx playwright test --config=playwright.config.ts` invocation (or a single canonical npm script) so no stray config file or alias can be silently substituted; 2) Consider deleting or renaming any legacy config files that are no longer meant to be used",`Reinstall Node.js on the new engineer's machine`,`The discrepancy is expected and does not need investigating`,`Force everyone to run tests only inside a shared cloud IDE`],0,`Silent config substitution (via a stray file or shell alias) is a subtle source of test-count mismatches — standardizing the exact invocation command avoids ambiguity.`],[`S`,`A`,'A team switches their playwright.config.ts to use ESM `import` syntax while the rest of the project remains a CommonJS package (no `"type": "module"` in package.json, `.ts` extension, no `.mts`). The config now fails to load with a module syntax error. What should the team check?',null,[`Whether the project's package.json "type" field, tsconfig module settings, and the config file's extension are all consistent with ESM usage — mixing CommonJS project defaults with ESM-only import syntax in the same file is a common source of this exact failure`,`ESM is entirely unsupported by Playwright and the import statements must be reverted`,`The fix is always to rename the file to playwright.config.mjs regardless of the rest of the project`,`This error only occurs on Windows and can be ignored on macOS/Linux`],0,`Playwright supports both module systems, but the surrounding project configuration (package.json, tsconfig) must be consistent with whichever style the config file uses.`],[`S`,`A`,`Four CI shards each upload their own blob-report artifact, but the pipeline never runs a merge step. The team currently opens all 4 separate report links manually to check for failures. What should be added, and what command performs the actual work?`,null,["A final CI job that downloads all 4 blob-report artifacts into one directory and runs `npx playwright merge-reports --reporter=html ./that-directory` to produce a single combined report that gets uploaded as the canonical build artifact",`A cron job that periodically deletes old blob reports to save space`,`Switching from blob to html reporter directly, which merges automatically with no extra step`,`Re-running the entire suite without sharding, since merging is too complex to automate`],0,`Sharded reports are individually incomplete by design — a dedicated merge job closes the loop and restores a single source of truth for pass/fail status.`],[`S`,`I`,"A self-hosted Docker-based CI runner starts throwing out-of-memory errors partway through a Playwright run after `workers` was bumped from 2 to 6 to speed things up, on a container limited to 4GB RAM. What is the most direct fix?",null,["Reduce `workers` back down (or raise the container's memory limit) — each worker launches its own browser process, and 6 concurrent Chromium instances on a 4GB-limited container can easily exceed available memory",`Switch the reporter to json, which uses less memory than html`,`Disable trace collection only, since traces are the sole cause of memory usage`,`Set headless: false, which reduces rendering memory overhead`],0,`Worker count directly multiplies concurrent browser memory usage — a resource-constrained container needs a worker count matched to its actual memory budget.`],[`S`,`A`,`A CI pipeline reads secrets from environment variables set in the CI provider's UI, but a teammate also commits a \`.env\` file with placeholder values "just to be safe." Locally, everyone's tests suddenly start hitting the wrong (placeholder) baseURL. What happened, and what is the fix?`,null,["`dotenv/config` loaded the committed `.env` file locally (since it exists), overriding what developers expected from their own shell environment or personal .env — the fix is to never commit real or placeholder `.env` files, keeping only a `.env.example` and .gitignore-ing `.env`",`dotenv only runs in CI, so the local behavior described is impossible and must have another cause`,`The baseURL must always be hardcoded, since environment variables are inherently unreliable`,`Committing .env files is required for CI secrets to work correctly`],0,"A committed `.env` silently loaded by dotenv can shadow personal, correct local values — this is exactly why `.env` belongs in .gitignore while `.env.example` documents the expected shape."],[`S`,`I`,"Locally, `retries: 0` gives fast feedback during development, but the same config value in CI means a genuinely intermittent test causes unnecessary build failures. The team does not want two separate config files. What is the standard fix?",null,["Use `retries: process.env.CI ? 2 : 0` in the single shared playwright.config.ts, so behavior differs automatically based on the CI environment variable without maintaining separate files",`Add a CLI flag to every local test command to override retries manually each time`,`Accept the extra CI failures as a cost of using a single config file`,`Set retries to 2 everywhere, sacrificing fast local feedback`],0,`This environment-conditional pattern is the standard way to get different behavior locally vs. CI from one shared configuration file.`],[`S`,`A`,`Visual regression tests pass consistently on every developer's macOS laptop but fail in Linux-based CI with tiny pixel-level diffs in font rendering, even though the underlying HTML/CSS is unchanged. What is the most reliable long-term fix?`,null,["Generate and update the baseline screenshots by running `--update-snapshots` inside the same Docker container/OS used by CI, ensuring baselines and CI comparisons use identical font rendering — rather than trying to make macOS and Linux render pixel-identically",`Increase maxDiffPixels until the failures disappear, regardless of the underlying cause`,`Disable visual regression testing on CI entirely and only run it locally`,`Switch fonts to a monospace font everywhere to eliminate rendering variance`],0,`Cross-OS font rendering differences are a very common cause of visual-diff false positives — baselines should be generated in the same environment where comparisons will run, typically the CI container itself.`],[`D`,`I`,'CI fails with "browserType.launch: Executable doesn\'t exist" immediately after `@playwright/test` was bumped to a newer minor version in package.json, but the CI workflow file was not otherwise changed. What is the fix?',null,["Re-run (or fix the cache key for) the `npx playwright install` step so it downloads the browser binaries matching the new package version — the cached browsers from before the bump no longer match",`Roll back Node.js to an older version`,"Add `--force` to the npm install command",`Switch the CI job to use a different browser engine entirely`],0,`A version bump changes which browser build the test runner expects — any browser cache keyed on the old version needs to be invalidated and refreshed.`],[`D`,`A`,`A CI pipeline runs 4 shards successfully, each showing "34 passed" in their own logs, but there is no single job reporting the overall pass/fail status for the pull request check. What is missing from the pipeline?`,null,["A final merge-and-summarize job: upload each shard's blob-report as an artifact, then add a job that downloads them all and runs `npx playwright merge-reports` to produce one combined report/summary that the PR status check can reference",`Nothing is missing — each shard reporting independently is the intended final state`,`The fix is to remove sharding and go back to one single job`,`Add --reporter=list to each shard, which automatically aggregates across jobs`],0,`Sharded jobs are independent by design — reconstructing one aggregate result requires an explicit downstream merge step in the pipeline.`],[`D`,`I`,"A `.env` file with `BASE_URL=https://staging.example.com` sits at the repo root and `dotenv/config` is imported in playwright.config.ts, yet `process.env.BASE_URL` is `undefined` when tests run in CI. Locally it works fine. What is the likely cause?",null,["CI environments typically do NOT check out `.env` files from source control (it is gitignored, as it should be) — the fix is to inject BASE_URL directly as a CI environment/secret variable rather than relying on a file that legitimately does not exist there",`dotenv is disabled by default whenever process.env.CI is set to true`,`BASE_URL must be renamed to PLAYWRIGHT_BASE_URL for CI compatibility`,`The .env file must be Base64-encoded before CI can read it`],0,`This is the expected, correct behavior — .env is (rightly) not committed, so CI needs its own mechanism (secrets/environment variables) to supply the same values.`],[`D`,`A`,"After adding `testIgnore: ['**/node_modules/**']` to exclude vendored files, the team notices some of their OWN test files under `src/features/**/tests/` are also being skipped unexpectedly. What is the likely issue with the glob pattern approach?",null,["A testIgnore or testMatch pattern that is too broad (or a testDir set too high in the tree) can unintentionally exclude legitimate test files — the patterns need to be scoped more precisely, and `--list` should be used to verify exactly which files are discovered after any change",`testIgnore can only contain exactly one pattern; additional patterns silently disable all matching`,`node_modules exclusion is automatic and this testIgnore entry is redundant, so it must be a different bug entirely`,`testIgnore only works when testDir is set to the project root`],0,`Broad exclude patterns are a common source of accidentally-skipped legitimate tests — always verify discovery with --list after changing testMatch/testIgnore.`],[`D`,`A`,'After `npm install`, running any Playwright command prints deprecation warnings about a peer dependency mismatch, and `npx playwright test` fails to launch any browser. `package.json` shows `@playwright/test: "^1.40.0"` but `playwright-core` was pinned separately at an older version by a transitive dependency. What should be checked and fixed?',null,["Run `npm ls playwright-core` (or check the lockfile) to confirm which version actually resolved, and align it with `@playwright/test` — a mismatched playwright-core version bundled/expected by a different package can cause exactly this kind of launch failure",`Uninstall Node.js and reinstall a newer version`,`Delete playwright.config.ts and let Playwright regenerate a default one`,`Switch from @playwright/test to the raw playwright package to avoid the peer dependency entirely`],0,`playwright-core version mismatches (often pulled in transitively) are a real, if uncommon, cause of launch failures — checking the resolved dependency tree helps pinpoint the conflict.`],[`L`,`I`,'Why does Playwright recommend closing over `process.env.CI` for many config decisions (retries, workers, reporter) rather than detecting "CI-ness" some other way?',null,[`process.env.CI is a de facto standard set by virtually every major CI provider (GitHub Actions, GitLab CI, CircleCI, Jenkins with the right plugin, etc.), making it a reliable, portable signal without vendor-specific detection logic`,`Playwright can only read this one specific environment variable and no others`,`CI-specific behavior is hardcoded into the Playwright binary and cannot be changed by config`,`process.env.CI is set automatically by playwright.config.ts itself at import time`],0,`Relying on a widely-adopted convention keeps the config portable across whichever CI system a team happens to use, without provider-specific branching.`],[`L`,`A`,"Why might a team deliberately choose the `list` reporter for local development but never rely on it as the sole reporter in CI?",null,[`list prints live, human-readable progress in the terminal which is great for watching a run interactively, but it produces no persistent, structured artifact (like HTML or JSON) that CI systems need for archiving results, PR annotations, or historical trend tracking`,`list reporter cannot run more than one test at a time`,`list is deprecated and only kept for backward compatibility`,`CI systems technically cannot parse terminal output at all, by design`],0,`A live terminal stream is great for a human watching in real time, but CI needs durable, structured output that survives after the job finishes and can be uploaded or queried.`],[`L`,`I`,`Why is it generally safer to specify Playwright's Node.js engine requirement (via package.json "engines") even though Playwright will often still install on unsupported versions?`,null,[`An unsupported or too-old Node.js version can produce subtle, hard-to-diagnose runtime failures well after installation succeeds — declaring the engines field documents the requirement and lets tools like npm/CI warn early rather than failing mysteriously mid-run`,`Node.js version has zero actual effect on Playwright behavior`,`The engines field is required for @playwright/test to type-check`,`Declaring engines automatically installs the correct Node.js version for you`],0,`Documenting the supported Node range surfaces version problems at install time instead of as a confusing failure deep into a test run.`],[`L`,`A`,`Why do some teams intentionally avoid running Playwright inside a lightweight Alpine Linux Docker base image, even though it produces smaller images?`,null,[`Alpine uses musl libc instead of glibc, and Playwright's official prebuilt browser binaries are built and tested against glibc-based distros (like Debian/Ubuntu) — using Alpine can lead to missing shared libraries or unsupported browser launches`,`Alpine Linux does not support Docker at all`,`Playwright refuses to install any npm packages on Alpine-based images`,`Alpine images cannot run headless Chromium under any circumstances`],0,`The glibc vs musl difference is a well-known source of native binary compatibility issues — Playwright's official Docker images are Debian-based specifically to sidestep this.`],[`O`,`I`,`What does this npm script accomplish?`,`"scripts": {
  "test:e2e": "playwright test",
  "test:e2e:ci": "playwright test --shard=$SHARD_INDEX/$SHARD_TOTAL --reporter=blob"
}`,[`It defines a plain local test command alongside a CI-specific variant that reads shard configuration from environment variables and outputs a mergeable blob report, keeping both use cases in one package.json`,`Both scripts always run the exact same command regardless of environment`,`test:e2e:ci will fail unless SHARD_INDEX and SHARD_TOTAL are hardcoded literally in the script string`,`npm scripts cannot reference environment variables inside their command string`],0,`A separate CI-oriented script is a common convention that keeps sharding/reporter details out of the everyday local command while still living in the same package.json.`],[`O`,`I`,`What is the effect of running this command locally after intentionally changing an application's button color?`,`npx playwright test --grep "@visual" --update-snapshots`,[`It runs only the tests tagged @visual and regenerates their stored screenshot baselines to match the current (new) rendering, rather than running and updating the entire suite's snapshots`,`It updates snapshots for every test in the suite, ignoring the --grep filter entirely`,`--update-snapshots cannot be combined with --grep in the same command`,`It only updates the snapshot for the very first matching test, then stops`],0,`Combining --grep with --update-snapshots lets you scope a baseline refresh to just the affected visual tests instead of regenerating the whole suite's baselines.`],[`O`,`A`,`What does this snippet inside playwright.config.ts accomplish?`,`export default defineConfig({
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ].filter(p => !process.env.SKIP_CHROMIUM || p.name !== 'chromium'),
});`,[`It conditionally excludes the chromium project from the run when the SKIP_CHROMIUM environment variable is set, since defineConfig accepts any plain JavaScript/TypeScript logic — including array methods like filter — to build the final config object`,`Config files can only contain static, literal objects; this filter() call would throw an error at load time`,`SKIP_CHROMIUM must be defined inside playwright.config.ts itself for this to have any effect`,`The projects array is fixed at exactly one entry regardless of the filter`],0,`playwright.config.ts is just a regular TypeScript/JavaScript module — any valid code, including conditional logic driven by environment variables, can shape the exported config.`],[`O`,`I`,`What does this config combination achieve?`,`export default defineConfig({
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? '50%' : undefined,
});`,[`It applies three separate CI-only safeguards/tunings in one config: blocking accidental .only() commits, adding retry tolerance for flaky tests, and capping worker count relative to the CI runner's cores — while leaving local development unrestricted and using Playwright's default worker count`,`It disables all three features entirely whenever CI is detected`,`workers: undefined causes a configuration error and must be replaced with a specific number`,`These three settings are mutually exclusive and cannot be combined in one config`],0,`Branching on process.env.CI is a widely-used pattern to apply stricter, more tolerant, or differently-scaled settings specifically for automated pipeline runs.`],[`O`,`A`,`What does this config do when running in a constrained CI container?`,`export default defineConfig({
  use: {
    launchOptions: {
      args: ['--no-sandbox', '--disable-dev-shm-usage'],
    },
  },
});`,[`It passes Chromium launch flags that disable the OS sandbox (often required when running as root inside certain containers) and avoid relying on limited /dev/shm space, both common workarounds for browser crashes in constrained Docker environments`,`It permanently disables headless mode for all tests`,`These flags only affect Firefox and WebKit, not Chromium`,`--no-sandbox increases test isolation between parallel workers`],0,`These two Chromium flags are among the most common Docker-specific workarounds for sandboxing restrictions and shared-memory limits in containerized CI.`],[`O`,`I`,`What is the purpose of this package.json engines field alongside a Playwright project?`,`"engines": {
  "node": ">=18.0.0"
}`,[`It documents the minimum Node.js version the project (and its Playwright test suite) requires, letting npm/CI tooling warn early if an incompatible Node version is used, rather than surfacing as a confusing runtime failure later`,`It automatically installs Node.js 18 if a different version is present`,`It changes which browser binaries Playwright downloads`,`engines has no relationship to Playwright and only affects unrelated npm packages`],0,`Declaring a minimum Node version is a general npm convention that helps catch environment mismatches early, which is especially relevant since Playwright drops support for old Node versions over time.`]],firsttest:[[`O`,`B`,"What does `test('title', async ({ page }) => {})` define in Playwright?",null,[`A browser launch configuration`,`A single test with a page fixture`,`A describe group of tests`,`A global setup function`],1,"Each call to `test()` registers one test. The string is its title shown in reports; the async callback receives fixtures like `page`."],[`O`,`B`,"Which module do `test` and `expect` come from in Playwright?",null,[`playwright`,`@playwright/browser`,`@playwright/test`,`playwright-test`],2,"`@playwright/test` is the test-runner package. The lower-level `playwright` package exists but does not export `test` or `expect`."],[`O`,`B`,"What does `test.describe('Group', () => { … })` do?",null,[`Downloads a browser group`,`Groups related tests together under a shared label`,`Runs tests only for the described browser`,`Sets a timeout for a group of tests`],1,"`test.describe()` creates a named group. It scopes `beforeEach`/`afterEach` hooks and organises the HTML report."],[`O`,`I`,"What does `test.only('…', async ({ page }) => {})` do when it appears in a test file?",null,[`Runs that test with elevated browser permissions`,`Runs only that test in the file, skipping all others`,`Marks the test as the first to run globally`,`Forces the test to run in a single worker`],1,"`test.only()` filters the run to just that test within its file. Other files are unaffected."],[`O`,`I`,"What does `test.skip('…', async ({ page }) => {})` tell Playwright?",null,[`Delete the test after it runs`,`Mark the test as skipped — do not execute it`,`Run the test but ignore its result`,`Retry the test automatically on failure`],1,"`test.skip()` excludes the test from execution. It still appears in the report, marked as skipped."],[`O`,`B`,"What is `page` in the callback `async ({ page }) => {}`?",null,[`A global singleton shared across all tests`,`A built-in fixture providing a fresh browser page for each test`,`A reference to the browser process`,`A DOM element handle`],1,"`page` is Playwright's most-used built-in fixture. It is created fresh before each test and disposed automatically after."],[`O`,`I`,"What does `test.fixme('…', async ({ page }) => {})` indicate?",null,[`A test that is intentionally slow`,`A known broken test that should be skipped for now`,`A test that requires manual verification`,`A test that always passes`],1,"`test.fixme()` is the idiomatic way to flag a known failure. It skips execution and shows up distinctly in the report."],[`O`,`B`,"Why must every Playwright action inside a test be prefixed with `await`?",null,[`To enable TypeScript type checking`,`Because all browser operations return Promises`,`To activate Playwright Inspector`,`Because Playwright uses worker threads`],1,"Navigation, clicks, fills, and assertions all return Promises. Without `await` the test continues before the browser does anything — causing flakiness."],[`O`,`I`,"What happens when you pass `{ timeout: 60_000 }` as the second argument to `test()`?",null,[`Sets a 60-second delay before the test starts`,`Overrides the timeout for that specific test to 60 seconds`,`Sets the global config timeout`,`Adds 60 seconds to every assertion timeout`],1,"A per-test options object as the second argument lets you override `timeout`, `annotation`, and `tag` for that single test only."],[`O`,`B`,"Which file extension does Playwright's default `testMatch` pattern look for?",null,[`.test.ts only`,`.spec.ts (and .spec.js)`,`any .ts file`,`playwright.ts only`],1,"The default glob is `**/*.spec.{ts,js}`. Files named `.test.ts` are not discovered unless you extend `testMatch`."],[`O`,`I`,"What does `await expect(page).toHaveTitle(/Playwright/)` assert?",null,[`The page URL contains "Playwright"`,`The page title matches the regex /Playwright/`,`A heading element contains "Playwright"`,`The page was built with Playwright`],1,"`toHaveTitle()` auto-retries until the document `<title>` matches the string or regex, then passes — or times out and fails."],[`O`,`A`,"If `test.describe.only()` wraps one describe block in a file, what happens to tests outside it?",null,[`They run before the .only block`,`They are skipped for this file`,`They run in a separate worker`,`They are permanently deleted`],1,"`describe.only()` restricts execution to that describe block within the file. Tests outside it — even in the same file — are skipped."],[`F`,`B`,"Fill in the blank: `import { test, ______ } from '@playwright/test';`",null,[`expect`,`assert`,`check`,`verify`],0,"`expect` is Playwright's assertion function. It wraps a value or locator and exposes auto-retrying matchers like `.toBeVisible()`."],[`F`,`B`,"Fill in the keyword: `test('login', ______ ({ page }) => {`",null,[`async`,`await`,`function`,`promise`],0,"The test callback must be `async` so that `await` can be used for every browser operation inside it."],[`F`,`I`,"Fill in the modifier: `test.______( 'skipped for now', async ({ page }) => { … })`",null,[`skip`,`only`,`fixme`,`ignore`],0,'`test.skip()` excludes the test from running while keeping it visible in the report as "skipped".'],[`F`,`B`,"Complete the describe wrapper: `test.describe('Cart', ______ => {`",null,[`()`,`async ()`,`function()`,`async function()`],0,"`test.describe()` takes a synchronous callback (no `async`). Marking it `async` causes a Playwright configuration error."],[`F`,`I`,"Fill in the timeout key: `test('slow upload', { ______: 120_000 }, async ({ page }) => { … })`",null,[`timeout`,`time`,`wait`,`delay`],0,"The per-test options object accepts `timeout` (ms), `annotation`, and `tag`. `{ timeout: 120_000 }` gives that test 2 minutes."],[`F`,`B`,"Complete the assertion: `await expect(______).toHaveTitle(/Home/)`",null,[`page`,`document`,`window`,`browser`],0,"`expect(page)` targets the whole page. `toHaveTitle()` checks the `<title>` element and auto-retries until it matches."],[`F`,`A`,"Fill in the modifier for a known failing test: `test.______( 'broken feature', async ({ page }) => { … })`",null,[`fixme`,`broken`,`pending`,`xfail`],0,"`test.fixme()` is the Playwright convention for known failures. If a fixme test starts passing, Playwright reports it as unexpected — a reminder to remove the marker."],[`E`,`B`,"What is wrong with this test?\n`test('check title', (page) => { page.goto('/') })`",null,["The callback is missing `async` and actions are missing `await`","`page` must be destructured from an object: `({ page })`",`The test title uses single quotes`,`goto() does not exist on page`],1,"Two errors: (1) the callback must be `async`, and (2) every action needs `await`. Also `page` should be `{ page }` — but the most critical missing parts are `async` and `await`."],[`E`,`B`,"What is wrong with this import?\n`import { test, expect } from 'playwright';`",null,[`Should import from '@playwright/test', not 'playwright'`,"`expect` cannot be imported alongside `test`",`Curly braces are invalid for named imports`,"Should use `require()` instead of `import`"],0,"The test-runner API lives in `@playwright/test`. The `playwright` package is a lower-level library that does not export `test` or `expect`."],[`E`,`I`,"Spot the error:\n```\ntest.describe('Login', async () => {\n  test('form loads', async ({ page }) => { … })\n})\n```",null,[`The describe callback must be synchronous, not async`,"`test` cannot be nested inside `describe`","`async` is required for describe",`describe must have at least two tests`],0,"`test.describe()` callbacks must be plain synchronous functions. Marking them `async` causes a Playwright runtime error because it changes how Playwright collects tests."],[`E`,`I`,`What is wrong here?
\`\`\`
test('check UI', async ({ page }) => {
  page.goto('http://example.com');
  expect(page).toHaveTitle('Example');
})
\`\`\``,null,["Both `page.goto()` and `expect()` are missing `await`","`expect` should wrap a locator, not `page`","`goto` needs a relative URL only",`The test has no assertion`],0,"Without `await`, `page.goto()` is a floating Promise and the assertion runs immediately on an unloaded page. Always prefix both with `await`."],[`E`,`A`,`Spot the error:
\`\`\`
test('modal', async ({ page }) => {
  await page.goto('/');
  const btn = page.getByRole('button');
  btn.click();
  await expect(btn).toBeHidden();
})
\`\`\``,null,["`btn.click()` is missing `await`","`getByRole` cannot target buttons","`toBeHidden` is not a valid matcher",`The locator must be re-queried after click`],0,"`btn.click()` returns a Promise. Without `await`, the click is dispatched asynchronously and the assertion runs before the button state changes."],[`E`,`I`,`What is wrong with this test file?
\`\`\`
test('A', async ({ page }) => { … })
test.only('B', async ({ page }) => { … })
test('C', async ({ page }) => { … })
\`\`\`
All three tests must run in CI.`,null,["`test.only()` will cause A and C to be skipped — remove it before committing","`test` and `test.only` cannot coexist in one file",`Tests must all use .only or none can`,`C must come before A for .only to work`],0,"`test.only()` scopes execution to test B only in this file. Tests A and C are skipped. Development modifiers like `.only` must always be removed before merging."],[`D`,`I`,"A developer wants to run only one test temporarily. Instead of `test.only()` what CLI alternative exists?",null,['`npx playwright test --grep "exact title"`','`npx playwright test --only "exact title"`','`npx playwright test --single "exact title"`','`npx playwright test --focus "exact title"`'],0,"`--grep` accepts a regex matched against test titles, letting you isolate tests without modifying source code."],[`D`,`B`,`Tests are flaky — sometimes they pass, sometimes fail. The logs show "ElementHandle is closed". What is the likely root cause?`,null,["Missing `await` on an action, causing the next step to run on a disposed element",`The browser was not installed correctly`,`The test timeout is too short`,`The assertion is using the wrong matcher`],0,"A missing `await` means Playwright moves to the next line while the previous action is still pending. The element reference may become stale or the page may navigate away."],[`D`,`A`,"`test.fixme()` was added to a test last sprint. This sprint the underlying bug was fixed and the test now passes. What does Playwright report?",null,[`A failure — a fixme test that passes is flagged as "should be marked passing"`,`A success — the test passes normally`,`A warning — the test is still marked fixme`,`A skip — fixme always skips regardless`],0,"Playwright deliberately fails a `.fixme()` test that passes. This forces the developer to notice and remove the `.fixme()` marker."],[`D`,`B`,"A team's CI skips 40 tests even though no `test.skip()` was added. Investigation shows `test.only()` was left in a spec file. What is the fix?",null,["Remove `test.only()` from the spec file before merging","Add `test.only()` to all other tests","Pass `--ignore-only` flag in CI",`Add skipOnly: false to playwright.config.ts`],0,"A single `test.only()` anywhere causes all other tests in that file to be skipped. The fix is always to remove it before committing."],[`D`,`I`,"A test navigates to `/` in `beforeEach`, but one specific test needs to start at `/admin`. How should the developer handle this?",null,["Call `await page.goto('/admin')` inside that specific test — it overrides beforeEach's navigation",`Duplicate the beforeEach inside the test`,`Use test.skip() on the beforeEach`,`Create a separate describe block with a different beforeEach`],0,"Navigating again inside the test simply replaces the beforeEach navigation. Playwright uses whichever `goto` ran last."],[`D`,`A`,"A developer uses `const title = page.title()` and then `expect(title).toBe('Home')`. The test always fails even though the title is correct. Why?",null,["`page.title()` returns a Promise, not a string — compare a Promise object to a string always fails","`toBe` does not work with strings","`page.title()` is not a valid Playwright method","`expect` requires `await` before it"],0,"`page.title()` is async and returns a Promise. Comparing a Promise with `toBe('Home')` compares an object, not the string. Use `await expect(page).toHaveTitle('Home')` instead."],[`C`,`B`,"Complete the test signature: `test('login', async ({ ______ }) => {`",null,[`page`,`browser`,`context`,`playwright`],0,"`page` is the most common fixture. Playwright provisions a fresh `Page` object before each test and tears it down after."],[`C`,`I`,"Complete the grouping call: `test.______( 'User flows', () => {`",null,[`describe`,`group`,`suite`,`section`],0,"`test.describe()` creates a named block that groups tests, scopes hooks, and labels the report section."],[`C`,`I`,"Complete the marker for a known failing test: `test.______( 'broken search', async ({ page }) => { … })`",null,[`fixme`,`skip`,`broken`,`pending`],0,"`test.fixme()` is the idiomatic known-failure marker. Unlike `test.skip()`, a fixme that starts passing is flagged as unexpected."],[`C`,`I`,"Complete the beforeEach fixture: `test.beforeEach(async ({ ______ }) => { await page.goto('/') })`",null,[`page`,`browser`,`context`,`request`],0,"`beforeEach` receives the same fixtures as `test`. Destructure `page` to run navigation or other setup before each test in the group."],[`C`,`A`,"Complete the per-test annotation: `test('bug', { ______: { type: 'issue', description: 'url' } }, async ({ page }) => { … })`",null,[`annotation`,`tag`,`label`,`meta`],0,"The `annotation` option links a test to an external issue URL. The annotation appears in the HTML report, making it easy to trace failures to tickets."],[`L`,`B`,"If `test.only()` is used in `file-a.spec.ts`, what happens to tests in `file-b.spec.ts`?",null,[`They are unaffected and run normally`,`They are all skipped`,`They run before file-a`,`They run only if file-b also has test.only`],0,"`test.only()` is file-scoped. Tests in other files continue to run normally regardless of `.only` in a different file."],[`L`,`I`,"What is the standard lifecycle order within a `test.describe` block?",null,[`beforeAll → beforeEach → test → afterEach → afterAll`,`beforeEach → beforeAll → test → afterAll → afterEach`,`beforeAll → test → beforeEach → afterEach → afterAll`,`test → beforeEach → afterEach → beforeAll → afterAll`],0,"Hooks run in this exact order: `beforeAll` once, then for each test: `beforeEach` → test body → `afterEach`, and finally `afterAll` once at the end."],[`L`,`I`,"Can `test.describe()` be nested inside another `test.describe()`?",null,[`Yes — nested describes create sub-groups with independently scoped hooks`,`No — describe cannot be nested`,`Yes — but only one level deep`,`No — use test.group() for nesting`],0,"Nesting `test.describe()` creates hierarchical sub-groups. Hooks in an inner describe only apply to tests within that inner block."],[`L`,`A`,"A `test.fixme()` test starts passing after a bug fix. What does Playwright report?",null,[`A failure — fixme tests that pass are flagged as unexpectedly passing`,`A success — the test passes`,`A warning but the suite still passes`,`A skip — fixme always skips`],0,"Playwright intentionally fails a `.fixme()` test that passes, prompting the developer to remove the marker. This prevents forgotten fixme annotations from accumulating."],[`L`,`I`,"Does Playwright automatically clean up the `page` fixture between tests?",null,[`Yes — page is created before each test and closed after automatically`,`No — you must call page.close() at the end of each test`,`Yes — but only if afterEach calls page.reset()`,`No — page persists across the entire test file`],0,"`page` is a test-scoped fixture. Playwright creates a fresh page before each test and destroys it afterwards — no manual cleanup needed."],[`R`,`B`,'How do you tag a test so it can be filtered with `--grep "@smoke"` from the CLI?',null,[`Include @smoke in the test title string`,`Add a tags: ['@smoke'] property to the test config`,`Create a file named smoke.spec.ts`,`Pass --tag flag with the test name`],0,"Playwright's `--grep` matches against test titles. Embedding `@smoke` in the title is the standard tagging convention."],[`R`,`I`,`Your team has 60 tests across 6 spec files. How should you organise them for maintainability?`,null,[`One spec file per feature/page, using test.describe to sub-group`,`One giant spec file with all tests`,`Separate folders for each test individually`,`One spec file per developer`],0,"The conventional structure is one spec file per feature or page, with `test.describe()` blocks for sub-scenarios. This keeps reports readable and failures easy to locate."],[`R`,`I`,`A test needs 90 seconds to complete a large file upload. The global timeout is 30 seconds. What is the least invasive fix?`,null,["Pass `{ timeout: 90_000 }` as the second argument to that specific `test()` call",`Raise the global timeout in playwright.config.ts`,`Split the test into three smaller tests`,`Use a mock to skip the real upload`],0,"A per-test `timeout` override is the least invasive — it affects only that test without raising the global ceiling for all tests."],[`R`,`A`,`You want to run only @critical tests in CI and all tests locally. How do you configure this without editing test files?`,null,['Add `--grep "@critical"` to the CI command in your workflow YAML',`Add test.only() to @critical tests`,`Create a separate playwright.config.ci.ts`,`Use environment variables in beforeEach`],0,"Keep the `--grep` flag in the CI workflow (e.g. GitHub Actions `run:` step). Local runs without `--grep` run all tests."],[`R`,`I`,"A new developer asks why you do not use `console.assert()` for assertions in Playwright tests. What is the key reason?",null,["`expect()` assertions auto-retry until the condition is met or the timeout expires; console.assert does not",`console.assert is not available in browsers`,`Playwright blocks native console methods`,`expect() is faster`],0,"Playwright's `expect()` assertions poll the DOM asynchronously, retrying on failure. `console.assert()` checks only once and does not understand async browser state."],[`S`,`I`,"A developer adds `test.only()` to one test, pushes to CI, and 49 other tests in the file are skipped. What is the correct remediation?",null,["Remove `test.only()` and replace with the regular `test()` before merging","Add `test.only()` to the other 49 tests","Pass `--run-all` flag in CI","Set `ignoreOnly: true` in playwright.config.ts"],0,"`ignoreOnly: true` in config makes Playwright ignore `.only()` modifiers — useful as a CI safeguard. But the real fix is removing it before committing."],[`S`,`A`,"A `beforeEach` navigates to `/login`. One test must start at `/admin` instead. What is the correct approach?",null,["Call `await page.goto('/admin')` inside that test — it replaces the beforeEach navigation",`Use test.skip() for the beforeEach in that test`,`Override beforeEach with a test-level beforeEach`,`Create a separate describe block with no beforeEach`],0,"Calling `goto()` again inside the test simply replaces the previous navigation. Playwright does not lock the page to the beforeEach URL."],[`S`,`I`,"A developer wrote `expect(page.getByText('Submit')).toBeVisible()` without `await`. The test always passes even when the button is absent. Why?",null,["Without `await`, the assertion returns a Promise that is never settled — Playwright treats an unresolved Promise as passing",`expect() works synchronously when called without await`,`toBeVisible() defaults to true without await`,`Playwright ignores assertions without await`],0,"An unawaited `expect()` call creates a floating Promise. The test function exits (passes) before the assertion resolves — the failure is silently swallowed."],[`S`,`A`,"A team has 5 spec files, each with 10 tests. A developer wraps 3 tests in `test.describe.only()` in one file. How many tests run in total?",null,[`3 — only the tests in the .only describe block in that file; other files are unaffected`,`3 — only the tests in that file, all others globally skipped`,`50 — describe.only has no effect`,`13 — 3 from the .only block plus 10 from each other file`],0,"`describe.only()` is file-scoped: the 3 tests inside it run, the other 7 tests in that file are skipped, and all 40 tests in the other 4 files run normally."],[`O`,`B`,"What does `test.skip()` do when called inside a test body?",null,[`Marks the test as failed`,`Skips the rest of the test body and marks it as skipped in the report`,`Skips all remaining tests in the file`,`Skips the test only if a condition is passed`],1,`test.skip() inside the body stops execution at that point and marks the test skipped — useful for conditional skips based on runtime state.`],[`O`,`B`,"What does `test.slow()` do?",null,[`Adds artificial delays between actions`,`Triples the test's timeout — useful for tests that legitimately take longer`,`Enables slow-motion for the browser`,`Reduces worker priority for that test`],1,`test.slow() multiplies the configured timeout by 3, signalling that the test is intentionally slow without changing global settings.`],[`O`,`I`,"What is `testInfo` in Playwright?",null,[`The playwright.config.ts object passed to globalSetup`,`An object available in every test via the second argument, containing test title, status, output path, annotations, and more`,`A global singleton holding suite metadata`,`The return value of page.evaluate()`],1,`testInfo gives each test access to its own metadata — attach files, read the test title, check retry count, and write to testInfo.outputDir for artifacts.`],[`O`,`B`,'What does `test.describe.configure({ mode: "serial" })` do?',null,[`Configures the describe block to run tests in alphabetical order`,`Forces all tests in the describe block to run sequentially on one worker`,`Enables serial network request logging`,`Disables parallelism for the entire suite`],1,`describe.configure is the newer API for controlling execution mode — equivalent to test.describe.serial() but more explicit.`],[`O`,`I`,'What does `test.step("name", async () => {})` provide?',null,[`A nested describe group with its own hooks`,`A labelled sub-step visible in the Trace Viewer and HTML report — useful for grouping related actions within one test`,`A synchronous block inside an async test`,`A checkpoint that can be retried independently`],1,`test.step creates named sub-steps in the trace and report, making long tests easier to navigate visually.`],[`O`,`B`,"What is the result of calling `expect(value).not.toBe(5)`?",null,[`Passes if value strictly equals 5`,`Passes if value does not strictly equal 5`,`Throws immediately without polling`,`Negates only the type check`],1,`.not inverts any matcher — expect(x).not.toBe(5) passes when x !== 5.`],[`O`,`I`,"What does `test.beforeAll` vs `test.beforeEach` do differently?",null,[`beforeAll runs before each test; beforeEach runs before the first test only`,`beforeAll runs once before all tests in the describe scope; beforeEach runs before every single test in the scope`,`They are identical`,`beforeAll is for page fixtures; beforeEach is for context fixtures`],1,`beforeAll is once-per-scope setup; beforeEach is per-test setup — wrong choice causes either repeated expensive setup or stale state.`],[`O`,`I`,"When does Playwright retry a failing test with `retries: 2`?",null,[`Immediately if any assertion fails`,`After the test function throws or an assertion rejects — the whole test runs again from the start, up to 2 additional times`,`Only if the failure is a timeout`,`Only on the second run in CI`],1,`Each retry re-runs the complete test function in a fresh browser context — there is no partial retry.`],[`O`,`B`,"What does `expect.soft()` do differently from `expect()`?",null,[`It is faster because it does not auto-retry`,`A soft assertion failure does not stop the test — execution continues and all soft failures are reported at the end`,`It only works on primitive values`,`It disables the web-first retry logic`],1,`Soft assertions let you collect multiple failures in one test run without stopping at the first — useful for form validation tests.`],[`O`,`I`,'What is the purpose of `test.info().attach("name", { body, contentType })`?',null,[`Adds a file to node_modules for the test`,`Attaches an artifact (screenshot, JSON, log) to the test's HTML report entry`,`Sends the body to a remote reporter`,`Annotates the test title in the report`],1,`attach() adds downloadable artifacts to the test report — helpful for debugging failures with custom data.`],[`O`,`A`,"What does `test.fail()` do when called at the top of a test?",null,[`Immediately fails the test and stops execution`,`Marks the test as expected to fail — if it passes, Playwright reports it as unexpectedly passing; if it fails, it is reported as expected failure (xfail)`,`Disables retries for the test`,`Makes the test a no-op`],1,`test.fail() is the xfail mechanism — document known broken tests without hiding them, and get alerted when they unexpectedly start passing.`],[`O`,`I`,'What does `test.fixme(condition, "reason")` do when condition is false?',null,[`The test is always skipped`,`When condition is false, the marker has no effect — the test runs normally; when true, it is skipped as fixme`,`The reason string is logged regardless`,`fixme requires a non-conditional call`],1,`Conditional fixme lets you skip tests only in specific environments (e.g. only on Windows) without separate if-blocks.`],[`F`,`B`,"Complete to add a per-test timeout override.\n```ts\ntest('slow upload', async ({ page }) => {\n  // ...\n}, { ______: 90_000 });\n```",null,[`timeout`,`maxTime`,`duration`,`wait`],0,`The third argument to test() is an options object — timeout overrides the global per-test limit for this one test.`],[`F`,`B`,`Complete to skip a test conditionally.
\`\`\`ts
test('windows only', async ({ page }) => {
  test.______(process.platform !== 'win32', 'Only runs on Windows');
  // ...
});
\`\`\``,null,[`skip`,`ignore`,`cancel`,`fixme`],0,`test.skip(condition, reason) skips the test when condition is true — execution of the test body stops at that line.`],[`F`,`I`,`Complete to attach a file to the report.
\`\`\`ts
await test.info().______("screenshot", {
  body: await page.screenshot(),
  contentType: "image/png",
});
\`\`\``,null,[`attach`,`addFile`,`saveArtifact`,`upload`],0,`attach() links the binary body to the test in the HTML report as a downloadable artifact.`],[`F`,`B`,`Complete to name a sub-step in the trace.
\`\`\`ts
await test.______("fill login form", async () => {
  await page.getByLabel("Email").fill("user@test.com");
  await page.getByLabel("Password").fill("secret");
});
\`\`\``,null,[`step`,`describe`,`group`,`action`],0,`test.step() creates a named section in the trace viewer and HTML report.`],[`F`,`I`,"Complete to access the current test title inside a test.\n```ts\nconst title = test.______.title;\n```",null,[`info()`,`config`,`context`,`meta`],0,`test.info() returns the TestInfo object for the currently running test — title, status, outputDir, etc.`],[`F`,`I`,`Complete to mark a test as expected to fail.
\`\`\`ts
test('known bug', async ({ page }) => {
  test.______();
  // ...
});
\`\`\``,null,[`fail`,`xfail`,`broken`,`skip`],0,`test.fail() marks the test as expected to fail — it flips the pass/fail signal.`],[`F`,`B`,"Complete to apply a hook to the whole file's tests.\n```ts\ntest.______(() => {\n  console.log('runs before every test in this file');\n});\n```",null,[`beforeEach`,`beforeAll`,`setup`,`before`],0,`test.beforeEach() without a wrapping describe applies to all tests in the file.`],[`E`,`B`,`What is wrong?
\`\`\`ts
test('check title', async ({ page }) => {
  await page.goto('https://example.com');
  expect(page.title()).toBe('Example Domain'); // missing await
});
\`\`\``,null,[`page.title() must use getByTitle() instead`,`page.title() returns a Promise — without await the Promise object is compared to the string, always failing or incorrectly passing`,`toBe() handles Promises automatically`,`title() is deprecated — use page.locator("title")`],1,`Always await page.title() — it is async. Unawaited Promises compared with toBe() always fail the strict equality check.`],[`E`,`B`,"What is wrong?\n```ts\ntest.beforeAll(async ({ page }) => {\n  await page.goto('/login');\n});\n```",null,[`beforeAll cannot be async`,`page is a test-scoped fixture and cannot be used in beforeAll — beforeAll receives worker-scoped fixtures only. Use a context or browser fixture instead`,`beforeAll must be inside a describe block`,`goto() is not valid in beforeAll`],1,`page is re-created per test; beforeAll runs once per describe/file scope — using a test-scoped fixture there is a lifecycle mismatch.`],[`E`,`I`,`What is wrong?
\`\`\`ts
test('soft check', async ({ page }) => {
  expect.soft(await page.textContent('h1')).toBe('Home');
  expect.soft(await page.textContent('h2')).toBe('Welcome');
  // no final check
});
\`\`\``,null,[`expect.soft() cannot be used twice in one test`,`Soft assertion failures accumulate but are only reported if the test itself completes — without a final expect(page) or asserting that there are no soft failures, the soft failures may be silently ignored if no hard assertion also runs`,`textContent() must be awaited inside expect`,`expect.soft() requires the test to be marked test.fail()`],1,`After soft assertions, call expect(page).not.toBeNull() or any hard assertion to ensure the test body finishes and soft failures are flushed to the report.`],[`E`,`I`,"What is wrong?\n```ts\ntest.afterEach(async ({ page }) => {\n  await page.close();\n});\n```",null,[`afterEach cannot be async`,`page is closed automatically after each test by the fixture system — calling page.close() manually again causes a "Target closed" error on the next test's page operations`,`afterEach must use context.close() instead`,`Closing page in afterEach is required for cleanup`],1,`Never close page manually — Playwright's test-scoped fixture closes it automatically. Double-closing causes errors.`],[`E`,`A`,`What is wrong?
\`\`\`ts
test('check values', async ({ page }) => {
  const price = await page.textContent('.price');
  const stock = await page.textContent('.stock');
  expect(Number(price)).toBeGreaterThan(0);
  expect(stock).not.toBeNull();
});
// Test passes despite .price showing "$0.00"
\`\`\``,null,[`textContent() returns raw text including "$" — Number("$0.00") is NaN, and expect(NaN).toBeGreaterThan(0) unexpectedly passes in some assertion libraries`,`toBeGreaterThan cannot handle NaN`,`Number() strips currency symbols automatically`,`This test would correctly fail`],0,`Number("$0.00") is NaN. NaN comparisons are tricky — strip the "$" before converting: Number(price!.replace("$","")).`],[`E`,`B`,"What is wrong?\n```ts\ntest('a', () => { // no async\n  page.goto('https://example.com');\n});\n```",null,[`test() callbacks must be synchronous`,"The callback is not async and there is no page fixture in scope — page comes from the fixture argument `{ page }`, and goto() must be awaited",`test() cannot take an arrow function`,`page is a global variable`],1,"Always destructure page from the fixture parameter `async ({ page }) =>` and await every Playwright call."],[`D`,`B`,`A test times out every time even though the page loads in 2 seconds. The global timeout is 5 seconds. What should you check?`,null,[`Restart the test runner`,`Look for a missing await on an async action — an unawaited goto() or fill() returns immediately, but a subsequent assertion may time out waiting for state that was never actually achieved`,`Increase timeout to 30 seconds`,`Switch to a faster browser`],1,`Missing await is the most common cause of "phantom" timeouts — the action appears instant but leaves the browser in an unexpected state.`],[`D`,`I`,"`test.beforeAll` runs but its changes are not visible in the tests. Why?",null,[`beforeAll changes are garbage collected`,`If beforeAll modifies a page fixture and tests use a fresh page, the changes are lost — beforeAll runs once and tests each get their own page. Use a worker-scoped context fixture to persist state across tests in the scope`,`beforeAll is asynchronous and tests start before it finishes`,`Tests run in a different worker from beforeAll`],1,`Test-scoped fixtures like page are reset per test — state from beforeAll only persists if stored in a worker-scoped fixture or a shared variable.`],[`D`,`B`,`A test assertion fails but the failure message shows a cryptic Promise object instead of a value. What is the cause?`,null,[`The assertion library has a bug`,`An async Playwright API (like page.textContent()) was not awaited — the Promise was passed directly to expect() instead of the resolved value`,`expect() does not support async values`,`The test needs a longer timeout`],1,`Always await Playwright APIs before passing their results to expect() — unawaited async calls produce Promise objects, not values.`],[`D`,`I`,`Tests fail with "Target page, context or browser has been closed" inside beforeEach. What is the likely cause?`,null,[`beforeEach always closes the browser`,`A previous afterEach or afterAll unexpectedly closed the context or browser — check teardown hooks for premature close() calls`,`beforeEach creates a new page automatically`,`The page fixture must be re-opened in beforeEach`],1,`Premature context/browser close in a teardown hook destroys the fixtures before subsequent tests can use them.`],[`D`,`A`,"A test file has both `test.describe.serial()` and `test.describe.parallel()` at the top level. Which tests run first?",null,[`Parallel tests always run before serial`,`Playwright schedules available workers greedily — serial blocks run on one worker in order; parallel blocks distribute across workers concurrently. The wall-clock ordering depends on worker availability, not file order`,`Serial always runs last`,`File order determines execution order`],1,`Understanding worker scheduling matters for debugging interleaving — serial is guaranteed sequential, parallel is concurrent, but cross-block ordering is not guaranteed.`],[`C`,`B`,`Complete to mark a test as a known broken test.
\`\`\`ts
test('broken feature', async ({ page }) => {
  test.______();
  await page.goto('/broken');
});
\`\`\``,null,[`fixme`,`skip`,`fail`,`pending`],0,`test.fixme() marks the test as expected to be fixed later — it is skipped with a "fixme" annotation in the report.`],[`C`,`I`,`Complete to triple the timeout for a slow test.
\`\`\`ts
test('import csv', async ({ page }) => {
  test.______();
  // long operation
});
\`\`\``,null,[`slow()`,`triple()`,`longTimeout()`,`extend()`],0,`test.slow() multiplies the configured timeout by 3 — no magic numbers needed.`],[`C`,`B`,"Complete the afterAll hook.\n```ts\ntest.______(() => {\n  db.disconnect();\n});\n```",null,[`afterAll`,`afterEach`,`teardown`,`cleanup`],0,`test.afterAll() runs once after all tests in its scope complete.`],[`C`,`I`,"Complete to annotate a test with a bug tracker link.\n```ts\ntest('checkout bug', async ({ page }) => {\n  test.______({ type: 'issue', description: 'https://github.com/org/repo/issues/42' });\n});\n```",null,[`info().annotations.push`,`annotate`,`tag`,`link`],0,`test.info().annotations.push({ type, description }) adds a clickable annotation to the HTML report entry.`],[`C`,`I`,'Complete to run only this describe block and skip others in the file.\n```ts\ntest.describe.______("focused group", () => {\n  test("a", …);\n});\n```',null,[`only`,`serial`,`parallel`,`focus`],0,`describe.only() restricts the run to that block within the file (subject to forbidOnly in CI).`],[`L`,`B`,"Why must every test callback be declared `async`?",null,[`Playwright requires async for TypeScript compatibility`,`Playwright actions (goto, fill, click) return Promises — without async/await they execute out of order or their results are never awaited`,`async enables the fixture injection system`,`Playwright uses async functions to enable retries`],1,`All Playwright browser interactions are async — without async/await the test finishes before the browser actions complete.`],[`L`,`I`,`What is the execution order of hooks in nested describes?
\`\`\`ts
describe("outer", () => {
  beforeEach(outerBefore)
  describe("inner", () => {
    beforeEach(innerBefore)
    test("t", ...) // order?
  })
})
\`\`\``,null,[`innerBefore → outerBefore → t`,`outerBefore → innerBefore → t`,`t → outerBefore → innerBefore`,`Both beforeEach run in parallel before t`],1,`Hooks run from outermost to innermost — outerBefore sets up context, innerBefore refines it, then the test runs.`],[`L`,`I`,"Does `test.afterAll` receive the `page` fixture?",null,[`Yes — afterAll receives all fixtures that the tests used`,`No — afterAll receives only worker-scoped fixtures, not test-scoped fixtures like page`,`Yes, but page is already closed`,`No — afterAll receives no fixtures at all`],1,`page is test-scoped and destroyed after each test — it does not exist in afterAll. Use worker-scoped fixtures like browser or context for afterAll teardown.`],[`L`,`B`,"If `retries: 2` is set and a test fails on the first attempt then passes on the second, what does the report show?",null,[`The test is shown as passed`,`The test is shown as "flaky" — it failed at least once and passed at least once`,`The test is shown as failed because it failed first`,`The test is shown as skipped`],1,`Playwright marks tests as "flaky" when they pass after one or more retries — helping teams identify tests that need investigation.`],[`L`,`A`,"Can you access `testInfo.outputDir` to save custom artifacts?",null,[`No — outputDir is read-only and Playwright controls all artifacts`,`Yes — testInfo.outputDir is the recommended directory for per-test artifacts; you can write files there and they appear in the report as attachments`,`Only if trace: "on" is set`,`Only in afterEach, not during the test`],1,`testInfo.outputDir provides a unique directory per test run — write anything there and optionally attach it with test.info().attach().`],[`R`,`B`,`You want the test title to appear in the screenshot filename for easier debugging. How?`,null,[`Playwright automatically uses the title`,"Use testInfo.title inside the filename: path.join(testInfo.outputDir, `${testInfo.title}.png`)",`Use a fixed filename — Playwright appends the title automatically`,`Set screenshotTitle: true in config`],1,`testInfo.title and testInfo.outputDir together give you a unique, descriptive artifact path per test.`],[`R`,`I`,`You need to run "sanity" tests (tagged @sanity) on every PR and all tests nightly. How?`,null,[`Maintain two playwright.config.ts files`,`Use --grep "@sanity" in the PR workflow and no --grep in the nightly workflow — same test suite, different filter`,`Create separate test files for sanity tests`,`Use test.describe.only() for sanity tests`],1,`CLI --grep filter on the same suite is the cleanest approach — one codebase, multiple run profiles.`],[`R`,`I`,`You need to pass a test-specific piece of data (user email) to a helper function. How?`,null,[`Use a global variable`,`Pass it as a parameter to the helper — keep tests stateless and explicit`,`Use testInfo.annotations`,`Store it in localStorage before the test`],0,`Explicit parameters are clearest — avoid global mutable state which causes parallel test interference.`],[`R`,`A`,`You want beforeEach to run for all tests in the file except one specific test. What is the approach?`,null,[`Use test.skip() in the problematic test to avoid beforeEach`,`Call test.use({ beforeEach: false })`,`Refactor: move the tests that need beforeEach into a describe block containing the hook; leave the exception outside that block`,`Delete the beforeEach and add setup to each test individually`],2,`Scoping beforeEach inside a describe block is the clean approach — hooks only apply within their scope.`],[`R`,`B`,`You want to add a note to a test explaining it is blocked by a third-party issue. How?`,null,[`Add a code comment`,`Call test.info().annotations.push({ type: "issue", description: "JIRA-123: blocked by vendor bug" }) — it appears in the HTML report`,`Use test.fixme("JIRA-123")`,`Add it to the test title string`],1,`Annotations appear in the HTML report alongside the test result — more discoverable than code comments.`],[`S`,`B`,`A test that was passing suddenly starts failing with "strict mode violation: multiple elements". The UI added a duplicate button. What is the correct fix?`,null,[`Use .first() as a quick fix`,`Update the locator to be uniquely specific (add a role, aria-label, or test-id) so only one element is matched — strict mode violations are Playwright telling you the selector is ambiguous`,`Disable strict mode in config`,`Add an nth() filter to always take the first match`],1,`Strict mode violations reveal ambiguous locators — the right fix is a more specific selector, not silencing the warning.`],[`S`,`I`,"A developer uses `test.describe.serial()` for a 10-step flow. Test 5 fails. What happens to tests 6-10?",null,[`Tests 6-10 run with the state from test 4`,`Tests 6-10 are skipped — serial mode stops the sequence on first failure (unless retries are configured)`,`Tests 6-10 run in parallel`,`All 10 tests are retried from the start`],1,`Serial block failure stops the sequence — subsequent tests depend on prior state that is now invalid.`],[`S`,`A`,`You need to verify that your app shows a rate-limit message after 5 rapid form submissions. How do you write this test cleanly?`,null,[`Submit the form 5 times with separate test() calls in serial`,`Write a loop inside one test: for (let i = 0; i < 5; i++) { await page.getByRole("button").click() } then assert the rate-limit message — all within a single test.step("submit 5 times", ...)`,`Use test.repeat(5) to run the submission test 5 times`,`Mock the rate-limit response`],1,`A loop inside a single test is cleaner than 5 separate tests — use test.step to label the loop for the trace viewer.`],[`S`,`I`,`Tests pass individually but fail when the full suite runs. What is the most likely cause?`,null,[`The browser cache is not cleared between tests`,`Shared mutable state between tests — a module-level variable, a beforeAll side-effect, or shared database records that one test modifies and another reads`,`retries: 0 causes them to fail without re-run`,`Tests must be run in alphabetical order`],1,`Tests must be independent — when run together, execution order exposes shared state bugs. Each test should create its own data.`],[`L`,`I`,"What is the difference between `test.describe()` (default parallel across files) and `test.describe.serial()`?",null,["Tests inside a `.serial()` block run in the order they are written, in a single worker, and if one fails, Playwright skips the remaining tests in that block by default — useful when tests depend on shared state built up step by step",`serial() makes tests run on multiple browsers simultaneously`,`They are functionally identical; serial is just a naming convention`,`serial() disables all assertions except the last one in the block`],0,`Serial mode exists specifically for the rare case where tests genuinely depend on each other's side effects — the default (independent, parallelizable tests) is almost always preferred.`],[`O`,`I`,"What is the scope of the `page` created by a `beforeEach` hook here?",`test.beforeEach(async ({ page }) => {
  await page.goto('/dashboard');
});
test('shows widgets', async ({ page }) => {
  await expect(page.locator('.widget')).toBeVisible();
});`,[`A fresh page (and BrowserContext) is created for EACH test, so beforeEach's navigation runs again from scratch before every single test in the file — no state leaks between tests`,`The same page instance and navigation persist across all tests in the file, only running once`,`beforeEach only applies to the very first test in the file`,`page is shared globally across all test files in the suite`],0,`Playwright's test isolation model gives each test its own context/page by default — beforeEach hooks re-run their setup for every single test, keeping tests independent.`],[`O`,`I`,"What does wrapping actions in `test.step()` provide, beyond just running the same code without it?",`test('checkout flow', async ({ page }) => {
  await test.step('add item to cart', async () => {
    await page.getByRole('button', { name: 'Add to cart' }).click();
  });
  await test.step('go to checkout', async () => {
    await page.getByRole('link', { name: 'Checkout' }).click();
  });
});`,[`It groups actions into named, collapsible sections in the HTML report and trace viewer, making it much easier to see exactly which logical step failed in a long test — with no change to actual execution behavior`,`It runs each step in a separate, isolated BrowserContext`,`It causes each step to retry independently up to 3 times on failure`,`test.step() is required for assertions to work inside a test`],0,`test.step() is purely a reporting/readability tool — it doesn't change what runs, just how clearly it's presented when you're debugging a failure.`],[`O`,`I`,"What happens when a `expect.soft()` assertion fails, compared to a regular `expect()`?",`test('profile page', async ({ page }) => {
  await expect.soft(page.locator('h1')).toHaveText('Profile');
  await expect.soft(page.locator('.email')).toBeVisible();
  await page.getByRole('button', { name: 'Edit' }).click();
});`,[`A failing soft assertion is recorded as a failure but does NOT stop the test immediately — execution continues to the next line, so you can see ALL failures from a single run instead of only the first one`,`expect.soft() failures are completely ignored and never reported`,`soft assertions can only be used for visual comparisons, never text checks`,`expect.soft() automatically retries indefinitely until it passes`],0,`Soft assertions trade 'stop at first failure' for 'collect every failure' — useful when checking many independent things on one page in a single test.`],[`L`,`B`,"What is the difference between `test.skip()` and `test.fixme()`?",null,["Both skip the test from running, but `test.fixme()` additionally communicates 'this test is known broken and needs fixing' — some tooling/reporting distinguishes fixme tests as tracked technical debt rather than an intentional, permanent skip",`test.fixme() actually runs the test but ignores its result; test.skip() does not run it at all`,`They are exact synonyms with no practical difference`,`test.fixme() can only be used on entire files, never individual tests`],0,`The semantic distinction matters for team communication — fixme signals "this needs attention," while skip can mean many things (not applicable, deprecated, environment-specific).`],[`R`,`I`,`You need to seed a test database once before the ENTIRE suite runs (not before every individual test), and tear it down once after everything finishes. What is the correct Playwright mechanism?`,null,["A `globalSetup` (and `globalTeardown`) file referenced in playwright.config.ts — these run exactly once for the whole test run, separately from any per-test or per-file hook","Put the seeding code in `test.beforeEach()` and rely on it being idempotent",`Playwright has no way to run something exactly once for an entire run`,`Use a worker-scoped fixture, since workers already run once per test file`],0,`globalSetup/globalTeardown are specifically for run-wide, one-time setup and cleanup — distinct from per-test (beforeEach) or per-worker (worker fixture) scopes.`],[`L`,`A`,"Why does Playwright provide `test.extend()` to create custom fixtures instead of just having every test import and call setup helper functions directly?",null,[`Fixtures are declarative and automatically resolved in dependency order with automatic teardown, work seamlessly with parallel workers, and are only instantiated when a test actually requests them — plain helper functions require manual invocation and manual cleanup in every test`,`test.extend() is purely syntactic sugar with no behavioral difference from calling a function at the top of each test`,`Custom fixtures can only be used with the page object, not with browser or context`,`test.extend() disables parallelism for any test that uses a custom fixture`],0,`The fixture system's real value is automatic lifecycle management (setup, teardown, lazy instantiation, dependency resolution) that hand-written helpers do not give you for free.`],[`L`,`A`,"What is the practical difference between a test-scoped fixture and a worker-scoped fixture created via `test.extend()`?",null,[`A test-scoped fixture is created fresh for every single test and torn down after; a worker-scoped fixture is created once per worker process and reused across all tests that run in that worker, making it suitable for expensive, shareable setup like a database connection`,`They are identical; scope only affects naming, not lifecycle`,`Worker-scoped fixtures cannot be used inside test-scoped fixtures under any circumstances`,`Test-scoped fixtures persist across the whole file while worker-scoped fixtures reset every test`],0,`Choosing the right scope is a tradeoff between full isolation (test-scoped) and performance (worker-scoped) for genuinely expensive, safely-shareable resources.`],[`L`,`A`,"Why might a fixture be declared with `{ auto: true }` in `test.extend()`?",null,[`An auto fixture runs for every test in scope automatically, even if no test destructures it from its parameters — useful for cross-cutting setup like starting a mock server or seeding shared test state that every test implicitly needs`,`auto: true makes the fixture run only once for the entire test file regardless of worker boundaries`,`auto fixtures cannot perform any teardown logic after the test`,`auto: true is required for any fixture that returns a Promise`],0,`Normal fixtures are lazy — only instantiated when destructured; auto fixtures opt out of that laziness for genuinely universal setup.`],[`L`,`I`,"Why does `test.use({ viewport: { width: 375, height: 812 } })` inside a `describe` block only affect tests within that block, not the whole file?",null,[`test.use() scopes its fixture option overrides to the nested block it is called within — sibling describe blocks or tests outside it keep the original (global or project-level) fixture values`,`test.use() always applies globally regardless of where it is called`,`test.use() can only be called once per file, at the very top`,`test.use() only works when called inside a beforeEach hook`],0,`This scoping lets you mix, say, a mobile-viewport describe block and a desktop-viewport describe block within the same spec file.`],[`L`,`A`,"Why does Playwright provide `mergeTests()` (and `mergeExpects()`) as separate utilities from `test.extend()`?",null,[`mergeTests() lets you combine multiple independently-defined fixture sets (often from separate files/packages) into a single test object, which is useful when different teams or plugins each define their own fixtures that a project wants to use together`,`mergeTests() is a deprecated alias for test.extend() kept only for backward compatibility`,`mergeTests() only works with worker-scoped fixtures, never test-scoped ones`,`mergeTests() automatically deduplicates test titles across merged fixture sets`],0,`As fixture-based plugins/packages become common, mergeTests() offers a clean way to compose them without manually re-declaring every fixture in one giant test.extend() call.`],[`L`,`I`,"What does `expect.poll()` provide that a plain `expect(locator).toHaveText(...)` assertion does not?",null,[`expect.poll() lets you retry an arbitrary synchronous or async function (not just a locator or page) until it returns a value matching the assertion — useful for polling a database query, an API response, or any custom condition, not just DOM state`,`expect.poll() is strictly faster than a normal locator assertion`,`expect.poll() disables the default assertion timeout entirely, waiting forever`,`expect.poll() can only be used with numeric values, never strings or objects`],0,`Web-first locator assertions already auto-retry against the DOM; expect.poll() extends that same retry-until-match behavior to any arbitrary callback function.`],[`L`,`I`,"Why would a team register a custom matcher via `expect.extend({ toBeWithinRange(received, floor, ceiling) { ... } })`?",null,["It lets domain-specific assertions read naturally in tests (e.g. `expect(price).toBeWithinRange(10, 20)`) and centralizes the comparison logic and failure message formatting in one reusable place instead of repeating raw comparisons across many tests",`Custom matchers are required before any test can use expect() at all`,`expect.extend() can only add matchers for locator-based assertions, never plain values`,`Custom matchers automatically replace all of Playwright's built-in matchers once registered`],0,`Custom matchers are a readability and DRY tool — they wrap repeated assertion logic into an expressive, reusable, well-named API.`],[`L`,`A`,"What does `expect(locator).toPass(async () => { ... })`-style retryable custom logic (via `expect.toPass()`) provide over a single `expect()` call?",null,[`toPass() repeatedly re-runs an entire block of arbitrary code (potentially containing multiple actions and assertions) until it succeeds or times out — useful when a sequence of steps together represents one eventually-consistent condition, not just a single assertion`,`toPass() is a synonym for expect.soft() with no functional difference`,`toPass() disables retries and requires the block to succeed on the first attempt`,`toPass() can only wrap exactly one expect() call at a time`],0,`toPass() generalizes Playwright's auto-retry philosophy to a whole callback of logic, not just a single locator check — handy for flaky multi-step eventually-consistent conditions.`],[`L`,`I`,"Why does wrapping a helper function's internal actions in `test.step(..., { box: true })` change what appears in the trace viewer, compared to a plain `test.step()`?",null,["`box: true` collapses the step's internal implementation details in the trace/report, showing only the step itself as a single clean entry — useful for reusable helper functions where the internals are noise and only the outcome matters to whoever is debugging a failing test",`box: true prevents the step from ever failing, always reporting success`,`box: true makes the step run in complete isolation from the rest of the test, with its own browser context`,`box: true is required for any step nested inside another step`],0,`box: true is aimed at library/helper authors — it hides internal noise from the trace so consumers of a helper see a clean, single step rather than a dozen nested sub-actions.`],[`L`,`I`,"Can a `test.step()` return a value, and if so, how is it typically used?",null,[`Yes — the callback passed to test.step() can return a value (including a Promise resolving to one), which becomes the return value of the awaited test.step() call itself, letting you extract data computed during a labeled step for use later in the test`,`No — test.step() always returns undefined regardless of what its callback returns`,`Yes, but only primitive values like numbers and strings are allowed, never objects`,`Returning a value from test.step() causes the step to be excluded from the report`],0,`test.step() behaves like a normal async function wrapper for reporting purposes — whatever the callback returns becomes available via await test.step(...).`],[`L`,`I`,"What does `testInfo.retry` tell you when accessed inside a running test?",null,[`It reports which attempt this is — 0 for the first run, 1 for the first retry, 2 for the second retry, and so on — letting test code behave differently on a retry (e.g. adding extra logging) if genuinely needed`,`It reports the total number of retries configured globally in playwright.config.ts`,`It is always 0 unless the test has already finished and failed permanently`,`testInfo.retry only exists when running with the --debug flag`],0,`testInfo.retry is most often used to add diagnostic behavior (like extra tracing) specifically on retry attempts, without needing separate test logic for first-run vs retried runs.`],[`L`,`B`,`Does Playwright forbid having two tests with the identical title string in the same file?`,null,["No — duplicate titles are allowed and both tests will run, but it makes reports and `--grep` filtering ambiguous since you cannot distinguish them by title alone, so unique, descriptive titles are strongly recommended",`Yes — Playwright throws a configuration error at collection time if two titles are identical`,`Duplicate titles are only allowed if the tests are in different describe blocks`,`The second test with a duplicate title is automatically renamed by appending a number`],0,`Nothing technically breaks with duplicate titles, but it undermines the very reason titles exist: to uniquely and clearly identify a specific test in reports and CLI filters.`],[`L`,`A`,"How does `fullyParallel: true` interact with a `test.describe.serial()` block inside the same file?",null,[`fullyParallel changes how independent tests/describes within a file are scheduled across workers, but it does not override the explicit sequential guarantee of a serial() block — tests inside serial() still run in order, on one worker, even in a fullyParallel file`,`fullyParallel forcibly converts every serial() block into a parallel one, ignoring the explicit mode`,`serial() blocks are ignored entirely and treated as an error when fullyParallel is enabled`,`fullyParallel has no effect at all in files containing any describe.serial() block`],0,`serial() is an explicit, stronger guarantee about relative ordering within that group — fullyParallel affects scheduling elsewhere in the file, not inside an already-serialized block.`],[`L`,`I`,"Why might a helper function accept a `page` parameter explicitly rather than trying to access a globally shared page reference?",null,[`Explicit parameters keep tests stateless and safe for parallel execution — each worker/test has its own page instance, and passing it explicitly avoids accidentally sharing or leaking state across tests that might run concurrently`,`Global variables are technically disallowed by the @playwright/test package and will throw a runtime error`,`page can only be accessed through function parameters; it is never available as an import`,`Passing page explicitly is required only when using TypeScript, not JavaScript`],0,`A page fixture only exists per-test — reaching for a shared global would either not compile conceptually or silently create hard-to-debug cross-test coupling.`],[`L`,`I`,"What is the purpose of passing a custom message as the second argument to `expect()`, e.g. `expect(count, 'cart should have 2 items after adding').toBe(2)`?",null,[`If the assertion fails, the custom message is included in the failure output alongside the standard diff, making it much faster to understand WHY that particular check mattered without having to re-read the surrounding test code`,`The second argument changes which matcher is used internally`,`Custom messages disable the assertion's auto-retry behavior`,`The message argument is only supported for locator-based assertions, not plain values`],0,`A well-chosen custom message turns a generic "expected 2, received 3" failure into a debugging shortcut that explains business context, not just raw values.`],[`L`,`A`,`Why does a worker-scoped fixture make sense for something like a shared test-database connection, but a bad fit for something like the current logged-in user?`,null,[`A database connection is expensive to create but safe to share (read connections do not usually conflict), while a logged-in user represents test-specific state — sharing it across tests in a worker would leak session/auth state between otherwise-independent tests`,`Worker-scoped fixtures cannot hold any object that has methods, only plain data`,`Database connections must always be test-scoped for correctness reasons`,`There is no meaningful difference; either can safely be worker-scoped`],0,`Fixture scope should match how "shareable" the underlying resource actually is — cheap-but-stateful things usually want test scope; expensive-but-stateless things are good worker-scope candidates.`],[`L`,`I`,"What does `test.step.skip()` allow you to do that a plain `if` statement around a `test.step()` call would not?",null,[`It explicitly marks a specific step as skipped in the trace/report (visible with a distinct icon/status) rather than the step simply never appearing at all, making it clear to someone reading the report that a step was intentionally bypassed rather than just absent`,`It skips the remainder of the entire test, not just that one step`,`It is functionally identical to wrapping the step body in an if statement, with no reporting difference`,`test.step.skip() can only be used inside test.describe.serial() blocks`],0,`Visibility in the report matters — an explicitly skipped step communicates intent, whereas a step that simply never runs due to an if-check leaves no trace at all.`],[`L`,`A`,"Why is `test.fail(condition, reason)` (conditional fail) useful compared to unconditionally calling `test.fail()`?",null,[`A conditional fail lets you mark a test as expected-to-fail only in specific circumstances (e.g. a known bug that only reproduces on a particular browser project), while the test runs normally everywhere else — unconditional fail() always flips the expectation regardless of environment`,`Conditional fail() disables the test entirely rather than marking it as expected to fail`,`test.fail(condition, reason) is only valid inside beforeEach hooks`,`There is no difference; the condition argument is ignored by Playwright`],0,`Conditional annotations (skip, fixme, fail, slow all support a condition argument) let known issues be scoped precisely to the environment where they actually occur.`],[`L`,`I`,"What is the relationship between a `describe` block's title and how nested `test.step()` names appear in the HTML report?",null,[`The report groups results hierarchically: describe blocks form top-level (and nested) sections, individual tests appear under their describe, and test.step() calls appear as an even finer-grained, collapsible breakdown within each individual test's result`,`test.step() names replace the describe title entirely in the report`,`Describe titles are only used for filtering with --grep and never appear in the HTML report itself`,`Steps are always flattened into a single list regardless of describe nesting`],0,`The reporting hierarchy mirrors the code structure: describe > test > step, each level adding a finer level of detail for navigating a large, well-organized report.`],[`L`,`A`,"Why might a team prefer a data-driven loop generating multiple `test()` calls over one single test that loops internally through multiple data cases?",null,["Generating a separate `test()` call per data case gives each case its own row in the report, its own pass/fail status, and its own retry behavior — a single test looping internally would report one aggregate result even if only one of several cases actually failed",`Playwright technically forbids calling test() inside a loop, which is why the internal-loop approach must be used instead`,`Generated tests from a loop cannot be filtered individually with --grep`,`A single looping test always runs faster because it avoids the fixture setup/teardown overhead per case`],0,`test() calls generated in a loop (each with a distinct, data-derived title) show up as genuinely separate entries in reports — much more informative than one monolithic test hiding per-case results.`],[`L`,`I`,"Why should the titles generated by a data-driven `for` loop over `test()` calls be dynamically interpolated (e.g. using the data value in the title) rather than left static?",null,[`Static, identical titles for every generated test would collide (indistinguishable in reports and impossible to target individually with --grep) — interpolating the actual data value into each title keeps them unique and immediately meaningful`,`Dynamic titles are required syntactically or the loop itself will not compile`,`Static titles cause Playwright to silently drop all but the first generated test`,`Interpolated titles automatically enable parallel execution for that group`],0,`The whole benefit of generating individual test() calls from data is lost if you cannot tell them apart afterward — the data itself is the natural, unique differentiator for the title.`],[`L`,`A`,"Why does a fixture's teardown code (the part after `await use(value)` in a `test.extend()` fixture definition) still run even if the test itself throws an error?",null,[`Playwright's fixture system guarantees teardown runs regardless of test outcome — similar to a try/finally block — so resources like temporary files, database rows, or open connections are cleaned up even when the test using them fails`,`Teardown only runs on success; failing tests skip fixture teardown entirely, which is why manual cleanup is still sometimes needed`,`Fixture teardown must be manually wrapped in a try/catch by the fixture author or it will not run reliably`,`Teardown code only executes when the fixture is worker-scoped, never for test-scoped fixtures`],0,`The fixture lifecycle is deliberately resilient to test failures — code after use() acts like a finally block, guaranteeing cleanup regardless of what happened during the test.`],[`L`,`I`,"What is the key difference between an annotation like `test.skip(condition, reason)` called at the top of a test body versus `test.skip()` called with no arguments at the top of the test file (outside any test)?",null,["Inside a running test, `test.skip(condition, reason)` conditionally skips just that one test based on runtime logic; `test.skip()` at the top level (as a standalone statement, not inside a test) is invalid — the modifier form `test.skip('title', fn)` or the in-test call are the two valid usages",`Both forms are functionally identical and interchangeable in every context`,`The top-level form skips every test defined for the rest of the file`,`The in-test conditional form can only accept boolean literals, never runtime expressions`],0,`test.skip has multiple valid call signatures (as a test modifier, or called conditionally inside a running test) — knowing which context you are in determines the correct usage.`],[`L`,`A`,"Why can nesting `test.describe.serial()` inside a `test.describe.parallel()` block (or vice versa) produce results that surprise developers unfamiliar with Playwright's scheduling model?",null,[`The outer mode governs how the group as a whole is scheduled relative to siblings, while the inner mode governs ordering within that nested group — mentally simulating the combined effect requires tracing both levels, and it is easy to assume the innermost or outermost mode simply "wins" globally`,`Nesting different modes is technically disallowed and Playwright throws a configuration error immediately`,`The inner mode is always silently ignored in favor of the outer one`,`Nested modes only take effect when fullyParallel is explicitly set to false`],0,`Scheduling modes compose rather than override each other — understanding the actual combined behavior requires reasoning about both levels of nesting together.`],[`L`,`I`,"Why does Playwright's `expect` library support both `.not` negation and dedicated opposite matchers (like `toBeVisible()` alongside `toBeHidden()`) instead of relying purely on `.not`?",null,[`Dedicated opposite matchers can express a more specific, actionable wait condition (e.g. toBeHidden() actively waits for an element to become hidden) whereas .not.toBeVisible() waits for the negation of visibility, which reads and behaves similarly but explicit dedicated matchers are sometimes clearer or more precise for certain states`,`.not negation is deprecated and dedicated opposite matchers are the only supported approach going forward`,`Dedicated matchers run without any retry/auto-wait behavior, unlike .not`,`toBeHidden() and .not.toBeVisible() are unrelated matchers with no conceptual connection`],0,`Both approaches typically achieve a similar retrying wait, but explicit dedicated matchers can sometimes be clearer to read and occasionally cover subtly different edge cases (e.g. detached vs hidden).`],[`L`,`A`,"Why would a team use `expect.configure({ soft: true })` to create a customized expect function rather than calling `expect.soft()` directly everywhere?",null,["expect.configure() lets you create a reusable, pre-configured expect variant (e.g. always-soft, or with a custom timeout) that can be imported and used consistently across many test files without repeating `.soft` or timeout options on every single call",`expect.configure() is required before expect.soft() can be used at all`,`expect.configure({ soft: true }) disables retries entirely for the configured instance`,`There is no such thing as expect.configure(); soft mode can only be set globally in playwright.config.ts`],0,`Configuring a reusable expect variant is a DRY convenience for teams that want a consistent assertion style (e.g. all-soft assertions in a validation-heavy test suite) without repeating options everywhere.`],[`L`,`I`,"Why is it generally discouraged to put real assertions (`expect()` calls) inside a `beforeEach` hook rather than inside the test body itself?",null,[`A failure inside beforeEach is reported as a hook failure rather than a specific test failure, which can make it harder to see at a glance exactly which behavior under test actually broke, and it fails every test in that scope rather than isolating the problem to one test`,`Playwright technically forbids calling expect() inside beforeEach and will throw an error`,`Assertions inside beforeEach do not support auto-retry, unlike assertions inside a test body`,`beforeEach hooks are stripped from the compiled test bundle before execution`],0,`Keeping assertions in the test body (and setup/navigation in hooks) makes failures easier to diagnose — a hook failure cascades to every test relying on it, obscuring which specific behavior is actually broken.`],[`L`,`A`,"Why does the fixture dependency graph (e.g. `page` depends on `context`, which depends on `browser`) matter when designing a custom fixture that needs both `page` and a custom `apiClient`?",null,[`Fixtures are resolved lazily in dependency order — a custom fixture that destructures both page and apiClient will trigger creation of page's entire dependency chain (browser → context → page) only if and when it is actually used, letting Playwright avoid unnecessary browser/context creation for fixtures that never need them`,`Fixture dependencies must always be declared in a single flat object with no chaining allowed`,`Custom fixtures cannot depend on more than one existing fixture at a time`,`The order fixtures are destructured in the function signature determines execution order, unrelated to actual dependencies`],0,`Playwright resolves the minimum necessary set of fixtures based on what is actually requested — understanding this graph helps you reason about when expensive resources like a browser context actually get created.`],[`L`,`I`,"Why might a test intentionally call `test.slow()` rather than manually setting `{ timeout: 90_000 }` as the test's second argument?",null,[`test.slow() communicates intent ("this test is inherently slower than most") by tripling whatever the current effective timeout already is, adapting automatically if the base timeout changes later, whereas a hardcoded override requires updating the magic number by hand if the global timeout changes`,`test.slow() and a hardcoded timeout override are functionally identical in every respect`,`test.slow() disables retries as a side effect, while a hardcoded timeout does not`,`test.slow() can only be called from within a beforeEach hook, not inside the test body`],0,`test.slow() is a relative, self-adjusting multiplier rather than an absolute number, which tends to age better as global config settings evolve.`],[`L`,`I`,'What does it mean that Playwright\'s web-first assertions (like `toBeVisible()`) are "auto-retrying," and why does that matter for how quickly a test can safely proceed after an assertion?',null,["The assertion repeatedly re-checks the condition against the live DOM until it passes or the timeout elapses, meaning a passing assertion also guarantees the condition was actually true at the moment it succeeded — eliminating most need for manual `waitForTimeout()` delays before an assertion",`Auto-retrying means the assertion runs a fixed number of times (always 3) regardless of whether it passes early`,`It only applies to text-based assertions, not visibility or attribute assertions`,`Auto-retrying assertions ignore timeouts entirely and will wait indefinitely`],0,`This retrying behavior is central to why Playwright tests are less flaky than tests relying on fixed sleeps — the assertion itself absorbs timing variability instead of requiring a manual guess at how long to wait.`],[`L`,`A`,"Why does combining `test.describe.configure({ mode: 'serial' })` with `retries` produce a subtly different recovery behavior than serial mode with no retries at all?",null,[`If a test in a serial block fails and retries are configured, Playwright reruns the ENTIRE serial sequence from the beginning of that block on retry, not just the failing test — because subsequent tests depend on state built up by earlier ones`,`Retries are completely ignored inside any serial() block regardless of configuration`,`Only the single failing test is retried in place, with all other tests in the block treated as already passed`,`Serial mode with retries enabled disables serial ordering and runs everything in parallel instead`],0,`Because serial tests build on each other's state, a partial retry (rerunning only the failed test) would not have valid prior state — Playwright reruns the whole block to preserve consistency.`],[`L`,`I`,"What is the purpose of `testInfo.annotations` compared to simply adding a code comment near a test?",null,[`Annotations are structured metadata attached to a test that appear directly in the HTML report (and can be machine-read by custom reporters or CI integrations) — a code comment is invisible to anyone looking only at the test results, not the source code`,`Annotations and code comments are functionally identical, differing only in syntax`,`testInfo.annotations can only be read, never written, from within a test`,`Annotations automatically create a corresponding entry in an external issue tracker`],0,`Annotations bridge the gap between source code and the visible test report — useful for linking a test to a ticket, flagging a known limitation, or tagging metadata that tooling can consume.`],[`L`,`A`,"Why might a fixture be designed to accept options (e.g. a `defaultUser` fixture that itself depends on an overridable `userRole` option) rather than being hardcoded?",null,["Overridable fixture options let different describe blocks or projects customize behavior (e.g. testing as 'admin' vs 'guest') by calling `test.use({ userRole: 'admin' })`, reusing the same fixture logic across many different scenarios instead of duplicating the setup for each role",`Fixture options can only be set once globally in playwright.config.ts and never overridden per test`,`Parameterized fixtures always run slower than hardcoded ones due to extra type-checking`,`Options-based fixtures require a separate test.extend() call for every possible option value`],0,`Treating a fixture's dependency (like userRole) as itself an overridable fixture option is a common pattern for building flexible, reusable setup across many test scenarios.`],[`L`,`I`,"Why does Playwright's test runner collect ALL test files and their `test()`/`describe()` calls before running anything, rather than executing tests as it encounters each file?",null,[`Collecting everything up front lets Playwright know the full list of tests ahead of time, which is necessary for accurately distributing work across parallel workers, applying --grep filters, sharding, and reporting an overall total before a single test has actually run`,`Collection-first execution is a legacy behavior kept only for backward compatibility with older config formats`,`It is required because JavaScript cannot execute asynchronous code before all files are loaded`,`This behavior only applies when the html reporter is configured`],0,`Parallel scheduling, sharding, and accurate progress reporting (e.g. "12 of 340 tests") all require knowing the complete test list before execution begins.`],[`O`,`A`,`What kind of fixture does this create, and how is it used?`,`import { test as base } from '@playwright/test';

type MyFixtures = { todoPage: { addTodo: (text: string) => Promise<void> } };

export const test = base.extend<MyFixtures>({
  todoPage: async ({ page }, use) => {
    await page.goto('/todos');
    const addTodo = async (text: string) => {
      await page.getByPlaceholder('What needs to be done?').fill(text);
      await page.keyboard.press('Enter');
    };
    await use({ addTodo });
  },
});`,["A custom test-scoped fixture named `todoPage` that wraps common page-object-style behavior (navigating and adding a todo) so every test using `{ todoPage }` gets a ready-to-use helper object instead of repeating the raw locator/keyboard calls",`A worker-scoped fixture that is shared across every test in the file regardless of which tests request it`,`A fixture that automatically runs before every test even if todoPage is never destructured`,`An invalid fixture definition because use() must be called synchronously, not inside an async arrow function`],0,`This is essentially a lightweight Page Object Model pattern implemented via the fixture system — todoPage is created and torn down per test, just like the built-in page fixture.`],[`O`,`I`,`What happens when this test runs, assuming the underlying database is genuinely eventually consistent?`,`test('order eventually appears in reporting', async () => {
  await expect.poll(async () => {
    const res = await fetch('https://api.example.com/orders/count');
    const body = await res.json();
    return body.count;
  }, { timeout: 10_000 }).toBe(5);
});`,[`Playwright repeatedly calls the fetch-and-parse function every so often for up to 10 seconds, checking each returned count against 5, and passes as soon as a call returns 5 or fails if the timeout elapses first`,`The fetch call only happens once; expect.poll() merely adds a fixed delay before checking the result`,`This code throws a compile error because expect.poll() cannot accept an async callback`,`expect.poll() ignores the timeout option and always waits exactly 5 seconds`],0,`expect.poll() generalizes the same retry-until-match mechanism used by locator assertions to any custom async function — ideal for polling backend state that is not directly reflected in the DOM.`],[`O`,`I`,`What does this custom matcher registration enable in later tests?`,`expect.extend({
  toBeWithinRange(received: number, floor: number, ceiling: number) {
    const pass = received >= floor && received <= ceiling;
    return {
      pass,
      message: () => \`expected \${received} to be within range \${floor}-\${ceiling}\`,
    };
  },
});

// later in a test:
expect(total).toBeWithinRange(95, 105);`,["Tests can now call `expect(value).toBeWithinRange(floor, ceiling)` as a first-class, readable matcher, with the pass/fail logic and a clear custom failure message centralized in one place instead of repeated inline comparisons everywhere",`This code has no effect unless it is also registered inside playwright.config.ts`,`toBeWithinRange automatically becomes available with auto-retry behavior identical to built-in locator matchers, with zero extra work`,`expect.extend() can only be called once per entire test run, in a single file`],0,`Custom matchers make domain-specific comparisons (like numeric ranges, or complex object shape checks) both more readable and centrally maintainable.`],[`O`,`A`,"What is the effect of `{ box: true }` in this fixture-adjacent helper function?",`async function login(page, user) {
  return test.step('login as ' + user.role, async () => {
    await page.goto('/login');
    await page.getByLabel('Email').fill(user.email);
    await page.getByLabel('Password').fill(user.password);
    await page.getByRole('button', { name: 'Sign in' }).click();
  }, { box: true });
}`,[`In the trace viewer/report, this step collapses to a single clean entry ("login as admin") without exposing the individual goto/fill/click sub-actions as separate nested steps — useful for a reusable helper whose internal details are not usually interesting to someone debugging a failing test`,`box: true prevents this step from ever appearing in the report at all`,`box: true forces the step to run inside a brand-new isolated BrowserContext`,`Without box: true, this code would throw a runtime error, since test.step() requires it by default`],0,`box: true is specifically for helper-function authors who want to hide internal implementation noise from consumers reading a trace, presenting a single clean step instead.`],[`O`,`I`,`What does this describe-scoped override achieve when the file also contains a sibling describe block with no such call?`,`test.describe('mobile checkout', () => {
  test.use({ viewport: { width: 390, height: 844 } });

  test('shows compact summary', async ({ page }) => { /* ... */ });
});

test.describe('desktop checkout', () => {
  test('shows full summary', async ({ page }) => { /* ... */ });
});`,[`Only tests inside "mobile checkout" get the overridden 390x844 viewport; "desktop checkout" tests continue using whatever viewport is configured globally or at the project level`,`The viewport override applies to every test in the file, including "desktop checkout", once test.use() is called anywhere`,`This causes a configuration conflict error since two describe blocks in one file cannot have different viewport settings`,`test.use() only takes effect on the very first test that runs after the call, not subsequent ones`],0,`test.use() overrides are scoped precisely to the describe block (or file) they are called within, which is exactly why the sibling block is unaffected.`],[`O`,`A`,`What is the outcome of this test if the underlying element takes 3 seconds to become visible, given the default 5-second assertion timeout?`,`test('shows success toast', async ({ page }) => {
  await page.getByRole('button', { name: 'Save' }).click();
  await expect(page.getByText('Saved successfully')).toBeVisible();
});`,[`The test passes — toBeVisible() polls the DOM repeatedly for up to 5 seconds by default, so it succeeds as soon as the toast appears at the 3-second mark, well within the timeout window`,`The test fails immediately because the element is not visible at the exact instant the assertion first runs`,`The test hangs indefinitely because no explicit timeout was passed to toBeVisible()`,`The test passes only if an explicit await page.waitForTimeout(3000) is added beforehand`],0,`This is the core value of auto-retrying web-first assertions — timing variability up to the assertion timeout is absorbed automatically without any manual wait.`],[`O`,`I`,"What does this code demonstrate about `test.step()` return values?",`test('checkout total is calculated correctly', async ({ page }) => {
  const total = await test.step('read cart total', async () => {
    const text = await page.locator('.cart-total').innerText();
    return Number(text.replace('$', ''));
  });

  expect(total).toBeGreaterThan(0);
});`,[`The value returned by the test.step() callback (a parsed number) becomes the resolved value of the awaited test.step() call, letting the rest of the test use it directly — while still getting a labeled, reportable step in the trace`,`test.step() always returns the raw DOM text regardless of what the callback returns`,`This code fails to compile because test.step() callbacks cannot return anything other than void`,`The returned value is only accessible inside a subsequent test.step() call, never directly in the test body`],0,`test.step() behaves like any async function for the purposes of its return value — the reporting/labeling behavior is additive, not a replacement for normal return semantics.`],[`O`,`I`,"What does `testInfo.retry` reveal when logged inside this test on its second execution attempt (after the first attempt failed and retries: 1 or higher is configured)?",`test('flaky network call', async ({ page }, testInfo) => {
  console.log('attempt number:', testInfo.retry);
  await page.goto('/dashboard');
});`,["It logs `1` on the retry attempt (0 on the original run), letting the test optionally branch on whether this is a fresh run or a retry",`It always logs 0 regardless of how many times the test has been retried`,`It logs the total configured retries value from playwright.config.ts, not the current attempt number`,`testInfo.retry is undefined unless PWDEBUG=1 is set`],0,`testInfo.retry increments with each retry attempt (starting at 0), giving test code visibility into which attempt is currently executing.`],[`O`,`A`,`What does this worker-scoped fixture accomplish across a test file with multiple tests?`,`export const test = base.extend<{}, { dbConnection: Database }>({
  dbConnection: [async ({}, use) => {
    const db = await Database.connect(process.env.TEST_DB_URL);
    await use(db);
    await db.close();
  }, { scope: 'worker' }],
});`,[`One database connection is established per worker process (not per test), reused by every test running in that worker, and closed once after all of that worker's tests finish — significantly cheaper than reconnecting for every individual test`,`A brand new database connection is opened and closed for every single test, identical to a test-scoped fixture`,`The dbConnection fixture runs even for tests that never destructure it, because worker scope implies auto: true`,`Worker scope means the connection is shared across ALL workers simultaneously, not just one`],0,"The `{ scope: 'worker' }` option is exactly for amortizing expensive, safely-shareable setup (like a DB connection) across many tests within the same worker process."],[`O`,`I`,`What is the effect of this loop when the test file is collected by Playwright?`,`const users = ['admin', 'editor', 'viewer'];

for (const role of users) {
  test(\`\${role} sees the correct dashboard\`, async ({ page }) => {
    await page.goto(\`/login?role=\${role}\`);
    await expect(page.getByRole('heading')).toContainText(role);
  });
}`,[`Three separate, independently reportable tests are registered — "admin sees the correct dashboard", "editor sees the correct dashboard", and "viewer sees the correct dashboard" — each with its own pass/fail status and each individually filterable via --grep`,`Only one test is registered, and it internally loops through all three roles at runtime`,`This throws a "duplicate test" error because a for loop is not allowed to call test() directly`,`All three generated tests share a single BrowserContext by default since they come from the same loop`],0,`Because JavaScript evaluates the loop during file collection (before tests run), each iteration's test() call registers a genuinely distinct test with its own interpolated title.`],[`O`,`A`,"What does this test verify about the difference between `expect()` and `expect.soft()`?",`test('profile form validation', async ({ page }) => {
  await page.goto('/profile');
  await expect.soft(page.getByLabel('Name')).toHaveValue('');
  await expect.soft(page.getByLabel('Email')).toHaveValue('');
  await expect(page.getByRole('button', { name: 'Save' })).toBeVisible();
});`,[`Even if both soft assertions fail (unexpected pre-filled values), execution continues to the final hard assertion; the test is ultimately reported as failed at the end, but the report shows ALL failures — both soft ones plus whatever the hard assertion found — instead of stopping at the first soft failure`,`A failing soft assertion silently passes the test since soft failures are never actually reported`,`The final hard expect() call never executes if either soft assertion above it failed`,`expect.soft() and expect() are functionally identical in this specific example`],0,`Soft assertions accumulate failures without halting the test — but a subsequent hard assertion (or the test simply finishing) is needed to surface and finalize all the recorded soft failures in the report.`],[`O`,`I`,`What does this test annotation accomplish?`,`test('known bug in Safari only', async ({ page, browserName }) => {
  test.fail(browserName === 'webkit', 'Tracked in JIRA-482, WebKit-specific rendering issue');

  await page.goto('/gallery');
  await expect(page.locator('.thumbnail')).toHaveCount(12);
});`,[`On WebKit specifically, the test is expected to fail — if it does fail there, it is reported as an expected failure; if it unexpectedly passes on WebKit, Playwright flags that as noteworthy; on chromium/firefox the test runs and is judged normally since the condition is false there`,`test.fail() with a condition unconditionally fails the test on every browser, ignoring the browserName check`,`This code throws an error because test.fail() cannot accept a condition argument, only a reason string`,`The reason string parameter has no functional effect and exists purely as a code comment`],0,`Conditional test.fail() scopes an xfail expectation precisely to the environment where a known bug actually manifests, while other environments are still held to normal pass/fail standards.`],[`O`,`A`,`What is the scheduling behavior implied by this nested describe structure?`,`test.describe.parallel('reports', () => {
  test.describe.serial('wizard steps', () => {
    test('step 1: choose template', async ({ page }) => { /* ... */ });
    test('step 2: fill fields', async ({ page }) => { /* ... */ });
  });

  test('export button is visible', async ({ page }) => { /* ... */ });
});`,[`The two "wizard steps" tests must run in order on the same worker (serial), while "export button is visible" is free to run independently and potentially concurrently with the wizard steps, since it sits in the outer parallel-mode group but is not part of the inner serial block`,`The inner serial() setting is overridden by the outer parallel() and all three tests run fully in parallel with no ordering guarantee`,`This structure is invalid; serial() cannot be nested inside parallel()`,`All three tests are forced to run strictly in the order they are written, including the unrelated export test`],0,`The serial guarantee is local to the tests explicitly grouped inside it — sibling tests/groups at the outer level are unaffected by that inner ordering constraint.`],[`O`,`I`,"What happens if this fixture's `use()` call is never reached due to an error thrown before it?",`export const test = base.extend<{ apiToken: string }>({
  apiToken: async ({}, use) => {
    const token = await fetchAuthToken(); // throws if the auth service is down
    await use(token);
  },
});`,[`Any test that depends on the apiToken fixture fails immediately with the thrown error, before the test body even starts executing — Playwright treats a fixture setup failure as blocking that test entirely`,`The test still runs normally, but apiToken is undefined inside the test body`,`Playwright automatically retries fetchAuthToken() up to 3 times before giving up, regardless of the retries config`,`The error is silently swallowed and the fixture resolves to an empty string`],0,`A fixture is fully responsible for producing its value before calling use() — if setup throws, any test requesting that fixture cannot proceed and fails at the setup stage.`],[`O`,`A`,`What does this snippet demonstrate about combining fixtures from two independently-defined sets?`,`import { mergeTests } from '@playwright/test';
import { test as dbTest } from './db-fixtures';
import { test as authTest } from './auth-fixtures';

export const test = mergeTests(dbTest, authTest);

test('user data persists', async ({ db, loggedInPage }) => {
  // db comes from dbTest, loggedInPage comes from authTest
});`,[`mergeTests() combines the fixtures defined in dbTest and authTest into a single test object, so a single test can use fixtures originally defined in two separate, independently maintained fixture files without manually re-declaring them together`,`mergeTests() only works if both fixture sets define fixtures with identical names`,`This throws an error because a test can only ever extend from one base test object`,`mergeTests() silently discards authTest's fixtures, keeping only dbTest's`],0,`mergeTests() is specifically for composing fixture sets maintained separately (e.g. by different teams or as reusable packages) into one usable test object.`],[`O`,`I`,`What is the effect of this annotation call inside a running test?`,`test('checkout with expired promo code', async ({ page }) => {
  test.info().annotations.push({
    type: 'issue',
    description: 'https://github.com/org/repo/issues/321',
  });

  await page.goto('/checkout?promo=EXPIRED10');
  await expect(page.getByText('Promo code expired')).toBeVisible();
});`,[`It attaches a clickable, structured annotation to this specific test's entry in the HTML report, linking it to the referenced GitHub issue without affecting whether the test passes or fails`,`It causes the test to be automatically skipped, since an "issue" annotation implies a known bug`,`test.info().annotations is read-only and this line throws a runtime error`,`The annotation is only visible in the terminal output, never in the HTML report`],0,`Annotations pushed at runtime work identically to those declared via the second test() argument — both surface as report metadata without changing pass/fail behavior.`],[`O`,`A`,"What is the likely intent and effect of this pattern combining `test.step()` with `expect.soft()`?",`test('settings page fields', async ({ page }) => {
  await page.goto('/settings');

  await test.step('verify all labels are present', async () => {
    await expect.soft(page.getByText('Display Name')).toBeVisible();
    await expect.soft(page.getByText('Email Notifications')).toBeVisible();
    await expect.soft(page.getByText('Theme')).toBeVisible();
  });
});`,[`It groups a batch of independent, non-blocking checks under one clearly labeled step, so the report shows exactly which step the checks belong to while still collecting every individual failure among the three labels rather than stopping at the first missing one`,`test.step() forces all expect.soft() calls inside it to behave like hard assertions instead`,`Soft assertions are not allowed inside test.step() and this throws a configuration error`,`Only the first expect.soft() call inside the step is actually evaluated; the rest are skipped`],0,`Combining a labeled step with soft assertions is a common pattern for checking several related, independent conditions together while keeping the report both organized and complete.`],[`O`,`I`,"What does destructuring `browserName` directly as a test parameter demonstrate here?",`test('renders differently per engine', async ({ page, browserName }) => {
  await page.goto('/');
  if (browserName === 'webkit') {
    await expect(page.locator('.safari-banner')).toBeVisible();
  } else {
    await expect(page.locator('.safari-banner')).toBeHidden();
  }
});`,[`browserName is itself a built-in fixture reporting which browser engine (chromium, firefox, or webkit) the current project is using, letting a single test file adapt its assertions per engine without needing separate test files`,`browserName can only be accessed via testInfo.project.name, and this code would throw a runtime error`,`This pattern requires a special config flag to enable per-browser conditional logic inside a test`,`browserName always returns "chromium" unless explicitly overridden in playwright.config.ts`],0,`browserName is one of several built-in fixtures (alongside page, context, browser) automatically available to every test, reflecting the currently running project's engine.`],[`R`,`A`,`Your team has 15 spec files that each need an authenticated page, a mock API server, and a seeded test user — currently duplicated via copy-pasted beforeEach blocks in every file. How should this be refactored using Playwright's fixture system?`,null,["Define custom fixtures (via test.extend()) for the authenticated page, mock server, and seeded user, then export a single customized `test` object that all 15 spec files import instead of `@playwright/test` directly — centralizing the setup logic in one place",`Move all the duplicated beforeEach code into a single shared JavaScript function called manually at the top of every test`,`Convert all 15 files into one giant file with a single beforeAll block`,`Use globalSetup to seed the user once, and manually pass it via a JSON file that every test reads`],0,`Fixtures are Playwright's purpose-built mechanism for exactly this — reusable, composable, automatically torn-down setup shared cleanly across many spec files.`],[`R`,`I`,`A test needs to verify that an async background job eventually updates a "processing" status to "complete" in a REST API response — there is no DOM element to check, only a polling HTTP call. What is the idiomatic Playwright approach?`,null,["Use `expect.poll(async () => { const res = await fetch(...); return (await res.json()).status; }, { timeout: 30_000 }).toBe('complete')` to repeatedly check the API until it matches or times out",`Manually write a while loop with a fixed page.waitForTimeout(1000) between each fetch call and a hardcoded max iteration count`,`This scenario cannot be tested with Playwright since it does not involve the DOM`,`Use expect(response).toHaveStatus(200) repeatedly inside a try/catch block`],0,`expect.poll() is specifically designed to bring Playwright's auto-retry philosophy to arbitrary async conditions that are not locator-based, like polling an API.`],[`R`,`I`,`You want the same suite of 50 tests to run once with viewport 1280x800 and again with 390x844, but you do not want to duplicate every single test() call. What is the cleanest approach?`,null,[`Define two projects in playwright.config.ts (e.g. "desktop" and "mobile") each with a different viewport in their use block — Playwright automatically runs the entire suite once per project without any duplicated test code`,`Wrap every test body in an if/else block checking a manually-set global variable for the current viewport`,`Duplicate every spec file into a "-mobile" variant with a different viewport hardcoded in each`,`Use test.use() inside every single test individually to set the viewport at runtime`],0,`Projects are exactly the mechanism for running an identical test suite against multiple configurations (viewport, browser, locale) without duplicating test code.`],[`R`,`A`,'A test suite has a recurring pattern: several tests need to check "is this specific numeric metric within an acceptable tolerance of the expected value," currently written as ad-hoc `expect(Math.abs(actual - expected) < tolerance).toBe(true)` scattered across many files. How should this be cleaned up?',null,["Register a custom matcher via `expect.extend({ toBeCloseToValue(received, expected, tolerance) { ... } })` so tests can write `expect(actual).toBeCloseToValue(expected, tolerance)` with a clear, reusable, well-named comparison and failure message",`Move the tolerance-checking logic into a shared constant file that every test imports and manually calls as a plain function`,`This cannot be improved; ad-hoc boolean assertions are the only option in Playwright`,`Replace all numeric comparisons with visual screenshot comparisons instead`],0,`A custom matcher is the right tool for centralizing and naming a repeated comparison pattern, improving both readability and the quality of failure messages.`],[`R`,`I`,`A QA lead wants failing tests to automatically retry once, but any test that only passes on retry should cause the nightly build to be flagged as needing attention (not just silently green). How should this be configured?`,null,['Set `retries: 1` in playwright.config.ts and also enable `--fail-on-flaky-tests` (or the equivalent config) so a test that fails then passes on retry is reported as "flaky" and causes the overall CI run to be marked as needing attention rather than a clean pass',`Set retries: 0, since retries would hide the problem regardless of any other flag`,`There is no way to distinguish a flaky pass from a clean pass in Playwright's reporting`,`Manually check the HTML report by eye after every nightly run for any "flaky" labels, since no automation can flag this`],0,`Retries and fail-on-flaky-tests work together — retries provide short-term tolerance while fail-on-flaky-tests ensures that tolerance does not silently mask a real reliability problem.`],[`R`,`A`,`A checkout test needs to verify three independent things on one page (shipping address is correct, order total is correct, payment method is correct) and the team wants to see ALL three results even if one fails, rather than stopping at the first failure. What Playwright feature fits this exactly?`,null,[`Use expect.soft() for each of the three checks instead of expect(), so all three are evaluated and reported even if one or two fail, with the test still ultimately marked failed if any soft assertion failed`,`Split the single test into three separate test() calls, each checking one thing, accepting the extra page-load overhead of navigating three times`,`Wrap each check in its own try/catch block and manually log failures to the console`,`Use test.step() alone, since steps automatically continue past failures unlike expect()`],0,`expect.soft() is the purpose-built tool for exactly this "check everything, report everything" pattern within a single test.`],[`R`,`I`,`A large spec file mixes fast unit-like assertions with a couple of genuinely slow (30+ second) operations like large file uploads, all sharing the same default 30-second test timeout. Some slow tests occasionally time out. What is the most targeted fix?`,null,["Call `test.slow()` at the top of just the slow tests (or pass `{ timeout: 90_000 }` as their second argument) rather than raising the global timeout for every test in the file, most of which do not need the extra time",`Raise the global timeout in playwright.config.ts to 90 seconds for the entire suite`,`Split the file into two separate files with different config imports`,`Disable the timeout entirely for the whole file using timeout: 0`],0,`Per-test overrides (test.slow() or an explicit timeout option) target exactly the tests that need more time without loosening standards for the majority of fast tests.`],[`R`,`A`,"A team wants a single reusable `login(page, role)` helper used across dozens of test files to show up cleanly in the trace viewer as one step, without exposing every internal fill/click call when someone is debugging an unrelated failure later in the test. What is the recommended approach?",null,["Wrap the helper's internals in `test.step('login as ' + role, async () => { ... }, { box: true })` so the trace collapses it into a single clean, labeled entry instead of a dozen granular sub-actions",`Rewrite the helper using page.evaluate() so its actions do not appear in the trace at all`,`Disable tracing entirely for any test that calls the login helper`,`There is no way to hide internal steps; every action always appears individually in the trace`],0,`box: true is specifically designed for this "helper function should read as one clean step" use case in shared test utilities.`],[`R`,`I`,`You need 10 tests to each verify a different combination of subscription tier and feature flag, and the report needs to clearly show which specific combination failed, if any. What is the best structural approach?`,null,["Generate the tests via a loop over an array of {tier, flag} combinations, calling test() with a dynamically interpolated title for each — e.g. test(`${tier} tier with ${flag} enabled`, ...) — so each combination is its own reportable, individually filterable test",`Write one test with 10 sequential if-blocks, each printing a console.log if that particular combination fails`,`Manually write out 10 nearly-identical test() calls by hand, copy-pasting and editing the tier/flag values each time`,`Use test.describe.serial() to chain the 10 combinations so they share state between checks`],0,`Data-driven generation with an interpolated title is the standard way to get both DRY test code and individually distinguishable, reportable results per combination.`],[`S`,`I`,"A developer notices that `expect(page.getByText('Loading...')).toBeHidden()` sometimes passes even when the loading spinner is clearly still visible on screen in a screenshot taken right after. What is the most likely explanation?",null,[`The assertion may have retried and eventually found the element hidden by the time it succeeded (well after the screenshot moment), or the element was actually removed and re-added by a re-render — the auto-retry window means "passed" reflects some moment within the timeout, not necessarily the very first instant`,`toBeHidden() is a synchronous, one-shot check that never retries, so this behavior is impossible and must be a Playwright bug`,`toBeHidden() only checks CSS opacity, not actual visibility, causing this discrepancy`,`The screenshot and the assertion always run in a completely different, unrelated BrowserContext`],0,`Because assertions retry over a window of time, "the assertion passed" only tells you the condition was true at SOME point during that window, not at any specific instant you might be comparing it to.`],[`S`,`A`,'A custom worker-scoped `dbConnection` fixture occasionally causes tests to fail with "connection already closed" errors, but only when running with a high worker count in CI, never locally with workers: 1. What is the most likely cause?',null,[`A single connection is being shared correctly within one worker, but if the fixture teardown logic has a bug causing it to close prematurely (e.g. a race condition when multiple tests finish near-simultaneously in the same worker), that worker's subsequent tests would fail — investigate the teardown's timing and any shared mutable close-tracking flag`,`Worker-scoped fixtures are fundamentally incompatible with any worker count above 1`,`The database itself has a hardcoded 1-connection limit that Playwright cannot work around`,`This always indicates the fixture should have been test-scoped instead, with no other possible fix`],0,`Higher concurrency exposes race conditions that a single-worker run would never trigger — the fixture's teardown/cleanup logic likely has a subtle timing bug when many tests finish close together.`],[`S`,`I`,"A team notices that soft assertion failures from `expect.soft()` are not showing up in their CI's failure summary at all, even though the HTML report clearly lists them. Investigation shows the CI check step only inspects the process exit code. What is happening?",null,["A test with only soft-assertion failures still ultimately exits as failed at the process level once the test itself completes, so the exit code should already reflect it — the real issue is more likely that the CI step is checking the wrong exit code (e.g. from a preceding command in a shell pipeline) or a reporter/exit-code interaction masking the failure, worth double-checking with `echo $?` right after the test command",`expect.soft() failures never affect the process exit code under any circumstances, by design`,`Soft assertions only affect the HTML report and are entirely invisible to any other reporter or the exit code`,`This is expected: soft assertions are meant to be purely informational and should never fail a build`],0,`Soft assertion failures do fail the overall test (and thus the process exit code) once the test finishes — if CI is not catching that, look for exit-code plumbing issues (like a lost exit code in a shell pipeline) rather than assuming soft assertions are silent.`],[`S`,`A`,'A suite that previously ran fine now shows dozens of tests suddenly appearing "flaky" in reports after a teammate refactored several `beforeEach` navigation calls into a new worker-scoped fixture meant to "save time" by reusing one page across tests in a worker. What is the likely root cause?',null,[`page is inherently test-scoped for isolation reasons — sharing one page instance across many tests in a worker via a worker-scoped fixture leaks state (cookies, DOM, in-progress navigation) between tests, producing exactly this kind of intermittent, hard-to-reproduce flakiness`,`Worker-scoped fixtures are always slower than test-scoped ones, which is unrelated to flakiness but should be reverted anyway`,`The refactor is unrelated; flakiness must be caused by a completely different recent application change`,`beforeEach hooks cannot be replaced by fixtures under any circumstances, causing a hard configuration error`],0,`page (and its underlying context) is deliberately test-scoped by Playwright's built-in fixtures specifically to prevent this class of bug — a well-intentioned "optimization" that shares it across tests reintroduces cross-test state leakage.`],[`S`,`I`,"A test using `test.step()` extensively shows a confusing trace where a helper function's dozens of internal locator calls clutter the view, making it hard to find the actual point of failure in a 40-step end-to-end test. What single, low-effort change would clean this up?",null,["Wrap the noisy helper function's test.step() call with `{ box: true }`, collapsing its internal sub-actions into one clean labeled entry in the trace, while still keeping the meaningful top-level steps of the test fully visible",`Remove all test.step() calls entirely and rely purely on raw actions with no labeling at all`,`Rewrite the entire 40-step test as a single unlabeled block with no steps whatsoever`,`Increase the trace viewer's zoom level, since the clutter is purely a rendering/display issue`],0,`box: true is a targeted fix for exactly this problem — hiding a specific helper's internal noise without discarding the value of step-based organization elsewhere in the test.`],[`S`,`A`,`A generated set of data-driven tests (via a for-loop calling test() with interpolated titles) all pass individually when run with --grep matching just one title, but several fail unpredictably when the whole file runs together. What should be investigated first?`,null,[`Whether the loop variable or any object referenced inside each generated test's closure is being captured correctly per iteration (a classic JavaScript closure pitfall) or whether the tests inadvertently share mutable state (e.g. a module-level array being pushed to across iterations) that only causes trouble when many run together`,`Data-driven tests generated via a loop can never run correctly together and must always be run one at a time using --grep`,`The issue must be a Playwright bug specific to for-loops, unrelated to the test code itself`,`Reduce the loop to fewer than 5 iterations, since Playwright cannot reliably run more than that in one file`],0,`Classic JavaScript closure-capture bugs (or shared mutable state across loop iterations) are a very common, non-Playwright-specific cause of this exact "passes alone, fails together" symptom.`],[`D`,`I`,`A test calling a custom fixture throws "Fixture 'apiClient' timed out" even though the fixture's setup function itself completes in under a second when tested in isolation. The fixture body is: \`async ({}, use) => { const client = new ApiClient(); await use(client); }\`. What is a likely bug?`,null,["The fixture never actually calls `use(client)` in some code path, or an earlier awaited step inside the fixture (not shown) hangs — Playwright waits for `use()` to be called within the fixture timeout, so anything blocking before that call causes exactly this symptom",`Fixture timeouts are unrelated to whether use() is called and are purely based on wall-clock file size`,`ApiClient must be awaited even though its constructor is synchronous, or the fixture always times out`,`This error only occurs when the fixture is worker-scoped, never test-scoped`],0,`Playwright fixture timeouts specifically track the time until use() is invoked — a fixture that hangs (or never calls use) before that point triggers this exact timeout error.`],[`D`,`A`,"A suite using `test.describe.configure({ mode: 'serial', retries: 2 })` shows that when test 3 of 5 in the block fails, ALL 5 tests re-run on retry, tripling the total run time for that block. Is this a bug, and what should the team do?",null,[`This is expected behavior, not a bug — serial mode retries rerun the entire block from the start because later tests depend on state built by earlier ones; if this is too costly, consider whether the tests genuinely need to be serial, or reduce retries for this specific block`,`This is a bug and the team should file it upstream, since only the failing test should ever retry`,`The fix is to remove retries entirely, since serial mode is fundamentally incompatible with retries`,`Switching to mode: 'parallel' fixes the retry cost without changing test semantics`],0,`Rerunning the whole serial group on any failure is intentional — the alternative (only rerunning the failed test) would run it against invalid state left over from the original attempt.`],[`D`,`I`,'A test using `expect.poll()` to check an API endpoint always fails with "Timeout exceeded while waiting on the predicate" even though manually calling the same endpoint in a browser shows the expected value almost immediately. What is a common cause to check first?',null,["The callback passed to expect.poll() may be missing an `await` on the fetch/parse call (or throwing inside the callback without being caught), so it never actually returns the expected resolved value for the matcher to compare against",`expect.poll() cannot make real network requests and only works with mocked data`,`The default expect.poll() timeout is always 0, so it fails no matter what`,`expect.poll() is only compatible with locator-based assertions, not raw values from fetch`],0,`A missing await or an uncaught thrown error inside the poll callback is a very common cause of this symptom — the callback needs to actually resolve to a comparable value on each poll attempt.`],[`D`,`A`,"After introducing `mergeTests()` to combine two fixture files, TypeScript reports that a test using `{ db, loggedInPage }` cannot find the `loggedInPage` fixture, even though it is clearly defined in the second fixture file passed to mergeTests(). What is a likely mistake?",null,["The merged `test` object exported from mergeTests() was not actually imported and used in the failing test file — it might still be importing the original `test` from `@playwright/test` (or from only one of the two source fixture files) instead of the merged version",`mergeTests() only merges fixture types for TypeScript but not the actual runtime fixture values`,`mergeTests() requires both fixture sets to define a fixture with the exact same name in order to work`,`loggedInPage must be manually re-declared a third time inside the mergeTests() call itself`],0,"A very common mistake after introducing mergeTests() is forgetting to update every affected test file's import to use the newly merged `test` export instead of the original, single-source one."],[`D`,`I`,"A test file consistently reports one fewer test than expected in `--list` output after a colleague added a data-driven loop generating tests dynamically. Investigation shows two entries in the array produce the exact same interpolated title. What is happening?",null,[`Two tests registered with the identical title are not silently merged or an error — both technically still run — but if the counting/reporting logic (or a --grep filter targeting one specific title) assumes titles are unique, duplicate titles can make it look like one test "disappeared" when in fact both ran under an indistinguishable name`,`Playwright automatically discards the second test whenever two titles are identical, which explains the missing count`,`Duplicate titles in a loop always throw a hard collection error, preventing any tests in the file from running`,`This is unrelated to the duplicate titles and must be a separate testMatch misconfiguration`],0,`Duplicate titles do not cause an outright error, but they create exactly this kind of confusing, hard-to-diagnose ambiguity in counts, reports, and grep-based filtering.`],[`L`,`I`,"Why does Playwright pass `testInfo` as the second parameter to a test callback (`async ({ page }, testInfo) => {}`) instead of only exposing it through fixtures?",null,[`testInfo carries per-test runtime metadata (title, status, retry count, output paths) that is fundamentally different from a fixture — it describes the CURRENT execution rather than providing a reusable resource, so it is passed positionally alongside the fixtures object rather than being one more fixture to destructure`,`testInfo is actually just an alias for the page fixture with a different name`,`testInfo can only be accessed inside hooks, never inside the test body itself`,`Passing testInfo positionally is a legacy holdover with no remaining functional purpose`],0,`The distinction is conceptual: fixtures provide resources you request, while testInfo describes the currently running test itself — that is why it has its own dedicated parameter position.`],[`L`,`A`,"Why might a team avoid overusing `test.step()` for every single line of a test, wrapping literally every individual action in its own step?",null,[`Over-fragmenting a test into dozens of single-action steps adds visual noise to the trace/report without adding much diagnostic value — steps are most useful when they group a meaningful, named chunk of behavior (like "fill the login form"), not when every click gets its own step`,`test.step() has a hard limit of 5 calls per test, causing an error beyond that`,`Excessive test.step() calls disable the assertion auto-retry mechanism for that test`,`Each test.step() call adds a fixed 1-second delay to the test, regardless of its content`],0,`Like any organizational tool, test.step() is most valuable at the right granularity — grouping meaningful chunks of behavior, not mechanically wrapping every single line.`],[`L`,`I`,"Why does destructuring only `{ page }` from the test callback (ignoring `context`, `browser`, etc.) not prevent you from later accessing `page.context()` inside the test?",null,["page always exposes a `.context()` method returning the BrowserContext it belongs to, so you can reach the context object at any point without needing to have destructured it as a separate fixture up front",`page.context() only works if context was also explicitly destructured earlier in the same test`,`Accessing page.context() forces Playwright to create an entirely new, second context`,`This is not actually possible; context must always be destructured explicitly to be used`],0,`Playwright's core objects have convenient back-references to their parents (page.context(), context.browser()), reducing how much you need to destructure up front.`],[`L`,`A`,"Why is it important that a custom fixture's teardown logic (after `use()`) does not itself throw an unhandled error when a resource was never fully created due to an earlier failure?",null,[`An error thrown during teardown can mask or overshadow the original test failure that is usually far more useful to see first, and can also prevent other fixtures' teardown from running cleanly — defensive checks (e.g. verifying a connection exists before closing it) keep cleanup robust even after partial setup failures`,`Teardown errors are always silently ignored by Playwright regardless of what they contain`,`Fixture teardown code cannot throw errors at all; attempting to do so is a TypeScript compile error`,`Teardown only matters for worker-scoped fixtures, never for test-scoped ones`],0,`Robust fixtures defensively guard their teardown logic (e.g. null checks) so a partially-initialized resource does not cause a confusing secondary error that buries the real failure.`],[`L`,`I`,"Why does Playwright encourage using `page.getByRole()`, `getByLabel()`, and similar role/accessibility-based locators as a first choice in freshly written tests, rather than CSS selectors, from the very first test you write?",null,[`Role and label-based locators mirror how real users and assistive technology perceive the page, making tests more resilient to CSS/class-name refactors and, as a side benefit, encouraging more accessible markup in the application itself`,`CSS selectors are technically deprecated and will be removed from a future Playwright version`,`Role-based locators are always faster to execute than CSS-based locators`,`CSS selectors cannot be auto-retried, unlike role-based locators`],0,`This is a resilience and accessibility argument, not a performance one — locators tied to semantic meaning survive visual/structural refactors better than locators tied to implementation details like class names.`],[`L`,`A`,"Why does the order in which fixtures are destructured in a test's parameter object, e.g. `async ({ apiClient, page }) => {}` vs `async ({ page, apiClient }) => {}`, not affect their actual setup order?",null,[`Playwright determines fixture setup order from the underlying DEPENDENCY graph declared when the fixtures were defined (via test.extend()), not from the syntactic order they happen to be listed in a particular test's destructuring — listing order is purely for readability`,`Setup order always exactly matches the left-to-right order fixtures are destructured in that specific test`,`Fixture order only matters for worker-scoped fixtures, and is irrelevant for test-scoped ones`,`Playwright throws an error if fixtures are not destructured in dependency order`],0,`The dependency graph (declared once, when fixtures are defined) governs actual execution order — the convenience of destructuring syntax at the call site does not change that underlying graph.`],[`O`,`I`,"What does this test demonstrate about `test.step()` nesting?",`test('multi-step form', async ({ page }) => {
  await test.step('fill personal info', async () => {
    await page.getByLabel('Name').fill('Alice');
    await test.step('fill address sub-fields', async () => {
      await page.getByLabel('Street').fill('123 Main St');
      await page.getByLabel('City').fill('Springfield');
    });
  });
});`,[`Steps can be nested arbitrarily deep — the trace/report shows "fill address sub-fields" as a child entry under "fill personal info," reflecting the actual code nesting hierarchy`,`Nested test.step() calls throw a runtime error; steps can only be used at the top level of a test`,`The nested step silently merges into the outer step, showing as a single flat entry`,`Nesting steps disables auto-retry for any assertions inside the innermost step`],0,`test.step() nesting mirrors code structure directly in the trace viewer, letting you organize even complex multi-part flows hierarchically.`],[`O`,`A`,`What does this auto fixture accomplish for every test in the file, even ones that never mention it?`,`export const test = base.extend<{ forEachTest: void }>({
  forEachTest: [async ({}, use) => {
    console.log('starting test');
    await use();
    console.log('finished test');
  }, { auto: true }],
});`,["Because it is declared with `{ auto: true }`, this fixture runs automatically for every test using this extended `test` object, logging before and after each one, even though no test destructures `forEachTest` by name",`Nothing happens unless a test explicitly destructures forEachTest from its parameters`,`auto: true fixtures only run once for the entire file, not once per test`,`This fixture definition throws an error because it does not return a usable value to use()`],0,`auto fixtures opt out of Playwright's normal lazy fixture instantiation — they run unconditionally for every applicable test, which is exactly the point of the auto flag.`],[`O`,`I`,"What does `testInfo.title` return inside this test, and what is it typically used for?",`test('renders the user avatar correctly', async ({ page }, testInfo) => {
  await page.goto('/profile');
  const screenshotPath = testInfo.outputPath(\`\${testInfo.title}.png\`);
  await page.screenshot({ path: screenshotPath });
});`,[`testInfo.title returns the string "renders the user avatar correctly" (this test's own title), commonly used to build unique, descriptive artifact filenames alongside testInfo.outputPath()`,`testInfo.title returns the title of the entire file, not the individual test`,`testInfo.title only works if the test uses test.step() internally`,`testInfo.title requires an explicit annotation to be set before it becomes available`],0,`Combining testInfo.title with testInfo.outputPath() is a common pattern for naming custom artifacts in a way that is traceable back to the specific test that produced them.`],[`O`,`A`,`What is the effect of this describe-level annotation applied via the second argument?`,`test.describe('legacy reports module', {
  annotation: { type: 'deprecated', description: 'Module scheduled for removal in Q3' },
}, () => {
  test('exports CSV', async ({ page }) => { /* ... */ });
  test('exports PDF', async ({ page }) => { /* ... */ });
});`,[`The annotation is applied to every test within the describe block, so both "exports CSV" and "exports PDF" show the deprecated annotation in the report without needing to repeat it on each individual test() call`,`Describe blocks cannot accept an options argument; this is invalid syntax`,`The annotation only applies to the first test defined inside the block`,`This annotation automatically causes both tests to be skipped since the module is deprecated`],0,`Passing an options object with annotation (or tag) to test.describe() applies it to every test nested within, avoiding repetitive per-test declarations.`],[`O`,`I`,`What does this code demonstrate about accessing the resolved configuration from within a test?`,`test('uses the configured base url', async ({ page }, testInfo) => {
  console.log(testInfo.project.use.baseURL);
  await page.goto('/');
});`,[`testInfo.project exposes the fully resolved configuration for the CURRENT project the test is running under, including its use block — letting a test introspect settings like baseURL at runtime`,`testInfo.project only exists when running with --debug enabled`,`baseURL cannot be read from testInfo; it is only accessible via process.env`,`testInfo.project reflects the global config only, ignoring any project-specific overrides`],0,`testInfo.project gives runtime visibility into exactly which resolved project configuration (after all merging) applies to the currently executing test.`],[`O`,`A`,"What happens in this test if `page.getByRole('button', { name: 'Submit' })` matches two elements on the page?",`test('submits the form', async ({ page }) => {
  await page.goto('/form');
  await page.getByRole('button', { name: 'Submit' }).click();
});`,[`Playwright throws a strict-mode violation error, refusing to click because the locator resolved to more than one element — the test fails immediately with a clear message rather than guessing which one to click`,`Playwright automatically clicks the first matching button without any warning`,`Playwright clicks both matching buttons in sequence`,`The test passes as long as at least one of the two buttons is visible`],0,`Strict mode is Playwright's default safety behavior — an ambiguous locator resolving to multiple elements is treated as a locator bug in the test, not silently resolved by guessing.`],[`O`,`I`,`What is the outcome of this code when the "Add to cart" button does not exist on the page at all?`,`test('optional promo banner does not block checkout', async ({ page }) => {
  await page.goto('/product/123');
  const banner = page.getByText('Limited time offer');
  if (await banner.isVisible()) {
    await banner.getByRole('button', { name: 'Dismiss' }).click();
  }
  await page.getByRole('button', { name: 'Add to cart' }).click();
});`,[`isVisible() returns a boolean immediately without waiting/retrying, so if the banner has not rendered yet at that exact instant, the condition may be false even though it appears moments later — this differs from an auto-retrying assertion like toBeVisible()`,`isVisible() always waits up to the default assertion timeout before resolving, just like toBeVisible()`,`This code throws an error because isVisible() cannot be used inside an if statement`,`isVisible() automatically clicks the element it checks, in addition to returning a boolean`],0,`isVisible() is a non-retrying, instantaneous query method — useful for conditional logic, but it does not wait the way a web-first assertion does, which is an important distinction to understand.`],[`O`,`A`,`What does this fixture composition achieve?`,`export const test = base.extend<{ page: Page }>({
  page: async ({ page }, use) => {
    await page.addInitScript(() => {
      window.localStorage.setItem('feature_flag_new_ui', 'true');
    });
    await use(page);
  },
});`,["It overrides (rather than adds) the built-in `page` fixture, layering extra setup (injecting a localStorage flag before any navigation) on top of the default page creation — every test using this extended test object automatically gets the feature flag pre-set",`This throws an error because built-in fixtures like page can never be overridden`,`It creates a second, entirely separate page fixture named "page" that conflicts with the built-in one, causing ambiguity`,`The addInitScript call only affects the very first test that runs, not subsequent ones`],0,`test.extend() can override existing fixtures, not just add new ones — this pattern is common for globally applying setup (like feature flags or mocks) to the standard page fixture across an entire suite.`],[`O`,`I`,"What does this test demonstrate about the relationship between `test.fixme()` and code review practices?",`test('handles unicode usernames', async ({ page }) => {
  test.fixme(true, 'Known issue: unicode normalization bug, see TICKET-501');
  await page.goto('/signup');
  await page.getByLabel('Username').fill('日本語ユーザー');
  await expect(page.getByText('Welcome, 日本語ユーザー')).toBeVisible();
});`,[`The test is skipped (fixme with an unconditional true), and the report clearly attributes the skip to a specific tracked issue rather than an unexplained absence — a reviewer scanning the report can immediately see this is known, tracked technical debt rather than an accidental gap in coverage`,`test.fixme(true, ...) causes the test to run normally, with the string treated only as a log message`,`The fixme call has no visible effect in the report unless combined with test.skip() as well`,`This syntax is invalid; test.fixme() cannot accept a condition and a reason together`],0,`Attaching a reason string to fixme/skip turns an opaque skip into documented, traceable technical debt visible directly in the test report.`],[`O`,`A`,"What does this pattern of overriding a fixture's default value via `test.use()` combined with a parameterized custom fixture demonstrate?",`export const test = base.extend<{ apiBaseUrl: string }>({
  apiBaseUrl: ['https://api-staging.example.com', { option: true }],
});

test.describe('production smoke test', () => {
  test.use({ apiBaseUrl: 'https://api.example.com' });

  test('health check responds', async ({ apiBaseUrl }) => {
    const res = await fetch(\`\${apiBaseUrl}/health\`);
    expect(res.status).toBe(200);
  });
});`,['Declaring apiBaseUrl as an `{ option: true }` fixture with a default value lets any describe block or project override it via test.use() — here, the "production smoke test" describe overrides the staging default with a production URL just for the tests nested inside it',`option: true fixtures cannot have their value changed after being declared, making test.use() here have no effect`,`This pattern requires a separate playwright.config.ts entry for every possible apiBaseUrl value`,`The override only applies to the very first test in the describe block, not all of them`],0,`Declaring a fixture as an overridable "option" (with a sensible default) is the standard way to build test configuration that is both DRY and flexible via test.use().`],[`O`,`I`,"What is the effect of calling `test.setTimeout(120_000)` inside a running test body, partway through, rather than passing timeout as the test's second argument?",`test('large file import', async ({ page }) => {
  await page.goto('/import');
  test.setTimeout(120_000);
  await page.getByRole('button', { name: 'Start Import' }).click();
  await expect(page.getByText('Import complete')).toBeVisible({ timeout: 100_000 });
});`,[`test.setTimeout() dynamically extends the CURRENT test's timeout at runtime — useful when the need for extra time only becomes apparent partway through a test (e.g. after checking a condition), rather than being statically known when the test is declared`,`test.setTimeout() can only be called before any await inside the test, or it silently does nothing`,`It sets the timeout for every subsequent test in the file, not just the current one`,`test.setTimeout() and the second-argument timeout option are mutually exclusive and cannot both ever be used in the same suite`],0,`Unlike a statically declared timeout option, test.setTimeout() lets you adjust the current test's allowed duration dynamically, based on runtime conditions.`],[`O`,`A`,"What is the likely purpose of this pattern combining a custom fixture with `test.info().attach()`?",`export const test = base.extend<{ page: Page }>({
  page: async ({ page }, use, testInfo) => {
    await use(page);
    if (testInfo.status !== testInfo.expectedStatus) {
      const screenshot = await page.screenshot();
      await testInfo.attach('failure-screenshot', { body: screenshot, contentType: 'image/png' });
    }
  },
});`,[`It overrides the page fixture to automatically capture and attach a screenshot to the report specifically when a test's actual outcome does not match what was expected (e.g. an unexpected failure), centralizing this diagnostic behavior for every test without repeating it manually`,`It takes a screenshot before every test starts, regardless of outcome`,`testInfo.status is only available before the test runs, so this comparison always evaluates to false`,`This fixture pattern is invalid because fixtures cannot access testInfo as a third parameter`],0,`Fixture teardown code (after use()) can inspect testInfo.status vs testInfo.expectedStatus to detect exactly when something went wrong and react — a common pattern for automatic failure diagnostics.`],[`O`,`I`,`What does this code snippet illustrate about locator strictness and counting?`,`test('cart shows correct item count', async ({ page }) => {
  await page.goto('/cart');
  await expect(page.locator('.cart-item')).toHaveCount(3);
});`,[`toHaveCount() is specifically designed to work with locators that match MULTIPLE elements, unlike most other assertions which require strict mode (exactly one match) — it retries until the number of matched elements equals 3 or the timeout elapses`,`This throws a strict-mode violation because .cart-item likely matches more than one element`,`toHaveCount() only works on locators guaranteed to match exactly one element`,`The count value must be a string, not a number, causing this test to fail`],0,`toHaveCount() is one of the few assertions explicitly built to work with a locator matching many elements at once — it is exempt from the usual single-match strict-mode requirement.`],[`O`,`A`,"What does this test's use of `test.info().errors` after several soft assertions demonstrate?",`test('accessibility landmarks', async ({ page }) => {
  await page.goto('/');
  await expect.soft(page.getByRole('banner')).toBeVisible();
  await expect.soft(page.getByRole('navigation')).toBeVisible();
  await expect.soft(page.getByRole('contentinfo')).toBeVisible();

  test.info().errors.forEach(err => console.log('soft failure:', err.message));
});`,[`testInfo.errors accumulates every recorded failure (including soft assertion failures) for the current test, letting code inspect or log all of them programmatically rather than relying solely on the final report`,`testInfo.errors only contains hard assertion failures, never soft ones`,`Accessing testInfo.errors clears the accumulated failures, making the test pass regardless of what failed`,`This code throws an error because testInfo.errors does not exist as a public API`],0,`testInfo.errors is the programmatic view into everything Playwright has recorded as a failure for the current test so far, including soft assertion failures.`],[`R`,`I`,`A team wants a small number of "canary" tests to run with a longer timeout and extra retries because they exercise an inherently slow third-party integration, without changing the timeout/retries for the other 200 tests in the suite. What is the cleanest approach?`,null,["Group the canary tests inside their own `test.describe()` block and call `test.describe.configure({ timeout: 90_000, retries: 3 })` at the top of that block, leaving the global config and all other tests untouched",`Duplicate playwright.config.ts into a second file just for these tests and maintain both indefinitely`,`Manually add { timeout: 90_000 } as the second argument to each of the canary tests individually, repeating the retries logic with a try/catch in every one`,`Set the global timeout and retries higher for the whole suite, accepting the side effect on all other tests`],0,`describe.configure() is exactly the tool for scoping timeout/retries adjustments to a specific, identifiable group of tests without a second config file or per-test repetition.`],[`S`,`I`,`A test intermittently fails with "strict mode violation: getByRole('listitem') resolved to 3 elements" only on days when a marketing team pushes a new promotional banner that happens to also render as a list item. What is the most robust long-term fix, versus a quick patch?`,null,[`Quick patch: scope the locator more narrowly (e.g. within a specific container, or by accessible name) so it only ever matches the intended element; robust fix: coordinate with the marketing team or use a more specific test-id-based locator that will not accidentally match unrelated future content`,`Add .first() permanently to silence the strict-mode error whenever it appears, regardless of which element ends up first`,`Disable strict mode globally in playwright.config.ts so ambiguous locators never cause failures`,`Delete the test since it is too fragile to maintain against a changing marketing banner`],0,`Strict-mode violations caused by unrelated content are a signal to make the locator more specific and resilient, not to silence the safety check that caught the ambiguity in the first place.`],[`S`,`A`,"A team notices that `test.info().attach()` calls inside a custom fixture's teardown are silently missing from the HTML report for tests that time out (as opposed to tests that fail via a thrown assertion). What is the likely explanation?",null,[`When a test times out, Playwright may force-terminate execution (including in-progress fixture teardown) before an async attach() call has a chance to fully complete and register — teardown logic reacting to a timeout should be written defensively and, where critical, use synchronous or fast operations`,`testInfo.attach() is fundamentally incompatible with any fixture teardown code, in general`,`Timed-out tests never run any teardown code at all, under any circumstances`,`attach() only works when called from inside the test body directly, never from a fixture`],0,`A hard timeout can cut off in-progress async work, including fixture teardown — attachments that depend on completing an async operation after a timeout are a known edge case worth testing for.`],[`S`,`I`,"After migrating from Cypress, a team's tests use lots of `page.locator('#submit-btn')` id-based selectors and complain that Playwright tests feel just as brittle to markup changes as their old suite. What is the recommended shift in approach?",null,[`Migrate toward Playwright's role/label/text-based locators (getByRole, getByLabel, getByText) or explicit test IDs (getByTestId) instead of relying on implementation-detail CSS ids or classes, which tend to change during unrelated refactors`,`Add more explicit page.waitForTimeout() calls before every locator interaction, since brittleness is really a timing problem`,`Switch every locator to XPath expressions, which are inherently more stable than CSS ids`,`Brittleness is unavoidable regardless of locator strategy and simply requires more frequent test maintenance`],0,`The specific locator strategy matters — Playwright's recommended user-facing locators (role, label, text, test-id) are deliberately designed to be more resilient to markup churn than raw CSS ids or classes.`],[`S`,`A`,"A worker-scoped `authToken` fixture fetches a token once per worker to save time, but tests intermittently fail hours into a long CI run with 401 Unauthorized errors, while the same suite passes reliably when run in a fast, short burst. What is the most likely cause?",null,[`The token has a limited time-to-live and is expiring mid-run because it was fetched once at worker startup and never refreshed — the fixture needs logic to detect an expired/expiring token and re-fetch it, or should be scoped more conservatively`,`Worker-scoped fixtures are only valid for exactly one hour by a hardcoded Playwright limit`,`401 errors are always caused by network flakiness, unrelated to fixture scope or token lifetime`,`The fix is to switch authToken to an auto fixture, which would resolve the expiration issue`],0,`A worker-scoped resource fetched once is only as good as its lifetime — a token intended to be reused across many tests needs an expiration/refresh strategy if the run can span longer than the token's validity window.`],[`O`,`I`,`What does this test tag declaration enable at the CLI?`,`test('critical checkout path', { tag: ['@smoke', '@critical'] }, async ({ page }) => {
  await page.goto('/checkout');
});`,['Tags declared in the test\'s options object can be filtered with `npx playwright test --grep "@smoke"`, giving a structured, first-class alternative to embedding tags as plain substrings inside the title string',`Tags are purely cosmetic metadata and cannot be used for CLI filtering at all`,`The tag option requires a separate tags.config.ts file to be registered before use`,`Only one tag per test is allowed; the array here would throw a configuration error`],0,`The dedicated tag option is functionally similar to embedding "@smoke" in the title for --grep purposes, but keeps tagging structured and separate from the human-readable title text.`],[`O`,`A`,`What is the effect of this fixture definition using a tuple with options instead of a plain async function?`,`export const test = base.extend<{ slowSetup: void }>({
  slowSetup: [async ({}, use) => {
    await use();
  }, { timeout: 60_000 }],
});`,[`The tuple form lets you attach fixture-specific options — here, extending this particular fixture's own setup/teardown timeout to 60 seconds — separate from the overall test timeout, useful when a fixture's setup is inherently slow`,`The array syntax is invalid; fixtures must always be defined as a plain async function, never a tuple`,`The timeout option in the tuple applies to every test in the file, overriding the global config`,`This syntax only works for worker-scoped fixtures, never test-scoped ones`],0,`The [fn, options] tuple form of a fixture definition is how you attach per-fixture configuration like scope or timeout, distinct from the plain single-function form.`],[`O`,`I`,`What does this code demonstrate about accessing a test's configured project name at runtime?`,"test('logs which project ran this test', async ({}, testInfo) => {\n  console.log(`Running under project: ${testInfo.project.name}`);\n});",[`testInfo.project.name reports the name of the currently active project (e.g. "chromium" or "Mobile Safari") as defined in playwright.config.ts, letting a test log or branch on which project configuration it is running under`,`testInfo.project.name is always the string "default" regardless of the actual project configuration`,`Accessing testInfo.project requires the project to explicitly opt in via a config flag`,`This throws a runtime error because testInfo cannot be destructured when no other fixtures are requested`],0,`testInfo.project exposes the full resolved project configuration the current test is running under, including its name, matching whatever was declared in the projects array.`]],browsers:[[`O`,`B`,`In Playwright's object model, what is a BrowserContext?`,null,[`The running browser process (Chromium/Firefox/WebKit)`,`An isolated session with its own cookies, localStorage, and auth state`,`A single tab open in the browser`,`A configuration object for browser launch options`],1,`A BrowserContext is completely isolated — its own cookies, localStorage, and authentication state. Multiple contexts in one browser never share state.`],[`O`,`B`,"What does `browser.newContext()` return?",null,[`A new browser process`,`A new isolated BrowserContext`,`A new Page (tab)`,`A new Playwright test worker`],1,"`newContext()` creates a fresh isolated session inside the existing browser process — like opening a new incognito window."],[`O`,`B`,"What happens to all pages when `context.close()` is called?",null,[`Pages remain open but are disconnected`,`All pages within that context are closed automatically`,`Only the most recently opened page is closed`,`Pages are moved to the default context`],1,`Closing a context tears down everything inside it. You never need to close individual pages when you're about to close the context.`],[`O`,`I`,"What is the key difference between `browser.newPage()` and `context.newPage()`?",null,["`browser.newPage()` opens a new window; `context.newPage()` opens a new tab","`browser.newPage()` creates a page in a brand-new default context; `context.newPage()` creates a page inside an existing context","`browser.newPage()` is faster; they are otherwise identical","`context.newPage()` requires authentication; `browser.newPage()` does not"],1,"`browser.newPage()` implicitly creates a new context for the page. `context.newPage()` reuses your custom context so the page inherits its viewport, locale, auth state, etc."],[`O`,`B`,"What does the `viewport` option in `browser.newContext()` control?",null,[`The resolution of the physical display`,`The width and height of the browser viewport for that context`,`The zoom level of the browser`,`The aspect ratio used for screenshots`],1,"`viewport: { width: 375, height: 812 }` sets the browser window size, directly affecting responsive layout breakpoints."],[`O`,`I`,"What does `storageState: 'auth.json'` do when passed to `browser.newContext()`?",null,[`Saves the current cookies to auth.json at test start`,`Pre-loads previously saved cookies and localStorage into the new context`,`Encrypts the context's storage`,`Blocks the context from writing to localStorage`],1,"`storageState` bootstraps the context with a saved session — skipping the login flow entirely. Run one login test, save the state, reuse it everywhere."],[`O`,`B`,`Which three browser engines does Playwright natively support?`,null,[`Chrome, Firefox, Safari`,`Chromium, Firefox, WebKit`,`Edge, Chrome, Firefox`,`Chromium, Gecko, Blink`],1,`Playwright bundles patched builds of Chromium, Firefox, and WebKit. WebKit powers Safari; Chromium powers Chrome and Edge.`],[`O`,`I`,`Are cookies shared between two BrowserContexts created from the same browser?`,null,[`Yes — they share the same browser process storage`,`No — each context is fully isolated with its own cookie jar`,`Only session cookies are shared; persistent cookies are isolated`,`Yes, unless incognito mode is enabled`],1,`Complete isolation is the whole point of BrowserContext. Context A's cookies, localStorage, and IndexedDB are invisible to Context B.`],[`O`,`I`,"What does `colorScheme: 'dark'` do in `browser.newContext()` options?",null,[`Changes the browser UI chrome to dark mode`,"Sets the CSS media feature `prefers-color-scheme` to `dark` for that context",`Inverts the page colors`,`Enables night mode screenshots`],1,"`colorScheme` controls the `prefers-color-scheme` media query, letting you test dark/light theme variants without OS-level changes."],[`O`,`I`,"What must you also include alongside `geolocation` coordinates in `browser.newContext()` for geolocation to work?",null,[`The country code as a string`,"`permissions: ['geolocation']` to grant the permission","`locale` matching the country","`timezoneId` for the location"],1,"Browsers prompt users for geolocation permission. In Playwright you pre-grant it via `permissions: ['geolocation']`; without it the API is blocked."],[`O`,`B`,"In a standard test using the `page` fixture, who creates and manages the BrowserContext?",null,[`The developer must create it manually`,"Playwright's fixture system creates it automatically from project `use` settings",`The operating system allocates it`,`The browser's built-in session manager`],1,"The `page` fixture is built on top of `context`, which is itself built on `browser`. Playwright's fixture chain handles the full lifecycle automatically."],[`O`,`A`,"What does `await context.storageState({ path: 'state.json' })` do?",null,[`Loads a previously saved state into the context`,`Saves the current cookies and localStorage of the context to a JSON file`,`Clears all cookies and saves an empty state`,`Exports the context configuration`],1,"`storageState()` captures the current cookies and localStorage snapshot. Reuse this file in `newContext({ storageState: 'state.json' })` to start future tests already authenticated."],[`F`,`B`,"Complete: `const context = await browser.______({ locale: 'fr-FR' })`",null,[`newContext`,`createContext`,`openContext`,`makeContext`],0,"`browser.newContext(options)` is the standard API for creating a customised isolated session."],[`F`,`B`,"Complete: `const page = await context.______()` to open a new tab inside the context.",null,[`newPage`,`openPage`,`createPage`,`addPage`],0,"`context.newPage()` opens a new browser tab (Page) that inherits all the context's settings — viewport, locale, cookies, etc."],[`F`,`I`,"Fill in the option key for window dimensions: `await browser.newContext({ ______: { width: 375, height: 812 } })`",null,[`viewport`,`windowSize`,`resolution`,`screen`],0,"`viewport` sets the browser viewport width and height, directly affecting responsive CSS breakpoints during tests."],[`F`,`I`,"Fill in the option to set the browser timezone: `await browser.newContext({ ______: 'Europe/London' })`",null,[`timezoneId`,`timezone`,`locale`,`region`],0,"`timezoneId` accepts an IANA timezone string. It affects `Date` output, API responses that return local times, and timezone-aware UI components."],[`F`,`I`,"Fill in the permissions array: `await browser.newContext({ permissions: ['______', 'notifications'] })`",null,[`geolocation`,`location`,`gps`,`coordinates`],0,"The permission string is `'geolocation'`. You must grant it here, otherwise the browser blocks `navigator.geolocation` calls."],[`F`,`A`,"Fill in the option to pre-load saved auth: `await browser.newContext({ ______: 'auth.json' })`",null,[`storageState`,`authState`,`sessionFile`,`cookieFile`],0,"`storageState` accepts a path to a JSON file produced by `context.storageState({ path: 'auth.json' })`."],[`F`,`I`,"Fill in the option to record a video: `await browser.newContext({ ______: { dir: 'videos/' } })`",null,[`recordVideo`,`video`,`captureVideo`,`screenRecord`],0,"`recordVideo: { dir: 'path/' }` starts recording a video for every page opened in the context. Useful for debugging CI failures."],[`E`,`B`,"What is wrong?\n`const context = browser.newContext({ viewport: { width: 1280, height: 720 } })`",null,["Missing `await` — `browser.newContext()` returns a Promise","`browser` is not available without importing it",`viewport values must be strings`,`newContext does not accept an options object`],0,"`browser.newContext()` is async and returns a Promise<BrowserContext>. Without `await` you store an unresolved Promise, not the context."],[`E`,`B`,"What is wrong?\n`const page = await context.newContext()`",null,["`newContext()` is a method on `browser`, not `context` — use `context.newPage()`","`context` must be closed before creating a new page","`newContext` requires options to be passed",`You cannot create pages from a context directly`],0,'`BrowserContext` has `newPage()`. The `newContext()` method lives on `Browser`. Calling it on context throws a "not a function" error.'],[`E`,`I`,`Spot the issue: a test creates two pages inside a single context and treats them as two separate users.`,null,[`Pages in the same context share cookies and auth — they are not isolated users`,`Two pages cannot exist in the same context`,`The second page will overwrite the first`,`Context can only hold one page at a time`],0,`Isolation happens at the context level, not the page level. Two pages in the same context share all session data — use two separate contexts for two users.`],[`E`,`I`,"What is wrong with this geolocation setup?\n`await browser.newContext({ geolocation: { latitude: 51.5, longitude: -0.1 } })`",null,["`permissions: ['geolocation']` is missing — without it the permission is blocked",`latitude/longitude values must be integers`,`geolocation must be a string, not an object`,`The context must navigate to a page before geolocation takes effect`],0,"Setting coordinates alone is not enough; the browser also needs the permission granted. Add `permissions: ['geolocation']` to the options object."],[`E`,`A`,`What is wrong with this cleanup sequence?
\`\`\`
const context = await browser.newContext();
const page = await context.newPage();
await page.close();
await page.goto('/');
\`\`\``,null,["`page.goto()` is called after `page.close()` — the page is already destroyed","`context.close()` must be called before `page.close()`","`page.close()` requires a URL argument",`pages cannot be closed individually`],0,"After `page.close()`, the page object is disposed. Calling any method on it (like `goto`) throws an error. Close pages only when you are finished with them."],[`E`,`I`,"What is wrong?\n```\nawait browser.newContext({ colorScheme: 'night' })\n```",null,["Valid values are `'dark'`, `'light'`, or `'no-preference'` — `'night'` is not valid",`colorScheme must be a boolean`,`colorScheme is set on the page, not the context`,"The option key should be `prefersDarkMode`"],0,"Playwright's `colorScheme` accepts `'dark'`, `'light'`, or `'no-preference'`. Any other string causes a context creation error."],[`D`,`B`,`Two sequential tests share login state even though each should start fresh. What is the most likely cause?`,null,[`Both tests reuse the same BrowserContext — they should each get an isolated context`,`The tests are running in parallel and sharing a browser`,`localStorage is not cleared between page navigations`,"The `page` fixture does not reset cookies"],0,"By default each test gets its own context via the `context` fixture. If you share a context manually between tests, state bleeds. Let Playwright's fixtures manage context lifecycle."],[`D`,`I`,`A mobile layout test passes on desktop but shows the wrong viewport. What configuration is missing?`,null,["The context `viewport` option was not set — add `viewport: { width: 375, height: 812 }`",`The test must run in headed mode`,"`userAgent` must be set to a mobile string",`Mobile tests require a separate browser installation`],0,"Without an explicit `viewport`, contexts use the default (usually 1280×720). Set `viewport: { width: 375, height: 812 }` or use `devices['iPhone 12']` preset."],[`D`,`I`,`A location-based feature returns the server's default city instead of the test location. The context has geolocation coordinates. What is missing?`,null,["`permissions: ['geolocation']` is missing from the context options","`locale` must match the geolocation country","`timezoneId` must be set alongside geolocation","The page must call `navigator.geolocation.getCurrentPosition()` manually"],0,"Without granting the geolocation permission, the browser silently blocks `navigator.geolocation`. Add `permissions: ['geolocation']` to the context options."],[`D`,`A`,"A two-user test creates `adminPage` and `guestPage` from the same context. Admin actions affect the guest session. How do you fix it?",null,[`Create two separate contexts: one for admin, one for guest`,"Call `context.clearCookies()` between creating each page",`Use different browsers for each user`,"Create one context per page with `browser.newPage()`"],0,"Isolation is a context-level concept. Create `adminCtx` and `guestCtx` separately — each with their own `storageState` — then create a page in each."],[`D`,`B`,"After a test finishes, memory usage keeps growing. Each test creates a context but never calls `context.close()`. What is the correct fix?",null,["Call `await context.close()` in `test.afterEach()` or let Playwright's `context` fixture handle cleanup","Call `page.close()` at the end of each test","Use `browser.disconnect()` instead",`Restart the browser between tests`],0,"Any context you create manually must be closed manually. Alternatively, use the built-in `context` fixture — Playwright closes it automatically after each test."],[`D`,`I`,"`await context.storageState({ path: 'auth.json' })` is called before the user has logged in. Later tests fail authentication. What went wrong?",null,[`The state was saved before login — it contains no auth cookies`,`auth.json was saved to the wrong directory`,"storageState must be called with `await` only after context.close()",`The path must be an absolute path`],0,"`storageState()` captures the state at the moment it is called. Save it only after the login flow is complete and the auth cookies are present."],[`C`,`B`,"Complete: `const ctx = await browser.______({ colorScheme: 'dark' })`",null,[`newContext`,`createContext`,`openContext`,`newSession`],0,"`browser.newContext(options)` is the method for creating a custom isolated browser session."],[`C`,`I`,"Complete to open a page inside the context: `const page = await ctx.______( )`",null,[`newPage`,`openPage`,`createPage`,`spawn`],0,"`context.newPage()` creates a new tab (Page) inside the context, inheriting all its settings."],[`C`,`I`,"Complete to emulate a mobile viewport: `await browser.newContext({ viewport: { width: 390, ______: 844 } })`",null,[`height`,`h`,`rows`,`length`],0,"`viewport` requires both `width` and `height` as numbers (in CSS pixels). This example emulates an iPhone 14 screen size."],[`C`,`A`,"Complete to save the logged-in session: `await context.______({ path: 'session.json' })`",null,[`storageState`,`saveState`,`exportCookies`,`persist`],0,"`context.storageState({ path: '…' })` writes a JSON snapshot of all cookies and localStorage, ready to be loaded by future contexts."],[`C`,`I`,"Complete to tear down the context and all its pages: `await context.______( )`",null,[`close`,`destroy`,`end`,`dispose`],0,"`context.close()` is the standard teardown method. It closes all pages inside the context, releases resources, and terminates the session."],[`L`,`B`,`If three pages are created from the same BrowserContext, do they share cookies?`,null,[`Yes — pages in the same context share all session state`,`No — each page is isolated`,`Only session cookies are shared`,`Only if they navigate to the same origin`],0,`Isolation is at the context level. All pages within one context share cookies, localStorage, and auth — they represent the same "user session".`],[`L`,`I`,"Which statement best describes `browser.newPage()` vs `browser.newContext().then(c => c.newPage())`?",null,["`browser.newPage()` implicitly creates a new default context; chaining newContext() gives you a custom context","They are identical — `browser.newPage()` uses the existing context","`browser.newPage()` is deprecated in favour of the chained approach","`browser.newPage()` creates a page without any context"],0,"`browser.newPage()` is a shortcut that creates a default context + page. Use the explicit `newContext()` approach when you need custom options or multiple contexts."],[`L`,`I`,`In what order should you close resources created manually?`,null,[`page → context (closing context also closes pages, so closing page first is optional)`,`context → page (you must close context before page)`,`browser → context → page`,`page → browser (context closes automatically)`],0,`You can close pages first and then the context, or just close the context which closes all its pages. Never try to use a page after its context is closed.`],[`L`,`A`,`Can a BrowserContext be shared between two different test files running in parallel workers?`,null,[`No — each worker runs in a separate process with its own browser instances`,`Yes — contexts are shared across the test suite`,"Yes — if using the shared `browser` fixture",`Only if both files import the same context variable`],0,`Playwright workers are separate Node.js processes. They cannot share JavaScript objects like a BrowserContext. Each worker manages its own browser and contexts.`],[`L`,`I`,"Does the built-in `page` fixture create a new BrowserContext for every test?",null,["Yes — `page` fixture depends on `context` fixture, which creates a fresh context per test",`No — all tests share one context per worker`,"Only if `reuseExistingServer` is set to false",`No — context is created once per test file`],0,"The fixture dependency chain is `browser → context → page`. Each test gets a fresh context (and page), guaranteeing full isolation between tests."],[`R`,`B`,`Your app behaves differently in Safari. How do you run a Playwright test specifically against WebKit?`,null,["Define a project with `use: { ...devices['Desktop Safari'] }` or `browserName: 'webkit'`",`Install Safari and connect Playwright to it`,"Use `browser.webkit.launch()` inside the test","Pass `--safari` to the CLI"],0,"Playwright's `webkit` project targets the same engine Apple uses for Safari. Add it to the `projects` array in playwright.config.ts."],[`R`,`I`,`You need to simultaneously test a real-time chat feature between two users. What is the recommended approach?`,null,[`Create two BrowserContexts (one per user) and a page in each — interact with both in the same test`,`Run the test twice with different users`,`Use two separate test files and synchronize with a database`,"Open two tabs in the same context and use `page.frames()`"],0,"Two isolated contexts simulate two users without any state bleed. Both pages can be driven within a single `test()` block."],[`R`,`I`,`You need to emulate an iPhone 12 for a mobile responsive test. What is the most concise approach?`,null,["`browser.newContext({ ...devices['iPhone 12'] })`","`browser.newContext({ mobile: true })`","`browser.newContext({ viewport: 'mobile' })`","Pass `--mobile` to the test runner"],0,"`devices['iPhone 12']` is a preset that includes viewport, userAgent, deviceScaleFactor, and touch support in one spread."],[`R`,`A`,`You need to test a feature that shows location-specific store hours (requires geolocation). What context options are needed?`,null,["`geolocation: { latitude, longitude }` AND `permissions: ['geolocation']`","Only `geolocation: { latitude, longitude }` is needed","Only `permissions: ['geolocation']` is needed — coordinates use device GPS","`locale` set to the country of the location"],0,`Both are required: the coordinates (what to report) and the permission grant (so the browser doesn't block the API call).`],[`R`,`I`,`You want every failed test to automatically produce a video recording for debugging. Where is the best place to configure this?`,null,["In `playwright.config.ts` under `use: { video: 'on-first-retry' }`","In every `browser.newContext()` call inside each test","In a `beforeEach` hook that creates a new context","Via the `--video` CLI flag only"],0,"Setting `video: 'on-first-retry'` in the global `use` block applies to all tests. On the first retry of a failing test, a video is recorded automatically."],[`S`,`I`,`A test creates two pages in the same context for a "two-user" chat test. The admin sees messages before the guest sends them. What is the root cause?`,null,[`Both pages share the same auth session — they are the same user in two tabs`,`The test has a race condition in sending messages`,`The context was not initialised with storageState`,`Pages in the same context cannot communicate`],0,"Shared context = shared identity. To truly simulate two users, create two separate contexts, each loaded with its own `storageState`."],[`S`,`A`,`CI videos show the wrong time zone in date/time components, making it hard to compare with expected values. How do you fix it?`,null,["Add `timezoneId: 'UTC'` (or the app's expected timezone) to `browser.newContext()` options",`Set the OS timezone on the CI server`,"Add a beforeEach that calls `Date.setTimezone()`","Use `locale` to control timezone display"],0,"`timezoneId` controls the browser's `Date` and `Intl` timezone. Pin it to a known value so tests produce consistent timestamps regardless of the CI server's OS timezone."],[`S`,`I`,"Each test starts logged in even though no `storageState` is configured. Investigation shows a manually created context is never closed. What is the fix?",null,["Close the context after each test with `await context.close()` or use the built-in `context` fixture","Call `context.clearCookies()` at the start of each test","Disable `storageState` in playwright.config.ts","Use `browser.newPage()` instead of `context.newPage()`"],0,"An unclosed context persists across tests in the same worker, carrying its cookies forward. Either close it explicitly or use the `context` fixture, which Playwright closes automatically."],[`S`,`A`,`You need to test three user roles (admin, editor, viewer) interacting simultaneously. What is the most maintainable structure?`,null,[`Create three contexts with their respective storageState files and a page in each, then drive all three in one test`,`Run three separate test files, one per role`,`Create one context and use page.evaluate() to switch users`,`Use test.describe.parallel() with three tests`],0,`Three contexts with pre-loaded auth states represent the three users cleanly. Drive all three pages in a single test to assert on real-time interactions between roles.`],[`O`,`B`,"What is the difference between `browser.newContext()` and `browser.newPage()`?",null,[`They are identical`,`newContext creates an isolated session; newPage opens a tab inside the default shared context — newPage does not provide isolation`,`newPage creates an isolated context automatically`,`browser.newPage() is deprecated`],1,`newPage uses the default context (shared state); newContext creates a fully isolated session. For isolation, always use newContext then context.newPage().`],[`O`,`I`,'What does `context.grantPermissions(["clipboard-read"])` do?',null,[`Grants Playwright permission to read the clipboard of the host OS`,`Grants the browser context permission to use the Clipboard API without showing a permission dialog`,`Enables clipboard monitoring for assertions`,`Allows the test to read clipboard in CI`],1,`grantPermissions bypasses the browser's permission dialog — the context treats the specified permission as already granted.`],[`O`,`B`,"What does `context.clearCookies()` do?",null,[`Clears cookies in the test runner cache`,`Removes all cookies from the browser context — subsequent requests start without any session cookies`,`Resets localStorage and sessionStorage`,`Deletes the storageState JSON file`],1,`clearCookies() removes context-level cookies without closing the context — useful for simulating logout within a test.`],[`O`,`I`,'What does `page.waitForLoadState("domcontentloaded")` wait for?',null,[`All network requests to finish`,`The HTML document to be parsed and the DOMContentLoaded event to fire — faster than "load" because it does not wait for images and stylesheets`,`The page to become visible on screen`,`All JavaScript to finish executing`],1,`domcontentloaded fires earlier than load — use it when you only need the DOM structure and don't need images or stylesheets.`],[`O`,`I`,"What is the difference between `browser.newContext` and the built-in `context` fixture?",null,[`They are the same thing`,`The built-in context fixture is test-scoped (auto-created and auto-closed per test); browser.newContext() requires manual management`,`The fixture creates an incognito context; newContext() creates a normal context`,`The fixture only works with Chromium`],1,`Always prefer the fixture for standard tests — manual context management risks forgetting close() and leaking state.`],[`O`,`B`,"What does `context.newPage()` return?",null,[`A new BrowserContext`,`A new Page (browser tab) inside the existing context — sharing cookies and storage with other pages in the same context`,`A new isolated browser window`,`A Frame object`],1,`Pages within the same context share cookies and localStorage — use different contexts for true isolation.`],[`O`,`I`,"What does setting `ignoreHTTPSErrors: true` in context options do?",null,[`Disables SSL verification for all requests`,`Makes the browser ignore SSL certificate errors — useful for testing against self-signed certificates in development environments`,`Allows HTTP requests in HTTPS pages`,`Disables Content Security Policy`],1,`Self-signed certs in dev/staging fail browser security checks — ignoreHTTPSErrors lets tests run against them without real certificate setup.`],[`O`,`B`,'What does `devices["iPhone 14"]` return?',null,[`A browser instance preconfigured for iPhone 14`,`An object with viewport, userAgent, deviceScaleFactor, isMobile, and hasTouch properties for iPhone 14 emulation`,`A device-specific test file template`,`The screen resolution of iPhone 14`],1,`devices is a dictionary of device presets — spread the object into context options to emulate that device completely.`],[`O`,`I`,"What does `context.route()` do compared to `page.route()`?",null,[`They are identical`,`context.route() intercepts requests from ALL pages in the context; page.route() only intercepts requests from that specific page`,`context.route() is faster because it uses a shared handler`,`page.route() is deprecated in favour of context.route()`],1,`Use context.route() when you want to intercept/mock requests from multiple pages; page.route() for single-page interception.`],[`O`,`A`,"What does `browser.contexts()` return?",null,[`The number of open contexts`,`An array of all BrowserContext objects currently open in the browser instance`,`An array of all open pages`,`A promise resolving to context metadata`],1,`browser.contexts() lets you introspect or close all contexts — useful in globalTeardown to clean up any leaked contexts.`],[`O`,`I`,"What does `context.addInitScript(script)` do?",null,[`Adds a script to the page's HTML head`,`Evaluates the script in every new page created in the context before any page scripts run — useful for mocking globals like Date or Math.random`,`Adds a script that runs after each navigation`,`Injects a script into existing pages only`],1,`addInitScript is the hook for mocking browser globals — it runs before the page's own scripts, so mocks are in place from the first line of application code.`],[`O`,`B`,"Does closing a `BrowserContext` also close its `Browser`?",null,[`Yes — the browser shuts down when all contexts are closed`,`No — closing a context only destroys its pages and session state; the browser process continues until browser.close() is called`,`Yes, but only if the context was the last one`,`Depends on the browser engine`],1,`Context and browser are independent lifecycle objects — close() on a context does not affect the browser process.`],[`F`,`B`,"Complete to create an isolated context with custom viewport.\n```ts\nconst context = await browser.______({ viewport: { width: 375, height: 812 } });\n```",null,[`newContext`,`newPage`,`createContext`,`openContext`],0,`browser.newContext(options) is the API for creating an isolated browser session.`],[`F`,`B`,"Complete to open a new tab inside a context.\n```ts\nconst page = await context.______();\n```",null,[`newPage`,`openPage`,`addPage`,`createTab`],0,`context.newPage() opens a new browser tab sharing the context's cookies and storage.`],[`F`,`I`,"Complete to grant microphone permission.\n```ts\nawait context.______(['microphone']);\n```",null,[`grantPermissions`,`setPermissions`,`allowPermissions`,`addPermissions`],0,`grantPermissions() accepts an array of permission strings — bypasses the browser prompt for the listed permissions.`],[`F`,`I`,"Complete to emulate an iPhone 12 using the devices preset.\n```ts\nconst context = await browser.newContext({\n  ...______['iPhone 12'],\n});\n```",null,[`devices`,`presets`,`emulators`,`deviceList`],0,`The devices object is imported from @playwright/test and contains presets for hundreds of devices.`],[`F`,`I`,`Complete to set a custom geolocation.
\`\`\`ts
const context = await browser.newContext({
  ______: { latitude: 51.5074, longitude: -0.1278 },
  permissions: ['geolocation'],
});
\`\`\``,null,[`geolocation`,`location`,`gps`,`coordinates`],0,`geolocation option sets the coordinates reported by the browser's Geolocation API.`],[`F`,`B`,"Complete to ignore SSL errors.\n```ts\nconst context = await browser.newContext({\n  ______: true,\n});\n```",null,[`ignoreHTTPSErrors`,`skipSSL`,`disableTLS`,`acceptInvalidCerts`],0,`ignoreHTTPSErrors: true makes the browser proceed past SSL certificate errors.`],[`F`,`I`,"Complete to inject a script before page scripts.\n```ts\nawait context.______(() => {\n  window.__TEST__ = true;\n});\n```",null,[`addInitScript`,`injectScript`,`runScript`,`addScript`],0,`addInitScript() injects a function to run in every new page before application code — perfect for setting up mocks.`],[`E`,`B`,"What is wrong?\n```ts\nconst page1 = await browser.newPage();\nconst page2 = await browser.newPage();\n// page1 and page2 are being used to simulate two different users\n```",null,[`browser.newPage() only works once`,`browser.newPage() creates pages in the same default context — they share cookies, so both represent the same user session. Create two separate contexts instead.`,`Pages cannot be created directly from browser`,`Each page must use a different browser`],1,`newPage() shares the default context — for isolated users, use browser.newContext() twice.`],[`E`,`I`,"What is wrong?\n```ts\nconst context = await browser.newContext();\nawait context.grantPermissions('camera'); // string, not array\n```",null,[`camera is not a valid permission name`,`grantPermissions expects an array of strings, not a single string — use ['camera']`,`grantPermissions is called on browser, not context`,`Permissions must be granted before newContext()`],1,`grantPermissions always takes an array — even for a single permission.`],[`E`,`B`,`What is wrong?
\`\`\`ts
const context = await browser.newContext();
const page = await context.newPage();
// test runs...
await browser.close(); // context never closed
\`\`\``,null,[`browser.close() automatically closes all contexts first — this is fine`,`When browser.close() is called, all contexts are forcibly closed. This is not an error but forgetting context.close() before browser.close() may skip teardown logic inside the context (like saving storageState)`,`browser.close() leaves contexts open`,`This causes a memory leak in the test runner`],1,`It is safe because browser.close() cascades — but explicitly closing the context first ensures any context-level teardown (storageState save) runs.`],[`E`,`I`,"What is wrong?\n```ts\nconst context = await browser.newContext({\n  geolocation: { lat: 51.5, lng: -0.1 }, // wrong property names\n});\n```",null,[`geolocation option is not supported`,`The properties are latitude and longitude, not lat and lng — wrong names are silently ignored`,`lng should be lon`,`geolocation requires permissions: true instead of an array`],1,`The geolocation object uses latitude and longitude — typos in property names are silently dropped.`],[`E`,`A`,`What is wrong?
\`\`\`ts
await context.addInitScript("window.flag = true");
// Later in a test:
const flag = await page.evaluate(() => window.flag);
console.log(flag); // undefined
\`\`\``,null,[`addInitScript must use a function, not a string`,`addInitScript was called after the page was created — init scripts only run in pages opened AFTER addInitScript is called. Call it before context.newPage().`,`window.flag assignment is async`,`evaluate cannot access window globals`],1,`addInitScript only affects pages opened after it is registered — existing pages miss the injection.`],[`E`,`I`,`What is wrong?
\`\`\`ts
const context = await browser.newContext({
  storageState: "./auth.json" // file doesn't exist yet
});
\`\`\``,null,[`storageState path must be absolute`,`If the storageState file does not exist, Playwright throws a file-not-found error. Ensure the auth setup project runs first (via dependencies) before this context is created`,`storageState is a config-only option, not a context option`,`JSON must be an object not a path string`],1,`The file must exist — use the dependencies: ["setup"] project pattern to guarantee it is created before test projects start.`],[`D`,`B`,`Two tests leave pages open after completing. The test runner shows "1 leaked resource" warnings. What should you fix?`,null,[`Add page.close() at the end of every test`,`Use the built-in page and context fixtures instead of manual browser.newPage() — fixtures handle cleanup automatically`,`Set cleanupLeaks: true in playwright.config.ts`,`Run tests with --cleanup flag`],1,`Fixtures are the solution — they guarantee teardown even when tests fail. Manual context/page creation requires manual cleanup that is often forgotten.`],[`D`,`I`,`After switching to WebKit, tests fail with "permission denied" for localStorage. What is the likely cause?`,null,[`WebKit does not support localStorage`,`WebKit has stricter default permissions — check if storageState requires permissions: ["storage-access"] or if the app uses partitioned cookies which WebKit handles differently`,`localStorage is a Chromium-only feature`,`WebKit requires a special context option`],1,`Cross-browser permission model differences are common gotchas — always test your context options against all target engines.`],[`D`,`B`,'`devices["Pixel 5"]` spread in context options but the test still runs at desktop viewport size. What was missed?',null,[`devices presets only set userAgent`,'Check that the context was created with the spread applied correctly: `browser.newContext({ ...devices["Pixel 5"] })`. If using the global use block, also verify the project uses the devices preset.',`Pixel 5 is not in Playwright's devices list`,`viewport must be set separately`],1,`A common mistake is spreading into the wrong object or into a project config that is overridden — verify the final options object logged by browser.newContext().`],[`D`,`A`,`A test checks geolocation but the browser returns null even though geolocation is set in context options. What is missing?`,null,[`geolocation requires a special browser flag`,`permissions: ["geolocation"] must also be set — without it the browser denies the Geolocation API access even if coordinates are provided`,`geolocation must be set after the page loads`,`The coordinates format is wrong`],1,`Coordinates tell the browser what to report; the permission tells it it's allowed to report anything — both are required.`],[`D`,`I`,"`context.addInitScript()` runs but the global it sets is undefined when checked with `page.evaluate()`. Why?",null,[`addInitScript does not affect evaluate()`,`The page may have navigated to a new URL after the init script ran — init scripts re-execute on each navigation. But if the app code overwrites the global after load, it would be lost. Check the timing with trace.`,`evaluate() uses a different JavaScript context`,`Init scripts only run on iframes`],1,`Init scripts run on navigation but application code runs after — if the app overwrites your global, inject it later in the lifecycle or use a more targeted mock.`],[`D`,`I`,'`browser.newContext({ colorScheme: "dark" })` set but the app still shows light mode. What should you check?',null,[`colorScheme only works in Firefox`,`The app may not use the prefers-color-scheme media query, or it reads a user preference from localStorage that overrides the OS setting. Check both.`,`colorScheme must be set on the page, not the context`,`dark requires headless: false`],1,`colorScheme emulates the OS preference — apps that store user preference in localStorage will use that instead. Inject the localStorage value as well.`],[`C`,`B`,"Complete to clear all context cookies.\n```ts\nawait context.______();\n```",null,[`clearCookies`,`deleteCookies`,`resetCookies`,`flushCookies`],0,`context.clearCookies() removes all cookies from the context session.`],[`C`,`I`,`Complete to add a cookie manually.
\`\`\`ts
await context.addCookies([{
  name: 'token',
  value: 'abc',
  domain: 'example.com',
  ______: '/',
}]);
\`\`\``,null,[`path`,`route`,`uri`,`url`],0,`path is a required cookie attribute alongside name, value, and domain.`],[`C`,`I`,"Complete to save the current context state to a file.\n```ts\nawait context.______({ path: 'state.json' });\n```",null,[`storageState`,`saveState`,`exportState`,`writeState`],0,`storageState() serialises the context cookies and storage to the given JSON file.`],[`C`,`B`,"Complete to get all open pages in a context.\n```ts\nconst pages = context.______();\n```",null,[`pages()`,`getPages()`,`allPages()`,`openPages()`],0,`context.pages() returns an array of all Page objects currently open in the context — synchronous.`],[`C`,`A`,'Complete to route all requests in the context to a handler.\n```ts\nawait context.______("**/*.png", route => route.abort());\n```',null,[`route`,`intercept`,`handle`,`proxy`],0,`context.route(pattern, handler) intercepts matching requests from all pages in the context.`],[`L`,`B`,`Why does Playwright use a separate BrowserContext per test by default?`,null,[`To avoid sharing browser processes between tests`,`To ensure each test starts with a clean slate — no cookies, no localStorage, no auth state from previous tests — preventing test interdependency`,`Contexts are cheaper to create than browsers`,`It is a legacy design decision`],1,`Isolation is the primary design principle — each test gets a fresh context so tests can run in any order without side effects.`],[`L`,`I`,`Can two BrowserContexts in the same browser instance share cookies?`,null,[`Yes — they share the browser's global cookie jar`,`No — contexts are completely isolated from each other's cookies, localStorage, and auth state`,`Only if they share the same origin`,`Only if both are created with storageState from the same file`],1,`Context isolation is absolute — two contexts are like two separate incognito sessions that know nothing about each other.`],[`L`,`B`,`What is the execution order when a test uses the page fixture?`,null,[`Test runs, then browser.newContext(), then context.newPage()`,`browser.newContext() → context.newPage() → test runs → page.close() → context.close()`,`context.newPage() → test runs → context.close()`,`Test runs first, then fixtures are created for assertions`],1,`Fixtures are created in dependency order before the test and torn down in reverse order after — browser → context → page.`],[`L`,`I`,"Why might `context.addInitScript()` need to be called before context.newPage()?",null,[`It doesn't — order does not matter`,`addInitScript registers the script for ALL future pages in the context — pages opened before the call miss the injection`,`addInitScript only works before navigation`,`newPage() clears all init scripts`],1,`Init scripts apply to pages opened after registration — call addInitScript() before creating pages to ensure full coverage.`],[`L`,`A`,`Is it safe to share a BrowserContext across tests in a beforeAll / afterAll pattern?`,null,[`Yes — it is the recommended pattern for speed`,`It works but risks state leakage between tests — test A's cookies are visible to test B. Use carefully only when tests are intentionally ordered and stateful, like an auth flow where login state must persist.`,`Playwright prevents it — contexts cannot be created in beforeAll`,`It is safe only if clearCookies() is called between tests`],1,`Shared context is a trade-off: faster (no context recreation) but requires careful cleanup between tests to maintain independence.`],[`R`,`B`,`You need to test how your app looks in dark mode. How do you configure this?`,null,[`Set colorScheme: "dark" in browser.newContext() options or in the use block — Playwright emulates the prefers-color-scheme media query`,`Use CSS injection to force dark mode`,`Toggle the system dark mode setting before tests`,`Pass --dark-mode to the CLI`],0,`colorScheme: "dark" makes the browser report prefers-color-scheme: dark — your CSS media queries respond accordingly.`],[`R`,`I`,`Your app uses service workers. How do you disable them for testing to avoid caching issues?`,null,[`Add serviceWorkers: "block" to context options — Playwright will prevent all service workers from registering`,`Delete the service worker registration in beforeEach`,`Use route() to block the service worker script`,`Service workers cannot be disabled in Playwright`],0,`serviceWorkers: "block" prevents registration — tests see the raw network responses without the service worker cache layer.`],[`R`,`A`,`You need to test a web app that uses WebUSB. How do you handle the permission?`,null,[`WebUSB cannot be tested in headless mode`,`Grant the permission using context.grantPermissions() — however WebUSB requires specific origin and device pairing that may need additional setup beyond permissions`,`Use a mocked USB device via context.addInitScript()`,`WebUSB testing requires real hardware`],1,`For specialized permissions like WebUSB, combine grantPermissions with addInitScript to mock the USB device API if real hardware isn't available.`],[`R`,`I`,`You want every context in CI to use a proxy for all requests. How do you configure this?`,null,[`Add proxy settings to each page.route() call`,`Set proxy: { server: "http://proxy:8080" } in the context options or globally in the config use block — all requests from the context route through the proxy`,`Use an environment variable HTTPS_PROXY`,`Add the proxy to the webServer config`],1,`The proxy option in context/use block routes all browser network traffic through the specified server — no per-request configuration needed.`],[`R`,`B`,`You want to verify that your app correctly handles users with slow network connections. What context option simulates this?`,null,[`Use networkThrottle in context options`,`Pass a network condition via the Chrome DevTools Protocol route — or set offline: true to test complete offline mode. For throttling, use page.route() to artificially delay responses.`,`Set latency: 500 in context options`,`Use navigator.connection.downlink mock`],1,`Playwright does not have a built-in throttle option — simulate slow networks by delaying responses in route handlers or using CDP to emulate network conditions.`],[`S`,`B`,`You create two pages in the same context for a "buyer/seller" feature test but they see each other's data. What is the fix?`,null,[`Use separate windows`,`Create two separate BrowserContexts — one for buyer (with buyer storageState) and one for seller (with seller storageState). Same-context pages share all session state.`,`Call context.isolate() between page creation`,`Use page.evaluate() to reset state`],1,`Separate contexts = separate users. Same context = same user in different tabs.`],[`S`,`I`,"A context created with `storageState` contains expired session cookies. Tests fail with auth errors. What should you do?",null,[`Re-run the auth setup project to generate fresh storageState`,`Check session TTL — if the auth setup runs in CI and tests run hours later, the session may expire mid-suite. Use short-lived tokens carefully or implement session refresh.`,`Clear the storageState file and re-run`,`Increase cookie maxAge in playwright.config.ts`],1,`storageState captures a moment-in-time snapshot — short-lived sessions can expire between setup and test execution in long CI queues.`],[`S`,`A`,"After enabling `recordHar`, the HAR file is empty. What are two likely causes?",null,[`HAR is not supported in WebKit`,`1) path was not specified in recordHar options — without a path the HAR is captured in memory only and not written to disk; 2) context.close() was not awaited — HAR finalisation happens in close()`,`HAR requires headed mode`,`HAR only works with page.route()`],1,`HAR is written on context close — both a valid path and awaited close() are required for the file to appear.`],[`S`,`I`,`A test opens 10 pages in one context for a bulk action test. The context becomes slow. What can you do?`,null,[`Use 10 separate contexts`,`Reduce the number of pages — or open pages lazily and close them immediately after use within the loop. Many open pages in one context compete for the same JavaScript thread.`,`Increase the timeout`,`Use headless: false for better performance`],1,`Each page in a context shares the browser's rendering engine — many simultaneous pages compete for CPU. Close pages when done within a loop.`],[`L`,`A`,`What is the key isolation guarantee a Playwright BrowserContext provides, and why is it central to running tests in parallel?`,null,[`Each BrowserContext has its own cookies, localStorage, and cache — like a separate incognito profile — so tests in different contexts never leak session/auth state into each other, even sharing the same browser process`,`Each context runs in a fully separate OS process, guaranteeing CPU isolation`,`Contexts share cookies but isolate JavaScript execution`,`A context can only ever open one page/tab at a time`],0,`Reusing one Browser instance but creating a fresh BrowserContext per test is the recommended way to get full state isolation without launching a new browser each time.`],[`O`,`I`,"What does `storageState: 'auth.json'` achieve?",`const context = await browser.newContext({ storageState: 'auth.json' });
const page = await context.newPage();
await page.goto('/dashboard');`,[`It pre-seeds the new context's cookies and localStorage from a previously saved file, so the page loads already logged in — skipping a manual login flow for every test`,`It saves the current session state to auth.json after the test ends`,`It restricts the context to read-only browser storage`,`It loads a mock DOM snapshot instead of navigating to a real page`],0,`Saving auth state once (via context.storageState({ path })) and reusing it across tests is the recommended pattern for skipping repeated logins.`],[`S`,`A`,`You need to test a feature where two users chat with each other in real time. How should you set this up in Playwright?`,null,[`Create two separate BrowserContexts (each with its own page), simulating two independent users/sessions, and interact with both pages within the same test`,`Use two tabs opened via window.open() inside a single page's JavaScript context`,`It is impossible to test multi-user scenarios in Playwright; you need two separate test runs`,`Use a single page and call page.reload() between each user's actions`],0,`Multiple BrowserContexts from the same Browser instance is the supported pattern for simulating multiple independent, isolated users at once.`],[`O`,`I`,"If playwright.config.ts defines three `projects` for chromium, firefox, and webkit, what happens when you run `npx playwright test`?",`test('runs on all configured browsers', async ({ page, browserName }) => {
  console.log(browserName);
  await page.goto('/');
});`,[`The test runs three times total — once per project — and browserName logs 'chromium', 'firefox', and 'webkit' respectively across those runs`,`The test runs once, and browserName logs all three names in one array`,`Only the first project (chromium) runs by default; the others require a separate command`,`All three browsers open simultaneously within a single test execution`],0,`Each projects entry in the config is effectively a separate test matrix dimension — Playwright multiplies your test files across every configured project.`],[`L`,`I`,`Why might a test pass reliably in Chromium but fail intermittently in WebKit, even though the code and selectors are identical?`,null,[`Different browser engines can have subtly different timing for rendering, animations, focus behavior, and default timeouts — cross-browser flakiness is often a real timing/rendering difference, not a test bug`,`WebKit doesn't support Playwright's actionability checks at all`,`Playwright always runs WebKit tests twice as fast, causing race conditions`,`This can only happen if the test uses browser-specific CSS selectors`],0,`This is exactly why cross-browser test suites exist — engines genuinely differ in rendering/timing edge cases.`],[`R`,`I`,`A test only needs to verify a CSS layout bug that was specific to Safari/WebKit rendering. How do you run just that one test in WebKit while keeping the rest of the suite running on all browsers?`,null,["Use `test.skip(({ browserName }) => browserName !== 'webkit', 'WebKit-only test')` inside the test, or tag/annotate it and filter with `--project=webkit`",`Delete the chromium and firefox projects from the config entirely`,`WebKit-only tests are not supported; you must run the whole suite on WebKit`,"Rename the test file to `*.webkit.spec.ts` — Playwright infers the browser from the filename"],0,`test.skip() with a browserName condition (or annotations combined with --project filtering) lets you scope tests to specific engines without splitting your whole suite.`],[`D`,`I`,"A suite passes on Chromium and Firefox but every WebKit run fails on the very first `page.goto()` with a timeout, even for a simple page. What should you check first?",null,["Whether the WebKit browser binary is actually installed (`npx playwright install webkit`) and no OS-level dependency is missing — a totally failed first navigation across all tests points to an environment issue, not a test logic bug",`Rewrite all locators to use WebKit-specific selector syntax`,`Increase every individual test timeout to 5 minutes`,`WebKit does not support page.goto(); use page.reload() instead`],0,`When every test fails at the exact same first step on one specific engine, suspect the environment (missing browser install/dependencies) before suspecting your test code.`],[`O`,`I`,`What does this configuration simulate?`,`import { devices } from '@playwright/test';
export default defineConfig({
  projects: [{ name: 'iphone', use: { ...devices['iPhone 13'] } }],
});`,[`Tests run with the iPhone 13's viewport size, user agent string, touch support, and device pixel ratio — closely emulating how the site behaves and looks on that specific mobile device, all within WebKit`,`It runs tests on an actual physical iPhone 13 connected to the test machine`,`It only changes the viewport width; everything else stays desktop-default`,`devices presets only work with the chromium browser`],0,`The devices dictionary bundles a realistic, accurate combination of viewport, user agent, and touch/DPI settings for real device emulation — not just a resized window.`],[`O`,`I`,`What does this code test?`,`const context = await browser.newContext({
  geolocation: { latitude: 51.5, longitude: -0.12 },
  permissions: ['geolocation'],
});
const page = await context.newPage();
await page.goto('/store-locator');`,[`A location-aware feature (like 'find nearest store') using a mocked GPS coordinate (London) with geolocation permission pre-granted — no real device location or manual browser permission popup needed`,`It blocks all geolocation requests, testing the app's fallback UI`,`It requires a real GPS-enabled test device to run`,`permissions: ['geolocation'] denies location access explicitly`],0,`Mocking geolocation and pre-granting permissions lets you deterministically test location-dependent features without relying on the test machine's real location or a manual permission dialog.`],[`L`,`I`,"What is the difference between `viewport` and `deviceScaleFactor` in a BrowserContext configuration?",null,["`viewport` sets the logical CSS pixel dimensions of the visible page area; `deviceScaleFactor` sets how many physical (device) pixels represent one CSS pixel — used together to accurately emulate high-DPI ('Retina') displays",`They control the exact same thing; deviceScaleFactor is a legacy alias for viewport`,`deviceScaleFactor controls network throttling speed, unrelated to visuals`,`viewport can only be set once globally and never overridden per test`],0,`A phone with a small viewport but a high deviceScaleFactor (e.g., 3) renders much sharper/denser than the same CSS pixel dimensions would suggest — both matter for accurate visual testing.`],[`O`,`A`,`What does this achieve?`,`const context = await browser.newContext();
const page1 = await context.newPage();
const page2 = await context.newPage();
await page1.goto('/chat');
await page2.goto('/chat');`,[`Two tabs within the SAME BrowserContext, sharing cookies/localStorage/session — useful for testing features that behave differently across multiple open tabs of the SAME logged-in user (e.g., real-time sync between tabs)`,`Two entirely separate, isolated user sessions, like two different BrowserContexts would provide`,`An error, since a context can only ever have one page open at a time`,`page2 automatically closes page1 when created`],0,`Multiple pages within ONE context share session state (unlike multiple contexts, which are isolated) — the right tool depends on whether you're testing "one user, many tabs" or "many independent users".`],[`L`,`I`,'Why would a team configure `channel: "chrome"` (or "msedge") in their Playwright project instead of using the bundled Chromium?',null,[`To test against the actual branded browser build users run (real Google Chrome or Microsoft Edge) rather than the open-source Chromium build Playwright ships by default — catching any real-world differences in codecs, extensions behavior, or branding-specific features`,`channel only changes the reported user-agent string, with no other effect`,`Chromium and Chrome are functionally identical, so this setting has zero effect`,`channel is required to enable headless mode`],0,`Playwright's bundled Chromium is very close to Chrome but not byte-identical — the channel option lets you test against the real, installed browser build when that distinction matters.`],[`R`,`I`,`You need to test how your app displays dates, currency, and text direction for French-Canadian users. What BrowserContext options should you set?`,null,["`locale: 'fr-CA'` (and `timezoneId` if time-dependent formatting matters) when creating the context — this affects `navigator.language`, `Intl` formatting, and the `Accept-Language` header sent to the server",`There is no way to test localization without physically changing your OS locale settings`,"Set the `viewport` to a French-formatted resolution",`Locale can only be tested by manually translating the page content before the test runs`],0,`locale and timezoneId are dedicated context options precisely for testing internationalization behavior without touching the host machine's settings.`],[`D`,`I`,'A test explicitly calls `browser.close()` at the end, but subsequent tests in the same file then fail with "Target closed" errors. What is the likely mistake?',null,["Closing the shared `browser` instance manually inside a single test affects EVERY other test that reuses it — Playwright's test runner manages the browser/context/page lifecycle automatically; tests should close only what they explicitly created (like a context they made), not the shared browser",`Playwright always creates a brand-new browser per test regardless of manual close() calls, so this shouldn't be possible`,`The fix is to add browser.close() to every single test file to be consistent`,`"Target closed" errors are unrelated to browser.close() and are a Playwright bug`],0,"The built-in `browser`/`page` fixtures are managed for you across the whole run — manually closing the shared browser inside one test breaks that lifecycle for everything after it."],[`L`,`A`,"Why does `browserType.launchPersistentContext()` not return a separate Browser object the way `browserType.launch()` does?",null,[`A persistent context launches the browser with a real, on-disk user data directory (like a genuine Chrome profile) and returns the BrowserContext directly — there is no separate Browser handle because the context IS the browser session, mirroring how a real user profile works`,`It is a deprecated API that Playwright keeps only for backward compatibility`,`launchPersistentContext() always launches multiple contexts at once, hence no single Browser reference makes sense`,`Persistent contexts do not support pages, only raw CDP sessions`],0,`launchPersistentContext() is designed for scenarios needing a real, disk-backed profile (extensions, saved browser state) — its object model reflects "one profile, one context" rather than "one browser, many contexts."`],[`L`,`A`,"Why would a team use `browserType.launchPersistentContext()` with `args: ['--disable-extensions-except=./my-extension', '--load-extension=./my-extension']` instead of a regular `browser.newContext()`?",null,[`Chrome extensions can only be loaded into a real, persistent browser profile at launch time — a regular non-persistent context/browser instance does not support loading unpacked extensions`,`Extensions load identically in both persistent and non-persistent contexts; the flags are purely cosmetic`,`launchPersistentContext() is required only for Firefox extensions, not Chrome`,`Extensions must be installed via the Chrome Web Store API before any Playwright context can use them`],0,`Testing a browser extension is one of the primary real-world use cases for launchPersistentContext(), since extensions require a genuine, on-disk browser profile to load.`],[`L`,`A`,"Why does Playwright provide `browserType.connectOverCDP()` as distinct from `browserType.connect()`?",null,[`connectOverCDP() attaches to any browser exposing the raw Chrome DevTools Protocol (e.g. a real Chrome instance launched with --remote-debugging-port), while connect() attaches specifically to a Playwright-native browser server started via browserType.launchServer() — the two target different kinds of remote endpoints`,`They are aliases for the exact same underlying connection mechanism`,`connectOverCDP() only works with Firefox, and connect() only works with Chromium`,`connect() is deprecated in favor of connectOverCDP() for all use cases`],0,`connect() speaks Playwright's own protocol to a launchServer()-started process; connectOverCDP() speaks the generic Chrome DevTools Protocol to any CDP-compatible browser, including a real, already-running Chrome.`],[`L`,`I`,"Why might a team use `browserType.launchServer()` and connect to it separately, rather than always launching a fresh local browser per test run?",null,[`launchServer() starts a browser process that exposes a WebSocket endpoint, letting multiple separate client processes (potentially on different machines) connect to and share the same running browser instance — useful for centralized or remote browser infrastructure`,`launchServer() is required before any headless test can run at all`,`It is functionally identical to browserType.launch() with no practical difference`,`launchServer() only works with WebKit, not Chromium or Firefox`],0,`launchServer() decouples "running the browser" from "driving the browser," enabling architectures like a shared browser farm or containerized browser service that test clients connect to remotely.`],[`L`,`A`,"Why does `context.newCDPSession(page)` exist even though Playwright already provides a rich high-level API for most browser interactions?",null,[`Some low-level browser capabilities (like precise performance metrics, custom network emulation profiles, or certain debugging protocol events) are not exposed by Playwright's high-level API — a raw CDP session gives direct access to the Chrome DevTools Protocol for exactly those edge cases`,`newCDPSession() is required before any page.goto() call can succeed`,`It replaces the page object entirely once created`,`CDP sessions are only usable in headless mode, never headed`],0,`The high-level API intentionally abstracts away most low-level protocol details for ease of use — CDP sessions are an escape hatch for the rare cases needing that raw access, and are Chromium-specific.`],[`L`,`I`,"Why does `context.exposeFunction()` (or `exposeBinding()`) matter for testing pages that need to call out to Node.js-side logic during execution?",null,[`It injects a function into every page of the context that, when called from browser-side JavaScript, actually executes Node.js code and returns the result back into the page — useful for mocking complex Node-side logic (like generating a signed token) that the page needs to call during a test`,`It exposes a page-side JavaScript function to Node.js, the reverse direction from what it actually does`,`exposeFunction() only works for synchronous functions, never ones returning a Promise`,`It is required before page.evaluate() can be used at all`],0,`exposeFunction/exposeBinding bridge browser JS to Node.js code, letting the page under test call into arbitrary Node logic — the opposite direction from page.evaluate(), which runs code inside the browser.`],[`L`,`I`,"What is the practical difference between `route.abort()`, `route.continue()`, and `route.fulfill()` when handling an intercepted request via `context.route()` or `page.route()`?",null,[`abort() cancels the request entirely (simulating a network failure); continue() lets the real request proceed to the network, optionally with modified headers/method/postData; fulfill() short-circuits the request by responding directly with custom status/body/headers, never hitting the real network at all`,`They are three interchangeable names for the same behavior`,`fulfill() always forwards the request to the real server first before returning a custom response`,`continue() always blocks the request, and abort() always allows it — the names are commonly confused as being reversed`],0,`These three route-handling actions represent the fundamental choices for intercepted traffic: kill it, let it through (possibly modified), or fake a response entirely — understanding the distinction is essential for network mocking.`],[`L`,`I`,"Why does `context.unroute()` matter in a test that registers multiple `context.route()` handlers over its lifetime?",null,[`unroute() removes a previously registered handler for a given URL pattern, which matters when you want later requests to behave differently (e.g. temporarily mock an endpoint, then restore real network behavior) within the same test or context`,`unroute() is required before context.close() will succeed`,`Route handlers are automatically removed after the first matching request, making unroute() unnecessary`,`unroute() clears all cookies in addition to removing route handlers`],0,`Without unroute(), a route handler stays active for the rest of the context's lifetime — unroute() gives fine-grained control to stop intercepting once a particular mock is no longer needed.`],[`L`,`I`,'Why does route matching use "last registered wins" precedence when multiple `context.route()` (or `page.route()`) handlers match the same URL pattern?',null,[`Playwright processes route handlers in reverse registration order (most recently added first), so a more specific or override handler registered later in a test naturally takes priority over a broader one registered earlier, without needing to unroute the first`,`The first handler registered always wins, and later duplicate handlers are silently ignored`,`Only one route handler can ever be registered per context at a time; registering a second replaces the first automatically`,`Route matching order is nondeterministic and should never be relied upon`],0,`This LIFO precedence is useful in practice: register a broad default mock early, then a test-specific override later, without having to unroute the earlier one first.`],[`L`,`I`,"Why is `page.waitForEvent('popup')` necessary for testing a link that opens `target=\"_blank\"` in a new tab, rather than just continuing to interact with the original `page` object?",null,[`A new tab/window is a genuinely separate Page object within the same context — the original page reference has no visibility into it, so the test must explicitly wait for and capture the new Page instance via the 'popup' event before it can interact with the newly opened tab`,`target="_blank" links are automatically blocked by Playwright unless explicitly allowed in context options`,`Popups always open in a brand-new BrowserContext, not the current one`,`page.waitForEvent('popup') is only needed when running in headed mode`],0,`A popup is a distinct Page object — you need to explicitly capture the event to get a handle on it before you can call any Playwright methods against that new tab.`],[`L`,`I`,"Why does handling native browser dialogs (alert/confirm/prompt) require an explicit `page.on('dialog', handler)` listener, rather than Playwright auto-dismissing them by default with no configuration needed?",null,[`A dialog blocks all further page JavaScript execution until it is explicitly accepted or dismissed, and different tests need different responses (accept, dismiss, or provide prompt text) — Playwright actually DOES auto-dismiss dialogs with no listener attached, but that default may not be the behavior a given test actually needs, so a custom handler lets you override it`,`Dialogs are completely invisible to Playwright and cannot be intercepted under any circumstances`,`Every test must always accept every dialog it encounters; there is no way to dismiss one`,`Dialog handling only works in Chromium, not Firefox or WebKit`],0,`Playwright's default (auto-dismiss with no listener) is a safe fallback so tests do not hang forever on an unexpected dialog, but registering a dialog handler lets you assert on its message and control accept/dismiss/prompt input precisely.`],[`L`,`A`,"Why does Playwright's config-driven `trace: 'on-first-retry'` differ meaningfully from manually calling `context.tracing.start()` and `context.tracing.stop()` inside a test?",null,[`The config option is a convenient, automatic policy applied uniformly across the whole suite (only tracing retries), while the manual tracing API gives fine-grained, ad-hoc control to start/stop tracing around a specific, arbitrary block of code within a single test — useful for isolating exactly one suspicious operation`,`They produce fundamentally different, incompatible trace file formats that cannot both be viewed in the Trace Viewer`,`Manual tracing can only be used in debug mode, never in a normal CI run`,`The config option only works with videos, not traces, despite the name`],0,`Config-level trace settings are a broad, suite-wide policy; the manual tracing API offers surgical, code-level control for capturing just a specific section of interest.`],[`L`,`I`,"Why does the `reducedMotion` context option matter for testing an application that uses CSS or JavaScript animations?",null,["Setting `reducedMotion: 'reduce'` emulates the prefers-reduced-motion media query, letting you test how the app behaves for users with that OS accessibility preference enabled — and as a side benefit, disabling animations can make visual regression screenshots more deterministic by avoiding mid-animation captures",`reducedMotion only affects video recording speed, not the page itself`,`It permanently disables all JavaScript execution on the page`,`reducedMotion is only relevant for mobile device emulation, never desktop`],0,`This option serves two purposes: genuinely testing an accessibility-related media query, and as a practical side effect, taming animation-related flakiness in visual comparisons.`],[`L`,`A`,"Why might a test set the `forcedColors` context option to `'active'` specifically, rather than relying only on `colorScheme: 'dark'`?",null,[`forcedColors emulates OS-level high-contrast/forced-colors accessibility modes (a distinct browser feature from light/dark theme), which some apps handle via a separate forced-colors CSS media query — testing it verifies accessibility behavior that colorScheme alone does not cover`,`forcedColors and colorScheme are exact synonyms with identical effects`,`forcedColors only works when javaScriptEnabled is set to false`,`Setting forcedColors automatically also sets colorScheme to dark as a side effect`],0,`High-contrast/forced-colors mode is a genuinely separate accessibility feature from dark mode — apps that only handle prefers-color-scheme may still fail under forced-colors, which this option lets you specifically test.`],[`L`,`I`,"Why would a context be created with `javaScriptEnabled: false` for certain tests?",null,[`It verifies that critical content or functionality degrades gracefully for users/crawlers with JavaScript disabled or blocked, or specifically to test server-rendered fallback content without any client-side hydration interfering`,`javaScriptEnabled: false is required before geolocation permissions can be granted`,`It only affects CSS rendering, not actual script execution`,`This option disables Playwright's own automation scripts, breaking all further test interactions`],0,`This is a niche but real use case — testing progressive enhancement, SEO-critical server-rendered content, or how an app behaves when JS fails to load entirely.`],[`L`,`I`,"Why is `extraHTTPHeaders` set at the context level (rather than only per-request via route handlers) a common pattern for testing APIs?",null,[`Setting a header like an API key or a feature-flag header once at context creation applies it automatically to every request from every page in that context, avoiding the need to intercept and modify every individual request manually`,`extraHTTPHeaders only applies to the very first request made in the context, not subsequent ones`,`Headers set at the context level are stripped automatically before reaching the actual server`,`extraHTTPHeaders can only set a single header per context, never multiple`],0,`Context-level headers are the simplest way to apply a blanket setting (like auth tokens or custom test-environment markers) across all requests without per-request route interception.`],[`L`,`I`,"Why does Playwright provide `httpCredentials` as a dedicated context option rather than requiring tests to manually handle a browser-native basic-auth popup?",null,[`Basic-auth browser popups are native OS/browser dialogs that Playwright cannot interact with like a normal page element — httpCredentials pre-supplies the username/password so the browser authenticates automatically without ever showing that native prompt`,`httpCredentials is only used for encrypting cookies, unrelated to HTTP Basic Authentication`,`The browser always skips basic-auth prompts by default, making httpCredentials purely cosmetic`,`httpCredentials requires the target site to use OAuth instead of HTTP Basic Auth`],0,`Native browser auth dialogs sit outside the page's DOM and cannot be automated with normal locators — httpCredentials avoids the dialog appearing at all by authenticating up front.`],[`L`,`I`,"Why would a test set `offline: true` on a context partway through a test via `context.setOffline(true)` rather than only at context creation?",null,[`Testing offline behavior (like a PWA's cached fallback UI) usually requires the app to first load normally while online, then transition to offline mid-test to observe how it reacts to connectivity loss — setting offline only at creation would never let the app load in the first place`,`setOffline() can only be called once per context and never toggled back to false`,`offline: true set at creation and setOffline(true) called later produce entirely different, incompatible network states`,`setOffline() only affects WebSocket connections, not regular HTTP requests`],0,`Toggling offline state at runtime (rather than only at context creation) is essential for testing realistic connectivity transitions, like a PWA detecting it just went offline.`],[`L`,`A`,"Why does `bypassCSP: true` sometimes need to be set on a context specifically for tests that use `page.addScriptTag()` or `context.addInitScript()` to inject custom test scripts?",null,[`A strict Content-Security-Policy on the page under test can block inline or externally-injected scripts, including ones Playwright itself tries to inject for test purposes — bypassCSP disables that enforcement so injected test scripts and mocks are not silently blocked by the page's own security policy`,`bypassCSP is required before any locator can interact with the page at all`,`CSP only affects styling (CSS), not JavaScript execution, so this option is unrelated to injected scripts`,`bypassCSP automatically disables all cookies for the context as a side effect`],0,`A real CSP header is designed to block exactly this kind of script injection — bypassCSP is the escape hatch so your own test tooling is not caught by the same protection meant for real attackers.`],[`L`,`I`,"Why does testing file downloads typically involve `page.waitForEvent('download')` rather than just clicking a download link and immediately checking the filesystem?",null,[`The download event fires asynchronously once the browser begins the download, and waiting for it gives you a Download object with methods like path() and saveAs() to reliably inspect or persist the file — checking the filesystem immediately after a click risks a race condition where the download has not started or finished yet`,`Playwright never actually downloads files to disk unless waitForEvent('download') is used first`,`Download events only fire in headed mode, never in headless mode`,`acceptDownloads must be explicitly set to true, and without it, waitForEvent('download') always times out`],0,`Waiting for the download event avoids a race condition between the click action and the asynchronous browser download process, giving you a reliable handle on the resulting file.`],[`L`,`A`,"Why is it meaningful that `acceptDownloads` defaults to `true` in modern Playwright, when earlier versions required it to be explicitly enabled?",null,[`The change reflects that testing download flows became common enough that requiring every context to opt in was unnecessary friction — defaulting to true means downloads "just work" out of the box while still being controllable if a team wants to explicitly disable them for a specific test`,`It means every downloaded file is now automatically opened in a new tab after downloading`,`The default change only applies to Firefox, not Chromium or WebKit`,`acceptDownloads defaulting to true disables all other context options related to file handling`],0,`Defaults tend to shift toward whatever most users actually need — download testing is common enough that opting out (rather than in) became the more sensible default.`],[`L`,`I`,"Why might a test call `context.setExtraHTTPHeaders()` partway through, rather than only setting `extraHTTPHeaders` once at `browser.newContext()` time?",null,[`Some test scenarios need to change the effective headers mid-test — e.g. simulating an API key rotation, or switching a feature-flag header value partway through a flow — and setExtraHTTPHeaders() lets the test update the applied headers dynamically without creating an entirely new context`,`setExtraHTTPHeaders() only works before the first page navigation, identical in restriction to the constructor option`,`Headers set via setExtraHTTPHeaders() only apply to WebSocket connections, not regular HTTP requests`,`This method does not actually exist; headers can only ever be set at context creation`],0,`Runtime header updates are useful whenever a test needs to simulate a mid-flow change in request context, without the overhead of tearing down and recreating the whole browser context.`],[`L`,`I`,"Why does `browser.version()` matter when debugging a test that behaves differently than expected across environments?",null,[`It reports the exact underlying browser engine version currently running, letting you confirm whether a CI environment and a local machine are actually using the same (or a meaningfully different) browser build — useful when ruling out environment drift as a cause of inconsistent behavior`,`browser.version() returns the version of the @playwright/test npm package, not the browser itself`,`It only works after at least one page has navigated somewhere`,`browser.version() always returns the same hardcoded string regardless of the actual browser`],0,`Confirming the actual browser build version is a useful first diagnostic step when suspecting an environment mismatch between CI and local development.`],[`L`,`A`,"Why might a script launch `chromium.launch()`, `firefox.launch()`, and `webkit.launch()` all directly and manually within one custom script, rather than relying on Playwright's `projects` array in playwright.config.ts?",null,[`Outside the standard test-runner workflow — e.g. a standalone automation script, a custom tool, or a scenario needing all three engines simultaneously within a single script's logic — manually launching each browserType gives full programmatic control that the projects-based test-runner model is not designed for`,`projects arrays can only ever launch a single browser engine, never more than one`,`Manual multi-engine launching is required before Playwright can install any browser binaries`,`This pattern is only valid inside globalSetup, never in a standalone script`],0,`The projects array is specifically a test-runner concept for running your test SUITE against multiple engines — manual multi-engine launches serve a different, more general automation use case outside that model.`],[`L`,`I`,"Why does `context.backgroundPages()` only return meaningful results for a persistent context loading a Chrome extension, and not for a normal `browser.newContext()`?",null,[`Background pages are a Chrome extension concept (the extension's background script/service worker running outside any visible tab) — they only exist when a real extension is actually loaded via a persistent context, which is not possible in a standard non-persistent context`,`backgroundPages() works identically in both cases but returns an empty array unless explicitly enabled via a config flag`,`It refers to browser tabs running in the background (not the focused tab), unrelated to extensions`,`backgroundPages() is deprecated and always returns an empty array in current Playwright versions`],0,`This API is specifically for extension testing — background pages are a Chrome extension architecture detail that only exists in the context of an actually-loaded extension.`],[`L`,`A`,"Why does emulating a mobile device sometimes require setting `isMobile` and `hasTouch` explicitly, rather than relying solely on a small `viewport` size?",null,[`Some responsive breakpoints and touch-specific UI behavior (like hover-vs-tap interactions, or a "mobile" media query relying on more than just width) depend on these separate signals — a narrow viewport alone does not tell the page it is being viewed on a genuinely touch-capable mobile device`,`isMobile and hasTouch have no actual effect and are purely for documentation purposes`,`Setting isMobile automatically forces viewport to a fixed 375x812 regardless of any explicit viewport option`,`hasTouch only affects video recording quality, not page behavior`],0,`Real mobile emulation is more than just a resized window — isMobile and hasTouch influence how the page/browser reports capabilities like touch support, which some responsive designs and JavaScript feature-detection rely on.`],[`L`,`I`,"Why does using a single, long-lived `Browser` instance across many tests (creating a fresh `BrowserContext` per test rather than a fresh `Browser`) improve performance without sacrificing test isolation?",null,[`Launching a browser process is comparatively expensive, while creating a new BrowserContext within an already-running browser is cheap and still gives full isolation of cookies, storage, and auth state — reusing the browser avoids repeated process-launch overhead while contexts still guarantee independence between tests`,`Reusing a Browser instance always leaks some state between contexts, trading isolation for speed`,`A single Browser instance can only ever support one active BrowserContext at a time`,`Creating a new BrowserContext is exactly as expensive as launching a whole new Browser process`],0,`This is exactly why the fixture chain is browser (once per worker) → context (once per test) → page — it captures the performance benefit of browser reuse while keeping full context-level isolation.`],[`L`,`A`,"Why might a company standardize on connecting Playwright to a remote, containerized browser farm (via a WebSocket endpoint and `browserType.connect()`) instead of installing browsers on every CI runner?",null,[`Centralizing browser execution in a dedicated, scalable service can reduce per-runner setup/maintenance overhead, provide consistent browser versions across all consumers, and allow scaling browser capacity independently from the CI compute running the actual test orchestration logic`,`connect() to a remote endpoint is required before any headless test can run at all`,`Local browser installation via npx playwright install is no longer supported in current versions`,`Remote browser farms are only usable for visual regression testing, not functional tests`],0,`This is a common architecture for large organizations — separating "where tests execute logic" from "where browsers actually run" for consistency and scalability.`],[`L`,`I`,"Why does saving `storageState` capture localStorage per-origin (as an `origins` array) rather than as one flat key-value structure?",null,[`localStorage is inherently scoped per-origin in browsers (each domain has its own isolated storage) — the storageState format mirrors that reality, recording a separate localStorage snapshot for every distinct origin the context visited, not a single global bucket`,`The origins array only exists for backward compatibility and is always empty in current Playwright versions`,`Cookies and localStorage are stored together under a single origin entry with no distinction`,`origins in storageState refers to allowed CORS origins, not localStorage data`],0,`storageState's structure reflects genuine browser storage semantics — localStorage never spans multiple origins, so a faithful snapshot needs to be organized the same way.`],[`O`,`A`,`What does this code accomplish?`,`const context = await chromium.launchPersistentContext('./user-data-dir', {
  headless: false,
  args: [
    '--disable-extensions-except=./my-extension',
    '--load-extension=./my-extension',
  ],
});
const [background] = context.backgroundPages();`,[`It launches a real, disk-backed Chrome profile with a specific unpacked extension loaded, then grabs a reference to the extension's background page — a pattern used specifically to test or interact with a Chrome extension's internals`,`It creates a standard isolated context identical to browser.newContext(), just with a longer syntax`,`backgroundPages() here returns every currently open tab, not extension-related pages`,`This code fails because launchPersistentContext() cannot accept an args option`],0,`Extension testing specifically requires a persistent context (a real profile) plus the extension-loading Chromium flags, after which backgroundPages() exposes the extension's own background script context.`],[`O`,`A`,`What does this code enable, and in what kind of environment would it typically be used?`,`const browser = await chromium.connectOverCDP('http://localhost:9222');
const context = browser.contexts()[0];
const page = context.pages()[0];
await page.goto('https://example.com');`,[`It attaches Playwright to an already-running Chrome instance that was started with remote debugging enabled (e.g. --remote-debugging-port=9222), then reuses that browser's existing first context/page — common for automating or inspecting a browser session that was launched outside of Playwright entirely`,`It launches a brand new Chromium instance listening on port 9222`,`connectOverCDP() only works with browsers that Playwright itself originally launched`,`This code creates a new, empty context and ignores whatever was previously open in the browser`],0,`connectOverCDP() is the bridge to any CDP-speaking browser, including a manually launched real Chrome — this pattern is common for attaching to an existing debugging session rather than starting fresh.`],[`O`,`I`,`What does this route handler configuration achieve?`,`await context.route('**/*.{png,jpg,jpeg,gif}', route => route.abort());
await page.goto('/gallery');`,[`Every image request across any page in the context is cancelled before reaching the network, letting the page load faster in tests that do not care about actual image content — commonly used to speed up unrelated functional tests`,`route.abort() lets the request through to the real server but discards the response body afterward`,`This only blocks images from the very first page in the context, not subsequent ones`,`The glob pattern only matches files in the root directory, not nested paths`],0,`Aborting requests matching a broad glob pattern is a common technique to strip out irrelevant, bandwidth-heavy resources (like images) that a particular test does not need to actually load.`],[`O`,`A`,`What is the effect of registering these two route handlers in this order?`,`await page.route('**/api/**', route => route.fulfill({ status: 200, body: '{"default": true}' }));
await page.route('**/api/users', route => route.fulfill({ status: 200, body: '{"users": []}' }));

await page.goto('/dashboard');`,[`Because Playwright matches route handlers in reverse (most-recently-registered-first) order, a request to /api/users is handled by the more specific second handler (returning the users array), while other /api/** requests fall through to the broader first handler`,`The first-registered handler always takes precedence, so /api/users would incorrectly receive the generic {"default": true} response`,`Both handlers execute for every matching request, merging their two responses together`,`This configuration throws an error because two handlers cannot match overlapping URL patterns`],0,`Registering a broad mock first, then a more specific override afterward, relies on Playwright's reverse-registration-order matching to make the specific handler win for its narrower pattern.`],[`O`,`I`,"What does this code verify, and why is `waitForEvent` necessary here?",`const [popup] = await Promise.all([
  page.waitForEvent('popup'),
  page.getByRole('link', { name: 'Open in new tab' }).click(),
]);
await popup.waitForLoadState();
await expect(popup).toHaveURL(/help-center/);`,[`It captures the new tab (Page object) opened by clicking a target="_blank" link, waits for it to finish loading, and asserts on its URL — the Promise.all pairs the click with the popup event listener so neither the click nor the event is missed due to timing`,`popup here refers to a native browser alert dialog, not a new tab`,`waitForEvent('popup') is unnecessary since page.getByRole().click() automatically returns the new page`,`This code always fails because a context can only ever have one page open at a time`],0,`The Promise.all pattern ensures the event listener is registered before the action that triggers it fires — a common idiom for reliably capturing events tied to a specific interaction.`],[`O`,`I`,`What does this dialog handler accomplish?`,`page.on('dialog', async dialog => {
  expect(dialog.message()).toContain('Are you sure you want to delete');
  await dialog.accept();
});

await page.getByRole('button', { name: 'Delete account' }).click();`,[`It listens for any native browser dialog (like a confirm() popup) triggered by the click, asserts on its message text, then programmatically accepts it — without this listener, Playwright would auto-dismiss the dialog by default, which might not be the desired test behavior`,`It creates a custom on-page HTML dialog element instead of intercepting the native browser one`,`dialog.accept() here also automatically closes the entire page`,`This handler only works for prompt() dialogs, not confirm() or alert()`],0,`Registering a dialog listener before the triggering action lets you both assert on the dialog's content and control its outcome (accept/dismiss/input text) instead of relying on the default auto-dismiss behavior.`],[`O`,`A`,"What does this manual tracing code accomplish, compared to setting `trace: 'on'` globally in config?",`await context.tracing.start({ screenshots: true, snapshots: true });
await page.goto('/checkout');
await page.getByRole('button', { name: 'Place order' }).click();
await context.tracing.stop({ path: 'checkout-trace.zip' });`,[`It captures a trace only for this specific, manually-bounded section of code (the checkout flow) within a test, rather than tracing the entire test uniformly — useful for isolating exactly one suspicious operation without the overhead of tracing everything else`,`It is functionally identical to setting trace: 'on' in config, just written differently`,`tracing.start()/stop() can only be called once per entire test run, not per test`,`This code produces a video file, not a trace file, despite the .zip extension`],0,`The manual tracing API gives surgical control over exactly which portion of a test gets traced, unlike the broader, uniform config-level trace setting.`],[`O`,`I`,`What is being tested by this context configuration?`,`const context = await browser.newContext({
  reducedMotion: 'reduce',
});
const page = await context.newPage();
await page.goto('/onboarding');
await expect(page.locator('.tour-tooltip')).toBeVisible();`,[`Whether the onboarding tour still functions correctly for users with the OS-level "reduce motion" accessibility preference enabled, which the reducedMotion context option emulates via the prefers-reduced-motion media query`,`Whether the page loads faster when animations are disabled, purely as a performance benchmark`,`reducedMotion only affects video recording frame rate, so this test is unrelated to the actual page content`,`This configuration disables the tooltip entirely, so the assertion would always fail`],0,`reducedMotion is specifically for testing how a page responds to the prefers-reduced-motion accessibility media query, not just for suppressing animations cosmetically.`],[`O`,`A`,`What does this context configuration test for?`,`const context = await browser.newContext({
  forcedColors: 'active',
});
const page = await context.newPage();
await page.goto('/settings');
await expect(page.locator('.icon-button')).toBeVisible();`,[`Whether the page remains usable and its elements stay visible/distinguishable under an OS-level forced-colors (high-contrast) accessibility mode — a genuinely different browser feature from light/dark color scheme testing`,`Whether the page correctly displays in dark mode, since forcedColors is a synonym for colorScheme: dark`,`forcedColors: active disables all icons on the page, so this test always fails`,`This option only has an effect when combined with reducedMotion`],0,`forced-colors mode is a distinct high-contrast accessibility feature from dark/light theming — testing icon/button visibility under it validates a different accessibility concern.`],[`O`,`I`,`What is the purpose of this context configuration when testing an internal staging environment?`,`const context = await browser.newContext({
  httpCredentials: { username: 'qa-team', password: process.env.STAGING_PASSWORD },
});`,[`It pre-supplies HTTP Basic Authentication credentials so the browser authenticates automatically against a staging environment protected by basic auth, avoiding a native browser auth popup that Playwright could not otherwise interact with directly`,`It sets the login form's username and password fields automatically on every page`,`httpCredentials only works for API testing via context.request, never for actual page navigation`,`This configuration encrypts all network traffic using the given credentials as a key`],0,`httpCredentials is specifically for handling the browser-native Basic Auth challenge, which sits outside the normal page DOM and cannot be filled like a regular login form.`],[`O`,`A`,"What does this test verify, and what would happen without the `context.setOffline()` call?",`await page.goto('/dashboard');
await expect(page.locator('.data-grid')).toBeVisible();

await context.setOffline(true);
await page.reload().catch(() => {});
await expect(page.getByText('You are offline')).toBeVisible();`,[`It verifies the app shows an offline banner after connectivity is lost mid-session (loaded first while online, then set offline before reloading) — without setOffline(), the page would never actually go offline and the offline banner assertion would fail`,`setOffline(true) here has no effect since offline mode can only be configured at context creation time`,`The .catch(() => {}) call means the test always passes regardless of the actual page behavior`,`context.setOffline(true) also automatically clears all cookies as a side effect`],0,`Testing a realistic online-to-offline transition requires the app to have already loaded successfully before toggling offline — setOffline() mid-test is exactly the tool for that sequence.`],[`O`,`I`,`What does this code demonstrate about downloads in Playwright?`,`const [download] = await Promise.all([
  page.waitForEvent('download'),
  page.getByRole('link', { name: 'Download invoice' }).click(),
]);
const path = await download.path();
expect(download.suggestedFilename()).toBe('invoice-2026.pdf');`,[`It reliably captures the Download object triggered by clicking the link, then inspects both the downloaded file's local temporary path and its suggested filename — using the same Promise.all pairing pattern as popup handling to avoid a race condition between the click and the event`,`download.path() returns the URL the file was downloaded from, not a local filesystem path`,`This code requires acceptDownloads: false to be set, or the download event never fires`,`suggestedFilename() only works for downloads triggered by a form submission, never a plain link click`],0,`The click-and-wait-for-event pairing pattern used for popups applies equally to downloads — both are asynchronous browser events that need to be captured reliably alongside the triggering action.`],[`O`,`A`,`What is the effect of this exposeFunction usage in a test?`,`await context.exposeFunction('generateSignedToken', (userId: string) => {
  return require('./tokenUtil').sign(userId);
});

await page.goto('/app');
const token = await page.evaluate(async (id) => {
  return await (window as any).generateSignedToken(id);
}, 'user-42');`,["It makes a Node.js function (that signs a token using server-side logic) callable from within the browser's JavaScript context as `window.generateSignedToken`, letting the page under test invoke real Node-side logic during execution — useful for mocking signing/auth logic that would otherwise require a real backend",`exposeFunction() only works with functions that take no arguments`,`The function only runs once per context regardless of how many times it is called from the page`,`page.evaluate() cannot call functions exposed via exposeFunction(); they are entirely separate mechanisms`],0,`exposeFunction() is the bridge from browser-side JS to Node.js — it lets in-page code call back into arbitrary server-side logic, which is the reverse direction of page.evaluate() running code inside the browser.`],[`O`,`I`,"What does `context.pages()` return in this scenario?",`const context = await browser.newContext();
const page1 = await context.newPage();
const page2 = await context.newPage();
await page1.close();

console.log(context.pages().length);`,[`1 — context.pages() reflects only the currently open pages at the moment it is called, so after page1 is closed, only page2 remains in the list`,`2 — pages() always reflects the total number of pages ever created in the context, including closed ones`,`0 — closing any page in a context automatically closes all other pages too`,`This throws an error because pages() cannot be called after any page in the context has been closed`],0,`context.pages() is a live, synchronous snapshot of currently open pages — closed pages are removed from that list immediately.`],[`O`,`A`,`What does this configuration achieve when running the same spec file across three projects?`,`export default defineConfig({
  projects: [
    { name: 'chromium-bundled', use: { ...devices['Desktop Chrome'] } },
    { name: 'chrome-branded', use: { ...devices['Desktop Chrome'], channel: 'chrome' } },
    { name: 'edge-branded', use: { ...devices['Desktop Edge'], channel: 'msedge' } },
  ],
});`,[`It runs the suite three times: once against Playwright's bundled Chromium build, once against the real installed Google Chrome, and once against the real installed Microsoft Edge — surfacing any differences between the open-source engine and the actual branded browsers users run`,`channel and devices presets are mutually exclusive and this configuration would throw an error`,`All three projects run against the exact same browser binary since they share the same underlying Chromium engine with no observable differences`,`Only the chromium-bundled project actually runs; the channel-based ones are ignored without a separate license`],0,`The channel option specifically targets the real, installed branded browser rather than Playwright's own bundled Chromium build, letting you catch any real-world divergence between them.`],[`O`,`I`,`What is the effect of this context creation on the reported viewport and device characteristics?`,`const context = await browser.newContext({
  ...devices['iPad (gen 7)'],
});
const page = await context.newPage();
const info = await page.evaluate(() => ({
  width: window.innerWidth,
  touch: 'ontouchstart' in window,
}));`,[`The page reports the iPad (gen 7) preset's viewport width and indicates touch support, since the devices preset bundles viewport, userAgent, deviceScaleFactor, isMobile, and hasTouch all together for accurate device emulation`,`devices presets only affect the User-Agent header, so window.innerWidth would show the default desktop width regardless`,`'ontouchstart' in window is always false for any Playwright-controlled browser, mobile emulation or not`,`This code throws an error because devices presets cannot be spread directly into newContext()`],0,`device presets bundle multiple related settings together, which is why both the viewport width and the touch-capability check reflect the emulated device accurately.`],[`O`,`A`,`What does this route handler with a regex pattern accomplish, and how does it differ from a glob pattern?`,`await context.route(/\\/api\\/v[12]\\/users\\/\\d+$/, route => {
  route.fulfill({ status: 200, body: JSON.stringify({ id: 1, name: 'Test User' }) });
});`,["It matches API requests to either /api/v1/users/{numeric-id} or /api/v2/users/{numeric-id} using a regular expression, giving more precise pattern control (like requiring a purely numeric trailing segment) than a simple glob pattern like `**/users/*` could easily express",`context.route() only accepts glob patterns; passing a regex here silently matches nothing at all`,`The regex is evaluated against the request body, not the URL, making this an invalid mock for a GET request`,`This pattern matches every request in the entire context regardless of the actual path`],0,`context.route() accepts either a glob string or a genuine regular expression, and regex is useful whenever the matching logic (like requiring digits only) is more precise than glob syntax easily allows.`],[`O`,`I`,`What is the purpose of this Docker-oriented launch configuration?`,`const browser = await chromium.launch({
  args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage'],
});`,[`These are common Chromium flags used to work around container-specific issues: disabling the OS sandbox (often required when running as root in certain container setups) and avoiding heavy reliance on a container's limited /dev/shm space`,`These flags disable all of Chromium's security features permanently, including for the pages under test`,`This launch configuration only works when combined with launchPersistentContext(), not a regular launch()`,`--no-sandbox is required for headless mode to function at all, even outside Docker`],0,`These specific flags are widely recognized Docker/CI workarounds for sandboxing restrictions and shared-memory limitations that commonly cause Chromium crashes in containers.`],[`O`,`A`,"What does this code snippet reveal about the relationship between `page.emulateMedia()` and the `colorScheme` context option?",`const context = await browser.newContext({ colorScheme: 'light' });
const page = await context.newPage();
await page.goto('/');
await page.emulateMedia({ colorScheme: 'dark' });
await page.reload();`,[`page.emulateMedia() can override the context-level colorScheme setting for that specific page at runtime, letting you switch a page's effective theme mid-test without needing to create an entirely new context`,`emulateMedia() only affects print media queries and has no effect on prefers-color-scheme`,`The context-level colorScheme always takes precedence, making this emulateMedia() call have no effect`,`This throws an error because colorScheme can only be set once per context and never changed afterward`],0,`emulateMedia() is a page-level, runtime override that can adjust media-query-driven behavior (color scheme, reduced motion, forced colors) after the context has already been created.`],[`O`,`I`,`What does this configuration achieve for a test suite hitting a corporate proxy-restricted environment?`,`const context = await browser.newContext({
  proxy: {
    server: 'http://corp-proxy.internal:8080',
    username: 'svc-test',
    password: process.env.PROXY_PASSWORD,
  },
});`,[`It routes every network request made by pages in this context through the specified authenticated corporate proxy, which is necessary in environments where direct internet/staging access is blocked without going through that proxy`,`It only proxies WebSocket connections, leaving regular HTTP/HTTPS requests unaffected`,`Setting a proxy at the context level requires also setting one identically in playwright.config.ts or the two will conflict`,`The username and password fields configure basic auth on the target site, not the proxy itself`],0,`Context-level proxy configuration (including authenticated proxies) routes all of that context's traffic through the specified server — a common requirement in locked-down corporate network environments.`],[`O`,`A`,"What does this code test, and why might `context.serviceWorkers()` be checked here?",`const context = await browser.newContext({ serviceWorkers: 'block' });
const page = await context.newPage();
await page.goto('/app');
console.log(context.serviceWorkers().length); // expect 0`,[`It verifies the app still functions correctly with service workers entirely blocked from registering, useful for testing without a caching layer interfering with fresh network responses — logging the length confirms no service worker actually registered despite the app attempting to`,`serviceWorkers: 'block' only blocks NEW service worker registrations while leaving already-active ones running, so the length would not necessarily be 0`,`context.serviceWorkers() always returns an empty array regardless of the serviceWorkers option`,`This configuration is invalid; serviceWorkers can only be set to true or false, not a string`],0,`serviceWorkers: 'block' is specifically meant to eliminate the unpredictability a caching service worker can introduce into a test — verifying zero registered workers confirms the block took effect.`],[`O`,`I`,"What is the outcome of this code, given that `browser.close()` terminates the entire browser process?",`const browser = await chromium.launch();
const context1 = await browser.newContext();
const context2 = await browser.newContext();

await browser.close();

console.log(browser.isConnected()); // ?`,[`false — after browser.close() completes, the Browser object is disconnected from the underlying process, and both context1 and context2 (and any pages within them) are also forcibly closed as a result`,`true — browser.close() only closes the contexts, leaving the browser process itself running and connected`,`This throws an error because isConnected() cannot be called after close()`,`true, but only if at least one context was still open before close() was called`],0,`browser.close() cascades down through every context and page it owns — isConnected() afterward correctly reflects that the whole hierarchy has been torn down.`],[`R`,`A`,"Your team needs to verify that a Chrome extension your company ships correctly injects a banner into third-party pages. Standard `browser.newContext()` cannot load unpacked extensions. What is the correct Playwright approach?",null,["Use `chromium.launchPersistentContext()` with `--disable-extensions-except` and `--load-extension` Chromium args pointing at the unpacked extension directory, since extensions require a real, disk-backed browser profile to load",`Manually inject the extension's content script via context.addInitScript(), simulating what the extension would do`,`Extensions cannot be tested with Playwright under any circumstances; a separate tool is required`,`Use browser.newContext({ extensions: ['./my-extension'] }), a dedicated option for this exact purpose`],0,`launchPersistentContext() with the appropriate Chromium extension-loading flags is the documented, supported way to test real browser extensions with Playwright.`],[`R`,`I`,`Your organization already runs a centralized, containerized pool of browser instances for other automation tools, and wants Playwright tests to reuse that same infrastructure rather than launching local browsers per CI job. What is the right connection approach?`,null,["Use `browserType.connect()` (for a Playwright-native remote server) or `browserType.connectOverCDP()` (for a generic CDP-exposed browser) to attach Playwright test runs to the existing remote browser pool's WebSocket endpoint",`This is not possible; Playwright can only ever drive browsers it launches itself locally`,`Install Playwright browsers directly inside the existing browser pool's containers and run tests from there via SSH`,`Use browser.newContext({ remote: true }) to automatically discover and connect to any nearby browser pool`],0,`Playwright is explicitly designed to support remote browser connections via connect()/connectOverCDP(), making it compatible with existing centralized browser infrastructure.`],[`R`,`I`,`A test suite frequently needs to mock a slow, unreliable third-party payment gateway API so tests are fast and deterministic, while letting all other network traffic (the app's own API) pass through normally. What is the right approach?`,null,["Register a `context.route()` handler scoped to the payment gateway's URL pattern that calls `route.fulfill()` with a canned response, while leaving all other requests unhandled (or explicitly calling route.continue()) so they hit the real network normally",`Set context.route('**/*', route => route.abort()) to block everything, then manually re-enable only the app's API afterward`,`Use context.setOffline(true) during the payment step, then setOffline(false) immediately after`,`Rewrite the entire application to remove its dependency on the third-party payment gateway during tests`],0,`Scoping a route handler's URL pattern precisely to just the third-party dependency is the standard way to mock only what needs mocking while leaving everything else realistic.`],[`R`,`A`,`Your app needs to be verified against real Google Chrome (not just Playwright's bundled Chromium) because a recent bug only reproduced in an actual Chrome release, related to a specific video codec. How do you set this up as an ongoing CI check?`,null,["Add a project in playwright.config.ts with `channel: 'chrome'` and ensure CI runs `npx playwright install chrome` before tests, so this project specifically exercises the real branded Chrome build rather than bundled Chromium",`Install Google Chrome manually on the CI image and set browserName: 'chrome' in the project — browserName only ever accepts chromium/firefox/webkit`,`This cannot be automated; the video codec bug must be verified manually by a human on every release`,`Use devices['Desktop Chrome'] alone, which already targets the real branded Chrome build by default`],0,`channel: 'chrome' (combined with installing that channel) is specifically for testing against the real, installed Chrome build rather than the bundled open-source Chromium engine.`],[`R`,`I`,`You need to test that your app correctly displays a "you are back online" banner after a network interruption during an active session. How do you construct this scenario in a Playwright test?`,null,["Let the page load normally online, call `await context.setOffline(true)` and interact with the app to trigger the interruption-aware behavior, then call `await context.setOffline(false)` and assert the reconnection banner appears",`Set offline: true in browser.newContext() options from the start, then immediately navigate and expect the banner on load`,`Use context.route('**/*', route => route.abort()) permanently for the whole test, since that simulates being offline`,`This scenario cannot be tested without physically disconnecting the test machine's network adapter`],0,`setOffline() is specifically designed to be toggled mid-test, letting you simulate a realistic online → offline → online transition within a single running session.`],[`R`,`A`,`Your app is a PWA and you need to verify it still shows meaningful cached content when the service worker intercepts a request while offline — but you also have OTHER, unrelated tests where the service worker's caching behavior just introduces unpredictable flakiness. How should you configure contexts differently across these two kinds of tests?`,null,["For the PWA offline-caching tests, use the default context (service workers enabled) combined with context.setOffline(); for the unrelated tests where caching just adds noise, create those contexts with `serviceWorkers: 'block'` to eliminate that variable entirely",`Always use serviceWorkers: 'block' everywhere, since caching behavior is never relevant to any test`,`Service workers cannot be selectively enabled or disabled per context; it is an all-or-nothing global setting`,`Use context.clearCookies() before each test to reset service worker state, since that is the only available control`],0,`serviceWorkers is a per-context option specifically so different tests within the same suite can choose whether caching behavior should be part of what they exercise or excluded as noise.`],[`R`,`I`,`You need to verify your app degrades gracefully (shows a clear message, not a blank broken page) for the rare users browsing with JavaScript disabled. How do you set up this specific test?`,null,["Create a context with `javaScriptEnabled: false`, navigate to the page, and assert that the expected fallback content (e.g. a `<noscript>` message) is visible instead of a broken or blank page",`Use page.evaluate(() => { window.stop() }) to simulate disabled JavaScript after the page has already loaded and executed scripts`,`This scenario is impossible to test since Playwright always requires JavaScript to drive the browser`,`Set headless: true, since headless mode automatically behaves as if JavaScript were disabled`],0,`javaScriptEnabled: false is the direct, supported way to test how a page behaves for users or crawlers that genuinely cannot execute JavaScript.`],[`R`,`A`,`Your CI infrastructure limits pipeline runtime, and browser tests are the slowest part because each of 500 test files launches its own fresh browser process. What architectural change (without reducing test coverage) would meaningfully speed this up?`,null,[`Ensure the test runner reuses one Browser instance per worker (the default Playwright fixture behavior) and creates a fresh BrowserContext per test rather than a fresh Browser — if custom code is bypassing this by manually calling launch() per test, switching to the standard context/page fixtures avoids repeated process-launch overhead`,`Switch every test to javaScriptEnabled: false, since disabling JS speeds up page loads significantly`,`Reduce the number of test files by merging all 500 into a single massive file, since fewer files means fewer processes`,`Set headless: false everywhere, since headed mode launches faster than headless mode`],0,`Launching a browser process is the expensive part; contexts are cheap by comparison — code that manually launches a new Browser per test (instead of relying on the standard fixture reuse) is a common, fixable source of unnecessary slowness.`],[`R`,`I`,`A test needs to confirm that a specific analytics beacon request is sent with the correct payload when a user completes checkout, without needing to mock or change the actual request/response. What is the appropriate Playwright approach?`,null,["Use `page.waitForRequest()` (or inspect `context.on('request', ...)`) to observe and assert on the real outgoing request's URL/payload, using route.continue() (or no route interception at all) so the real network call still proceeds normally",`Use context.route() with route.fulfill() to fake a response, then assert on the faked data you created yourself`,`Use context.route() with route.abort() and assert the checkout still completes despite the blocked analytics request`,`This can only be verified by inspecting server-side logs after the fact, since Playwright cannot observe outgoing browser requests`],0,`When you want to observe real traffic rather than mock it, request/response event listeners (or route.continue() without altering the request) let you assert on genuine outgoing calls.`],[`R`,`A`,`Your team wants to verify a specific low-level performance metric (like exact paint timing) that Playwright's high-level API does not directly expose as an assertion target. What is the appropriate escape hatch?`,null,["Use `context.newCDPSession(page)` to access the raw Chrome DevTools Protocol directly and query the specific performance/tracing domain that exposes the metric you need",`This is not possible with Playwright under any circumstances; a separate profiling tool must be used instead`,`Use page.evaluate() to run performance.now() in a loop and average the results as an approximation`,`Set trace: 'on' in config and manually eyeball the desired metric from the visual trace timeline`],0,`CDP sessions are the documented escape hatch for exactly this situation — low-level protocol access when the high-level Playwright API does not directly expose what you need (Chromium only).`],[`S`,`A`,"A test loading a Chrome extension via `launchPersistentContext()` passes locally but fails in CI with the extension seemingly not loaded at all, even though the same Chromium args are used. What should be checked first?",null,["Whether CI is running in headless mode — older Chromium versions historically had limited or no extension support in headless mode, so the persistent context may need `headless: false` (or a newer Chromium version supporting the new headless extension support) to actually load the extension in CI",`Extensions are fundamentally incompatible with any CI environment and can only be tested on a local developer machine`,`The extension directory path must always be an absolute path on CI, even though a relative path works locally, with no other differences possible`,`launchPersistentContext() silently ignores the --load-extension flag whenever process.env.CI is set`],0,`Headless extension support has historically been limited or version-dependent in Chromium — this is one of the first things to check when extension-loading behaves differently between local (often headed by default during dev) and CI (often headless) runs.`],[`S`,`I`,"A route handler mocking `**/api/orders` works fine for the first test in a file but silently stops intercepting requests in later tests within the same file. What is the most likely cause?",null,[`The route was registered on a manually-created, long-lived context or page that got replaced/recreated between tests (or registered on the page instead of the context, and a later test uses a different page) — since each test normally gets a brand-new context/page fixture, a route registered on one specific instance does not automatically carry over`,`Route handlers automatically expire after being triggered exactly once, regardless of how they were registered`,`This is expected: route handlers can only ever be used in the very first test of a file`,`context.route() only works when called inside a beforeAll hook, never inside a test or beforeEach`],0,`Because the built-in page/context fixtures are fresh per test, any route registered against a specific test's context/page does not persist into the next test's freshly created context — route registration typically needs to happen per-test (e.g. in a beforeEach) if every test needs it.`],[`S`,`A`,`A test verifying "download completes successfully" passes locally but times out waiting for the download event in CI. The CI Docker container has no special download-blocking configuration. What should be investigated?`,null,["Whether the CI environment's context is missing `acceptDownloads` (if explicitly set to false somewhere) or whether the download is being blocked by a route handler / proxy / CSP difference between environments — also check whether the download's target directory has correct write permissions inside the container",`Downloads are fundamentally impossible to test inside any Docker container, regardless of configuration`,`The fix is always to increase the test timeout to at least 5 minutes, since downloads are inherently slow in CI`,`waitForEvent('download') only works in headed mode, so headless CI can never capture this event`],0,`Download-related CI-only failures are usually configuration differences (route interception, permissions, or an explicit acceptDownloads override) rather than a fundamental CI limitation — Playwright fully supports downloads headlessly.`],[`S`,`I`,"A test using `context.grantPermissions(['camera', 'microphone'])` works on Chromium but the equivalent WebKit project reports the permissions were never actually granted when the page checks `navigator.permissions.query()`. What is the likely explanation?",null,[`Permission APIs and their level of support can differ meaningfully across browser engines — WebKit's implementation of certain permission types (like camera/microphone via navigator.permissions.query) may behave differently or have gaps compared to Chromium, so cross-engine testing of granular permissions sometimes needs engine-specific handling or expectations`,`grantPermissions() is a Chromium-only API that silently does nothing on any other browser, by design, and always throws in that case`,`This indicates a Playwright bug that should always be reported rather than investigated as an engine difference`,`WebKit requires permissions to be granted twice — once via grantPermissions() and once via a manual page.evaluate() call`],0,`Cross-engine permission handling is a known area of real behavioral divergence — differences here are often genuine browser engine differences rather than Playwright bugs, worth verifying against each engine's actual capabilities.`],[`S`,`A`,'A team\'s tests intermittently fail with "Target page, context or browser has been closed" specifically when running with a `browserType.connect()` connection to a shared remote browser server used by multiple CI jobs simultaneously. What is the most likely explanation?',null,[`Another concurrent CI job (or a cleanup process) may have closed a context or the browser itself on the shared remote server while this job's test was still using it — shared remote browser infrastructure needs careful context isolation and lifecycle coordination between concurrent consumers to avoid one job disrupting another`,`browserType.connect() is fundamentally incompatible with running more than one test at a time, under any configuration`,`This error only ever indicates a bug in the test's own code, unrelated to the shared remote browser`,`Remote browser connections automatically time out and close after exactly 30 seconds regardless of activity`],0,`Shared remote browser infrastructure introduces a new failure mode not present with locally-launched browsers — cross-job interference is a real risk if context lifecycle is not carefully isolated per consumer.`],[`S`,`I`,"A visual regression test using `colorScheme: 'dark'` passes for most components but one specific widget still renders in light-mode colors. Investigation shows the widget reads its theme from `localStorage.getItem('user-theme')` rather than the OS-level `prefers-color-scheme` media query. What should the test do differently?",null,[`In addition to setting colorScheme: 'dark' on the context, use context.addInitScript() (or seed storageState) to also set the app's own localStorage theme key to 'dark', since the widget clearly reads a stored user preference rather than relying purely on the OS media query`,`This indicates a Playwright limitation that cannot be worked around; colorScheme should always be sufficient on its own`,`Switch to forcedColors: 'active' instead of colorScheme, since forcedColors overrides any localStorage-based theming`,`The widget's behavior is a bug that must be fixed in application code before the test can ever pass, with no test-side workaround possible`],0,`colorScheme only emulates the OS-level media query — an app that additionally (or instead) reads its own stored preference needs that preference seeded directly, e.g. via addInitScript or storageState.`],[`D`,`I`,"A test calling `context.route('**/api/**', handler)` never seems to intercept any request, even though the app clearly makes fetch calls to `/api/users` after the page loads. The route is registered immediately after `context.newPage()` but before `page.goto()`. What is a likely mistake to check?",null,[`Whether the glob pattern actually matches the real request URLs (e.g. the app might call a fully-qualified absolute URL like https://api.example.com/users that does not match a pattern written assuming a relative path) — route matching is based on the actual request URL string, and a pattern mismatch is the most common reason a route silently never fires`,`context.route() must always be registered on page, never on context, or it never fires`,`Routes registered before goto() are discarded once navigation occurs and must be re-registered after`,`route handlers can only intercept GET requests, never POST/fetch calls`],0,`A URL pattern mismatch (relative vs absolute, missing wildcard segments, wrong domain) is the most frequent cause of a route handler that silently never triggers — double-check the actual request URLs against the pattern.`],[`D`,`A`,`A test using \`context.exposeFunction('getSignedUrl', ...)\` throws "getSignedUrl: window.getSignedUrl already registered" when running the full suite, but passes when run in isolation. What is the likely cause?`,null,[`The exposed function is being registered on a shared or reused context/page across multiple tests (perhaps via a worker-scoped fixture or a manually shared context) instead of a fresh per-test context — exposeFunction() throws if called twice for the same name on the same page/context without an intervening navigation or new context`,`exposeFunction() can only ever be called once per entire test run, globally, across every test file`,`This error indicates a Playwright bug requiring a version downgrade`,`exposeFunction() names must be globally unique across the whole application's codebase, not just within a test`],0,`Registering the same exposed function name twice on the same underlying context/page (often due to accidental context reuse across tests) triggers this exact "already registered" error — check for unintended context sharing.`],[`D`,`I`,"A dialog-handling test hangs indefinitely and eventually times out, even though `page.on('dialog', handler)` is registered before the action that should trigger a `confirm()` popup. What is a common mistake to check?",null,[`The dialog listener may have been registered AFTER the triggering click already fired (a timing/ordering bug), or the handler itself never calls dialog.accept()/dismiss() (e.g. it throws before reaching that call), leaving the dialog open and blocking all further page execution indefinitely`,`page.on('dialog', ...) only works for alert(), never for confirm() or prompt()`,`Dialog handlers are ignored unless PWDEBUG=1 is set in the environment`,`Dialogs automatically resolve themselves after 5 seconds regardless of whether a handler is registered`],0,`A dialog blocks page execution until explicitly accepted or dismissed — if the handler is registered too late or fails to call accept()/dismiss(), the whole test hangs waiting on a dialog nothing is resolving.`],[`D`,`A`,"A suite using `browser.newContext({ storageState: 'auth.json' })` intermittently fails with 401 errors specifically in a long-running nightly CI job, but passes reliably in fast PR-check runs using the identical storageState file. What should be investigated?",null,[`Whether the session/token captured in auth.json has a short expiration time that outlives fast PR runs but not the longer nightly run — a storageState snapshot is only valid as long as the underlying session it captured remains valid, and needs to be regenerated (or use a longer-lived credential) if the run duration can exceed the token's lifetime`,`storageState files are only valid for exactly one hour after being created, as a hardcoded Playwright limit`,`This indicates file corruption in auth.json and the file should be deleted and never regenerated`,`401 errors when using storageState are always caused by an incorrect file path, unrelated to session validity`],0,`storageState captures a point-in-time session snapshot — if the underlying token/session expires partway through a longer-running suite, requests relying on it will start failing even though the file itself is perfectly valid.`],[`D`,`I`,"A test creates a context with `recordVideo: { dir: 'videos/' }` but the resulting video file is 0 bytes or missing entirely after the test finishes. What is the most likely cause?",null,[`The context was never explicitly closed (or the process exited abruptly) before the video could be finalized — video files, like HAR files, are only properly written and finalized when context.close() completes, so a context left open (or a crashed process) results in a missing or corrupt video`,`recordVideo requires headless: false to actually capture any frames`,`Video recording is fundamentally incompatible with WebKit and always produces empty files there`,`The dir path must be an absolute path or video recording silently no-ops`],0,`Similar to HAR recording, video finalization happens on context.close() — an unclosed or manually-torn-down context can leave the video file incomplete or empty.`],[`L`,`I`,"Why does `context.grantPermissions()` accept an optional second argument specifying an `origin`, rather than always granting permissions context-wide?",null,[`Some permissions are meaningfully scoped per-origin in real browsers (a site should only get geolocation access for itself, not for every other origin a page might later navigate to) — specifying origin lets you grant a permission precisely to the domain that needs it, rather than blanket-granting it for the whole context`,`The origin argument is purely for logging purposes and does not affect actual permission scoping`,`Permissions granted without an origin are automatically revoked after the first navigation`,`grantPermissions() requires an origin argument in every case; it is never optional`],0,`Origin-scoped permission granting mirrors how real browsers scope permissions per site, letting tests be precise about exactly which domain should have access.`],[`L`,`A`,`Why might cross-browser test failures be more common around focus and keyboard-navigation behavior specifically, compared to simple click interactions?`,null,[`Focus management, tab order, and certain keyboard event semantics have historically had more subtle implementation differences across Chromium, Firefox, and WebKit than basic pointer/click handling, which is a fairly well-standardized and consistently implemented browser behavior`,`Playwright does not support keyboard events at all in Firefox or WebKit`,`Click interactions are entirely synchronous while keyboard interactions are always asynchronous, causing the difference`,`Focus-related tests always require headed mode, which behaves differently across engines`],0,`This is a genuine, well-known source of cross-browser flakiness — focus/tab-order edge cases are one of the areas where browser engines have historically diverged more than basic click/pointer handling.`],[`L`,`I`,`Why does Playwright's WebKit build specifically matter for teams targeting Safari users, given that Playwright cannot run real Apple Safari on non-Apple operating systems?`,null,[`Playwright's WebKit is the same underlying rendering/JS engine family that powers Safari, so testing against it on Linux/Windows CI gives a reasonably close approximation of Safari behavior without needing actual macOS hardware, though it is not byte-identical to Apple's exact Safari build`,`WebKit and Safari share no meaningful code and testing against WebKit provides no useful signal about Safari behavior`,`Playwright secretly runs real Safari inside a virtual machine when the webkit browserName is selected on any OS`,`WebKit can only be tested when the CI runner is physically an Apple-manufactured machine`],0,`WebKit as an engine family underlies Safari, making it the closest practical approximation available for CI environments that cannot run genuine Safari, while acknowledging it is not a perfect substitute.`],[`L`,`I`,'Why does Playwright provide a distinct `browserType` object (chromium, firefox, webkit) as the entry point, rather than a single unified "browser" launcher that picks an engine via a string parameter alone?',null,[`Each engine has genuinely different launch options, capabilities, and quirks, so exposing chromium/firefox/webkit as first-class, strongly-typed objects (each with its own launch()/connect()/launchPersistentContext() methods) gives clearer, more accurate API surface and autocompletion than a single generic launcher juggling all three`,`It is purely a historical/legacy naming choice with no functional benefit today`,`Only chromium actually supports the full browserType API; firefox and webkit are simplified stubs`,`A unified launcher was removed in a past version due to a security vulnerability`],0,`Distinct browserType objects let the API surface (and its TypeScript types) accurately reflect any engine-specific options or behaviors, rather than forcing a lowest-common-denominator generic interface.`],[`L`,`A`,"Why is it generally considered safer to grant permissions via `context.grantPermissions()` at test setup time rather than relying on the application to somehow bypass its own permission-request UI during tests?",null,[`Bypassing or mocking the app's own permission-request flow would mean the test is no longer exercising real app code, and native browser permission prompts cannot be interacted with like normal DOM elements anyway — grantPermissions() operates at the browser level, pre-authorizing access without needing the app to behave differently under test`,`grantPermissions() actually modifies the application's source code temporarily during the test run`,`There is no meaningful difference; both approaches produce identical test coverage`,`Native permission prompts can be clicked directly using page.getByRole('button') like any other UI element`],0,`Native browser permission dialogs live outside the page DOM and cannot be automated with normal locators — grantPermissions() is the correct, browser-level mechanism to pre-authorize access without touching app code.`],[`L`,`I`,"Why might `page.route()` be preferred over `context.route()` when a test needs different mocking behavior for two separate tabs opened within the same context?",null,[`page.route() scopes the interception to just that one specific Page, letting two tabs in the same context have entirely different mocked responses for the same URL pattern, whereas context.route() would apply the same handler uniformly to every page in the context`,`page.route() and context.route() are functionally identical, and the choice is purely stylistic`,`page.route() can only intercept requests made during the initial navigation, not subsequent ones`,`context.route() automatically overrides any page.route() handler in the same context`],0,`Choosing between page-level and context-level routing is about scope — page.route() is the right tool when different tabs within one context genuinely need different mocked behavior.`],[`L`,`A`,"Why does emulating geolocation via context options represent a more reliable testing strategy than trying to mock `navigator.geolocation.getCurrentPosition` directly via `page.addInitScript()`?",null,[`The built-in geolocation context option is a first-class Playwright feature that integrates properly with permission grants and works consistently across pages/navigations in the context, whereas a hand-rolled addInitScript() mock has to correctly reimplement the full Geolocation API surface (including watchPosition, error callbacks, and permission semantics) to be equally reliable`,`addInitScript() cannot execute any code related to navigator objects, making the mock approach technically impossible`,`The context option and an addInitScript() mock are functionally identical, and using one over the other is purely a style preference`,`Geolocation context options only work in Chromium, so addInitScript() mocking is required for Firefox and WebKit`],0,`The dedicated geolocation option is purpose-built and battle-tested for this scenario — a hand-rolled mock via addInitScript() has to reimplement more of the API surface correctly to be equally trustworthy.`],[`L`,`I`,"Why would a team specifically want to assert on `request.postDataJSON()` inside a `page.on('request', ...)` listener rather than only checking that a request to a given URL happened at all?",null,[`Confirming a request merely occurred does not verify its actual payload was correct — inspecting postDataJSON() lets a test assert the exact data sent (e.g. the right cart contents in a checkout POST request), catching bugs where the request fires but with incorrect or incomplete data`,`postDataJSON() is required before route.continue() can be called on the same request`,`Only GET requests expose accessible request data; POST body inspection is not supported by Playwright`,`request listeners fire before the actual network call is made, so postDataJSON() always returns undefined`],0,`The presence of a request alone is a weak assertion — verifying its actual payload content closes the gap between "something was sent" and "the correct thing was sent".`],[`L`,`A`,"Why might Playwright tests intentionally avoid asserting on exact CDP-level metrics (like precise paint timing captured via `context.newCDPSession()`) as a routine part of functional test suites?",null,[`Low-level performance metrics from CDP tend to be more variable machine-to-machine and run-to-run than functional DOM state, making them prone to flaky, environment-sensitive assertions if used as hard pass/fail gates in a typical CI functional suite — they are usually better suited to dedicated performance-monitoring pipelines with statistical tolerance`,`CDP sessions are only available in a paid Playwright tier, making this a licensing rather than a design consideration`,`CDP metrics cannot be captured at all once a page has navigated more than once`,`Functional test suites are technically incapable of calling context.newCDPSession()`],0,`Performance metrics are inherently noisier than deterministic DOM state — mixing them into a functional pass/fail suite as hard assertions is a common source of unrelated flakiness.`],[`L`,`I`,`Why does Playwright's isolation model (fresh context per test) make it comparatively straightforward to run large test suites safely in parallel across many workers, compared to a testing approach that reused a single shared browser session for the whole suite?`,null,[`Because each test gets its own uncontaminated cookies/storage/auth state via a fresh context, tests can run concurrently across workers without one test's login, cart contents, or cached data leaking into another's — a shared single session would require careful manual cleanup between every test to achieve the same safety`,`Parallelism has nothing to do with context isolation; it is purely a function of how many CPU cores are available`,`A shared browser session approach would actually be safer for parallelism since there is only one thing to manage`,`Context isolation only matters for sequential test runs, not parallel ones`],0,`Per-test context isolation is precisely what makes safe, correctness-preserving parallelism practical — without it, careful manual state cleanup between every test would be required to avoid cross-test contamination.`],[`O`,`I`,`What does this code verify about outgoing request data, without mocking anything?`,`const [request] = await Promise.all([
  page.waitForRequest(req => req.url().includes('/api/checkout') && req.method() === 'POST'),
  page.getByRole('button', { name: 'Place order' }).click(),
]);
const payload = request.postDataJSON();
expect(payload.items).toHaveLength(2);`,[`It captures the real outgoing checkout POST request (without intercepting or altering it) and asserts on its actual JSON payload — verifying the exact data sent to the server rather than just that some request occurred`,`waitForRequest() blocks the actual request from being sent until the assertion completes`,`postDataJSON() only works on GET requests, so this code would throw for a POST request`,`This code mocks the checkout endpoint's response before the assertion runs`],0,`waitForRequest() with a predicate lets you observe genuine outgoing traffic and inspect its real payload, distinct from route()-based interception which can also modify or fake the request/response.`],[`O`,`A`,`What is the effect of granting a permission with a specific origin argument here?`,`const context = await browser.newContext();
await context.grantPermissions(['geolocation'], { origin: 'https://maps.example.com' });

const page = await context.newPage();
await page.goto('https://maps.example.com/directions');`,[`Geolocation access is granted specifically for the https://maps.example.com origin — if the same context later navigates to a different origin, that other site would not automatically inherit this geolocation grant`,`The origin argument is ignored, and geolocation is granted context-wide regardless of what is specified`,`This throws an error because grantPermissions() does not accept an options object as a second argument`,`Origin-scoped grants only work for the "notifications" permission, not geolocation`],0,`Scoping the grant to a specific origin mirrors how real browsers scope permissions per site, avoiding an overly broad grant across every origin the context might visit.`],[`O`,`I`,"What does `page.route()` scoped like this achieve compared to using `context.route()`?",`const page1 = await context.newPage();
const page2 = await context.newPage();

await page1.route('**/api/config', route => route.fulfill({ body: '{"theme":"dark"}' }));
await page2.route('**/api/config', route => route.fulfill({ body: '{"theme":"light"}' }));

await page1.goto('/settings');
await page2.goto('/settings');`,[`Each tab gets its own independently mocked response for the identical URL pattern — page1 sees dark theme config, page2 sees light theme config — something context.route() alone could not achieve since it would apply one shared handler to both pages`,`Both pages end up using whichever route was registered last, since page-level routes are actually stored at the context level internally`,`This throws an error because two pages in the same context cannot have different route handlers for the same pattern`,`page.route() only works for the first navigation and is ignored on subsequent goto() calls`],0,`page-scoped routing is exactly the right tool when different tabs sharing one context need genuinely different mocked behavior for the same endpoint.`],[`O`,`A`,"What does this geolocation mocking approach demonstrate as an alternative to the built-in `geolocation` context option?",`await context.grantPermissions(['geolocation']);
await context.addInitScript(() => {
  const mockPosition = { coords: { latitude: 48.8566, longitude: 2.3522 }, timestamp: Date.now() };
  navigator.geolocation.getCurrentPosition = (success) => success(mockPosition);
});`,[`It manually overrides the browser's getCurrentPosition method via an injected init script as a hand-rolled alternative to the built-in geolocation context option — functional, but it only covers getCurrentPosition and not watchPosition or error-handling paths that the built-in option handles uniformly`,`This code is functionally identical in every respect to using the geolocation context option, with zero tradeoffs`,`addInitScript() cannot override built-in browser APIs like navigator.geolocation under any circumstances`,`grantPermissions() is unnecessary here since addInitScript() bypasses the permission system entirely`],0,`This illustrates why the dedicated geolocation option is generally preferred — a manual mock like this covers less of the real API surface (e.g. watchPosition) than the built-in, more complete implementation.`],[`O`,`I`,`What does this test demonstrate about page-level versus context-level emulateMedia calls?`,`const context = await browser.newContext({ colorScheme: 'dark' });
const page1 = await context.newPage();
const page2 = await context.newPage();

await page1.emulateMedia({ colorScheme: 'light' });

// page1 now renders light, page2 still renders dark`,[`page.emulateMedia() overrides the color scheme for that specific page only, while sibling pages in the same context continue using the context-level default — media emulation can be tuned per-page even within a shared context`,`emulateMedia() always applies to every page sharing the same context, making the described per-page difference impossible`,`page1 and page2 must always match because they share the same underlying BrowserContext`,`colorScheme set at the context level cannot be overridden by emulateMedia() under any circumstances`],0,`emulateMedia() operates at the page level, letting individual tabs diverge from the context-wide default when a test specifically needs that granularity.`],[`O`,`A`,`What does this code check, and what real-world scenario does it validate?`,`const cdpSession = await context.newCDPSession(page);
await cdpSession.send('Network.emulateNetworkConditions', {
  offline: false,
  downloadThroughput: (500 * 1024) / 8,
  uploadThroughput: (500 * 1024) / 8,
  latency: 400,
});
await page.goto('/dashboard');`,[`It uses a raw CDP session to simulate a slow, high-latency network connection (roughly a throttled 3G-like profile) before loading the dashboard, testing how the app behaves for users on poor network conditions — a capability not exposed directly by Playwright's high-level API`,`It permanently disables the network for the entire browser, identical to context.setOffline(true)`,`CDP sessions can only be used to inspect network requests, never to modify network behavior`,`This code requires WebKit specifically, since Chromium does not support CDP network emulation`],0,`Network throttling via CDP's emulateNetworkConditions is a classic example of a capability only reachable through the raw protocol, since Playwright's high-level API has no direct network-throttling option.`],[`O`,`I`,"What does this assertion demonstrate about `browserType.name()`?",`test('engine-specific banner', async ({ page, browserName }) => {
  await page.goto('/');
  if (browserName === 'firefox') {
    await expect(page.locator('.firefox-notice')).toBeVisible();
  }
});`,[`browserName is a built-in test fixture (distinct from any manual browserType.name() call) that reports which of chromium/firefox/webkit the current project is configured to run — used here to conditionally check a Firefox-specific UI element only when relevant`,`browserName only reports "chromium" regardless of the actual configured project, due to a known limitation`,`This code requires manually calling browserType.name() since browserName is not otherwise available inside a test`,`The if-check is unnecessary because .firefox-notice is automatically hidden on non-Firefox engines by Playwright`],0,`browserName is one of Playwright's built-in fixtures, giving tests visibility into which engine is currently active without needing any manual lookup.`],[`O`,`A`,`What is the significance of the origin-scoped permission check in this test?`,`const context = await browser.newContext();
await context.grantPermissions(['clipboard-read', 'clipboard-write'], {
  origin: 'https://app.example.com',
});
const page = await context.newPage();
await page.goto('https://app.example.com/editor');
await page.getByRole('button', { name: 'Paste' }).click();`,[`Clipboard access is pre-authorized specifically for the app.example.com origin, allowing the Paste button's clipboard-read interaction to succeed without triggering a native permission prompt that Playwright could not otherwise click through`,`grantPermissions() has no effect on clipboard-related APIs, only on geolocation and notifications`,`The origin option here is required syntax but has no actual scoping effect on clipboard permissions specifically`,`This code requires the page to also call navigator.clipboard.requestPermission() manually before Paste will work`],0,`Clipboard access is one of several permission-gated browser APIs where grantPermissions() avoids a native prompt Playwright cannot directly interact with — origin scoping keeps the grant precise.`],[`O`,`I`,`What does this test check by combining a route handler with a real network assertion?`,`let capturedHeaders;
await context.route('**/api/profile', async route => {
  capturedHeaders = route.request().headers();
  await route.continue();
});
await page.goto('/profile');
expect(capturedHeaders['authorization']).toMatch(/^Bearer /);`,[`It intercepts the real request just to inspect its headers (confirming an Authorization bearer token is attached) while still letting the request continue on to the real network via route.continue(), rather than faking a response`,`route.continue() prevents the request from actually being sent, making this assertion meaningless`,`Header inspection is only possible via route.fulfill(), not route.continue()`,`This code silently drops the Authorization header before the real request goes out, due to how route.continue() works by default`],0,`route.continue() (optionally after inspecting or even modifying the request) is the right choice when you want to observe or slightly adjust real traffic without faking the response entirely.`],[`O`,`A`,"What does this test demonstrate about combining `context.setOffline()` with an in-flight request?",`const requestPromise = page.getByRole('button', { name: 'Sync now' }).click();
await context.setOffline(true);
await expect(page.getByText('Sync failed — you are offline')).toBeVisible();
await requestPromise;`,[`It simulates connectivity dropping in the middle of an in-progress action, verifying the app surfaces a clear offline error rather than hanging or silently failing — testing a more realistic failure mode than simply starting offline from the beginning`,`setOffline(true) has no effect on requests that were already initiated before it was called`,`This code always throws because context.setOffline() cannot be called while another action's promise is unresolved`,`requestPromise must be awaited before setOffline() is called, or the test is invalid`],0,`Toggling offline state mid-action (rather than only at the start) tests a genuinely different and often more realistic failure scenario: connectivity loss during an active operation.`],[`O`,`I`,`What is the effect of combining these two context options together?`,`const context = await browser.newContext({
  locale: 'de-DE',
  timezoneId: 'Europe/Berlin',
});
const page = await context.newPage();
await page.goto('/order-summary');`,[`Pages in this context report German as the preferred language (affecting navigator.language, Intl formatting, and the Accept-Language header) and use Berlin's time zone for any Date-based rendering — together giving realistic German-locale test coverage for both language and time formatting`,`locale only affects text translation and has no relationship to date/number formatting`,`timezoneId overrides locale, so the page actually behaves as if locale were unset`,`These two options conflict with each other and Playwright will throw a configuration error`],0,`locale and timezoneId are complementary — locale governs language/number/date formatting conventions, while timezoneId governs the actual clock time used for Date calculations, and testing internationalization often needs both together.`],[`O`,`A`,"What does this code check, and why is `context.pages()` iterated rather than just checking one page?",`const context = await browser.newContext();
const page1 = await context.newPage();
await page1.goto('/dashboard');
await page1.getByRole('link', { name: 'Open report' }).click(); // opens a new tab

for (const p of context.pages()) {
  await expect(p).not.toHaveURL(/error/);
}`,[`It verifies that NEITHER the original tab NOR any newly opened tab (like the report opened in a new tab) ended up on an error page, checking every currently open page in the context rather than assuming there is still only one`,`context.pages() only ever returns closed pages, so this loop would check nothing meaningful`,`This code causes an error since a context can only have exactly one page open at any given time`,`The loop is redundant because all pages in a context always share an identical URL`],0,`Actions that open new tabs increase context.pages().length — iterating over all of them (rather than assuming a fixed single page) is the robust way to assert across every currently open tab.`],[`O`,`I`,"What does this test validate about a context created with `bypassCSP: true`?",`const context = await browser.newContext({ bypassCSP: true });
const page = await context.newPage();
await page.goto('/secure-page'); // serves a strict CSP header blocking inline scripts
await page.addScriptTag({ content: 'window.__testFlag = true;' });
const flag = await page.evaluate(() => (window as any).__testFlag);
expect(flag).toBe(true);`,[`Without bypassCSP, the page's strict Content-Security-Policy would likely block the inline script injected via addScriptTag(), causing __testFlag to never be set; bypassCSP: true disables that enforcement so the test's own injected script is not blocked by the same policy meant for real attackers`,`bypassCSP only affects network requests, not injected script tags, so this test would behave identically either way`,`addScriptTag() is immune to CSP restrictions regardless of the bypassCSP setting`,`This configuration disables all JavaScript execution on the page, contradicting the assertion that follows`],0,`A real CSP header is designed specifically to block inline/injected scripts like this — bypassCSP is the documented way to prevent your own test tooling from being caught by that same protection.`],[`O`,`A`,`What is being demonstrated by launching three engines directly in this standalone script (outside the standard test runner)?`,`import { chromium, firefox, webkit } from 'playwright';

for (const engineType of [chromium, firefox, webkit]) {
  const browser = await engineType.launch();
  const page = await browser.newPage();
  await page.goto('https://example.com');
  console.log(engineType.name(), await page.title());
  await browser.close();
}`,[`It manually launches, uses, and closes each of the three browser engines sequentially within one plain script — a pattern used outside the @playwright/test runner for general browser automation tasks, distinct from the projects-array approach used to run a test SUITE against multiple engines`,`This code only actually launches chromium; firefox and webkit are silently ignored inside the loop`,`The three browsers run genuinely simultaneously, competing for the same page object`,`engineType.launch() requires being inside a test() callback and would throw an error in a standalone script`],0,`This is the general-purpose, low-level automation pattern (used outside the test-runner's project/fixture model) for scripts that need direct, manual control over multiple browser engines.`],[`S`,`I`,"A test using `context.route()` to mock an API works correctly when run alone, but when the full suite runs, an unrelated test elsewhere starts unexpectedly receiving the same mocked response for a request to a similarly-named endpoint. What is the most likely explanation?",null,["The route pattern used (likely a broad glob like `**/api/**`) is unintentionally matching a different, unrelated endpoint's URL in another test — and if both tests share a manually created, long-lived context instead of getting fresh per-test contexts, a route registered in one test could still be active when the other runs",`Route handlers are global across the entire test run by design, and this is expected, unavoidable behavior`,`This can only happen if both tests are in the exact same file, never across separate files`,`context.route() automatically applies retroactively to any test that ran earlier in the same worker`],0,`An overly broad glob pattern is often the real culprit — but if contexts are being manually shared instead of using the fresh-per-test fixture, a route can also leak across tests that were never meant to share it.`],[`S`,`A`,"After adding `bypassCSP: true` globally to speed up injecting test helper scripts across the whole suite, a security-focused test that specifically verifies the app's CSP header blocks inline scripts now fails, since the injected malicious-looking script tag succeeds instead of being blocked. What is the correct fix?",null,[`Scope bypassCSP: true only to the specific tests/projects that actually need it for helper script injection, and create the CSP-verification test's context WITHOUT bypassCSP so the real security enforcement is still exercised and can be genuinely verified`,`Remove the CSP-verification test entirely, since bypassCSP is now a global requirement for the suite`,`bypassCSP cannot be scoped per-test; it must be all-or-nothing for the entire config`,`Add a manual check inside the CSP test that reads the response header directly, since bypassCSP prevents the browser's actual enforcement from ever being testable again`],0,`A global bypassCSP setting undermines any test whose entire purpose is verifying CSP enforcement — scope the option to just the contexts/projects that genuinely need it, leaving security-verification tests using default (enforcing) behavior.`],[`S`,`I`,"A test suite relies on `context.grantPermissions(['notifications'])` globally in a shared setup, but a newly added test specifically wants to verify what happens when a user DENIES notification permission. What should this one test do differently?",null,[`Create its own context WITHOUT calling grantPermissions() for notifications (or explicitly using context.clearPermissions() if permissions were already granted at a broader scope), so the app's actual denied-permission code path can be genuinely exercised rather than being masked by the suite-wide grant`,`This scenario cannot be tested because grantPermissions(), once called anywhere in the suite, cannot be undone for any subsequent test`,`Denied permission scenarios must always be tested manually outside of Playwright entirely`,`Add a second call to grantPermissions(['notifications']) with an empty array to simulate denial`],0,`clearPermissions() (or simply not granting the permission for that specific context) lets a test exercise the denied-permission path even when other tests in the same suite rely on a broader grant.`],[`S`,`A`,"A team notices that tests using `page.waitForRequest()` to verify analytics beacons occasionally miss the request entirely, reporting a timeout, even though manual testing confirms the beacon is sent. What race condition is most likely occurring?",null,[`The waitForRequest() call may be registered AFTER the triggering action has already fired and the request has already completed — like the popup/download event pattern, request-waiting needs the listener set up (often via Promise.all pairing with the triggering action) before the action that causes the request, not after`,`waitForRequest() only works for requests that are still pending, and analytics beacons are too fast for it to ever catch reliably`,`This is expected and unfixable; beacon requests are fundamentally untestable in Playwright`,`The fix is to always use context.route() with route.continue() instead of waitForRequest(), since route() never has this timing issue`],0,`This is the same event-listener-ordering pitfall as popups and downloads — pairing the wait with the triggering action via Promise.all ensures the listener is active before the request can possibly fire.`]],navigation:[[`O`,`B`,"What event does `page.goto(url)` wait for by default?",null,[`DOMContentLoaded — HTML is parsed`,`The load event — all resources have finished loading`,`networkidle — no requests for 500 ms`,`commit — first byte received`],1,"The default `waitUntil` is `'load'`, which waits for the browser's `load` event to fire — all scripts, images, and stylesheets are done."],[`O`,`B`,"What does `page.reload()` do?",null,[`Navigates to the homepage`,`Reloads the current page (same as pressing F5)`,`Clears the page cache and re-downloads all assets`,`Restarts the browser context`],1,"`page.reload()` triggers a full page refresh. It also accepts `{ waitUntil }` and `{ timeout }` options identical to `page.goto()`."],[`O`,`I`,"What does `waitUntil: 'networkidle'` mean in `page.goto()` options?",null,[`Waits until all images are loaded`,`Waits until there have been no network requests for at least 500 ms`,`Waits until the WebSocket connection is established`,`Waits until all async JavaScript has executed`],1,"`'networkidle'` is the most thorough wait: Playwright pauses until there are no open network connections for 500 ms. Useful for data-heavy pages but can time out on sites with persistent polling."],[`O`,`B`,"Is `page.url()` synchronous or asynchronous?",null,[`Asynchronous — it returns a Promise<string>`,`Synchronous — it returns the current URL string immediately`,`It depends on the browser engine`,`It is asynchronous only in Firefox`],1,"`page.url()` is one of the rare synchronous Playwright page methods. No `await` needed — it reads the cached URL from the current frame."],[`O`,`I`,"What does `page.waitForURL('/dashboard')` do?",null,[`Asserts that the current URL is exactly /dashboard`,`Navigates the page to /dashboard`,`Waits (polls) until the current URL becomes /dashboard`,`Blocks the test for a fixed timeout then checks the URL`],2,"`waitForURL()` polls the current URL until it matches the string, regex, or glob pattern provided, or until the timeout expires."],[`O`,`B`,"What does `waitUntil: 'domcontentloaded'` wait for?",null,[`All external resources (scripts, images, CSS) to load`,`Only the HTML document to be parsed (DOMContentLoaded event)`,`No network requests for 500 ms`,`The page title to be set`],1,"`'domcontentloaded'` fires as soon as HTML is parsed into a DOM — before scripts execute or images download. It is the fastest `waitUntil` option and suits static or server-rendered pages."],[`O`,`B`,"What does `page.goBack()` do?",null,[`Reloads the current page`,`Navigates to the previous entry in the browser history`,`Navigates to the homepage`,`Clears the current page URL`],1,"`page.goBack()` is the programmatic equivalent of clicking the browser's back button. It returns a Response or null if navigation failed."],[`O`,`I`,`Which assertion is preferred for checking the page URL in Playwright?`,null,["`expect(page.url()).toBe('/dashboard')`","`assert(page.url() === '/dashboard')`","`await expect(page).toHaveURL('/dashboard')`","`expect(await page.title()).toBe('/dashboard')`"],2,"`expect(page).toHaveURL()` auto-retries until the URL matches, making it resilient to async redirects. Comparing `page.url()` directly is a single snapshot that can fail on in-flight redirects."],[`O`,`I`,"What does `page.waitForLoadState('networkidle')` wait for?",null,[`The page load event only`,`No active network connections for 500 ms`,`All page JavaScript to finish executing`,`All WebSocket messages to be received`],1,"`waitForLoadState('networkidle')` is identical in meaning to `waitUntil: 'networkidle'` in `goto()` — it can be called independently to re-wait after an action."],[`O`,`B`,"If `baseURL` is `'http://localhost:3000'` in playwright.config.ts, what full URL does `page.goto('/login')` navigate to?",null,[`http://localhost:3000login`,`http://localhost:3000//login`,`http://localhost:3000/login`,`It throws an error because the URL is not absolute`],2,"When `baseURL` is configured, Playwright prepends it to relative paths. `'/login'` becomes `'http://localhost:3000/login'`."],[`O`,`A`,"What does `waitUntil: 'commit'` mean in `page.goto()`?",null,[`Waits for the git commit to complete`,`Waits until the first byte of the response is received (earliest possible)`,`Waits until all cookies are committed to storage`,`Waits until the service worker caches the response`],1,"`'commit'` is the earliest wait state — the response headers are received and the navigation is committed. The body hasn't loaded yet. Useful for very fast checks on response headers."],[`O`,`I`,"What does `page.title()` return?",null,[`The current URL string synchronously`,`A Promise that resolves to the document title string`,`The page's meta description`,`The H1 heading text of the page`],1,"`page.title()` is async and returns a Promise<string>. Always `await` it. For assertions, prefer `await expect(page).toHaveTitle()` which auto-retries."],[`F`,`B`,"Fill in the method: `await page.______('/home')`",null,[`goto`,`navigate`,`open`,`visit`],0,"`page.goto()` is the primary navigation method in Playwright. It accepts a URL string and an optional options object."],[`F`,`I`,"Fill in the option key: `await page.goto('/page', { ______: 'networkidle' })`",null,[`waitUntil`,`waitFor`,`until`,`loadState`],0,"`waitUntil` controls which lifecycle event `goto()` waits for before resolving. Valid values: `'commit'`, `'domcontentloaded'`, `'load'`, `'networkidle'`."],[`F`,`I`,"Fill in the method: `await page.______URL('/success')`",null,[`waitFor`,`assertFor`,`checkFor`,`pollFor`],0,"`page.waitForURL(pattern)` waits until the current URL matches the given string, regex, or glob. Essential after clicks that trigger redirects."],[`F`,`B`,"Fill in the method to refresh the current page: `await page.______( )`",null,[`reload`,`refresh`,`reset`,`restart`],0,"`page.reload()` refreshes the current page. It takes the same `{ waitUntil, timeout }` options as `page.goto()`."],[`F`,`I`,"Fill in the method: `await page.______LoadState('networkidle')`",null,[`waitFor`,`await`,`check`,`poll`],0,"`page.waitForLoadState(state)` can be called at any point to wait for the page to reach a specific load state, independent of navigation."],[`F`,`B`,"Fill in the matcher: `await expect(page).toHave______('/checkout')`",null,[`URL`,`Path`,`Location`,`Href`],0,"`toHaveURL()` is the auto-retrying URL assertion. It accepts a string, regex, or URL object."],[`F`,`I`,"Fill in to get the current page title: `const t = await page.______( )`",null,[`title`,`getTitle`,`pageTitle`,`documentTitle`],0,"`page.title()` is an async method that returns the current `document.title`. Always `await` it or use `expect(page).toHaveTitle()` instead."],[`E`,`B`,"What is wrong?\n`page.goto('https://example.com')`",null,["Missing `await` — `page.goto()` is async and returns a Promise","`goto` requires a second options argument",`The URL must be relative, not absolute`,"`goto` is deprecated; use `page.navigate()` instead"],0,"Without `await`, the navigation starts but the test continues immediately — actions on the next line run against an unloaded or previous page."],[`E`,`I`,"What is wrong with this check?\n`expect(page.url()).toBe('/dashboard')`",null,[`The URL check is done at one point in time with no retry — a redirect in progress will cause a false failure`,"`page.url()` must be awaited","`toBe` does not work with URL strings","`page.url()` returns a full URL; this comparison will always fail"],0,"This is a synchronous snapshot check with no retry. Use `await expect(page).toHaveURL('/dashboard')` which polls until the URL matches or times out."],[`E`,`B`,"What is wrong?\n`await page.goto('/page', { waitUntil: 'idle' })`",null,["`'idle'` is not a valid waitUntil value — use `'networkidle'`","`waitUntil` must be the first argument, not inside options","`goto` does not accept an options object","`waitUntil` requires a numeric millisecond value"],0,"The valid `waitUntil` strings are `'commit'`, `'domcontentloaded'`, `'load'`, and `'networkidle'`. `'idle'` throws a validation error."],[`E`,`I`,"What is wrong?\n```\nawait page.getByRole('button', { name: 'Submit' }).click();\nconst url = page.url();\nexpect(url).toBe('/success');\n```",null,[`The URL is read immediately after click with no wait for the navigation to complete`,"`page.url()` cannot be called after a click","`expect` requires `await`","`getByRole` cannot find buttons by name"],0,"`page.url()` is synchronous but navigation after a click is async. The URL read happens before the redirect completes. Use `await page.waitForURL('/success')` then assert."],[`E`,`A`,"What is wrong with using `waitUntil: 'networkidle'` on an app with persistent WebSocket connections?",null,[`The goto will time out because WebSockets keep the network active indefinitely`,`WebSockets are not counted toward network activity`,"`networkidle` is only valid with HTTP/1.1","WebSocket connections require `waitUntil: 'websocket'`"],0,"`networkidle` counts open connections including WebSockets. A live WebSocket means the 500 ms quiet window never occurs, causing a timeout. Use `'load'` or `'domcontentloaded'` instead."],[`E`,`I`,"What is wrong?\n`await expect(page).toHaveURL('dashboard')`",null,["The URL pattern `'dashboard'` will match any URL containing the substring — use `'/dashboard'` for an exact-path check","`toHaveURL` does not accept strings","`expect(page)` should be `expect(page.url())`",`The string must be wrapped in a regex`],0,"`toHaveURL('dashboard')` does a substring match. Pass `'/dashboard'` (leading slash) or a regex like `//dashboard$/` to avoid matching unintended URLs like `/admin/dashboard`."],[`D`,`B`,"A test asserts the URL right after `page.goto()` returns, but the URL is still the old one. What is happening?",null,["The page performs a client-side redirect after load — add `await page.waitForURL('/final')`","`page.goto()` does not update the URL","The `baseURL` is prepending the wrong host","The assertion is using `toBe` instead of `toHaveURL`"],0,"Server-rendered redirects are handled by `goto()`, but JavaScript-based redirects happen after the `load` event. Use `waitForURL()` to wait for the final destination."],[`D`,`I`,"A test navigates back with `page.goBack()` but the assertion runs before the previous page loads. How do you fix it?",null,["`await page.goBack()` and then `await page.waitForLoadState('load')`","Call `page.reload()` instead of `page.goBack()`","Add `{ waitUntil: 'domcontentloaded' }` to `goto()`","Use `await page.waitForNavigation()` before `goBack()`"],0,"`goBack()` is async — always `await` it. If the previous page has dynamic content, follow up with `await page.waitForLoadState('load')` or `await page.waitForLoadState('networkidle')`."],[`D`,`I`,"Tests pass locally but time out in CI because pages load slowly. What `page.goto()` option should be added?",null,["`{ timeout: 60_000 }` to increase the navigation timeout for this call","`{ waitUntil: 'commit' }` to finish faster","`{ retries: 3 }` to retry on timeout","`{ slow: true }` to switch to slow mode"],0,"`{ timeout: 60_000 }` raises the navigation timeout to 60 seconds for that specific `goto()` call without changing the global config."],[`D`,`B`,"A developer compares `page.title()` with `===` and always gets `false`. What is wrong?",null,["`page.title()` returns a Promise — comparing a Promise with `===` to a string always returns false","`page.title()` returns the meta description, not the `<title>` tag","`===` is not valid inside a Playwright test","`page.title()` is not a function in Playwright"],0,"`page.title()` is async. Without `await` you get a Promise object, and `Promise === 'My Title'` is always `false`. Use `await page.title()` or `await expect(page).toHaveTitle('…')`."],[`D`,`A`,"An SPA loads content lazily after the `load` event. Tests pass locally (fast machine) but fail in CI. What is the best fix?",null,["After navigation, call `await page.waitForLoadState('networkidle')` or wait for a specific element to appear","Use `waitUntil: 'load'` in goto() options",`Increase the global timeout only`,"Add a fixed `page.waitForTimeout(3000)` call"],0,"Waiting for `networkidle` or for a specific element (`await page.getByText('Results').waitFor()`) is the correct approach. Fixed `waitForTimeout` sleeps are brittle and slow."],[`D`,`I`,"`await page.waitForURL('/profile')` times out even though the URL visually changes to `/profile/123`. What is wrong?",null,["The pattern must match the full URL — use `'/profile/**'` or `/\\/profile/` regex","`waitForURL` only accepts exact strings",`The page must be reloaded after navigation`,"`waitForURL` cannot match after a click, only after goto()"],0,"`'/profile'` matches only the exact string. The actual URL is `/profile/123`. Use the glob `'/profile/**'` or the regex `/\\/profile/` to match the dynamic segment."],[`C`,`B`,"Complete: `await page.______('/products')`",null,[`goto`,`navigate`,`open`,`visit`],0,"`page.goto()` is the standard navigation command. It awaits the configured `waitUntil` event before resolving."],[`C`,`I`,"Complete for the fastest navigation wait: `await page.goto('/page', { waitUntil: '______' })`",null,[`domcontentloaded`,`load`,`networkidle`,`commit`],0,"`'domcontentloaded'` is faster than `'load'` because it doesn't wait for external resources. Ideal for server-rendered pages where you interact with DOM immediately."],[`C`,`I`,"Complete the URL assertion: `await expect(page).toHave______('/cart')`",null,[`URL`,`Path`,`Href`,`Link`],0,"`toHaveURL()` is the auto-retrying URL matcher. It accepts a string, regex, or URL object and polls until it matches or times out."],[`C`,`I`,"Complete to wait after a click causes a redirect: `await page.waitFor______('/success')`",null,[`URL`,`LoadState`,`Navigation`,`Redirect`],0,"`page.waitForURL(pattern)` is the canonical way to wait for the URL to change after an action. It supports strings, globs (`'**/path**'`), and regexes."],[`C`,`A`,"Complete to wait for full page stability after navigation: `await page.waitForLoadState('______')`",null,[`networkidle`,`load`,`domcontentloaded`,`commit`],0,"`'networkidle'` waits until there are no open connections for 500 ms — the most thorough wait state, ideal for verifying that all async data has loaded."],[`L`,`B`,"What is the key difference between `page.url()` and `page.title()`?",null,["`page.url()` is synchronous; `page.title()` is async and returns a Promise",`Both are asynchronous; they just return different values`,"`page.url()` is deprecated; use `page.title()` instead","`page.url()` requires a page argument; `page.title()` does not"],0,"`page.url()` is synchronous — it reads from the cached current frame URL. `page.title()` is async because it queries the live `document.title` from the browser."],[`L`,`I`,"When should you choose `'domcontentloaded'` over `'networkidle'`?",null,[`When the page has no images`,`When you only need the HTML structure to interact with and don't need all resources loaded`,`When using Firefox instead of Chromium`,`When you are testing a REST API endpoint`],1,"`'domcontentloaded'` is ideal when you immediately interact with DOM elements rendered server-side. For SPAs that fetch data after load, `'networkidle'` or element-level waits are safer."],[`L`,`I`,"What happens if `page.goto()` navigates to a URL that returns a 404 status?",null,[`Playwright throws an error by default`,`The Promise resolves normally — goto() doesn't throw on non-2xx status`,`The test is automatically retried`,`The page stays on the current URL`],1,"By default `page.goto()` does not throw on HTTP error responses. It resolves with the Response object. You must explicitly assert the status with `expect(response.status()).toBe(200)` if needed."],[`L`,`A`,`What is the correct chronological order of page load events?`,null,[`commit → DOMContentLoaded → load → networkidle`,`load → DOMContentLoaded → commit → networkidle`,`DOMContentLoaded → commit → networkidle → load`,`networkidle → load → DOMContentLoaded → commit`],0,"The order is: `commit` (first byte) → `DOMContentLoaded` (HTML parsed) → `load` (all resources) → `networkidle` (no active requests for 500 ms)."],[`L`,`I`,"Does `page.waitForURL()` accept regex patterns?",null,[`Yes — string, regex, and glob patterns are all supported`,`No — only exact string matching`,`Yes — regex only, not strings`,`Only if the URL contains query parameters`],0,"`waitForURL()` accepts a string (substring match), a regex (`/\\/profile\\/\\d+/`), or a glob pattern (`'**/checkout**'`). Use the most specific form to avoid false matches."],[`R`,`B`,`After clicking "Submit", a form redirects to a confirmation page. What is the best way to verify the user landed on the correct URL?`,null,["`await page.waitForURL('/confirmation')` followed by `await expect(page).toHaveURL('/confirmation')`","`expect(page.url()).toBe('/confirmation')`","`await page.reload()` then check the URL","`await page.waitForTimeout(1000)` then `page.url()`"],0,"`waitForURL()` handles the async redirect; `toHaveURL()` confirms the destination with auto-retry. The synchronous `page.url()` check and fixed waits are fragile alternatives."],[`R`,`I`,`Your SPA fetches user data via API after the initial render. Tests fail because elements aren't populated yet. What is the recommended strategy?`,null,["Use `await page.waitForLoadState('networkidle')` after navigation, or wait for a specific element","Use `waitUntil: 'domcontentloaded'` in goto()",`Add a 2-second sleep after each navigation`,`Reload the page after navigating`],0,"`waitForLoadState('networkidle')` waits for all fetch calls to finish. Even better: wait for the specific element you need — `await page.getByText('Welcome').waitFor()`."],[`R`,`I`,`You are testing that clicking the browser back button returns the user to the correct previous page. What Playwright API models this?`,null,["`await page.goBack()` followed by `await expect(page).toHaveURL('/previous')`","`await page.goto(page.url() - 1)`","`await page.history(-1)`","`await page.navigate('back')`"],0,"`page.goBack()` is the direct equivalent of the browser's back button. Always `await` it, then assert the URL or visible content."],[`R`,`A`,"Your app has long-running background polling every 2 seconds. Tests using `waitUntil: 'networkidle'` always time out. What is the fix?",null,["Switch to `waitUntil: 'load'` or `'domcontentloaded'` and wait for a specific element instead",`Increase the timeout to 10 minutes`,`Disable background polling only during tests`,"Use `page.waitForResponse()` to wait for the polling response"],0,"Continuous background requests prevent `networkidle` from ever triggering. Use `'load'` + element-level waits instead, which are unaffected by background network activity."],[`R`,`I`,`A test navigates to several pages and needs to confirm the title changes correctly on each. What is the best assertion?`,null,["`await expect(page).toHaveTitle('Expected Title')` after each navigation","`assert(await page.title() === 'Expected Title')`","`expect(page.url()).toContain('title')`","`await page.waitForTitle('Expected Title')`"],0,"`toHaveTitle()` auto-retries and accepts strings or regexes, making it resilient to async title updates from client-side routing."],[`S`,`I`,"A login test clicks Submit, then immediately checks `page.url()`. It fails because the URL still shows `/login`. How do you fix it?",null,["Add `await page.waitForURL('/dashboard')` before reading the URL","Add `await page.reload()` after the click","Use `waitUntil: 'networkidle'` in the original `page.goto()`","Use `page.waitForTimeout(500)` to let the redirect happen"],0,"The redirect is async. `page.waitForURL()` polls until the URL changes, so the subsequent URL check sees the correct value."],[`S`,`A`,"In CI, `page.goto()` times out on a page that loads in under 5 seconds locally. The global timeout is 30 s. What steps should you take?",null,["Increase the per-call timeout: `{ timeout: 60_000 }`, and verify there are no network differences between local and CI (proxy, firewall)","Set `headless: false` in CI to speed up rendering",`Add retries at the config level`,"Set `waitUntil: 'commit'` for all navigations"],0,"Always first rule out environmental differences (proxy, DNS). Then increase the timeout for that specific call. Setting `'commit'` skips useful waiting and can cause subsequent failures."],[`S`,`I`,"A test uses `await page.waitForLoadState('networkidle')` after a form submit, but the page has an analytics script that sends a request 3 seconds after load. Tests are consistently slow. What is the better approach?",null,["Wait for the specific element you need instead: `await page.getByText('Order confirmed').waitFor()`","Increase `timeout` for the waitForLoadState call","Block analytics requests using `page.route()`","Switch to `waitUntil: 'load'`"],0,"Element-level waiting is faster and more precise than `networkidle`. Waiting for the element that proves success decouples the test from unrelated network activity."],[`S`,`A`,"A developer checks `expect(await page.title()).toBe('Home')` and it sometimes fails because the title is set dynamically by JavaScript after paint. What should replace this?",null,["`await expect(page).toHaveTitle('Home')` — it auto-retries until the title matches","`await page.waitForTimeout(1000)` then `page.title()`","`await page.waitForFunction(() => document.title === 'Home')`","`expect(page.title()).resolves.toBe('Home')`"],0,"`expect(page).toHaveTitle()` polls until the `<title>` element matches, making it resilient to async title updates from JS frameworks. `waitForFunction()` also works but is more verbose."],[`O`,`B`,"What does `page.goForward()` do?",null,[`Navigates to the next page in a wizard flow`,`Triggers the browser's forward navigation — moves forward one step in the history stack`,`Navigates to the next URL alphabetically`,`Goes forward only if a previous goBack() was called`],1,`goForward() is the complement of goBack() — it moves forward through the navigation history.`],[`O`,`I`,"What does `page.waitForResponse(url)` wait for?",null,[`The page to finish loading`,`A specific network response matching the URL or predicate — returns the Response object when the match is received`,`The next page navigation`,`A WebSocket message`],1,`waitForResponse is useful for asserting API calls — you can match by URL string, glob, regex, or a predicate function that inspects the response.`],[`O`,`I`,"What does `page.waitForRequest(url)` return?",null,[`A boolean indicating whether the request was made`,`A Promise<Request> that resolves when a network request matching the pattern is sent`,`A list of all requests made so far`,`A mocked response object`],1,`waitForRequest resolves with the Request object — you can then inspect method, URL, post data, and headers.`],[`O`,`B`,"What does `page.evaluate(fn)` do?",null,[`Runs a Node.js function in the test process`,`Executes a JavaScript function inside the browser page context and returns the result to the test`,`Evaluates a CSS selector and returns matching elements`,`Runs a Python expression in the browser`],1,`evaluate() bridges Node.js (test) and browser (page) — the function runs in the browser's JavaScript context and the return value is serialised back to Node.`],[`O`,`I`,"What does `page.frames()` return?",null,[`An array of all browser tabs`,`An array of all Frame objects in the page including the main frame and all iframes`,`An array of animation frames`,`The current call stack of the page`],1,`frames() gives you access to all frames — iterate to find a specific iframe by name or URL.`],[`O`,`B`,"What is the difference between `page.waitForSelector()` and `locator.waitFor()`?",null,[`They are identical`,`waitForSelector is a legacy API returning an ElementHandle; locator.waitFor() is the modern way and integrates with the locator API — prefer locator.waitFor()`,`waitForSelector auto-retries but locator.waitFor() does not`,`waitForSelector works in frames but locator.waitFor() does not`],1,`locator.waitFor() is the modern replacement for waitForSelector — it stays in the locator world without dropping to ElementHandle.`],[`O`,`I`,"What does `page.mainFrame()` return?",null,[`The primary browser window`,`The main Frame of the page (the top-level document frame)`,`The first visible frame on screen`,`The iframe with the highest z-index`],1,`mainFrame() is the top-level document — use it to distinguish from iframes when working with frames directly.`],[`O`,`B`,"What does `page.waitForTimeout(2000)` do and why is it discouraged?",null,[`Waits for 2 seconds and auto-retries — recommended for loading states`,`Adds a fixed 2-second delay — discouraged because it makes tests slow and brittle (too short fails, too long wastes time). Use element-level waits instead.`,`Waits up to 2 seconds for the page to load`,`Retries the last action for 2 seconds`],1,`Fixed waits are an anti-pattern — use waitFor() on the element you actually need and let Playwright's auto-wait handle timing.`],[`O`,`I`,"What does `page.addScriptTag({ url })` do?",null,[`Navigates to the script URL`,`Injects an external script tag into the current page — the script is fetched and executed`,`Mocks a script resource`,`Blocks a script from loading`],1,`addScriptTag is useful for injecting polyfills or testing libraries into a live page during a test.`],[`O`,`A`,"What does `page.waitForFunction(fn)` wait for?",null,[`A specific URL pattern`,`Executes fn in the browser and polls until it returns a truthy value — generalised waiting for any browser-side condition`,`Waits for the next requestAnimationFrame`,`Waits for a DOM mutation`],1,`waitForFunction is the escape hatch for complex waiting conditions that don't fit the standard locator or load state APIs.`],[`O`,`I`,"What is the return type of `page.goto(url)`?",null,[`void`,`Promise<Response | null> — the HTTP response for the navigation, or null for navigations like data: URLs`,`Promise<boolean> indicating success`,`Promise<string> of the final URL`],1,`goto() returns the Response so you can immediately assert status, headers, or URL without additional calls.`],[`F`,`B`,"Complete to navigate to a URL and wait for network idle.\n```ts\nawait page.goto('/products', { ______: 'networkidle' });\n```",null,[`waitUntil`,`wait`,`state`,`loadState`],0,`waitUntil is the option controlling which page load event triggers goto() to resolve.`],[`F`,`B`,"Complete to go back one step in browser history.\n```ts\nawait page.______();\n```",null,[`goBack`,`back`,`navigate(-1)`,`historyBack`],0,`page.goBack() is the Playwright API for browser back navigation.`],[`F`,`I`,"Complete to execute JavaScript in the browser and return its result.\n```ts\nconst title = await page.______(() => document.title);\n```",null,[`evaluate`,`exec`,`run`,`inject`],0,`page.evaluate() runs the function in the browser context and returns the serialised result.`],[`F`,`I`,`Complete to wait for a specific response.
\`\`\`ts
const [response] = await Promise.all([
  page.______('/api/products'),
  page.getByRole('button', { name: 'Load' }).click(),
]);
\`\`\``,null,[`waitForResponse`,`waitForRequest`,`waitForAPI`,`waitForFetch`],0,`waitForResponse resolves with the Response matching the pattern — wrap it with the triggering action in Promise.all.`],[`F`,`B`,"Complete to reload the current page.\n```ts\nawait page.______();\n```",null,[`reload`,`refresh`,`goTo(page.url())`,`restart`],0,`page.reload() triggers a full page refresh.`],[`F`,`I`,"Complete to get the page's outer HTML.\n```ts\nconst html = await page.______();\n```",null,[`content()`,`html()`,`source()`,`getHTML()`],0,`page.content() returns the full serialised HTML of the current page.`],[`F`,`I`,`Complete to wait for navigation triggered by a button click.
\`\`\`ts
await Promise.all([
  page.______(),
  page.getByRole('link', { name: 'Next' }).click(),
]);
\`\`\``,null,[`waitForNavigation`,`waitForURL`,`waitForLoad`,`waitForRedirect`],0,`waitForNavigation() is the classic pattern for click-triggered navigations — but prefer waitForURL() when you know the target URL.`],[`E`,`B`,"What is wrong?\n```ts\nawait page.click('button');\nconst url = page.url();\nexpect(url).toBe('/dashboard');\n```",null,[`click() is deprecated`,`The redirect happens asynchronously — page.url() is read immediately after click() before the redirect completes. Use waitForURL before reading the URL.`,`url must be awaited`,`expect() cannot take a string directly`],1,`Click triggers a navigation but does not wait for it — always waitForURL() or let auto-wait assertions handle timing.`],[`E`,`I`,`What is wrong?
\`\`\`ts
const result = await page.evaluate(async () => {
  const resp = await fetch('/api/data');
  return resp.json();
});
\`\`\``,null,[`evaluate cannot contain async functions`,`evaluate() can contain async functions BUT the return value must be serialisable to JSON — this is fine, though you should ensure the API endpoint is available`,`fetch is not available inside evaluate`,`page.evaluate() runs synchronously`],1,`This pattern is valid — evaluate can use fetch and async/await. The main constraints are that the return value must be JSON-serialisable and the function runs in the browser's JS context.`],[`E`,`B`,"What is wrong?\n```ts\nawait page.goto('/items');\nawait page.waitForLoadState('networkidle');\nawait page.waitForLoadState('networkidle'); // called twice\n```",null,[`waitForLoadState cannot be called twice on the same page`,`The second call waits for a NEW networkidle event — if no new navigation happens, it waits and eventually times out. Call it only once after navigation.`,`networkidle is not a valid load state`,`waitForLoadState returns a one-shot promise`],1,`waitForLoadState waits for the next occurrence of the state — if the page is already idle, the first call resolves immediately, but the second waits indefinitely.`],[`E`,`I`,"What is wrong?\n```ts\nawait page.goto('https://example.com');\nconst frame = page.frame('sidebar');\nawait frame.click('button'); // frame might be null\n```",null,[`frame() requires an index, not a name`,`page.frame(name) returns null if no frame with that name exists — you should check for null before interacting: if (!frame) throw new Error('sidebar frame not found')`,`frame() must be awaited`,`click() on a Frame is not supported`],1,`Always guard against null from frame() — a missing iframe causes a null dereference error.`],[`E`,`A`,`What is wrong?
\`\`\`ts
const [request] = await Promise.all([
  page.waitForRequest('/api/save'),
  page.getByRole('button', { name: 'Save' }).click(),
]);
// request is used here
\`\`\``,null,[`Promise.all cannot mix waitFor and click`,`This pattern is correct — waitForRequest and click() run concurrently, so the request is captured even if it completes quickly after click. Nothing is wrong.`,`waitForRequest must come after click`,`Promise.all requires three or more elements`],1,`This is the correct pattern — starting waitForRequest before click() ensures no race condition even if the request completes very quickly.`],[`E`,`I`,`What is wrong?
\`\`\`ts
await page.evaluate((data) => {
  fetch('/api', { method: 'POST', body: JSON.stringify(data) });
  // no await
}, { name: 'test' });
\`\`\``,null,[`evaluate() cannot accept arguments`,`The fetch call is not awaited inside evaluate — the function returns before the fetch completes. Use async function and await the fetch, or use page.request.post() instead.`,`body must be a FormData object`,`JSON.stringify is not available in evaluate`],1,`Unawaited Promises inside evaluate are fire-and-forget — evaluate() resolves before the fetch finishes. Always await async operations inside evaluate.`],[`D`,`B`,'`page.goto("/products")` resolves but the product list is empty. Tests that assert on product items fail. What should you do?',null,[`Reload the page`,'The products are fetched asynchronously after initial render — wait for a product item to be visible: `await page.getByRole("listitem").first().waitFor()`',`Switch to waitUntil: "networkidle"`,`Increase the goto timeout`],0,`Waiting for specific content is more precise than networkidle — it confirms the data is rendered, not just that requests stopped.`],[`D`,`I`,'`page.waitForResponse("/api/users")` never resolves. What are two things to check?',null,[`The API is down`,`1) Verify the request URL matches exactly — use a glob or regex if the URL has query parameters: "**/api/users**". 2) Ensure the action that triggers the request is called AFTER waitForResponse is set up (use Promise.all).`,`Add retry: true to waitForResponse`,`Increase the timeout to 60 seconds`],0,`URL mismatch and wrong Promise ordering are the two most common causes of waitForResponse hanging.`],[`D`,`B`,'`page.evaluate()` throws "TypeError: fn is not a function". What is the likely cause?',null,[`evaluate() only accepts strings`,`The function was accidentally serialised incorrectly — ensure you pass an arrow function or function expression directly: page.evaluate(() => ...) not page.evaluate(myVar) where myVar might be undefined`,`evaluate() requires async functions only`,`The browser context is closed`],1,`evaluate() takes a function literal or a serialisable function — passing an undefined variable or a non-function value causes this error.`],[`D`,`A`,'Tests that use `page.on("dialog", dialog => dialog.accept())` handle the first dialog but subsequent dialogs are not handled. Why?',null,[`page.on only fires once`,`page.on("dialog", handler) registers a persistent listener — but if the handler calls dialog.accept() synchronously and another dialog appears before the next event loop tick, it may be missed. Use dialog.dismiss() with a guard, or register the listener in beforeEach.`,`Dialogs after the first require dialog.waitFor()`,`The listener is removed after the first dialog`],0,`dialog event listeners persist — but timing issues can arise if dialogs appear very quickly in sequence. Registering in beforeEach ensures fresh listeners per test.`],[`D`,`I`,"`page.frames()` returns only one frame even though the page has two iframes. Why?",null,[`frames() only counts visible iframes`,`The iframes may not have finished loading — wait for them to be attached: await page.waitForSelector("iframe") before calling frames(). Also, cross-origin iframes may require a different approach.`,`frames() is limited to one result in headless mode`,`iframes are counted as separate pages`],1,`waitForSelector on the iframe elements ensures they are attached to the DOM before frames() is called.`],[`C`,`B`,"Complete to execute JavaScript in the browser.\n```ts\nawait page.______(() => window.scrollTo(0, 500));\n```",null,[`evaluate`,`run`,`exec`,`inject`],0,`page.evaluate() executes the function in the browser's JavaScript context.`],[`C`,`I`,`Complete to wait for a network response before proceeding.
\`\`\`ts
const [res] = await Promise.all([
  page.______("**/api/cart"),
  page.getByRole("button", { name: "Add" }).click(),
]);
\`\`\``,null,[`waitForResponse`,`waitForRequest`,`on("response",`,`waitForNavigation`],0,`waitForResponse with a glob pattern captures the cart API response triggered by the button click.`],[`C`,`B`,'Complete to get the inner text of the page body.\n```ts\nconst text = await page.______("body");\n```',null,[`innerText`,`textContent`,`getByText`,`content`],0,`page.innerText(selector) returns the rendered text of the matching element — equivalent to element.innerText.`],[`C`,`I`,"Complete to wait for the page URL to match.\n```ts\nawait page.______(/\\/dashboard/);\n```",null,[`waitForURL`,`waitForNavigation`,`assertURL`,`checkURL`],0,`page.waitForURL() accepts a string, glob, or regex and resolves when the current URL matches.`],[`C`,`A`,`Complete to inject an argument into page.evaluate.
\`\`\`ts
const id = 42;
await page.______(userId => {
  window.currentUserId = userId;
}, ________);
\`\`\``,null,[`evaluate, id`,`exec, id`,`run, id`,`eval, id`],0,`The second argument to evaluate() is passed as the first argument to the browser function — it must be JSON-serialisable.`],[`L`,`B`,"Why should you avoid `page.waitForTimeout()` in production tests?",null,[`It is not supported in Firefox`,`Fixed waits are brittle — too short fails on slow machines, too long wastes time. Playwright's locator.waitFor() and auto-wait assertions adapt to actual timing.`,`It blocks the Node.js event loop`,`page.waitForTimeout() was removed in Playwright 1.30`],1,`Dynamic waits tied to actual DOM state are always preferable to arbitrary delays.`],[`L`,`I`,"What is the difference between `page.evaluate()` and `page.evaluateHandle()`?",null,[`They are identical`,`evaluate() returns a serialised JS value (string, number, object); evaluateHandle() returns a JSHandle — a reference to an object in the browser that is not serialised. Use evaluateHandle when the return value is not serialisable.`,`evaluateHandle() runs in Node.js; evaluate() runs in the browser`,`evaluateHandle() is deprecated`],1,`evaluateHandle is for passing browser-side object references between evaluate calls — useful for complex DOM manipulations.`],[`L`,`I`,"Can `page.evaluate()` access Node.js variables from the test scope directly?",null,[`Yes — evaluate shares scope with the test`,`No — evaluate runs in the browser's JavaScript context, completely isolated from Node.js. Pass variables as the second argument to evaluate().`,`Yes, but only primitives`,`Yes, if the variable is declared with const`],1,`Scope isolation is fundamental — the test is Node.js, evaluate is browser. Bridge them via the args parameter.`],[`L`,`B`,"What is the recommended way to handle browser `alert()` dialogs in Playwright?",null,[`Playwright dismisses them automatically`,`Register a handler with page.on("dialog", dialog => dialog.accept()) BEFORE the action that triggers the dialog`,`Use page.waitForDialog() after the triggering action`,`Dialogs block Playwright and must be avoided`],1,`The dialog event fires synchronously with the browser's dialog — the handler must be registered before the triggering action or the dialog will hang.`],[`L`,`A`,"When is `Promise.all([page.waitForNavigation(), page.click(link)])` necessary vs just `await page.click(link)`?",null,[`Always — click() never waits for navigation`,`When the click triggers a full-page navigation, Promise.all captures it reliably. However, Playwright's auto-wait on locator.click() covers most cases — waitForNavigation is mainly needed for legacy page.click() or when click triggers a delayed navigation.`,`Never — waitForNavigation is deprecated`,`Only in Firefox which doesn't auto-wait`],1,`Modern locator.click() auto-waits for stability and common navigation patterns — waitForNavigation is mainly a legacy pattern, still useful for complex or delayed navigations.`],[`R`,`B`,`You need to test that clicking a link opens a new tab. How do you capture the new page?`,null,[`page.on("popup", handler) captures the new tab — or use const [newPage] = await Promise.all([context.waitForEvent("page"), link.click()])`,`Playwright cannot test new tabs`,`Use page.newPage() to pre-create the tab`,`Use window.open() mock`],0,`context.waitForEvent("page") resolves with the new Page when a popup/new tab opens — combine with the triggering click in Promise.all.`],[`R`,`I`,`You need to test that downloading a file saves correctly. What Playwright API handles downloads?`,null,[`page.waitForDownload() combined with the triggering click — then access download.path() to verify the file`,`page.waitForResponse() with a binary content check`,`page.route() to intercept the download`,`page.evaluate() to check navigator.downloads`],0,`page.waitForEvent("download") (or context.waitForEvent) captures the download — await download.saveAs(path) then verify the file.`],[`R`,`I`,`After a form submission, an email is sent. You need to verify the email contains the right content in a test. What is the cleanest approach?`,null,[`Wait 30 seconds for the email to arrive`,`Use a test email service (Mailhog, Mailosaur, or MailTrap) and make an API call to check the inbox after the form submission — Playwright handles the UI, the API call verifies the email`,`Use page.evaluate() to intercept SMTP`,`Check the database for the email record instead`],1,`Email testing is an integration concern — a test email service with API access is the standard approach for verifying email content in E2E tests.`],[`R`,`A`,`You need to test that the app correctly requests camera permission and shows a preview when granted. How?`,null,[`Mock navigator.mediaDevices in evaluate()`,`Grant the camera permission in context options: { permissions: ["camera"] }, then navigate to the feature. Intercept getUserMedia with addInitScript to return a fake MediaStream to avoid real camera access.`,`Use headless: false so the real camera works`,`Camera permission cannot be tested in Playwright`],1,`Combine grantPermissions with an addInitScript mock of getUserMedia — this tests the permission flow and UI without requiring actual camera hardware.`],[`R`,`B`,`You need to take a screenshot at a specific point in a long test for debugging. What is the API?`,null,[`test.screenshot()`,`await page.screenshot({ path: "debug.png" })`,`await page.capture()`,`test.info().screenshot()`],1,`page.screenshot() captures the current state — pass path to save to disk, or omit for a Buffer. Attach to the report with test.info().attach().`],[`S`,`B`,"A test must interact with a `<select>` dropdown inside an iframe. How do you access it?",null,[`iframes are not accessible in Playwright`,`Get the frame with const frame = page.frameLocator("iframe") or page.frame({ name: "myFrame" }), then use frame's locator methods normally`,`Use page.evaluate to access the iframe DOM directly`,`Use context.frames() and spread into separate pages`],0,`frameLocator("iframe") returns a FrameLocator that scopes all locator calls to the iframe — use it like a normal locator.`],[`S`,`I`,`A test clicks a "Delete" button that shows a native browser confirm() dialog. Without a dialog handler, what happens?`,null,[`Playwright clicks OK automatically`,`The dialog blocks — Playwright times out waiting because the browser is stuck on the modal dialog. Always register page.on("dialog") before actions that trigger dialogs.`,`The dialog is dismissed automatically`,`confirm() dialogs are ignored in headless mode`],1,`Dialog handlers must be registered proactively — Playwright cannot click OK on a dialog that wasn't anticipated.`],[`S`,`A`,"A SPA uses hash-based routing (#/products). `page.waitForURL` with a regex doesn't match. What is wrong?",null,[`Hash routing requires a different API`,`Hash changes don't trigger a new navigation event — they use the hashchange browser event. Use page.waitForURL(/#\\/products/) — Playwright does support hash fragments in URL matching. If still failing, verify the regex escapes the # correctly.`,`Use page.goto with hash fragment separately`,`waitForURL cannot match hash fragments`],1,`Playwright includes the hash in URL matching — double-check the regex syntax and escaping if matches fail.`],[`S`,`I`,`Tests that navigate between 10 pages are slow. Each navigation awaits networkidle. How do you speed them up?`,null,[`Increase workers to run navigations in parallel`,`Replace networkidle waits with element-specific waits — await the element that proves the page is ready rather than waiting for all network activity to cease. networkidle on data-heavy SPAs adds 500ms+ per navigation.`,`Use goto with waitUntil: "commit" everywhere`,`Disable images in context options`],0,`Element-level waits are faster and more accurate — they resolve as soon as the content you need is visible, not when all background activity stops.`],[`O`,`I`,"What does this accomplish, compared to a fixed `waitForTimeout` after a client-side redirect?",`await page.getByRole('button', { name: 'Continue' }).click();
await page.waitForURL('**/checkout/confirm');`,[`It waits specifically until the page's URL matches the given pattern, however long that actually takes — reliable regardless of network speed, unlike a fixed guess-based delay that could be too short (flaky) or too long (slow)`,`It performs the actual navigation itself, replacing the need for a click`,`It only works for full page reloads, not client-side (SPA) route changes`,`waitForURL blocks all other page interactions until the whole suite finishes`],0,`waitForURL is a condition-based wait (matching the ACTUAL state you care about) rather than a fixed-duration guess — the general Playwright philosophy over arbitrary timeouts.`],[`L`,`I`,'What is the difference between `page.goto(url, { waitUntil: "domcontentloaded" })` and the default `"load"`?',null,[`'domcontentloaded' resolves as soon as the initial HTML is parsed, before images/stylesheets/subframes finish loading; 'load' waits for the FULL page (including all subresources) to finish — domcontentloaded is faster but may run before some visual content is ready`,`'domcontentloaded' waits longer than 'load' in every case`,`They are identical aliases for the same event`,`'load' only applies to the first navigation in a test, never subsequent ones`],0,`These map directly to the browser's own DOMContentLoaded and load events — the right choice depends on whether your test needs to wait for every image/asset or just the DOM structure.`],[`O`,`A`,`What does this code handle?`,`const [popup] = await Promise.all([
  page.waitForEvent('popup'),
  page.getByRole('link', { name: 'Open in new tab' }).click(),
]);
await popup.waitForLoadState();
console.log(await popup.title());`,[`A link that opens a NEW browser tab/window (target='_blank') — waitForEvent('popup') captures the new Page object created by that action, letting the test interact with the new tab directly`,`It prevents the popup from opening at all, testing popup-blocker behavior`,`It only works for actual browser alert()/confirm() popups, not new tabs`,`Promise.all here runs the two operations sequentially, one after the other`],0,`New tabs/windows opened via target='_blank' create a fresh Page object — waitForEvent('popup') is the standard pattern to grab a reference to it, run in parallel with the triggering click via Promise.all so neither event is missed.`],[`O`,`I`,"What does `frameLocator()` let you do that a normal `page.locator()` cannot?",`await page.frameLocator('#payment-iframe').getByLabel('Card number').fill('4242424242424242');`,[`It scopes a locator to look INSIDE a specific iframe's document — regular page-level locators cannot see into iframes, since each iframe has its own separate DOM/document`,`It creates a new, separate BrowserContext just for that iframe`,`frameLocator only works for same-origin iframes, never cross-origin ones`,`It waits for all iframes on the page to finish loading before continuing`],0,`An iframe is a nested, independent document — page.locator() alone cannot reach into it; frameLocator() is the bridge that lets you query elements inside that nested document.`],[`S`,`I`,`You're testing a single-page app where clicking the browser's back button should return to a previous filtered list view (not reload the whole app). How do you simulate and verify this in Playwright?`,null,["`await page.goBack();` followed by an assertion on the resulting URL/UI state (e.g., `await expect(page).toHaveURL(...)`) — this simulates the actual browser back button, testing the SPA's client-side routing/history handling",`page.reload() is the correct way to simulate the back button`,`Browser back-button behavior cannot be tested in Playwright; only forward navigation is supported`,`You must manually re-navigate to the exact previous URL string instead of using history`],0,`page.goBack() drives the browser's real navigation history — exactly what a user's back-button click does, which is essential for testing SPA history/routing correctness.`],[`D`,`I`,"A test calls `page.reload()` expecting a filter selection to persist (since it's stored in the URL query string), but the assertion right after fails because the page hasn't finished re-rendering yet. What's the fix?",`await page.reload();
await expect(page.locator('.filtered-item')).toHaveCount(3);`,["Nothing extra is usually needed — `toHaveCount()` already auto-retries/polls until it matches or times out; if it still fails, the real issue is likely that the filter state genuinely is NOT restored after reload, which is a legitimate bug to investigate rather than a timing flake","Add `await page.waitForTimeout(5000)` immediately after reload() to guarantee reliability",`reload() does not exist in Playwright; use goto(page.url()) instead`,`toHaveCount() must always be preceded by an explicit waitForSelector call`],0,`Before reaching for a manual wait, remember web-first assertions already retry — a persistent failure after reload is more likely revealing a real state-persistence bug than a timing issue.`],[`L`,`A`,`What is the precedence order when multiple timeout settings could apply to a single page.goto() call?`,null,[`A per-call {timeout} option always wins over the config-level use.navigationTimeout, which in turn overrides the built-in 30s default`,`The built-in default always wins regardless of configuration`,`use.navigationTimeout always wins over a per-call {timeout}`,`Only test.setTimeout() can control navigation timeouts`],0,`Playwright resolves timeouts from most specific to least specific: an explicit per-call option beats project/global config, which beats the built-in default.`],[`L`,`I`,`What is the difference between page.setDefaultNavigationTimeout() and page.setDefaultTimeout()?`,null,[`They are aliases for the same setting`,`setDefaultNavigationTimeout() only affects goto/reload/goBack/goForward/waitForURL; setDefaultTimeout() affects those AND all other actions/assertions unless overridden`,`setDefaultTimeout() only affects navigation, not actions`,`setDefaultNavigationTimeout() is deprecated and ignored`],1,`Navigation timeout is a narrower override that only applies to navigation-related calls, while the general default timeout is the fallback for everything else.`],[`O`,`I`,`What does the referer option do in this call?`,`await page.goto('/checkout', { referer: 'https://partner-site.example.com' });`,[`It sets the Referer HTTP header sent with the navigation request, letting you test referer-based logic (like affiliate tracking) without a real prior page`,`It rewrites the browser's address bar to show the referer URL`,`It blocks the navigation unless the referer matches an allowlist`,`It has no effect outside of Chromium`],0,`The referer option lets a test simulate arriving from a specific external page, useful for testing analytics or affiliate-attribution logic tied to the Referer header.`],[`L`,`A`,`What is the practical difference between listening for page.on('framenavigated') versus page.on('load')?`,null,[`framenavigated fires whenever any frame (including the main frame) commits to a new URL, including client-side SPA route changes that update history without a full reload, while load only fires once per full page load`,`They fire at exactly the same moment for every navigation`,`load fires more often because it also covers SPA route changes`,`framenavigated only fires for iframe navigations, never the main frame`],0,`framenavigated is tied to URL/frame identity changes and can fire multiple times per page including SPA route transitions, whereas load is a one-shot full-page lifecycle event.`],[`O`,`A`,`What is being tested here?`,`await context.route('**/checkout', route => route.abort());
await page.goto('/cart');
await page.getByRole('link', { name: 'Checkout' }).click();
await expect(page).toHaveURL('/cart');`,[`That the app gracefully handles a blocked navigation request (e.g. showing an error) instead of leaving the user on a broken page; the URL assertion confirms the browser never actually left /cart`,`That the checkout page loads instantly`,`That route.abort() deletes the checkout page from the server`,`That clicking a link with an aborted route retries automatically until it succeeds`],0,`Aborting the route simulates a network-level failure for that navigation, letting you verify the app's failure handling rather than needing a real broken backend.`],[`D`,`A`,`A test occasionally fails with page.goto: net::ERR_ABORTED even though the target server is healthy. What is the most likely cause?`,null,[`A second navigation (e.g. a client-side redirect or another goto/click) started before the first one finished, cancelling it; inspect the app for an unexpected redirect racing with the test's own navigation`,`net::ERR_ABORTED means the DNS lookup failed`,`The test must add { waitUntil: 'commit' } to avoid this error entirely`,`This error only occurs in Firefox`],0,`ERR_ABORTED commonly means the browser cancelled the in-flight navigation because something else, often the app itself, triggered a competing navigation.`],[`L`,`I`,`What does page.goto('data:text/html,<h1>Hi</h1>') return for its Response value?`,null,[`A full Response object with status 200`,`null, since data: URLs are not fetched over the network so there is no HTTP response to represent`,`It throws because data: URLs are unsupported`,`A Response object with status 204`],1,`goto() only returns a Response for real network navigations; data:, about:blank, and similar non-network URLs resolve with a null response.`],[`R`,`A`,`An app shows a native 'leave site?' confirmation (via a beforeunload handler) when navigating away from a form with unsaved changes. How do you handle this in a Playwright test?`,null,[`Register a page.on('dialog') handler; beforeunload prompts are exposed through the same dialog API and must be accepted or dismissed like any other dialog`,`beforeunload dialogs are automatically accepted with no setup needed`,`Playwright cannot navigate away from a page with a beforeunload handler at all`,`Use page.evaluate() to remove the beforeunload listener before every test`],0,`Browser-native beforeunload prompts surface through Playwright's dialog event just like alert/confirm, so the same handler pattern applies.`],[`S`,`A`,`An SSO login flow redirects the browser across three different domains before landing back on your app. What is the most reliable way to assert the final destination in Playwright?`,null,[`await page.waitForURL(/\\/dashboard$/) after triggering the login; this waits out however many redirects occur and only checks the final resting URL, rather than trying to assert on each intermediate hop`,`Assert page.url() immediately after each redirect step manually`,`Use page.goto() to skip directly to the final expected URL`,`Multi-domain redirect chains cannot be tested with Playwright`],0,`For redirect chains you rarely care about (or can reliably predict) every intermediate hop, waiting for the final URL pattern is far more robust than tracking each hop.`],[`O`,`A`,`What does this code inspect?`,`const response = await page.goto('/dashboard');
let r = response;
const chain = [];
while (r) {
  chain.push(r.url());
  r = r.request().redirectedFrom()?.response() ?? null;
}
console.log(chain.reverse());`,[`It walks backward through the chain of server-side redirects that led to the final response, then prints them in original (first-to-last) order`,`It logs every network request made by the page, not just redirects`,`redirectedFrom() only works for client-side JavaScript redirects, so this always logs a single-item array`,`It causes an infinite loop because redirectedFrom() never returns null`],0,`request().redirectedFrom() lets you trace an HTTP redirect chain hop by hop, useful for debugging unexpected extra redirects in a login or tracking flow.`],[`L`,`B`,`When a single-page app changes routes via history.pushState() with no full page reload, does Playwright's load event fire again?`,null,[`Yes, every client-side route change refires load`,`No, load only fires for a true full-document navigation; client-side route changes need to be detected via URL polling (waitForURL) or waiting for new content to appear`,`Only if the SPA framework explicitly calls window.location.reload()`,`It depends on whether the browser is Chromium or WebKit`],1,`pushState() changes the URL and browser history without asking the browser to reload the document, so document-lifecycle events like load simply do not occur again.`],[`O`,`A`,`What is this code waiting for?`,`await Promise.all([
  page.waitForEvent('framenavigated', f => f === page.mainFrame() && f.url().includes('/results')),
  page.getByRole('button', { name: 'Search' }).click(),
]);`,[`A client-side (SPA) route change on the main frame to a URL containing /results, triggered by clicking Search; useful when the app never issues a full network navigation for this transition`,`A full page reload to a URL containing /results`,`An iframe named results to finish loading`,`It always resolves immediately regardless of the predicate`],0,`framenavigated with a predicate lets you catch history-API-driven route changes that other waits might otherwise miss if configured incorrectly.`],[`L`,`I`,`What happens when page.goto() targets a hostname that cannot be resolved (DNS failure)?`,null,[`It resolves normally with a null response, same as a 404`,`It resolves with a Response whose status() is 0`,`It rejects (throws) with an error like net::ERR_NAME_NOT_RESOLVED; unlike an HTTP error status, a connection-level failure causes goto() itself to fail`,`It silently retries indefinitely until the timeout, then resolves with the previous page`],2,`goto() only avoids throwing for a completed HTTP exchange with a non-2xx status; failures before any HTTP response exists (DNS, connection refused, TLS errors) reject the promise.`],[`D`,`B`,`A test fails immediately in CI with page.goto: net::ERR_CONNECTION_REFUSED, but passes locally. What is the most likely cause?`,null,[`The CI environment's dev/preview server was not fully started (or not started on the expected port) before the test suite began navigating`,`ERR_CONNECTION_REFUSED means the test's CSS failed to load`,`Playwright is incompatible with CI environments`,`The baseURL protocol must be ftp:// in CI`],0,`This is one of the most common CI-only failures; the app server needs its own readiness check before Playwright starts issuing real navigations against it.`],[`L`,`A`,`When is the ignoreHTTPSErrors context option relevant to navigation testing?`,null,[`When navigating to a site served over HTTPS with a self-signed or otherwise untrusted certificate (common on internal staging/preview environments); without it, goto() rejects with a certificate error`,`It is required for every HTTPS site, trusted or not`,`It disables all encryption for faster test runs`,`It only affects WebSocket connections, not page navigation`],0,`Real browsers block untrusted certs with a warning page; ignoreHTTPSErrors lets a test bypass that specifically for internal environments where the cert is known-safe but not CA-signed.`],[`O`,`I`,`What is the purpose of this assertion in a navigation smoke test?`,`const response = await page.goto('/pricing');
expect(response.status()).toBeLessThan(400);`,[`It confirms the navigation resulted in a successful or redirect-class response rather than a client/server error, which a bare goto() resolving without throwing would NOT by itself guarantee`,`It confirms the page finished rendering all images`,`It is equivalent to expect(page).toHaveURL()`,`It fails on any 3xx redirect, which is treated as an error here`],0,`Because goto() resolves even for 4xx/5xx responses, explicitly checking the status code is the only way to catch a page-loaded-but-server-error situation in a smoke test.`],[`L`,`I`,`Do multiple page objects created from the same BrowserContext share navigation history or state when one navigates?`,null,[`Yes, navigating one page also navigates all other pages in the context`,`No, each Page has its own independent navigation history and URL, even though they share the context's cookies, storage, and permissions`,`No, and they also do not share cookies`,`Only the first page created in a context can navigate`],1,`Cookies, storage, and permissions are scoped to the BrowserContext and shared across its pages, but navigation state (URL, history stack) belongs to each individual Page/tab.`],[`S`,`I`,`You need to confirm a link with target='_blank' rel='noopener' opens a new tab that has no reference back to the opening page. How do you verify the noopener behavior specifically?`,null,[`Capture the new tab via context.waitForEvent('page') alongside the click, then run await newPage.evaluate(() => window.opener) and assert it is null`,`Just check that a new Page object was created, which alone proves noopener is working`,`Inspect page.url() on the original page after the click`,`noopener cannot be verified through Playwright and requires manual browser testing`],0,`noopener specifically nulls out the child tab's window.opener reference, so directly evaluating that property in the new tab is the precise way to confirm the attribute is working.`],[`O`,`A`,`What is a risk with calling page.close() immediately after starting this navigation, before it resolves?`,`const navPromise = page.goto('/slow-report', { waitUntil: 'load' });
await page.close();
await navPromise;`,[`navPromise will typically reject with an error indicating the target/page was closed mid-navigation, since the page it depended on no longer exists`,`navPromise always resolves successfully regardless of the close() call`,`close() is a no-op while a navigation is pending`,`Playwright automatically delays close() until navigation finishes`],0,`Closing the page out from under an in-flight navigation removes the target the promise was waiting on, so awaiting it afterward typically surfaces a closed-style error rather than a normal Response.`],[`L`,`B`,`Can page.goto() be called a second time on a page that is still mid-navigation from a previous goto() call?`,null,[`No, it throws a navigation-in-progress error`,`Yes, calling goto() again simply starts a new navigation, which supersedes and cancels the still-pending one, whose promise then typically rejects with ERR_ABORTED`,`Yes, but only if the previous goto() used waitUntil: 'commit'`,`No, you must call page.stop() first`],1,`Playwright pages behave like a real browser tab: issuing a new navigation while one is in flight cancels the old one rather than queuing or blocking it.`],[`L`,`A`,`A test intermittently throws 'Execution context was destroyed, most likely because of a navigation' from a page.evaluate() call right after a click. What is happening?`,null,[`The click triggered a navigation, and evaluate() executed in the old document just as it was being torn down for the new one; the fix is to wait for the navigation to settle (e.g. waitForURL or a specific element) before evaluating`,`evaluate() is fundamentally incompatible with any test that involves navigation`,`The context destruction is a Playwright bug with no workaround`,`evaluate() must always be called before any click(), never after`],0,`Any JS execution context tied to a page is torn down the moment that page navigates, so racing an evaluate() call against a click-triggered navigation is a classic source of this error.`],[`L`,`I`,`If page JavaScript calls history.replaceState() to change the URL without navigating, does page.url() reflect the change immediately?`,null,[`No, page.url() only updates after the next full navigation`,`Yes, page.url() reads the frame's current URL, which the History API updates synchronously in the browser, so it reflects the change right away with no navigation event needed`,`Only if page.reload() is called afterward`,`page.url() throws an error until a framenavigated event fires`],1,`The browser updates the address-bar/current-URL state synchronously when History API methods are called, and page.url() simply reads that live state.`],[`R`,`A`,`Your app is restored from the browser's back-forward cache (bfcache) when a user hits the back button, rather than doing a fresh load. How does this complicate a Playwright navigation test?`,null,[`A bfcache restore does not refire the load/DOMContentLoaded events, so tests relying on waitForLoadState after goBack() may resolve based on stale expectations; asserting on visible content is more reliable than load-state waits here`,`bfcache is disabled automatically inside Playwright, so this scenario cannot occur in tests`,`goBack() always forces a full reload, bypassing bfcache entirely`,`bfcache only affects Firefox, so Chromium-based tests are unaffected`],0,`A bfcache restoration resurrects the exact previous document state instead of reloading it, so lifecycle events used for waiting may not fire the way a test author expects.`],[`O`,`I`,`What does calling page.goto('about:blank') accomplish in this teardown snippet?`,`test.afterEach(async ({ page }) => {
  await page.goto('about:blank');
});`,[`Resets the page to a neutral, content-free state between tests, which can help release memory or leftover timers from a heavy SPA without closing and recreating the whole page`,`It clears all cookies and local storage for the context`,`It closes the current page entirely`,`It is required for Playwright to function correctly between tests`],0,`about:blank is a lightweight built-in page with no app code running, useful as a cheap reset point without the overhead of tearing down and rebuilding the whole context.`],[`L`,`A`,`What is the difference between context.setOffline(true) and using page.route() to abort all requests, for testing navigation failures?`,null,[`setOffline(true) simulates a real network-down condition at the browser level so subsequent goto() calls fail as if there is no connectivity at all, while route()-based aborting lets you selectively fail specific requests while keeping the rest of the network functional`,`They are functionally identical in every case`,`route() aborting is faster but less realistic than setOffline`,`setOffline only works for WebSocket connections`],0,`setOffline is a blunt, browser-level network toggle useful for full no-connectivity scenarios, whereas route() interception gives fine-grained control over which specific requests fail.`],[`S`,`I`,`You want to test that a page shows a loading spinner while a slow initial navigation is in progress, without waiting many real seconds in CI. What is a clean approach?`,null,[`Use context.route() to add an artificial delay to the main document or API responses so the spinner has a controllable, short window to appear and be asserted`,`Use page.waitForTimeout(5000) to simulate slowness in the test itself`,`Deploy an artificially slow version of the app just for this test`,`This scenario cannot be tested without a real slow network`],0,`Intercepting and deliberately delaying the relevant response lets you deterministically create a window during which the loading state is visible, without depending on real network conditions.`],[`D`,`I`,`await page.waitForURL(/\\/user.profile/) unexpectedly matches the URL /userXprofile too. What is the mistake?`,null,[`In a regex, an unescaped dot matches any character, not just a literal period; it should be written as /\\/user\\.profile/ to match a literal period`,`waitForURL does not support regex at all`,`The regex needs a dollar-sign anchor to work at all`,`waitForURL always performs exact string matching, ignoring regexes entirely`],0,`This is a classic regex gotcha: an unescaped dot is a wildcard for any single character, so it happily matches characters other than a literal period.`],[`L`,`B`,`Is it valid for a single Page to navigate to a completely different origin than its current URL, e.g. from your app to an external OAuth provider and back?`,null,[`No, a Page is permanently bound to the origin it was first created with`,`Yes, a Page can navigate across origins freely; storage and permissions are scoped per-origin within the context, but the Page object itself is not restricted to one origin`,`Only if the browser is launched with a special disable-web-security flag`,`Cross-origin navigation always requires creating a new context`],1,`Real browser tabs routinely navigate across origins during flows like OAuth, and Playwright Pages model that same behavior; only site-scoped data like cookies and localStorage is partitioned by origin.`],[`O`,`A`,`What is problematic about this code?`,`await Promise.all([
  page.goto('/page-a'),
  page.goto('/page-b'),
]);`,[`Issuing two navigations on the SAME page concurrently is a race; the second goto() effectively cancels the first, so which URL the page ends up on and what the response objects settle to is unreliable`,`This is the correct, idiomatic way to navigate to two pages in parallel`,`It will always navigate to page-a first, then page-b`,`Promise.all requires the two calls to target different pages, or it throws`],0,`A single Page can only be doing one navigation at a time, so firing two at once on the same page is inherently a race condition, not true parallelism.`],[`R`,`I`,`A page uses a meta-refresh tag to redirect after 3 seconds. How should a Playwright test wait for the resulting navigation?`,null,[`await page.waitForURL('/welcome') after the initial goto; it simply waits out the meta-refresh delay before the URL changes, without needing a fixed sleep matched to the delay value`,`Meta-refresh redirects are invisible to Playwright and cannot be waited for`,`Use waitUntil: 'networkidle' on the original goto(), since it already accounts for scheduled meta-refreshes`,`Manually call page.goto('/welcome') again after page.waitForTimeout(3000)`],0,`waitForURL polls until the condition is met however long it takes, naturally absorbing a scheduled delay like a meta-refresh timer without hardcoding the wait duration.`],[`L`,`I`,`What is the default navigation timeout in Playwright if neither a per-call timeout nor a config-level navigationTimeout is set?`,null,[`5 seconds`,`15 seconds`,`30 seconds`,`There is no default; navigation waits indefinitely`],2,`30 seconds is the built-in fallback for navigation-related waits such as goto, waitForURL, and goBack, unless explicitly overridden.`],[`S`,`A`,`Clicking a link on your page actually triggers a file download instead of a normal page navigation. What is the correct way to handle this in a test?`,null,[`Wrap the click together with page.waitForEvent('download') in Promise.all, rather than expecting a normal navigation or Response; a download-triggering link typically does not produce a page navigation at all`,`Call page.goto() on the link's href and then assert a download starts`,`Downloads always throw a navigation error, so the test must catch and ignore it`,`Use waitForURL to detect when the download completes`],0,`A link that triggers a download is a fundamentally different browser event than a page navigation; the download event, not a URL or response, is what should be awaited.`],[`O`,`I`,`What is this pattern demonstrating?`,`try {
  await page.goto('/slow', { timeout: 3000 });
} catch (error) {
  if (error instanceof errors.TimeoutError) {
    console.log('Navigation timed out as expected');
  } else {
    throw error;
  }
}`,[`Deliberately catching and asserting on a navigation timeout, distinguishing Playwright's specific TimeoutError from other unexpected errors that should still fail the test`,`A way to silently ignore all navigation failures`,`Retrying the navigation up to 3 times`,`Converting a synchronous timeout into an asynchronous one`],0,`Checking instanceof errors.TimeoutError lets a test specifically verify that a slow navigation times out as expected, while still surfacing any other kind of failure.`],[`L`,`B`,`Does page.waitForURL() accept a predicate function in addition to strings, regexes, and globs?`,null,[`No, only strings and regexes are supported`,`Yes, you can pass a function that receives the URL as a URL object and returns a boolean, useful for matching on things like specific query parameter values`,`Yes, but only for relative paths, never absolute URLs`,`Predicate functions are only supported in waitForResponse, not waitForURL`],1,`A predicate gives full programmatic control over the match logic, handy when a simple string, glob, or regex cannot express the condition.`],[`O`,`A`,`What condition does this wait for?`,`await page.waitForURL(url => url.searchParams.get('id') === '123');`,[`The current page URL to have a query parameter id whose value is exactly 123, regardless of the rest of the path or other query params`,`Any URL containing the digits 123 anywhere`,`The literal string id=123 to appear in the URL`,`It waits forever, since searchParams is not a property of the URL argument`],0,`waitForURL's predicate receives a parsed URL object, so you can inspect structured parts like searchParams instead of doing fragile string matching.`],[`L`,`B`,`baseURL is set to https://staging.example.com, but a test calls page.goto with a full absolute URL to a different host and lands somewhere unexpected. What is the explanation?`,null,[`baseURL is ONLY prepended to relative paths passed to goto(); a full absolute URL is used exactly as given and completely ignores baseURL`,`baseURL always overrides the domain of any URL passed to goto()`,`goto() throws an error whenever a URL differs from baseURL's host`,`baseURL only applies to the very first navigation in a test`],0,`baseURL is purely a convenience for relative paths; once a fully-qualified URL is supplied, Playwright has no reason or mechanism to rewrite it.`],[`L`,`I`,`Within the same BrowserContext, does localStorage written on one page survive navigating away and back to the same origin?`,null,[`No, localStorage is cleared on every navigation`,`Yes, localStorage is persisted per-origin within a context much like cookies, so returning to the same origin later in the same context still sees the earlier data, unlike sessionStorage which is tied to the specific tab that set it`,`Yes, but only if page.reload() is used instead of goto()`,`localStorage requires an explicit context.storageState() call to persist across navigations`],1,`localStorage is an origin-scoped, context-persistent store, so re-navigating to the same origin within the same context reveals previously written data.`],[`R`,`B`,`How would you verify that clicking a particular link does NOT open a new tab, i.e. it lacks target='_blank'?`,null,[`Click the link, then assert context.pages().length stays at 1 rather than trying to catch a popup event that should never fire`,`There is no way to prove a negative like this in Playwright`,`Assert that page.url() is unchanged, which alone proves no new tab opened`,`Use page.on('popup') and expect it to throw`],0,`Checking the context's page count after the click is a simple, direct way to confirm no additional tab was spawned by the interaction.`],[`S`,`I`,`A 'Next' link paginates search results, and the URL's page query parameter should increment by one each time it is clicked. How do you verify this across 5 clicks?`,null,[`Loop 5 times: click Next, then await expect(page).toHaveURL with a regex for the expected page number each iteration, letting the auto-retrying assertion absorb any navigation timing`,`Click Next 5 times in a row with no waits, then check the final URL once`,`Use page.waitForTimeout(500) between each click before checking the URL`,`Pagination cannot be reliably tested since the URL format may vary`],0,`Asserting after every click, rather than only at the very end, both pins down the exact increment logic and lets toHaveURL's built-in retry absorb the async navigation delay each time.`],[`L`,`A`,`What does request.redirectedTo() return, as the counterpart to request.redirectedFrom()?`,null,[`The next Request in a redirect chain that this request was redirected TO, i.e. traversing the chain forward, or null if this request was the final one`,`The original request before any redirects occurred`,`A boolean indicating whether a redirect happened at all`,`The HTTP status code of the redirect response`],0,`redirectedFrom() walks a redirect chain backward toward the origin; redirectedTo() is the forward-pointing counterpart for walking toward the final destination.`],[`O`,`I`,`What is the risk of this navigation call?`,`await page.goto('/dashboard', { timeout: 0 });`,[`timeout: 0 disables the navigation timeout entirely; if the page never finishes loading, the test hangs indefinitely instead of failing with a clear timeout error`,`It is a syntax error; timeout must be a positive number`,`It sets the timeout to the fastest possible value`,`It behaves identically to the default 30-second timeout`],0,`A timeout of 0 means wait forever, useful in rare controlled situations, but it removes the safety net that turns a hang into an actionable test failure.`],[`L`,`B`,`What does page.goto() return when navigating to a link that causes the browser to download a PDF file directly, with no HTML page rendered?`,null,[`It throws, since there is no page to load`,`It typically resolves with a Response reflecting the PDF's content-type and status; whether the browser renders a built-in viewer or triggers a download depends on the browser, separate from what goto() reports`,`It always returns null`,`It automatically saves the file and returns its local path`],1,`goto() cares about the HTTP response, not what the browser chooses to visually do with that response, so a PDF still produces a normal Response object.`],[`L`,`A`,`A navigation test passes locally over plain HTTP but fails only in a CI pipeline serving the app over HTTPS behind a proxy, with a mixed-content-blocked style console error. What should you check?`,null,[`Whether the app is requesting an insecure http:// subresource from an https:// page; browsers block such mixed content requests, which can stall a resource-dependent load state like networkidle`,`This is always a Playwright configuration bug unrelated to the app`,`ignoreHTTPSErrors will fix any mixed-content issue`,`Switching waitUntil to 'commit' resolves mixed content blocking`],0,`Mixed-content blocking is a browser security feature independent of Playwright; an HTTPS page trying to load HTTP resources has those requests silently blocked, which can hang certain load-state waits.`],[`L`,`I`,`Why might page.waitForLoadState('load') be insufficient proof that a React-style SPA is ready to interact with?`,null,[`The load event only guarantees static assets like scripts, styles, and images have finished downloading; it says nothing about whether client-side JavaScript has finished hydrating or rendering the interactive UI, which often completes afterward`,`load never fires for any SPA`,`load always fires before the HTML is even parsed`,`React apps do not support the load event at all`],0,`Framework hydration and rendering is JS work that happens on top of the browser's own load event, so waiting only for load can leave you interacting with a page before it is functionally ready.`],[`R`,`A`,`You want to verify a deep link like /product/123 renders the correct product directly, without first visiting the home page. What is the right way to test this?`,null,[`await page.goto('/product/123') as the very first navigation in the test, then assert on the rendered product content, validating the route works when entered directly rather than only via in-app navigation`,`You must first goto('/') and then click through to the product to properly initialize the SPA`,`Deep-linking cannot be tested because Playwright always starts from about:blank`,`Use page.evaluate() to manually call the router's navigate function`],0,`Testing the deep link as the very first navigation catches bugs where a route only works after some client-side initialization that a full page load skips.`],[`O`,`B`,`Why might this assertion fail even though the page clearly navigated to the root path?`,`await page.goto('/');
await expect(page).toHaveURL('/');`,[`If baseURL or the app itself normalizes or redirects the root path, the resulting full URL might not exactly equal what a plain-string match expects; using a regex or checking pathname can be more forgiving`,`toHaveURL never accepts relative paths, only full URLs`,`goto('/') is invalid syntax`,`This code can never fail under any circumstances`],0,`toHaveURL with a plain string requires an exact match against the full resolved URL, so subtle differences like trailing slashes or normalization redirects can cause an otherwise obviously-correct assertion to fail.`],[`L`,`A`,`Does the Frame class, as opposed to Page, expose a waitForURL() method for waiting on an iframe's own navigation?`,null,[`Yes, identical to Page.waitForURL()`,`No, waitForURL() is a Page-level method only; to detect an iframe's URL changing you typically need to poll frame.url() yourself or listen for the frame's navigation events`,`Yes, but it requires importing a separate frame-utils module`,`No, and Frame objects have no way to read their current URL at all`],1,`A common gotcha: several convenience waiting methods that exist on Page are not mirrored on Frame, so iframe-specific waiting sometimes needs a more manual approach.`],[`S`,`B`,`An app has a bug causing an infinite redirect loop between two routes. How do you write a test that specifically asserts this failure mode rather than just having the test itself time out unhelpfully?`,null,[`Wrap the goto() call and assert it rejects (or throws a TimeoutError) with a shorter explicit timeout, so the test fails fast with a clear, intentional assertion about the redirect loop`,`There is no way to test for a redirect loop; you must fix the bug first`,`Set waitUntil: 'commit' so goto() succeeds despite the loop`,`Use page.waitForTimeout(60000) and assume failure if it is reached`],0,`Explicitly asserting that the navigation promise rejects with a tight timeout turns an otherwise-confusing hang into a clear, intentional regression test for the redirect-loop bug.`],[`O`,`A`,`What is the purpose of this test?`,`await expect(async () => {
  await page.goto('/broken-redirect', { timeout: 2000 });
}).rejects.toThrow();`,[`It asserts that navigating to a known-broken route fails, times out or errors, within a short explicit timeout, turning a bug regression into a fast, clear assertion instead of a long hang`,`It asserts the page loads successfully within 2 seconds`,`It retries the navigation until it eventually succeeds`,`It is equivalent to expect(page).not.toHaveURL('/broken-redirect')`],0,`Wrapping the navigation in an async function and asserting rejects lets you turn a should-fail scenario into an explicit, fast-failing test rather than relying on an ambient timeout.`],[`L`,`B`,`What does response.request() return, on the Response object returned by page.goto()?`,null,[`The originating Request object for that navigation, useful for inspecting the method, headers, or post data that were actually sent`,`A new, unrelated Request object`,`The Request of whatever page was open before this navigation`,`undefined, since Response objects do not track their request`],0,`Every Response is tied to the Request that produced it, and request() is the accessor for walking from response back to the request that triggered it.`],[`L`,`I`,`A code review flags a test using Promise.all with waitForNavigation() alongside a link click, and suggests removing it. Why is this considered outdated?`,null,[`waitForNavigation() is a legacy pattern; for a locator-based click that triggers a URL change, awaiting the click followed by page.waitForURL(pattern) is clearer and more precise about which navigation you are waiting for`,`waitForNavigation() has been completely removed and the code will throw`,`Promise.all can never be used with click()`,`link.click() already automatically waits for full navigation with no further code needed`],0,`waitForNavigation() waits for whatever the next navigation turns out to be, which is vaguer than explicitly waiting for the specific URL pattern you actually expect via waitForURL.`],[`R`,`I`,`A link is rendered with aria-disabled=true and JavaScript that prevents its default click behavior, rather than being a real disabled anchor. How do you verify clicking it does NOT navigate?`,null,[`Click it, then assert the URL is unchanged after a brief moment; since toHaveURL retries, a short grace period naturally covers any accidental navigation attempt without a fixed sleep`,`It cannot be tested because aria-disabled does not actually prevent clicking`,`Assert that click() throws an error`,`Use toBeDisabled(), which works for any element regardless of tag`],0,`Because a soft-disabled link is still clickable in the DOM sense, the real thing to verify is the absence of a resulting navigation, which an unchanged toHaveURL assertion captures well.`],[`L`,`A`,`Why can't Playwright reliably detect every kind of soft navigation, i.e. SPA route change, automatically in the general case?`,null,[`Different client-side routing libraries implement route changes differently; some update the URL via the History API which is detectable, while a poorly-behaved one might change only in-memory UI state without touching the URL at all, which no navigation-tracking API can observe`,`Playwright detects 100 percent of all route changes with zero exceptions, so this is a non-issue`,`Soft navigations always trigger a full framenavigated plus load pair identical to hard navigations`,`Client-side routing is disabled by default when running under Playwright`],0,`Playwright's navigation detection ultimately hooks into browser-level signals like the History API and document lifecycle; if an app changes views without touching the URL, there is no navigation-level signal to observe.`],[`O`,`I`,`What is this loop testing?`,`for (const lang of ['en', 'fr', 'de']) {
  await page.goto(\`/?lang=\${lang}\`);
  await expect(page.getByRole('heading')).toContainText(translations[lang].title);
}`,[`That each locale query-parameter value correctly drives the rendered page content into the corresponding language, by re-navigating with a fresh goto() for every locale`,`That the page automatically detects the browser's language without any query parameter`,`That only the English locale is supported`,`It tests navigation speed across different locales`],0,`Because each locale needs an independent full page load to apply, looping through fresh goto() calls, rather than one navigation, is the correct way to validate several locale variants.`],[`S`,`A`,`A framework's client-side router prefetches the target page's data as soon as a link is hovered, before it is even clicked. This confuses a test's page.waitForResponse() call set up right before the click. What is happening?`,null,[`The prefetch request may already be in flight or even completed by the time waitForResponse is registered right before the click, causing the wait to miss it or match a stale request; registering the wait before any hover that could trigger prefetching is safer`,`waitForResponse cannot be used with any framework that supports prefetching`,`The fix is always to disable prefetching entirely in every test`,`Playwright automatically ignores prefetch requests`],0,`Prefetch-on-hover features quietly move network activity earlier in the interaction timeline than the actual click, which can violate the assumption that click triggers the request being watched for.`],[`L`,`B`,`What is the difference between page.reload({ waitUntil }) and page.goto(page.url(), { waitUntil })?`,null,[`They are functionally very similar in that both revisit the current page, but reload() is semantically clearer and guaranteed to be treated as a true reload rather than potentially being optimized by the app's router as navigating to the same URL`,`goto(page.url()) always throws an error since navigating to the current URL is invalid`,`reload() clears cookies while goto(page.url()) does not`,`They behave identically in absolutely every scenario with zero distinction`],0,`Some SPA routers specifically special-case navigating to the URL you are already on as a no-op, so an explicit reload() is a more reliable way to force an actual fresh load.`],[`L`,`B`,`A test calls page.goto('/dashboard') with no baseURL configured, and gets a Protocol error about navigating to an invalid URL. What is missing?`,null,[`A relative path like /dashboard needs either a configured baseURL in playwright.config, or a fully-qualified URL including protocol and host passed directly to goto()`,`goto() requires the URL to be percent-encoded manually first`,`This error means the target server is down`,`Relative paths are never supported by goto() under any configuration`],0,`Without a baseURL to resolve against, a bare path like /dashboard is not a URL the browser can navigate to at all.`],[`L`,`I`,`Why is it considered bad practice to build a page.goto() URL from unsanitized dynamic test data, e.g. directly interpolating an environment variable of unknown origin?`,null,[`It can lead to tests navigating to unexpected or unintended URLs, including internal or staging endpoints not meant to be hit, or malformed URLs that break the test in confusing ways; the same care given to dynamically constructed URLs in application code applies to test code`,`goto() automatically sanitizes and validates every URL before navigating, making this a non-issue`,`This only matters for API testing, never for browser navigation`,`Playwright blocks any URL containing special characters by default`],0,`Test code that builds URLs from loosely-controlled inputs is just as susceptible to the garbage-in-confusing-failure-out problem as application code.`],[`R`,`B`,`A site redirects requests for /page with no trailing slash to /page/ with a trailing slash via a 301. How do you assert this canonicalization behavior in a test?`,null,[`Navigate to the non-canonical form and assert the browser ends up on the canonical, trailing-slash URL via toHaveURL with a regex anchored on the trailing slash`,`This kind of redirect cannot be observed by Playwright since it happens at the server level`,`Assert response.status() equals 301 after goto() resolves`,`Use page.goBack() to detect the redirect`],0,`goto() transparently follows server-side redirects, so the way to observe the redirect's effect is simply to check where the browser ultimately ended up.`],[`O`,`A`,`What is the benefit of this setup before running navigation-heavy tests?`,`await context.route(/analytics|tracking-pixel/, route => route.abort());`,[`It prevents unrelated third-party analytics or tracking requests from contributing to network activity, which speeds up and stabilizes any test relying on waitForLoadState('networkidle') by removing noise unrelated to the app's own functionality`,`It blocks the app's own API calls, testing an offline scenario`,`It disables all navigation entirely`,`It has no effect since route only works for navigation requests, not subresources`],0,`networkidle waits for ALL network activity to quiet down, and background analytics beacons are a common, avoidable source of flakiness or slowness for that particular wait strategy.`],[`L`,`A`,`Why might waitUntil: 'load' behave very differently in timing for a fully server-rendered page versus a client-hydrated one, even if both eventually show identical final content?`,null,[`A server-rendered page's load event roughly coincides with meaningful content being visible, while a client-hydrated page's load event fires once the possibly near-empty initial HTML shell and its scripts finish downloading, with actual content appearing only afterward once JS renders`,`waitUntil: 'load' behaves identically regardless of rendering strategy in all cases`,`Server-rendered pages never fire a load event`,`Client-hydrated pages skip the load event entirely`],0,`The load event only measures resource-loading completion, not whether there is meaningful interactive content on screen, and that gap is much larger for client-rendered apps.`],[`S`,`I`,`A cookie-consent banner blocks interaction with the rest of the page until dismissed, and the app under test loads the same way in every test file. What is a clean way to avoid repeating goto-then-dismiss-banner in every single test?`,null,[`Create a shared fixture, or a beforeEach hook, that performs the navigation and dismisses the banner once, then hands back a ready-to-use page to each test, centralizing the setup logic`,`Manually dismiss the banner inside every single test body`,`Disable the banner globally by editing the app's source code just for testing`,`Rely on force clicks everywhere to click through the banner without dismissing it`],0,`Repeated setup boilerplate across many tests is exactly what fixtures and hooks are for; centralizing it also means a banner change only needs a single update.`],[`L`,`A`,`A navigation to a content-heavy page hangs at waitUntil: 'load' because a third-party ad iframe keeps an open long-polling connection indefinitely. What is a good fix?`,null,[`Switch to waitUntil: 'domcontentloaded', which does not wait for every subresource or frame to finish, combined with an explicit wait for the specific app content needed, sidestepping the ad iframe's never-ending activity entirely`,`Increase the timeout to several minutes and wait it out`,`Block all iframes globally, which is guaranteed to be safe for any app`,`Switch to waitUntil: 'networkidle', which is stricter and thus more reliable here`],0,`load waits for every frame's resources including third-party iframes you do not control; targeting a narrower wait signal for the content you actually need avoids being held hostage by unrelated network activity.`],[`L`,`B`,`What does page.goto() return when navigating to an in-page anchor on the current document, like a hash fragment, where no new document is fetched?`,null,[`A full Response identical to a normal page load`,`Typically null or a response tied to no new network fetch, since jumping to a same-document fragment does not trigger a new HTTP request`,`It always throws, since fragment-only navigation is unsupported`,`It re-fetches the entire page from the server unconditionally`],1,`A pure same-document fragment navigation does not involve a new network request, so there is no real HTTP Response for goto() to hand back.`],[`O`,`I`,`Why does this test use waitForURL with a hash-anchored regex instead of asserting on a Response?`,`await page.getByRole('link', { name: 'FAQ' }).click();
await page.waitForURL(/#faq$/);`,[`Clicking an in-page anchor link only changes the URL fragment without a real network navigation, so there is no new Response to check; waiting for the URL to update is the correct and only signal available`,`It is testing that the click was disabled`,`waitForURL never works with hash fragments, so this test is broken`,`This pattern only works in Firefox`],0,`Since fragment-only navigations produce no server round trip, URL-based waiting is the appropriate tool rather than anything response-based.`],[`R`,`A`,`A login redirect preserves a next query parameter so users land back where they intended, e.g. login redirects to dashboard after success. How do you test this specific behavior?`,null,[`Navigate directly to the protected page while logged out, triggering the redirect with the next parameter set, log in, then assert the final URL is the originally intended page, exercising the return-to-intended-page logic specifically`,`Just log in from the bare login page and assert you land on the homepage`,`Manually construct the final URL and goto() it directly, skipping the login form entirely`,`This behavior cannot be meaningfully tested since it depends on server session state`],0,`Starting the test from the protected page, not the login page directly, is what actually exercises the remember-where-I-was-headed redirect logic end-to-end.`],[`L`,`I`,`Can page.route() be used to modify the headers of the actual navigation request itself, not just subsequent API calls?`,null,[`No, route() only applies to subresources, never the top-level document request`,`Yes, route() intercepts the top-level navigation request too, and route.continue with a headers option can add or override headers on the navigation itself before it proceeds`,`Only in Firefox, due to Chromium restrictions on modifying navigation headers`,`Yes, but only for GET requests, never navigations`],1,`route() intercepts requests at the network layer regardless of whether they are the main document or a subresource, so header modification applies equally to both.`],[`S`,`B`,`A protected page redirects to /login whenever the session cookie is missing or expired. How do you simulate an expired session for a test without waiting for real expiry?`,null,[`Use context.clearCookies(), or set an already-expired cookie via context.addCookies, before navigating to the protected page, then assert the redirect to /login occurs`,`Wait in real time for the session's actual TTL to elapse before navigating`,`Manually edit the app's server-side session store from the test`,`This cannot be simulated; only a real expired session triggers the redirect`],0,`Directly manipulating the context's cookie jar lets you deterministically recreate an expired or missing session state instantly, instead of waiting on real time.`],[`O`,`A`,`What is this route handler simulating for the navigation test?`,`await context.route('**/promo-landing', route =>
  route.fulfill({ status: 200, contentType: 'text/html', body: '<h1>Fake Promo Page</h1>' })
);
await page.goto('/promo-landing');`,[`A completely mocked navigation response; the browser never actually reaches a real server for this URL, useful for testing client-side behavior that reacts to arriving on this route without needing a real backend page to exist`,`A redirect from the real promo-landing page to a different URL`,`It blocks navigation to promo-landing entirely`,`It only modifies the response headers, not the body`],0,`route.fulfill() lets you fabricate an entire response, effectively letting you test app behavior tied to a route even if the real page or backend for it does not exist yet.`],[`D`,`I`,`page.goto() times out for a URL that loads fine in a real browser when typed manually. What under-tested possibility should you check?`,null,[`The site may require HTTP Basic Authentication, which pops a native browser credentials prompt that Playwright will not handle automatically unless httpCredentials is configured on the browser or context`,`goto() is fundamentally broken for any URL requiring authentication of any kind`,`The fix is always to increase the timeout further`,`This can only happen with self-signed HTTPS certificates`],0,`A native basic-auth prompt blocks page load much like an unhandled dialog would; it needs its own dedicated context or browser-level configuration rather than a dialog handler.`],[`L`,`A`,`What does the httpCredentials option, set on browser or context launch, accomplish for navigation testing?`,null,[`It supplies username and password for HTTP Basic or Digest authentication automatically, so a goto() to a basic-auth-protected page does not hang on a native credentials prompt`,`It logs into the application's own login form automatically`,`It stores cookies persistently across test runs`,`It disables HTTPS certificate validation`],0,`HTTP Basic Auth is handled entirely at the browser and network layer via a native prompt, so it needs its own dedicated configuration rather than any in-page interaction.`],[`R`,`I`,`A multi-step signup wizard supports resuming any step directly via a URL query parameter for the step number. How do you test that deep-linking to a mid-wizard step works?`,null,[`Navigate directly to the URL for step 3, then assert that step's specific UI is rendered, bypassing steps 1 and 2 entirely to confirm the app correctly initializes state from the URL alone`,`Click through steps 1 and 2 first, then navigate to step 3, to be safe`,`This can only be tested by completing the whole wizard from the start`,`Use page.evaluate() to force the wizard's internal state to step 3`],0,`The whole point of testing a deep link is to confirm the app can bootstrap correctly from just the URL, and skipping the prior steps is exactly what proves that.`],[`L`,`B`,`Why would clicking a real link with a role-based locator generally be preferred over calling page.goto() with the link's href, in an end-to-end test verifying that link works?`,null,[`Clicking exercises the actual UI element and any JS click handlers attached to it, such as analytics or preventDefault-based routing, verifying the real user-facing path, whereas goto() with the href bypasses the link entirely and only proves the destination URL itself is reachable`,`They are functionally identical in every respect, so it never matters which one is used`,`goto() with an href always throws an error`,`Clicking a link is always slower and should be avoided in favor of goto()`],0,`goto() with a copy-pasted href tests the destination, not the link element itself; clicking is what actually validates that the link, as rendered, does what a user expects.`],[`O`,`I`,`What is the benefit of wrapping this navigation and assertion in a named test.step?`,`await test.step('Navigate to checkout and verify cart total', async () => {
  await page.getByRole('button', { name: 'Checkout' }).click();
  await page.waitForURL('**/checkout');
  await expect(page.getByTestId('cart-total')).toHaveText('$49.99');
});`,[`It groups the navigation and its assertion into a single, clearly labeled unit in the HTML report and trace viewer, making it much easier to see which logical phase of the test failed`,`It changes how the navigation itself is executed at the network level`,`It is required syntax for any navigation-triggering click`,`It disables auto-waiting for the wrapped calls`],0,`test.step is purely a reporting and organizational aid; it has no effect on execution semantics, but makes failures in a long test far easier to localize.`],[`S`,`A`,`After submitting a form using the Post/Redirect/Get pattern, you click the browser back button. What should you verify to confirm the app is NOT vulnerable to a duplicate form resubmission on back navigation?`,null,[`Call goBack(), then assert the page shown is the pre-submission form or a safe cached view rather than triggering another POST, and that no duplicate record or side-effect occurred by checking a subsequent state assertion`,`Simply confirm the URL after goBack() matches the form's URL and nothing else needs checking`,`This cannot be verified through the browser and needs a server log inspection`,`Call page.reload() instead of goBack() to properly test this`],0,`PRG exists specifically to prevent a page reload or back-navigation from resubmitting a form, so the test should confirm both the visual outcome and that no unintended duplicate action occurred.`],[`L`,`B`,`An assertion on the URL path intermittently fails because analytics adds a query string to the URL shortly after load. What is the most robust fix?`,null,[`Anchor the regex on the pathname only rather than the very end of the full URL string, or match against the pathname of a parsed URL object directly, so trailing query params do not break the match`,`Disable the analytics script entirely for every test run as the only fix`,`Switch to an exact string match, which is more forgiving of appended query params`,`This is unfixable; toHaveURL cannot ignore any part of a URL`],0,`A dollar-sign anchor requires the match to reach the literal end of the string, so any later-appended query string breaks it; matching just the meaningful part sidesteps that fragility.`],[`L`,`I`,`When expect(page).toHaveURL() is given a regex without anchors, what part of the current URL does it test against?`,null,[`Only the pathname, ignoring protocol, host, and query`,`The full, absolute current URL string, including protocol, host, path, and query, so the regex just needs to find a match somewhere within that whole string`,`Only the query string`,`Only the hash fragment`],1,`toHaveURL with a regex tests against the entire absolute URL as one string, so an unanchored pattern can match anywhere within it, including inside the host if you are not careful.`],[`O`,`A`,`What extra capability does this call use, beyond a plain page.goto()?`,`await page.waitForURL('**/results', { waitUntil: 'networkidle' });`,[`waitForURL also accepts a waitUntil option, so beyond just matching the URL pattern it additionally waits for the specified load state on the resulting page, combining URL matching with load-state waiting in a single call`,`waitUntil is ignored by waitForURL and has no effect`,`It performs the navigation itself, rather than just waiting for one`,`It only works for the very first navigation of the test`],0,`waitForURL mirrors several of goto()'s options, including waitUntil, letting you require both the right URL and a specific load state together.`],[`R`,`B`,`After a logout action, you need to verify both that the user is redirected to the homepage AND that authentication cookies are actually cleared. How do you check the second part?`,null,[`Read context.cookies() and assert the session cookie is no longer present, inspecting the context's actual cookie jar directly after the redirect completes`,`Cookies cannot be inspected from a Playwright test`,`Check document.cookie via a getByText locator, since that is the only way to see cookies`,`Assume clearing happened if the URL redirect succeeded, without checking cookies directly`],0,`context.cookies() gives direct, structured access to the actual cookie jar, which is the precise way to confirm a specific cookie was really removed rather than just inferring it from a redirect.`],[`L`,`A`,`What is the effect of context.clearCookies() on a subsequent page.goto() to a session-protected route?`,null,[`It removes the context's cookies including any session cookie, so the next navigation to a protected route behaves as if the user is logged out, often triggering an auth redirect exactly like a real expired or cleared session would`,`It only clears cookies set by third-party domains, never first-party session cookies`,`It has no effect until the browser is fully restarted`,`It clears localStorage too, but leaves cookies untouched`],0,`clearCookies() wipes the actual cookie jar the context uses for every subsequent request, which is precisely what determines whether the server treats the next navigation as authenticated.`],[`L`,`A`,`A test asserts toHaveTitle right after a navigation and it flakes occasionally because the client-side router sets the document title slightly after the load event fires. Is a fix even necessary?`,null,[`toHaveTitle() already auto-retries and polls until the title matches or the timeout elapses, so no extra fix should be needed for a slightly delayed title update; if it still fails consistently, the more likely explanation is a real bug worth investigating rather than a timing flake`,`Add a fixed waitForTimeout(1000) before the assertion as the correct fix`,`toHaveTitle() does not retry, so a manual polling loop must be added`,`Replace toHaveTitle() with a direct comparison against page.title(), which is more reliable`],0,`Reaching for a manual wait before a web-first assertion is usually unnecessary and can even mask a genuine bug, since the assertion's own retry behavior already covers small timing gaps.`],[`L`,`B`,`What is context.setExtraHTTPHeaders() useful for in navigation-heavy test suites?`,null,[`Attaching custom headers, like a bypass auth token or a feature-flag header, to every subsequent request the context makes including navigations, useful for skipping a UI login flow when it is not the focus of the test`,`Setting the page's visual theme`,`It only affects requests made via page.request, never actual page navigations`,`It permanently overrides the User-Agent for the whole test run and nothing else`],0,`Because it applies context-wide to every outgoing request including navigations, it is a convenient way to inject something like an auth header without going through a real login UI in every test.`],[`O`,`I`,`What does this setup accomplish before the navigation?`,"await context.setExtraHTTPHeaders({ Authorization: `Bearer ${testUserToken}` });\nawait page.goto('/admin/settings');",[`It authenticates the navigation request itself using a bearer token header, letting the test reach an authenticated route directly without going through a UI login flow first`,`It sets a cookie named Authorization for the session`,`It only affects XHR or fetch calls made after the page loads, not the navigation request`,`It requires the app to be using cookie-based sessions, not token-based auth`],0,`Because setExtraHTTPHeaders applies to every request from the context including the top-level navigation request, this is a common shortcut for skipping repetitive UI login in tests that do not need to test login itself.`],[`R`,`A`,`Your site redirects the root path to a locale-specific path based on the Accept-Language header. How do you test the French redirect specifically?`,null,[`Create a context with locale set to French, which sets the Accept-Language header accordingly, then navigate to the root path and assert the URL contains the French locale segment`,`Manually set a French cookie before navigating, since locale has no effect on headers`,`This requires physically changing the test machine's OS language settings`,`Pass a lang query parameter on the goto() call regardless of the real redirect logic`],0,`The locale context option is specifically designed to drive Accept-Language among other locale-aware behaviors, which is exactly the signal a server-side language redirect typically inspects.`],[`L`,`I`,`Why don't two parallel test workers, each navigating to the same URL at the same time, interfere with one another's navigation state?`,null,[`Each worker runs its own separate browser or at least its own isolated BrowserContext and Page, so navigation state, cookies, and history are completely independent per worker even when hitting the identical URL simultaneously`,`Playwright automatically queues navigations from different workers to run one at a time`,`It only works safely if the worker count is set to one`,`Parallel workers actually do share navigation state, which is why global setup order matters`],0,`Test isolation is achieved by giving each worker and typically each test its own browser context, not by serializing access to shared state, which is exactly what makes safe parallelization possible.`],[`S`,`B`,`You need to verify that navigating to an undefined route renders a custom Not Found page with the correct HTTP status. What is the most complete assertion?`,null,[`Check BOTH the actual HTTP status code from the response AND that the rendered UI shows the correct Not Found heading, since either alone is an incomplete test`,`Only check that the custom 404 heading is visible; the status code does not matter for a good user experience`,`Only check the status code, since the rendered UI is a purely cosmetic detail`,`Assert the current URL exactly equals a fixed 404 path as the sole check`],0,`Checking only the rendered UI can hide a server misconfiguration that returns 200 for missing pages, and checking only the status can miss a broken or missing UI, so a complete test verifies both.`],[`L`,`A`,`Why is checking only response.status() from page.goto() insufficient to catch a page-not-found bug in many single-page applications?`,null,[`Many SPAs serve the same 200-status index.html for every route since client-side routing decides what to render, so an invalid route can still return HTTP 200 even though the visible content correctly shows a not-found state, or incorrectly fails to`,`Every SPA always returns a distinct HTTP status per route by design`,`response.status() is undefined for any SPA route`,`This is only a concern for server-rendered apps, never client-rendered ones`],0,`Client-side routing decouples the HTTP response status, often uniformly 200 for the app shell, from what the router decides to actually render, so a missing route often needs a UI-level assertion rather than a status-code check.`],[`L`,`I`,`A page has an onload handler that calls window.print(), opening the native browser print dialog. This blocks a Playwright test's navigation-completion wait. What is the correct handling?`,null,[`The page.on('dialog') handler does not cover native print dialogs since they are a distinct browser UI, not a JS alert, confirm, or prompt; a more reliable fix is disabling or stubbing window.print via an injected init script before the page loads`,`Register a dialog handler exactly as you would for alert()`,`Print dialogs are automatically dismissed by Playwright with no setup required`,`Use page.keyboard.press('Escape') immediately after goto() to close it`],0,`Not every native browser UI surface is exposed through the same dialog event; window.print() specifically falls outside of it, so a different mitigation is needed.`],[`L`,`B`,`What happens if page.goto('') is called with an empty string while baseURL is configured?`,null,[`It navigates to just the baseURL itself, since an empty relative path resolves to the base, effectively the site's root as configured`,`It always throws a validation error regardless of configuration`,`It navigates to about:blank, ignoring baseURL entirely`,`It is equivalent to page.reload()`],0,`URL resolution treats an empty relative path the same way a browser would treat a relative link with an empty href, resolving right back to the base.`],[`R`,`I`,`A resume-where-you-left-off feature stores reading progress in a URL query parameter. How do you test that reloading the page restores the correct position?`,null,[`Navigate with the progress parameter set, interact if needed, then reload the page and assert the same scroll position or highlighted section is restored, verifying state survives a full page reload rather than only persisting in memory`,`Only test this via a soft SPA route change, since reload() clears all relevant state by definition`,`This cannot be tested since scroll position is a browser-only concept invisible to Playwright`,`Use page.goBack() instead of reload() to test this feature`],0,`A reload wipes any purely in-memory JS state, so successfully restoring the position afterward proves the feature genuinely persists via the URL or storage rather than accidentally relying on memory.`],[`S`,`A`,`Before considering a dashboard ready, a test needs to wait specifically for a GraphQL POST response whose body has a particular operation name, since a generic URL match on the GraphQL endpoint is too broad. How do you express this?`,null,[`Use a predicate function passed to waitForResponse that checks the URL AND parses the request's post data to inspect the operation name, since a predicate can inspect the actual outgoing POST body, not just the URL`,`waitForResponse can only match by URL, so this cannot be done directly`,`Switch to REST endpoints, since GraphQL responses cannot be inspected this way`,`Use waitForRequest instead, since only requests, not responses, expose post data`],0,`A predicate function receives the full Response, and through it the originating Request, letting you filter on details like the GraphQL operation name that a plain URL pattern cannot express.`],[`O`,`A`,`Why is inspecting request.postData() necessary in this waitForResponse predicate, rather than just matching on the URL?`,`await page.waitForResponse(res =>
  res.url().endsWith('/graphql') &&
  JSON.parse(res.request().postData() || '{}').operationName === 'GetDashboard'
);`,[`Because a single GraphQL endpoint typically serves many different operations over the same URL, the operationName inside the POST body is the only way to distinguish which specific query or mutation this response corresponds to`,`postData() is required syntax for any waitForResponse call, regardless of the API style`,`It has no real purpose; the URL check alone would already be sufficiently specific`,`JSON.parse is unnecessary since postData() already returns a parsed object`],0,`GraphQL's single-endpoint design means URL-based matching alone cannot distinguish between different queries or mutations, so you need to look inside the request payload itself.`],[`L`,`I`,`Does a Playwright test author need to manually handle browser-level site isolation or process swapping when a Page navigates across origins?`,null,[`No, Playwright abstracts this away entirely; from the test's perspective a Page is a single logical entity you interact with the same way regardless of what the browser does internally across origin boundaries`,`Yes, a new Page object must be manually created for every distinct origin visited`,`Yes, site isolation must be explicitly disabled via a launch flag before any cross-origin goto() will work`,`No, but only in Chromium; other browsers require special handling`],0,`Internal browser architecture details like process-per-origin isolation are implementation concerns hidden from you, and the Page API stays consistent regardless.`],[`D`,`B`,`Tests that call page.goto() in a beforeEach hook occasionally fail right at the start of a run with connection errors, but never mid-run. What is the likely fix?`,null,[`Configure a webServer entry in playwright.config with a health-check url, so Playwright waits for the dev or preview server to actually be ready before running any test, rather than racing the very first navigation against server startup`,`Add a retry to just the first test in each file`,`Increase every individual test's navigation timeout to 2 minutes`,`Switch beforeEach to beforeAll, which magically avoids the race`],0,`Failures clustered specifically at the start of a run are a classic sign of a startup race between the app server and the very first test, which the webServer readiness check exists to solve.`],[`L`,`A`,`What role does the webServer.url option play in playwright.config, distinct from webServer.command?`,null,[`command tells Playwright how to start the dev or preview server; url tells Playwright where to poll to confirm the server is actually ready to accept requests before any test's first navigation is allowed to run`,`url overrides baseURL for every test in the suite`,`command and url are interchangeable aliases for the same setting`,`url specifies where test result artifacts should be uploaded`],0,`Starting a process and that process actually being ready to serve requests are two different moments in time, and the url readiness check is what bridges that gap safely.`],[`R`,`B`,`A site offers a print-friendly version of an article reachable via a print query parameter, which hides navigation chrome and ads. How would you verify this variant renders correctly?`,null,[`Navigate to the article URL with the print parameter, then assert the navigation and ad elements are NOT present while the core article content still IS visible`,`Navigate to the normal article URL and simulate pressing Ctrl+P, then assert on the print preview`,`This can only be verified with a visual or screenshot diff, never with structural assertions`,`Print-specific query parameters cannot trigger any testable difference in server-rendered HTML`],0,`If the print variant is implemented as a distinct rendering mode, asserting on the presence or absence of specific structural elements is a fast, reliable way to verify it.`],[`O`,`I`,`What is a subtlety of using waitUntil: 'commit' together with reading the resulting Response's status?`,`const response = await page.goto('/report', { waitUntil: 'commit' });
console.log(response.status());`,[`commit resolves as soon as the response headers are received, before the body downloads or the DOM is built, but the status code is already known at that point, so reading status() here is valid and fast; it is the page CONTENT that is not ready yet, not the status`,`response.status() is unavailable until load fires, so this always returns undefined`,`commit never returns a usable Response object at all`,`This code is equivalent to using waitUntil: 'networkidle'`],0,`The HTTP status line arrives with the response headers, exactly the point commit resolves at, so status-code checks work fine even under the earliest wait option.`],[`L`,`I`,`What is a key limitation of relying on waitUntil: 'networkidle' for apps that use periodic background polling (e.g. a live dashboard refreshing every few seconds)?`,null,[`Continuous background requests can prevent the network from ever reaching a quiet window, so networkidle may time out entirely on such apps; a targeted element-level or load-state wait is usually more appropriate`,`networkidle ignores background polling requests automatically, so there is no limitation`,`networkidle only fails on apps using WebSockets, not periodic HTTP polling`,`This limitation only applies to Firefox, not Chromium or WebKit`],0,`networkidle is defined purely in terms of network quiet time, so any app design that never goes quiet for the required window will defeat it regardless of whether the content you care about is actually ready.`],[`L`,`A`,`A goto() call to a route protected by a CSP that disallows framing intermittently fails only when the test runs inside a nested iframe-based test runner UI. What is the likely cause?`,null,[`The target page's Content-Security-Policy frame-ancestors directive is blocking it from being loaded inside another frame, which is a browser security behavior unrelated to Playwright itself and would also affect a real user in the same nested-iframe scenario`,`This is always a Playwright bug when running under any test runner UI`,`CSP headers are stripped automatically by Playwright, so this cannot be the cause`,`Only navigationTimeout, not CSP, can ever block a goto() call`],0,`CSP frame-ancestors is a genuine browser security control, so a failure tied to running inside a nested frame is worth investigating as real CSP behavior rather than assuming it is test-tooling related.`],[`O`,`I`,`What does this test verify about a navigation-triggered download link?`,`const [download] = await Promise.all([
  page.waitForEvent('download'),
  page.getByRole('link', { name: 'Export CSV' }).click(),
]);
expect(download.suggestedFilename()).toBe('report.csv');`,[`That clicking the export link starts a real browser download and that the filename the browser would save it as matches the expected value, without the click producing a normal page navigation`,`That the link navigates to a new page named report.csv`,`That download.suggestedFilename() reads the file contents to guess a name`,`That waitForEvent('download') also waits for the file to finish writing to disk`],0,`suggestedFilename() reflects the name the browser would use to save the file (from the Content-Disposition header or URL), letting a test verify the right file is being offered without ever touching the filesystem.`]],locators:[[`O`,`B`,`What is the recommended first-choice locator strategy in Playwright?`,null,[`page.locator() with a CSS class`,`page.getByRole() using ARIA roles`,`page.getByTestId() for all elements`,`page.locator() with XPath`],1,"`getByRole()` is the top priority because it matches elements the same way screen readers and ARIA do — meaningful, user-centric, and resilient to styling changes."],[`O`,`B`,"What does `page.getByLabel('Email address')` find?",null,[`The <label> element with that text`,`The input or control associated with the label containing "Email address"`,`Any element whose text is "Email address"`,`A form with an aria-label of "Email address"`],1,"`getByLabel()` traverses the label→input association (via `for`/`id`, wrapping, or `aria-labelledby`) and returns the form control, not the label element itself."],[`O`,`I`,"What does `page.getByRole('button', { name: 'Submit' })` find?",null,[`Any element with the CSS class "button" and text "Submit"`,`A button element with accessible name "Submit"`,`A button with id="Submit"`,`An input of type="button" only`],1,"`getByRole()` uses ARIA semantics. The `name` option matches the accessible name — which can come from visible text, `aria-label`, or `aria-labelledby`."],[`O`,`B`,"What HTML attribute does `page.getByTestId('checkout-btn')` look for by default?",null,[`id`,`data-testid`,`aria-label`,`data-pw`],1,"By default `getByTestId()` queries `data-testid`. This can be changed globally via `testIdAttribute` in playwright.config.ts."],[`O`,`I`,"What does `locator.filter({ hasText: 'Sale' })` do?",null,[`Replaces the locator with one that only matches elements containing "Sale"`,`Clicks the first element containing "Sale"`,`Adds "Sale" as a CSS class filter`,`Waits until an element contains "Sale"`],0,"`.filter()` narrows an existing locator set to only those elements that contain the specified text or child locator — without changing the matched element type."],[`O`,`B`,"What does `page.getByAltText('Company logo')` match?",null,[`An element with aria-label="Company logo"`,`An element with alt="Company logo" (typically an <img>)`,`Any element whose visible text is "Company logo"`,`A <figure> element with a caption "Company logo"`],1,'`getByAltText()` finds elements by their `alt` attribute, used primarily on `<img>` and `<input type="image">` elements.'],[`O`,`I`,"What happens when a Playwright locator matches multiple elements and you call `.click()` on it?",null,[`It clicks the first matched element automatically`,`It throws a "strict mode violation" error`,`It clicks all matched elements`,`It waits until only one element matches`],1,"Playwright's strict mode requires a locator to resolve to exactly one element before performing an action. Narrow with `.first()`, `.nth()`, `.filter()`, or a more specific selector."],[`O`,`B`,"What does `locator.nth(2)` return?",null,[`The second element (1-indexed)`,`The third element (0-indexed)`,`The element 2 pixels from the top`,`The element at CSS nth-child(2)`],1,"`.nth()` is 0-indexed. `.nth(0)` = first, `.nth(1)` = second, `.nth(2)` = third, and so on."],[`O`,`I`,"What is the key advantage of `page.getByRole('button')` over `page.locator('.btn')`?",null,[`getByRole is faster to execute`,`getByRole is resilient to CSS class renames; .locator('.btn') breaks if the class changes`,`getByRole only matches visible elements`,`locator() is deprecated in favour of getByRole`],1,`Semantic locators are coupled to meaning, not implementation. CSS class names are implementation details that often change during refactoring.`],[`O`,`I`,"What does chaining `page.locator('.card').getByRole('button', { name: 'Buy' })` do?",null,[`Finds all buttons on the page named "Buy"`,`Searches for a "Buy" button only within elements that match .card`,`Searches for .card elements inside a "Buy" button`,`Throws an error — chaining locators is not supported`],1,`Chaining scopes the inner locator search to the matched set of the outer locator — like a CSS descendant selector but semantic.`],[`O`,`A`,`When is a Playwright locator evaluated against the DOM?`,null,[`At the moment the locator is created`,`Only when an action or assertion is called on it (lazy evaluation)`,`When the test file is compiled`,`After every page navigation`],1,"Locators are lazy by design. Creating `page.getByRole('button')` does nothing — the DOM query runs (and auto-waits) only when you call `.click()`, `.fill()`, `expect(locator).toBeVisible()`, etc."],[`O`,`B`,"What does `page.getByText('Welcome', { exact: false })` do differently from `page.getByText('Welcome')`?",null,[`exact: false ignores whitespace only`,`exact: false performs a case-insensitive substring match; default exact: true requires the full visible text to match`,`exact: false matches by regex`,`exact: false searches all attributes, not just text`],1,'By default `getByText()` does a full visible-text match. `{ exact: false }` matches any element whose text content *contains* "Welcome" (case-insensitive).'],[`F`,`B`,"Fill in to locate a button by its accessible name: `page.______Role('button', { name: 'Login' })`",null,[`getBy`,`findBy`,`selectBy`,`queryBy`],0,"`page.getByRole()` is the primary semantic locator. It takes an ARIA role string and an optional options object."],[`F`,`B`,"Fill in the option to match the accessible name: `page.getByRole('textbox', { ______: 'Email' })`",null,[`name`,`text`,`label`,`title`],0,"The `name` option in `getByRole()` matches the element's accessible name — from visible text, `aria-label`, or `aria-labelledby`."],[`F`,`I`,"Fill in the method to narrow by text: `page.getByRole('listitem').______({ hasText: 'Featured' })`",null,[`filter`,`where`,`narrow`,`find`],0,"`.filter({ hasText })` is the standard way to narrow a locator set. It keeps only elements whose subtree contains the specified text."],[`F`,`B`,"Fill in to get the third match (0-indexed): `page.getByRole('row').______( 2 )`",null,[`nth`,`item`,`at`,`index`],0,"`.nth(n)` is 0-indexed. `.nth(2)` returns the third matched element."],[`F`,`I`,`Fill in the XPath prefix: \`page.locator('______=//button[@type="submit"]')\``,null,[`xpath`,`xp`,`query`,`path`],0,"Playwright uses the `xpath=` prefix to distinguish XPath expressions from CSS selectors inside `page.locator()`."],[`F`,`B`,"Fill in to get the last matching element: `page.getByRole('option').______( )`",null,[`last`,`end`,`final`,`tail`],0,"`.last()` is a shorthand for `.nth(-1)` or `.nth(count - 1)` — it returns the final element in the matched set."],[`F`,`A`,"Fill in to combine two conditions: `page.getByRole('button').______(page.getByTitle('Download'))`",null,[`and`,`also`,`with`,`plus`],0,"`.and(locator)` creates a new locator that matches elements satisfying *both* conditions — the role constraint AND the title constraint."],[`E`,`B`,"What is wrong with this locator?\n`page.getByRole('button', { text: 'Submit' })`",null,["The option key should be `name`, not `text`",`ARIA role "button" is not valid`,"`getByRole` does not accept an options object",`Accessible names must be uppercase`],0,"`getByRole()` uses `{ name: '…' }` to filter by accessible name. `{ text }` is not a recognised option and will be silently ignored, matching all buttons instead."],[`E`,`I`,"What is wrong with this XPath?\n`page.locator('xpath=/html/body/div/button')`",null,["Absolute XPath from the root is fragile — use relative `xpath=//button` instead",`XPath must not start with xpath=`,"`/html/body` is not valid XPath",`page.locator() does not support XPath`],0,"Absolute XPaths break whenever any ancestor element changes. Always use `//` to write relative XPath expressions."],[`E`,`I`,"A locator matches 5 `<li>` elements. `await locator.click()` is called. What error occurs?",null,[`Strict mode violation: locator matched 5 elements — narrow it to one`,`It clicks all 5 elements sequentially`,`It logs a warning but clicks the first`,`It automatically waits until only 1 matches`],0,"Playwright's strict mode throws when an action locator resolves to more than one element. Use `.first()`, `.nth()`, `.filter()`, or a more specific selector."],[`E`,`I`,`What is wrong?
\`await page.getByText('Add to cart', { exact: true }).click()\`
when the button text is "Add to cart →"?`,null,[`exact: true requires the full visible text to match — "Add to cart" won't match "Add to cart →"`,`getByText cannot target buttons`,`exact: true is the default and cannot be changed`,`The arrow character is invalid in text matchers`],0,'With `exact: true` (the default), the entire visible text must match. Since the button says "Add to cart →", the locator finds nothing and times out. Use `{ exact: false }` or include the arrow.'],[`E`,`A`,"What is problematic about this locator for selecting the third product?\n`page.locator(':nth-child(3)')`",null,["`:nth-child(3)` selects by DOM position — if items are reordered, the wrong element is targeted",`CSS nth-child is not supported in Playwright`,"`:nth-child(3)` selects the parent, not the child",`The colon prefix is invalid in page.locator()`],0,"`:nth-child()` is a CSS structural pseudo-class that relies on DOM position. Use `page.getByRole('listitem').nth(2)` which is semantic and 0-indexed."],[`E`,`I`,"What is wrong?\n`const el = await page.$('.submit-btn'); await el.click()`",null,["`page.$()` is deprecated — use `page.locator('.submit-btn').click()` instead","`.click()` cannot be called on an ElementHandle","`await` is not valid on `page.$()`",`CSS selectors are not supported on page.$()`],0,"`page.$()` returns an ElementHandle, which is a deprecated API. Modern Playwright uses locators, which auto-wait and re-query the DOM on every action."],[`D`,`B`,`\`page.getByRole('button')\` matches 4 buttons and the test throws a strict mode error. How do you target just the "Add to cart" button?`,null,["`page.getByRole('button', { name: 'Add to cart' })`","`page.getByRole('button').first()`","`page.locator('button:first-child')`","`page.getByRole('button').filter({ count: 1 })`"],0,"Adding `{ name: 'Add to cart' }` to `getByRole()` filters by accessible name, resolving the ambiguity without needing positional tricks."],[`D`,`I`,"A test breaks after a CSS refactor renamed `.user-name` to `.profile-name`. What is the most resilient replacement?",null,["`page.getByTestId('user-name')` or `page.getByRole('heading', { name: … })` depending on the element","`page.locator('.profile-name')`","`page.locator('div > span.name')`","`page.locator('xpath=//span[@class]')` "],0,"Semantic locators (`getByRole`, `getByLabel`, `getByTestId`) don't depend on CSS class names and survive styling refactors. CSS locators must be updated whenever classes change."],[`D`,`I`,`A list has 10 "Delete" buttons. The test must delete the item whose row contains "Invoice #42". What is the correct approach?`,null,["`page.getByRole('row').filter({ hasText: 'Invoice #42' }).getByRole('button', { name: 'Delete' }).click()`","`page.getByRole('button', { name: 'Delete' }).nth(42).click()`","`page.getByText('Invoice #42').getByRole('button').click()`",`\`page.locator('[data-invoice="42"] button').click()\``],0,"Chain `.filter({ hasText })` to scope the row, then `.getByRole('button')` inside it. This is both semantic and resilient to list reordering."],[`D`,`A`,`\`page.getByRole('listitem').nth(3)\` keeps selecting the wrong product after the team adds a "Featured" item at the top. How do you fix it?`,null,["Use `.filter({ hasText: 'product name' })` instead of `.nth()` to select by content rather than position",`Increment nth to 4`,"Use `.nth(-1)` to count from the end",`Add a CSS class to the 4th item`],0,"Position-based selectors (`nth`) break when items are inserted or reordered. Content-based selectors (`.filter({ hasText })`) are stable regardless of order."],[`D`,`I`,"A developer writes `page.getByLabel('Email')` but the input has no `<label>` — it only has `placeholder=\"Email\"`. The locator finds nothing. What is the fix?",null,["Replace with `page.getByPlaceholder('Email')`","Use `page.locator('input')`","Add `{ exact: false }` to getByLabel","Use `page.getByRole('textbox')`"],0,"`getByLabel` requires a `<label>` element or `aria-label`. When only a placeholder exists, `getByPlaceholder()` is the correct semantic alternative."],[`D`,`B`,"A test creates the locator `const btn = page.getByRole('button', { name: 'Save' })` at the top and reuses it after a modal re-renders the button. Does this cause stale element errors?",null,[`No — Playwright locators re-query the DOM on every action, so re-rendered elements are found fresh`,`Yes — locators cache the element reference and go stale after re-render`,`Yes — the locator must be recreated after any DOM change`,`No — but only in Chromium; Firefox uses stale references`],0,"Unlike `ElementHandle`, Playwright locators are lazy and re-evaluate the query each time an action is performed. Stale element errors are an `ElementHandle` problem, not a locator problem."],[`C`,`B`,"Complete to find the H1 heading: `page.______Role('heading', { level: 1 })`",null,[`getBy`,`findBy`,`locateBy`,`selectBy`],0,"`page.getByRole('heading', { level: 1 })` finds `<h1>` elements using ARIA semantics. The `level` option maps to `aria-level`."],[`C`,`I`,"Complete to narrow a list: `page.getByRole('listitem').______({ hasText: 'Featured' })`",null,[`filter`,`where`,`narrow`,`reduce`],0,"`.filter({ hasText })` reduces the matched set to only elements whose subtree contains the given text."],[`C`,`I`,"Complete to scope the search: `page.locator('.sidebar').______Role('link')`",null,[`getBy`,`findBy`,`selectBy`,`locateBy`],0,"`locator.getByRole()` chains a semantic locator inside the parent. The search is scoped to descendants of `.sidebar`."],[`C`,`I`,"Complete to pick the second item (0-indexed): `page.getByRole('row').______( 1 )`",null,[`nth`,`item`,`at`,`eq`],0,"`.nth(1)` is 0-indexed, returning the second matched element. `.nth(0)` = first, `.nth(1)` = second."],[`C`,`A`,"Complete to find a button that ALSO has a title attribute: `page.getByRole('button').______(page.getByTitle('Save changes'))`",null,[`and`,`also`,`plus`,`with`],0,"`.and(locator)` intersects two locators — the result matches elements satisfying both the role condition and the title condition simultaneously."],[`L`,`B`,`What is the recommended priority order for Playwright locator strategies?`,null,[`getByRole → getByLabel → getByText → getByTestId → CSS → XPath`,`getByTestId → CSS → getByRole → getByLabel`,`CSS → XPath → getByRole → getByLabel`,`getByText → getByRole → CSS → XPath`],0,`Playwright's official guidance: role → label → placeholder → text → alt → title → testId → CSS/XPath. Higher-priority strategies are more resilient to refactoring.`],[`L`,`I`,"When should you prefer `getByTestId()` over `getByRole()`?",null,[`Always — testId is the most stable`,"When the element has no meaningful ARIA role or natural accessible name and a `data-testid` is added specifically for testing",`When the element is hidden`,`When running tests in Firefox only`],1,"`getByTestId` is a deliberate test hook for elements that lack a natural role or label — e.g. complex custom components. Prefer semantic locators when available."],[`L`,`I`,"What is the difference between `.filter({ hasText })` and chaining `parent.getByRole()`?",null,[`filter() narrows the matched set by a text condition; chaining scopes the inner search within the parent's subtree`,`They are identical`,`filter() is faster; chaining is more readable`,`Chaining only works with getByRole; filter works with all locators`],0,`\`.filter()\` post-processes an existing set (e.g. "of these rows, keep only those containing 'Sale'"). Chaining creates a fresh search scoped to the parent's DOM subtree.`],[`L`,`A`,`Are Playwright locators evaluated eagerly (at creation) or lazily (at action time)?`,null,[`Eagerly — the DOM query runs when the locator is created`,`Lazily — the DOM query runs only when an action or assertion is performed`,`It depends on whether the element is in the viewport`,`Eagerly for CSS selectors; lazily for semantic locators`],1,"All Playwright locators are lazy. Creating `page.getByRole('button')` does zero DOM work. The query runs (and auto-waits) the moment you call `.click()`, `.fill()`, or `expect(…)`."],[`L`,`I`,"What does `.and(anotherLocator)` return?",null,[`A locator that matches elements satisfying BOTH conditions`,`A locator that matches elements satisfying EITHER condition`,`An array of all matched elements`,`A boolean indicating whether both locators match the same element`],0,'`.and()` is an intersection — equivalent to a CSS multi-selector like `button[title="Save"]`. Both conditions must be true for an element to match.'],[`R`,`B`,'A form has an "Email address" label linked to an `<input>`. What is the best locator?',null,["`page.getByLabel('Email address')`",`\`page.locator('input[type="email"]')\``,"`page.getByPlaceholder('Email address')`","`page.locator('#email')`"],0,"`getByLabel()` uses the label→input association — exactly how a real user navigates the form. It survives input type changes and id renames."],[`R`,`I`,'An image gallery has 10 images each with unique `alt` text. How do you locate the image with alt "Golden Gate Bridge"?',null,["`page.getByAltText('Golden Gate Bridge')`",`\`page.locator('img[alt="Golden Gate Bridge"]')\``,"`page.getByRole('img', { name: 'Golden Gate Bridge' })`",`Both A and C are correct`],3,"Both `getByAltText()` and `getByRole('img', { name })` work — they are functionally equivalent for images. `getByAltText()` is more concise; `getByRole()` is more explicit about the ARIA role."],[`R`,`I`,`A table has 20 rows, each with a "Delete" button. How do you delete the row for "Alice Johnson"?`,null,["`page.getByRole('row').filter({ hasText: 'Alice Johnson' }).getByRole('button', { name: 'Delete' }).click()`","`page.getByRole('button', { name: 'Delete' }).nth(2).click()`","`page.getByText('Alice Johnson').locator('../button').click()`",`\`page.locator('tr:has-text("Alice Johnson") button').click()\``],0,`Filter the row by content, then locate the action button within it. This is semantic, readable, and resilient to row reordering.`],[`R`,`A`,`A dynamic list reorders items on every page load. A test must always select the item with the text "Priority Task". What locator is correct?`,null,["`page.getByRole('listitem').filter({ hasText: 'Priority Task' })`","`page.getByRole('listitem').nth(0)`","`page.locator('li:first-child')`","`page.getByRole('listitem').first()`"],0,"Content-based filtering is stable regardless of render order. Position-based selectors (`nth`, `first`, CSS `:first-child`) are fragile in dynamic lists."],[`R`,`I`,'An `<input>` has no associated label but has `placeholder="Search products"`. What is the most appropriate locator?',null,["`page.getByPlaceholder('Search products')`","`page.locator('input')`","`page.getByRole('textbox')`","`page.getByTestId('search')`"],0,"`getByPlaceholder()` is designed exactly for unlabelled inputs. It's more specific than `getByRole('textbox')` when there are multiple textboxes on the page."],[`S`,`I`,"A test uses `page.locator('.btn-primary')` to find the Submit button. After a rebrand, the class changes to `.btn-accent` and the test breaks. What is the resilient fix?",null,["`page.getByRole('button', { name: 'Submit' })` — tied to accessible name, not CSS class","`page.locator('.btn-accent')`","`page.locator('button')`","`page.getByText('Submit')`"],0,'CSS classes are implementation details. `getByRole` with `name` is coupled to the semantic meaning — it survives any CSS refactor as long as the button label stays "Submit".'],[`S`,`A`,`A product card component contains: a heading, an image, a price, and two buttons ("Add to cart" and "Wishlist"). How do you reliably click "Wishlist" for the second card?`,null,["`page.locator('.product-card').nth(1).getByRole('button', { name: 'Wishlist' }).click()`","`page.getByRole('button', { name: 'Wishlist' }).nth(1).click()`","`page.locator('.product-card:nth-child(2) .wishlist-btn').click()`",`B and A both work — they are equivalent`],1,`Option A is safer: scoping into the card first avoids ambiguity if there are "Wishlist" buttons elsewhere. Option B works only if there are exactly 2 Wishlist buttons total.`],[`S`,`I`,`A test \`await page.getByRole('button').nth(3).click()\` breaks when a "Back to top" button is added to the page header, shifting all button indices. What is the fix?`,null,["Use content-based filtering: `page.getByRole('button', { name: '…specific name…' })`",`Increment nth to 4`,"Scope with `page.locator('form').getByRole('button').nth(3)`","Pass `--skip-header` to the test runner"],0,`Positional selectors are fragile. Identify the button by its accessible name or scope the locator to the relevant container so unrelated buttons outside the scope don't shift the index.`],[`S`,`A`,`A modal dialog has a "Confirm" button and the page background also has a "Confirm" button (now disabled behind the overlay). \`getByRole('button', { name: 'Confirm' })\` throws a strict mode error. How do you fix it?`,null,["`page.getByRole('dialog').getByRole('button', { name: 'Confirm' })`","`page.getByRole('button', { name: 'Confirm' }).first()`","`page.locator('.modal button')`","`page.getByRole('button', { name: 'Confirm' }).filter({ visible: true })`"],0,"Scoping into `getByRole('dialog')` limits the search to the modal's subtree, resolving the ambiguity cleanly and semantically."],[`O`,`B`,"What does `locator.count()` return?",null,[`A locator representing a count element`,`A Promise<number> — the number of elements matching the locator at the time of the call`,`A synchronous count of matching elements`,`Always 1 in strict mode`],1,`locator.count() resolves with the integer count of matching elements — useful for asserting list lengths with expect(locator).toHaveCount().`],[`O`,`I`,"What does `locator.allTextContents()` return?",null,[`The concatenated text of all matching elements`,`A Promise<string[]> — an array of text contents for all matching elements`,`The HTML of all matching elements`,`A locator filtered by text content`],1,`allTextContents() is useful for asserting all items in a list without looping — compare the whole array with toEqual().`],[`O`,`B`,'What does `page.getByText("Submit")` match by default?',null,[`Only elements with exact text "Submit"`,`Elements whose visible text contains "Submit" as a substring`,`Only button elements with "Submit" text`,`Elements with aria-label "Submit"`],1,`getByText uses substring matching by default — use { exact: true } to require an exact full-text match.`],[`O`,`I`,"What is the purpose of `locator.locator(childSelector)`?",null,[`Creates a completely independent new locator`,`Scopes a new locator search within the matched elements — finds childSelector only inside elements matched by the parent locator`,`Filters the parent locator by the child selector`,`Is equivalent to page.locator(parentSelector + " " + childSelector)`],1,`Chaining locators creates a scoped search — actions on the chain can only match elements inside the parent's DOM subtree.`],[`O`,`B`,'What does `page.getByRole("heading", { level: 1 })` match?',null,[`Any heading element`,`Only h1 elements (or elements with role="heading" and aria-level="1")`,`All headings sorted by level`,`h1 elements that are visible`],1,`The level option maps directly to h1-h6 — level: 1 targets h1 elements.`],[`O`,`I`,'What does `.filter({ has: page.getByRole("img") })` do to a list locator?',null,[`Removes list items that contain images`,`Keeps only the elements from the matched set that CONTAIN a descendant matching the nested locator`,`Returns only the image elements within the list`,`Filters elements that ARE images`],1,`has: option keeps parent elements that have at least one child matching the nested locator — scoped containment filtering.`],[`O`,`I`,"What does `locator.elementHandle()` return and when should you avoid it?",null,[`A synchronous DOM element reference`,`A Promise<ElementHandle> — a reference to the specific DOM element at that moment. Avoid it because ElementHandles are point-in-time snapshots that can become stale if the DOM changes. Use locators instead.`,`A promise that always returns null in strict mode`,`A serialised JSON representation of the element`],1,`ElementHandles are legacy — they don't auto-retry like locators. Prefer the locator API for everything possible.`],[`O`,`B`,'What does `page.getByRole("checkbox", { name: "Accept terms" })` require the checkbox to have?',null,[`An id="accept-terms" attribute`,`An accessible name of "Accept terms" — from an associated label, aria-label, or aria-labelledby`,`A class of "accept-terms"`,`A value attribute of "accept-terms"`],1,`getByRole with name uses the accessible name — which comes from label association, not HTML attributes like id or class.`],[`O`,`I`,'What is the difference between `getByText("Price")` and `getByLabel("Price")`?',null,[`They are identical`,`getByText matches any element whose visible text contains "Price"; getByLabel matches the form control associated with a label whose text is "Price"`,`getByLabel is for select elements only`,`getByText only matches span elements`],1,`getByText is for arbitrary visible text; getByLabel is specifically for form control ↔ label associations.`],[`O`,`A`,"What does `locator.or(otherLocator)` do?",null,[`Returns a locator that matches the union of both — elements matching either the first OR the second locator`,`Returns a locator that matches the intersection`,`Creates a conditional locator that retries with the second if the first fails`,`Appends the second locator's CSS to the first`],0,`or() creates a union — useful when an element can appear in two different forms (e.g. signed-in banner or guest banner) and you want to handle either case.`],[`O`,`I`,'What does `page.locator("text=Submit")` match?',null,[`Only button elements with text "Submit"`,`Any element whose visible text contains "Submit" — equivalent to getByText("Submit")`,`Elements with id="Submit"`,`A Playwright-specific text CSS extension (not CSS standard)`],1,`text= is a Playwright engine prefix for text matching — it is equivalent to getByText(). The semantic locators (getByRole, getByLabel) are preferred over this engine syntax.`],[`O`,`B`,'Can you chain `getByRole()` onto a locator returned by `page.getByRole("dialog")`?',null,[`No — getByRole can only be called on page`,`Yes — locator.getByRole() searches for the role within the parent locator's subtree`,`Yes — but only in Chromium`,`Only if using test.use({ strictMode: false })`],1,`All semantic locator methods (getByRole, getByLabel, getByText, etc.) are available on both page and locator objects — chaining scopes the search.`],[`F`,`B`,'Complete to find an element by its test ID attribute.\n```ts\nconst btn = page.______("submit-button");\n```',null,[`getByTestId`,`getByData`,`locator('[data-testid]')`,`findById`],0,`getByTestId("submit-button") matches elements with data-testid="submit-button" (or the configured testIdAttribute).`],[`F`,`I`,'Complete to locate a row containing specific text.\n```ts\nconst row = page.getByRole("row").______({ hasText: "Alice" });\n```',null,[`filter`,`find`,`where`,`select`],0,`filter({ hasText }) narrows the matched set to elements whose visible text contains the given string.`],[`F`,`B`,'Complete to get the count of list items.\n```ts\nconst count = await page.getByRole("listitem").______();\n```',null,[`count()`,`length`,`size()`,`total()`],0,`locator.count() returns a Promise<number> of matched elements.`],[`F`,`I`,'Complete to get text from all matching elements.\n```ts\nconst texts = await page.getByRole("listitem").______();\n```',null,[`allTextContents()`,`allTexts()`,`texts()`,`getTexts()`],0,`allTextContents() returns a string array — one entry per matched element.`],[`F`,`I`,'Complete to find a button ONLY inside a form.\n```ts\nconst btn = page.getByRole("form").______("button", { name: "Submit" });\n```',null,[`getByRole`,`locator`,`find`,`query`],0,`locator.getByRole() (or any semantic method) scopes the search to the parent — finds Submit only inside the form.`],[`F`,`B`,'Complete to match elements with exact text.\n```ts\nconst el = page.getByText("Log in", { ______: true });\n```',null,[`exact`,`strict`,`full`,`complete`],0,`{ exact: true } requires the full text to match exactly — "Log in" does not match "Log in to continue".`],[`F`,`A`,'Complete to union two locators.\n```ts\nconst banner = page.getByRole("alert").______(page.getByRole("status"));\n```',null,[`or`,`and`,`union`,`merge`],0,`locator.or() creates a union — matches elements satisfying either locator.`],[`E`,`B`,'What is wrong?\n```ts\nconst items = page.getByRole("listitem");\nconsole.log(items.length); // always undefined\n```',null,[`listitem is not a valid role`,`Locators do not have a .length property — they are lazy queries, not arrays. Use await locator.count() to get the number of matching elements.`,`length is async and must be awaited`,`You must call locator.all() first`],1,`Locators are not arrays — they have no length property. Use count() for the integer count or all() to get an array of locators.`],[`E`,`I`,'What is wrong?\n```ts\nconst el = await page.getByRole("button", { name: "Save" }).elementHandle();\nawait page.reload();\nawait el.click(); // stale element\n```',null,[`click() cannot be called on an ElementHandle`,`ElementHandle is stale after page.reload() — the DOM element it referenced was destroyed and recreated. Use locator.click() which re-queries on each action.`,`reload() clears ElementHandles only in Firefox`,`await is not needed for elementHandle.click()`],1,`ElementHandles capture a reference to a specific DOM node — navigation or page reload destroys the node, making the handle stale. Locators re-query on every action.`],[`E`,`B`,'What is wrong?\n```ts\nconst btn = page.getByRole("button");\nawait btn.click();\n// Strict mode violation: multiple elements matched\n```',null,[`getByRole("button") is too broad — it matches ALL buttons. Add a name option or scope it: page.getByRole("button", { name: "Submit" })`,`Strict mode must be disabled to use getByRole`,`click() requires exactly one element always`,`getByRole returns all buttons; use .first()`],0,`Strict mode prevents ambiguous actions — always narrow locators to match exactly one element, either with a name option or by scoping within a parent.`],[`E`,`I`,'What is wrong?\n```ts\nconst locator = page.getByRole("row").nth(2);\nconst text = locator.textContent();\n// text is a Promise, not a string\n```',null,[`textContent() is not available on locators`,`textContent() is async — it returns a Promise<string | null>. Without await, text is a Promise object, not the string value.`,`nth() makes the locator synchronous`,`textContent() needs strict mode off`],1,`All locator value-retrieval methods (textContent, innerText, inputValue, etc.) are async — always await them.`],[`E`,`A`,`What is wrong?
\`\`\`ts
const rows = page.getByRole("row");
for (const row of rows) { // cannot iterate
  await row.click();
}
\`\`\``,null,[`getByRole returns a special iterable in TypeScript`,`Locators are not iterables — use const rowList = await locator.all() to get an array of individual locators, then iterate that array.`,`for...of requires async iteration`,`row.click() must be replaced with await row.evaluate(el => el.click())`],1,`Locators are lazy query objects, not arrays. Use locator.all() to materialise an array of per-element locators.`],[`E`,`B`,'What is wrong?\n```ts\nconst price = page.getByText("$");\nawait expect(price).toHaveText("$29.99");\n```',null,[`toHaveText cannot be used with getByText`,`getByText("$") matches ALL elements containing a dollar sign — this might match multiple elements causing a strict mode violation. Use a more specific locator like getByTestId("price") or scope within the product card.`,`$ is a special regex character and must be escaped`,`toHaveText expects a number`],0,`getByText("$") is too broad for pages with multiple prices — strict mode will throw if more than one element matches.`],[`D`,`B`,'`expect(locator).toHaveCount(5)` fails with "received 0" even though 5 items are visible. What should you check?',null,[`Add await before expect`,`The locator may not match the right elements — log the locator's selector string, check that the role/selector is correct, and verify the items are not inside an iframe or shadow DOM.`,`toHaveCount requires locator.all() first`,`Add visible: true to the locator`],1,`Count mismatches usually mean wrong selector, elements in a different frame/shadow, or elements not yet rendered — inspect the locator match in the Trace Viewer.`],[`D`,`I`,'A test uses `locator.filter({ hasText: "Active" })` but it matches items containing "Inactive" too. Why?',null,[`filter() uses regex by default`,`hasText uses substring matching — "Inactive" contains "Active". Use hasText: /^Active$/ (exact regex) or exact: true to restrict to exact matches.`,`filter() is case-insensitive`,`hasText only matches direct text, not descendants`],1,`Substring matching is the default — use a regex anchored with ^ and $ for exact matching in filter.`],[`D`,`B`,'`page.getByRole("textbox", { name: "Search" })` returns no elements even though a search input is visible. What should you check?',null,[`textbox role requires type="text"`,`The input may be associated with a label whose text differs slightly ("Search…" vs "Search"), or the label association is missing (no for/id). Inspect with the Playwright Inspector or add data-testid as a fallback.`,`getByRole textbox is Chromium-only`,`Inputs inside forms are not accessible by role`],1,`Inspect the input's accessible name in the browser's accessibility tree — even a trailing space or ellipsis prevents a match.`],[`D`,`I`,"`locator.all()` returns an empty array even though elements are visible on screen. What is the likely cause?",null,[`all() is deprecated`,`all() materialises the current DOM state — it does not wait. If called before the elements are rendered, the array is empty. Use waitFor() on the locator first: await locator.first().waitFor(), then call all().`,`all() only returns the first 10 elements`,`all() requires strict mode off`],1,`all() does not auto-wait — ensure elements are present before calling it by waiting for at least one to appear.`],[`D`,`A`,`A locator works in headed mode but fails in headless. What are two things to check?`,null,[`Headed and headless are identical`,`1) Hover-triggered elements (tooltips, dropdowns) may not appear in headless without explicit hover() calls. 2) Viewport size differences — elements may be outside the viewport in headless and hidden. Check visibility and scroll into view.`,`Headless disables ARIA semantics`,`Locators behave differently in headless mode only for Firefox`],1,`Headed tests benefit from automatic scrolling and hover effects triggered by mouse movement — headless requires explicit Playwright actions to achieve the same.`],[`C`,`B`,'Complete to get all matching locators as an array.\n```ts\nconst items = await page.getByRole("listitem").______();\n```',null,[`all()`,`toArray()`,`getAll()`,`elements()`],0,`locator.all() materialises the matched elements into an array of individual locators — does not auto-wait.`],[`C`,`I`,'Complete to locate a paragraph inside a section.\n```ts\nconst para = page.getByRole("region", { name: "Summary" })\n  .______("p");\n```',null,[`locator`,`getByRole`,`find`,`query`],0,`locator.locator(selector) scopes a CSS or semantic selector search within the parent locator's subtree.`],[`C`,`B`,'Complete to get the text content of one element.\n```ts\nconst label = await page.getByTestId("price").______();\n```',null,[`textContent()`,`getText()`,`innerText()`,`content()`],0,`textContent() returns the full text content including hidden text; innerText() returns only visible text (both are async on locators).`],[`C`,`I`,'Complete to filter rows that have an image.\n```ts\nconst imageRows = page.getByRole("row")\n  .filter({ ______: page.getByRole("img") });\n```',null,[`has`,`hasChild`,`contains`,`includes`],0,`filter({ has: nestedLocator }) keeps only elements that contain at least one descendant matching the nested locator.`],[`C`,`A`,'Complete to intersect two locators.\n```ts\nconst activeBtn = page.getByRole("button")\n  .______(page.locator(".active"));\n```',null,[`and`,`filter`,`or`,`intersect`],0,`locator.and() returns elements satisfying BOTH the original locator and the argument — set intersection.`],[`L`,`B`,`Why are CSS class-based locators considered fragile?`,null,[`CSS classes are case-sensitive`,`CSS class names are implementation details that change with refactoring, design system updates, or build-tool hashing — semantic locators (role, label, text) are coupled to user-visible semantics that change far less often.`,`CSS locators are slower than role locators`,`Playwright does not support CSS class locators`],1,`Coupling tests to CSS classes means every UI refactor breaks tests — semantic locators decouple tests from implementation.`],[`L`,`I`,"Can `getByRole()` match custom web components?",null,[`No — web components are black boxes`,`Yes — if the custom component correctly sets its ARIA role (via role attribute or ARIA attributes), getByRole() matches it. Without correct ARIA, use data-testid as a fallback.`,`Only in Chromium which supports web components natively`,`Only if the shadow DOM is open`],1,`ARIA semantics work regardless of whether the element is a native element or a custom component — the key is correct ARIA role assignment.`],[`L`,`I`,"What is the difference between `locator.innerText()` and `locator.textContent()`?",null,[`They are identical`,`innerText() returns the rendered visible text (respects CSS display:none); textContent() returns all text content including hidden elements`,`innerText() is async; textContent() is synchronous`,`textContent() strips HTML tags; innerText() does not`],1,`textContent can include text from hidden elements; innerText respects CSS visibility — use innerText when you want what a user actually sees.`],[`L`,`A`,'When should you use `page.locator("xpath=//table//td[2]")` over semantic locators?',null,[`Always — XPath is the most powerful selector`,`XPath should be a last resort when semantic locators cannot uniquely identify the element — e.g. complex table cell positioning that has no accessible label or test ID. It is brittle and unreadable.`,`XPath is required for tables`,`XPath is faster than role-based locators`],1,`XPath tightly couples tests to HTML structure — a table column reorder breaks the test. Exhaust semantic options first.`],[`L`,`B`,"Does `locator.waitFor()` auto-retry?",null,[`No — it checks once and throws if not found`,`Yes — waitFor() polls until the element matches the state option (visible, hidden, attached, detached) or the timeout expires`,`Yes — but only in Chromium`,`No — use expect(locator).toBeVisible() for auto-retry`],1,`waitFor() is an auto-retrying wait — it is the imperative companion to the declarative expect(locator).toBeVisible() assertion.`],[`R`,`B`,`A navigation menu has both "Home" links in the desktop nav and mobile nav (both in the DOM). A strict mode error occurs. How do you fix it?`,null,[`Use .first() to take the desktop link`,`Scope to the visible nav: page.getByRole("navigation", { name: "Desktop" }).getByRole("link", { name: "Home" })`,`Add data-testid to both and use .nth()`,`Use page.locator("nav:first-child a")`],1,`Scoping by the named landmark (navigation with an accessible name) uniquely identifies which nav you mean — resilient and semantic.`],[`R`,`I`,`A test needs to verify all product names in a list match a pattern. What is the most concise approach?`,null,[`Iterate with a for loop and assert each item`,`Get all names with const names = await page.getByRole("heading", { level: 3 }).allTextContents() then use expect(names).toEqual(expectedNames)`,`Use expect(locator).toHaveText([...]) with an array`,`Both B and C — toHaveText accepts an array and auto-retries, making it more robust than allTextContents()`],3,`expect(locator).toHaveText([...]) is the most idiomatic — it auto-retries and handles async rendering; allTextContents() is useful when you need the values for further processing.`],[`R`,`I`,`A form has many fields. You need to find the field labelled "Date of Birth (DD/MM/YYYY)". How?`,null,[`page.getByLabel("Date of Birth (DD/MM/YYYY)")`,`page.getByLabel(/Date of Birth/)`,`page.getByPlaceholder("DD/MM/YYYY")`,`Any of the above — choose based on whether exact match is needed`],3,`getByLabel with exact string matches precisely; getByLabel with regex is useful when the label text might vary slightly; getByPlaceholder works if the field uses placeholder not label. Choose based on what the element actually has.`],[`R`,`A`,`A data table loads rows asynchronously and the count changes. How do you wait for exactly 10 rows before asserting?`,null,[`await page.waitForTimeout(2000)`,`await expect(page.getByRole("row")).toHaveCount(10) — it auto-retries until the count matches or the timeout expires`,`await page.getByRole("row").nth(9).waitFor()`,`const rows = await page.getByRole("row").all(); expect(rows.length).toBe(10)`],1,`toHaveCount() auto-retries — it is the cleanest way to assert a dynamic list reaches a specific length.`],[`R`,`B`,"How do you verify that a specific option is selected in a `<select>` element?",null,[`await expect(page.getByRole("combobox")).toHaveValue("expected-value")`,`page.getByRole("combobox").selectedIndex`,`expect(await page.getByRole("combobox").evaluate(el => el.value)).toBe("expected")`,`Both A and C — toHaveValue is preferred as it auto-retries`],0,`toHaveValue on a select element asserts the selected option's value with auto-retry — the most idiomatic approach.`],[`S`,`B`,`A test clicks a "Delete" link in a table row. There are 5 Delete links and the test must click the one for "Product A". How do you write this?`,null,[`page.getByRole("row").filter({ hasText: "Product A" }).getByRole("link", { name: "Delete" }).click()`,`page.getByRole("link", { name: "Delete" }).nth(0).click()`,`page.locator("tr:contains('Product A') a").click()`,`page.getByText("Delete").filter({ hasText: "Product A" }).click()`],0,`Filter the row by content then scope the link within — semantically precise and order-independent.`],[`S`,`I`,`After applying a discount, a price element updates from "$100" to "$80". A test asserts toHaveText("$80") immediately after clicking "Apply". It sometimes fails. Why?`,null,[`The price update is asynchronous — toHaveText auto-retries but if the old text is still showing, the timing needs analysis. Check if the element briefly shows a loading state that toHaveText can't match.`,`toHaveText does not auto-retry`,`The $ sign requires escaping in assertions`,`The element must be re-queried after update`],0,`toHaveText auto-retries — brief loading states that replace the price element with a spinner might cause the element reference to become stale. Use a fresh locator after the update if the element is replaced in the DOM.`],[`S`,`A`,"You need to test a complex drag-and-drop reorderable list. `locator.dragTo()` works but the order does not change. What should you investigate?",null,[`dragTo() is not supported for reorderable lists`,`Check that the drag coordinates land in the right drop zone — dragTo() dispatches mouse events but some drag-and-drop libraries require specific drop zones or use pointer events instead. Try using mouse.move() + mouse.down() + mouse.up() manually for precise control.`,`The list must be sorted before drag tests`,`Drag-and-drop requires headless: false`],1,`Library-specific drag implementations vary — when dragTo() fails, manual mouse event sequences give precise control over drag start, move, and drop coordinates.`],[`S`,`I`,'`locator.filter({ hasText: "Sale" })` returns 0 elements but the page visually shows "Sale" tags. What should you check?',null,[`hasText is case-sensitive by default — check if the actual text is "SALE" or "sale"`,`Elements with CSS text-transform: uppercase display differently than their actual text content — hasText matches the actual DOM text, not the rendered display. Also check if "Sale" is in a ::before pseudo-element (invisible to textContent).`,`hasText only works on paragraph elements`,`The filter is applied before the page loads`],0,`text-transform and ::before content are common gotchas — the DOM text is "sale" but CSS renders it as "SALE". Inspect the actual textContent in DevTools.`],[`L`,`A`,"What is the key advantage of Playwright's `getByRole()` locator over a raw CSS selector like `.btn-primary` for locating a submit button?",null,[`getByRole() queries by accessible role and name, matching how assistive technology and real users identify elements — it's resilient to class/style refactors and doubles as an accessibility check`,`getByRole() is faster because it skips the DOM entirely`,`getByRole() only works in Chromium`,`CSS selectors are deprecated in Playwright`],0,`Locating by role+accessible name ties your test to user-facing semantics instead of implementation details like class names, which change often.`],[`O`,`I`,"What does `.first()` do here, and what happens if `.item` matches zero elements when `.click()` runs?",`await page.locator('.item').first().click();
await expect(page.locator('.item')).toHaveCount(5);`,["`.first()` narrows the locator to the first matching element; if zero elements match, the click auto-waits (up to the timeout) for at least one to appear, then throws a timeout error if none ever does","`.first()` returns an array and click() iterates over all matches","`.first()` throws immediately if more than one match exists",`Locators require exactly one match or they silently no-op`],0,"Playwright locators are lazy and auto-retrying — `.first()` just narrows the eventual query; the actual waiting/error happens at the action call."],[`L`,`I`,"Why does Playwright recommend `page.getByTestId('submit-btn')` as a fallback locator strategy when no accessible role/text/label uniquely identifies an element?",null,[`data-testid attributes are explicit test hooks that don't change when visual design, class names, or copy text change, making tests resilient to unrelated refactors`,`getByTestId() is the only locator that supports cross-browser testing`,`Test IDs are required by the HTML5 spec for interactive elements`,`getByTestId() bypasses Playwright's auto-waiting mechanism for faster tests`],0,`Role/text-based locators are preferred first because they mirror real users, but data-testid is a deliberate, stable escape hatch when nothing else uniquely identifies an element.`],[`R`,`I`,`A table has 50 rows, each with a 'Delete' button. You need to click the delete button in the row containing the text 'Invoice #1042'. What's the most robust locator approach?`,null,["`page.locator('tr', { hasText: 'Invoice #1042' }).getByRole('button', { name: 'Delete' })` — scope to the row containing the text, then find the button within it","`page.getByRole('button', { name: 'Delete' }).nth(1042)`","`page.locator('#delete-1042')` — assume the ID always matches the invoice number","`page.locator('tr:nth-child(1042) button')`"],0,"Chaining locators — scoping to the row via `hasText`, then querying within that scope — avoids brittle assumptions about row order or ID naming."],[`L`,`A`,`What is Playwright's recommended locator priority order, from most to least preferred, and why?`,null,[`getByRole (accessible role+name) > getByLabel/getByText (user-visible content) > getByTestId (explicit test hook) > CSS/XPath (implementation detail) — because each step down moves further from how a real user identifies the element and closer to fragile implementation structure`,`CSS selectors are always preferred because they are fastest to write`,`XPath should always be the first choice since it can express the most complex queries`,`Priority does not matter; all locator strategies are equally resilient`],0,`This ordering mirrors 'testing like a user' — the closer a locator strategy is to what a human notices (role, label, visible text), the less likely it is to break on unrelated refactors.`],[`O`,`I`,"What does `.filter()` do here?",`const row = page.locator('tr').filter({ hasText: 'Invoice #1042' });
await row.getByRole('button', { name: 'Delete' }).click();`,['It narrows the `tr` locator down to only the row(s) whose text content includes "Invoice #1042", without needing a specific CSS selector for that row',`It removes all rows containing "Invoice #1042" from the page entirely`,`It filters which BROWSER the locator runs on, not which elements match`,`filter() only works with exact text matches, never partial substrings`],0,`.filter({ hasText / has }) is designed exactly for this "narrow a broad locator down using content or a nested locator" pattern.`],[`O`,`A`,`What does this select?`,`const button = page.getByRole('button', { name: 'Submit' })
  .and(page.locator('.primary'));`,[`Only elements matching BOTH conditions: a button with accessible name "Submit" AND having the CSS class "primary" — an intersection of the two locators`,`Either condition alone is sufficient — it behaves like a logical OR`,`It selects the button first, then separately clicks anything with class .primary`,`and() combines two page objects into one merged page`],0,`.and() performs a logical intersection — useful for disambiguating when a role/name alone matches multiple elements but only one also has a specific distinguishing attribute.`],[`L`,`I`,"What is the difference between `.first()`, `.last()`, and `.nth(2)` on a Locator?",null,[`They narrow a multi-element locator down to a single specific match by position — first (index 0), last (final match), and nth(2) (the third match, zero-indexed) — all still lazy and re-evaluated at action time`,`.first() and .last() return arrays, while .nth() returns a single element`,`nth(2) selects the 2nd match (one-indexed), same as .last() would for a 2-item list`,`These methods immediately execute the query and freeze the result, unlike other locator methods`],0,`All of these remain lazy locators — they narrow the match by position, but the actual DOM query still runs (and retries) at the moment an action or assertion uses them.`],[`L`,`A`,`Why does Playwright's documentation generally discourage XPath locators in favor of role/text/CSS-based ones?`,null,[`XPath expressions tend to encode brittle structural/positional assumptions about the DOM (like exact ancestor paths) and are harder to read/maintain than semantic locators — while occasionally necessary for edge cases, they should be a last resort`,`XPath is not supported at all by the Playwright locator API`,`XPath only works in Chromium, not Firefox or WebKit`,`XPath is slower to execute than every other locator strategy by an order of magnitude`],0,`It's not a technical limitation (Playwright does support XPath) — it's a maintainability recommendation, since XPath commonly encourages exactly the kind of fragile, structure-dependent locators best practices try to avoid.`],[`O`,`A`,"What does `toMatchAriaSnapshot()` verify that a simple `toBeVisible()` check on individual elements does not?",`await expect(page.getByRole('navigation')).toMatchAriaSnapshot(\`
  - link "Home"
  - link "About"
  - link "Contact"
\`);`,[`It asserts the entire accessible structure (roles, names, and their relative order/nesting) of a region matches an expected snapshot — catching accessibility regressions like a missing link, wrong order, or wrong role that individual visibility checks would miss`,`It compares the exact CSS pixel layout of the region against a screenshot`,`It only checks whether the region is visible, identical to toBeVisible()`,`It validates HTML validity against the W3C spec`],0,`ARIA snapshot testing captures the accessibility tree shape as a whole — a powerful way to guard against both visual AND structural/accessibility regressions in one assertion.`],[`R`,`I`,"A one-off need arises to read a raw DOM property Playwright has no built-in locator method for (e.g., a custom `data-computed-hash` set by JS at runtime). What is the appropriate escape hatch, and why should it be used sparingly?",null,["`await locator.evaluate(el => el.dataset.computedHash)` — runs arbitrary JS against the matched element for cases the built-in API doesn't cover, but it bypasses Playwright's auto-waiting/actionability guarantees, so it should be a last resort, not a habit",`Rewrite the entire test in raw Puppeteer instead`,`This is impossible; Playwright cannot read arbitrary DOM properties`,`Use page.content() and parse the returned HTML string manually with regex`],0,`locator.evaluate() is a genuine escape hatch for truly custom needs — but every use trades away the auto-retrying safety net that makes Playwright locators reliable in the first place.`],[`L`,`A`,`Do Playwright's built-in CSS and text-based locator engines pierce into open shadow DOM by default?`,null,[`Yes, CSS and text engines automatically traverse open shadow roots as if they were part of the regular DOM tree, but the XPath engine does NOT pierce shadow DOM at all`,`No locator engine can ever see inside shadow DOM without a special shadow= prefix`,`Only the XPath engine pierces shadow DOM; CSS and text engines stop at the shadow boundary`,`Shadow DOM elements are never actionable in Playwright under any circumstances`],0,`This is a deliberate design choice so that modern component libraries using shadow DOM still work with ordinary getByRole/getByText/CSS locators, while XPath's spec-defined tree model has no such piercing behavior.`],[`O`,`I`,`What does this locator match, given a custom design-system button implemented as a web component with an open shadow root?`,`await page.locator('my-button').getByRole('button', { name: 'Save' }).click();`,[`It finds the my-button custom element, then looks INSIDE its shadow DOM for a descendant with an accessible button role named Save, since getByRole traverses through open shadow roots transparently`,`It always fails because getByRole cannot be chained onto a custom element locator`,`It clicks the my-button host element itself, ignoring the name filter entirely`,`It only works if the shadow root is created with mode: 'closed'`],0,`Chaining a role-based locator onto a custom element locator works precisely because Playwright's queries traverse open shadow boundaries as though they were not there.`],[`L`,`I`,`What does the CSS pseudo-class :visible do inside a Playwright locator, e.g. page.locator('button:visible')?`,null,[`It is a Playwright-specific CSS extension that restricts matches to elements Playwright considers visible (non-zero size, not display:none/visibility:hidden), useful when a hidden duplicate of an element exists in the DOM (e.g. mobile vs desktop nav)`,`It is standard CSS supported by all browsers outside of Playwright too`,`It only matches elements currently scrolled into the viewport`,`It has the opposite effect of the visible option on getByRole`],0,`:visible is one of several Playwright-specific selector engine extensions, not standard CSS, and it is commonly reached for when a page has both a visible and a hidden copy of similarly-structured markup.`],[`O`,`I`,`Why might this locator be preferred over a plain page.getByRole('link', { name: 'Home' }) when both a desktop and mobile nav exist in the DOM?`,`await page.locator('a:visible', { hasText: 'Home' }).click();`,[`Because both navs render an identically-named Home link in the DOM at the same time (one hidden via CSS at the current viewport), :visible narrows the match to only the one actually shown, avoiding a strict-mode violation`,`hasText only works when combined with :visible, never alone`,`This locator is functionally identical to getByRole and offers no benefit`,`Playwright ignores the :visible pseudo-class when hasText is also present`],0,`Responsive layouts commonly keep both nav variants in the DOM and toggle visibility with CSS, so filtering by actual visibility is one legitimate way to disambiguate which copy a test should interact with.`],[`L`,`A`,`What layout-based (proximity) pseudo-classes does Playwright's CSS engine support, beyond standard CSS selectors?`,null,[`:right-of(), :left-of(), :above(), :below(), and :near() let you select an element based on its screen position relative to another locator, useful when there is no reliable role/text/testid to anchor on directly`,`Only :right-of() and :left-of() exist; vertical relationships are not supported`,`These are deprecated aliases for nth-child and no longer function`,`They only work when the browser window is in fullscreen mode`],0,`These layout pseudo-classes are a genuine escape hatch for visually-obvious-but-semantically-unlabeled relationships, like an input immediately to the right of a text label with no explicit association.`],[`O`,`A`,`What does this locator select?`,`const input = page.locator('input:right-of(:text("Username"))');`,[`The first input element positioned to the right of whatever element contains the text Username, useful when the input has no label or aria-label tying it to that text`,`Every input on the page, since :right-of has no filtering effect`,`An input whose value attribute equals Username`,`It throws a syntax error since :right-of cannot be combined with :text`],0,`Layout-based selectors compare bounding boxes, so this expresses I want the input visually to the right of this label, which is a reasonable fallback when semantic association is missing.`],[`L`,`I`,`By default, does getByRole('button', { name: 'Submit' }) perform an exact or substring, case-sensitive or case-insensitive match on the accessible name?`,null,[`Substring and case-insensitive by default; pass { exact: true } to require a full, case-sensitive match of the entire accessible name`,`Exact and case-sensitive by default; substring matching requires a regex`,`Substring but case-sensitive by default`,`Exact but case-insensitive by default, with no way to change it`],0,`The default is deliberately forgiving, matching getByText's default behavior, so minor whitespace or casing differences in visible text do not break a locator; exact:true tightens this when precision matters.`],[`O`,`I`,`Why is the regex used here more precise than a plain string for this locator?`,`page.getByRole('button', { name: /^Submit$/ });`,[`Anchoring with ^ and $ requires the ENTIRE accessible name to equal exactly 'Submit', preventing accidental matches against buttons named 'Submit Order' or 'Re-submit'`,`Regex name matching is always case-sensitive, unlike a plain string, which is the only benefit here`,`This is functionally identical to just passing the string 'Submit' with default options`,`Anchored regexes disable Playwright's usual substring matching entirely, even without the anchors`],0,`A plain string name filter still does a case-insensitive substring match by default, so an anchored regex is one way to force an exact-name match without setting exact:true.`],[`L`,`I`,`Besides name, what other filtering options does getByRole() accept to narrow by ARIA state?`,null,[`Options like checked, pressed, expanded, selected, disabled, and level let you filter roles by their current ARIA state or heading level, not just their accessible name`,`getByRole() only ever accepts the name option; all other filtering requires .filter()`,`These options exist only for the checkbox role, not for other roles`,`level is the only additional option, and it applies to every role, not just headings`],0,`These extra options map directly onto the corresponding ARIA attributes (aria-checked, aria-pressed, aria-expanded, aria-selected, aria-disabled), letting you target, for example, only the currently expanded accordion header.`],[`O`,`I`,`What does this locator uniquely target among a list of toggle buttons?`,`page.getByRole('button', { name: 'Follow', pressed: false });`,[`Only the Follow buttons whose aria-pressed attribute is currently false, i.e. buttons a user has NOT yet toggled on, distinguishing them from ones already pressed/active`,`Every button labeled Follow regardless of state, since pressed is ignored for buttons`,`Only buttons where the pressed attribute is missing entirely; false explicitly excludes those`,`Buttons that are disabled, since pressed:false is interpreted as not clickable`],0,`The pressed option filters directly on aria-pressed, which is exactly the ARIA attribute a toggle button uses to communicate its current on/off state to assistive tech.`],[`D`,`I`,`getByRole('heading', { name: 'Order Summary' }) finds nothing, even though the heading text is visible on screen. Inspecting the DOM shows the heading has style="display:none" applied briefly then removed by a later animation frame, or the element sits inside a container with visibility:hidden that a CSS transition later reveals. What is the likely root cause of the miss?`,null,[`getByRole excludes elements the accessibility tree currently considers hidden; if the query runs before the reveal animation/transition completes, the heading is momentarily invisible to it, so waiting (via an auto-retrying assertion) rather than querying instantly is the fix`,`getByRole never supports the heading role under any circumstances`,`Headings are excluded from the accessibility tree by the ARIA spec entirely`,`The name option cannot match multi-word heading text`],0,`Because role-based matching is tied to the accessibility tree's notion of visibility, anything that transiently hides an element (even briefly) can cause a snapshot-style check to miss it, which is exactly why action/assertion methods auto-retry instead of checking once.`],[`L`,`A`,`What does the accessible name computation algorithm prioritize when both an aria-label and visible inner text exist on the same element?`,null,[`aria-label (and aria-labelledby, which takes even higher precedence) overrides the element's visible text content entirely for accessible-name purposes, so getByRole name matching reflects the aria-label, not what a sighted user sees printed on the button`,`Visible text always wins over aria-label, since that is what most users actually see`,`Both are concatenated together into one combined accessible name`,`Whichever attribute is defined first in the HTML source wins`],0,`This mirrors how screen readers compute accessible names per the W3C accname spec: explicit ARIA labeling attributes take precedence over the element's rendered content.`],[`O`,`I`,`Given <button aria-label="Save">Submit</button>, what name does this locator need to match successfully?`,`await page.getByRole('button', { name: 'Save' }).click();`,[`'Save', because aria-label overrides the visible text 'Submit' when computing the accessible name that getByRole matches against`,`'Submit', because getByRole always matches visible text regardless of aria-label`,`Both 'Save' and 'Submit' will match, since Playwright checks both attributes independently`,`Neither will match; conflicting labels make the element unmatchable by role`],0,`getByRole's name filter matches the computed accessible name, and aria-label takes precedence over an element's own text content in that computation.`],[`L`,`I`,`What does locator.describe() do (a feature aimed at trace/report readability)?`,null,[`It attaches a custom, human-readable description string to a locator, which then shows up in the trace viewer and error messages instead of a raw, hard-to-read selector string`,`It runs a live description of the element's current CSS properties and prints them to the console`,`It generates a natural-language accessibility description of the element automatically, replacing the need for aria-label`,`It is an alias for locator.textContent()`],0,`When a suite relies on longer or chained locators, a custom description makes failures in the report immediately understandable without deciphering the underlying selector.`],[`O`,`I`,`What is the benefit of adding .describe() to this locator?`,`const primaryCta = page.getByRole('button', { name: 'Get Started' }).first().describe('primary hero CTA button');`,[`If an action or assertion on primaryCta fails, the trace/report will reference 'primary hero CTA button' instead of the raw chained selector, making the failure far easier to understand at a glance`,`It changes which element the locator actually matches on the page`,`It disables strict mode checks for this specific locator`,`It is required before any action can be performed on a locator`],0,`describe() is purely a labeling aid for diagnostics; it has zero effect on which elements are matched or how actions behave.`],[`L`,`I`,`What do the newer .filter({ hasNotText }) and .filter({ hasNot }) options do, as the negative counterparts to hasText and has?`,null,[`hasNotText keeps only elements whose text does NOT contain the given string; hasNot keeps only elements that do NOT contain a descendant matching the given nested locator, letting you express exclusion filters directly instead of via workarounds`,`They are aliases with identical behavior to hasText and has, added purely for readability`,`hasNotText removes matching text from the element rather than filtering the element set`,`hasNot only works when combined with getByTestId`],0,`Before these existed, expressing an exclusion filter required more awkward patterns; hasNotText/hasNot let you state the negative condition directly.`],[`O`,`I`,`What does this locator select from a product listing?`,`const inStock = page.getByRole('listitem').filter({ hasNotText: 'Out of Stock' });`,[`All product list items EXCEPT those whose text content includes the phrase Out of Stock, giving you the in-stock subset without writing a more convoluted positive filter`,`Only the list items whose text is exactly Out of Stock`,`It removes the Out of Stock text from every list item's DOM`,`Every list item on the page, since hasNotText is ignored when hasText is absent`],0,`hasNotText is the direct negative counterpart to hasText, purpose-built for expressing an exclude-if-contains condition cleanly.`],[`S`,`I`,`A test uses .filter({ hasText: 'Sale' }) to find promotional badges, but it also matches items labeled 'On Sale Now' and, unexpectedly, one labeled 'Wholesale Pricing'. What is happening, and how should it be fixed?`,null,[`hasText performs a case-insensitive substring match by default, so any text CONTAINING 'sale' matches, including 'wholeSALE'; anchor with a regex like /^Sale$/ or use .filter({ hasNotText }) alongside it, or switch to an exact getByText match, to restrict the match precisely`,`filter({ hasText }) is broken and should never be used for this kind of matching`,`The fix is to lowercase the page's HTML so casing no longer matters`,`hasText only matches whole words already, so this behavior is a Playwright bug`],0,`Substring matching is powerful but indiscriminate; when a word appears as a fragment of a longer unrelated word, an anchored regex or an exact-text locator is the safer choice.`],[`L`,`I`,`What is the difference between the CSS locator engine's :text() and :text-is() pseudo-classes?`,null,[`:text() matches elements whose text content contains the given substring (case-insensitive), while :text-is() requires the text content to equal the given string exactly`,`They are identical aliases for the same substring-matching behavior`,`text-is() is deprecated in favor of :text(), which now handles both cases automatically`,`text() only matches leaf text nodes, while text-is() matches only container elements`],0,`This mirrors the exact vs default substring distinction seen elsewhere in Playwright's text-matching APIs, just expressed through the CSS pseudo-class engine instead of getByText's options object.`],[`O`,`I`,`What does this CSS locator find in a list of product cards, one of which contains an out-of-stock badge image?`,`const availableCards = page.locator('.product-card').filter({ hasNot: page.getByRole('img', { name: 'Out of stock' }) });`,[`Only the product cards that do NOT contain a descendant image with the accessible name Out of stock, i.e. the cards representing available products`,`Only the card that DOES contain the out-of-stock image`,`Every product card, since hasNot with a nested locator has no effect`,`It throws, since filter() cannot accept a locator built from a different role`],0,`hasNot performs containment exclusion using a full nested locator (not just text), so it works well even when the distinguishing marker is an image, icon, or any other element rather than plain text.`],[`L`,`I`,`What does locator.page() return, and when is it useful?`,null,[`The Page object that owns this locator, which is handy inside a reusable helper function that receives a bare Locator and needs to perform page-level operations like waiting for load state or taking a full-page screenshot`,`The specific HTML page number the element appears on for paginated content`,`A new, blank Page created specifically for this locator`,`It always throws, since locators are not tied to a specific page`],0,`Since a Locator itself does not expose page-level methods, page() is the bridge back to the owning Page when you only have a Locator reference in scope, such as inside a generic utility function.`],[`O`,`I`,`Why is locator.page() called inside this helper function?`,`async function waitForStableList(rowLocator) {
  await rowLocator.page().waitForLoadState('networkidle');
  return rowLocator.count();
}`,[`The helper only receives a Locator argument, not a Page, so page() is needed to reach page-level functionality like waitForLoadState from within a generic, reusable utility`,`waitForLoadState() is actually a Locator method, and page() is redundant here`,`page() converts the locator into an ElementHandle for the waitForLoadState call`,`This code is invalid since Locators cannot be passed as function arguments`],0,`Writing reusable helpers that accept a Locator rather than a full Page is common, and locator.page() is precisely the escape hatch for reaching page-scoped APIs from inside them.`],[`L`,`I`,`What do the toHaveAccessibleName() and toHaveAccessibleDescription() assertions verify, and why are they useful for accessibility regression testing?`,null,[`They assert on an element's computed accessible name/description (from aria-label, aria-labelledby, aria-describedby, or fallback content), catching regressions where a visual redesign leaves an icon-only button without a meaningful label for screen reader users`,`They assert that an element is visually described by adjacent text, purely a layout check`,`They are aliases for toHaveText() and toHaveTitle() respectively`,`They only work on form elements, never on buttons or icons`],0,`An icon-only button can look fine visually while silently losing its accessible name during a refactor; these assertions catch exactly that class of accessibility regression.`],[`O`,`I`,`What accessibility property is this test protecting against regressing?`,`await expect(page.getByRole('button', { name: 'Close' })).toHaveAccessibleName('Close dialog');`,[`That the icon-only close button retains a meaningful, specific accessible name (Close dialog) for screen reader users, even if its visible content is just an X icon with no visible text`,`That the button's CSS class contains the word close`,`That the button is positioned in the top-right corner of the dialog`,`That the button's visible text literally reads Close dialog`],0,`toHaveAccessibleName lets a test pin down the actual accessible name independent of whatever icon or minimal visible marker the button uses, which is exactly what matters for screen reader users.`],[`S`,`I`,`An accessibility regression test suite needs to verify that every icon-only button in a toolbar has a meaningful accessible name, not just an empty or generic one like 'button'. What is a good approach?`,null,[`For each icon button locator, assert toHaveAccessibleName() with the specific expected label, or more broadly assert the accessible name is non-empty and not a generic placeholder, catching regressions where a redesign drops the label attribute`,`Assert that each button has a visible text label, since icon-only buttons cannot be accessible by definition`,`Skip icon-only buttons in accessibility tests since they cannot be meaningfully tested`,`Rely solely on a visual screenshot diff to catch missing accessible names`],0,`Icon-only controls are exactly where accessible-name regressions hide, since the visual review process has nothing to flag when the label attribute silently disappears.`],[`L`,`B`,`Does getByRole('button') require an element to have an explicit role="button" attribute to match?`,null,[`No, native elements have implicit ARIA roles baked into the HTML spec, so a plain <button> or an <input type="button"> already has the button role without any explicit attribute`,`Yes, every element must have an explicit role attribute for getByRole to match it, with zero exceptions`,`Only <button> elements can ever match the button role; other elements can never be given that role`,`getByRole only recognizes explicit role attributes on custom elements, not native HTML tags`],0,`Implicit ARIA roles are part of the HTML accessibility mapping, so most native interactive elements already have a role without any explicit attribute being written.`],[`D`,`I`,`A custom <div onclick="..."> styled to look exactly like a button is not matched by page.getByRole('button', { name: 'Save' }). Why, and what are the fixes?`,null,[`A bare div has no implicit button role, so it is invisible to role-based queries; add role="button" (and typically tabindex="0" plus keyboard handlers for real accessibility) to the div, or fall back to getByTestId if fixing the component is out of scope`,`getByRole never works on div elements under any circumstances, even with an explicit role attribute`,`The fix is always to add a data-role="button" attribute instead of role`,`onclick handlers are incompatible with ARIA roles entirely`],0,`Custom clickable divs are a common accessibility anti-pattern; the correct application-level fix is adding a real ARIA role (and keyboard support), with getByTestId as a pragmatic short-term test workaround.`],[`L`,`I`,`What does locator.boundingBox() return, and when does it return null?`,null,[`A Promise resolving to the element's pixel position and size (x, y, width, height) relative to the main frame, or null if the element is not visible or not currently attached to the DOM`,`A CSS string describing the element's border-box styling`,`An array of all bounding boxes for every matched element, ignoring strict mode`,`It always throws rather than returning null, even for hidden elements`],0,`boundingBox() is the tool for pixel-level custom logic (like computing drag coordinates), and it gracefully returns null instead of throwing when there is no visible box to measure.`],[`O`,`I`,`Why does this code check for a null result before proceeding?`,`const box = await page.locator('#slider-handle').boundingBox();
if (!box) throw new Error('Slider handle not visible');
const targetX = box.x + box.width * 0.75;`,[`boundingBox() returns null when the element is not visible or not attached, so guarding against that avoids a confusing 'cannot read properties of null' error later when computing targetX`,`box is always non-null; the check is unnecessary defensive code with no real purpose`,`boundingBox() throws synchronously, so this check can never actually trigger`,`null indicates the element was found but has zero width, which this check does not actually detect`],0,`Pixel-coordinate calculations built on top of boundingBox() need an explicit guard, since silently proceeding with a null box turns into a much less clear runtime error a few lines later.`],[`L`,`B`,`What is the difference between locator.screenshot() and page.screenshot({ clip })?`,null,[`locator.screenshot() automatically scrolls the element into view and captures just its bounding box; page.screenshot({ clip }) captures a manually specified rectangle of the full page, requiring you to already know the exact coordinates`,`They produce identical output in every case, differing only in method name`,`locator.screenshot() can only capture the entire viewport, never a single element`,`page.screenshot({ clip }) automatically finds and crops to a given CSS selector`],0,`locator.screenshot() is the more convenient, element-relative option since Playwright computes the crop region for you from the element's own geometry, including scrolling it into view first.`],[`R`,`I`,`Playwright's codegen tool records a selector like page.locator('div:nth-child(3) > span:nth-child(2)') for an item in a dynamically generated list. What is the concern with keeping this selector as-is, and what should replace it?`,null,[`Deeply positional CSS selectors break the moment the list's item count, order, or intermediate markup changes even slightly; refactor toward a role/testid-based locator (e.g. getByRole('listitem').filter({ hasText })) that targets the item by its actual identity or content instead of DOM position`,`codegen-recorded selectors are always production-ready and never need refactoring`,`nth-child selectors are immune to reordering as long as the count of siblings stays the same`,`The fix is to add more nth-child levels for extra specificity`],0,`Recorder tools optimize for quickly capturing something that works right now, not for long-term resilience; treating codegen output as a first draft to refine is standard practice.`],[`L`,`A`,`Does locator.all() wait for elements to appear before returning its array of per-element locators?`,null,[`No, all() immediately snapshots whatever currently matches in the DOM with no waiting or retrying; if called before the elements render, it can return an empty or incomplete array`,`Yes, all() polls and retries exactly like an auto-retrying assertion until at least one element appears`,`all() waits, but only up to a fixed 5-second internal timeout that cannot be configured`,`all() always returns exactly the number of elements matched at the time the locator was first created, cached forever`],0,`Because all() has no built-in wait, it is best used after you already know the elements are present, e.g. following an auto-retrying assertion that confirms a non-zero count.`],[`D`,`A`,`A loop calls .all() on a list of 'Remove' buttons, then clicks each one in sequence, but only every other item actually gets removed. What is the bug?`,`const removeButtons = await page.getByRole('button', { name: 'Remove' }).all();
for (const btn of removeButtons) {
  await btn.click();
}`,[`Each click removes a row from the DOM, shifting the indices of the remaining rows, but the array captured by .all() is a fixed snapshot taken before any clicks happened, so later locators in the loop may now point to the wrong (or already-removed) elements; instead, repeatedly locate and click .first() until the count reaches zero`,`Locator click() does not work inside a for...of loop under any circumstances`,`all() returns stale ElementHandles that always throw when clicked twice`,`The fix is to reverse the array order before iterating`],0,`A one-time snapshot of positional matches becomes stale the moment the underlying list mutates; repeatedly re-querying (e.g. always acting on .first()) avoids relying on a fixed snapshot at all.`],[`L`,`B`,`Why does Playwright's locator API deliberately throw a strict-mode error on an ambiguous match instead of silently acting on the first one, unlike some older automation tools?`,null,[`Silently picking the first match can hide real bugs (an unintentionally broad selector matching multiple unrelated elements) and cause a test to pass for the wrong reason; an explicit error forces the author to disambiguate intentionally`,`Strict mode is purely a legacy compatibility requirement with no real design rationale`,`It has no benefit and can be disabled globally to avoid interruptions`,`Strict mode only applies to CSS locators, not semantic ones, so this is a moot distinction for most tests`],0,`A test that quietly acts on 'whichever element happened to be first' can pass even when the underlying selector logic is wrong, which is precisely the class of bug strict mode is designed to surface early.`],[`R`,`I`,`A legacy test suite consists entirely of page.locator('.someClass') style CSS selectors. Rewriting the whole suite to semantic locators all at once is too risky before a release. What is a pragmatic migration strategy?`,null,[`Migrate incrementally: apply semantic locators to any new tests and any test being touched anyway (bug fix, flake investigation), rather than a risky big-bang rewrite of the entire suite at once`,`Leave the suite entirely unchanged forever, since any migration risk is unacceptable`,`Rewrite the whole suite in a single large pull request right before the release to minimize context switching`,`Replace every CSS selector with an equivalent XPath expression as an intermediate improvement step`],0,`Incremental migration tied to code you are already touching spreads the risk and effort out over time, rather than introducing a large, high-risk change all at once.`],[`L`,`I`,`What does the testIdAttribute option in playwright.config control?`,null,[`It changes which HTML attribute page.getByTestId() looks for, letting a project use a different convention like data-qa or data-cy instead of the default data-testid`,`It changes the attribute used by getByRole() to compute accessible names`,`It sets a required prefix that all data-testid values in the app must start with`,`It has no effect unless combined with a custom locator engine registration`],0,`Teams that already have an established test-attribute convention from another framework can point Playwright at it instead of renaming every attribute in their app.`],[`O`,`I`,`What does this configuration change about how getByTestId() behaves across the whole test suite?`,`export default defineConfig({
  use: { testIdAttribute: 'data-qa' },
});`,[`Every call to page.getByTestId('x') now looks for an element with data-qa="x" instead of the default data-testid="x"`,`It renames all data-testid attributes in the app's HTML automatically before each test run`,`It disables getByTestId() entirely in favor of CSS attribute selectors`,`It only affects newly-written tests, not existing ones already using getByTestId()`],0,`testIdAttribute is a single global default for the whole config, so once set, every getByTestId() call in the suite consistently targets the configured attribute.`],[`S`,`I`,`A test that previously used locator.selectOption('US') for a country dropdown starts throwing 'Element is not a <select> element' after the team replaces the native select with a custom React-based dropdown component. What is the correct response?`,null,[`selectOption() only works on real native <select> elements; a custom div-based dropdown needs to be driven like any other custom widget, typically clicking to open it and then clicking the desired option (e.g. via getByRole('option'))`,`Add { force: true } to selectOption() to bypass the element-type check`,`This means the custom dropdown component is broken and must be reverted`,`Use page.evaluate() to directly call selectOption() from inside the browser context instead`],0,`selectOption() is intentionally scoped to native <select> semantics; swapping the underlying component to a custom widget changes the interaction pattern the test needs to use, not just the locator.`],[`L`,`I`,`What is the ARIA role difference typically expected between a native <select> element and a custom-built dropdown menu component?`,null,[`A native <select> implicitly has the combobox role, while a well-built custom dropdown often exposes a combobox trigger paired with a listbox of option elements, which is why the two need different locator strategies for their options`,`Both always use the identical role of textbox, regardless of implementation`,`Native selects use the role menu, while custom dropdowns use combobox exclusively`,`There is no meaningful ARIA distinction; role assignment is purely cosmetic and never affects locator strategy`],0,`Custom dropdown implementations that follow ARIA authoring practices typically expose a combobox/listbox/option role structure, which getByRole can target directly once you know that structure.`],[`O`,`I`,`What is this locator chain relying on to find and count options in a custom autocomplete component as the user types?`,`await page.getByRole('combobox', { name: 'Country' }).fill('Ger');
await expect(page.getByRole('option')).toHaveCount(1);
await page.getByRole('option', { name: 'Germany' }).click();`,[`The custom autocomplete correctly exposing combobox and option ARIA roles, which lets the test filter and select suggestions the same semantic way it would with a native input, without needing any CSS class knowledge of the component's internals`,`fill() automatically opens native browser autocomplete suggestions independent of the app's own JavaScript`,`getByRole('option') only works for native <select><option> elements, so this code is broken`,`toHaveCount(1) here asserts there is exactly one combobox on the page, unrelated to suggestions`],0,`When a component author follows ARIA combobox authoring practices, testing it becomes just as semantic and resilient as testing native form controls.`],[`L`,`A`,`When you chain page.locator('.a').locator('.b'), does Playwright send two separate round trips to the browser to resolve the query, or one combined query?`,null,[`Playwright compiles a chained locator into a single combined query that is resolved in one pass at execution time, rather than issuing the outer and inner queries as two separate browser round trips`,`It always performs two full separate queries, incurring roughly double the overhead of a single CSS selector`,`Chaining is purely syntactic sugar for string concatenation with a space, evaluated as one plain CSS descendant selector`,`Chaining only works efficiently for CSS-based locators, never for role-based ones`],0,`Understanding that chained locators are a single efficient query, not a sequence of separate DOM fetches, is useful when deciding between chaining and writing one combined selector for readability.`],[`L`,`I`,`page.locator('iframe').locator('button') fails to find a button that clearly exists inside the iframe's document. What is the mistake?`,null,[`Regular locator chaining does not pierce into an iframe's separate document; a FrameLocator (page.frameLocator('iframe')) must be used instead to search inside the iframe's content`,`locator() has a hard limit of one chain level and cannot go two levels deep`,`iframe elements can never be selected by page.locator() under any circumstances`,`The fix is to add { includeIframes: true } to the locator() call`],0,`An iframe boundary is a genuinely separate document, unlike shadow DOM; ordinary locator chaining stops at the iframe element itself rather than reaching inside it.`],[`L`,`I`,`Why does Playwright expose a dedicated FrameLocator API instead of simply letting locator() chain into iframe content automatically?`,null,[`An iframe hosts an entirely separate document (and often a separate origin), so Playwright makes the frame boundary explicit in the API rather than silently reaching across it, which also keeps behavior predictable for cross-origin iframes`,`FrameLocator exists purely as an alias with zero functional difference from chaining locator()`,`It is a historical leftover API kept only for backward compatibility with old test suites`,`FrameLocator only works in Chromium, so it had to be a separate, optional API`],0,`Explicit iframe handling avoids ambiguity about which document a query targets, especially important when the iframe might be cross-origin and subject to different browser-level restrictions.`],[`R`,`A`,`A payment form is embedded via a third-party iframe (like a card-details widget). How do you fill in the card number field inside it?`,null,[`Use page.frameLocator('iframe[name="card-frame"]').getByLabel('Card number').fill(...), scoping all locator calls to the iframe's content the same way you would on the main page`,`Card details cannot be filled by Playwright at all, since the iframe is provided by a third party`,`Use page.evaluate() to reach into the iframe's DOM directly from the main page context, bypassing frameLocator entirely`,`Iframes always require switching to a completely new browser context before you can interact with them`],0,`frameLocator() lets you keep writing normal, semantic locator code even though the actual elements live inside a nested document, which is exactly the common case for embedded payment widgets.`],[`L`,`A`,`What does locator.contentFrame() do, as a way to bridge from a Locator matching an iframe element into that iframe's content?`,null,[`It converts a Locator that resolves to an <iframe> element into a FrameLocator (or the underlying Frame), letting you then query inside that iframe's document using the same locator you already had, rather than writing a brand-new selector from page.frameLocator()`,`It captures a screenshot of the iframe's visible content only`,`It merges the iframe's content into the parent document permanently`,`It is only usable on the very first iframe on the page, regardless of the locator's actual match`],0,`contentFrame() is useful when you have already precisely identified which iframe element you want (perhaps among several similar ones) and want to pivot into its content without re-deriving the frame selector separately.`],[`O`,`I`,`What does this code accomplish?`,`const frame = page.locator('#payment-frame').contentFrame();
await frame.getByLabel('Card number').fill('4242424242424242');`,[`It first precisely locates the specific iframe element with id payment-frame, converts that match into its content frame, and then fills a field inside that iframe's document, useful when multiple similar iframes exist and a generic frameLocator selector might be ambiguous`,`It fills the Card number field on the main page, ignoring the iframe entirely`,`contentFrame() here creates an entirely new browser tab for the iframe's content`,`This code is equivalent to page.frameLocator('#payment-frame') with no practical difference in any scenario`],0,`contentFrame() is particularly useful when you have already disambiguated a specific iframe element via a Locator, and now want to act inside it without writing a second, separately-resolved frame selector.`],[`R`,`A`,`page.getByRole('img', { name: 'logo' }) matches nothing even though the page clearly displays a logo image with alt="logo". What should you check first?`,null,[`Verify the exact alt text casing/spelling and whether the accessible name matching is being done with exact:true unexpectedly, and also check whether a different, decorative logo image with alt="" is what actually renders on screen instead of the one you assumed had the alt text`,`getByRole never supports the img role under any version of Playwright`,`Images can only be located via getByAltText, never via getByRole`,`The image must have an explicit role="img" attribute, since <img> has no implicit role`],0,`A quick mismatch check (exact casing, exact:true, or confirming which of possibly several similar images actually carries the expected alt text) resolves the majority of these no-match surprises.`],[`L`,`I`,`Why are decorative images with alt="" intentionally excluded from getByRole('img') matches?`,null,[`An empty alt attribute signals to assistive technology (and thus to the accessibility tree Playwright's role queries read from) that the image is purely decorative and should be ignored, effectively giving it role="presentation" rather than img`,`alt="" is a bug in the HTML that Playwright happens to work around`,`getByRole always requires a non-empty alt regardless of intent, so this is unrelated to decoration`,`Decorative images are excluded only in Firefox due to a rendering quirk`],0,`This is standard, spec-defined accessibility behavior, not a Playwright-specific quirk: an empty alt is the documented way to mark an image as decorative and accessibility-tree-invisible.`],[`L`,`I`,`Is accessible-name matching in getByRole whitespace-sensitive, e.g. does a button with text spread across multiple lines/elements with extra internal whitespace still match a single-spaced expected name?`,null,[`No, accessible name computation normalizes internal whitespace (collapsing runs of spaces/newlines into single spaces and trimming ends), so minor formatting differences in the markup do not affect the matched name`,`Yes, any extra whitespace or line break in the source breaks the match entirely`,`Whitespace normalization only applies to getByText, never to getByRole's name option`,`Normalization only happens when exact:true is explicitly set`],0,`This normalization is why role-based locators remain robust to incidental formatting changes like added line breaks or extra spaces introduced by a template refactor.`],[`O`,`I`,`Does this locator still find the button below, given the accessible-name whitespace normalization Playwright applies?`,`// HTML: <button>Save
  Changes</button>
await page.getByRole('button', { name: 'Save Changes' }).click();`,[`Yes, because the internal newline and extra spaces in the button's text collapse to a single space during accessible-name computation, matching the plain 'Save Changes' string`,`No, the embedded newline makes the accessible name different from the plain string, so this will time out`,`Only if exact: false is explicitly passed, otherwise it fails`,`Only in Chromium; other engines do not normalize whitespace this way`],0,`Whitespace normalization is part of the standard accessible-name computation that role-based locators rely on, independent of exact-match settings.`],[`L`,`I`,`Are ARIA role strings themselves (like 'button', 'link', 'checkbox') case-sensitive when passed to getByRole()?`,null,[`Yes, role names must be lowercase per the ARIA spec's defined role vocabulary; getByRole('Button') will simply fail to match anything since 'Button' is not a recognized role string`,`No, role name casing is fully ignored, so 'Button' and 'BUTTON' work identically to 'button'`,`Only custom ARIA roles are case-sensitive; native HTML implicit roles ignore case`,`Case sensitivity depends on the browser engine being used`],0,`Unlike the accessible-name matching (which is case-insensitive by default), the role identifier itself must match one of the exact, lowercase ARIA role tokens.`],[`D`,`B`,`page.getByRole('Button', { name: 'Save' }) never matches, even though a real button with that text exists. What is the mistake?`,null,[`The role string 'Button' is capitalized; ARIA role tokens are lowercase, so it must be written as 'button' to match anything`,`name filtering does not work when combined with the button role`,`The button element needs an explicit role="button" attribute added`,`getByRole cannot be used for elements containing the word Save`],0,`This is a simple but easy-to-miss typo class: role strings are a fixed, lowercase vocabulary, unlike the flexible, case-insensitive name matching.`],[`L`,`A`,`Does Playwright's strict-mode enforcement (throwing on ambiguous multi-element matches) apply to locator.count() and locator.all()?`,null,[`No, count() and all() are inherently multi-match-aware and never throw for matching more than one element; strict mode specifically applies to actions (click, fill) and most locator-based assertions that expect a single element`,`Yes, count() and all() throw a strict-mode error the moment more than one element matches, exactly like click()`,`Strict mode applies only to CSS-based locators, so role-based count()/all() calls are always exempt`,`Strict mode can be toggled on for count()/all() via a config flag, but is off by default`],0,`Distinguishing which locator operations are strict-mode-sensitive (single-element actions/assertions) from which are inherently plural (count, all) avoids confusion about when a multi-match locator is actually fine to use.`],[`R`,`I`,`A test needs to conditionally dismiss a promotional banner only if it happens to be present, without failing when it is absent. How do you check for its presence without triggering a strict-mode error or an unnecessary wait?`,null,[`Use await locator.count() (or isVisible() with a short custom timeout) to branch the test logic, since these are non-strict, single-check helpers well suited to optional conditional flows, unlike an action or a hard assertion`,`Always call click() on the banner's close button regardless of whether it exists, and swallow any resulting error`,`Use expect(locator).toBeVisible() and catch the failure, since assertions are meant for this kind of optional branching`,`This kind of conditional UI cannot be handled in Playwright at all`],0,`count() and isVisible() are specifically the tools meant for control-flow decisions in a test, as opposed to expect() assertions, which are meant to express a hard requirement about final correctness.`],[`S`,`I`,`A cookie-consent banner may or may not appear depending on the user's region/cookie state, and a test suite needs to dismiss it when present before continuing. What pattern avoids both flakiness and false failures?`,null,[`await page.getByRole('button', { name: 'Accept' }).count() (or isVisible() with a small timeout) to check presence first, then conditionally click only if found, rather than unconditionally clicking or hard-asserting the banner exists`,`Always click the Accept button and let the test fail if it is not found, since the banner should always be there`,`Wrap the click in a try/catch with a generic error swallow, regardless of what the error actually is`,`Use page.waitForTimeout(3000) before deciding whether the banner appeared`],0,`Because the banner's presence is genuinely conditional application behavior (not a timing flake to be waited out), a presence check followed by a conditional action is more correct than either a hard requirement or a blind wait.`],[`L`,`I`,`Is locator.isVisible() an auto-retrying check, the same way expect(locator).toBeVisible() is?`,null,[`No, isVisible() performs one immediate, non-retrying check of the current DOM state and returns a boolean right away; toBeVisible() polls and retries until the condition holds or the timeout elapses`,`Yes, they behave identically in every respect, including retry behavior`,`isVisible() retries but only up to 3 attempts, unlike toBeVisible() which retries until timeout`,`isVisible() is deprecated and internally just calls toBeVisible() under the hood`],0,`This distinction matters: isVisible() is meant for a snapshot-in-time decision (control flow), while toBeVisible() is meant for a correctness assertion that should tolerate timing variance.`],[`O`,`I`,`Why might this conditional check occasionally behave incorrectly, compared to using an auto-retrying assertion?`,`if (await page.getByText('New notification').isVisible()) {
  await page.getByRole('button', { name: 'Dismiss' }).click();
}`,[`isVisible() checks the DOM at that exact instant with no retrying, so if the notification appears a moment AFTER this check runs, the dismiss click is skipped even though the notification is genuinely present shortly after`,`isVisible() always returns true regardless of actual visibility, making this check meaningless`,`getByText inside an if-statement is invalid syntax and this code cannot run`,`isVisible() automatically clicks the element itself as a side effect, making the second line redundant`],0,`Because isVisible() is a single, non-waiting snapshot, using it to gate a time-sensitive decision can race against content that appears just slightly later than the check.`],[`L`,`A`,"A test using `if (await locator.isVisible()) { await locator.click(); }` sometimes skips clicking an element that does appear moments later, causing an intermittent failure downstream. Why, and how should it be restructured for a case where the click really is expected to happen?",null,[`isVisible() does not wait or retry, so a slightly-delayed element is missed by the snapshot check; if the click is actually expected (not truly optional), replace the conditional entirely with a direct auto-waiting action or an explicit toBeVisible() assertion before clicking, rather than gating it behind isVisible()`,`This is expected, correct behavior and the test does not need any changes`,`isVisible() has an internal 30-second timeout that should be increased instead`,`Replacing isVisible() with count() > 0 fixes the timing issue automatically`],0,`The key judgment call is whether the action is genuinely conditional/optional (then isVisible()/count() branching is appropriate) or actually expected to happen (then it should just be a normal auto-waiting action, with no snapshot check gating it).`],[`L`,`I`,`When is it legitimate to use locator.count() or isVisible() instead of an expect() assertion in a test?`,null,[`Only for genuine conditional branching in test logic (e.g. an optional banner that may or may not appear), never as the final correctness check that determines whether the test passes or fails, which should always go through an auto-retrying expect() assertion`,`Anytime, since they are functionally interchangeable with expect() assertions in every situation`,`Never; count()/isVisible() should be removed from test code entirely in favor of assertions`,`Only inside beforeEach/afterEach hooks, never inside the test body itself`],0,`Keeping this distinction clear prevents a common flakiness trap: using a non-retrying snapshot check as if it were a reliable correctness assertion.`],[`R`,`I`,`A 'Load more' button should be clicked repeatedly until it disappears, revealing all items in a long list. How do you write this loop safely, avoiding a runaway infinite loop if something goes wrong?`,null,[`Loop while await loadMoreButton.isVisible() is true, clicking it each iteration, but cap the loop with a maximum iteration count as a safety guard against an application bug that never removes the button`,`Loop forever with no exit condition, trusting the button will always eventually disappear`,`Use expect(loadMoreButton).not.toBeVisible() inside the loop condition itself, since assertions can be used as boolean loop conditions`,`Click the button exactly once, then assume the rest of the list loads automatically regardless of button presence`],0,`A bounded safety limit turns a hypothetical application bug (button never disappearing) into a clear test failure rather than a suite that hangs indefinitely.`],[`O`,`I`,`What safety measure is missing from this pagination loop, and why does it matter?`,`const loadMore = page.getByRole('button', { name: 'Load more' });
while (await loadMore.isVisible()) {
  await loadMore.click();
}`,[`There is no maximum iteration cap, so if the button never disappears due to an application bug, this loop runs forever and hangs the whole test run rather than failing with a clear, actionable error`,`isVisible() cannot be used inside a while condition at all, so this code throws a syntax error`,`click() needs to be awaited twice per iteration for this to work correctly`,`The loop is missing a call to loadMore.scrollIntoViewIfNeeded() before each click`],0,`Any loop whose exit condition depends on external/application state should have a hard iteration ceiling, so a genuine bug produces a fast, clear failure instead of an indefinitely hanging test.`],[`L`,`I`,`What is locator.waitFor({ state: 'detached' }) useful for, compared to waiting for the element to become hidden?`,null,[`Waiting for an element to be fully removed from the DOM entirely, as opposed to merely being visually hidden (e.g. via CSS display:none) while still present in the document, which matters when a test needs to confirm something was truly deleted, not just hidden`,`It is identical in every respect to state: 'hidden', just an alternate name`,`It waits for the element to be re-attached after being removed, the opposite of what the name suggests`,`detached only applies to elements inside iframes, not the main document`],0,`Hidden and detached represent genuinely different DOM states, and choosing the wrong one can make a test pass even when the underlying behavior (was it really deleted, or just visually hidden?) is not what was intended.`],[`O`,`I`,`What is this code specifically waiting to confirm, that a plain visibility check would not?`,`await page.getByRole('progressbar', { name: 'Loading' }).waitFor({ state: 'detached' });`,[`That the loading spinner element has been completely removed from the DOM, not just hidden via CSS, which is a stronger guarantee that the loading phase has genuinely concluded and cleaned up after itself`,`That the spinner is currently spinning at full speed`,`That the spinner is momentarily invisible but still present, ready to reappear`,`It is functionally identical to waitFor({ state: 'hidden' }) in every case`],0,`For components that are truly unmounted after loading (rather than just toggled invisible), detached is the more precise state to wait for.`],[`L`,`I`,`Which four states does locator.waitFor({ state }) accept?`,null,[`attached, detached, visible, and hidden; there is no 'stable' option for waitFor, since stability is only checked internally as part of an action's actionability pipeline, not as a standalone waitable state`,`attached, detached, stable, and unstable`,`visible, hidden, enabled, and disabled`,`loaded, unloaded, rendered, and unrendered`],0,`It is worth distinguishing waitFor's four selectable states from the broader, action-specific actionability checks (like stability) that only apply internally during click/fill and are not directly waitable on their own.`],[`L`,`I`,`A test asserts expect(row).toBeHidden() to confirm a deleted table row is gone, and it passes, yet a later assertion reveals the row's stale data is still technically present in the DOM (just visually collapsed by a CSS animation). What assertion would have caught this correctly?`,null,[`expect(row).toHaveCount(0), which only passes when the element is truly absent from the match set, rather than toBeHidden() which is satisfied by a merely CSS-hidden-but-still-attached element`,`toBeHidden() and toHaveCount(0) are functionally identical, so no change would help`,`Adding { force: true } to toBeHidden() would have caught the discrepancy`,`This can only be caught with a full page reload before asserting`],0,`toBeHidden() deliberately tolerates a still-attached-but-invisible element, which is exactly right for testing visual hiding but wrong for testing true removal from the DOM.`],[`L`,`I`,`How does the accessibility tree assign an implicit role to structural HTML5 landmark elements like <nav>, <main>, <header>, and <footer>?`,null,[`<nav> maps to the navigation role and <main> maps to the main role reliably; <header> and <footer> map to banner and contentinfo roles only when they are NOT nested inside another sectioning element like article or section, in which case they have no distinct landmark role`,`All four elements always map to the identical generic role of region regardless of context`,`None of these elements have any implicit ARIA role; role attributes must always be added manually`,`<header> and <footer> always map to banner and contentinfo regardless of nesting context`],0,`The context-dependent mapping for header/footer is a genuinely tricky nuance: the same tag can be a global page landmark or just a local section header depending on where it sits in the document.`],[`O`,`I`,`What does this locator rely on to uniquely find the primary site navigation, given that a page might also have a secondary breadcrumb <nav>?`,`await page.getByRole('navigation', { name: 'Main' }).getByRole('link', { name: 'Pricing' }).click();`,[`The primary <nav> element has an explicit aria-label of 'Main', letting getByRole distinguish it from any other unlabeled or differently-labeled navigation landmark on the page, then scoping the link search within just that landmark`,`All nav elements automatically get the name 'Main' as their default accessible name`,`navigation role only ever matches a single element per page by specification, so the name option is unnecessary here`,`This code searches every link on the page named Pricing regardless of which nav it is in`],0,`Multiple same-role landmarks are only disambiguated by their accessible name (typically via aria-label), which is exactly why an unlabeled duplicate landmark causes strict-mode ambiguity.`],[`L`,`I`,`What is the difference between locator.inputValue() and locator.textContent() when applied to a text <input> element?`,null,[`inputValue() reads the element's live .value property, which is the correct way to read what a user has typed into an input, select, or textarea; textContent() reads DOM text content, which is empty (or irrelevant) for a plain <input> since its value is not represented as child text nodes`,`They are interchangeable for every form element type`,`textContent() is the more modern, recommended API and inputValue() is deprecated`,`inputValue() only works on <textarea>, never on <input> elements`],0,`This is a common source of a confusing empty-string bug: <input> elements store their content in the value property, not as DOM child text, so textContent() simply is not the right tool for reading them.`],[`D`,`I`,`A test calls locator.textContent() on a text input expecting to read back the value the user typed, and it always returns an empty string. What is the fix?`,null,[`Use locator.inputValue() instead, since a text input's typed value lives in its value property, not as DOM text content that textContent() would read`,`textContent() only fails on password-type inputs, so switching input type fixes it`,`Add { force: true } to textContent() to make it read the value property`,`This is expected; input values cannot be read back through Playwright at all`],0,`inputValue() exists specifically because textContent() and innerText() are not meaningful for reading back what is in a form control's value attribute.`],[`L`,`I`,`For testing a data table row, what is the more robust approach compared to a hard-coded CSS selector like td:nth-child(3)?`,null,[`Scope to the row via role and content (e.g. getByRole('row').filter({ hasText: 'target identifier' })), then locate the specific cell within it via getByRole('cell') or getByRole('columnheader')-based lookup, rather than relying on fixed column position`,`Always use nth-child, since table columns never get reordered in practice`,`Wrap the entire table in a single getByTestId and never query into individual cells`,`Use XPath exclusively for any table-related locator, since CSS cannot express row/column relationships`],0,`Row-then-cell chaining based on content and semantic roles survives column reordering and row-count changes far better than a position-dependent selector.`],[`R`,`A`,`In a finance dashboard table, you need the balance value for the row labeled 'Checking Account'. How do you construct a robust locator for that specific cell?`,null,[`page.getByRole('row').filter({ hasText: 'Checking Account' }).getByRole('cell').nth(N), where N is the known zero-indexed position of the Balance column, or better, use getByRole('columnheader', { name: 'Balance' }) to determine that position dynamically rather than hardcoding it`,`page.locator('td:nth-child(2)') applied globally across the whole table with no row scoping at all`,`Use getByText('Checking Account') and assume the value is always the very next DOM sibling element`,`This cannot be tested reliably since tables have no meaningful locator strategy`],0,`Scoping to the row by content first, then locating a specific column's cell within that row (ideally cross-referenced against the header), is the standard resilient pattern for tabular data.`],[`L`,`I`,`If a page has two <nav> landmarks and neither has an aria-label or aria-labelledby, what happens when you call page.getByRole('navigation')?`,null,[`It matches both, since they are indistinguishable by accessible name; any single-element action or assertion on this locator throws a strict-mode violation until you add distinguishing labels or scope by another means (like a testId or a containing landmark)`,`Playwright automatically picks the first one in DOM order without error`,`It throws immediately at locator creation time, before any action is attempted`,`Both navs are automatically merged into a single combined locator match`],0,`Without any distinguishing accessible name, two same-role landmarks are genuinely ambiguous to a role-based query, which is exactly the scenario strict mode is designed to catch rather than silently guess.`],[`S`,`I`,`A form has two identical 'Add' buttons, one per repeatable field group (e.g. 'Add phone number' appearing twice with the same accessible name in two separate sections). How do you reliably click just the one in the 'Emergency Contacts' section?`,null,[`Scope the search to the containing section first (e.g. page.getByRole('group', { name: 'Emergency Contacts' }).getByRole('button', { name: 'Add' })) rather than trying to disambiguate by accessible name alone, since both buttons share the same name`,`Use .first() or .last() and hope the DOM order never changes`,`Rely on a fixed pixel coordinate click at the button's expected screen position`,`This cannot be disambiguated and the application must be redesigned before it can be tested`],0,`When two elements genuinely share the same accessible name, scoping by an enclosing landmark or container is the correct disambiguation strategy, rather than positional tricks.`],[`L`,`I`,`Does Playwright's CSS locator engine support case-insensitive attribute matching, like the standard CSS i flag in [data-state="Active" i]?`,null,[`Yes, this is standard CSS attribute selector syntax (not Playwright-specific) and works normally within page.locator() CSS strings`,`No, Playwright strips the i flag and always performs case-sensitive attribute matching`,`Yes, but only within getByRole's name option, never inside raw CSS locator strings`,`The i flag only works for the class attribute, not arbitrary data attributes`],0,`Because Playwright's CSS engine is largely standards-compliant CSS with a few added extensions, ordinary CSS features like the case-insensitivity attribute flag continue to work as expected.`],[`O`,`I`,`What does the trailing i inside this CSS attribute selector accomplish?`,`await page.locator('[data-status="active" i]').click();`,[`It makes the attribute value comparison case-insensitive, so data-status values like 'Active', 'ACTIVE', or 'active' all match the same selector`,`It inverts the match, selecting every element WITHOUT that attribute value`,`It is invalid syntax inside Playwright's locator() and will throw`,`It restricts the match to elements with exactly one data attribute`],0,`This is standard CSS Selectors Level 4 syntax; it is a genuinely useful, easy-to-overlook tool when an app's data attribute values have inconsistent casing across releases.`],[`L`,`A`,`In a Page Object Model, why might defining locators as getter methods (e.g. get saveButton() { return this.page.getByRole(...); }) be preferable to computing and storing a Locator once in the constructor?`,null,[`A getter re-derives the locator fresh every time it is accessed, so it always reflects the current page/DOM state and never risks becoming a stale reference tied to a page instance from an earlier point in the test`,`Getters execute faster than a stored property lookup in every JavaScript engine`,`Stored Locators become literally invalid objects after the first use and must be recreated`,`There is no practical difference; it is purely a stylistic preference with zero functional impact`],0,`Because Playwright locators are already lazy, wrapping their creation in a getter costs nothing while ensuring the underlying page reference used is always the currently-relevant one.`],[`S`,`A`,`A Page Object stores this.saveButton = page.getByRole('button', { name: 'Save' }) once in its constructor. Later, the same Page Object instance is reused after the test opens a new tab and switches the active page reference elsewhere. What goes wrong?`,null,[`The stored Locator remains bound to the ORIGINAL page it was created on, so actions on this.saveButton continue to target the old tab, not whichever page the test currently considers active, producing confusing 'element not found' or wrong-tab results`,`Locators automatically re-bind themselves to whatever page is currently in focus`,`This is not actually a problem since Locators are page-agnostic by design`,`The fix is to call page.bringToFront() before every action, leaving the stored Locator unchanged`],0,`A Locator is tied to the specific Page it was created from; multi-tab or multi-page test scenarios need the Page Object to either take the active page as a parameter or re-derive locators against the currently relevant page.`],[`R`,`I`,`A design system's toast notifications stack in a shared global container, and a test needs to reference 'the most recently shown toast' regardless of how many are currently on screen. How do you write a robust helper locator for this?`,null,[`Scope to the toast container, then use .last() on the collection of toast items (assuming new toasts are appended, not prepended), e.g. page.getByRole('status').last(), documenting that assumption clearly since it depends on the app's actual stacking order`,`Always use .first(), assuming toasts are always prepended regardless of the app's actual behavior`,`Use a fixed nth() index equal to the expected total toast count, recalculated manually for every test`,`Toast notifications cannot be reliably located since they are transient`],0,`Positional helpers like this are legitimate as long as the assumption about append order is verified against the actual app behavior and ideally documented in the helper itself.`],[`L`,`I`,`When would you reach for locator.and() rather than simply writing one combined CSS selector like button.primary[aria-pressed="true"]?`,null,[`and() is most useful for combining two DIFFERENT locator strategies that a single selector syntax cannot express together, such as an accessibility-role-based locator intersected with a CSS-based one, rather than replacing something a single CSS selector could already do just as well`,`and() should always be preferred over any combined CSS selector for performance reasons`,`and() only works when both locators use the exact same engine (e.g. both CSS)`,`There is no functional difference in any case; and() is purely a stylistic alternative`],0,`When the two conditions come from fundamentally different query mechanisms (role-based vs CSS-based, for example), and() is genuinely necessary rather than just a style choice, since a single selector string cannot mix engines that way.`],[`O`,`I`,`What specific subset of buttons does this locator target on a social feed with many Follow/Following toggle buttons?`,`const unfollowedButtons = page.getByRole('button', { name: 'Follow' }).and(page.locator('[aria-pressed="false"]'));`,[`Only the buttons that are both accessibly named 'Follow' AND currently have aria-pressed="false", i.e. the users not yet followed, excluding any button whose name or pressed-state does not match both conditions`,`Every Follow or Following button on the page, since and() behaves like a union here`,`Only buttons where either the name or the aria-pressed condition is true, but not necessarily both`,`It throws, since and() cannot combine a role-based locator with a CSS-based one`],0,`and() performs a true set intersection, which is exactly the tool needed when you must satisfy two independently-expressed conditions simultaneously to identify the right subset of otherwise-identical elements.`],[`S`,`I`,`A social feed has many Follow/Following toggle buttons with identical accessible names but differing aria-pressed states. A test needs to click only the ones not yet followed. What locator strategy handles this cleanly?`,null,[`Combine a role-based name filter with a CSS-based aria-pressed state filter via .and(), since neither condition alone is sufficient to identify only the unfollowed buttons`,`Rely on DOM order and always click the first N buttons, assuming unfollowed ones always come first`,`Use .filter({ hasText: 'false' }) to match the aria-pressed value as if it were visible text`,`This cannot be tested without adding new, dedicated test IDs to every button first`],0,`Intersecting a semantic locator with a state-based CSS condition via and() is exactly the tool designed for disambiguating otherwise-identical elements by their current state.`],[`L`,`B`,`Why is relying on page.locator() with raw CSS selectors for everything, even when a getByRole() equivalent would work just as well, generally discouraged in code review?`,null,[`It couples the test to implementation details (class names, DOM structure) that change far more often than user-facing semantics, and forfeits the free accessibility check that role-based locators provide as a side effect`,`Raw CSS selectors are technically incapable of matching most elements, so this concern is purely theoretical`,`CSS locators execute noticeably slower than role-based locators in every case`,`CSS locators cannot be combined with actions like click() or fill()`],0,`The core review concern is long-term maintainability and the incidental accessibility signal role-based queries provide, not any technical limitation of CSS selectors themselves.`],[`L`,`A`,`What is the accessible-name difference between a native <select> element and an ARIA-based custom combobox implementation, in terms of what getByRole('combobox', { name }) matches against?`,null,[`For a native select, the accessible name typically comes from an associated <label>; for a custom ARIA combobox, it is usually driven by aria-label or aria-labelledby on the combobox's trigger element, so the same getByRole call works for both once the custom component follows ARIA authoring practices correctly`,`Native selects have no accessible name at all, only custom comboboxes do`,`getByRole cannot target the combobox role for native <select> elements under any circumstances`,`Only aria-label works for native selects; label association never contributes an accessible name for them`],0,`Both native and custom implementations ultimately feed into the same accessible-name computation, which is exactly why a correctly-authored custom component can be tested with the same getByRole pattern as a native control.`],[`O`,`I`,`What does .and() combined with a negated attribute selector accomplish in this locator, for finding an enabled Save button among several states?`,`const activeSave = page.getByRole('button', { name: 'Save' }).and(page.locator(':not([disabled])'));`,[`It intersects the role-based match with a CSS condition excluding disabled elements, yielding only the Save button(s) that are currently enabled and interactive`,`It excludes every Save button entirely, since :not() always negates the whole locator chain`,`It is redundant, since getByRole never matches disabled elements in the first place`,`This throws an error because :not() cannot be used inside and()`],0,`getByRole does not exclude disabled elements by default, so explicitly intersecting with a not-disabled CSS condition is a legitimate way to filter down to only the currently actionable ones.`],[`L`,`I`,`What does locator.evaluateAll(fn) do, as the multi-element counterpart to evaluate()?`,null,[`It runs the given function once per matched element, passing an array of all matched elements as the argument to the function (rather than one element at a time), and returns the function's serialized return value, letting you compute something across the whole set inside the browser context in one call`,`It runs evaluate() once for every element and returns an array of individual results automatically`,`evaluateAll() is only usable when the locator matches exactly one element`,`It behaves identically to allTextContents(), just with a different name`],0,`evaluateAll() hands your callback the whole array of DOM elements at once, which is useful for computations that need to see all matched elements together, such as checking their relative order.`],[`O`,`I`,`What does this code verify about a set of table rows using evaluateAll()?`,`const isSorted = await page.getByRole('row').evaluateAll(rows =>
  rows.every((row, i) => i === 0 || row.textContent >= rows[i - 1].textContent)
);
expect(isSorted).toBe(true);`,[`That the rows' text content appears in non-decreasing order relative to one another, computed entirely inside the browser in a single call rather than pulling each row's text out to Node.js individually first`,`That every row has identical text content to every other row`,`That the table has exactly the same number of rows as columns`,`evaluateAll() here only checks the very first row's content, ignoring the rest`],0,`Because evaluateAll() hands the whole matched array to a single in-browser function, order-dependent checks across multiple elements can be expressed in one compact call.`],[`L`,`I`,`What is the difference between locator.allInnerTexts() and locator.allTextContents()?`,null,[`allInnerTexts() returns the rendered, visible text of each matched element (respecting CSS like display:none), while allTextContents() returns the raw text content of each element including text hidden via CSS, mirroring the innerText vs textContent distinction on a per-element locator basis`,`They are aliases for the exact same underlying browser call, just with different names`,`allInnerTexts() only works on a single-element locator, unlike allTextContents()`,`allTextContents() strips whitespace while allInnerTexts() preserves it, which is the only difference`],0,`This mirrors the well-known innerText vs textContent distinction, just extended to the array-returning, multi-element locator methods.`],[`D`,`I`,`A test uses allTextContents() to verify visible list item labels, but the assertion unexpectedly includes text from a visually hidden tooltip nested inside one of the items. What is the fix?`,null,[`Switch to allInnerTexts(), which respects CSS visibility and excludes text from elements hidden via display:none or visibility:hidden, unlike allTextContents() which includes all text regardless of visibility`,`This is a Playwright bug with no available fix`,`Add { visible: true } as an option to allTextContents(), which does not exist but would otherwise fix it`,`Wrap the call in expect().toBeVisible() first to filter hidden text automatically`],0,`Whenever hidden auxiliary content (tooltips, screen-reader-only text) leaks into a text-based assertion, checking whether you meant the visible-text variant of the API is the first thing to verify.`],[`L`,`A`,`Can a Playwright locator be constructed from an XPath expression that references a shadow DOM element's descendant?`,null,[`No, standard XPath as implemented by Playwright's XPath engine does not pierce shadow DOM boundaries, unlike the CSS and text engines; an XPath expression simply cannot see past a shadow root into its content`,`Yes, XPath pierces shadow DOM identically to CSS and text engines`,`XPath can pierce shadow DOM, but only closed shadow roots, not open ones`,`This depends entirely on which browser engine is running the test`],0,`This is one of the concrete, practical reasons XPath is discouraged for modern component-based UIs: its non-piercing behavior with shadow DOM is a real functional limitation, not just a style preference.`],[`O`,`A`,`Why does this XPath-based locator fail to find a button that a role-based locator would successfully find, in an app built with shadow-DOM-based web components?`,`await page.locator('xpath=//my-button//button').click();`,[`If my-button renders its internal <button> inside an open shadow root, the XPath engine cannot traverse into that shadow boundary at all, so this expression finds nothing even though the button visibly exists on the page`,`XPath expressions cannot contain the // operator twice in the same selector`,`This code is invalid syntax and Playwright rejects it before running`,`xpath= locators only work on the very first element of the entire document`],0,`This is a very concrete illustration of the XPath-versus-shadow-DOM limitation: the exact same button would be trivially reachable via page.locator('my-button').getByRole('button'), which does pierce the shadow root.`],[`L`,`I`,`Does getByPlaceholder() match against an input's placeholder attribute using exact or substring matching by default?`,null,[`Substring, case-insensitive matching by default, just like getByText and getByRole's name option, with an { exact: true } option available to require a full match`,`Exact, case-sensitive matching by default, with no way to loosen it`,`It only supports exact regex matching, never plain strings`,`It matches against the value attribute, not placeholder, despite the method's name`],0,`Playwright keeps default matching behavior consistent across its text-based locator methods (getByText, getByLabel, getByPlaceholder, and the name option), all defaulting to substring/case-insensitive.`],[`O`,`I`,`Why does this locator successfully match an input whose placeholder is the longer phrase 'Search products or categories...'?`,`await page.getByPlaceholder('Search products').fill('shoes');`,[`getByPlaceholder performs a case-insensitive substring match by default, so a shorter search string matches as long as it appears somewhere within the actual placeholder text`,`getByPlaceholder always requires an exact, full match, so this code would actually fail, not succeed`,`fill() ignores the locator's matching logic and fills whatever the first input on the page is`,`Placeholder matching is regex-only, and this plain string is silently treated as an empty pattern`],0,`Just like getByText and getByRole's name filter, getByPlaceholder's default forgiving substring match is convenient but worth being aware of when placeholder text might overlap unexpectedly across different inputs.`],[`L`,`I`,`Why might getByLabel() fail to find an input that visually appears to have a label right next to it in the rendered page?`,null,[`getByLabel() requires an actual programmatic association (a <label for="id"> wired to a matching input id, the input nested inside the label, or aria-labelledby), not just visual proximity; text that merely sits nearby with no such association is invisible to getByLabel()`,`getByLabel() always works based on visual proximity between text and input regardless of markup structure`,`getByLabel() only works for checkboxes and radio buttons, never for text inputs`,`This can only happen if the input is inside an iframe`],0,`This is a frequent gap between how a form LOOKS and how it is actually MARKED UP: visually-adjacent text with no explicit label association is exactly the case that trips up getByLabel() and is also a real accessibility bug in the app.`],[`D`,`I`,`A form field visually shows the text 'Email' right beside an input, but page.getByLabel('Email') times out finding it. What should you inspect, and what is the fallback?`,null,[`Inspect whether the 'Email' text is actually a <label> programmatically associated with the input (via for/id, wrapping, or aria-labelledby) rather than just adjacent, unassociated text; if the association truly is missing, this is an accessibility bug worth flagging, with getByText or getByPlaceholder as a pragmatic short-term fallback`,`getByLabel() is fundamentally broken for any field named Email specifically`,`Add { exact: false } to getByLabel(), which will fix any association problem`,`This means the input must be re-created using JavaScript before the test runs`],0,`A getByLabel() miss on something that visually looks labeled is often a genuine, worth-reporting accessibility defect in the markup, not just a locator problem to route around silently.`],[`L`,`I`,`What is the recommended fallback order when neither role, label, placeholder, nor visible text can uniquely identify an element?`,null,[`getByTestId(), using a deliberately-added data-testid hook, is the recommended semantic-adjacent fallback before reaching for raw CSS or XPath selectors, since it is explicit, stable, and clearly signals test-only intent`,`Jump straight to XPath, since it is the most powerful and flexible option available`,`Use page.locator() with the element's auto-generated CSS class name, since that is usually stable enough`,`There is no recommended fallback; such elements simply cannot be reliably tested`],0,`getByTestId occupies a deliberate middle ground: more resilient than CSS/XPath (since it is a dedicated, stable hook) but acknowledged as a fallback rather than the first choice, since it does not reflect real user-facing semantics.`],[`R`,`A`,`A complex custom slider component has no accessible role, label, or test ID, and the team cannot modify its source right now. What is a pragmatic locator strategy given the constraint?`,null,[`Use boundingBox() on a stable ancestor container combined with layout-based CSS pseudo-classes (like :near() or :right-of()) or precise coordinate math as a temporary workaround, while flagging the missing ARIA semantics as a real accessibility gap to fix upstream`,`Skip testing the slider entirely until the component is rewritten`,`Use a screenshot-based visual diff as the only form of verification for this component, since no other option exists`,`Force a data-testid attribute onto the element at runtime using page.evaluate() in every test that touches it`],0,`When you cannot change the app, layout-based and coordinate-based fallbacks (imperfect as they are) can unblock testing in the short term, while the missing semantics remain a legitimate defect worth escalating.`],[`L`,`I`,`What does the has option in .filter({ has: nestedLocator }) require, as opposed to hasText?`,null,[`has requires the element to contain a DESCENDANT matching an arbitrary nested Locator (any role, attribute, or structure), whereas hasText is limited specifically to matching visible text content`,`has and hasText are functionally identical, differing only in naming convention`,`has only works with CSS-based nested locators, never role-based ones`,`has matches elements that do NOT contain the nested locator, the opposite of what its name suggests`],0,`has is strictly more general than hasText, since a nested Locator can express any matchable condition, not just text, such as containing a specific icon, image, or ARIA state.`],[`O`,`I`,`What kind of product cards does this locator select from a listing page?`,`const discountedCards = page.locator('.product-card').filter({ has: page.getByText('% off') });`,[`Only the product cards that contain a descendant element with text including '% off', i.e. cards currently showing a discount badge`,`Only cards that do NOT show a discount, since filter always negates its condition`,`Every card on the page, since has requires an exact structural match that this locator does not provide`,`It throws, since getByText cannot be passed as a nested locator inside has`],0,`has performs containment filtering using a full nested locator, so any matchable condition, including specific visible text somewhere inside the card, works as the filter criterion.`],[`L`,`A`,`Why is it usually better to prefer .filter({ hasText }) over embedding a :has-text() pseudo-class directly inside a raw CSS locator string, when a semantic base locator like getByRole is available?`,null,[`Keeping the base query semantic (getByRole/getByLabel/etc.) and layering .filter() on top preserves the resilience and readability benefits of role-based matching, whereas folding everything into one long CSS string reintroduces the implementation-coupling problems semantic locators are meant to avoid`,`They are functionally and philosophically identical, so this is purely a stylistic nitpick with zero real consequence`,`CSS's :has-text() pseudo-class does not actually exist in Playwright's engine, so the CSS version simply fails`,`filter({ hasText }) executes an extra network round trip that a single CSS string avoids`],0,`Chaining a semantic base locator with .filter() keeps the readable, role-based intent clear, rather than collapsing everything into a single opaque CSS string that is harder to reason about and refactor later.`],[`O`,`I`,`What is the practical benefit of this pattern when asserting on a filtered subset of rows?`,`const urgentRows = page.getByRole('row').filter({ has: page.getByText('Urgent') });
await expect(urgentRows).toHaveCount(2);
await urgentRows.first().getByRole('button', { name: 'Resolve' }).click();`,[`It narrows to only the rows containing an Urgent marker, asserts the expected number of such rows exist, and then acts on one of them, combining filtering, counting, and action into a single readable chain`,`It clicks every row on the page regardless of its content`,`toHaveCount(2) here asserts there are exactly 2 rows in the whole table, ignoring the filter entirely`,`filter({ has }) and getByText cannot be combined in the same expression`],0,`Chaining filter-then-count-then-act is a common, readable pattern for asserting on a meaningful subset of a larger, dynamic list before interacting with one of its members.`]],actions:[[`O`,`B`,"What does `locator.fill('text')` do to a text input?",null,[`Appends 'text' to the current value`,`Clears the field then sets it to 'text'`,`Types 'text' character by character and fires keydown events`,`Submits the form with 'text' as the value`],1,`fill() is a two-step operation: clear the field, then set its value.`],[`O`,`B`,`Which actionability check verifies the element is not hidden by CSS?`,null,[`attached`,`stable`,`visible`,`enabled`],2,`Playwright checks: attached · visible · stable · enabled/editable · receiving events.`],[`O`,`I`,"What is the key difference between `fill()` and `pressSequentially()`?",null,[`fill() is async; pressSequentially() is synchronous`,`fill() sets the value atomically; pressSequentially() dispatches individual key events`,`fill() only works on textareas; pressSequentially() works on all inputs`,`fill() triggers form validation; pressSequentially() bypasses it`],1,`Use pressSequentially when the app listens to individual keydown/keyup events.`],[`O`,`B`,"What does `locator.hover()` do?",null,[`Clicks the element gently`,`Moves the mouse over the element without clicking`,`Waits for the element to be visible`,`Scrolls the element into view`],1,`hover() triggers :hover CSS states and reveals tooltip/dropdown menus.`],[`O`,`I`,"What does `locator.selectOption('US')` do?",null,[`Searches for the text US inside the element`,`Selects the <option> whose value attribute equals US`,`Clicks the option labeled US`,`Fires a change event with the value US`],1,`selectOption() matches by value, label, or index depending on the argument.`],[`O`,`B`,"How does Playwright handle a `click()` on an element that is still loading?",null,[`It throws immediately`,`It retries until the element passes all actionability checks or times out`,`It clicks wherever the locator last pointed`,`It skips the click and logs a warning`],1,`Auto-waiting is built into every action — no manual wait needed.`],[`O`,`I`,"What does `{ force: true }` do when passed to `click()`?",null,[`Clicks with extra mouse pressure simulation`,`Skips actionability checks like visibility and enabled state`,`Forces the element into the viewport before clicking`,`Bypasses network throttling during the action`],1,`force: true is needed when you deliberately interact with hidden or covered elements.`],[`O`,`B`,"What does `locator.dblclick()` produce?",null,[`A right-click followed by a left-click`,`Two rapid left-click events on the element`,`A single click with the middle mouse button`,`A click that holds Shift simultaneously`],1,`dblclick() is equivalent to click({ clickCount: 2 }).`],[`O`,`I`,"What does `locator.setInputFiles('cv.pdf')` do?",null,[`Opens a file picker dialog for cv.pdf`,`Sets the files of a file input element to cv.pdf`,`Reads cv.pdf and fills the input with its text content`,`Uploads cv.pdf to the server immediately`],1,`setInputFiles() bypasses the OS file picker and attaches files directly.`],[`O`,`B`,"What does `locator.press('Enter')` do?",null,[`Submits the nearest form unconditionally`,`Dispatches keydown · keypress · keyup events for Enter on the element`,`Clicks the first submit button in the form`,`Moves focus to the next focusable element`],1,`press() simulates low-level keyboard events — useful for form submission or autocomplete.`],[`O`,`I`,"What does `locator.click({ button: 'right' })` trigger?",null,[`A double right-click`,`A contextmenu event on the element`,`A middle-mouse-button click`,`A click with the Alt modifier`],1,`Right-click fires the contextmenu event, opening context menus.`],[`O`,`A`,"What does `locator.click({ trial: true })` do?",null,[`Performs a real click and then undoes it`,`Runs all actionability checks without actually dispatching a click`,`Clicks in slow-motion for visual debugging`,`Retries the click up to three times`],1,`trial: true is a dry run — useful for verifying actionability without side effects.`],[`F`,`B`,"Complete: `await page.getByLabel('Email')._______('user@test.com')`",null,[`fill`,`type`,`setValue`,`sendKeys`],0,`fill() clears and sets — the standard method for text inputs.`],[`F`,`I`,"Complete: `await page.getByLabel('Country').________Option('US')`",null,[`select`,`choose`,`pick`,`set`],0,`selectOption() handles <select> dropdowns.`],[`F`,`B`,"Complete: `await page.getByLabel('Terms').______()` to tick a checkbox",null,[`check`,`tick`,`select`,`click`],0,`check() is the semantic way to tick a checkbox — it auto-waits for the element to be editable.`],[`F`,`I`,"Complete: `await locator._______('ArrowDown')` to press the down-arrow key",null,[`press`,`key`,`send`,`keyboard`],0,`press() accepts any KeyboardEvent key name.`],[`F`,`I`,"Complete: `await page.getByLabel('Resume').________Files('cv.pdf')`",null,[`setInput`,`attach`,`upload`,`fill`],0,`setInputFiles() is Playwright's file-upload method.`],[`F`,`I`,"Complete: `await page.locator('#src').______To(page.locator('#dst'))`",null,[`drag`,`move`,`transfer`,`drop`],0,`dragTo() performs a drag-and-drop from source to target.`],[`F`,`A`,"Complete: `await locator.click({ ________: ['Control'] })` for Ctrl+Click",null,[`modifiers`,`keys`,`shortcuts`,`buttons`],0,`modifiers accepts an array of modifier keys: Control, Shift, Alt, Meta.`],[`E`,`B`,"What is wrong?\n```ts\nawait page.getByRole('button', { name: 'Submit' }).click;\n```",null,[`getByRole returns undefined for buttons`,`click is missing () — it references the method without calling it`,`name should be text not a string`,`await cannot be used without async`],1,`Missing () means the promise is never created and no click occurs.`],[`E`,`I`,`What is wrong?
\`\`\`ts
await page.getByLabel('Country').selectOption('Australia');
// <select> has only "US" and "UK" options
\`\`\``,null,[`selectOption does not accept strings — use { value: }`,`The call will timeout because the option value does not exist`,`selectOption only works with numeric indices`,`await is unnecessary on selectOption`],1,`Playwright retries until the value matches an option; if it never does, it times out.`],[`E`,`B`,"What is wrong?\n```ts\nawait page.getByLabel('Terms').check();\n// The label targets a <div> not a checkbox\n```",null,[`check() is only valid on <input type="checkbox"> or <input type="radio">`,`Labels cannot target divs in HTML`,`check() always needs force: true`,`The await is in the wrong place`],0,`check() throws when the element is not checkable — use the correct locator or add a role.`],[`E`,`I`,"What is wrong?\n```ts\nawait locator.click({ modifiers: ['ctrl'] });\n```",null,[`'ctrl' is the wrong modifier name — use 'Control'`,`modifiers must be a string, not an array`,`click does not support modifiers`,`ctrl should be uppercase: 'CTRL'`],0,`Playwright modifier names match KeyboardEvent.key: 'Control', 'Shift', 'Alt', 'Meta'.`],[`E`,`I`,"What is wrong?\n```ts\nawait page.getByRole('menu').hover();\nawait page.getByRole('menuitem', { name: 'Delete' }).click();\n// menuitem disappears before click\n```",null,[`hover() auto-dismisses menus after 100ms`,`menuitem is not a valid ARIA role`,`The menu closes because moving the mouse to click the item leaves the hover target`,`click() should be replaced with press('Enter')`],2,`hover() briefly positions the mouse; moving to the menuitem must happen before the menu closes — consider chaining or using a stable anchor.`],[`E`,`A`,`What is wrong?
\`\`\`ts
await page.getByLabel('Upload').setInputFiles('report.pdf');
// The input has type="text", not type="file"
\`\`\``,null,[`setInputFiles cannot upload PDFs`,`setInputFiles only works on <input type="file"> elements`,`The path must be absolute`,`setInputFiles requires await false`],1,`setInputFiles() is exclusively for file inputs — using it on text inputs throws.`],[`D`,`B`,'`click()` fails with "element is not visible". The element exists but a loading spinner covers it. What is the best fix?',null,[`Use click({ force: true })`,`Wait 2 seconds then click`,`Reload the page`,`Use page.evaluate() to click via JS`],0,`force: true skips the visibility check — appropriate when a known overlay exists.`],[`D`,`I`,"`fill()` sets the value but the form validation message never appears. What should you try instead?",null,[`Use type() which is more compatible`,`Use pressSequentially() to fire individual key events that trigger the validation listener`,`Use setValue via JavaScript`,`Add { force: true } to fill`],1,`Validation listeners often fire on keydown/keyup events that fill() bypasses.`],[`D`,`B`,'`check()` throws "element is not editable". The checkbox is visible. What is the likely cause?',null,[`Checkboxes require double-click not check()`,`The checkbox has the disabled or readonly attribute`,`check() only works on radio buttons`,`The locator matched multiple elements`],1,`Disabled checkboxes fail the editable actionability check.`],[`D`,`I`,"`selectOption('dark')` times out even though the dropdown is visible. What should you check first?",null,[`The option's value attribute — it might be 'Dark Mode' not 'dark'`,`Replace with click() on the option directly`,`Increase the global timeout`,`Use selectOption({ index: 1 }) instead`],0,`selectOption matches by value attribute, label text, or index — verify the exact value.`],[`D`,`I`,"`dragTo()` seems to do nothing — the element is not dropped. What is a common cause?",null,[`dragTo() requires force: true on all drag operations`,`The drop zone uses custom JS events that dragTo() does not trigger`,`Drag-and-drop only works in headed mode`,`dragTo() needs two awaits`],1,`Some drag libraries require page.mouse.move/down/up for precise control instead of dragTo.`],[`D`,`A`,"`click({ modifiers: ['Control'] })` opens a new tab. How do you capture the new tab?",null,[`Use click({ modifiers: ['ctrl'] }) in lowercase to prevent new tab`,`Wrap in context.waitForEvent('page') to capture the new tab`,`Replace with page.keyboard.press('Control+Click')`,`Use { force: true } to suppress new tabs`],1,`context.waitForEvent('page') captures popups/new tabs triggered by the action.`],[`C`,`B`,"Complete the code to clear a text input without typing anything new.\n```ts\nawait page.getByLabel('Search').______();\n```",null,[`clear`,`reset`,`empty`,`delete`],0,`clear() removes all text from an input.`],[`C`,`I`,"Complete to select by visible label text.\n```ts\nawait page.getByLabel('Theme').selectOption({ ______: 'Dark Mode' });\n```",null,[`label`,`text`,`name`,`value`],0,`selectOption accepts { label } for visible text, { value } for the value attribute.`],[`C`,`I`,"Complete to Shift+Click a table row.\n```ts\nawait page.locator('tr').nth(3).click({ ______: ['Shift'] });\n```",null,[`modifiers`,`keys`,`buttons`,`shortcuts`],0,`modifiers is the option key for modifier keys in click().`],[`C`,`B`,"Complete to upload two files at once.\n```ts\nawait page.getByLabel('Attachments').setInputFiles(['a.pdf', ______]);\n```",null,[`'b.pdf'`,`{ file: 'b.pdf' }`,`new File(['b.pdf'])`,`Buffer.from('b.pdf')`],0,`setInputFiles() accepts a string path or an array of paths.`],[`C`,`A`,"Complete to scroll the page down 500 pixels with the mouse wheel.\n```ts\nawait page.mouse.______( 0, 500 );\n```",null,[`wheel`,`scroll`,`move`,`drag`],0,`page.mouse.wheel(deltaX, deltaY) scrolls the page programmatically.`],[`L`,`B`,"Why does `fill()` not require a separate `clear()` call before it?",null,[`fill() uses the clipboard which always overwrites`,`fill() automatically clears the input before setting the new value`,`fill() appends to an empty string by default`,`fill() selects all text first via Ctrl+A`],1,`fill() = clear + set. Only use clear() when you want to empty without refilling.`],[`L`,`I`,"When should you choose `pressSequentially()` over `fill()`?",null,[`When the input accepts more than 10 characters`,`When the app has keydown/keyup/input event listeners for auto-suggest, validation, or masking`,`When fill() is unavailable in the version`,`When the input is inside a shadow DOM`],1,`pressSequentially() fires real key events; fill() sets the value without them.`],[`L`,`I`,`Which of these is NOT one of Playwright's actionability checks?`,null,[`The element is attached to the DOM`,`The element is visible`,`The element has no sibling elements`,`The element is not covered by another element`],2,`Playwright checks: attached, visible, stable, enabled/editable, receiving events — siblings are irrelevant.`],[`L`,`B`,"What happens if you call `check()` on a checkbox that is already checked?",null,[`It throws a "already checked" error`,`It unchecks then re-checks the checkbox`,`It does nothing — the checkbox stays checked`,`It always calls click() regardless of current state`],2,`check() is idempotent — it only clicks if the checkbox is not already checked.`],[`L`,`A`,"Why is `locator.click({ trial: true })` useful in CI pipelines?",null,[`It reduces flakiness by clicking more slowly`,`It verifies an element is actionable without side effects — good for asserting readiness`,`It skips the click if CI is slow`,`It records the click for replay`],1,`trial: true validates actionability without triggering the action.`],[`R`,`B`,`You need to fill a login form and press Enter to submit. Which two calls do you need?`,null,[`fill('pass'); press('Enter') on the password field`,`fill('pass'); click('submit')`,`pressSequentially('pass'); keyboard.press('Enter')`,`type('pass'); submit()`],0,`fill() for text, then press('Enter') — the idiomatic login pattern.`],[`R`,`I`,`A multi-select list allows choosing several options. How do you select 'JS' and 'TS' in one call?`,null,[`click('JS'); click('TS') with modifiers`,`selectOption(['JS', 'TS'])`,`check('JS'); check('TS')`,`fill('JS,TS')`],1,`selectOption() accepts an array of values for multi-select elements.`],[`R`,`I`,`How do you right-click a file in a file-manager UI to open its context menu?`,null,[`locator.click({ type: 'context' })`,`locator.click({ button: 'right' })`,`locator.rightClick()`,`locator.contextMenu()`],1,`{ button: 'right' } dispatches the contextmenu event.`],[`R`,`I`,`A user can upload a profile photo. How do you attach a local image in a test?`,null,[`page.getByLabel('Photo').fill('avatar.png')`,`page.getByLabel('Photo').setInputFiles('avatar.png')`,`page.getByLabel('Photo').type('avatar.png')`,`page.getByLabel('Photo').attach('avatar.png')`],1,`setInputFiles() bypasses the OS file picker and attaches the file directly.`],[`R`,`A`,`A kanban board lets you drag a card from "Todo" to "Done". Which call models this?`,null,[`page.locator('.card').moveTo(page.locator('#done'))`,`page.locator('.card').dragTo(page.locator('#done'))`,`page.mouse.click('.card').release('#done')`,`page.drag('.card', '#done')`],1,`dragTo() performs a complete drag-and-drop sequence.`],[`S`,`I`,"A search field triggers autocomplete on each keypress. `fill()` populates the box but suggestions never appear. What is the correct fix?",null,[`Use click() first to activate the field`,`Replace fill() with pressSequentially() so key events trigger the listener`,`Add { force: true } to fill()`,`Use page.evaluate() to set the value`],1,`pressSequentially() fires real keydown/keyup events that the autocomplete listener needs.`],[`S`,`I`,`A modal has a "Confirm Delete" button. The app treats Shift+Click as bypass confirmation. How do you write the call?`,null,[`locator.click({ key: 'Shift' })`,`locator.click({ modifiers: ['Shift'] })`,`page.keyboard.press('Shift'); locator.click()`,`locator.shiftClick()`],1,`modifiers on click() holds the key down during the mouse event.`],[`S`,`A`,"You drop a file onto a drop zone but `dragTo()` does nothing. The zone uses a custom drag library with JS event handlers. What should you try?",null,[`Add force: true to dragTo()`,`Use page.mouse.move() + down() + up() to manually simulate the drag events`,`Switch to headed mode — dragTo() only works headless`,`Reload the page first`],1,`Some drag libraries require low-level mouse events instead of the high-level dragTo().`],[`S`,`A`,'A styled checkbox has opacity:0 on the actual <input>. `check()` fails with "not visible". What are your options?',null,[`Use check({ force: true }) to skip visibility, or click the visible <label> that controls it`,`Use fill(true) instead of check()`,`Set the checkbox via page.evaluate()`,`Replace with selectOption('checked')`],0,`force: true skips visibility; clicking the <label> is the alternative semantic approach.`],[`O`,`B`,"What does `locator.clear()` do?",null,[`Removes the element from the DOM`,`Clears the current value of an input or textarea field`,`Resets all form fields`,`Clears the browser cache`],1,`clear() is equivalent to triple-clicking to select all then pressing Delete — it empties the input without typing new text.`],[`O`,`I`,"What does `locator.type(text)` do differently from `fill(text)`?",null,[`type() triggers character-by-character keyboard events; fill() sets the value directly. type() is slower but fires all keydown/keypress/keyup events.`,`They are identical`,`fill() triggers keyboard events; type() does not`,`type() is deprecated and fill() should always be used`],0,`type() simulates real typing and fires all keyboard events — useful for apps that listen to individual key events. fill() is faster but skips individual key events.`],[`O`,`B`,'What does `page.keyboard.press("Enter")` do?',null,[`Submits any focused form`,`Fires a keydown then keyup event for Enter on whatever element currently has focus — the application handles it`,`Navigates to the next page`,`Clicks the first button on the page`],1,`keyboard.press() sends keyboard events to the focused element — the behavior depends entirely on the application's event listeners.`],[`O`,`I`,"What does `locator.focus()` do?",null,[`Highlights the element in the browser`,`Programmatically focuses the element, firing focus and focusin events — subsequent keyboard.press() calls target that element`,`Scrolls the element into view`,`Enables the element if it was disabled`],1,`focus() is useful when you need to type using keyboard.press() on a specific element without clicking it first.`],[`O`,`B`,"What does `locator.blur()` do?",null,[`Removes the element from the DOM`,`Removes focus from the element, firing blur and focusout events`,`Clears the element's value`,`Makes the element visually blurred via CSS`],1,`blur() is used to trigger form validation that fires on blur events — test inline validation messages.`],[`O`,`I`,"What does `page.mouse.move(x, y)` do?",null,[`Navigates to coordinates (x, y)`,`Moves the mouse cursor to absolute pixel coordinates — fires mousemove events that the application can respond to`,`Scrolls to position (x, y)`,`Clicks at coordinates (x, y)`],1,`mouse.move() is useful for hover-triggered UI (tooltips, dropdowns) that require a physical mouse movement rather than an explicit element hover.`],[`O`,`I`,'What is the difference between `locator.click()` and `locator.dispatchEvent("click")`?',null,[`They are identical`,`locator.click() simulates a real user click (waits for element, scrolls into view, fires all pointer events); dispatchEvent fires a synthetic event on the element directly, bypassing Playwright's auto-wait and the full event chain`,`dispatchEvent is for custom events only`,`locator.click() is deprecated in favour of dispatchEvent`],1,`click() is the recommended API — dispatchEvent is an escape hatch for custom events or when the standard click doesn't work as expected.`],[`O`,`B`,'What does `locator.selectOption("value")` do for a `<select>` element?',null,[`Clicks the option with that text`,`Sets the select's value to the matching option and fires change and input events`,`Types the value into the select`,`Finds and clicks the option in a dropdown`],1,`selectOption accepts value, label, or index — it directly sets the selection and fires the appropriate events.`],[`O`,`A`,"What does `page.mouse.wheel(deltaX, deltaY)` do?",null,[`Navigates the page history`,`Fires a wheel event at the current mouse position — triggers scroll handlers in the application`,`Scrolls the page by the specified pixels`,`Zooms the viewport`],1,`mouse.wheel() fires the wheel event without actually scrolling the page — useful for testing custom scroll event handlers.`],[`O`,`I`,'What does `locator.press("Tab")` do?',null,[`Opens a new browser tab`,`Fires Tab keydown/keyup on the element, typically moving focus to the next focusable element`,`Submits the current form`,`Moves to the next matching element`],1,`press("Tab") is used to test keyboard navigation — verify focus order through form fields and interactive elements.`],[`O`,`I`,"What does the `trial: true` option do in `locator.click({ trial: true })`?",null,[`Runs the click in trial mode with extra logging`,`Performs all pre-click checks (visibility, enabled, stable) WITHOUT actually dispatching the click event — useful for verifying a click would succeed before performing it`,`Retries the click up to 3 times`,`Enables slow motion for this click`],1,`trial: true is a dry-run mode — Playwright checks all preconditions but stops before the actual click, useful for debugging "why is this click failing".`],[`F`,`B`,'Complete to press Enter on a focused element.\n```ts\nawait page.keyboard.______("Enter");\n```',null,[`press`,`key`,`send`,`type`],0,`keyboard.press() sends a keydown + keyup pair for the specified key.`],[`F`,`B`,'Complete to clear an input field.\n```ts\nawait page.getByLabel("Search").______();\n```',null,[`clear`,`reset`,`empty`,`delete`],0,`locator.clear() empties the input field and fires the appropriate input events.`],[`F`,`I`,'Complete to upload a file.\n```ts\nawait page.getByLabel("Resume").______("resume.pdf");\n```',null,[`setInputFiles`,`upload`,`attach`,`sendFile`],0,`setInputFiles() attaches files to an <input type="file"> element without triggering the OS file picker.`],[`F`,`I`,'Complete to focus an element.\n```ts\nawait page.getByLabel("First name").______();\nawait page.keyboard.type("Alice");\n```',null,[`focus`,`activate`,`click`,`hover`],0,`focus() programmatically focuses the element so subsequent keyboard.type() targets it.`],[`F`,`B`,'Complete to double-click an element.\n```ts\nawait page.getByRole("cell").______();\n```',null,[`dblclick()`,`doubleClick()`,`click({ clickCount: 2 })`,`Both A and C work`],3,`Both dblclick() and click({ clickCount: 2 }) produce a double-click — dblclick() is more readable.`],[`F`,`I`,'Complete to right-click an element for a context menu.\n```ts\nawait page.getByRole("row").______({ button: "right" });\n```',null,[`click`,`rightClick`,`contextMenu`,`press`],0,`click({ button: "right" }) fires a right-click, triggering contextmenu events.`],[`F`,`A`,'Complete to press multiple keys simultaneously.\n```ts\nawait page.keyboard.______("Control+A");\n```',null,[`press`,`hold`,`down`,`chord`],0,`keyboard.press("Control+A") sends the key combination as a chord — Ctrl held down while A is pressed.`],[`E`,`B`,'What is wrong?\n```ts\nawait page.getByRole("button", { name: "Submit" }).fill("test");\n```',null,[`Buttons cannot have names`,`fill() is only for input/textarea elements — calling it on a button throws an error. Use click() or textContent() on buttons.`,`fill() requires visible: true`,`Buttons must use type() not fill()`],1,`fill() is exclusively for editable form fields (input, textarea, contenteditable). Buttons are not editable.`],[`E`,`I`,'What is wrong?\n```ts\nawait page.getByLabel("Password").type("secret");\nawait expect(page.getByLabel("Password")).toHaveValue("secret");\n// sometimes fails\n```',null,[`type() does not set the value property`,`type() fires keyboard events but may be intercepted by the application — if the app masks or transforms input during typing, the final value may differ. Use fill() for reliable value setting, type() only when key events matter.`,`toHaveValue doesn't work with password fields`,`type() is asynchronous and needs an extra await`],1,`type() simulates keystrokes — application key handlers can intercept and modify the typed value. fill() sets the value directly.`],[`E`,`B`,'What is wrong?\n```ts\nawait page.locator("input").fill("hello");\n// Strict mode violation if multiple inputs exist\n```',null,[`fill() requires a single element`,`Page.locator("input") with no qualifier matches ALL inputs — strict mode throws if more than one is found. Use a labelled locator: page.getByLabel("Name").`,`Strict mode cannot be disabled for fill`,`fill() must include a type option`],1,`Unscoped CSS selectors matching multiple elements cause strict mode violations — always use a semantic locator or scope with a parent.`],[`E`,`I`,"What is wrong?\n```ts\nawait locator.click({ timeout: 0 });\n```",null,[`timeout must be a string`,`timeout: 0 disables the timeout entirely — if the element never becomes ready, the test hangs indefinitely. Only use this when you are certain the element will be immediately available.`,`0 is not a valid timeout value`,`click() does not accept a timeout option`],1,`timeout: 0 means infinite wait — it removes the safety net. Use it only in controlled situations where hanging is impossible.`],[`E`,`A`,'What is wrong?\n```ts\nawait page.mouse.down();\nawait page.goto("/other-page"); // mid-drag navigation\nawait page.mouse.up();\n```',null,[`mouse.down() cannot cross navigations`,`Navigation while a mouse button is held causes undefined behavior — the mouse state is reset on navigation. Complete the drag on the same page.`,`mouse.up() must be called before goto()`,`goto() cancels all pending mouse events`],1,`Browser navigations reset pointer state — always complete mouse sequences (down → move → up) on the same page.`],[`E`,`I`,'What is wrong?\n```ts\nawait page.getByRole("combobox").selectOption({ value: "us" });\n// expects USA to be selected but Germany is selected\n```',null,[`selectOption requires label not value`,`selectOption({ value: "us" }) selects the option whose value attribute equals "us" exactly — if the option's value attribute is "US" (uppercase), it does not match. Check the exact value attribute in the HTML.`,`combobox role requires click() first`,`selectOption fires change but not input events`],1,`selectOption value matching is case-sensitive and must match the option's value attribute exactly — inspect the HTML to confirm the casing.`],[`D`,`B`,"`locator.click()` succeeds but the form is not submitted. What should you check?",null,[`The button must have type="submit"`,`Check if click() hit the button or an overlapping element — use the Trace Viewer to see the actual click target and its coordinates. Also verify the button triggers form submission vs. a JavaScript click handler.`,`Add force: true to bypass validation`,`Increase the click timeout`],1,`The Trace Viewer shows exactly which element received the click — an overlapping element is a common cause of "click happened but nothing changed".`],[`D`,`I`,"`pressSequentially()` works but autocomplete suggestions appear then immediately disappear. Why?",null,[`pressSequentially() is too slow`,`An event handler closes the suggestions on blur or after a keyup — a following action (like the assertion) might trigger blur. Add a waitFor on the suggestions before asserting.`,`pressSequentially() fires events in wrong order`,`Suggestions require click() on the input first`],1,`Timing between keystroke events and suggestion appearance can be tight — wait for the suggestions to be visible before interacting with them.`],[`D`,`B`,'`locator.check()` throws "locator.check: Not a checkbox or radio button". What is the likely cause?',null,[`check() is deprecated`,`The locator matched a different element type — perhaps a div styled as a checkbox. Use page.evaluate to check the element type, or use a more specific locator targeting the actual <input type="checkbox">.`,`Checkboxes must use fill(true)`,`check() requires type="checkbox" explicitly`],1,`Styled checkbox components often hide the real input — the locator may be hitting the visual wrapper instead of the actual input.`],[`D`,`I`,"`dragTo()` runs but the drop target shows no change. The app uses the HTML5 Drag and Drop API. What should you check?",null,[`dragTo() doesn't support HTML5 DnD`,`HTML5 DnD requires dataTransfer — dragTo() may not populate it. Use page.evaluate to manually dispatch drag events with a dataTransfer object, or use mouse.move/down/up with the exact coordinates.`,`Add force: true to dragTo()`,`HTML5 DnD requires headless: false`],1,`Some HTML5 DnD implementations check dataTransfer contents — manual event dispatching with proper dataTransfer is sometimes necessary.`],[`D`,`A`,'`page.keyboard.press("Escape")` does not close a modal. What are two things to check?',null,[`Escape key is blocked in Playwright`,`1) The modal may not have focus — click() it first or call focus() on the modal. 2) The application may handle Escape on a specific element (like a close button) rather than globally on document.`,`Escape requires keyboard.down then keyboard.up separately`,`Escape is a Chromium-only shortcut`],1,`Keyboard events go to the focused element — if the modal isn't focused, Escape goes nowhere. Focus the modal or a focusable element inside it first.`],[`C`,`B`,'Complete to type text character by character.\n```ts\nawait page.getByRole("combobox").______("New York");\n```',null,[`pressSequentially`,`type`,`fill`,`keyboard.type`],0,`pressSequentially() fires individual key events — useful for autocomplete that reacts to each keystroke.`],[`C`,`I`,'Complete to drag a locator to a target.\n```ts\nawait page.locator(".card").______(\n  page.locator("#done-column")\n);\n```',null,[`dragTo`,`moveTo`,`dropTo`,`drag`],0,`dragTo() performs the full drag-and-drop gesture from the source element to the target.`],[`C`,`B`,'Complete to select by visible option text.\n```ts\nawait page.getByRole("combobox").______({ label: "United States" });\n```',null,[`selectOption`,`pick`,`choose`,`set`],0,`selectOption({ label: "..." }) selects by the visible text of the option — as opposed to { value } which uses the value attribute.`],[`C`,`I`,'Complete to scroll within a scrollable container.\n```ts\nawait page.getByRole("log").______({ deltaY: 300 });\n```',null,[`dispatchEvent("wheel",`,`scroll`,`evaluate(el => el.scrollTop +=`,`hover`],0,`dispatchEvent("wheel", { deltaY: 300 }) fires a wheel event on the element — triggers scroll listeners in scrollable containers.`],[`C`,`A`,'Complete to perform a triple-click to select all text.\n```ts\nawait page.getByLabel("Content").______({ clickCount: 3 });\n```',null,[`click`,`dblclick`,`tripleClick`,`focus`],0,`click({ clickCount: 3 }) fires three rapid clicks — standard triple-click behavior in inputs and textareas selects all text.`],[`L`,`B`,`Why does Playwright auto-wait before actions like click() and fill()?`,null,[`For legal compliance with browser automation regulations`,`To ensure the element is visible, enabled, stable (not animating), and in the viewport before interacting — mimicking a real user who cannot click a hidden or moving element`,`To throttle test execution speed`,`To allow event handlers to register`],1,`Auto-wait prevents flaky tests caused by clicking before the UI is ready — Playwright performs the same checks a human user would naturally pass.`],[`L`,`I`,"What is the difference between `force: true` and removing auto-wait?",null,[`They are identical`,`force: true dispatches the event to the element regardless of its visibility/enabled/stable state — it bypasses the auto-wait checks but still targets the specific element. It does not remove timeout — it just skips precondition checks.`,`force: true disables retries`,`force: true is equivalent to dispatchEvent()`],1,`force: true is a targeted bypass — use it only when you intentionally need to interact with an element that Playwright would normally refuse (e.g. opacity:0 custom inputs).`],[`L`,`I`,"When is `dispatchEvent` preferable to standard action methods?",null,[`Always — it is faster`,`When you need to fire custom events (like "myapp:confirm") that have no Playwright API equivalent, or when you need to pass custom event details that standard actions cannot`,`dispatchEvent is only for mouse events`,`dispatchEvent is preferred in Firefox`],1,`Custom events and detailed event data are the main use cases — for standard interactions (click, fill, check), always prefer the dedicated Playwright action methods.`],[`L`,`B`,"What check does `locator.click()` perform before clicking?",null,[`Only visibility`,`Visible → enabled → stable (not animating) → in viewport → receives pointer events — all five checks must pass before the click is dispatched`,`Only enabled`,`Only attached to DOM`],1,`The five preconditions mirror real-user interaction — Playwright won't click a hidden, disabled, still-animating, off-screen, or pointer-events:none element.`],[`L`,`A`,'Can `fill()` trigger form validation errors that fire on the "input" event?',null,[`No — fill() bypasses all events`,`Yes — fill() fires input and change events after setting the value, so any validation handlers listening to "input" will run`,`Only in Chromium`,`Only if the input has a required attribute`],1,`fill() sets the value then fires input and change — validation handlers on these events will fire and show validation messages.`],[`R`,`B`,`A rich text editor (contenteditable div) does not respond to fill(). How do you type into it?`,null,[`focus() then keyboard.type()`,`page.getByRole("textbox").fill() — fill works on contenteditable`,`Contenteditable elements cannot be tested`,`page.evaluate(el => el.innerHTML = text)`],0,`Contenteditable divs often need keyboard.type() after focus() — or pressSequentially() — since they rely on real keypress events for their internal editor logic.`],[`R`,`I`,`A date picker opens a calendar on click. You need to select "2025-03-15". What is the most reliable approach?`,null,[`fill("2025-03-15") on the date input`,`Click the calendar popup navigation to March 2025, then click the 15th cell by its content — or set the value directly via fill/keyboard if the input accepts typed dates`,`Use page.evaluate to set input.value`,`datepickers cannot be tested reliably in Playwright`],1,`The right approach depends on the picker library — some accept typed input; others require UI navigation. Inspect the rendered HTML to decide.`],[`R`,`A`,`A slider input needs to be dragged to 75% of its track. How?`,null,[`locator.fill("75")`,`Calculate the target x-coordinate (trackStart.x + trackWidth * 0.75) then use mouse.move + down + up, or use page.evaluate to set input.value and dispatch change events`,`locator.dragTo({ x: 75 })`,`locator.setValue(75)`],1,`Native range inputs can often be set via evaluate + dispatch. Custom sliders need manual mouse coordinates.`],[`R`,`I`,`A form has a "Country" combobox that filters options as you type. How do you select "Germany"?`,null,[`page.getByRole("combobox").selectOption("Germany")`,`page.getByRole("combobox").fill("Ger") then wait for the "Germany" option to appear and click it`,`page.getByRole("combobox").type("Germany") then press Enter`,`All of the above may work — depends on implementation`],3,`It depends on the implementation — a native select uses selectOption; a custom combobox requires typing and then clicking the suggestion. Inspect the component type first.`],[`R`,`B`,`You need to verify a file input accepts a PDF but rejects a PNG. How do you test both?`,null,[`Mock the file type validation with page.evaluate`,`Set a PDF with setInputFiles and assert success; set a PNG and assert the validation error message — both are real test cases for file type validation`,`File inputs cannot be tested in Playwright`,`Use route() to intercept file uploads`],1,`setInputFiles works for both test cases — test the happy path and the error case in separate tests.`],[`S`,`B`,`A form has autofill that replaces your typed value with a stored email. How do you prevent this?`,null,[`Disable autofill in playwright.config.ts`,`Set autocomplete: "off" on the input is the app-level fix, but in Playwright: use page.evaluate to set the field's autocomplete property to "off" before fill(), or disable autofill in the browser context via extraHTTPHeaders`,`Autofill cannot be disabled in tests`,`Use pressSequentially to bypass autofill`],1,`Autofill interference is a browser feature — context-level disabling via browser launch args (--disable-autofill-keyboard-accessory-view) or input-level autocomplete="off" are the solutions.`],[`S`,`I`,`You need to test keyboard shortcut Ctrl+Shift+P opens a command palette. How?`,null,[`page.keyboard.press("Ctrl"); page.keyboard.press("Shift"); page.keyboard.press("P");`,`await page.keyboard.press("Control+Shift+P") — Playwright combines modifiers with + notation in a single press call`,`page.keyboard.chord("ctrl","shift","p")`,`page.getByRole("menu").press("Control+Shift+P")`],1,`keyboard.press() with the + notation sends all modifiers simultaneously — matching how a user would press the shortcut.`],[`S`,`A`,`A rich text editor loses focus when you switch to another test action, causing typed text to be lost. What is the fix?`,null,[`Use fill() instead of type()`,`Keep keyboard interactions within a test.step: first click the editor, then type, then immediately assert — do not interleave actions on other elements between typing and asserting`,`Set autosave on the editor`,`Use evaluate() to insert text programmatically`],1,`Interleaving actions on other elements can blur the editor — group editor interactions together and avoid context switches mid-typing.`],[`S`,`I`,`You test that pressing Tab from the last form field wraps focus back to the first. How do you assert this?`,null,[`expect(page.focusedElement).toBe(firstField)`,`await firstField.focus(); then await page.keyboard.press("Tab") × N times (once per field), then await expect(firstField).toBeFocused()`,`Use page.accessibility to check focus order`,`Tab cycling cannot be tested in Playwright`],1,`Playwright tracks focus — expect(locator).toBeFocused() asserts the element has focus. Tab through the form until you return to the start.`],[`L`,`A`,"Playwright actions like `click()` and `fill()` include built-in 'actionability checks' before executing. What do these checks verify?",null,[`That the element is attached to the DOM, visible, stable (not animating), enabled, and not obscured by another element — all before the action fires`,`Only that the element exists in the DOM, nothing else`,`That the network is idle before any action runs`,`That the page has fully loaded (load event fired)`],0,`This is why Playwright rarely needs manual waitForTimeout() calls — actions retry the actionability checks automatically until they pass or the timeout elapses.`],[`O`,`A`,"Why is `waitFor({ state: 'visible' })` largely redundant here?",`await page.locator('#save').click();
await page.locator('#toast').waitFor({ state: 'visible' });
await expect(page.locator('#toast')).toHaveText('Saved!');`,["`expect(...).toHaveText()` already auto-retries/polls until the assertion passes or times out, so it implicitly waits for the toast to become visible with the right text",`waitFor() and toHaveText() are aliases for the same internal function`,`click() already waits for #toast to appear before returning`,`waitFor() is deprecated and does nothing in current Playwright versions`],0,`Web-first assertions (expect(locator).toHave...) are auto-retrying by design — an explicit waitFor before them is usually unnecessary.`],[`D`,`I`,`This test fails intermittently with 'element is not stable' errors. What's the most likely fix?`,`await page.locator('.card').click();`,[`The element is likely mid-animation when the click is attempted — Playwright already waits for stability, so the real fix is usually to reduce/disable animations in the test environment rather than adding a blind sleep`,`Replace click() with dispatchEvent('click') to skip all actionability checks`,"Add `{ force: true }` to bypass the instability as a best practice",`The bug is a network timing issue, so add a waitForLoadState('networkidle')`],0,'"Not stable" means the bounding box is still changing between animation frames — a flaky failure usually means the animation duration exceeds the default timeout; `force: true` should be a last resort since it also skips other safety checks.'],[`S`,`I`,`A test clicks a button that triggers an API call, and the resulting data appears in a list a moment later. Which is the correct way to wait for the list to update, per Playwright best practices?`,null,["`await expect(page.locator('.list-item')).toHaveCount(expectedCount)` — assert on the resulting UI state directly, letting auto-retry handle the timing","`await page.waitForTimeout(2000)` then assert","`await page.waitForLoadState('networkidle')` immediately after the click, with no further assertion","Wrap the click in `Promise.all` with `page.waitForNavigation()`"],0,`Asserting on the actual state you care about (with an auto-retrying web-first assertion) is more reliable than arbitrary fixed waits or generic network-idle waits.`],[`O`,`I`,`What does this test?`,`const source = page.locator('#item-3');
const target = page.locator('#trash-bin');
await source.dragTo(target);`,[`A drag-and-drop interaction — Playwright's dragTo() simulates the full sequence of mouse events (mousedown on source, move, mouseup on target) needed to trigger drag-and-drop handlers`,`It simply teleports the element in the DOM without simulating real mouse events`,`dragTo() only works for native HTML5 drag-and-drop, never custom JS-based implementations`,`It requires manually calling page.mouse.down() and page.mouse.up() separately; dragTo() does not exist`],0,`dragTo() is a high-level convenience that composes the low-level mouse actions a real drag gesture requires.`],[`O`,`I`,`What does this accomplish?`,`await page.getByLabel('Upload resume').setInputFiles('./fixtures/resume.pdf');`,[`It attaches the specified local file to a file input element, exactly as if a user had selected it in the OS file picker dialog — without needing to interact with any real OS-level dialog`,`It opens the OS file picker dialog and waits for a human to manually select a file`,`It uploads the file directly via an HTTP request, bypassing the page entirely`,`setInputFiles only works with image files, not PDFs`],0,`setInputFiles() is specifically designed to sidestep the fact that OS-native file picker dialogs cannot be automated directly — it sets the input's files programmatically instead.`],[`O`,`I`,`What does this sequence simulate?`,`await page.getByRole('textbox', { name: 'Search' }).press('Control+A');
await page.keyboard.press('Delete');
await page.keyboard.type('playwright');`,[`Select-all in the search box, delete the selected text, then type a new query — simulating a real user clearing and replacing input text via keyboard shortcuts`,`It presses all three keys simultaneously as one combined shortcut`,`keyboard.type() only works after calling page.goto() a second time`,`press("Control+A") selects every element on the entire page, not just text`],0,`Chaining press() for shortcuts and type() for literal text is the standard way to simulate realistic keyboard-driven interactions.`],[`O`,`I`,"What does this do for a native `<select>` element?",`await page.getByLabel('Country').selectOption('CA');`,["It selects the `<option>` whose value attribute is 'CA', firing the same change event a real user selecting it would trigger",`It types the literal text "CA" into the dropdown as if it were a text input`,`It removes the option with value "CA" from the list`,`selectOption only works with custom JS-based dropdowns, never native <select> elements`],0,`selectOption() is purpose-built for native <select> elements — it can match by value, label, or index, and properly fires the change event.`],[`L`,`I`,"Why does Playwright automatically scroll an element into view before clicking it, without any explicit `scrollIntoView()` call in the test?",null,[`Scrolling-into-view is part of Playwright's built-in actionability checks before performing an action — a real user cannot click something outside the viewport either, so the framework handles this automatically to mimic realistic interaction`,`It does not scroll automatically; every click requires a manual scroll call first`,`Scrolling only happens for elements at the very bottom of a page, never elsewhere`,`This behavior is a bug that should be worked around with force: true`],0,`This is one of several actionability checks (alongside visibility, stability, enabled state) baked into every action call, precisely to remove the need for manual scroll-then-click boilerplate.`],[`D`,`I`,`A click action on a specific button consistently times out after the default 30s, even though the element is visible and enabled — it just has an unusually slow-to-attach event handler due to heavy JS initialization. What's a targeted, minimal fix?`,`await page.locator('#slow-init-btn').click();`,["Override the timeout for just this one action: `await page.locator('#slow-init-btn').click({ timeout: 60000 });` — scoping the extended timeout to the specific slow action instead of inflating the global timeout for the whole suite",`Increase the GLOBAL test timeout to 5 minutes to fix this one slow element`,`Use force: true to skip all actionability checks and click immediately regardless of readiness`,`Replace click() with dispatchEvent('click'), which has no timeout at all`],0,`Per-action timeout overrides are the surgical fix for a genuinely slow, specific interaction — a blanket global timeout increase would mask real timing issues elsewhere in the suite.`],[`R`,`I`,`You need to verify that a custom keyboard-navigable dropdown correctly moves focus to the next item when pressing ArrowDown. How do you assert this in Playwright?`,null,["Press the key (`await page.keyboard.press('ArrowDown')`) and then assert focus with `await expect(page.getByRole('option', { name: 'Next Item' })).toBeFocused()`",`Focus cannot be tested in Playwright; only click-based interactions are supported`,"Check `document.activeElement` manually using console.log statements, since there is no built-in assertion for focus",`Use toBeVisible() as a proxy for focus state, since a focused element is always the only visible one`],0,`toBeFocused() is a dedicated web-first assertion built exactly for verifying keyboard-navigation and focus-management behavior.`],[`L`,`I`,`What context configuration is required before locator.tap() can be used to simulate a touch interaction?`,null,[`The BrowserContext must be created with hasTouch: true (or via a mobile device preset that already sets it), otherwise tap() throws since the browser has no touch capability enabled`,`No special configuration is needed; tap() works identically to click() in every context`,`tap() requires launching the browser with headless: false`,`tap() only works when the viewport width is below 400 pixels`],0,`Touch event support is a browser capability that must be explicitly enabled per context, mirroring how a real device either does or does not support touch input.`],[`O`,`I`,`What does this setup enable for the subsequent action?`,`const context = await browser.newContext({ hasTouch: true });
const page = await context.newPage();
await page.getByRole('button', { name: 'Menu' }).tap();`,[`It creates a touch-capable context so that tap() can dispatch realistic touch events (touchstart/touchend) instead of mouse events, letting you test touch-specific handlers`,`It has no effect; tap() works the same with or without hasTouch`,`It forces the browser into mobile emulation mode with a fixed viewport size`,`hasTouch only affects pinch-to-zoom gestures, not tap()`],0,`tap() is specifically meant to fire the touch event sequence, and hasTouch is the prerequisite that makes the underlying browser willing to generate those events at all.`],[`L`,`A`,`What is the difference between locator.tap() and the lower-level page.touchscreen.tap(x, y)?`,null,[`locator.tap() resolves an element first, runs actionability checks, and taps its center (or a given position) exactly like click() but with touch events; page.touchscreen.tap(x, y) is a raw, position-only primitive with no element resolution, waiting, or actionability checking at all`,`They are aliases for the exact same underlying implementation`,`page.touchscreen.tap() automatically finds the nearest tappable element to the given coordinates`,`locator.tap() cannot accept a position option, unlike page.touchscreen.tap()`],0,`This mirrors the general locator-action-vs-raw-input-primitive distinction seen with mouse/keyboard: the locator-based version gives you safety and auto-waiting, the raw primitive gives you precise, low-level control with none of that.`],[`L`,`I`,`What does locator.setChecked(boolean) do, compared to calling check() or uncheck() separately?`,null,[`It unifies both operations into a single call driven by a boolean, internally calling check() or uncheck() as appropriate, which is convenient when the desired state is computed dynamically (e.g. from test data) rather than hardcoded`,`It only works for radio buttons, never checkboxes`,`It sets the checked state without firing any change or input events, unlike check()/uncheck()`,`setChecked() is a read-only method and cannot modify the checkbox at all`],0,`Parameterized tests that loop over both true/false expectations benefit from a single, boolean-driven call instead of branching between two differently-named methods.`],[`O`,`I`,`What is the benefit of using setChecked() in this data-driven test compared to an if/else with check()/uncheck()?`,`for (const { label, expected } of testCases) {
  await page.getByLabel(label).setChecked(expected);
}`,[`It lets the desired checkbox state come directly from test data as a boolean, avoiding an explicit if/else branch that chooses between check() and uncheck() for each case`,`setChecked() runs twice as fast as check()/uncheck() internally`,`It is required syntax; check()/uncheck() cannot be used inside a for loop`,`setChecked() automatically generates the test cases from the checkbox's current state`],0,`Data-driven tests read more cleanly when the action itself accepts the varying parameter directly, rather than the test author writing the branching logic by hand each time.`],[`L`,`A`,`What does locator.selectText() do, and when does it actually matter versus using fill()?`,null,[`It selects (highlights) all existing text within an input, textarea, or contenteditable element without deleting it; it matters when you plan to type over the selection manually via keyboard actions (which respects the current selection), since fill() already clears the field on its own and has no need for a prior selection step`,`It permanently deletes the currently selected text from the field`,`selectText() is a synonym for fill() with no meaningful difference`,`It only works on read-only fields, since editable fields do not need explicit selection`],0,`selectText() is useful specifically as a setup step before manual keyboard typing (which types over whatever is selected), not before fill(), which already clears the field itself as part of its own behavior.`],[`O`,`I`,`Why is selectText() called here before typing, rather than just calling keyboard.type() directly?`,`await page.getByLabel('Coupon code').selectText();
await page.keyboard.type('SAVE20');`,[`Selecting the existing text first means the subsequent typed characters replace the whole selection, overwriting any pre-filled coupon code, rather than being inserted alongside it`,`selectText() is required before keyboard.type() can be called at all, or it throws`,`selectText() automatically submits the form after selecting`,`This code is equivalent to a single fill() call with no functional difference in any scenario`],0,`Because raw keyboard.type() inserts at the current cursor position (or replaces a selection, if one exists), pre-selecting the existing content is what makes the typed text overwrite rather than append.`],[`L`,`I`,`A test wants to overwrite a pre-filled field using fill(), and a teammate suggests calling selectText() first as a precaution. Is that necessary?`,null,[`No, fill() already clears the field's existing value before setting the new one as part of its own built-in behavior, so a preceding selectText() call is redundant when using fill(); selectText() only matters before manual keyboard-based typing`,`Yes, fill() appends rather than replaces unless the field is pre-selected first`,`Yes, without selectText(), fill() silently fails on any pre-filled field`,`No, but only because fill() automatically calls selectText() internally, making the manual call harmless but wasteful either way`],0,`Recognizing that fill() and manual keyboard typing have fundamentally different relationships to existing field content avoids adding unnecessary steps to already-correct code.`],[`L`,`I`,`What does the position option in locator.click({ position: { x, y } }) let you do that a plain click() cannot?`,null,[`It clicks at a specific pixel offset relative to the element's top-left corner, rather than the element's center (the default), which matters for elements like a canvas, a slider track, or any custom-drawn UI where different points within the same element have different meaning`,`It clicks at absolute page coordinates instead of relative to the element`,`It has no effect and is silently ignored by all current browser engines`,`It requires the element to have position: absolute set in its own CSS`],0,`For a genuinely large or spatially meaningful element (unlike a small button where the center is fine), being able to target an arbitrary point within it is essential.`],[`O`,`I`,`What is this click targeting on the slider track element?`,`await page.locator('.slider-track').click({ position: { x: 150, y: 10 } });`,[`A specific point 150 pixels right and 10 pixels down from the slider track element's own top-left corner, letting the test click a precise position along the track rather than always hitting its center`,`The 150th pixel of the entire page, ignoring the element's own position`,`It clicks the element's center regardless of the position values given`,`It scrolls the page so the element is 150 pixels from the top of the viewport before clicking its center`],0,`position coordinates are always relative to the target element's own bounding box, not the page, which is what makes them useful for elements like sliders where the click location itself carries meaning.`],[`R`,`I`,`You need to click on a specific bar within an HTML5 <canvas>-based chart component to trigger its tooltip. Since a canvas has no addressable DOM sub-elements, how do you target that specific bar?`,null,[`Compute the bar's approximate pixel location using the canvas element's boundingBox() plus known chart geometry (e.g. bar index and chart dimensions), then click() the canvas locator with a position option set to those coordinates`,`Use getByRole('img') to find the individual bar, since canvas elements expose each drawn shape as a separate accessible element`,`This cannot be tested at all since canvas content has no DOM representation`,`Use page.evaluate() to directly call the chart library's internal click handler by name, bypassing Playwright's click() entirely`],0,`A canvas is a single opaque element to the DOM; the only way to interact with a specific visual feature inside it is by computing real pixel coordinates and using position-based clicking.`],[`L`,`I`,`What does the delay option in locator.click({ delay: ms }) control?`,null,[`The time between the mousedown and mouseup events that make up the click, letting you simulate a longer press/hold rather than an instantaneous click, useful for UI that specifically listens for a held-down duration`,`The time Playwright waits before starting the click's actionability checks`,`The delay before the click event bubbles up to parent elements`,`It slows down the whole test's execution speed globally, not just this one click`],0,`Some UI patterns (long-press to reveal a menu, hold-to-confirm) specifically distinguish a quick tap from a longer hold, and delay is the tool for simulating that distinction.`],[`S`,`A`,`An app requires the user to press and hold a Delete button for 2 seconds before the delete action actually triggers, to prevent accidental taps. How do you test this specific interaction?`,null,[`Use locator.click({ delay: 2000 }) to hold the simulated mouse button down for the required duration before releasing, then assert the deletion occurred; also test a SHORTER delay to confirm the action does NOT trigger prematurely`,`Click the button normally and assume the app's own timer handles everything correctly without needing to simulate the hold duration`,`Use page.waitForTimeout(2000) after a normal click, since the delay location does not matter`,`This interaction pattern cannot be tested since Playwright clicks are always instantaneous`],0,`Testing both the positive case (long enough hold triggers it) and the negative case (too-short hold does not) gives real confidence in a hold-to-confirm interaction, rather than just assuming the happy path works.`],[`L`,`A`,`What do keyboard.down(key) and keyboard.up(key) let you do that keyboard.press(key) cannot on its own?`,null,[`They let you hold a key down across multiple separate subsequent actions (like several clicks), rather than press()'s single, self-contained keydown-then-keyup pair for one atomic key press`,`They are simply slower, less efficient aliases for press() with no functional difference`,`down()/up() only work with modifier keys, never with letter or number keys`,`press() actually calls down() and up() with a mandatory 5-second gap between them internally`],0,`Holding Shift or Control while performing several separate clicks, for a multi-select interaction, is exactly the scenario where the key needs to stay down across more than one subsequent action.`],[`O`,`I`,`What interaction is this code simulating?`,`await page.keyboard.down('Shift');
await rowA.click();
await rowC.click();
await page.keyboard.up('Shift');`,[`A shift-click range selection: holding Shift down across two separate row clicks, exactly as a user would to select a contiguous range in a file manager or spreadsheet`,`Two completely independent, unrelated clicks with no connection to each other`,`A single double-click on rowA followed by an unrelated click on rowC`,`It throws an error because keyboard.down() cannot span multiple locator actions`],0,`keyboard.down()/up() bracketing multiple actions is precisely how you simulate a user holding a modifier key across a sequence of interactions, rather than a single keypress.`],[`R`,`I`,`You need to test a file manager's shift-click range-select feature: clicking the first item, then shift-clicking the last item, should select every item in between. How do you write this?`,null,[`Click the first item normally, then await page.keyboard.down('Shift'); click the last item; await page.keyboard.up('Shift'); and finally assert that all items in between show a selected state`,`Pass { modifiers: ['Shift'] } to both the first and last click calls independently`,`Use dragTo() from the first item to the last item to simulate the range selection`,`Click each item in the range individually with no modifier keys at all`],0,`Because the range-select behavior depends on Shift being held specifically during the SECOND click while the first click already established a starting point, keyboard.down/up bracketing the second click is the correct pattern, as opposed to modifiers on a single click().`],[`L`,`A`,`What does the steps option in page.mouse.move(x, y, { steps }) do, and why does it matter for some drag-and-drop libraries?`,null,[`It generates that many intermediate mousemove events along the path to the destination, rather than jumping directly there in one instantaneous move; some sortable/drag libraries compute drag distance or velocity from a sequence of mousemove samples and do not respond correctly to a single instantaneous jump`,`It controls how many pixels the mouse moves per virtual millisecond, affecting only animation smoothness in screen recordings`,`steps only affects click(), not mouse.move(), despite being documented on move()`,`It has no functional effect and exists purely for internal Playwright debugging`],0,`Real mouse movement naturally happens over many small increments, and some JS drag implementations rely on receiving several mousemove events to correctly compute what is happening, which a single instantaneous jump cannot replicate.`],[`O`,`A`,`Why might this manual sequence succeed at reordering a sortable list where a plain dragTo() call previously failed?`,`const source = await page.locator('#item-3').boundingBox();
const target = await page.locator('#item-6').boundingBox();
await page.mouse.move(source.x + 5, source.y + 5);
await page.mouse.down();
await page.mouse.move(target.x + 5, target.y + 5, { steps: 15 });
await page.mouse.up();`,[`Breaking the move into 15 intermediate steps produces a sequence of mousemove events the sortable library needs to detect drag distance and threshold crossing, which a single-jump drag (as dragTo() may effectively perform) can fail to trigger`,`boundingBox() is required before dragTo() can ever work, so this sequence is functionally identical to dragTo() with extra steps`,`mouse.down() and mouse.up() fire different underlying events than dragTo() uses internally, unrelated to the number of steps`,`This sequence only works because it avoids using locators entirely, which dragTo() is forced to use`],0,`When a drag-and-drop implementation is picky about receiving realistic incremental movement, manually composing mouse.move/down/up with a steps option gives you that control in a way the higher-level dragTo() convenience method may not expose as tunably.`],[`L`,`I`,`What does the delay option in locator.pressSequentially(text, { delay }) simulate, and what kind of UI does it help test?`,null,[`A real gap in milliseconds between each simulated keystroke, useful for debounced inputs (like a search-as-you-type box) where the app deliberately waits for a pause in typing before firing a request, since instantaneous keystrokes might not reliably trigger the debounce logic the same way`,`The delay before the very first keystroke is sent, with all subsequent keystrokes still instantaneous`,`It slows down the whole test suite's execution, not just this one typing action`,`pressSequentially() ignores the delay option; it only exists on fill()`],0,`Debounce logic is time-based, so a test exercising it benefits from keystrokes that are actually spread out in real time rather than fired all at once, which delay provides.`],[`O`,`I`,`Why is a delay specified here, given that pressSequentially() already fires individual keydown/keyup events?`,`await page.getByRole('combobox', { name: 'City' }).pressSequentially('Chicago', { delay: 150 });`,[`Firing individual key events alone does not guarantee real elapsed time between them; some debounce or throttle logic in the app measures actual wall-clock gaps between keystrokes, so an explicit delay makes the simulated typing behave more like genuine human typing speed`,`delay is required syntax and pressSequentially() throws without it`,`It has no effect since pressSequentially() always inserts a fixed 150ms delay regardless of this option`,`delay here controls how long the combobox stays open after typing finishes`],0,`Individual key events firing back-to-back with no time gap is different from a debounce mechanism's perspective than events spread out over real time, which is exactly the distinction the delay option lets you control.`],[`L`,`A`,`What does page.keyboard.insertText(text) do differently from pressSequentially(text)?`,null,[`insertText() directly inserts the given text into the focused element without dispatching individual keydown/keyup events for each character, which is useful for characters that have no simple 1:1 keyboard key mapping, like emoji or certain non-Latin scripts entered via IME composition`,`They are functionally identical, differing only in name`,`insertText() only works on <textarea> elements, never on <input>`,`insertText() requires the element to already contain existing text to insert into`],0,`Because pressSequentially() simulates real keyboard events per character, it struggles with characters that do not correspond to a straightforward physical key, which is exactly the gap insertText() fills by inserting the text value directly.`],[`O`,`I`,`Why is insertText() a better choice than pressSequentially() for this particular input?`,`await page.getByLabel('Status update').focus();
await page.keyboard.insertText('Launch day! 🎉🚀');`,[`Emoji characters generally have no direct keyboard key equivalent for pressSequentially() to simulate a keypress for, whereas insertText() bypasses key-event simulation entirely and inserts the text value directly`,`insertText() is required before focus() can take effect`,`pressSequentially() cannot be used inside any async function`,`insertText() automatically translates emoji into their text shortcode equivalents, like :tada:`],0,`Key-event-based typing assumes a physical keyboard mapping exists for each character, which breaks down for emoji and many non-Latin scripts; insertText() sidesteps that assumption entirely.`],[`D`,`A`,`A test tries page.keyboard.press('Control+V') to test a paste feature, expecting real clipboard content to be pasted, but nothing happens reliably in a headless CI run. What is the likely issue, and what is a better approach?`,null,[`Real OS clipboard state is not a reliable, controllable thing to depend on in CI, and there is no dedicated high-level paste() API; simulating the paste more directly, e.g. by dispatching a synthetic ClipboardEvent with constructed data via page.evaluate, is a more deterministic approach than relying on the actual system clipboard`,`Control+V is the wrong key combination; the correct one is Shift+Insert in every browser`,`This is a Playwright bug that will be fixed in a future release with no current workaround`,`Headless mode disables all keyboard shortcuts globally, including Control+V specifically`],0,`Depending on an out-of-band, host-machine resource like the real system clipboard introduces exactly the kind of environment-dependent flakiness that is worth engineering around with a more deterministic, in-page simulation.`],[`O`,`A`,`What is this code working around, given that Playwright has no dedicated high-level paste() action?`,`await page.evaluate(() => {
  const data = new DataTransfer();
  data.setData('text/plain', 'Pasted content');
  const event = new ClipboardEvent('paste', { clipboardData: data });
  document.activeElement.dispatchEvent(event);
});`,[`The lack of real, reliable OS clipboard access in an automated test environment; it manufactures a synthetic paste event with fake clipboard data and dispatches it directly to the focused element, testing the app's paste-handling logic deterministically`,`It is simulating a copy action, not a paste action, despite the event name`,`This code requires context.grantPermissions(['clipboard-write']) or it silently does nothing`,`It only works for plain <input> elements, never for rich text editors`],0,`When there is no dedicated, high-level API for a browser interaction, constructing and dispatching the underlying native event yourself inside evaluate() is a legitimate, commonly used escape hatch.`],[`L`,`I`,`When is context.grantPermissions(['clipboard-read', 'clipboard-write']) actually necessary for a test?`,null,[`When testing application code that itself calls the navigator.clipboard API (e.g. a Copy to clipboard button using navigator.clipboard.writeText), since browsers gate that API behind an explicit permission that a real user would otherwise be prompted for`,`It is required before ANY keyboard action, including plain typing, can be performed`,`It only affects screenshots, not clipboard behavior at all despite its name`,`It must be granted before every single test regardless of whether clipboard APIs are used`],0,`The permission requirement exists at the browser API level (navigator.clipboard), not for Playwright's own internal keyboard/mouse simulation, so it only matters when the APP itself is exercising that specific API.`],[`R`,`I`,`An app has a 'Copy to clipboard' button that calls navigator.clipboard.writeText() internally. How do you verify the correct text was actually copied?`,null,[`Grant clipboard-read/clipboard-write permissions on the context, click the button, then use page.evaluate(() => navigator.clipboard.readText()) to read back what was written and assert it matches the expected value`,`Click the button and trust that it worked, since clipboard content cannot be verified from a test`,`Use page.keyboard.press('Control+V') into a hidden textarea and assert on its value`,`Intercept the button's click event with page.route() to inspect the clipboard payload over the network`],0,`Because the write and read both go through the same real navigator.clipboard API (once permissions are granted), reading it back inside the page is a direct, reliable way to verify the copy actually happened correctly.`],[`L`,`A`,`Why can actions fail to reach an item in a virtualized (windowed) list even though scrollIntoViewIfNeeded normally happens automatically before an action?`,null,[`A virtualized list only renders items currently near the visible viewport into the DOM at all; an item far down the list simply does not exist in the DOM yet, and no amount of auto-scrolling an already-existing element can reveal an element that has not been mounted in the first place`,`Virtualized lists are fundamentally incompatible with Playwright and cannot be tested under any circumstances`,`scrollIntoViewIfNeeded always fails on any list longer than 100 items regardless of virtualization`,`This only happens in Firefox, since Chromium handles virtualization transparently`],0,`Auto-scroll-into-view only works on an element that already exists in the DOM; a virtualized list's whole point is to avoid rendering off-screen items, so the target element for a far-off item genuinely does not exist until the list is scrolled toward it first.`],[`D`,`A`,`Clicking the 500th item in a virtualized list times out with an element-not-found style error, even though the list definitely contains that many items. What is the fix?`,null,[`Programmatically scroll the virtualization container itself (e.g. repeated mouse.wheel calls, or evaluate() to set scrollTop directly) in increments, checking after each increment whether the target item has mounted into the DOM yet, then act on it once it appears`,`Increase the action's timeout to several minutes so Playwright eventually finds the item on its own`,`Use force: true on the click to bypass the requirement that the element exist in the DOM`,`Switch the whole test to use page.evaluate() to call the click handler by array index instead of ever locating a DOM element`],0,`Since the item genuinely is not in the DOM until the list scrolls near it, the fix has to actively drive the virtualization container's scroll position, not just wait longer or bypass checks on an element that does not exist yet.`],[`L`,`I`,`What does 'receiving events' specifically check, as one of Playwright's actionability checks before dispatching a click?`,null,[`That the target point of the element (its center, or the given position) is not covered by a different, unrelated element at the moment of the click, e.g. a sticky header or a modal overlay sitting on top of it`,`That the element has at least one registered event listener attached to it`,`That the browser tab itself currently has OS-level focus`,`That the element's CSS pointer-events property is explicitly set to auto`],0,`This check exists specifically to catch the common real-world case where an element is technically visible and enabled but something else visually sits on top of the exact point Playwright would click.`],[`L`,`I`,`A click on a button beneath a sticky page header intermittently times out with an actionability error about the element being covered. What are appropriate fixes, in order of preference?`,null,[`First try scrolling with extra margin so the sticky header does not overlap the target (e.g. scrolling further than the default auto-scroll would), or dismiss/collapse the sticky element if that reflects real user behavior; reach for force: true only as a last resort since it masks what may be a real, user-facing overlap bug`,`Immediately add force: true, since that is the fastest and generally safest fix for any covered-element error`,`Increase the click's timeout, since a longer wait will eventually let the sticky header move out of the way on its own`,`Switch to page.mouse.click() at fixed coordinates, since it bypasses the overlap check entirely`],0,`An element being covered by a sticky header is sometimes a genuine UX bug real users would also hit, so masking it with force:true without first considering that possibility risks hiding an actual defect.`],[`S`,`I`,`A cookie-consent overlay temporarily covers a button the test needs to click first. Force-clicking through the overlay makes the test pass, but is this the right approach?`,null,[`Generally no; the more correct approach is to actually dismiss the overlay first (as a real user would have to), reserving force:true for cases where covering is expected and harmless, since force-clicking through a blocking overlay risks masking whether the real interaction would even be possible for an actual user`,`Yes, force:true is always the correct and preferred solution whenever any element is covered by anything else`,`Yes, since overlays are always safe to click through given Playwright's actionability checks are overly strict by design`,`No, but the only alternative is to reload the page repeatedly until the overlay does not appear`],0,`The general principle is: reproduce what an actual user would have to do (dismiss the blocking element) rather than reaching for a bypass that produces a passing test without validating the real interaction path.`],[`L`,`A`,`When an element Playwright is acting on gets replaced by a re-render mid-action (e.g. React swaps in a new DOM node with the same visible content), how does Playwright's action retry mechanism handle it?`,null,[`Playwright retries the ENTIRE action pipeline from scratch, including re-resolving the locator against the current DOM and re-running all actionability checks, rather than continuing to operate on a now-stale, detached reference to the old element`,`It throws immediately the moment any DOM mutation occurs during an action, with no retry attempted`,`It continues acting on the old, now-detached element reference, which usually silently fails with no visible error`,`Playwright pauses the whole test and waits for manual intervention when this occurs`],0,`This whole-pipeline retry behavior is a big part of why locator-based actions are far more resilient to React/Vue-style re-renders than a one-shot ElementHandle-based approach would be.`],[`O`,`A`,`Why does this click reliably succeed even if the button element is replaced by a fresh DOM node partway through the click attempt?`,`await page.getByRole('button', { name: 'Add to cart' }).click();`,[`Because locator.click() re-resolves the locator against the live DOM and re-checks actionability on every retry attempt, so if the original element detaches mid-attempt, Playwright simply finds and acts on the new matching element instead of failing on a stale reference`,`Because getByRole caches a permanent reference to the very first matching element found, immune to any DOM changes thereafter`,`Because click() disables all DOM mutation observers on the page for its duration`,`This code is not actually resilient to such a replacement and would need { force: true } added to work reliably`],0,`The resilience comes from repeatedly re-querying via the locator's underlying selector, not from holding onto any single specific DOM node reference throughout the action.`],[`L`,`A`,`What specifically does the 'stable' actionability check measure before a click is dispatched?`,null,[`That the element's bounding box has not changed between two consecutive animation frames, meaning any CSS transition or animation currently affecting its position or size must finish before the click proceeds (or the check eventually times out)`,`That the element's CSS class list has not changed in the last second`,`That the element has existed in the DOM for at least 100 milliseconds`,`That the page's overall load event has already fired`],0,`This check exists specifically to avoid clicking a target that is still visually moving, such as an element mid-way through a slide-in or fade-in CSS animation.`],[`D`,`I`,`A button behind a 300ms fade-in and slide CSS transition causes an intermittent 'element is not stable' error. What is the better long-term fix, versus reaching for a manual wait?`,null,[`Reduce or disable CSS animations/transitions specifically in the test environment (e.g. via injected CSS honoring prefers-reduced-motion, or a test-only feature flag), rather than adding a fixed waitForTimeout matched to the animation's duration, which is fragile if the duration ever changes`,`Add force: true to every click on any element that might be mid-animation, as a blanket policy`,`Increase the global action timeout to 10 seconds so the animation always finishes well within it`,`Switch entirely to dispatchEvent('click') to skip the stability check, as the standard fix for any animation-related flakiness`],0,`Eliminating the root cause (unnecessary motion in a test environment where visual polish is irrelevant) is more robust than working around it with timing guesses tied to a specific animation duration that could change independently of the test.`],[`R`,`I`,`Multiple tests across the suite intermittently fail with 'element is not stable' errors caused by various CSS transitions throughout the app. What is a suite-wide mitigation, rather than fixing each test individually?`,null,[`Globally inject CSS (e.g. via addInitScript or addStyleTag applied to every test through a fixture) that disables or drastically shortens animations and transitions across the whole app for the test environment, addressing the root cause once instead of per-test workarounds`,`Add force: true as a blanket default option to every click() call across the entire suite`,`Set the global action timeout extremely high (like 60 seconds) so every animation reliably finishes before Playwright gives up`,`Rewrite the app to remove all CSS animations permanently, even in production`],0,`A recurring, systemic flakiness pattern is often best addressed with a systemic fix (disabling animations suite-wide) rather than patching each individual test that happens to be affected.`],[`L`,`A`,`What is the practical difference between locator.click({ force: true }) and page.mouse.click(x, y) at fixed coordinates?`,null,[`force: true still resolves the specific target element via the locator and dispatches a full, realistic pointer event sequence at its actual current location, just skipping the pre-click actionability checks; page.mouse.click(x, y) is a raw, position-only click with no element awareness at all, so it breaks immediately if the layout shifts even slightly`,`They are functionally identical in every respect, just written with different syntax`,`force: true is slower than a raw mouse.click() by an order of magnitude`,`page.mouse.click(x, y) automatically re-resolves its target if the layout changes, unlike force: true`],0,`force: true keeps the benefit of element-relative positioning (it still finds and targets the actual current element) while only skipping the safety checks; a raw coordinate click has no concept of a target element whatsoever.`],[`L`,`I`,`Besides a file path string, what other form of input does locator.setInputFiles() accept?`,null,[`An in-memory object with { name, mimeType, buffer } (or an array of such objects), letting you attach dynamically-generated file content without needing a fixture file to already exist on disk`,`Only a File object created via the browser's own File constructor, never a plain object`,`A base64-encoded data URL string exclusively, never a Buffer`,`setInputFiles() only accepts filesystem paths; there is no in-memory alternative`],0,`This in-memory form is especially useful for generating varied test-file content on the fly, like a CSV with specific edge-case rows, without maintaining a folder of static fixture files.`],[`O`,`I`,`What is the advantage of this upload approach over referencing a file on disk?`,`await page.getByLabel('Import CSV').setInputFiles({
  name: 'report.csv',
  mimeType: 'text/csv',
  buffer: Buffer.from('name,total\\nAlice,120\\nBob,95'),
});`,[`The file content is generated dynamically inline in the test code, so different edge cases (empty file, malformed rows, huge row count) can be produced on the fly without maintaining a separate library of static fixture files on disk`,`This code is invalid; setInputFiles() strictly requires a path string as its only supported argument`,`It uploads the file directly to the server via an API call, bypassing the actual file input entirely`,`mimeType here is ignored; the browser always infers it from the file's actual contents`],0,`In-memory file objects are particularly convenient for testing many small variations of file content without cluttering the repository with numerous near-identical fixture files.`],[`R`,`I`,`You need to test several edge cases for a CSV upload feature: an empty file, a file with one malformed row, and a file with a wrong file extension. What is an efficient way to do this without a folder of fixture files?`,null,[`Use setInputFiles() with an in-memory { name, mimeType, buffer } object generated per test case, constructing each edge case's content directly in code rather than maintaining a separate static fixture file for every variant`,`Create and commit a dedicated fixture file to the repository for each edge case, even if there end up being dozens of near-identical variants`,`This can only be tested by manually uploading files through a real browser session and recording the result`,`Use page.route() to intercept and fake the upload response instead of actually testing the file input`],0,`In-memory file generation scales much better than a growing folder of static fixtures, especially for edge cases that are really just small variations on a common pattern.`],[`L`,`I`,`What does calling locator.setInputFiles([]) with an empty array do to a file input?`,null,[`It clears the file input's current selection, useful for testing a 'remove attached file' flow where the user detaches a previously chosen file`,`It throws an error, since an empty array is not a valid argument for setInputFiles()`,`It uploads a zero-byte placeholder file named 'empty'`,`It resets the entire form, not just the file input`],0,`An empty array is the documented way to represent 'no files selected,' which maps naturally onto testing a clear/remove-attachment interaction.`],[`L`,`I`,`What actually happens to the file input's change event when setInputFiles() is called a second time with a different file, replacing a previous upload?`,null,[`The change (and input) events fire again for the new selection, exactly as they did for the first call, so an app correctly listening for those events will process the replacement file normally; there is no special limitation preventing a second setInputFiles() call from working`,`The second call is silently ignored; a file input can only be set once per test`,`The events fire, but only in Chromium; other engines require reload() between calls`,`setInputFiles() requires clearing with an empty array before it can be called a second time with new content`],0,`There is no inherent API limitation here; if a test observes a second upload not being processed correctly, the more likely explanation is an app-level bug or UI-state issue, not a Playwright constraint.`],[`L`,`I`,`Why is testing a full workflow using ONLY keyboard actions (Tab, Enter, Space, arrow keys), with no click() calls at all, a valuable accessibility regression test pattern?`,null,[`It verifies the workflow is genuinely operable by keyboard-only users (a real accessibility requirement), which a suite that only ever uses click() would never catch regressing, such as a control that is visually clickable but not reachable via Tab or not activatable via Enter/Space`,`It runs measurably faster than the equivalent click-based test in every case`,`Keyboard-only tests are required by Playwright's own internal architecture and are not optional`,`It is only useful for testing physical hardware keyboards, not virtual/on-screen ones`],0,`A control that works perfectly with a mouse click can still be a real accessibility failure if it is unreachable or unusable via keyboard alone, and only a keyboard-driven test path can catch that class of bug.`],[`O`,`I`,`What accessibility property is this sequence specifically verifying?`,`await page.keyboard.press('Tab');
await page.keyboard.press('Tab');
await page.keyboard.press('Enter');
await expect(page.getByText('Order placed')).toBeVisible();`,[`That the form can be completed and submitted using only Tab to move focus and Enter to activate the final control, with no mouse click involved at any point, verifying real keyboard-only operability`,`That the form has exactly two focusable fields, since only two Tab presses are used`,`That pressing Enter always submits any form regardless of focus`,`That the order confirmation message appears within two tab-stops of the page load`],0,`The absence of any click() call in this sequence is the deliberate point: it proves the entire flow is reachable and completable via keyboard alone.`],[`S`,`I`,`A custom dropdown/listbox component should be fully operable via keyboard (ArrowDown to move the highlighted option, Enter to select it) as part of an accessibility test suite. How do you write this test?`,null,[`Focus the combobox trigger, press ArrowDown one or more times to move the highlight, press Enter to confirm the selection, then assert the combobox's displayed value updated to the expected option, with no click() used on any option`,`Click each option in sequence with the mouse to determine which one Enter would have selected, then assert on that`,`Use page.keyboard.press('Tab') repeatedly until the correct option is focused, since Tab is the correct key for moving between options within an open listbox`,`This interaction pattern cannot be reliably tested without a real physical keyboard device attached`],0,`A keyboard-operable listbox specifically uses arrow keys to move the highlighted option (not Tab, which usually moves between distinct widgets), so the test should mirror that exact expected interaction pattern.`],[`L`,`A`,`Why should locator.dispatchEvent() generally be reserved for custom, non-standard events rather than standard interactions like clicks?`,null,[`dispatchEvent() fires exactly the one named event you specify with no surrounding realistic event sequence and completely bypasses Playwright's actionability checks, so using it for a standard interaction like click loses both the safety net and the full, realistic event chain (pointerdown, mousedown, mouseup, click, etc.) some frameworks depend on`,`dispatchEvent() is deprecated and scheduled for removal in a future Playwright version`,`dispatchEvent() only works for events on the document object, never on individual elements`,`dispatchEvent() automatically converts any event name into its closest standard Playwright action equivalent`],0,`Standard action methods exist precisely because they reproduce the full, realistic sequence of low-level events a real browser interaction generates; dispatchEvent() intentionally skips all of that in favor of firing just the one event you name.`],[`O`,`I`,`What is this call testing, and why can't a standard action method achieve the same thing directly?`,`await page.getByTestId('kanban-card').dispatchEvent('myapp:card-moved', { detail: { cardId: 5, column: 'done' } });`,[`A bespoke, application-specific custom DOM event (myapp:card-moved) that some component is presumably listening for, with a custom detail payload; no built-in Playwright action method corresponds to an arbitrary custom event name like this, so dispatchEvent() is the only way to fire it directly`,`A standard drag-and-drop event that dragTo() could have produced identically`,`It is equivalent to click() on the kanban card, just using a differently-named event for stylistic reasons`,`This is invalid; dispatchEvent() cannot accept a detail payload as its second argument`],0,`Custom application-defined events with their own semantics and payload shape have no standard Playwright action counterpart, which is exactly the gap dispatchEvent() is meant to fill.`],[`L`,`I`,`A test calls locator.dispatchEvent('click') and it resolves without error, but the application does not respond as if a real click happened. What is the explanation?`,null,[`dispatchEvent() fires only the exact event named ('click' in this case) with none of the surrounding realistic pointer event sequence (pointerdown, mousedown, mouseup) that some frameworks' event delegation or gesture-detection logic depends on; locator.click() is the correct choice for a standard click interaction`,`dispatchEvent('click') is identical to click() in every respect, so the app itself must have a genuine bug`,`dispatchEvent() requires an explicit await twice to actually take effect`,`The element must have tabindex set before dispatchEvent('click') can work`],0,`Modern UI frameworks sometimes attach listeners to a broader set of pointer-related events, or rely on event bubbling through a specific sequence, which a single synthetic 'click' event dispatched in isolation does not reproduce.`],[`L`,`A`,`Why might dragTo()'s default, relatively simple mouse simulation sometimes fail to trigger a native HTML5 drag-and-drop implementation (draggable="true" with dragstart/dragover/drop listeners)?`,null,[`Native HTML5 drag-and-drop relies on a DataTransfer object being populated and passed through the dragstart/dragover/drop event sequence; if dragTo()'s simulated events do not construct and carry an appropriate DataTransfer the way a genuine browser-initiated drag does, the app's drop handler may never receive the data it expects`,`dragTo() only works for library-based (non-native) drag-and-drop implementations, and never for native HTML5 drag-and-drop, categorically`,`Native drag-and-drop cannot be automated by any tool, including Playwright, under any circumstances`,`dragTo() requires the draggable attribute to be explicitly removed before it can function`],0,`Native browser drag-and-drop is a distinct browser subsystem built around DataTransfer, which is a more specific mechanism than the generic mousedown/mousemove/mouseup sequence dragTo() composes for simpler, JS-driven drag implementations.`],[`O`,`A`,`What problem is this code specifically working around, given that dragTo() proved unreliable for this component?`,`await page.evaluate(({ sourceSel, targetSel }) => {
  const source = document.querySelector(sourceSel);
  const target = document.querySelector(targetSel);
  const dataTransfer = new DataTransfer();
  source.dispatchEvent(new DragEvent('dragstart', { dataTransfer, bubbles: true }));
  target.dispatchEvent(new DragEvent('dragover', { dataTransfer, bubbles: true }));
  target.dispatchEvent(new DragEvent('drop', { dataTransfer, bubbles: true }));
}, { sourceSel: '#card-3', targetSel: '#done-column' });`,[`That native HTML5 drag-and-drop needs a real DataTransfer object carried through the dragstart/dragover/drop event chain, which this code manually constructs and dispatches, rather than relying on dragTo()'s simpler simulated mouse gesture, which may not satisfy the app's native drag handlers`,`It is simulating a completely unrelated feature, testing keyboard-based reordering instead of drag-and-drop`,`This code is functionally identical to calling page.locator('#card-3').dragTo(page.locator('#done-column'))`,`It only works in Chromium, since DragEvent is not implemented in other engines`],0,`When a native HTML5 drag implementation genuinely depends on the DataTransfer object being present and populated at each stage, manually constructing and dispatching that full event sequence in evaluate() becomes the more reliable escape hatch.`],[`R`,`A`,`A kanban board uses native HTML5 drag-and-drop, and dragTo()-based tests for card reordering are unreliable. What is a sensible way to migrate this test?`,null,[`Diagnose whether the app's drop handlers depend on DataTransfer contents, and if so, replace the dragTo() call with a manual evaluate()-based simulation that constructs and dispatches dragstart/dragover/drop events carrying a real DataTransfer object`,`Immediately conclude the feature cannot be tested and remove drag-and-drop coverage from the suite entirely`,`Increase the test's timeout substantially, since drag-and-drop reliability issues are usually just a matter of insufficient wait time`,`Switch the whole test to a full page reload between each individual drag step, hoping that resets any stuck internal drag state`],0,`Diagnosing the actual mechanism the app depends on (DataTransfer-driven native DnD versus a simpler mouse-event-driven library) is the necessary first step before choosing dragTo() versus a manual evaluate()-based simulation.`],[`L`,`I`,`How does Playwright's actionability check treat an element styled with CSS pointer-events: none, as distinct from an element with the disabled attribute?`,null,[`pointer-events: none causes the 'receiving events' check to fail, since the element genuinely cannot receive pointer input at that point on screen, even though the element itself is otherwise considered visible and enabled; this differs from a true disabled attribute, which fails a distinct 'enabled' check`,`Both produce the exact same error message and failure mode, since Playwright treats them identically internally`,`pointer-events: none has no effect on actionability checks at all and click() proceeds normally`,`Playwright automatically overrides pointer-events: none via injected CSS before every click attempt`],0,`These are genuinely two different actionability failure modes under the hood, which matters for correctly diagnosing why a click is failing: is the element truly disabled, or just visually made non-interactive via CSS?`],[`D`,`A`,`A visually-grayed-out 'disabled-looking' button (styled with pointer-events: none but no actual disabled attribute) causes click() to time out rather than fail immediately with a clear error. Why the different failure mode, and how do you correctly assert this state?`,null,[`Playwright still considers the element enabled (since there is no disabled attribute), so it keeps retrying the 'receiving events' check hoping the CSS changes, eventually timing out; to correctly test this, assert the specific condition that actually matters, e.g. that the button's pointer-events computed style is none or that clicking it produces no visible effect, rather than expecting toBeDisabled() to be true`,`toBeDisabled() will correctly report true for this element since it looks disabled, so no special handling is needed`,`This is a bug in Playwright, since any visually gray button should be treated identically to a disabled one`,`The fix is to always add { force: true }, which converts the timeout into an immediate, cleaner failure`],0,`toBeDisabled() checks the actual disabled attribute/ARIA state, not visual appearance or pointer-events, so a CSS-only 'fake disabled' look needs its own, more specific assertion to be tested meaningfully.`],[`L`,`I`,`What is the benefit of asserting expect(saveButton).toBeDisabled() over checking a CSS class like 'btn-disabled' to verify a button is genuinely non-interactive?`,null,[`toBeDisabled() checks the real disabled attribute/ARIA state that actually prevents interaction and is respected by the browser and assistive technology, whereas a CSS class name is purely a styling hook that says nothing about whether the element is truly non-interactive`,`They are functionally equivalent checks and either is equally valid in every case`,`toBeDisabled() only works on <button> elements, never on custom disabled-looking components`,`Checking a CSS class is always more reliable since it does not depend on ARIA semantics`],0,`A button can be styled to LOOK disabled via a CSS class while remaining fully clickable and functional underneath, which is exactly the gap between a purely cosmetic check and a semantic, behavior-backed one.`],[`L`,`I`,`Why might using locator.focus() be preferable to click() when the goal is purely to move keyboard focus onto an element, such as before testing arrow-key navigation in a custom widget?`,null,[`focus() sets focus directly without any of the side effects a real click might also trigger, such as toggling a dropdown open/closed or triggering a click-outside handler elsewhere on the page, which could interfere with the specific keyboard-navigation behavior being tested`,`focus() and click() are functionally identical in every respect for any focusable element`,`focus() automatically also performs a click as an implementation detail, so there is no real difference`,`click() cannot be used at all on elements that only respond to keyboard events`],0,`Isolating exactly the behavior you intend to test (focus-driven keyboard navigation) from unrelated side effects (like a click also toggling something open) is a good reason to reach for the more targeted focus() call.`],[`L`,`I`,`A test calls click() on a custom combobox purely to give it keyboard focus before testing arrow-key navigation, but the click also unexpectedly toggles the dropdown's open/closed state depending on its current state, breaking the test. What is the fix?`,null,[`Replace click() with focus(), which sets keyboard focus on the element without triggering the click handler responsible for toggling the dropdown open/closed, isolating just the focus behavior needed for the subsequent arrow-key test`,`Click the combobox twice in a row to cancel out the toggle effect`,`Add a fixed page.waitForTimeout() between the click and the arrow-key presses to let the toggle settle`,`This cannot be avoided since combobox components always toggle on any interaction, including focus`],0,`When a test's actual goal is just establishing focus (not clicking as a user interaction), focus() is the more precise tool since it sidesteps whatever click-specific behavior the component happens to have wired up.`],[`L`,`I`,`Why might locator.blur() be necessary to properly test a field that only validates its content on blur, rather than while typing?`,null,[`Some validation logic specifically listens for the blur event (fired when focus leaves the field), so a test that fills a field but never causes it to lose focus may never trigger that validation at all; explicitly calling blur() ensures the same condition a real user tabbing or clicking away would produce`,`fill() automatically triggers blur validation as part of its own internal implementation, making an explicit blur() call always redundant`,`blur() is required before fill() can be called on any input, regardless of the app's validation strategy`,`blur-based validation cannot be tested in Playwright under any circumstances`],0,`If the app's validation is specifically wired to the blur event, a test needs to actually produce that event to exercise the validation path, rather than assuming filling the value alone is equivalent.`],[`O`,`I`,`Why is blur() necessary here for the validation message to appear?`,`await page.getByLabel('Email').fill('not-an-email');
await page.getByLabel('Email').blur();
await expect(page.getByText('Invalid email format')).toBeVisible();`,[`The app's validation logic is presumably wired to the blur event rather than the input/change events fill() already fires, so without explicitly leaving the field (blur), the validation check never runs and the error message never appears`,`blur() is unnecessary here since fill() already fires a blur event automatically as part of its own behavior`,`blur() submits the form, which is what actually triggers the validation check shown`,`fill() cannot be followed by any other locator action on the same element, requiring blur() as a required separator`],0,`fill() dispatches input and change events, but not necessarily blur, so a validation strategy specifically tied to losing focus needs an explicit blur() call to be exercised in a test.`],[`S`,`I`,`A form validates several fields individually only on blur (not on submit). How do you efficiently test each field's validation message without submitting the whole form repeatedly?`,null,[`For each field, fill() an invalid value, call blur() on it, and assert its specific validation message appears, repeating this fill-then-blur pair per field independently rather than filling everything and submitting once per field under test`,`Submit the entire form after each individual field change to trigger all validations at once, then filter for the one message relevant to the current field`,`This cannot be tested field-by-field; blur-based validation can only be verified via a full form submission`,`Use page.keyboard.press('Tab') after filling every field simultaneously, then assert on all validation messages appearing together`],0,`Because the validation is genuinely tied to individual field blur events rather than a form-wide submit, testing each field's fill-then-blur pair independently is both more precise and avoids the overhead of repeated full submissions.`],[`L`,`I`,`Is page-level page.type(selector, text) (as opposed to locator.pressSequentially()) still functional in current Playwright, and how is it viewed?`,null,[`The page-level, selector-string-based legacy action methods (page.type, page.click, page.fill, etc.) still exist for backward compatibility, but locator-based equivalents are the recommended, modern approach and are what new code should use`,`Page-level type() was removed entirely and any call to it throws an error in current Playwright`,`Page-level type() is actually the more modern API, while locator.pressSequentially() is the older, deprecated one`,`They are two names for the exact same underlying implementation, so it does not matter which one new code uses`],0,`Playwright's API evolved from page-level, selector-string methods toward the locator model; the older methods remain for compatibility but locator-based code is what current guidance and code review generally favor.`],[`L`,`A`,`What is the resolution order Playwright uses to determine the effective timeout for a single action call, from most to least specific?`,null,[`A per-call {timeout} option on that specific action, then the project/config-level use.actionTimeout, then the built-in default timeout, with a more specific setting always taking precedence over a broader one`,`The built-in default always wins over any configured or per-call value`,`use.actionTimeout always overrides a per-call {timeout}, regardless of which is more specific`,`Action timeouts are entirely separate from any config and can only ever be set per-call`],0,`This mirrors the general Playwright pattern seen with navigation timeouts too: the most specific, closest-to-the-call setting takes precedence over broader defaults.`],[`O`,`I`,`What is the purpose of overriding the timeout only on this one call?`,`await page.getByRole('button', { name: 'Sync Now' }).click({ timeout: 5000 });`,[`It scopes a shorter (or longer, depending on the value) timeout to JUST this specific action, without changing the suite's global actionTimeout default that applies to every other action`,`It sets the total test timeout to exactly 5 seconds, overriding test.setTimeout()`,`It delays the click by 5 seconds before executing it`,`It has no real effect since actionTimeout can only be set globally, never per-call`],0,`Per-call timeout overrides let you tune the wait behavior for a single known-flaky-timing action without loosening (or tightening) the default budget for every other action in the suite.`],[`S`,`I`,`One specific action in an otherwise fast, reliable test suite is known to occasionally need extra time, because it waits on a slow third-party widget to finish becoming interactive. How should this be handled without slowing down the whole suite's failure-detection speed?`,null,[`Add a per-call { timeout } override to just that one action, keeping the suite's global actionTimeout tight for everything else, so only the known-slow interaction gets extra patience while unrelated actions still fail fast if something is genuinely wrong`,`Raise the global actionTimeout for the entire suite so every action gets the same longer allowance`,`Wrap the flaky action in a retry loop with catch blocks that silently swallow timeout errors`,`Move the flaky action into a separate, entirely different test file with a longer overall test timeout`],0,`A targeted, per-call timeout override is more precise than a global change, since it avoids masking genuinely slow or broken actions elsewhere in the suite behind an overly generous default.`],[`L`,`I`,`Are an action's overall timeout (governing actionability retries) and pressSequentially's per-character delay option related settings?`,null,[`No, they serve entirely different purposes: the action timeout bounds how long Playwright will keep retrying actionability checks before giving up, while delay controls the deliberate pacing BETWEEN individual keystrokes during typing, independent of any timeout budget`,`Yes, delay is simply a percentage of the overall action timeout, calculated automatically`,`Yes, increasing delay automatically increases the effective timeout by the same amount`,`No, but delay always overrides timeout when both are present on the same call`],0,`It is easy to conflate 'how long we wait for something to become ready' with 'how much time elapses between simulated keystrokes,' but they are conceptually and mechanically unrelated settings.`],[`L`,`I`,`Do locator.check() and locator.uncheck() accept a position option, similar to click()?`,null,[`Yes, both accept a position option, which is relevant for custom checkbox components where the actual clickable hit area might be a small visual icon within a larger clickable wrapper, letting you target that specific spot precisely`,`No, position is exclusive to click() and dblclick(); check()/uncheck() always target the element's exact center with no way to override it`,`Yes, but only on radio button elements, not on checkboxes`,`No, check()/uncheck() do not perform any click-like gesture internally at all, so position is meaningless for them`],0,`Since check()/uncheck() are implemented as a click on the underlying checkable element, they inherit the same position-targeting capability click() has for precisely hitting a specific point within a larger element.`],[`O`,`I`,`Why might position be specified on this check() call for a custom checkbox component?`,`await page.getByRole('checkbox', { name: 'Subscribe' }).check({ position: { x: 5, y: 5 } });`,[`The actual checkbox input might be visually tiny and positioned in a specific corner of a much larger clickable label wrapper, so targeting a precise point near its known location increases the chance of hitting the genuinely interactive area rather than the element's overall (possibly much larger) center`,`position is required syntax for check() and has no functional effect on where the interaction occurs`,`It moves the checkbox to those coordinates on the page before checking it`,`check() ignores the position option entirely; only click() respects it`],0,`Custom-styled checkboxes sometimes have a real interactive hit-area that is smaller or offset from what visually appears to be the whole clickable region, which position lets you account for precisely.`],[`L`,`I`,`Why are Playwright's high-level action methods (click, fill, check) generally preferred over composing raw page.mouse/page.keyboard calls, whenever a suitable high-level method exists?`,null,[`The high-level actions bundle locator resolution, actionability waiting, auto-scrolling, and retries together, whereas page.mouse/page.keyboard are low-level primitives that do none of that on their own, leaving all of the safety and correctness handling to the test author`,`page.mouse and page.keyboard calls are deprecated and scheduled for removal`,`High-level actions execute meaningfully faster in every case due to internal optimizations unavailable to raw primitives`,`There is no real difference; the choice is purely a matter of code style`],0,`The high-level actions exist specifically to encapsulate the safety net (waiting, retrying, resolving) that raw mouse/keyboard primitives intentionally leave entirely up to you.`],[`R`,`A`,`When are low-level page.mouse/page.keyboard calls genuinely necessary, despite high-level actions generally being preferred?`,null,[`For interactions with no addressable DOM sub-elements to target with a locator, such as drawing inside a canvas, custom drag physics needing precise intermediate movement, or exact pixel-coordinate interactions that a semantic locator simply cannot express`,`Never; high-level actions can always be made to work for absolutely any interaction with enough configuration`,`Only when running tests in headless mode, since headed mode always supports high-level actions fully`,`Only for testing native OS dialogs, which is the sole legitimate use case for low-level primitives`],0,`The genuine need for low-level primitives arises specifically when there is no DOM element to hand a locator, such as coordinate-based drawing surfaces or physics-sensitive drag gestures.`],[`L`,`A`,`Does a dragTo() call resolving without throwing an error guarantee that the application's drop handler actually succeeded in processing the drop?`,null,[`No, dragTo() only guarantees that the simulated mouse gesture (down, move, up) occurred without error; it says nothing about whether the application's own drop-handling logic actually accepted and processed the drop correctly, which is why an explicit assertion on the resulting application state afterward is essential`,`Yes, a successful dragTo() resolution is a complete, reliable guarantee that the drop was fully processed by the application`,`dragTo() automatically also asserts that the item's position changed as expected, with no further assertion needed`,`dragTo() only resolves successfully if the app's drop handler returns a specific success value`],0,`dragTo() is purely a gesture simulator; treating its successful resolution as proof of application-level success conflates 'the mouse events happened' with 'the app reacted correctly to them,' which are two separate things worth asserting separately.`],[`L`,`A`,`A dragTo() call in a reorder test resolves with no error, but a later check shows the backend never actually persisted the new item order. What is the most likely explanation, and what should the test do differently?`,null,[`dragTo() only confirms the mouse gesture completed; the app's own drop handler may have silently failed to fire, or failed to call its persistence logic, so the test needs an explicit assertion on the resulting order (in the UI and/or via a follow-up API check) rather than trusting dragTo()'s resolution alone as proof of success`,`This is a Playwright bug where dragTo() reports success even when the underlying mouse events never actually fired`,`The fix is to always add a page.reload() immediately after every dragTo() call to force persistence`,`dragTo() cannot be used for any test involving a backend, since it only operates at the DOM level`],0,`Separating 'did the gesture happen' from 'did the application correctly react to it' is the key diagnostic insight; the test needs to explicitly verify the latter rather than inferring it from the former.`],[`L`,`I`,`What does the position option on locator.hover({ position }) let you target that hovering the element as a whole might not?`,null,[`A specific sub-region within the element's bounding box, useful when only part of a larger element actually triggers a hover-dependent behavior, such as a tooltip that only appears when hovering directly over a truncation ellipsis inside a wider table cell`,`It has no effect on hover(), since hovering always targets the whole element uniformly`,`It changes which CSS :hover selector rule applies, rather than which point is hovered`,`It requires the element to already be scrolled fully into view beforehand, unlike a plain hover()`],0,`Some hover-triggered behaviors are deliberately scoped to a small part of a larger element (like an ellipsis or an icon), and position lets a test aim precisely at that spot instead of the element's default center.`],[`O`,`I`,`Why might position be used here for a data-grid cell showing truncated text with a tooltip?`,`await page.getByRole('cell', { name: /Quarterly Rev/ }).hover({ position: { x: 195, y: 8 } });
await expect(page.getByRole('tooltip')).toBeVisible();`,[`The tooltip may only be wired to appear when hovering specifically near the truncation indicator (like an ellipsis icon positioned toward the cell's right edge), not the cell as a whole, so hovering the default center might not trigger it at all`,`position here has no bearing on whether the tooltip appears; it only affects how long the hover lasts`,`It is required because getByRole('cell') cannot be hovered without an explicit position`,`The coordinates specify which row of a multi-row cell to hover, unrelated to tooltip triggering`],0,`When a hover-triggered affordance is deliberately scoped to a small visual indicator rather than the whole containing element, a plain hover() at the element's default center may simply miss the actual trigger zone.`],[`S`,`I`,`A data grid shows a tooltip with a cell's full value only when the mouse hovers directly over its truncation ellipsis, not anywhere else in the cell. How do you test this precise behavior?`,null,[`Use locator.hover({ position }) targeting a point near where the ellipsis is expected to render (e.g. toward the cell's trailing edge), then assert the tooltip becomes visible, ideally also testing that hovering elsewhere in the same cell does NOT show the tooltip`,`Use a plain hover() on the cell as a whole and assume the tooltip logic will trigger correctly regardless of cursor position`,`This precise a hover target cannot be reliably tested and should be verified only through manual QA`,`Use click() instead of hover(), since tooltips are functionally equivalent to click-triggered popovers`],0,`Testing both the positive case (hovering the actual trigger zone) and, ideally, the negative case (hovering elsewhere does not trigger it) gives real confidence in a deliberately narrow hover target.`],[`L`,`I`,`What does the receiving-events actionability check protect against when an element is technically visible and enabled but sits underneath a transparent overlay div?`,null,[`It fails the click attempt (retrying until it either succeeds or times out) because the actual point Playwright would click is intercepted by the overlay rather than reaching the intended element, mirroring what would happen if a real user tried to click through an invisible-but-present blocking layer`,`It succeeds immediately, since Playwright only checks the target element's own CSS properties, not what else occupies the same screen coordinates`,`It silently redirects the click to whatever element the overlay belongs to instead`,`This scenario cannot occur, since visible and enabled together always imply the element is also clickable`],0,`This check specifically simulates what a real user's click would actually hit at that screen location, catching the common bug pattern of an invisible or transparent element unintentionally intercepting clicks meant for something beneath it.`],[`L`,`I`,`What is a key limitation of locator.dispatchEvent() regarding actionability checks, compared to click() or fill()?`,null,[`dispatchEvent() performs none of the standard actionability checks (visibility, stability, enabled, receiving events) before firing the event, so it can fire an event on an element that a real user could never have actually interacted with, silently masking bugs that click()/fill() would have caught`,`dispatchEvent() performs the exact same actionability checks as click(), just with a different underlying event name`,`dispatchEvent() only skips the visibility check, while still enforcing all the others`,`dispatchEvent() is stricter than click(), requiring the element to also be scrolled fully into view first`],0,`Because dispatchEvent() is an intentional escape hatch that bypasses the safety net entirely, using it in place of a standard action can accidentally hide a real interactivity bug that a proper actionability-checked action would have surfaced.`],[`O`,`I`,`What safety net does this code lose by using evaluate() to trigger the click, compared to locator.click()?`,`await page.locator('#hidden-but-clickable').evaluate(el => el.click());`,[`All of Playwright's actionability checks (visible, stable, enabled, receiving events) are bypassed since the click is triggered directly via native JavaScript inside the page, so this succeeds even on an element a real user could never actually see or click`,`evaluate() automatically applies the same actionability checks as locator.click() before running the callback`,`This code is functionally identical to locator.click() in every respect, including auto-scrolling`,`evaluate()-triggered clicks fire fewer underlying browser events than a real click, but are otherwise identical in behavior`],0,`Triggering a native click via JS inside evaluate() is a genuine escape hatch, appropriate only for deliberate, rare cases, precisely because it discards every one of the actionability guarantees a normal locator.click() provides.`],[`L`,`A`,`What is the correct order of operations Playwright performs internally for a standard locator.click() call?`,null,[`Resolve the locator to a single matching element, wait for it to pass all actionability checks (attached, visible, stable, enabled, receiving events), scroll it into view if needed, determine the click point (center or given position), then dispatch the full pointer/mouse event sequence`,`Dispatch the click event immediately, then check afterward whether the element was actually visible and enabled`,`Scroll the entire page to the top first, then search the whole document for any matching element`,`Wait for the whole page's networkidle state before resolving the locator at all`],0,`Understanding this exact ordering clarifies why click() can safely be called immediately after a locator is created: the waiting and checking all happen internally, in the right order, before anything is actually dispatched.`],[`S`,`I`,`A click() call silently 'does nothing' in the application, with no thrown error, and you need to determine whether the issue is Playwright failing to interact correctly versus a genuine app-level bug. How do you isolate the cause?`,null,[`Temporarily add { trial: true } to the click, which runs all actionability checks without actually dispatching the click; if trial succeeds, the element is genuinely actionable and the problem is in the app's own response to the click, not in locating/targeting it`,`Add { force: true } permanently, since a successful forced click always means the original problem is fully resolved`,`Increase the timeout to several minutes and assume the issue will resolve itself given enough time`,`Rewrite the test using dispatchEvent('click') instead, since it is inherently more reliable than click()`],0,`trial:true is specifically designed for this diagnostic purpose: confirming whether an element is actionable at all, independent of whether the application then does what you expect once actually clicked.`],[`L`,`I`,`Can the modifiers and button options both be combined in a single locator.click() call?`,null,[`Yes, they are independent options that can be combined freely, e.g. simulating a Ctrl+Right-click for an application-specific custom shortcut or context action`,`No, button and modifiers are mutually exclusive; only one can be specified per click() call`,`Yes, but only when button is set to 'left', the default`,`modifiers only applies to keyboard-triggered actions, never to click(), despite appearing in its options`],0,`These options are orthogonal settings on the same click configuration, so any legitimate combination of button and modifiers can be expressed together in one call.`],[`O`,`I`,`What real-world interaction does this click simulate?`,`await page.locator('.timeline-event').click({ button: 'right', modifiers: ['Control'] });`,[`A Ctrl+Right-click, useful for testing an application-specific custom shortcut that is distinct from a plain right-click's normal context menu behavior`,`A plain right-click, since modifiers is silently ignored when button is set to 'right'`,`A left-click while holding the Control key, since button is overridden by the presence of modifiers`,`Two separate, sequential clicks: one right-click, followed by one Control-only click`],0,`button and modifiers compose together in a single dispatched click, letting a test express exactly the kind of modified-click shortcut some applications define beyond the browser's own default context-menu behavior.`],[`R`,`A`,`A spreadsheet-like grid supports Ctrl+Click to toggle individual cell selection and Shift+Click to select a contiguous range. How do you test both behaviors across several cells?`,null,[`For Ctrl+Click: click each target cell individually with { modifiers: ['Control'] } and assert each toggles independently; for Shift+Click: click the first cell normally, then click the last cell with { modifiers: ['Shift'] }, and assert the whole range between them becomes selected`,`Use dragTo() from the first cell to the last cell for both behaviors, since dragTo() already implies both modifier semantics`,`Test only one of the two behaviors, since they cannot both be reliably automated in the same test suite`,`Use keyboard.down('Control') and keyboard.down('Shift') simultaneously for every single click in the test, regardless of which behavior is being tested`],0,`Because the two interactions have genuinely different semantics (independent toggle vs. contiguous range), each deserves its own click sequence using the modifier option that matches its real-world keyboard interaction.`],[`L`,`A`,`Why can fill() silently fail to set an out-of-range or malformed value on an <input type="number"> with constraints like step or min/max, unlike a plain <input type="text">?`,null,[`Number inputs enforce their own parsing/validation rules at the browser level (a value that does not conform to the step/min/max constraints may be rejected or coerced), so fill()'s underlying value-setting can behave differently than it would for an unconstrained text input, making it worth verifying the actual resulting value with inputValue() rather than assuming success`,`fill() always throws an explicit error for any invalid numeric value, so silent failure cannot occur`,`Number inputs behave identically to text inputs in every respect, so this is not a real concern`,`fill() automatically rounds any out-of-range numeric value to the nearest valid one and never fails silently or explicitly`],0,`Constrained input types have their own browser-level validation semantics that plain text inputs do not, which is a good reason to verify the actual resulting value after fill() rather than assuming it always behaves like a text field.`],[`D`,`A`,`A test calls fill('12.5') on <input type="number" step="1"> and later assertions behave as if the value never took effect. What should you check, and what value should the test use instead?`,null,[`Check the actual value with inputValue() to confirm what really landed in the field, since a value that violates the step constraint may not be accepted the way it would for an unconstrained text input; if step="1" is a real constraint, use a whole-number value like '12' or '13' that conforms to it in the test data`,`This is always a Playwright bug; number inputs should be reported to the Playwright team`,`Add { force: true } to fill(), which resolves any numeric input validation constraint automatically`,`Replace <input type="number"> in the app's source with type="text" so fill() always succeeds`],0,`Rather than assuming fill() behaves identically across all input types, verifying the actual resulting value and adjusting the test's input to respect real constraints (like step) is the more robust fix.`],[`L`,`I`,`Why does page.keyboard.press('Down') silently do nothing, rather than throwing a clear error, when the correct key name is 'ArrowDown'?`,null,[`press() expects a specific key identifier from the KeyboardEvent.key naming convention; an unrecognized or mismatched name is generally treated as simply not matching any real key rather than causing a validation error, so the call can resolve as if it succeeded, without actually producing the intended keyboard effect on the page`,`press('Down') is a valid alias for ArrowDown in every current Playwright version`,`press() always throws synchronously for any key name not in a fixed known allowlist`,`This behavior only occurs in Firefox; Chromium and WebKit throw a clear error for invalid key names`],0,`This is a sneaky class of bug precisely because the call does not error out, so a test can appear to run cleanly while the intended key press simply never had any real effect on the page.`],[`D`,`I`,`A test presses 'Down' hoping to move a highlighted selection in a custom list, but the highlight never actually moves, and no error is thrown anywhere in the test. What is the mistake, and how do you avoid it recurring?`,null,[`The key name should be 'ArrowDown', not 'Down'; because an incorrect key name does not throw, it is worth following up such an interaction with an assertion on the actual resulting state (e.g. which item is now highlighted) rather than assuming a silent, error-free press() call necessarily worked as intended`,`This is a genuine Playwright bug in the press() implementation with no available fix`,`Down is correct for Firefox, and the test must special-case the key name per browser engine`,`press() requires the key name to be passed twice for arrow keys specifically, once for down and once for up`],0,`Following any keyboard-driven interaction with an assertion on its actual observable effect (not just trusting that press() resolved without error) is what catches this exact class of silent, no-op key-name mistake.`],[`L`,`A`,`Why is it valuable for a thorough accessibility test suite to verify a menu can be opened via BOTH hover() (mouse users) and focus()-plus-a-key (keyboard users) as two separate, independent test cases?`,null,[`Because these represent two genuinely different interaction paths a real user might rely on exclusively, a test suite that only exercises hover() could pass even if the keyboard-accessible path is completely broken, silently leaving keyboard-only users unable to use the feature at all`,`hover() and focus()-based activation are always implemented identically under the hood, so testing both is purely redundant`,`Testing both paths doubles the total execution time for no additional coverage benefit`,`Keyboard-based menu activation is not a real-world pattern and does not need dedicated test coverage`],0,`Mouse-hover and keyboard-focus activation are often implemented as separate code paths in a component, so verifying one working says nothing reliable about whether the other one does too.`],[`R`,`I`,`You need to verify a dropdown menu opens correctly for both mouse users (hover) and keyboard users (focus then a key press). How do you structure these as tests?`,null,[`Write two separate, independent test cases: one that hovers the trigger and asserts the menu opens, and another that focuses the trigger and presses the appropriate key (like Enter or ArrowDown) and separately asserts the menu opens, verifying feature parity across both interaction modes`,`Write a single test that both hovers and focuses the trigger simultaneously, then asserts the menu is open once, covering both paths in one assertion`,`Test only the hover path, since focus-based activation is generally assumed to work the same way without separate verification`,`Skip testing the interaction entirely and rely only on a visual screenshot of the open menu`],0,`Keeping the two interaction paths as separate tests ensures a regression in either one is caught individually, rather than being potentially masked by testing them together in a single combined case.`],[`L`,`A`,`Does locator.click() on a link with target="_blank" itself wait for or provide access to the resulting new browser tab?`,null,[`No, click() only handles the click actionability/dispatch itself; capturing a reference to the resulting new tab still requires separately wrapping the click with context.waitForEvent('page') inside Promise.all, since actions and navigation-adjacent waiting are handled by distinct APIs`,`Yes, click() automatically returns the new Page object as its resolved value when the target link opens a new tab`,`Yes, but only if { waitForPopup: true } is explicitly passed as an option to click()`,`No, and there is no way at all to obtain a reference to a tab opened via target="_blank"`],0,`click() and context.waitForEvent('page') address two conceptually separate concerns: performing the click itself, and separately capturing whatever new page object results from it.`],[`S`,`A`,`A 'Download Report' button opens a new browser tab, and that new tab itself then triggers a file download. How do you write a single test that coordinates capturing both the new tab and the resulting download?`,null,[`Wrap the button click together with context.waitForEvent('page') in Promise.all to capture the new tab, then on that new page, wrap whatever triggers the download (or simply await it if it starts automatically) with page.waitForEvent('download') to capture the file, coordinating both waits around their respective triggering actions`,`Use a single page.waitForEvent('download') call on the original page and assume it also captures anything happening on the new tab automatically`,`Download events cannot be captured on any page other than the one the test started with`,`Wrap the click in context.waitForEvent('download') directly, since download events fire on the context, skipping the new-tab step entirely`],0,`Multi-stage interactions like this require carefully sequencing each Promise.all-wrapped wait around its own specific triggering action, first for the new tab, then for the download inside it.`],[`O`,`B`,`What does locator.uncheck() do to an already-unchecked checkbox?`,`await page.getByLabel('Newsletter').uncheck();`,[`Nothing; like check(), uncheck() is idempotent and only clicks the checkbox if it is currently checked, leaving an already-unchecked box untouched`,`It always clicks the checkbox once regardless of its current state, potentially checking it instead`,`It throws an error if the checkbox is already unchecked`,`It resets the entire form the checkbox belongs to`],0,`Both check() and uncheck() first inspect the current checked state and only dispatch a click when a state change is actually needed, avoiding an accidental toggle.`],[`O`,`I`,`What does the { modifiers: [] } (an explicitly empty array) do when passed to click(), compared to simply omitting the modifiers option?`,`await page.getByRole('button', { name: 'Delete' }).click({ modifiers: [] });`,[`It is functionally equivalent to omitting modifiers entirely, ensuring the click happens with no modifier keys held, which can matter if a prior step left a key logically down`,`It throws a validation error since modifiers cannot be an empty array`,`It applies every possible modifier key simultaneously`,`It disables all keyboard input for the duration of the click`],0,`An explicit empty array is simply the no-modifiers case spelled out, useful in generated or parameterized code paths where the modifiers list is computed dynamically and might end up empty.`],[`O`,`I`,`What does this sequence verify about focus order in a modal dialog?`,`await page.getByRole('dialog').getByRole('button', { name: 'Cancel' }).focus();
await page.keyboard.press('Tab');
await expect(page.getByRole('dialog').getByRole('button', { name: 'Confirm' })).toBeFocused();`,[`That pressing Tab from the Cancel button moves focus to the Confirm button next, verifying the dialog's intended tab order between its two action buttons`,`That clicking Cancel automatically opens a Confirm sub-dialog`,`That the dialog cannot be tabbed out of once Cancel is focused`,`That focus() and toBeFocused() are aliases for the same assertion`],0,`Focusing a known starting point explicitly, then pressing Tab and asserting on the next focused element, is a precise way to verify a specific tab-order relationship rather than the whole page's tab sequence.`],[`O`,`A`,`What is being verified by combining trial and a real click here?`,`const btn = page.getByRole('button', { name: 'Pay Now' });
await btn.click({ trial: true });
await btn.click();`,[`The first call confirms the button is actionable without triggering the payment, and the second call performs the real click, letting a test explicitly separate readiness-checking from the actual side-effecting action for clearer diagnostics if something fails`,`It performs the payment exactly once; the trial call is a harmless no-op with zero purpose`,`It clicks the button twice, potentially triggering two payments`,`trial: true converts the second, real click into a dry run as well`],0,`Separating a non-committal readiness check from the real action can make it clearer, when something fails, whether the problem is the element not being actionable or the application not responding correctly to the click.`],[`O`,`I`,`What does this code verify about a range slider input?`,`const slider = page.getByRole('slider', { name: 'Volume' });
await slider.focus();
await page.keyboard.press('ArrowRight');
await page.keyboard.press('ArrowRight');
await expect(slider).toHaveValue('52');`,[`That a native range input responds to keyboard arrow keys by incrementing its value, starting from 50 and moving to 52 after two ArrowRight presses, exercising keyboard-based slider control rather than mouse dragging`,`That the slider can only be adjusted via mouse drag, and this test is expected to fail`,`ArrowRight always increments a slider by exactly one unit per press, regardless of its configured step size, which this test assumes without verifying`,`That focus() automatically sets the slider's value to 52`],0,`Native range inputs support keyboard adjustment by design, so testing that path with focus() plus arrow key presses is a legitimate, often simpler alternative to computing drag coordinates for a slider.`],[`O`,`B`,`What is the effect of calling dblclick() on a table cell to enter inline-edit mode?`,`await page.getByRole('cell', { name: 'Widget A' }).dblclick();
await expect(page.getByRole('textbox')).toBeVisible();`,[`It simulates a real double-click gesture (two rapid clicks) which the grid component listens for to switch that cell into an editable textbox, and the test then asserts the textbox appeared`,`It performs two entirely independent single clicks with an arbitrary, unspecified delay between them`,`dblclick() always requires an explicit clickCount option to function, unlike this example`,`It permanently deletes the cell's original content as a side effect of double-clicking`],0,`dblclick() is a convenience for a common, specific gesture (two rapid clicks) that many UI patterns, like inline-edit-on-double-click grids, are built to respond to.`],[`O`,`I`,`What is the purpose of pressing Escape after opening this dropdown?`,`await page.getByRole('combobox', { name: 'Filter' }).click();
await page.keyboard.press('Escape');
await expect(page.getByRole('listbox')).toBeHidden();`,[`To verify the dropdown correctly closes in response to the Escape key, a common accessibility expectation for dismissible listboxes and popups`,`To submit the currently selected filter value`,`To permanently disable the combobox for the remainder of the test`,`Escape here is being used as a substitute for clicking outside the dropdown, which is a completely different interaction the test conflates incorrectly`],0,`Escape-to-close is a widely expected keyboard behavior for popups and comboboxes, and explicitly testing it is a good way to catch a missing or broken keyboard-dismiss handler.`],[`O`,`A`,`What interaction does this sequence test for a resizable panel divider?`,`const divider = page.locator('.panel-divider');
const box = await divider.boundingBox();
await page.mouse.move(box.x + box.width / 2, box.y + box.height / 2);
await page.mouse.down();
await page.mouse.move(box.x + 150, box.y + box.height / 2, { steps: 10 });
await page.mouse.up();`,[`Dragging a resizable split-panel divider 150 pixels to the right using a manual, stepped mouse sequence, which gives fine-grained control appropriate for a divider whose resize logic reacts to incremental mousemove events`,`Clicking the divider twice at two different locations, unrelated to resizing`,`dragTo() being called internally, since mouse.move/down/up is just an alias for it`,`Testing that the divider cannot be moved at all, expecting the sequence to fail`],0,`A resize handle is a good example of an interaction where manual mouse.move/down/up with intermediate steps gives more reliable control than a higher-level convenience method, similar to custom sortable-list dragging.`],[`O`,`I`,`What does this code confirm about a custom star-rating widget built from five separate clickable icons?`,`await page.getByRole('radio', { name: '4 stars' }).check();
await expect(page.getByRole('radiogroup', { name: 'Rating' })).toHaveAccessibleName('Rating');
await expect(page.getByRole('radio', { name: '4 stars' })).toBeChecked();`,[`That the star-rating widget is correctly implemented as a radio group of individually selectable options, and that selecting the 4-star option updates its checked state as expected, rather than relying on raw icon clicks with no semantic state`,`That exactly four radio buttons exist in the group, no more and no fewer`,`That toBeChecked() and toHaveAccessibleName() are interchangeable assertions`,`That star ratings can only ever be tested by counting filled versus unfilled icon images`],0,`When a custom widget is built on top of proper ARIA radio-group semantics, testing it becomes just as straightforward as testing a native set of radio buttons, using check() and toBeChecked() directly.`],[`R`,`I`,`A rich text editor toolbar has a Bold button that should toggle bold formatting on selected text. How do you test that selecting text, clicking Bold, and checking the result works correctly?`,null,[`Use locator.selectText() (or a keyboard-based selection) on the target text, click the Bold toolbar button, then assert the resulting DOM reflects bold formatting, e.g. checking for a <strong> or <b> wrapper via evaluate() or a CSS-based assertion, since typical text assertions do not capture formatting`,`Use fill() to replace the paragraph with pre-bolded text, then click Bold and assert nothing changed`,`This cannot be tested since rich text formatting is purely visual and has no DOM representation`,`Take a screenshot before and after clicking Bold and rely solely on an image diff, since no other verification method exists`],0,`Formatting toggles in a rich text editor typically manifest as real DOM structure changes (wrapping selected text in a formatting element), which can be verified directly rather than resorting only to visual screenshot comparison.`],[`S`,`A`,`A custom multi-select tag input lets users type text and press Enter to add each tag, and Backspace on an empty input removes the last tag. How do you test adding two tags and then removing one via Backspace?`,null,[`pressSequentially or fill the input with the first tag text, press Enter, repeat for the second tag, assert both tags are rendered, then with the input empty, press Backspace and assert only one tag remains, verifying both the add and remove interactions in sequence`,`Use fill() to set the input's value to both tags separated by commas in one call, since the app is expected to parse comma-separated input automatically`,`Test only the add behavior, since Backspace-based removal is too implementation-specific to be worth testing`,`Use setInputFiles() to attach the tag text as if it were a file, then assert the tags appear`],0,`Testing this kind of interactive, keyboard-driven widget benefits from mirroring the exact sequence a real user would perform, add-add-then-remove, and asserting on the resulting tag count/content at each meaningful step.`]],assertions:[[`O`,`B`,"What makes web-first Playwright assertions different from generic `expect()` assertions?",null,[`They use a different syntax`,`They auto-retry until the condition passes or a timeout expires`,`They only work with async/await`,`They require importing from a separate package`],1,`Web-first assertions poll the DOM repeatedly — no manual sleep needed before asserting.`],[`O`,`B`,"What does `await expect(locator).toBeVisible()` do if the element is momentarily hidden during a CSS animation?",null,[`It fails immediately`,`It retries until the element becomes visible or the timeout expires`,`It skips the assertion silently`,`It waits exactly 1 second then checks once`],1,`Auto-retry is the key advantage — assertions tolerate transient DOM states.`],[`O`,`I`,"What is the difference between `toHaveText()` and `toContainText()`?",null,[`toHaveText() is for headings; toContainText() is for paragraphs`,`toHaveText() requires the full text to match; toContainText() allows a partial match`,`toHaveText() is case-sensitive; toContainText() is not`,`They are identical — both do partial matching`],1,`Use toContainText() when you only care that a substring or regex appears.`],[`O`,`B`,"What does `await expect(locator).not.toBeVisible()` assert?",null,[`That the locator matches zero elements`,`That the element exists but is hidden or absent from the visible area`,`That the element has display:none in CSS`,`That the element was never attached to the DOM`],1,`.not negates the assertion — the element must not be visible (hidden or detached).`],[`O`,`I`,"What does `await expect(locator).toHaveCount(3)` do?",null,[`Asserts the element has exactly 3 child nodes`,`Asserts that exactly 3 elements match the locator`,`Asserts the element's text is the number 3`,`Loops through 3 matching elements`],1,`toHaveCount() verifies the number of elements that match the locator expression.`],[`O`,`B`,"What does `await expect(page).toHaveURL(/dashboard/)` check?",null,[`That the page title contains "dashboard"`,`That the current browser URL matches the regex /dashboard/`,`That the page has a link to /dashboard`,`That the page element with id=dashboard is visible`],1,`toHaveURL() is a page-level assertion that auto-retries until the URL matches.`],[`O`,`I`,"What does `expect.soft(locator).toHaveText('Name')` do differently from a regular assertion?",null,[`It records the failure without stopping the test`,`It waits twice as long before failing`,`It only asserts during the first test run`,`It suppresses the failure from the report`],0,`Soft assertions let you collect all failures in one run instead of stopping at the first.`],[`O`,`I`,"What does `await expect(locator).toHaveValue('user@test.com')` check?",null,[`That the element's inner text is user@test.com`,`That an input element's current value property is user@test.com`,`That the element's placeholder is user@test.com`,`That the form was submitted with this email`],1,`toHaveValue() reads the value property of an <input>, <textarea>, or <select>.`],[`O`,`B`,"What does `await expect(locator).toBeChecked()` assert?",null,[`That the element has a CSS class called checked`,`That a checkbox or radio button is in the checked state`,`That the element passed a validation check`,`That the element has aria-required=true`],1,`toBeChecked() is the semantic assertion for checkbox/radio state.`],[`O`,`I`,"What does `await expect(response).toBeOK()` assert?",null,[`That the response body is not empty`,`That the HTTP status code is in the 200-299 range`,`That the response time is under 200 ms`,`That the response Content-Type is application/json`],1,`toBeOK() is an API-response assertion equivalent to status >= 200 && status < 300.`],[`O`,`A`,"What does `expect.poll(fn, { timeout: 30_000 })` do?",null,[`Runs the test 30 times`,`Calls fn repeatedly until its return value satisfies the chained matcher or the timeout expires`,`Waits 30 seconds then runs fn once`,`Mocks the timer so fn runs synchronously`],1,`expect.poll() is for retrying custom async functions — use it when there is no locator to observe.`],[`O`,`I`,`What is the default auto-retry timeout for web-first assertions in Playwright?`,null,[`1 second`,`5 seconds`,`10 seconds`,`30 seconds`],1,`The default is 5 s (configurable via expect.timeout in playwright.config.ts).`],[`F`,`B`,"Complete: `await expect(locator).toBe______()` to assert the element is visible",null,[`Visible`,`Shown`,`Displayed`,`Present`],0,`toBeVisible() is the standard visibility assertion.`],[`F`,`I`,"Complete: `await expect(locator).toHave______( 'user@test.com' )` to check an input's value",null,[`Value`,`Text`,`Content`,`Input`],0,`toHaveValue() reads the value property of form elements.`],[`F`,`B`,"Complete: `await expect(page).toHave______(/dashboard/)` to check the current URL",null,[`URL`,`Path`,`Route`,`Address`],0,`toHaveURL() is the page-level URL assertion.`],[`F`,`I`,"Complete: `await expect(locator).toHave______( 'btn-primary' )` to check a CSS class",null,[`Class`,`Style`,`Attribute`,`CSS`],0,`toHaveClass() checks that the element has the given class name.`],[`F`,`I`,"Complete: `await expect(locator).toHave______( 'aria-expanded', 'true' )` to check an attribute",null,[`Attribute`,`Property`,`Tag`,`Role`],0,`toHaveAttribute(name, value) checks a specific HTML attribute.`],[`F`,`I`,"Complete: `await expect(locator).toHave______( 4 )` to assert 4 items match the locator",null,[`Count`,`Length`,`Size`,`Total`],0,`toHaveCount(n) asserts the number of matched elements.`],[`F`,`A`,"Complete: `await expect.______( () => fetchStatus() ).toBe( 'done' )` to poll a custom function",null,[`poll`,`retry`,`wait`,`check`],0,`expect.poll(fn) repeatedly calls fn until the matcher passes or timeout expires.`],[`E`,`B`,"What is wrong?\n```ts\nexpect(page.getByRole('heading')).toHaveText('Welcome');\n```",null,[`getByRole is not valid inside expect()`,`await is missing — the assertion runs detached and may not report failures`,`toHaveText() only accepts RegExp`,`The heading role requires a level option`],1,`Web-first assertions return a Promise; without await the check runs asynchronously and failures are lost.`],[`E`,`I`,"What is wrong?\n```ts\nawait expect(5).toBeVisible();\n```",null,[`5 is not a valid assertion target — toBeVisible() only works on Locators`,`5 should be wrapped in quotes`,`toBeVisible() requires a page argument too`,`expect() needs async/await on the number`],0,`toBeVisible() is a web-first assertion and only accepts Locator or Page, not plain values.`],[`E`,`B`,"What is wrong?\n```ts\nawait expect(locator).toHaveText(42);\n```",null,[`toHaveText() cannot be used with await`,`toHaveText() accepts a string or RegExp, not a number`,`The locator must be resolved first`,`42 needs to be converted with String()`],1,`toHaveText() only accepts string or RegExp — passing a number causes a TypeScript/runtime error.`],[`E`,`I`,"What is wrong?\n```ts\nawait expect(locator).not().toBeVisible();\n```",null,[`not is not a valid property on expect()`,`not is a property, not a method — it should be .not.toBeVisible()`,`toBeVisible() cannot be negated`,`not requires a reason string argument`],1,".not is a property accessor, not a function — `expect(l).not.toBeVisible()` not `.not().toBeVisible()`."],[`E`,`I`,"What is wrong?\n```ts\nawait expect(locator).toHaveValue('user@test.com');\n// locator targets a <div>, not an input\n```",null,[`toHaveValue() should be toContainText() for divs`,`toHaveValue() should be toHaveAttribute() for divs`,`toHaveValue() only works on form elements with a value property`,`Divs cannot be targeted with locators`],2,`toHaveValue() reads the value property — meaningless on a div; use toHaveText() or toContainText() instead.`],[`E`,`A`,"What is wrong?\n```ts\nawait expect.soft(locator).toBeVisible();\nthrow new Error('stop');\n// Soft failures are never reported\n```",null,[`expect.soft() suppresses all failures — use regular expect`,`The thrown error prevents the test framework from flushing soft assertion results`,`soft requires a second boolean argument`,`expect.soft() needs its own await block`],1,`Throwing before test end skips the Playwright framework's end-of-test flush — soft failures are lost.`],[`D`,`B`,"An assertion `await expect(locator).toBeVisible()` fails after 5 s even though the element appears after 6 s. What is the fix?",null,[`Add a manual sleep(6000) before the assertion`,`Pass { timeout: 10_000 } to extend the retry window`,`Use toBeEnabled() instead of toBeVisible()`,`Break the test into two tests`],1,`{ timeout: ms } overrides the default 5 s for this specific assertion.`],[`D`,`I`,`\`await expect(locator).toHaveText('Hello')\` fails but the element clearly shows "Hello". What should you check first?`,null,[`The locator might be matching the wrong element`,`The text might include hidden whitespace, newlines, or Unicode spaces`,`toHaveText() is broken in this version`,`The assertion needs force: true`],1,`toHaveText() trims whitespace by default but surrounding HTML nodes or invisible chars can still cause mismatches.`],[`D`,`B`,"`await expect(page).toHaveURL('/success')` fails immediately after `click()`. Why?",null,[`toHaveURL() does not work on the default page`,`Navigation is async — the URL has not yet changed when the assertion first runs`,`click() does not trigger navigation`,`The URL check needs a regex not a string`],1,`toHaveURL() auto-retries so it will eventually pass once navigation completes — if it fails immediately, the navigation itself likely failed.`],[`D`,`I`,'`await expect(locator).toBeChecked()` fails on a custom toggle built with a <div aria-checked="true">. What is wrong?',null,[`toBeChecked() only works on <input type="checkbox"> elements`,`toBeChecked() reads aria-checked for elements with role="checkbox" — the div needs role="checkbox"`,`toBeChecked() requires force: true for ARIA elements`,`ARIA checkboxes must use toHaveAttribute() instead`],1,`toBeChecked() checks aria-checked when the element has a checkbox or radio role — add role="checkbox" to the div.`],[`D`,`I`,`Multiple soft assertions fail but the test report shows only one error. What is likely wrong?`,null,[`expect.soft() only captures the first failure`,`The test throws or crashes before reaching the end where soft failures are flushed`,`Soft assertions are disabled in CI mode`,`The reporter does not support soft assertions`],1,`An unhandled throw or test crash before end-of-test prevents Playwright from flushing accumulated soft failures.`],[`D`,`A`,"`expect.poll()` resolves immediately with the wrong value on the first call. How do you add a delay between retries?",null,[`Add { delay: 2000 } to the poll options`,`Use the intervals option: { intervals: [2_000, 5_000] }`,`Poll cannot be configured — use a manual while loop`,`Add await page.waitForTimeout(2000) before expect.poll`],1,`The intervals option specifies milliseconds between successive retries.`],[`C`,`B`,"Complete to assert a button is disabled.\n```ts\nawait expect(page.getByRole('button', { name: 'Save' })).toBe______();\n```",null,[`Disabled`,`Hidden`,`Inactive`,`Locked`],0,`toBeDisabled() is the semantic assertion for the disabled state.`],[`C`,`I`,"Complete to assert a paragraph contains the word \"success\".\n```ts\nawait expect(locator).______Text('success');\n```",null,[`toContain`,`contain`,`includes`,`toHave`],0,`toContainText() asserts a partial text match.`],[`C`,`I`,"Complete to assert a list renders exactly 5 items.\n```ts\nawait expect(page.locator('li')).toHave______( 5 );\n```",null,[`Count`,`Length`,`Total`,`Size`],0,`toHaveCount(n) checks how many elements match the locator.`],[`C`,`I`,"Complete to assert the URL matches the pattern /success.\n```ts\nawait expect(page).toHave______(/\\/success/);\n```",null,[`URL`,`Path`,`Route`,`Title`],0,`toHaveURL() is the page-level URL assertion.`],[`C`,`A`,"Complete to collect failures without stopping the test.\n```ts\nawait expect.______( locator ).toBeVisible();\n```",null,[`soft`,`silent`,`collect`,`lazy`],0,`expect.soft() records the failure and lets the test continue.`],[`L`,`B`,`Why must web-first assertions be awaited?`,null,[`They are synchronous and await has no effect`,`They return a Promise; without await the assertion runs detached and failures go unreported`,`The Playwright runner requires await for syntax reasons`,`await is optional — it only affects timeout behavior`],1,`Without await, the Promise is created but not waited on — the assertion may run after the test already passed.`],[`L`,`I`,"When should you use `expect.poll(fn)` instead of a locator assertion?",null,[`When the assertion needs to run more than once`,`When you need to assert a condition that has no corresponding DOM element — e.g. an API response or a JS variable`,`When the locator is inside a shadow DOM`,`When the assertion value is a number`],1,`expect.poll() is for custom async conditions that cannot be observed via a locator.`],[`L`,`I`,"A test has three `expect.soft()` calls — the first and third fail. What does Playwright report?",null,[`Only the first failure — execution stops there`,`Only the last failure`,`Both failures are reported together at the end of the test`,`The test is marked as skipped`],2,`Soft assertions accumulate; all failures are reported together when the test ends normally.`],[`L`,`B`,"What is the key behavioral difference between `toHaveText()` and `toContainText()`?",null,[`toHaveText() matches the trimmed full text; toContainText() accepts a substring or regex`,`toHaveText() is case-insensitive; toContainText() is case-sensitive`,`toHaveText() works on inputs; toContainText() works on divs`,`They are identical`],0,`toHaveText() requires the whole text to match; toContainText() allows partial matches.`],[`L`,`A`,"How does `toHaveCSS('color', 'rgb(0, 128, 0)')` differ from `toHaveAttribute('style', 'color: green')`?",null,[`They are identical — both read the style attribute`,`toHaveCSS() reads the computed CSS value from the browser; toHaveAttribute() reads the raw style attribute string`,`toHaveCSS() is asynchronous; toHaveAttribute() is synchronous`,`toHaveAttribute() always wins when both are used together`],1,`toHaveCSS() reads computed styles (after cascading) — more reliable than reading the raw style attribute.`],[`R`,`B`,`After clicking "Login", you need to assert the user reached the dashboard. Which assertion do you use?`,null,[`expect(page.getByText('dashboard')).toBeVisible()`,`await expect(page).toHaveURL('/dashboard')`,`expect(page.url()).toBe('/dashboard')`,`await expect(page).toHaveTitle('dashboard')`],1,`toHaveURL() auto-retries after navigation — the safest way to assert URL change.`],[`R`,`I`,`A toast notification appears briefly after a form submission. How do you assert it was shown?`,null,[`expect(locator).toBeVisible() with a short timeout`,`await expect(page.getByRole('alert')).toBeVisible()`,`page.waitForSelector('.toast') then assert`,`Use expect.soft() for transient elements`],1,`toBeVisible() auto-retries — it will catch the toast even if the element takes a moment to mount.`],[`R`,`I`,`After clicking "Clear", you need to assert the search input is empty. Which assertion fits?`,null,[`await expect(locator).toHaveValue('')`,`await expect(locator).toBeEmpty()`,`await expect(locator).toHaveText('')`,`Both A and B are valid for an input`],3,`toHaveValue('') or toBeEmpty() both work for an input — toBeEmpty() is more concise.`],[`R`,`I`,`You want to assert a dropdown currently shows "Dark Mode". Which assertion is most precise?`,null,[`await expect(locator).toContainText('Dark Mode')`,`await expect(locator).toHaveValue('dark')`,`await expect(locator).toHaveText('Dark Mode')`,`await expect(locator).toHaveAttribute('selected', 'true')`],1,`toHaveValue() checks the selected <option>'s value attribute — the most reliable for dropdowns.`],[`R`,`A`,`You fire a POST request from a test and need to assert the API returned 201. Which assertion do you use?`,null,[`expect(response.status()).toBe(201)`,`await expect(response).toBeOK()`,`await expect(response).toHaveStatus(201)`,`expect(response).toHaveStatus('Created')`],0,`toBeOK() only checks 200-299; for a specific status code use the generic expect with response.status().`],[`S`,`I`,"A page shows a loading spinner for 3 s then renders the actual heading. `toHaveText()` fires immediately and fails. What is the correct fix?",null,[`Add a 3 s sleep before the assertion`,`The assertion already auto-retries — check whether the locator targets the heading, not the spinner`,`Use toContainText() instead — it is more lenient`,`Use waitForSelector() first`],1,`toHaveText() already retries for up to 5 s — the issue is likely the locator matching the wrong element.`],[`S`,`A`,`You need to verify five separate UI properties after a form submit, and you want the report to show ALL failures in one run. How do you write the assertions?`,null,[`Use five regular expect() calls — they already collect failures`,`Wrap all five in a try/catch`,`Use expect.soft() for all five assertions`,`Use toHaveCount(5) to check them in one call`],2,`expect.soft() on each assertion lets all five run regardless of individual failures.`],[`S`,`I`,`\`await expect(locator).toHaveText('€ 1,000')\` fails even though the page shows "€ 1,000". What could cause this?`,null,[`toHaveText() cannot handle Unicode characters`,`The € sign is rendered as an HTML entity (&euro;) that the browser resolves to € — the actual text should match`,`toHaveText() strips currency symbols`,`The comma inside the number breaks the string parser`],1,`Browsers resolve HTML entities before Playwright reads text, so the comparison should work — more likely the text includes non-breaking spaces or different Unicode code points.`],[`S`,`A`,"You want to poll a JS global `window.__jobDone` until it is true, with a 20 s timeout. Which approach is correct?",null,[`await expect(page.locator('body')).toHaveAttribute('data-done', 'true')`,`await expect.poll(() => page.evaluate(() => window.__jobDone), { timeout: 20_000 }).toBe(true)`,`await page.waitForFunction(() => window.__jobDone, null, { timeout: 20_000 })`,`Both B and C are valid — expect.poll is cleaner`],3,`waitForFunction() is the older API; expect.poll() with page.evaluate() gives you the full matcher ecosystem and integrates with test reporting.`],[`O`,`B`,"What does `expect(locator).toBeChecked()` assert?",null,[`The element has a CSS :checked pseudo-class only`,`The checkbox or radio button is in a checked state — equivalent to the checked property being true`,`The element is selected in a listbox`,`The element has attribute checked="true"`],1,`toBeChecked() is the idiomatic assertion for checkbox and radio state — auto-retries until checked or times out.`],[`O`,`I`,'What does `expect(locator).toHaveCSS("color", "rgb(255, 0, 0)")` assert?',null,[`The element has color: red in the stylesheet`,`The computed CSS value of the color property equals the given string — computed values use rgb() notation, not colour names`,`The element has a class containing red`,`The style attribute has color: red`],1,`toHaveCSS checks the computed (resolved) CSS value — always use computed format (rgb, px) not the authored format (red, 1rem).`],[`O`,`B`,"What does `expect(locator).toBeEmpty()` check?",null,[`The element has no children and no text`,`For input/textarea: value is ""; for other elements: no descendants and no text content`,`The DOM element does not exist`,`The element has display:none`],1,`toBeEmpty has different semantics for inputs vs other elements — inputs must have empty value; others must have no content.`],[`O`,`I`,'What does `expect(locator).toHaveValues(["a", "b"])` assert for a multi-select?',null,[`The element has both a and b as option values (unselected)`,`The multi-select element has exactly "a" and "b" selected — checks the array of selected option values`,`The element's text contains "a" or "b"`,`Asserts exactly 2 options exist`],1,`toHaveValues is the multi-select equivalent of toHaveValue — checks the array of selected values.`],[`O`,`I`,'What does `expect(page).toHaveScreenshot("name.png")` do on first run?',null,[`Fails because no baseline exists`,`Creates a baseline screenshot file in a __screenshots__ directory — subsequent runs compare against it`,`Passes trivially without a comparison`,`Requires a manual review step`],1,`On first run, toHaveScreenshot creates the baseline. On subsequent runs, it compares and fails if pixels differ beyond the threshold.`],[`O`,`B`,"What does `expect(locator).not.toBeVisible()` assert?",null,[`The element does not exist in the DOM`,`The element is not visible — either hidden, detached, or has display:none/visibility:hidden`,`The element has opacity: 0`,`The element is outside the viewport`],1,`not.toBeVisible() passes when the element is hidden by CSS or detached from the DOM — it auto-retries.`],[`O`,`I`,"What does `expect(response).toBeOK()` assert?",null,[`The response body is not empty`,`The HTTP response status code is in the 200-299 range`,`The response content-type is application/json`,`The response arrived within the timeout`],1,`toBeOK() is a convenience for any 2xx status — use expect(response.status()).toBe(201) for a specific code.`],[`O`,`A`,"What does `expect.configure({ timeout: 10_000 })` do?",null,[`Sets the timeout for all network requests`,`Returns a custom expect function with a default timeout of 10 seconds — assertions using this expect use 10 s instead of the global expect.timeout`,`Sets the global expect.timeout permanently`,`Creates a new assertion namespace`],1,`expect.configure returns a scoped expect — useful for sharing custom timeout or soft configuration across a helper module.`],[`O`,`I`,"What does `expect(locator).toHaveCount(0)` assert?",null,[`The first element has index 0`,`No elements matching the locator exist in the DOM`,`The locator matched 0 times per second`,`All elements are hidden`],1,`toHaveCount(0) is the idiomatic way to assert an element does not exist — auto-retries until count reaches 0 or timeout.`],[`O`,`B`,"What does `expect(locator).toBeEditable()` assert?",null,[`The element has the contenteditable attribute`,`The form field is not disabled and not readonly — it can receive user input`,`The element is in an editable state from the last action`,`The element's CSS cursor is "text"`],1,`toBeEditable checks that the control is interactive — not disabled and not readonly.`],[`O`,`I`,'What does `expect(locator).toHaveAttribute("aria-expanded", "true")` assert?',null,[`The element has any attribute named aria-expanded`,`The aria-expanded attribute is present AND its value equals "true" exactly`,`The element is expanded in the DOM tree`,`aria-expanded is true (truthy)`],1,`toHaveAttribute checks both presence and exact value — the string "true" is the value of the aria-expanded attribute.`],[`F`,`B`,'Complete to assert a checkbox is unchecked.\n```ts\nawait expect(page.getByRole("checkbox")).not.______();\n```',null,[`toBeChecked()`,`toBeEnabled()`,`toBeVisible()`,`toBeSelected()`],0,`not.toBeChecked() asserts the checkbox is in the unchecked state.`],[`F`,`I`,'Complete to assert computed background color.\n```ts\nawait expect(locator).toHaveCSS("background-color", "______");\n```',null,[`rgb(0, 128, 0)`,`green`,`#008000`,`0,128,0`],0,`Computed CSS uses rgb() notation — "green" and hex are the authored values, not the computed ones.`],[`F`,`B`,'Complete to assert no results exist.\n```ts\nawait expect(page.getByRole("listitem")).toHaveCount(______);\n```',null,[`0`,`null`,`undefined`,`[]`],0,`toHaveCount(0) asserts zero matching elements — auto-retries.`],[`F`,`I`,"Complete to assert a locator has a specific CSS class.\n```ts\nawait expect(locator).toHaveClass(/______/);\n```",null,[`active`,`class="active"`,`.active`,`[class=active]`],0,`toHaveClass accepts a string or regex matching the element's class list — just the class name without the dot.`],[`F`,`I`,'Complete to assert multiple selected options in a multi-select.\n```ts\nawait expect(page.getByRole("listbox")).toHaveValues(["en", "______"]);\n```',null,[`fr`,`'fr'`,`lang-fr`,`french`],0,`toHaveValues takes an array of selected option value strings — use the option's value attribute values.`],[`F`,`A`,'Complete to assert a visual snapshot.\n```ts\nawait expect(page).toHaveScreenshot("______");\n```',null,[`homepage.png`,`snapshot`,`screen.jpg`,`screenshot.webp`],0,`The filename argument identifies the baseline — choose a descriptive name; Playwright stores it in a __screenshots__ folder.`],[`F`,`B`,'Complete to assert the page URL contains "dashboard".\n```ts\nawait expect(page).toHaveURL(/______/);\n```',null,[`dashboard`,`#dashboard`,`.*dashboard.*`,`/dashboard/`],0,`The regex /dashboard/ matches any URL containing the word "dashboard".`],[`E`,`B`,'What is wrong?\n```ts\nexpect(await page.title()).toHaveTitle("Home");\n```',null,[`toHaveTitle cannot take a string`,`toHaveTitle is a page-level assertion: await expect(page).toHaveTitle("Home") — calling it on a string value is invalid`,`page.title() must not be awaited`,`await is not needed before expect()`],1,`toHaveTitle is called on expect(page) not expect(string) — it is a web-first assertion that auto-retries.`],[`E`,`I`,'What is wrong?\n```ts\nawait expect(locator).toHaveText(/Price: \\$\\d+/);\n// fails even though text shows "Price: $99"\n```',null,[`Regex is not supported by toHaveText`,`The regex uses \\$ to escape the dollar sign (valid in regex) but Playwright's toHaveText matches the element's FULL text — if the element also contains other text, the regex must match the full string. Use toContainText(/Price: \\$\\d+/) for partial matching.`,`Regex requires the i flag`,`toHaveText cannot match numbers`],1,`toHaveText requires the full text to match the regex; use toContainText for substring/partial regex matching.`],[`E`,`B`,'What is wrong?\n```ts\nawait expect(locator).toBe("visible");\n```',null,[`toBe() cannot take a string`,`toBe("visible") compares the locator object itself to the string — that is never equal. Use await expect(locator).toBeVisible() instead.`,`visible must be capitalized`,`await is not needed before expect(locator)`],1,`Playwright locator assertions have dedicated methods — never use toBe() with strings for visibility checks.`],[`E`,`I`,"What is wrong?\n```ts\nexpect.soft(locator).toBeVisible();\n// soft assertion runs but result is ignored\n```",null,[`soft assertions must use await`,`expect.soft() returns a Promise — without await the assertion is never settled, and failures are silently discarded. Always await soft assertions.`,`soft assertions cannot be used with locators`,`locator must be resolved before expect.soft`],0,`Missing await on soft assertions is as bad as missing await on regular assertions — the test may pass despite failures.`],[`E`,`A`,'What is wrong?\n```ts\nawait expect(locator).toHaveScreenshot("btn.png");\n// --update-snapshots was never run after a UI change\n```',null,[`toHaveScreenshot always passes on first run`,`The test fails because the existing baseline does not match the updated UI. Run npx playwright test --update-snapshots to regenerate the baseline, then commit the new snapshot.`,`toHaveScreenshot requires a special plugin`,`Snapshots expire after 30 days`],1,`After intentional UI changes, always update baselines with --update-snapshots and review them visually before committing.`],[`E`,`I`,'What is wrong?\n```ts\nawait expect(locator).toHaveText(["Item 1", "Item 2"]);\n// locator matches 3 elements\n```',null,[`toHaveText does not accept arrays`,`toHaveText with an array asserts exact correspondence — 3 elements vs 2 strings causes a mismatch. Include all 3 expected strings in the array or scope the locator to only 2 elements.`,`Array comparison is not ordered`,`toHaveText requires allTextContents() first`],1,`toHaveText with an array does a 1:1 ordered comparison — array length must match element count.`],[`D`,`B`,'`toHaveText("Submit")` fails but the button visually shows "Submit". What should you check?',null,[`The button element does not have text`,`1) The text may have leading/trailing whitespace. 2) The button may have an icon element that adds extra text to textContent. 3) The text may use a different Unicode space character. Use { trim: true } or toContainText for partial matching.`,`Add await before expect`,`Use toHaveValue instead`],0,`Whitespace and hidden icon text are common causes — inspect the element's textContent in DevTools or the Playwright Inspector.`],[`D`,`I`,'`expect(locator).toHaveClass("btn-active")` fails even though DevTools shows the class is present. Why?',null,[`toHaveClass is broken in Firefox`,`toHaveClass checks the class list — if the element has multiple classes like "btn btn-active", pass the exact class string "btn btn-active" OR use a regex: /btn-active/. Passing only "btn-active" when the element has "btn btn-active" may fail depending on the matcher mode.`,`DevTools is not reliable`,`Classes with hyphens require escaping`],1,`toHaveClass with a string expects the full classList string — use a regex to match a single class within a multi-class element.`],[`D`,`B`,'`toHaveURL("/dashboard")` fails even though the browser shows "http://localhost:3000/dashboard". What is the fix?',null,[`Add a http:// prefix to the expected string`,`With a baseURL configured, use toHaveURL("/dashboard") which should work — if it fails, the issue is likely a trailing slash mismatch or query parameters. Try toHaveURL(/dashboard/) as a regex for robustness.`,`toHaveURL requires a full absolute URL`,`Add await before page.waitForURL`],1,`toHaveURL with a partial string does a contains check when used as regex — for exact paths, verify the trailing slash and query string match exactly.`],[`D`,`A`,"`toHaveScreenshot()` passes locally but fails in CI with minor pixel differences. What are two solutions?",null,[`Switch to a text-based assertion`,`1) Set maxDiffPixels or threshold: 0.01 in the options to allow minor rendering differences between OS/GPU. 2) Run tests with a fixed Docker image that matches CI's rendering environment exactly — Docker eliminates font and antialiasing differences.`,`Switch to headless: false in CI`,`Reduce the viewport size`],1,`Pixel differences between OS/GPU are the classic cross-environment snapshot problem — Docker or threshold relaxation are the standard solutions.`],[`D`,`I`,"`expect.soft()` failures are not appearing in the report. What is the likely cause?",null,[`soft assertions are a beta feature`,`Soft failures are flushed at the end of the test only if the test completes. If the test throws before completing (e.g. from a hard assertion or unhandled error), soft failures are NOT reported. Ensure the test body runs to completion.`,`The reporter does not support soft assertions`,`Soft assertions require flush() to be called`],1,`Hard assertions and unhandled errors abort the test — call expect(page).toBeVisible() as a final hard assertion to ensure soft failures are captured.`],[`C`,`B`,'Complete to assert text is not empty.\n```ts\nawait expect(page.getByRole("status")).not.______();\n```',null,[`toBeEmpty()`,`toHaveText("")`,`toBeNull()`,`toBe(undefined)`],0,`not.toBeEmpty() asserts the element has some content — the correct inverse of toBeEmpty().`],[`C`,`I`,"Complete to assert a regex URL match.\n```ts\nawait expect(page).toHaveURL(______(/checkout\\/step\\//));\n```",null,[`/checkout\\/step\\//`,`"checkout/step/"`,`new RegExp("checkout")`,`expect.stringContaining`],0,`toHaveURL accepts regex directly — /checkout\\/step\\// matches any URL containing that path.`],[`C`,`B`,"Complete to assert the element is not in the DOM.\n```ts\nawait expect(locator).toHaveCount(______);\n```",null,[`0`,`null`,`-1`,`undefined`],0,`toHaveCount(0) asserts the locator matches no elements — the element does not exist.`],[`C`,`I`,"Complete a soft assertion.\n```ts\nawait expect.______( locator ).toBeVisible();\n```",null,[`soft`,`weak`,`optional`,`may`],0,`expect.soft(locator) creates a soft assertion that does not stop the test on failure.`],[`C`,`A`,"Complete to use a custom timeout for one assertion.\n```ts\nawait expect(slowLocator).toBeVisible({ ______: 15_000 });\n```",null,[`timeout`,`wait`,`maxWait`,`delay`],0,`The timeout option in assertion options overrides the global expect.timeout for that one assertion.`],[`L`,`B`,`Why do web-first assertions auto-retry?`,null,[`To handle flaky network connections`,`Browser UIs are asynchronous — elements appear, change text, and become enabled over time. Auto-retry makes assertions robust against the timing uncertainty inherent in UI rendering.`,`To improve test performance`,`Because JavaScript is single-threaded`],1,`Auto-retry is the fundamental design principle — it replaces fragile fixed waits with dynamic waiting tied to actual UI state.`],[`L`,`I`,"Can you use `toHaveText()` on a locator that matches multiple elements?",null,[`No — strict mode throws`,`Yes — pass an array of expected strings: expect(locator).toHaveText(["a","b","c"]) performs ordered 1:1 matching`,`Only if you use .first()`,`Only with toContainText`],1,`toHaveText with an array asserts each element's text in order — the array length must equal the element count.`],[`L`,`I`,`What happens when a soft assertion fails and a subsequent hard assertion also fails?`,null,[`Only the soft failure is reported`,`Both failures are reported — the soft assertion failure is queued; the hard assertion aborts the test and all queued soft failures are included in the report`,`The hard failure takes precedence and soft failures are ignored`,`Soft failures are cleared when a hard failure occurs`],1,`Both are reported — soft failures queue up and hard failures flush them along with the hard failure message.`],[`L`,`B`,"Can `expect()` be used without `await` for any assertions?",null,[`Never — all expects must be awaited`,`Yes — non-web-first assertions like expect(5).toBe(5) are synchronous and don't need await. Web-first assertions like toBeVisible() return Promises and must be awaited.`,`All locator assertions are synchronous`,`await is optional — it only affects timing`],1,`Synchronous matchers (toBe, toEqual, toContain) don't need await; web-first locator/page assertions do.`],[`L`,`A`,"Does `expect(locator).toBeVisible()` pass if the element exists but has `visibility: hidden`?",null,[`Yes — the element is in the DOM`,`No — toBeVisible checks that the element is both in the DOM AND has visible CSS (not hidden by display:none, visibility:hidden, or opacity:0 combined with pointer-events:none)`,`Depends on the browser engine`,`Yes — visibility:hidden elements are considered visible by ARIA standards`],1,`toBeVisible uses strict visibility semantics — hidden, zero-opacity, and display:none all count as not visible.`],[`R`,`B`,`You want to assert a success toast appears after form submission. The toast auto-dismisses in 3 seconds. How?`,null,[`Increase the timeout to 3 seconds`,`await expect(page.getByRole("status")).toBeVisible() — the default timeout (5s) covers the toast appearance, and the auto-dismiss happens after the assertion passes`,`Use toContainText with timeout: 1000`,`Mock the toast component to prevent auto-dismiss`],1,`The assertion just needs to catch the toast while it's visible — the default 5 s timeout is usually sufficient for a 3 s visible toast.`],[`R`,`I`,`You need to verify a table has exactly 10 rows after filtering. What is the assertion?`,null,[`expect(await page.getByRole("row").count()).toBe(10)`,`await expect(page.getByRole("row")).toHaveCount(10)`,`Both A and B — B auto-retries and is preferred`,`await page.waitForFunction(() => document.querySelectorAll("tr").length === 10)`],2,`toHaveCount(10) is preferred because it auto-retries — the row count may change dynamically as filters apply.`],[`R`,`I`,`You need to assert that a form field shows an accessible error message. Which assertion best captures this?`,null,[`expect(await page.locator(".error").textContent()).toContain("required")`,`await expect(page.getByRole("alert")).toContainText("required") — or assert the input has aria-invalid and is associated with the error message via aria-describedby`,`Check the network request for a 400 response`,`Use toHaveAttribute("error", "required")`],1,`Testing the accessible error presentation (alert role, aria-invalid) verifies both functionality and accessibility — a combined assertion.`],[`R`,`A`,`You want to assert a drag-and-drop reorder result. After dragging item 2 to position 1, how?`,null,[`await expect(page.getByRole("listitem").first()).toHaveText("Item 2")`,`Drag then assert: await expect(page.getByRole("listitem").nth(0)).toHaveText("Item 2") — verify the first item now shows the dragged content`,`Compare screenshots before and after`,`Use toHaveValues on the list`],1,`Text assertion on the first item verifies the reorder — content-based checks are more resilient than position-based pixel checks.`],[`R`,`B`,`A numeric counter should show "42" after 5 rapid clicks on a "+1" button. How do you assert it?`,null,[`Wait 5 seconds then assert`,`Click 5 times then await expect(counter).toHaveText("42") — toHaveText auto-retries so it handles any async counter update`,`Use expect.poll to check every 100ms`,`Assert after each click`],1,`toHaveText auto-retries — click all 5 times then let the assertion wait for the final state.`],[`S`,`B`,'`toHaveCSS("color", "red")` fails even though DevTools shows "color: red". What is wrong?',null,[`toHaveCSS does not support color assertions`,`DevTools shows "red" as the authored value but the computed value is "rgb(255, 0, 0)". Use toHaveCSS("color", "rgb(255, 0, 0)") — Playwright works with computed values.`,`color requires the # notation`,`toHaveCSS checks inline styles only`],0,`Computed CSS values use rgb() — always check the computed (not authored) representation when writing toHaveCSS assertions.`],[`S`,`I`,`A card component shows sale price in red and regular price in grey. How do you differentiate them in assertions?`,null,[`Assert both prices have different text`,`Assert the sale price element has toHaveCSS("color", "rgb(220, 38, 38)") and the regular price has a different computed colour — combining text and colour assertions verifies both content and presentation`,`Use toHaveClass("text-red-600")`,`Check the aria-label on each price element`],1,`Combining toHaveText and toHaveCSS verifies content accuracy AND visual presentation in one test.`],[`S`,`A`,`You compare screenshots between feature branches and find a 3-pixel difference in button padding. Should you update the baseline?`,null,[`Yes — always update to match the current UI`,`Investigate first: is the 3px difference intentional (design change) or accidental (a regression)? Only update the baseline after confirming it is an intended change — never update blindly.`,`No — 3px is within acceptable range`,`Yes — all pixel differences are acceptable`],1,`Snapshot updates are a deliberate decision — always review the visual diff before updating baselines to avoid accidentally accepting regressions.`],[`S`,`I`,`You want to assert that after switching themes, the background changes from white to dark. How?`,null,[`Take two screenshots and compare with diff`,`Assert before: await expect(body).toHaveCSS("background-color", "rgb(255,255,255)"), click theme toggle, assert after: await expect(body).toHaveCSS("background-color", "rgb(17,24,39)")`,`Use toHaveClass("dark") on the html element`,`Check localStorage for the theme value`],1,`Computed CSS assertions on the background colour directly verify the visual theme change — more reliable than class or storage checks.`],[`O`,`A`,"What does this accomplish, that a plain `toHaveText()` assertion could not express as easily?",`await expect.poll(async () => {
  const res = await fetch('/api/queue-length');
  const { length } = await res.json();
  return length;
}, { timeout: 10000 }).toBeLessThan(5);`,[`It repeatedly re-evaluates an arbitrary custom async function (an API call here, not a locator) until the returned value satisfies the matcher or the timeout elapses — extending web-first-style auto-retrying assertions to non-DOM conditions`,`poll() only works on Locator objects, exactly like toHaveText()`,`It runs the fetch call exactly once and immediately fails if the condition is not already true`,`expect.poll() is a synonym for setInterval with no retry/assertion semantics`],0,`expect.poll() generalizes the "retry until it matches" pattern beyond locators — to any custom, possibly-async condition your test needs to wait on.`],[`L`,`I`,"What is the practical benefit of `expect.soft()` over a regular `expect()` when validating multiple independent fields on a form after submission?",null,[`If one soft assertion fails, the test continues checking the rest of the fields, and the report shows ALL failing fields at once — instead of stopping at the first failure and requiring multiple test re-runs to discover the next issue`,`Soft assertions run faster because they skip Playwright's auto-waiting entirely`,`Soft assertions can check visual appearance; regular assertions cannot`,`There is no practical difference; soft assertions exist purely for cosmetic report styling`],0,`This is directly analogous to a form validator that shows every error at once rather than one at a time — soft assertions do the same for test diagnostics.`],[`O`,`I`,"What does the `.not` modifier do here?",`await expect(page.locator('.error-banner')).not.toBeVisible();`,[`It asserts the element is currently NOT visible, retrying until that becomes true (or timing out if it stays visible) — the logical negation of toBeVisible(), with the same auto-retry behavior`,`It immediately fails the test if .error-banner exists anywhere in the DOM, visible or not`,`It removes the element from the DOM before continuing`,`.not disables auto-retrying, checking the condition exactly once`],0,`Negated assertions still retry — this is important: "not visible yet" during a transition would otherwise cause a false pass if checked only once, immediately.`],[`O`,`A`,"What problem does `toPass()` solve here?",`await expect(async () => {
  const count = await page.locator('.notification').count();
  expect(count).toBeGreaterThan(0);
  await page.locator('.notification').first().click();
  expect(await page.locator('.notification').count()).toBe(count - 1);
}).toPass();`,[`It wraps an entire multi-step block of assertions and actions, retrying the WHOLE block as a unit until it passes without throwing — useful when several operations together are flaky as a group rather than a single locator being slow`,`It converts a failing test into a passing one automatically after 3 attempts, regardless of correctness`,`toPass() only works on a single, simple boolean expression, never multi-line blocks`,`It replaces the need for any other assertions in the test file`],0,`toPass() generalizes retry-until-success to an arbitrary block of code — beyond what a single web-first assertion on one locator could express.`],[`O`,`B`,`What does this pair of assertions verify after a navigation?`,`await page.getByRole('link', { name: 'Pricing' }).click();
await expect(page).toHaveURL(/\\/pricing$/);
await expect(page).toHaveTitle('Pricing Plans');`,[`That the URL ends in "/pricing" (via regex) AND the document title is exactly "Pricing Plans" — confirming both the navigation destination and the resulting page's identity`,`That the page title contains the word "Pricing" anywhere, case-insensitively, by default`,`toHaveURL() only accepts exact string matches, never regular expressions`,`These two assertions are redundant; toHaveURL alone would guarantee the title too`],0,`toHaveURL and toHaveTitle are both web-first, auto-retrying assertions — commonly paired to confirm a navigation landed on the right page with the right content.`],[`O`,`I`,`What does this assert?`,`await expect(page.locator('.alert')).toHaveCSS('background-color', 'rgb(220, 38, 38)');`,[`That the computed background-color style of the .alert element is exactly rgb(220, 38, 38) — useful for verifying a specific visual state (like an error/red alert) without a full screenshot comparison`,`That the CSS file defining .alert contains that exact color value as text`,`It sets the background color to that value rather than asserting it`,`toHaveCSS only works with class names, never inline styles or computed styles`],0,`toHaveCSS checks the COMPUTED style value (what the browser actually renders), regardless of whether it comes from a class, inline style, or inherited rule.`],[`L`,`A`,"When would a team write a custom matcher via `expect.extend()` instead of composing existing Playwright assertions?",null,[`When a specific, reusable domain-shaped check (e.g., "toBeAValidInvoiceRow") is asserted repeatedly across many tests — a custom matcher centralizes that logic and gives clearer, more readable failure messages than a long chain of generic assertions inline`,`Custom matchers are required for Playwright to run at all`,`expect.extend() can only be used for visual/screenshot comparisons`,`Built-in matchers cannot be combined with custom application-specific logic under any circumstances`],0,`Custom matchers are a maintainability tool — they turn a repeated, verbose inline check into a single, self-documenting, reusable assertion.`],[`O`,`A`,`What does this assertion check that toBeVisible() would not?`,`await expect(page.getByRole('alert')).toBeAttached();`,[`That the element is present in the DOM, regardless of whether it is visually hidden, off-screen, or has zero size — a weaker check than toBeVisible(), which also requires the element to actually be rendered and visible`,`That the element is visible and has a non-zero size`,`That the element has focus`,`toBeAttached() and toBeVisible() are identical aliases`],0,`toBeAttached() only cares about DOM presence — use it when you specifically want to allow display:none or visibility:hidden elements to pass.`],[`O`,`A`,`What does the ratio option do here?`,`await expect(page.getByTestId('banner')).toBeInViewport({ ratio: 0.5 });`,[`It requires at least 50% of the element’s bounding box to intersect the visible viewport before the assertion passes, not just any pixel of it`,`It resizes the viewport to 50% of the element’s size`,`It checks that the element occupies exactly 50% of the page width`,`ratio is ignored by toBeInViewport and always defaults to 100%`],0,`Without ratio, toBeInViewport() passes as soon as any part of the element intersects the viewport; ratio raises the bar to a minimum visible fraction.`],[`O`,`I`,`What does this assertion verify after pressing Tab?`,`await page.keyboard.press('Tab');
await expect(page.getByLabel('Email')).toBeFocused();`,[`That the Email field currently has keyboard focus — i.e. it is the document’s active element`,`That the Email field is visible on screen`,`That the Email field’s value is not empty`,`That the Tab key successfully submitted the form`],0,`toBeFocused() checks document.activeElement against the locator — the standard way to test keyboard-navigation order.`],[`O`,`A`,`What is being verified by this assertion?`,`await expect(page.getByRole('button', { name: 'Close' })).toHaveAccessibleName('Close dialog');`,[`That the computed accessible name (from aria-label, aria-labelledby, or text content per the accessibility tree) equals "Close dialog", even if the visible button text differs`,`That the button’s HTML id attribute equals "Close dialog"`,`That the button’s CSS class list contains "Close dialog"`,`toHaveAccessibleName() only checks visible innerText, identical to toHaveText()`],0,`The accessible name is what screen readers announce — it can come from aria-label even when the visible text is just an icon, so this assertion catches a11y regressions toHaveText() would miss.`],[`O`,`A`,`What does this assert about the element’s ARIA semantics?`,`await expect(page.locator('.custom-select')).toHaveRole('combobox');`,[`That the element’s computed ARIA role (explicit via role= or implicit from its tag) is "combobox" — useful for verifying a custom widget exposes the correct semantics to assistive tech`,`That the element has a CSS class named combobox`,`That the element is a native <select> tag`,`toHaveRole() only works on elements with an explicit role attribute, never implicit roles`],0,`toHaveRole() reads the resolved accessibility-tree role, catching cases where a <div> built to look like a dropdown never got role="combobox" added.`],[`O`,`A`,`What does this snapshot-style assertion compare against?`,`await expect(page.getByRole('navigation')).toMatchAriaSnapshot(\`
  - navigation:
    - link "Home"
    - link "Pricing"
\`);`,[`The accessibility tree (roles, names, and structure) rooted at the locator, against a YAML-like baseline — similar in spirit to toHaveScreenshot() but comparing semantics instead of pixels`,`The raw HTML markup of the element`,`The exact pixel screenshot of the navigation bar`,`The computed CSS of every child element`],0,`toMatchAriaSnapshot() lets you assert on structure and accessible names without being brittle to CSS/markup changes that don’t affect a11y.`],[`O`,`I`,`What does this assertion check?`,`await expect(page.locator('#main-form')).toHaveId('main-form');`,[`That the element’s id attribute equals exactly "main-form"`,`That the element is the main landmark of the page`,`toHaveId() checks the element’s class attribute, not id`,`It asserts the element was created by an ID generator`],0,`toHaveId() is a thin, readable wrapper — equivalent to toHaveAttribute("id", "main-form") but more expressive of intent.`],[`O`,`A`,`How does this assertion differ from checking the "value" HTML attribute?`,`await expect(page.locator('input#qty')).toHaveJSProperty('valueAsNumber', 5);`,[`toHaveJSProperty reads the live DOM property directly from the JS object (e.g. the parsed numeric value the browser maintains internally), bypassing the HTML attribute string entirely — useful when the attribute and the live property can diverge, like after user typing`,`It is identical to toHaveAttribute("value", "5") in every case`,`It only works on elements with a value attribute already set in the HTML source`,`toHaveJSProperty always returns a string, never a number`],0,`The value attribute reflects the initial HTML, but the live DOM property (like valueAsNumber or checked) reflects current state — toHaveJSProperty targets the latter.`],[`O`,`I`,`What does expect.poll() return here, and what is being asserted on it?`,`await expect.poll(async () => {
  const items = await page.evaluate(() => window.cart.items);
  return items.length;
}, { timeout: 15000 }).toBeGreaterThanOrEqual(3);`,[`expect.poll repeatedly re-runs the async callback (reading a JS global’s array length here) and applies a generic matcher (toBeGreaterThanOrEqual) to whatever it returns, retrying until it passes or 15s elapses`,`toBeGreaterThanOrEqual only works with locator-based assertions, so this code throws at runtime`,`expect.poll executes the callback exactly once and compares synchronously`,`The 15000 controls how many times the callback is retried, not how long`],0,`expect.poll() detaches the "keep retrying" behavior from locators entirely — any function returning any value can be polled with any matcher.`],[`O`,`I`,`What does this generic (non-web-first) assertion verify?`,`const body = await (await page.request.get('/api/user/42')).json();
expect(body).toEqual({ id: 42, name: 'Ada', active: true });`,[`A deep, exact structural equality between the parsed JSON object and the given literal — every key and nested value must match precisely, with no auto-retry since this is a plain synchronous Jest-style matcher, not a web-first one`,`A shallow reference-equality check (===) between the two objects`,`toEqual auto-retries for up to 5 seconds like toHaveText() does`,`It only checks that body has at least the given keys, extra keys are ignored`],0,`Generic matchers like toEqual, unlike toHaveText or toBeVisible, are synchronous and do not retry — you must already have the final value in hand before asserting.`],[`O`,`A`,`What is expect.arrayContaining doing in this assertion?`,`expect(response.tags).toEqual(expect.arrayContaining(['urgent', 'billing']));`,[`It is an asymmetric matcher: response.tags must be an array that contains at least 'urgent' and 'billing' somewhere, in any order, alongside any other elements — a full exact-array equality is NOT required`,`It requires response.tags to contain ONLY those two values and nothing else`,`It requires the two values to appear consecutively in that exact order`,`expect.arrayContaining is not valid inside toEqual and will throw`],0,`Asymmetric matchers like arrayContaining, stringContaining, and expect.any() let you assert on part of a structure while ignoring the rest.`],[`O`,`I`,`What is the effect of the second argument in this assertion call?`,`await expect(page.getByTestId('total'), 'Cart total should reflect the discount').toHaveText('$45.00');`,[`It provides a custom message that Playwright prepends to the failure output if the assertion fails, making CI failures easier to diagnose without changing the assertion’s behavior`,`It sets a custom timeout of that string’s character-length in milliseconds`,`It changes toHaveText to be case-insensitive`,`The second argument is invalid; expect() only accepts one argument`],0,`expect(value, message) is supported specifically to give shared helper functions and page objects more readable failure output than the generic assertion error alone.`],[`O`,`A`,"What does this achieve compared to `.not.toBeVisible()`?",`await expect(page.locator('.spinner')).toBeVisible({ visible: false });`,[`It is functionally equivalent to .not.toBeVisible() — the visible: false option is an alternative, sometimes more readable way to express the same negated check, especially when the visibility flag is computed dynamically`,`It asserts the opposite: that the spinner IS visible`,`visible: false disables auto-retrying for this one assertion`,`toBeVisible() does not accept an options object at all`],0,`The { visible: boolean } option is handy when you want to toggle expected visibility with a variable rather than branching between expect(...).toBeVisible() and expect(...).not.toBeVisible().`],[`O`,`A`,`What state does this assertion check for, that plain toBeChecked() cannot?`,`await expect(page.getByRole('checkbox', { name: 'Select all' })).toBeChecked({ indeterminate: true });`,[`The tri-state "indeterminate" visual state some checkboxes use to mean "some but not all children selected" — distinct from both checked and unchecked`,`That the checkbox is disabled`,`That the checkbox has never been interacted with`,`indeterminate: true is not a supported option on toBeChecked()`],0,`A "select all" checkbox commonly needs a third state — indeterminate — for partial selections; this option lets you assert that specific ARIA/DOM state directly.`],[`O`,`I`,`What is the purpose of the option passed here?`,`await expect(page.getByRole('button', { name: 'Submit' })).toBeEnabled({ enabled: false });`,[`It is an alternative spelling of toBeDisabled() — asserting the button is currently disabled — useful when the expected state is held in a boolean variable`,`It asserts the button will become enabled later in the test`,`enabled: false permanently disables the button element in the DOM`,`This throws a TypeScript error; toBeEnabled() has no options`],0,`Symmetric options like { enabled: false } and { visible: false } avoid awkward conditional branching when the expected boolean state comes from test data rather than being hardcoded.`],[`O`,`A`,`What does the mask option accomplish in this visual assertion?`,`await expect(page).toHaveScreenshot('dashboard.png', {
  mask: [page.locator('.last-updated-timestamp')],
});`,[`It covers the matched locator(s) with a solid box before capturing and comparing, so a naturally-changing element like a live timestamp does not cause false-positive diffs on every run`,`It hides the element from the DOM entirely for the rest of the test`,`It blurs the whole page except for the masked locator`,`mask deletes the baseline image so a new one is always generated`],0,`Masking is the standard fix for screenshot flakiness caused by legitimately-dynamic content (clocks, ads, animations) that has nothing to do with the UI regression you actually care about.`],[`O`,`A`,`Why is the animations option set here?`,`await expect(page.locator('.carousel')).toHaveScreenshot({ animations: 'disabled' });`,[`It freezes CSS animations and transitions at their final state (and disables infinite ones) before the screenshot is taken, preventing a mid-animation frame from causing a flaky pixel diff`,`It permanently removes all CSS animation rules from the stylesheet`,`It speeds up the animation so the screenshot captures it faster`,`animations is only relevant to video recording, not screenshots`],0,`animations: "disabled" (the default for toHaveScreenshot, in fact) exists specifically because animated UI is one of the most common sources of visual-test flakiness.`],[`O`,`A`,`What does maxDiffPixelRatio control here, versus a fixed pixel count?`,`await expect(page).toHaveScreenshot('hero.png', { maxDiffPixelRatio: 0.02 });`,[`It allows up to 2% of the total pixels in the image to differ before the comparison fails — scaling naturally with image size, unlike a fixed maxDiffPixels count which would be too strict for large images and too lenient for small ones`,`It requires at least 2% of pixels to differ, or the test fails for being "too similar"`,`It sets the JPEG compression ratio used to store the baseline`,`maxDiffPixelRatio only applies to toMatchSnapshot, never toHaveScreenshot`],0,`Percentage-based thresholds are more robust than absolute pixel counts when the same test runs against screenshots of very different resolutions across projects.`],[`O`,`I`,`What is the effect of the fullPage option here?`,`await expect(page).toHaveScreenshot('full-article.png', { fullPage: true });`,[`It captures the entire scrollable page content, not just the current viewport — Playwright scrolls and stitches the capture automatically`,`It captures the screenshot at full (100%) zoom level regardless of device pixel ratio`,`fullPage makes the assertion ignore any mask options`,`It forces the browser window to maximize before capturing`],0,`Without fullPage, toHaveScreenshot() only captures what is currently visible in the viewport — long pages need fullPage: true to compare the whole thing.`],[`O`,`A`,`How does toMatchSnapshot() differ in purpose from toHaveScreenshot() here?`,`const pdfText = await extractTextFromPdf(await download.path());
expect(pdfText).toMatchSnapshot('invoice-text.txt');`,[`toMatchSnapshot is the general-purpose file-based snapshot matcher — it works on any Buffer or string (like extracted text, not just images), whereas toHaveScreenshot() is specialized for page/element image comparisons with built-in pixel-diff options`,`They are exact synonyms; toMatchSnapshot only exists for backward compatibility`,`toMatchSnapshot can only compare PNG images, same as toHaveScreenshot`,`toMatchSnapshot always requires a live page object as its target, unlike toHaveScreenshot`],0,`Reach for toMatchSnapshot() when you want a versioned baseline for non-image output — generated text, JSON exports, PDFs converted to text, etc.`],[`O`,`I`,`What value must this assertion match against?`,`await expect(page.locator('.badge')).toHaveCSS('color', /rgb\\(2[0-4]\\d, \\d+, \\d+\\)/);`,[`toHaveCSS accepts a RegExp as well as an exact string, so this passes as long as the computed color property matches the pattern (e.g. any reddish rgb() value), not one fixed color`,`toHaveCSS only accepts string literals; passing a RegExp throws a runtime error`,`The regex is matched against the CSS class name, not the computed value`,`This assertion always fails because computed colors are never expressed with rgb()`],0,`A RegExp is useful when a design system allows a small range of acceptable values (e.g. any shade of red) rather than one exact computed color.`],[`O`,`I`,`What does this assertion accept as valid attribute values?`,`await expect(page.locator('[data-testid="card"]')).toHaveAttribute('data-status', /^(pending|processing)$/);`,[`toHaveAttribute also accepts a RegExp for the expected value, so the assertion passes if data-status is exactly "pending" or exactly "processing"`,`It requires the attribute value to contain the literal text "^(pending|processing)$"`,`toHaveAttribute cannot take a RegExp; only toHaveText and toHaveCSS support that`,`This checks for the PRESENCE of the attribute only, ignoring the regex entirely`],0,`Regex-based attribute assertions are useful for values with a small closed set of valid states without needing an .or() chain of exact-match assertions.`],[`O`,`I`,`Why is a RegExp used here instead of an exact string?`,`await expect(page.locator('button')).toHaveClass(/(^|\\s)btn-primary(\\s|$)/);`,[`The button likely has multiple space-separated classes (e.g. "btn btn-primary large"), and toHaveClass with a plain string requires matching the WHOLE class attribute exactly — the regex instead matches btn-primary as a standalone token anywhere in the list`,`toHaveClass never accepts plain strings, only regular expressions`,`The regex checks that the element has exactly one class`,`Regex matching is required because class names are case-insensitive in HTML`],0,`A common toHaveClass() gotcha: passing just "btn-primary" as a string fails against a multi-class element because it compares the ENTIRE class string, not a substring — a regex like /\\bbtn-primary\\b/ or with anchors avoids that trap.`],[`O`,`A`,`What does this custom matcher definition allow tests to write?`,`expect.extend({
  async toBeValidInvoiceRow(locator) {
    const text = await locator.textContent();
    const pass = /^INV-\\d{5} \\| \\$[\\d.]+$/.test(text ?? '');
    return { pass, message: () => \`Expected a valid invoice row, got: "\${text}"\` };
  },
});`,["Tests can now call `await expect(locator).toBeValidInvoiceRow()` directly, centralizing the invoice-format check in one reusable, self-documenting assertion instead of repeating the regex inline in every test",`This registers a new fixture named toBeValidInvoiceRow that must be destructured in the test signature`,`The custom matcher automatically applies to every locator in the suite without being called explicitly`,`expect.extend requires the pass property to be a Promise, not a boolean`],0,`Custom matchers via expect.extend() turn a repeated, easy-to-get-wrong inline check into a named, testable, and clearly-messaged assertion.`],[`O`,`A`,`What retry timing does the intervals option configure here?`,`await expect.poll(fetchQueueDepth, {
  intervals: [1000, 2000, 5000],
  timeout: 30000,
}).toBe(0);`,[`The callback is retried after waiting 1s, then 2s, then 5s between attempts (repeating the last interval for any further retries) up to a total of 30s, instead of Playwright’s default backoff schedule`,`It runs fetchQueueDepth exactly 3 times regardless of the timeout`,`intervals sets the number of allowed failures before erroring`,`The array values are pixel-diff thresholds, not time delays`],0,`Custom intervals are useful when you know a condition needs specific spaced-out checks (e.g. a queue that only updates every few seconds) rather than Playwright’s default rapid-then-slowing polling.`],[`O`,`I`,`What does the ignoreCase option change here?`,`await expect(page.getByRole('heading')).toHaveText('welcome back', { ignoreCase: true });`,[`The comparison becomes case-insensitive, so a heading rendering "Welcome Back" or "WELCOME BACK" still passes against the lowercase expected string`,`ignoreCase only affects toContainText(), and is silently ignored on toHaveText()`,`It ignores the entire text content and only checks that the element exists`,`ignoreCase converts the actual page text to uppercase before display in the report`],0,`ignoreCase is handy for content whose exact casing is a CMS/localization detail you don’t want the test to be brittle against.`],[`O`,`A`,`What does chaining .not onto a soft assertion do here?`,`await expect.soft(page.locator('.error-banner')).not.toBeVisible();
await expect.soft(page.locator('.success-banner')).toBeVisible();`,[`Both are valid, independent soft checks — the first records a (non-fatal) failure if an error banner IS visible, the second records one if the success banner is NOT visible; the test keeps running after either fails so both get evaluated`,`.not cannot be combined with .soft; this throws a TypeError at runtime`,`The two assertions are combined into a single logical AND that must both fail to report anything`,`Soft assertions ignore .not and always check for presence, never absence`],0,`.soft and .not compose freely — soft controls whether the TEST continues past a failure, .not controls what condition is being checked; they are orthogonal.`],[`O`,`A`,`What is being verified about the HTTP response here, beyond toBeOK()?`,`const response = await page.request.get('/api/report');
await expect(response).toBeOK();
expect(response.headers()['content-type']).toContain('application/json');`,[`toBeOK() only confirms the status is 2xx; the second, generic assertion additionally confirms the response actually declares a JSON content type, catching cases where a 200 response accidentally returns an HTML error page or wrong format`,`toBeOK() already validates the content-type internally, making the second line redundant`,`headers() returns a Promise that must be awaited, so this code throws`,`content-type is not accessible from an APIResponse object`],0,`toBeOK() is deliberately narrow — combining it with assertions on headers or parsed body gives a much more complete picture of API correctness.`],[`O`,`I`,`What does this pair of assertions verify about a completed download?`,`const download = await downloadPromise;
expect(download.suggestedFilename()).toMatch(/^report-\\d{4}-\\d{2}-\\d{2}\\.csv$/);`,[`That the browser-suggested filename for the download follows the expected dated CSV naming pattern — a generic (non-locator) assertion on a plain string returned by the Download API`,`That the actual downloaded file’s bytes match a CSV schema`,`toMatch() is a web-first assertion that will retry until the filename changes to match`,`suggestedFilename() opens the file and reads its first line`],0,`Download-related checks are usually plain synchronous string/expect assertions since the download object itself isn’t a locator with retrying semantics.`],[`O`,`I`,`What does this assertion check about the filtered list?`,`await expect(page.getByRole('listitem').filter({ hasText: 'Overdue' })).toHaveCount(2);`,[`That exactly 2 list items, among all matching listitems, contain the text "Overdue" — combining locator filtering with a retrying count assertion`,`That the list has exactly 2 items total, ignoring the filter`,`filter() runs once and toHaveCount() is evaluated on a static, non-retrying snapshot`,`hasText only works with exact string matches, not partial text`],0,`Chaining .filter({ hasText }) before toHaveCount() keeps the assertion auto-retrying while narrowing scope to only the relevant subset of elements.`],[`O`,`I`,`What is the purpose of wrapping these assertions in test.step()?`,`await test.step('Verify order confirmation', async () => {
  await expect(page).toHaveURL(/\\/order\\/confirmed/);
  await expect(page.getByText('Thank you')).toBeVisible();
});`,[`It groups the two assertions under one named, collapsible entry in the HTML report and trace viewer, improving readability without changing retry or pass/fail semantics of the assertions themselves`,`It makes both assertions run in a single, combined retry loop with one shared timeout`,`test.step() disables auto-retrying for anything inside it`,`It causes the assertions to run only once even if the test is retried`],0,`test.step() is purely a reporting/organization tool — it labels a logical chunk of the test for the report, it doesn’t alter what the assertions inside actually do.`],[`O`,`A`,`What does this custom async matcher enable, and how does its async pass function work?`,`expect.extend({
  async toHaveMinRating(locator, min) {
    const stars = await locator.locator('.star.filled').count();
    return {
      pass: stars >= min,
      message: () => \`Expected at least \${min} filled stars, found \${stars}\`,
    };
  },
});
await expect(page.locator('.rating')).toHaveMinRating(4);`,[`expect.extend supports async pass functions, so the matcher can await further locator operations (like .count()) before deciding pass/fail — this lets domain logic that needs page interaction live inside a reusable custom matcher`,`Custom matchers must always be synchronous; this code will throw because pass functions cannot use await`,`toHaveMinRating auto-retries internally exactly like a built-in web-first assertion, with no extra work needed`,`The message function runs even when pass is true, always failing the test`],0,`Because expect.extend() supports async matcher functions, custom matchers can encapsulate arbitrarily complex domain-specific checks, not just simple value comparisons.`],[`O`,`A`,"Given baseURL is configured as `https://staging.example.com`, what does this assertion check?",`await expect(page).toHaveURL(/\\/checkout\\/success$/);`,[`It checks the FULL current URL (including the baseURL portion) against the regex — the regex here only needs to match a suffix of the URL, so it passes for e.g. https://staging.example.com/checkout/success`,`toHaveURL only ever compares against the path portion, stripping baseURL automatically before matching`,`baseURL is irrelevant to toHaveURL(); it only affects page.goto()`,`The regex requires the URL to be an exact character-for-character match with no unmatched leading characters`],0,`toHaveURL() always evaluates against the complete current URL string — baseURL only shortens what you type into goto(), it doesn’t change what a URL assertion compares against.`],[`O`,`A`,`Why might this negated assertion take nearly the full 15 seconds even on a passing run?`,`await page.getByRole('button', { name: 'Save' }).click();
await expect(page.locator('.saving-spinner')).not.toHaveClass(/active/, { timeout: 15000 });`,[`If the spinner still has the "active" class when the assertion first runs (because saving takes real time), Playwright must keep polling until the class is actually removed — the elapsed time reflects how long the real save operation takes, not overhead from the assertion itself`,`not.toHaveClass() always waits the full timeout regardless of the actual DOM state, by design`,`The 15000 value is a fixed delay Playwright always waits before checking anything`,`Negated assertions ignore the timeout option entirely and use a hardcoded 30s`],0,`A retrying assertion returns as soon as its condition is met — a long-feeling negative assertion usually means the awaited condition genuinely takes that long to become true, not that the assertion mechanism is slow.`],[`O`,`I`,`What is the significance of awaiting expect.soft() here?`,`await expect.soft(page.getByTestId('firstName')).toHaveText('Ada');
await expect.soft(page.getByTestId('lastName')).toHaveText('Lovelace');`,[`Just like hard assertions, expect.soft() returns a Promise that must be awaited — skipping await would let the check run detached from the test’s control flow, risking the failure being reported too late or not at all`,`await has no effect on soft assertions since they never throw`,`Soft assertions are always synchronous and awaiting them is a harmless no-op`,`Awaiting a soft assertion converts it into a hard assertion`],0,`expect.soft() differs from expect() only in whether a failure stops the test — it is exactly as async and exactly as requiring of await as a hard web-first assertion.`],[`L`,`A`,`What is the practical difference between toBeAttached() and toBeVisible()?`,null,[`toBeAttached() only requires the element to exist in the DOM (it can be hidden, zero-size, or off-screen); toBeVisible() additionally requires it to actually be rendered visibly — every visible element is attached, but not every attached element is visible`,`They are exact synonyms kept for historical compatibility`,`toBeAttached() checks CSS visibility while toBeVisible() checks DOM presence — the reverse of the intuitive naming`,`toBeAttached() only works on elements inside an iframe`],0,`Use toBeAttached() specifically when you want to allow a hidden-but-present element to pass, such as a modal container that exists before it is opened.`],[`L`,`I`,`What does toBeInViewport() add beyond checking that an element is visible?`,null,[`It also confirms the element currently intersects the browser’s visible scrolled viewport — a visible-but-scrolled-out-of-view element would pass toBeVisible() but fail toBeInViewport()`,`toBeInViewport() is identical to toBeVisible() but with a shorter default timeout`,`It checks that the element is centered exactly in the middle of the screen`,`toBeInViewport() only applies to elements with position: fixed`],0,`toBeVisible() cares about rendering (not display:none/hidden); toBeInViewport() additionally cares about scroll position relative to the viewport.`],[`L`,`I`,`Why is toBeFocused() a more reliable check than reading document.activeElement manually in page.evaluate()?`,null,[`toBeFocused() is a web-first assertion that auto-retries until the element becomes focused or the timeout elapses, absorbing normal timing between an action (like clicking a field) and the browser actually moving focus — a one-shot evaluate() check has no such retry`,`document.activeElement is not accessible from Playwright at all`,`toBeFocused() checks CSS :focus-visible specifically, which evaluate() cannot detect`,`Manual evaluate() checks run inside the Node.js process instead of the browser`],0,`Any assertion built from a single page.evaluate() snapshot loses the auto-retry safety net that makes web-first assertions robust against timing.`],[`L`,`A`,`Why would a team prefer toHaveAccessibleName() over toHaveText() when testing an icon-only button?`,null,[`An icon-only button often has empty or purely decorative visible text, so toHaveText() would fail or be meaningless — toHaveAccessibleName() instead checks the computed accessible name (from aria-label, aria-labelledby, title, etc.), which is what actually matters for screen reader users`,`toHaveAccessibleName() is faster to execute than toHaveText()`,`toHaveText() cannot be used on <button> elements at all`,`They check identical values; the choice is purely stylistic`],0,`Accessible-name assertions validate what assistive technology actually announces — critical for icon buttons, image links, and other elements with no meaningful rendered text.`],[`L`,`A`,`What kind of regression does toHaveRole() catch that a purely visual screenshot test would miss?`,null,[`A custom-built widget (e.g. a <div> styled to look like a dropdown) that visually looks correct but lacks the correct ARIA role, making it invisible or wrong to screen readers and other assistive technology — pixels can look identical while semantics silently broke`,`toHaveRole() catches performance regressions in rendering time`,`It detects when an element’s z-index has changed`,`toHaveRole() only detects changes to the DOM tag name, not ARIA attributes`],0,`Visual and accessibility correctness are separate axes — a screenshot diff cannot see whether role="combobox" silently disappeared from a custom widget.`],[`L`,`A`,`How is the workflow for toMatchAriaSnapshot() similar to toHaveScreenshot(), and how does it differ?`,null,[`Both compare a live capture against a committed baseline and require the baseline to be regenerated (e.g. with --update-snapshots) after an intentional change — the difference is toMatchAriaSnapshot() captures accessibility-tree structure and names (roles, labels, nesting) rather than pixels, so it is immune to purely visual/CSS changes that don’t affect semantics`,`toMatchAriaSnapshot() requires no baseline at all and always passes`,`toHaveScreenshot() and toMatchAriaSnapshot() produce identical output formats`,`toMatchAriaSnapshot() can only be used on the whole page, never a scoped locator`],0,`Aria snapshots give you the "detect structural/semantic regressions" benefit of visual snapshot testing while being resilient to unrelated styling changes — but like visual snapshots, they still need deliberate baseline review and updates.`],[`L`,`B`,`What is toHaveId() essentially shorthand for?`,null,[`toHaveAttribute('id', expectedValue) — a more expressive, purpose-built assertion for the common case of checking an element’s id`,`toHaveClass() with the id used as a class name`,`A check on the element’s position in the DOM tree`,`toHaveId() checks the id of the parent element, not the locator itself`],0,`Dedicated assertions like toHaveId() exist mainly for readability and clearer failure messages over the equivalent generic toHaveAttribute() call.`],[`L`,`A`,`Why might toHaveJSProperty() succeed where toHaveAttribute('value', ...) fails on the same <input>?`,null,[`The HTML value attribute reflects the element’s initial/default value from markup, but the live DOM property can diverge after the user types — reading via toHaveJSProperty targets the current in-memory state rather than the static attribute string`,`toHaveJSProperty() always ignores type coercion issues that toHaveAttribute() has`,`toHaveAttribute() cannot target <input> elements at all`,`They are functionally identical; any difference indicates a Playwright bug`],0,`This is a classic HTML footgun: element.getAttribute('value') vs element.value can differ once a user interacts with the field — Playwright exposes both, and picking the right one matters.`],[`L`,`I`,`Why are generic matchers like toEqual() and toBe() NOT auto-retrying, unlike toHaveText()?`,null,[`Generic matchers operate on plain JavaScript values you already have in hand (e.g. a parsed JSON object) rather than on a Locator representing a live, changing DOM element — there is nothing for them to "wait for" since the value is already resolved`,`Auto-retrying was deliberately disabled for performance reasons in older Playwright versions and never re-enabled`,`toEqual() and toBe() DO auto-retry, but only for numeric comparisons`,`Generic matchers can only be used inside expect.poll(), never standalone`],0,`Web-first assertions retry because their target (a Locator) represents something that changes over time in the browser; a plain value passed to toEqual() is already final by the time you call expect() on it.`],[`L`,`A`,`What is expect.any() used for inside a toEqual() or toMatchObject() call?`,null,[`It is an asymmetric matcher that accepts any value matching a given constructor/type (e.g. expect.any(Number)) — useful for fields like timestamps or generated IDs whose exact value is unpredictable but whose TYPE should be checked`,`It matches literally any value including undefined, making the whole assertion always pass`,`expect.any() can only be used with primitive types, never class instances`,`It is a synonym for expect.anything(), with no difference`],0,`expect.any(Type) lets you assert the SHAPE of dynamic data (e.g. "createdAt should be a Date") without hardcoding an exact, ever-changing value.`],[`L`,`A`,`Why can a failing negated assertion take just as long as a failing positive one, even though "nothing is happening"?`,null,[`A web-first assertion cannot know in advance that a condition will never flip — it must keep polling until the timeout expires before it can conclude the (negated or positive) expectation truly failed, so both directions cost the full timeout budget on failure`,`Negated assertions always use double the configured timeout by default`,`Only positive assertions retry; negated ones fail immediately without polling`,`This only happens when the .soft() modifier is also used`],0,`This is why an unnecessarily long timeout on an assertion that is destined to fail can silently make a whole suite slower — the "waiting for nothing" cost is real and scales with the configured timeout.`],[`L`,`B`,"What is the risk of reading a locator’s text with `await locator.textContent()` and comparing it with a plain `expect(text).toBe(...)`, instead of using `expect(locator).toHaveText(...)`?",null,[`The manual textContent() read happens once, at that instant — if the text hasn’t updated yet (e.g. an async render just started), the test fails immediately instead of giving the UI time to catch up the way toHaveText()’s auto-retry would`,`textContent() cannot be called on a Locator object at all`,`Manual reads are slower than web-first assertions, but otherwise equivalent in reliability`,`toBe() automatically converts to toHaveText() internally when given a locator’s text`],0,`Losing auto-retry by manually reading a value first is one of the most common sources of flaky, timing-dependent Playwright tests.`],[`L`,`A`,`What is the distinction between an assertion’s timeout and an action’s timeout (e.g. on locator.click())?`,null,[`An action timeout governs how long Playwright waits for a UI element to become actionable (visible, enabled, stable) before performing a click/fill/etc.; an assertion timeout governs how long a web-first assertion keeps re-checking a condition — they are separate budgets that can be configured independently`,`They are the same setting under two different names in the config`,`Action timeouts only apply in headed mode; assertion timeouts only apply headless`,`Assertions do not have their own timeout; they inherit the action timeout of the preceding step`],0,`Playwright deliberately separates "time to become interactable" (actions) from "time to become verifiably true" (assertions), letting each be tuned to the actual UI behavior it’s waiting on.`],[`L`,`A`,`What value does expect.poll() default its retry timeout to if none is specified?`,null,[`The project/global expect.timeout configured in playwright.config.ts (5 seconds if unconfigured) — the same default that governs ordinary web-first locator assertions`,`expect.poll() always defaults to 0, meaning it checks exactly once`,`It defaults to the test’s overall timeout (typically 30 seconds)`,`expect.poll() requires an explicit timeout and throws if one is not provided`],0,`expect.poll() intentionally shares the same default timeout source as other assertions so custom-condition polling behaves consistently with the rest of the suite.`],[`L`,`I`,`When would you reach for toMatchSnapshot() instead of toHaveScreenshot()?`,null,[`When the thing you want to snapshot-compare is not an image — e.g. a text export, a generated PDF converted to text, a serialized JSON blob, or any Buffer/string — toMatchSnapshot() is the general-purpose baseline-file matcher, while toHaveScreenshot() is specialized for page/element screenshots with pixel-diff-specific options`,`toMatchSnapshot() is strictly older and deprecated in favor of toHaveScreenshot() for all use cases`,`toHaveScreenshot() cannot compare full pages, only single elements`,`toMatchSnapshot() requires a running browser page, exactly like toHaveScreenshot()`],0,`Both share the "compare against a committed baseline, review and update deliberately" workflow — the difference is the kind of data being compared.`],[`L`,`B`,`Why does toHaveCSS() always compare against the computed value rather than the value authored in a stylesheet or style attribute?`,null,[`Because the computed value is what the browser actually renders after applying the cascade, inheritance, and any shorthand/keyword resolution (e.g. "red" resolves to "rgb(255, 0, 0)") — comparing against the computed value is what actually reflects the real visual outcome`,`toHaveCSS() reads the authored value too but converts it internally for display purposes only`,`It is a historical limitation that Playwright plans to fix in a future version`,`Computed values are used because they are faster for Playwright to retrieve than authored ones`],0,`This is why toHaveCSS('color', 'red') fails even on a genuinely red element — you must write the computed form the browser actually reports, like 'rgb(255, 0, 0)'.`],[`L`,`I`,`How does the trace viewer typically represent an "expect" assertion step compared to an action like click()?`,null,[`Assertion steps appear as their own distinct entries (often labeled with the matcher name, e.g. "expect.toBeVisible") separate from action steps, making it easy to see exactly which check passed, failed, or how long it spent retrying`,`Assertions never appear in the trace viewer; only actions and network requests are recorded`,`Assertions and actions are merged into a single combined step with no way to distinguish them`,`The trace viewer only shows the final pass/fail result of the whole test, with no per-assertion breakdown`],0,`Being able to see individual assertion steps (and their retry duration) in the trace viewer is one of the most useful tools for diagnosing exactly where and why a test failed.`],[`L`,`I`,`Does wrapping several assertions in a test.step() change their retry or failure behavior?`,null,[`No — test.step() is purely an organizational/reporting construct; each assertion inside still retries independently with its own timeout and fails independently, test.step() just groups them under one named, collapsible entry in reports and traces`,`Yes — all assertions inside a step share a single combined timeout budget`,`Yes — test.step() converts all contained assertions into soft assertions automatically`,`test.step() prevents any assertion inside it from failing the overall test`],0,`test.step() improves readability of the report without touching the underlying semantics of what is running inside it.`],[`L`,`A`,`What is a good reason to move a repeated, verbose inline assertion into a custom matcher via expect.extend() rather than a plain helper function that throws?`,null,[`A custom matcher integrates with Playwright’s reporting the same way built-in assertions do (readable failure messages via the message() callback, consistent report formatting, composability with .soft()/.not()) — a plain throwing helper function loses that integration and reads less like an assertion at the call site`,`Custom matchers execute faster than any hand-written helper function`,`Plain helper functions cannot be async, unlike custom matchers`,`expect.extend() is required before any assertion library can be imported`],0,`The value of expect.extend() isn’t just DRY code — it’s that the resulting check reads and behaves exactly like every other assertion in the suite, including working with .soft() and .not().`],[`L`,`I`,"Why should you avoid replacing `expect(locator).toHaveCount(0)` with `expect((await locator.all()).length).toBe(0)`?",null,[`The manual .all().length read is a one-shot snapshot with no auto-retry — if the elements haven’t finished being removed from the DOM yet, the test fails immediately instead of giving the removal time to complete the way toHaveCount() would`,`.all() cannot be called on a Locator, only on a Page`,`toBe(0) does not work with numbers, only strings and booleans`,`They are functionally identical in every respect, including retry behavior`],0,`Any pattern that resolves a locator to a concrete value BEFORE calling expect() throws away the retrying behavior that makes web-first assertions reliable.`],[`L`,`B`,`What happens when toHaveURL() is given a relative-looking pattern while baseURL is configured?`,null,[`toHaveURL() always compares against the full absolute current URL string — a regex like /\\/pricing$/ works because it only needs to match a SUFFIX of that full URL, not because baseURL is stripped or specially handled by the assertion`,`baseURL is automatically prepended to the expected pattern before comparison`,`toHaveURL() throws an error if baseURL is configured and a relative pattern is passed`,`Playwright resolves the pattern against document.baseURI instead of the configured baseURL`],0,`baseURL only affects how you write page.goto() and request URLs — assertions like toHaveURL() always operate on the real, full current URL.`],[`L`,`A`,"If an element attaches and then detaches from the DOM within an assertion’s retry window, what does `expect(locator).toHaveCount(0)` report?",null,[`It passes, because toHaveCount() keeps polling and only needs the condition to be true at SOME point before the timeout — the assertion succeeds as soon as it observes a count of 0, regardless of what happened before that observation`,`It fails, because the element existed at some point during the retry window`,`It reports a flaky/inconclusive result requiring manual review`,`toHaveCount() only checks the DOM state once, at the very start of the retry window`],0,`Retrying assertions are inherently about "does this become true within the window", not "was this true for the entire window" — understanding this prevents misreading why a seemingly-transient state still passes.`],[`L`,`I`,`Why does expect.poll() need to return a value from its callback, unlike a simple boolean check inside the test body?`,null,[`expect.poll() applies a CHAINED matcher (like .toBe(), .toBeGreaterThan(), .toEqual()) to whatever the callback returns, so it needs an actual value to compare — this lets you use the full range of Jest-style matchers against any custom async condition, not just a plain pass/fail boolean`,`The callback’s return value is only used for logging purposes and does not affect pass/fail`,`expect.poll() ignores the return value entirely and only checks whether the callback throws`,`A boolean-returning callback is invalid and will throw a type error`],0,`This is what makes expect.poll() more expressive than a manual retry loop with an if-check — you keep the full matcher vocabulary (toBeGreaterThan, toContain, toMatchObject, etc.) applied to a custom async value.`],[`L`,`A`,`How can you inspect all recorded soft-assertion failures programmatically after a test, e.g. in a custom reporter or fixture teardown?`,null,[`Via test.info().errors (populated with all soft assertion failures that were recorded during the test, alongside any hard failure) — this lets tooling like custom reporters or a debugging fixture surface everything that went wrong, not just the first failure`,`Soft assertion failures are only visible in the terminal output and cannot be accessed programmatically`,`You must manually push failures into an array yourself; Playwright provides no built-in aggregation`,`testInfo.softFailures() is the dedicated API, separate from testInfo.errors`],0,`Because soft assertions accumulate rather than throwing immediately, Playwright records them on testInfo so reporting tools can see the complete picture of what failed.`],[`L`,`I`,`What is a valid use for toHaveText() with an array of expected strings on a locator matching multiple elements?`,null,[`Asserting the ordered, full text content of each matched element in one call — e.g. verifying a list renders exactly ["Apple", "Banana", "Cherry"] in that order, where the array length must equal the number of matched elements`,`It only works when all elements have identical text`,`The array is a set of alternative acceptable values for a SINGLE element, not a per-element mapping`,`toHaveText() with an array ignores element order entirely and does a set comparison`],0,`This 1:1 ordered array comparison is a compact way to assert a whole list’s rendered content without looping and asserting on each item individually.`],[`L`,`B`,"Do you ever need to assert `toBeVisible()` immediately before calling `.click()` on the same locator?",null,[`Usually not — actionability checks (visible, enabled, stable, receives events) are already built into actions like click(), so an explicit toBeVisible() beforehand is often redundant; it can still add value for documenting test intent or producing a clearer failure message pinpointing visibility specifically`,`Yes, always — click() has no built-in waiting and will fail instantly on a hidden element`,`toBeVisible() must be called first because click() disables auto-waiting when chained directly`,`Explicit visibility checks are required only in headless mode`],0,`Understanding what actions already auto-wait for (visibility, being enabled, stability, receiving pointer events) helps avoid redundant assertions while still knowing when an explicit check adds real diagnostic value.`],[`L`,`A`,`What does toMatchObject() check, that a full toEqual() would not permit?`,null,[`toMatchObject() passes if the actual object contains all the expected object’s properties with matching values, while allowing EXTRA properties on the actual object that aren’t mentioned — toEqual() instead requires an exact match with no extra or missing keys`,`toMatchObject() ignores property values and only checks that the keys exist`,`toMatchObject() can only be used with arrays, never plain objects`,`They are functionally identical; toMatchObject is simply a legacy alias`],0,`toMatchObject() is useful when asserting on part of a larger API response object without needing to enumerate every field the backend happens to also return.`],[`L`,`B`,`Why must every web-first (locator/page) assertion be awaited, while some generic matchers technically work without it?`,null,[`Web-first assertions return a Promise that represents an async polling operation happening in the browser — without await, the test may finish before the check resolves and its result (pass or fail) is silently lost; generic sync matchers like toBe() on a plain already-known value don’t involve any async work, so a missing await has no functional effect (though it’s still poor practice)`,`Generic matchers are always awaited automatically by the test runner behind the scenes`,`All matchers, generic or web-first, throw synchronously so await is cosmetic everywhere`,`Missing await only matters for custom matchers created via expect.extend()`],0,`This distinction — Promise-returning web-first assertions vs synchronous generic matchers — is exactly why "forgot to await an assertion" is a much more dangerous, silent bug for locator assertions than for a plain toBe(5).`],[`L`,`A`,"Why is it valuable to assert on both the response headers and the parsed JSON body after `toBeOK()`, rather than relying on toBeOK() alone?",null,[`toBeOK() only confirms the HTTP status is in the 2xx range — a server can return 200 with the wrong content type, malformed JSON, or missing fields, so additional generic assertions on headers()/json() are needed to actually validate the response is correct, not just "not an error"`,`toBeOK() already fully validates the response schema internally`,`Header and body assertions are only necessary when testing against a mock server`,`toBeOK() cannot be combined with any other assertion on the same response object`],0,`2xx status alone says very little about correctness — a robust API test layers status, headers, and body-shape assertions together.`],[`L`,`A`,"What is the tradeoff of using `mask` on a toHaveScreenshot() assertion versus excluding dynamic content from the page entirely (e.g. via test-mode CSS)?",null,[`mask keeps the dynamic element in the layout (preserving realistic spacing/flow) while blanking its visual content for comparison purposes — hiding it entirely with display:none could shift surrounding layout and mask a genuine regression in how the page reflows around that element`,`mask and hiding the element produce identical results in every scenario`,`mask is slower than hiding the element because it requires a second screenshot pass`,`mask can only be applied to text elements, never images or timestamps`],0,`mask() is deliberately a "blank out, don’t remove" tool — it lets you ignore genuinely-variable content while still verifying everything AROUND it renders and lays out correctly.`],[`L`,`I`,`What is the difference between toContainText() and a plain toHaveText() when the target locator matches a single element with nested children containing extra text?`,null,[`toHaveText() requires the locator’s FULL normalized text content to equal the expected string; toContainText() only requires the expected string to appear somewhere within it — so extra surrounding text (like an icon’s aria-hidden label) breaks toHaveText() but not toContainText()`,`They behave identically for single-element locators; the difference only appears with multiple matched elements`,`toContainText() is deprecated in favor of toHaveText() with a regex`,`toHaveText() ignores nested child element text automatically, unlike toContainText()`],0,`This full-match-vs-substring distinction is the most common reason toHaveText() unexpectedly fails on an element that visually "clearly" contains the right text.`],[`L`,`A`,"Why would a shared assertion helper library benefit from consistently using the `expect(value, message)` custom-message form?",null,[`Generic reusable helpers often assert on values computed several steps away from the original test intent — a custom message lets the failure output explain WHAT business rule was being checked (e.g. "cart total should include tax") rather than just the raw locator/value mismatch, which speeds up triage in CI logs`,`Custom messages are required by TypeScript when assertions are called from inside a function rather than a test body`,`The custom message replaces the need for a hint/description in test titles entirely`,`Custom messages disable retrying, so they should only be used for generic (non-web-first) assertions`],0,`The further an assertion is from the literal test body (e.g. buried in a shared page-object method), the more valuable a descriptive custom message becomes for whoever is debugging a red CI run.`],[`L`,`A`,`Why is toBeChecked({ indeterminate: true }) meaningfully different from simply checking toHaveAttribute('aria-checked', 'mixed')?`,null,[`toBeChecked() with the indeterminate option is a purpose-built, auto-retrying web-first assertion with a clear name expressing intent, while manually checking the raw aria-checked attribute value works but is more brittle to how a specific component library expresses the mixed state and less self-documenting`,`toHaveAttribute() does not support checking for the value "mixed" under any circumstances`,`They check completely unrelated DOM properties with no overlap`,`toBeChecked({ indeterminate: true }) does not actually retry, unlike toHaveAttribute()`],0,`Purpose-built assertions like this exist to encode common semantic checks (tri-state checkboxes are a well-known UI pattern) in a more readable, retry-safe way than reaching for a raw attribute check every time.`],[`L`,`B`,`Can you assert that a currency value formatted as "$1,234.56" in the UI equals a raw number 1234.56 from your test data, and if so how?`,null,[`Not directly with toHaveText() (which compares strings) — either format the expected number to match the exact displayed string first, use a regex/toContainText() for partial matching, or read a live numeric DOM property with toHaveJSProperty() if the element actually exposes one`,`toHaveText() automatically parses currency-formatted strings into numbers before comparing`,`Numeric comparisons against formatted text are impossible in Playwright and require a custom browser extension`,`You must always round the number to the nearest string length before asserting`],0,`Formatting mismatches (currency symbols, thousand separators, locale-specific decimal points) are a very common cause of "obviously correct but failing" text assertions — pick the assertion strategy that matches how the value is actually exposed.`],[`L`,`A`,"Why does chaining `.not` onto `expect.poll()` work the same way as `.not` on a locator assertion?",null,["`.not` is a modifier on the expect() wrapper itself regardless of what’s inside it — it negates whatever matcher is ultimately applied, so `expect.poll(fn).not.toBe(0)` retries fn until its return value stops being (or never becomes) 0, following the same underlying retry-until-satisfied mechanics as any other assertion",`.not is only valid on locator-based assertions and throws a runtime error when used with expect.poll()`,`expect.poll() ignores .not silently and always checks the positive condition`,`.not converts expect.poll() into a synchronous, non-retrying check`],0,`Because .not, .soft, and the matcher chain are all composable pieces of the same expect() API surface, they combine consistently whether the underlying check is a locator, a Response, or a custom polled function.`],[`L`,`I`,"Why does asserting `expect(await page.locator('li').allTextContents()).toEqual([...])` behave differently from `expect(page.locator('li')).toHaveText([...])` in terms of reliability?",null,[`allTextContents() resolves immediately to a one-shot array with no retrying, so if the list hasn’t finished rendering yet the assertion fails outright; toHaveText() with an array is a true web-first assertion that keeps re-reading the list and retrying until it matches or times out`,`They are identical in every respect including retry semantics`,`allTextContents() can only be called on a Page, never a Locator`,`toHaveText() with an array does not support ordered comparison, only allTextContents() does`],0,`Any API that returns a resolved value BEFORE you call expect() on it (like allTextContents(), textContent(), or count()) forfeits the auto-retry that makes locator-based assertions robust.`],[`L`,`A`,`Why might a team choose expect.poll() over a custom expect.extend() matcher for a one-off, rarely-repeated async check?`,null,[`expect.poll() requires no upfront registration or shared setup — it is inline and disposable, ideal for a check used once or twice; a custom matcher makes more sense once the same check is repeated across many tests and benefits from a name and shared failure message`,`expect.poll() is strictly faster at runtime than any custom matcher`,`Custom matchers cannot use async functions, forcing the use of expect.poll() instead`,`expect.poll() is deprecated in favor of expect.extend() and should never be used for new code`],0,`The choice is about reuse and cost of abstraction — expect.poll() is the lightweight option, expect.extend() the reusable-DRY option, for two different points on that spectrum.`],[`L`,`B`,"Why is `expect(locator).toHaveCount(n)` generally preferred over `expect((await locator.count())).toBe(n)` in a UI that updates asynchronously?",null,[`locator.count() returns a snapshot at the instant it is called, with no retrying — toHaveCount() instead keeps re-checking the count until it matches or times out, tolerating in-flight renders like a list still being filtered or populated`,`locator.count() throws an error if called before the page fully loads`,`toBe(n) cannot compare numbers, only strings`,`They behave identically; the difference is purely stylistic`],0,`This mirrors the general rule for every Playwright API: prefer the version that returns a Locator/Promise you pass directly to expect() over one that resolves a value first.`],[`L`,`A`,`What does it mean that toHaveScreenshot() and toMatchAriaSnapshot() are both considered "baseline" assertions, and what discipline does that require from a team?`,null,[`Both compare a live capture against a committed reference file, so an intentional change to the UI (visual or structural) requires deliberately regenerating and reviewing the new baseline before merging — accepting a diff blindly can silently bake in a real regression`,`Baseline assertions never need review because they always represent a correct, ground-truth state`,`Only toHaveScreenshot() requires review; toMatchAriaSnapshot() baselines auto-update safely`,`Baseline assertions are unrelated to code review practices and are purely a CI configuration concern`],0,`The single biggest risk with any baseline-comparison testing style is treating "update the baseline" as a rubber stamp rather than a genuine review step.`],[`L`,`I`,"Why does `expect(locator).toHaveAttribute('aria-expanded', 'true')` fail if the attribute value is the boolean `true` set via JavaScript rather than the string `\"true\"`?",null,[`HTML attributes are always strings — even if application JS sets a boolean, the DOM coerces it to the string "true" or "false" when reflected as an attribute, so toHaveAttribute() comparing against the string 'true' should actually still pass; a real failure here more likely means the attribute is missing entirely or holds a different string value than expected`,`toHaveAttribute() cannot compare against string values, only against numbers and booleans directly`,`aria-expanded is a special attribute that Playwright cannot read`,`The assertion silently passes regardless of the actual value due to a known limitation`],0,`Because HTML attributes are string-typed by nature, debugging an unexpected toHaveAttribute() failure should start by checking the actual attribute value in the trace/DOM snapshot, not by assuming a type-coercion issue.`],[`R`,`A`,`Your team’s login button is an icon with no visible text, but screen-reader users need it to announce "Log in". How do you assert this correctly?`,null,[`await expect(page.getByRole('button')).toHaveAccessibleName('Log in')`,`await expect(page.getByRole('button')).toHaveText('Log in')`,`await expect(page.getByRole('button')).toHaveAttribute('title', 'Log in')`,`await expect(page.getByRole('button')).toContainText('')`],0,`toHaveAccessibleName() checks the resolved accessible name from aria-label/aria-labelledby/etc. — exactly what a screen reader would announce, independent of visible text.`],[`R`,`A`,`A marketing page shows a "Last updated: <live timestamp>" badge that changes every second. You need a visual regression test for the whole page without constant false failures. What do you do?`,null,[`Use await expect(page).toHaveScreenshot('page.png', { mask: [page.locator('.last-updated')] })`,`Disable visual testing for this page entirely`,`Set trace: 'on' to capture the timestamp instead of asserting on it`,`Increase maxDiffPixelRatio to 1.0 to allow unlimited differences`],0,`Masking the genuinely-dynamic element preserves the value of visual testing for everything else on the page.`],[`R`,`A`,`Your suite repeatedly checks that invoice rows follow the format "INV-XXXXX | $amount" across dozens of tests, each with the same inline regex check. How do you clean this up?`,null,[`Register a custom matcher with expect.extend({ toBeValidInvoiceRow })`,`Copy the regex into a shared constant but keep the inline expect() calls`,`Move the check into a beforeEach hook in every spec file`,`Write a comment explaining the regex above each usage`],0,`expect.extend() turns a repeated inline check into a single, self-documenting, reusable assertion with a clear custom failure message.`],[`R`,`I`,`You want to verify that pressing Tab three times from the page load lands focus on the "Submit" button, to confirm correct tab order. Which assertion do you use after the key presses?`,null,[`await expect(page.getByRole('button', { name: 'Submit' })).toBeFocused()`,`await expect(page.getByRole('button', { name: 'Submit' })).toBeEnabled()`,`expect(await page.evaluate(() => document.activeElement.tagName)).toBe('BUTTON')`,`await expect(page.getByRole('button', { name: 'Submit' })).toBeVisible()`],0,`toBeFocused() is the auto-retrying, purpose-built assertion for keyboard-navigation and focus-order testing.`],[`R`,`A`,'After a POST to `/api/orders`, you need to confirm the response is successful AND that the returned JSON contains at minimum an `id` (any number) and `status: "created"`, without caring about other fields the backend might add later. How do you assert this?',null,[`await expect(response).toBeOK(); then expect(await response.json()).toMatchObject({ id: expect.any(Number), status: 'created' })`,`expect(await response.json()).toEqual({ id: expect.any(Number), status: 'created' })`,`await expect(response).toHaveJSProperty('status', 'created')`,`await expect(response).toHaveText('created')`],0,`toMatchObject() with expect.any() lets you check the shape/type of dynamic fields and specific expected fields while tolerating extra ones — toEqual() would fail on any additional field.`],[`R`,`I`,`A custom-built dropdown (a styled <div>, not a native <select>) needs to expose role="listbox" for accessibility. Which assertion verifies this directly?`,null,[`await expect(page.locator('.dropdown')).toHaveRole('listbox')`,`await expect(page.locator('.dropdown')).toHaveClass(/listbox/)`,`await expect(page.locator('.dropdown')).toHaveAttribute('data-type', 'listbox')`,`await expect(page.locator('.dropdown')).toHaveText('listbox')`],0,`toHaveRole() reads the resolved accessibility-tree role directly — the correct check for verifying a custom widget’s ARIA semantics.`],[`R`,`A`,`Your app renders a downloaded PDF invoice, and you extract its text server-side to verify line items. Which matcher fits comparing the extracted text against a saved baseline?`,null,[`expect(extractedText).toMatchSnapshot('invoice.txt')`,`await expect(page).toHaveScreenshot('invoice.png')`,`expect(extractedText).toHaveText('invoice.txt')`,`await expect(download).toBeOK()`],0,`toMatchSnapshot() is the general file-based baseline matcher for non-image data like extracted text, unlike toHaveScreenshot() which is specialized for pixel comparisons.`],[`R`,`B`,`You need to verify a "select all" checkbox shows its tri-state (partial selection) indicator when only some rows are checked. Which assertion checks this?`,null,[`await expect(page.getByRole('checkbox', { name: 'Select all' })).toBeChecked({ indeterminate: true })`,`await expect(page.getByRole('checkbox', { name: 'Select all' })).toBeChecked()`,`await expect(page.getByRole('checkbox', { name: 'Select all' })).not.toBeChecked()`,`await expect(page.getByRole('checkbox', { name: 'Select all' })).toHaveAttribute('checked', 'partial')`],0,`The indeterminate option specifically targets the tri-state UI pattern common for bulk-select checkboxes.`],[`R`,`A`,`A shared page-object helper method performs a complex assertion buried several calls away from the actual test. How can you make a CI failure from this helper easier to diagnose?`,null,[`Pass a descriptive custom message: await expect(locator, 'Cart total should include applied discount').toHaveText(expected)`,`Wrap the assertion in a try/catch that logs to the console and rethrows a generic Error`,`Move the assertion logic into a comment above the helper function`,`Avoid using helper methods for any assertions`],0,`The expect(value, message) form is designed exactly for this — giving reusable, generic-looking assertions a business-meaningful failure message.`],[`R`,`I`,`You want to assert a notifications list contains exactly the unread items with text "New", ignoring already-read notifications also present in the DOM. What is the cleanest assertion?`,null,[`await expect(page.getByRole('listitem').filter({ hasText: 'New' })).toHaveCount(3)`,`expect((await page.getByRole('listitem').all()).length).toBe(3)`,`await expect(page.getByRole('listitem')).toHaveCount(3)`,`await page.waitForSelector('.notification.new'); expect(true).toBe(true)`],0,`Combining .filter({ hasText }) with toHaveCount() keeps the assertion scoped and retrying, without a separate manual count read.`],[`S`,`A`,`A visual regression suite passes locally but fails in CI on nearly every run, with diffs concentrated around anti-aliased text edges and a live "3 users online" counter. What combination of fixes addresses this?`,null,[`Mask the dynamic counter with mask: [...], set animations: 'disabled', and allow a small maxDiffPixelRatio for font-rendering noise between environments`,`Delete the visual tests since they are inherently unreliable`,`Increase the test timeout to give fonts more time to render identically`,`Switch every toHaveScreenshot() call to toMatchSnapshot() instead`],0,`Visual test flakiness usually has multiple independent causes (genuine dynamic content, animation timing, cross-environment rendering) that each need their own targeted fix.`],[`S`,`I`,`A team has 12 near-identical inline checks across the suite verifying that phone numbers are formatted as "(XXX) XXX-XXXX". A new hire proposes moving this into a shared custom matcher. What is the best next step?`,null,[`Register expect.extend({ toBeFormattedPhoneNumber }) once, and replace the 12 inline checks with the new matcher call`,`Extract the regex into a shared constant but keep each inline expect() call as-is`,`Add a code comment near each of the 12 checks pointing to a canonical definition`,`Leave it as-is since 12 occurrences is not enough to justify the refactor`],0,`expect.extend() is exactly the right tool once a check is both repeated and expresses a clear, reusable domain concept.`],[`S`,`A`,"A test asserts `await expect(locator).not.toHaveClass(/loading/)` right after triggering a save action, and it takes nearly the full 5-second default timeout to pass on every run, even though the save genuinely only takes about 4 seconds. Is this a bug in the assertion?",null,[`No — the assertion is working correctly; it must keep polling as long as the "loading" class is still present, and the observed near-timeout duration accurately reflects how long the save operation actually takes. Raising the timeout slightly for headroom (not "fixing a bug") is the right response`,`Yes — negated assertions should return instantly and this indicates Playwright is broken`,`Yes — .not should never be combined with .toHaveClass() and this is undefined behavior`,`No, but the class name comparison should use toHaveAttribute() instead to be faster`],0,`A retrying assertion taking close to the real duration of the thing it’s waiting on is expected behavior, not a flaw — the fix (if any) is in the app’s save speed or the configured timeout headroom, not the assertion itself.`],[`S`,`A`,`You need to verify a multi-step checkout wizard reaches the correct final state: URL changes, a confirmation message appears, AND the order total (a live DOM property, not the rendered text) equals a specific number. How do you structure this verification?`,null,[`Combine three assertions: toHaveURL() for navigation, toBeVisible()/toHaveText() for the confirmation message, and toHaveJSProperty() (or a generic expect on a parsed value) for the exact numeric total`,`Use a single toHaveScreenshot() call and rely on visual diffing to catch any of these issues`,`Use only toHaveURL() since a correct URL implies the rest of the state is also correct`,`Use expect.poll() exclusively for all three checks since it is the most general-purpose assertion`],0,`Different aspects of "did the wizard finish correctly" are best verified with the assertion purpose-built for that aspect — navigation, content, and precise numeric state each have a natural fit.`],[`S`,`I`,"A test using `expect.poll(async () => page.evaluate(() => window.__queue.length))` always returns the SAME stale value on every retry, even though the queue is genuinely changing in the browser. What is the most likely bug?",null,['The callback is missing `await` on the inner `page.evaluate()` call, so poll is comparing against an unresolved Promise object (always "truthy"/same reference) instead of the actual number',`expect.poll() caches the first return value and never re-invokes the callback`,`page.evaluate() cannot be used inside expect.poll() at all`,`The queue global variable must be renamed to avoid a naming collision with Playwright internals`],0,`A missing await inside a polled callback is a classic silent bug — the callback resolves to a Promise object instead of its eventual value, so the "value" never appears to change across retries.`],[`S`,`A`,`Your backend renders a chart to a PNG buffer server-side (no browser page involved) and you want to snapshot-test that output against a baseline image. Should you use toHaveScreenshot()?`,null,[`No — toHaveScreenshot() specifically operates on a live Page or Locator; for a raw Buffer with no page context, toMatchSnapshot() is the right general-purpose matcher for comparing binary/image data against a baseline`,`Yes — toHaveScreenshot() accepts any Buffer directly as its target`,`Neither works; you must manually diff the images with an external tool`,`Convert the buffer to base64 text first, then use toHaveText()`],0,`toHaveScreenshot() is specifically for page/locator captures; toMatchSnapshot() is the matcher built to compare an arbitrary Buffer or string against a stored baseline.`],[`S`,`I`,`Two failure reports look confusingly similar: one says an element "was never visible" and another says it "became hidden after being visible." How can you tell which actually happened just from reading the assertion?`,null,["`await expect(locator).toBeVisible()` failing tells you the element was never observed as visible during the retry window; `await expect(locator).not.toBeVisible()` failing after a period of retries tells you the element WAS/stayed visible the whole time — checking which assertion (positive vs negated) failed, plus trace viewer screenshots at retry intervals, disambiguates the two scenarios",`Both failures are indistinguishable and require re-running the test with logging added`,`Playwright always converts hidden-after-visible failures into a different, distinctly worded error type automatically`,`Only the exact millisecond timestamp of the failure can tell them apart`],0,`The trace viewer’s step-by-step screenshots during an assertion’s retry window are the most direct way to see exactly what state the element was in at each polling attempt.`],[`S`,`A`,`A team wants to review accessibility-tree changes for a component the same deliberate way they review visual screenshot diffs, without pixel comparisons. What tool fits, and what is the review workflow?`,null,[`toMatchAriaSnapshot() with a committed baseline — like toHaveScreenshot(), an intentional structural/semantic change requires regenerating and reviewing the new baseline (e.g. via --update-snapshots) before committing it, rather than blindly accepting a diff`,`toHaveScreenshot() configured with a very low maxDiffPixelRatio to catch semantic changes indirectly`,`There is no accessibility-tree equivalent to visual snapshot testing in Playwright`,`Manually copy the accessibility tree into a comment and compare by eye on every PR`],0,`toMatchAriaSnapshot() is designed to bring the same "baseline + deliberate review + update" discipline of visual testing to accessibility structure instead of pixels.`],[`S`,`I`,`User-generated display names can appear in any casing ("JohnDoe", "johndoe", "JOHNDOE") depending on how the account was created, but the test only cares that the greeting shows the right name regardless of case. How should the assertion be written?`,null,[`await expect(page.getByTestId('greeting')).toHaveText('Welcome, johndoe', { ignoreCase: true })`,`await expect(page.getByTestId('greeting')).toHaveText(/Welcome, johndoe/)`,`await expect(page.getByTestId('greeting')).toHaveText('Welcome, JohnDoe')`,`Normalize the actual page content to lowercase using page.evaluate() before asserting`],0,`The ignoreCase option is built exactly for this scenario — content whose casing is a legitimate variable, not a thing under test.`],[`S`,`A`,`A report-generation page can legitimately take anywhere from 2 to 25 seconds depending on data size, and the default 5-second assertion timeout is too short for the "Report ready" heading to appear. What is the most targeted fix?`,null,[`Raise the timeout for just that specific assertion: await expect(heading).toBeVisible({ timeout: 30000 }), leaving the global default untouched for the rest of the suite`,`Raise the global expect.timeout in playwright.config.ts to 30 seconds so every assertion in the suite gets more time`,`Add a fixed page.waitForTimeout(25000) before the assertion to guarantee it has finished`,`Retry the whole test up to 5 times using the retries config option until it happens to pass fast enough`],0,`A per-assertion timeout override targets exactly the one known-slow check without silently loosening every other assertion in the suite (which could hide unrelated regressions).`],[`D`,`I`,"`await expect(locator).toBeInViewport()` fails even though the element is clearly visible on screen in a screenshot from the trace viewer. What is the likely cause?",null,[`Only a small sliver of the element intersects the viewport (below the default minimal-intersection threshold, or below an explicitly-set ratio option) — check whether a stricter ratio like { ratio: 0.5 } was set, or whether most of the element is actually scrolled just out of view`,`toBeInViewport() does not exist and this code throws a different error`,`The element must also pass toBeVisible() first, in a separate assertion, before toBeInViewport() can be used`,`toBeInViewport() only works in headed mode, never headless`],0,`A "clearly visible" screenshot can still fail a ratio-constrained toBeInViewport() check if only part of the element is within the visible scroll area.`],[`D`,`A`,`A custom matcher registered via expect.extend() fails a test, but the HTML report just shows a generic "assertion failed" with no useful detail about WHAT was wrong. What is the fix?`,null,["The matcher’s returned object is missing (or has an unhelpful) `message` function — add a descriptive `message: () => `...`` that explains exactly what was expected vs actually found",`Custom matchers can never produce detailed messages; this is a known Playwright limitation`,`The matcher must be registered again inside a beforeEach hook to enable messages`,`Switch from expect.extend() to a plain thrown Error to get a better message`],0,`The message() callback is the whole point of the { pass, message } return shape — without a good one, custom matchers give strictly worse diagnostics than built-in ones.`],[`D`,`I`,"`await expect(locator).toMatchAriaSnapshot(...)` starts failing after a developer only changed some CSS spacing, with no markup or ARIA attribute changes. What is happening?",null,[`This should NOT happen from pure CSS changes since aria snapshots capture structure/roles/names, not styling — the failure suggests the CSS change also altered generated content, conditionally-rendered elements, or dynamically-computed ARIA attributes tied to layout (e.g. a "visually hidden" utility class toggling an element’s accessible presence)`,`Aria snapshots are actually pixel-based under the hood, so any visual change breaks them`,`toMatchAriaSnapshot() is inherently flaky and unrelated to the actual change`,`The baseline simply needs regenerating with no further investigation, since aria snapshots break on any commit`],0,`When an aria snapshot breaks from an apparently unrelated change, look for CSS that toggles element visibility/presence (like display:none or visually-hidden classes) rather than assuming the tool itself is unreliable.`],[`D`,`A`,`\`await expect(locator).toHaveJSProperty('value', 5)\` fails with a message showing the actual value as the string "5", not the number 5. What is wrong?`,null,[`The expected value passed to toHaveJSProperty() must match the actual JS type exactly — an <input> element’s .value property is always a string in the DOM, so the expected argument should be '5' (a string), not the number 5; use valueAsNumber for the actual numeric property`,`toHaveJSProperty() is broken and always coerces types incorrectly`,`JS properties can never be compared for equality in Playwright`,`The input element needs a type="number" attribute before this assertion can work`],0,`Different DOM properties on the same element can have different native types — .value is always a string, while .valueAsNumber gives the actual parsed number for numeric inputs.`],[`D`,`A`,"A test has both `await expect.soft(a).toBeVisible()` and, later, `throw new Error('unexpected state')` thrown directly from application code inside the test body. The soft failure never shows up in the report. Why?",null,[`An unhandled thrown error aborts the test before Playwright reaches the end-of-test point where accumulated soft-assertion failures are flushed into the report — only failures recorded before the throw AND surviving to test completion are guaranteed to be reported`,`expect.soft() failures are only ever shown if the test ultimately passes`,`The thrown Error automatically clears any previously recorded soft failures`,`Soft assertions require a special reporter plugin to appear at all`],0,`This is the same root cause as several similar footguns: soft assertions rely on the test running to completion to flush their accumulated results — any hard-abort short-circuits that.`]],config:[[`O`,`B`,`What file contains all Playwright test configuration?`,null,[`tests/setup.ts`,`playwright.config.ts`,`jest.config.ts`,`tsconfig.json`],1,`playwright.config.ts at the project root exports a defineConfig() object.`],[`O`,`B`,"What does the `use` block in playwright.config.ts configure?",null,[`The list of test files to include`,`Browser-level defaults applied to every test`,`The reporter format for CI`,`The Node.js version requirement`],1,`use sets defaults like baseURL, headless, viewport, trace, screenshot, and video.`],[`O`,`I`,"What is the difference between `timeout` and `expect.timeout` in the config?",null,[`They are the same setting with two names`,`timeout limits per-test wall-clock time; expect.timeout controls how long assertions auto-retry`,`timeout applies to CI; expect.timeout applies locally`,`expect.timeout is deprecated — use timeout for both`],1,`A test can run for 30 s (timeout) while each assertion retries for up to 5 s (expect.timeout).`],[`O`,`B`,"What does `retries: 2` in the config do?",null,[`Runs every test 3 times regardless of result`,`Re-runs a failed test up to 2 additional times before marking it as failed`,`Retries flaky assertions 2 times within a test`,`Restarts the browser 2 times per test file`],1,`retries gives failed tests a second (and third) chance — essential for reducing flakiness in CI.`],[`O`,`I`,"What does `fullyParallel: true` change compared to the default?",null,[`It runs all projects simultaneously instead of sequentially`,`It runs tests within the same file in parallel, not just across files`,`It disables retries for faster parallel execution`,`It assigns one worker per test file regardless of count`],1,`By default only files run in parallel; fullyParallel: true also parallelises tests within each file.`],[`O`,`B`,"What does setting `baseURL: 'http://localhost:3000'` in `use` enable?",null,[`Playwright automatically navigates to that URL at test start`,`Tests can call page.goto('/login') with relative paths instead of full URLs`,`The webServer starts on port 3000`,`All network requests are intercepted on that host`],1,`With baseURL set, relative URLs in goto() and request methods are resolved against it.`],[`O`,`I`,"What does the `projects` array control?",null,[`The list of test spec files to include`,`Which browsers and device profiles tests run against`,`The order in which test files execute`,`The number of workers per test run`],1,`Each project entry names a browser/device and can override the shared use settings.`],[`O`,`I`,"What does `trace: 'on-first-retry'` do?",null,[`Records a trace for every test run`,`Records a trace only when a test is retried for the first time`,`Replays the last recorded trace on retry`,`Disables tracing except during retries`],1,`on-first-retry keeps traces only for tests that actually fail, minimising disk usage.`],[`O`,`B`,"What does the `webServer` config option do?",null,[`Configures the Playwright Test server for distributed runs`,`Starts a local dev server before tests and tears it down after`,`Sets the browser's network proxy server`,`Defines the base URL for API requests`],1,`webServer = automatic dev server lifecycle management — no separate terminal needed.`],[`O`,`I`,"What does `workers: undefined` mean in Playwright config?",null,[`Tests run with zero workers (serial)`,`Playwright chooses the worker count automatically based on CPU cores`,`All workers are disabled and tests run in-process`,`One worker per browser project`],1,`undefined lets Playwright auto-scale workers — typically half the CPU cores.`],[`O`,`I`,"What does `globalSetup` point to?",null,[`A file that runs before every individual test`,`A file that runs once before the entire test suite`,`A hook that sets global variables in each test file`,`The setup block inside playwright.config.ts`],1,`globalSetup runs once per suite — perfect for expensive operations like DB seeding or shared auth.`],[`O`,`A`,"What does `reuseExistingServer: !process.env.CI` do in the webServer config?",null,[`Skips starting the server entirely in CI`,`Reuses a running server locally so dev startup is skipped, but always starts fresh in CI`,`Forces the server to restart on every test in CI`,`Shares the server process across multiple workers`],1,`!process.env.CI = true locally (reuse), false in CI (always start fresh for a clean environment).`],[`F`,`B`,"Complete: `export default ______Config({ … })` — the wrapper function from @playwright/test",null,[`define`,`create`,`make`,`export`],0,`defineConfig() provides TypeScript types and IDE autocomplete for the config object.`],[`F`,`I`,"Complete: `use: { ______: 'http://localhost:3000' }` to set the shared base URL",null,[`baseURL`,`host`,`origin`,`url`],0,`baseURL enables relative paths in page.goto() and request methods.`],[`F`,`B`,"Complete: `retries: process.env.CI ? 2 : ______` for zero retries locally",null,[`0`,`false`,`null`,`undefined`],0,`0 retries locally means tests fail fast without masking flakiness during development.`],[`F`,`I`,"Complete: `{ name: 'chromium', use: { ...______['Desktop Chrome'] } }`",null,[`devices`,`browsers`,`presets`,`profiles`],0,`devices is an object exported from @playwright/test containing browser+viewport presets.`],[`F`,`I`,"Complete: `trace: '______-first-retry'` to record traces only on retries",null,[`on`,`capture`,`record`,`save`],0,`on-first-retry is the recommended trace strategy — captures only failed tests.`],[`F`,`I`,"Complete: `______Server: { command: 'npm run dev', url: 'http://localhost:3000' }`",null,[`web`,`dev`,`local`,`app`],0,`webServer is the config key for automatic dev server lifecycle management.`],[`F`,`A`,"Complete: `______: true` to run tests within the same file in parallel",null,[`fullyParallel`,`parallel`,`concurrent`,`async`],0,`fullyParallel: true enables intra-file parallelism on top of the default inter-file parallelism.`],[`E`,`B`,`What is wrong?
\`\`\`ts
export default {
  testDir: './tests',
  timeout: 30_000,
};
\`\`\``,null,[`testDir is not a valid config key`,`The config object must be wrapped in defineConfig()`,`timeout should be a string like '30s'`,`The file must be named playwright.config.js not .ts`],1,`Exporting a plain object loses TypeScript type-checking and IDE autocomplete — always use defineConfig().`],[`E`,`I`,"What is wrong?\n```ts\ntimeout: 5_000,  // intended to control assertion retry window\n```",null,[`timeout is not a valid top-level config key`,`timeout controls per-test duration; assertion retry uses expect: { timeout: 5_000 }`,`5_000 uses invalid numeric separators`,`The unit should be seconds not milliseconds`],1,`timeout and expect.timeout are separate settings — mixing them up causes assertions to time out at the wrong interval.`],[`E`,`I`,"What is wrong?\n```ts\nretries: true,\n```",null,[`retries must be a number (0, 1, 2 …), not a boolean`,`retries: true enables infinite retries`,`true is interpreted as 1 retry — it works but is not explicit`,`retries is not a valid config key`],0,`retries expects a number; boolean true coerces to 1 in JS but is semantically wrong and confusing.`],[`E`,`B`,"What is wrong?\n```ts\nuse: { timeout: 30_000 }\n```",null,[`timeout is not a valid key inside the use block`,`The use block only accepts browser flags like headless`,`timeout in use is correct and overrides the global timeout`,`use is only for locator options`],0,`timeout belongs at the top level of defineConfig, not inside use — inside use it is silently ignored.`],[`E`,`I`,`What is wrong?
\`\`\`ts
webServer: {
  command: 'npm run dev',
  url: 'http://localhost:3000',
}
// Tests fail when a dev server is already running
\`\`\``,null,[`url should be port not a full URL`,`command should be an array not a string`,`reuseExistingServer is missing — Playwright tries to start a second server and fails`,`webServer requires a ready timeout`],2,`Without reuseExistingServer: true, Playwright fails if the port is already occupied.`],[`E`,`A`,"What is wrong?\n```ts\nglobalSetup: async () => {\n  // setup code inline\n}\n```",null,[`globalSetup must export a default async function from a separate file, not an inline function`,`Inline functions are slower than file-based setups`,`globalSetup requires TypeScript not JavaScript`,`The async keyword is not supported here`],0,`globalSetup must be a file path string pointing to a module with a default export function.`],[`D`,`B`,`Tests pass locally but assertions time out in CI after exactly 5 s. What should you check?`,null,[`Increase timeout in the use block`,`The CI machine may be slower — increase expect.timeout in the config`,`Switch from fullyParallel to sequential`,`Disable retries in CI`],1,`expect.timeout controls assertion retry duration; CI machines are slower and often need a higher value.`],[`D`,`I`,"`retries: process.env.CI ? 2 : 0` shows 0 retries in CI. What is the likely cause?",null,[`The ternary operator syntax is wrong`,`process.env.CI is not set in the CI environment — set it to "true" in the CI pipeline`,`retries: 0 is the maximum — use retries: 2 on both sides`,`The config file is not being read in CI`],1,`CI environments (GitHub Actions, Jenkins, etc.) do not automatically set CI — add CI: true to the pipeline env.`],[`D`,`I`,"Tests are slow even with `workers: 4`. What does adding `fullyParallel: true` change?",null,[`It doubles the worker count to 8`,`It allows tests within the same file to run in parallel, not just across files`,`It disables the per-test timeout for faster execution`,`It caches browser launches between workers`],1,`Without fullyParallel, tests in a single file run sequentially even with multiple workers.`],[`D`,`B`,`\`page.goto('/login')\` throws "Invalid URL" even though baseURL is set. What is the likely cause?`,null,[`Relative URLs are not supported in Playwright`,`The use block setting is inside a project that overrides baseURL with undefined`,`goto() requires a full URL — use template literals instead`,`baseURL needs a trailing slash`],1,`A project-level use block can accidentally shadow or remove the global baseURL — check project overrides.`],[`D`,`I`,"The webServer starts a new process on every `npx playwright test` run even locally, slowing startup. What option fixes this?",null,[`Set workers: 1 to reduce server load`,`Add reuseExistingServer: true (or !process.env.CI) to reuse a running server`,`Use the --no-server CLI flag`,`Move webServer to globalSetup`],1,`reuseExistingServer: true skips launching if the URL is already responding.`],[`D`,`A`,`globalSetup saves auth state to auth.json but tests still hit the login page. What is missing?`,null,[`globalSetup must be async`,`storageState: 'auth.json' must be set in the use block so tests load it`,`The auth.json file must be committed to the repo`,`globalSetup runs after tests by default`],1,`Saving storageState in globalSetup is only half — the use block must point to it so each test's browser context loads the saved cookies/localStorage.`],[`C`,`B`,"Complete a project entry for Firefox.\n```ts\n{ name: 'firefox', use: { ...devices['______'] } }\n```",null,[`Desktop Firefox`,`firefox-desktop`,`Firefox Latest`,`browser-firefox`],0,`The exact key is 'Desktop Firefox' in the Playwright devices preset map.`],[`C`,`I`,"Complete to set the assertion retry window to 8 s.\n```ts\nexpect: { ______: 8_000 },\n```",null,[`timeout`,`retryTimeout`,`assertTimeout`,`wait`],0,`expect.timeout controls how long web-first assertions keep retrying.`],[`C`,`I`,"Complete to reuse an existing server locally but always start fresh in CI.\n```ts\nreuseExistingServer: ______,\n```",null,[`!process.env.CI`,`process.env.CI`,`true`,`false`],0,`!process.env.CI = true locally (reuse), false in CI (start fresh).`],[`C`,`I`,"Complete to capture screenshots only on test failure.\n```ts\nuse: { screenshot: '______' }\n```",null,[`only-on-failure`,`on-fail`,`failure`,`always`],0,`The valid values are: off · on · only-on-failure.`],[`C`,`A`,"Complete the reporter array for HTML + GitHub Actions output.\n```ts\nreporter: [['______'], ['github']],\n```",null,[`html`,`report`,`output`,`junit`],0,`html generates the interactive Playwright report; github annotates PR checks.`],[`L`,`B`,"Why is `retries: process.env.CI ? 2 : 0` the recommended pattern?",null,[`CI machines are faster so they need more retries`,`CI detects flakiness correctly; retries locally hide bugs during development`,`0 retries is invalid outside CI`,`CI runs single-threaded and needs retries to compensate`],1,`Retries in CI catch network/timing flakiness; zero retries locally force you to fix flaky tests immediately.`],[`L`,`I`,"What is the difference between `workers` and `fullyParallel`?",null,[`They are synonyms — setting one sets the other`,`workers controls how many parallel processes run; fullyParallel controls whether tests within a file run in parallel`,`fullyParallel sets workers to the maximum CPU count`,`workers only applies to CI; fullyParallel applies locally`],1,`workers = concurrency level; fullyParallel = whether intra-file parallelism is enabled.`],[`L`,`I`,"Why should `globalSetup` handle authentication instead of `beforeEach`?",null,[`beforeEach does not support async operations`,`globalSetup runs once for the suite — saves a browser launch and login per test; beforeEach repeats the full login on every test`,`globalSetup has access to test fixtures; beforeEach does not`,`Playwright requires authentication in globalSetup`],1,`Login once → save storageState → every test loads cookies instantly. beforeEach would repeat the full login flow N times.`],[`L`,`B`,"What is the purpose of spreading `...devices['Desktop Chrome']` into a project?",null,[`It imports the Chromium browser binary`,`It applies Playwright's built-in viewport, userAgent, and browser settings for that device`,`It clones the test files for Chrome-specific runs`,`It sets the network conditions for Chrome emulation`],1,`devices presets bundle viewport size, user agent string, and device pixel ratio into one spread.`],[`L`,`A`,"A test overrides `timeout` using `test.setTimeout(60_000)`. What takes precedence?",null,[`The config always wins — test.setTimeout is ignored`,`test.setTimeout overrides the global config timeout for that specific test`,`They are added together: 30_000 + 60_000 = 90_000 ms`,`test.setTimeout only works inside beforeEach`],1,`test.setTimeout() overrides the config timeout for that individual test — config is the default, not the ceiling.`],[`R`,`B`,"You want all tests to use `http://localhost:4000` without writing it in every test. What do you configure?",null,[`Set url: 'http://localhost:4000' in each test file`,`Set baseURL: 'http://localhost:4000' in the use block`,`Set origin in the projects array`,`Use a .env file — Playwright reads it automatically`],1,`baseURL in use is the single source of truth for the host; all tests can then use relative paths.`],[`R`,`I`,`You need tests to run in Chrome, Firefox, and Safari. How do you configure this?`,null,[`Pass --browser=all to the CLI`,`Add three entries to the projects array, each spreading the appropriate devices preset`,`Set browsers: ['chromium', 'firefox', 'webkit'] in use`,`Run npx playwright test three times with different --browser flags`],1,`projects is the standard multi-browser setup — one entry per engine.`],[`R`,`I`,`You want traces captured for every failed test in CI. Which trace option do you use?`,null,[`trace: 'always'`,`trace: 'on-failure'`,`trace: 'retain-on-failure'`,`trace: 'on-first-retry'`],2,`retain-on-failure keeps traces for tests that fail regardless of whether they were retried.`],[`R`,`I`,`You want the dev server to start automatically before tests. What config block do you add?`,null,[`server: { command: 'npm run dev' }`,`preTest: { start: 'npm run dev' }`,`webServer: { command: 'npm run dev', url: 'http://localhost:3000' }`,`globalSetup: './start-server.ts'`],2,`webServer with command + url is the built-in server lifecycle mechanism.`],[`R`,`A`,`You need to log in once and share the session across all 200 tests. What is the architecture?`,null,[`Use beforeAll in every test file to log in`,`Create a globalSetup that logs in and saves storageState, then set use: { storageState } in the config`,`Use a shared page fixture with session persistence`,`Set cookies manually in each test via page.context().addCookies()`],1,`globalSetup + storageState is the canonical Playwright pattern for shared authentication.`],[`S`,`I`,`A test suite has 500 tests and runs in 8 minutes locally but 40 minutes in CI. What config changes would help?`,null,[`Increase workers and enable fullyParallel — CI may have defaulted to workers: 1`,`Reduce retries to 0 in CI — retries are causing repetition`,`Set fullyParallel: false — parallel tests compete for resources in CI`,`Remove the webServer config — it adds startup time`],0,`CI environments often limit workers — explicitly setting workers: N and fullyParallel: true can dramatically cut runtime.`],[`S`,`I`,`You add a new device project for iPhone 13. Tests that worked in Chromium fail on WebKit. How do you isolate which project fails?`,null,[`Run with --browser=webkit`,`Run with --project='mobile-safari' to limit execution to that project only`,`Set workers: 1 and check logs`,`Remove other projects temporarily`],1,`--project='mobile-safari' runs only that project's tests — useful for isolating cross-browser failures.`],[`S`,`A`,`The CI pipeline runs E2E tests but the test report shows no traces even though trace: 'on-first-retry' is set. What went wrong?`,null,[`Traces are only available in headed mode`,`The CI artifact upload step is missing — traces are generated but not persisted`,`on-first-retry requires retries: 1 or higher — with retries: 0 no retry occurs and no trace is captured`,`trace files are deleted after each test in CI`],2,`on-first-retry only captures a trace when a retry happens — with retries: 0 tests fail immediately and no trace is saved.`],[`S`,`A`,`You want different timeouts for smoke tests vs full regression tests in the same project. How do you achieve this?`,null,[`Create two playwright.config.ts files and switch between them with --config`,`Use test.setTimeout() in individual tests or test.describe blocks to override the global timeout`,`Set two timeout values in the config separated by commas`,`Timeouts cannot vary within a project — use separate projects`],1,`test.setTimeout() per test or test.describe overrides the global timeout — no need for separate configs.`],[`O`,`I`,'What does `testIdAttribute: "data-cy"` in the config do?',null,[`Makes Cypress compatible with Playwright`,`Changes what attribute getByTestId() looks for — instead of data-testid it now matches elements with data-cy`,`Adds a data-cy attribute to all elements`,`Generates test IDs automatically`],1,`testIdAttribute lets teams use their preferred test ID attribute without changing every getByTestId() call.`],[`O`,`B`,'What does the `reporter: "html"` option generate?',null,[`An HTML file inline in the terminal`,`A detailed interactive HTML report in playwright-report/ with test results, screenshots, traces, and videos`,`A coverage report in HTML format`,`A report sent to an HTML email`],1,`The HTML reporter generates an interactive web app — open it with npx playwright show-report.`],[`O`,`I`,'What does `trace: "on-first-retry"` capture?',null,[`A trace for every test`,`A trace only when a test fails and is being retried for the first time — helps diagnose intermittent failures`,`A trace for the first test in each file`,`A trace whenever a network request is made`],1,`on-first-retry is the recommended default — it saves the trace only when you actually need it (a retry = something went wrong).`],[`O`,`B`,'What does `screenshot: "only-on-failure"` do?',null,[`Takes screenshots every second during the test`,`Takes an automatic screenshot only when a test fails — stored in test-results/`,`Disables screenshots entirely`,`Takes a screenshot before and after each test`],1,`only-on-failure is a practical default — captures visual evidence of failures without generating screenshots for passing tests.`],[`O`,`I`,"What does the `webServer.reuseExistingServer` option do?",null,[`Reuses the browser from the previous test run`,`If the server is already running on the configured port, Playwright uses it instead of starting a new one — prevents "port in use" errors during development`,`Saves the server state between test runs`,`Shares one server across multiple playwright.config.ts files`],1,`reuseExistingServer: true is the developer ergonomics option — your already-running dev server is reused without restarting.`],[`O`,`A`,"What does the `globalTimeout` config option set?",null,[`The timeout for each individual test`,`A maximum wall-clock time for the entire test suite — if the suite exceeds this, it is aborted`,`The timeout for globalSetup and globalTeardown`,`The timeout for browser launch`],1,`globalTimeout is a CI safeguard — prevent runaway suites from consuming infinite CI minutes by setting a hard cap.`],[`O`,`I`,'What does `use: { locale: "de-DE" }` do?',null,[`Translates test output to German`,`Sets the browser's locale — affects navigator.language, Intl formatting, and Accept-Language headers sent to the server`,`Translates error messages to German`,`Changes the keyboard layout`],1,`locale emulation lets you test date/number formatting and locale-specific UI without changing the OS setting.`],[`O`,`I`,"What does `use: { actionTimeout: 5000 }` configure?",null,[`The timeout for page.goto() navigations`,`The default timeout for individual Playwright actions like click(), fill(), and press() — overrides the implicit action timeout`,`The timeout for each test step`,`The maximum time for network requests`],1,`actionTimeout is separate from the test timeout — it limits how long each single action can wait for preconditions.`],[`O`,`B`,'What does `use: { video: "on" }` do?',null,[`Records only failing tests`,`Records a video of every test run — videos are saved in test-results/ regardless of pass or fail`,`Enables live video streaming of tests`,`Records only network traffic`],1,`video: "on" records every test — useful for debugging but generates large storage. retain-on-failure is more storage-efficient.`],[`O`,`A`,"What does the `expect.timeout` config option set?",null,[`The timeout for the entire expect() chain`,`The default timeout for web-first assertion retries — how long assertions like toBeVisible() wait before failing`,`The timeout for expect.poll()`,`The time between assertion retries`],1,`expect.timeout is independent of the test timeout — an assertion can retry for 5 s while the overall test has 30 s.`],[`F`,`I`,'Complete to set the custom test ID attribute.\n```ts\nexport default defineConfig({\n  ______: "data-cy",\n});\n```',null,[`testIdAttribute`,`testId`,`selectorAttribute`,`dataAttribute`],0,`testIdAttribute changes what attribute getByTestId() matches — set it at the top level, not in use:.`],[`F`,`B`,'Complete to capture traces on failure.\n```ts\nuse: {\n  trace: "______",\n}\n```',null,[`retain-on-failure`,`on-failure`,`on`,`capture`],0,`retain-on-failure keeps traces only for tests that fail — a storage-efficient production default.`],[`F`,`I`,'Complete to configure the HTML reporter.\n```ts\nreporter: [["______", { open: "never" }]],\n```',null,[`html`,`htm`,`report`,`playwright-report`],0,`The "html" reporter generates an interactive web report — { open: "never" } prevents auto-opening in CI.`],[`F`,`I`,`Complete to start a dev server before tests.
\`\`\`ts
export default defineConfig({
  ______: {
    command: "npm run dev",
    url: "http://localhost:3000",
  },
});
\`\`\``,null,[`webServer`,`devServer`,`startServer`,`server`],0,`webServer is the built-in mechanism for starting and stopping the app server around test runs.`],[`F`,`B`,'Complete to set the per-test artifact output directory.\n```ts\nexport default defineConfig({\n  ______: "artifacts",\n});\n```',null,[`outputDir`,`testDir`,`reportDir`,`artifactDir`],0,`outputDir sets where per-test artifacts (screenshots, videos, traces) are saved.`],[`F`,`A`,"Complete to set a global suite timeout.\n```ts\nexport default defineConfig({\n  ______: 600_000, // 10 minutes max\n});\n```",null,[`globalTimeout`,`suiteTimeout`,`maxTime`,`totalTimeout`],0,`globalTimeout caps the entire suite wall-clock time — a CI safety net.`],[`E`,`B`,"What is wrong?\n```ts\nexport default defineConfig({\n  retries: -1,\n});\n```",null,[`Negative retries disable the feature`,`retries must be 0 or a positive integer — a negative value is invalid and Playwright will throw a configuration error`,`retries: -1 means unlimited retries`,`retries: -1 disables retries in CI`],1,`retries must be 0 or a positive integer — use 0 to disable retries.`],[`E`,`I`,`What is wrong?
\`\`\`ts
export default defineConfig({
  use: {
    storageState: "./auth.json",
  },
  // No auth setup project defined
});
\`\`\``,null,[`storageState cannot be in the global use block`,`If auth.json does not exist before tests run, all tests fail with a file-not-found error. Define a setup project that generates auth.json and add it as a dependency.`,`storageState must be in each project separately`,`auth.json must be in the root directory`],0,`storageState in the global use block is fine, but the file must exist. Use a setup project with dependencies: ["setup"] to generate it first.`],[`E`,`B`,`What is wrong?
\`\`\`ts
export default defineConfig({
  testDir: "tests",
  testDir: "e2e", // duplicate key
});
\`\`\``,null,[`testDir can only be set in projects`,`TypeScript/JavaScript objects cannot have duplicate keys — the second testDir silently overrides the first. Only one of them is used (the last one in most JS engines).`,`testDir cannot be set at the top level`,`e2e directory must be the first testDir`],1,`Duplicate object keys are a JS footgun — the last value wins silently. TypeScript strict mode or linting catches this.`],[`E`,`I`,`What is wrong?
\`\`\`ts
// playwright.config.ts
export default defineConfig({
  use: { headless: "false" }, // string
});
\`\`\``,null,[`headless defaults to false anyway`,`headless must be a boolean, not a string — "false" is truthy in JavaScript so the browser runs in headless mode despite the developer's intent. Use headless: false (boolean).`,`headless must be in a project, not use`,`Strings are coerced to booleans`],1,`String "false" is truthy — always use boolean false for headless: false.`],[`E`,`A`,'What is wrong with this reporter config?\n```ts\nreporter: "html, json",\n```',null,[`html and json cannot be combined`,`reporter must be an array of arrays, not a comma-separated string: reporter: [["html"], ["json"]]`,`reporter cannot accept json in the same run`,`The comma must be replaced with a pipe character`],1,`reporter with multiple outputs requires the array-of-arrays format: [["html"], ["json", { outputFile: "..." }]].`],[`E`,`I`,`What is wrong?
\`\`\`ts
projects: [
  { name: "chromium", use: { browserName: "chromium" } },
  { name: "chromium", use: { browserName: "firefox" } }, // same name
],
\`\`\``,null,[`Duplicate project names are not supported`,`Duplicate project names make it impossible to filter with --project="chromium" — Playwright runs both. Use unique names like "firefox" for the Firefox project.`,`firefox must use browserName: "webkit"`,`Projects with the same name merge their settings`],1,`Project names must be unique — --project filtering, report display, and dependency declarations all use these names.`],[`D`,`B`,'Tests fail with "Error: browserType.launch: Chromium is not installed". The config has `projects: [{ use: { browserName: "chromium" } }]`. What is the fix?',null,[`Add chromium: true to the use block`,`Run npx playwright install chromium to download the browser binary — the config declares what to use but does not download it`,`Change browserName to "chrome"`,`Install Google Chrome separately`],1,`Playwright's browser binaries require explicit installation — npx playwright install downloads the declared browsers.`],[`D`,`I`,`A test project succeeds but the HTML report is not generated. What should you check?`,null,[`The test must fail for a report to be generated`,`Verify reporter: [["html"]] (or "html") is in the config. Also check that playwright-report/ is not in .gitignore causing confusion — the report is generated but you may be looking in the wrong directory.`,`HTML reports require headed mode`,`Reports are only generated in CI`],1,`Missing reporter config is the primary cause — add html to the reporter array. Run npx playwright show-report to verify location.`],[`D`,`B`,'`webServer` starts but tests fail with "net::ERR_CONNECTION_REFUSED". What is the issue?',null,[`webServer is not supported in this version`,`The server may not be ready by the time tests start — increase webServer.timeout or add a more specific readyPattern that waits for a "Server started" log message`,`Add retry: true to webServer`,`The port must match the URL exactly`],1,`webServer.timeout defaults to 60s but sometimes servers need more — increase it or use readyPattern to wait for a specific log message indicating readiness.`],[`D`,`A`,`In CI, traces are empty (0 bytes) despite trace: "on-first-retry" being set. What are two likely causes?`,null,[`Traces are not supported in CI`,`1) retries: 0 — with no retries, on-first-retry never fires. Set retries: 1 or higher. 2) The artifact upload in CI is not configured — traces are generated but not persisted between jobs.`,`Traces require headed mode`,`trace: "on-first-retry" requires trace: "on" as a fallback`],1,`Both retries and CI artifact persistence must be configured — retries create the traces, artifact upload saves them.`],[`D`,`I`,`Two projects in the config share the same storageState but the auth session in one project is corrupted by tests from another. Why?`,null,[`Projects cannot share storageState files`,`Projects share the state FILE but each creates its own browser context copy — the file is read-only at runtime. If one project writes new storageState during tests, it can overwrite the file used by other projects. Use separate state files per project.`,`storageState is automatically isolated`,`Context isolation prevents this`],1,`Tests that update storageState during a run can corrupt the shared file — always write to separate per-project state files.`],[`C`,`B`,'Complete to use half the CPU cores.\n```ts\nexport default defineConfig({\n  ______: "50%",\n});\n```',null,[`workers`,`parallel`,`cores`,`threads`],0,`workers: "50%" (with quotes) uses half the detected CPU cores — a percentage string.`],[`C`,`I`,"Complete to set a 10-second action timeout.\n```ts\nuse: {\n  ______: 10_000,\n}\n```",null,[`actionTimeout`,`timeout`,`clickTimeout`,`waitTimeout`],0,`actionTimeout sets the default per-action timeout (click, fill, etc.) independently of the test timeout.`],[`C`,`B`,'Complete to configure locale emulation.\n```ts\nuse: {\n  ______: "fr-FR",\n}\n```',null,[`locale`,`language`,`lang`,`region`],0,`locale sets the browser's language/region — affects Intl formatting and Accept-Language headers.`],[`C`,`I`,`Complete to add a JUnit reporter alongside HTML.
\`\`\`ts
reporter: [
  ["html"],
  ["junit", { outputFile: "______" }],
],
\`\`\``,null,[`results.xml`,`junit.html`,`report.json`,`output.xml`],0,`JUnit reporter outputs XML — the filename convention is typically results.xml for CI integration.`],[`C`,`A`,"Complete to set a timeout only for the expect() retry loop.\n```ts\nexport default defineConfig({\n  expect: { ______: 8_000 },\n});\n```",null,[`timeout`,`retry`,`interval`,`maxWait`],0,`expect.timeout controls how long web-first assertions retry — independent of the per-test timeout.`],[`L`,`B`,"Why is `defineConfig()` used instead of directly exporting a plain object?",null,[`It is required for TypeScript compilation`,`defineConfig provides TypeScript IntelliSense, type checking for all config options, and validates the object structure at author time — without it you lose type safety`,`defineConfig is required by the test runner at runtime`,`It sets default values that the plain object does not have`],1,`defineConfig is a type helper — wrap your config in it to get full autocomplete and catch typos in option names.`],[`L`,`I`,"If both the global `use` block and a project's `use` block set `viewport`, which wins?",null,[`Global use always wins`,`Project-level use merges with and overrides global use — the project's settings take precedence. Test-level test.use() further overrides project settings.`,`The first one defined wins`,`They are merged by averaging the values`],1,`Specificity hierarchy: test.use() > project use > global use — innermost setting wins.`],[`L`,`B`,"What is the purpose of `projects` vs just `use` in playwright.config.ts?",null,[`Projects are for CI only`,`projects enables running the same tests against multiple configurations (different browsers, viewports, environments) in one run — use alone applies one configuration to all tests`,`projects replaces use completely`,`use is deprecated in favour of projects`],1,`projects multiplies test runs — each project gets its own configuration and runs all tests. use sets the defaults for every project.`],[`L`,`A`,"When does `globalSetup` run relative to `webServer`?",null,[`globalSetup runs before webServer starts`,`webServer starts first (Playwright waits for it to be ready), then globalSetup runs. This ensures the server is available when globalSetup needs to make requests.`,`They run in parallel`,`globalSetup and webServer are mutually exclusive`],1,`webServer is started and verified before globalSetup runs — globalSetup can safely make requests to the server.`],[`L`,`I`,"Can you have multiple `webServer` entries in playwright.config.ts?",null,[`No — only one webServer is allowed`,`Yes — webServer accepts an array: webServer: [{ command: "npm run api", url: "..." }, { command: "npm run web", url: "..." }] to start multiple servers`,`Yes — use a single shell command that starts all servers`,`Only if they use different ports`],1,`Multiple webServer entries let you start an API server and a frontend server simultaneously — Playwright waits for all of them.`],[`R`,`B`,`You run tests against three environments (dev, staging, prod) using different base URLs. What is the cleanest config approach?`,null,[`Maintain three playwright.config.ts files`,`Read BASE_URL from an environment variable: use: { baseURL: process.env.BASE_URL || "http://localhost:3000" } — pass the URL at run time with BASE_URL=https://staging.example.com npx playwright test`,`Hard-code all three URLs and use a flag`,`Use three projects each with a different baseURL`],1,`Environment variables are the standard way to parameterise base URLs — one config, multiple environments.`],[`R`,`I`,`You want to run a specific group of tests tagged @smoke using a CLI command without modifying the config. How?`,null,[`Add a smoke project to the config`,`npx playwright test --grep "@smoke" — the grep flag filters test titles without changing playwright.config.ts`,`Add testMatch: ["**/@smoke.spec.ts"] to config`,`Create a separate playwright.smoke.config.ts`],1,`--grep is the CLI filter for tags — no config change needed, making it ideal for CI workflow YAML customisation.`],[`R`,`A`,"You need different `storageState` files per environment (dev uses dev-auth.json, staging uses staging-auth.json). How?",null,[`Create separate auth setup projects per environment`,`Use: storageState: process.env.ENV === "staging" ? "staging-auth.json" : "dev-auth.json" — environment variables drive config values dynamically`,`Hardcode both paths and use if/else in tests`,`Use separate playwright.config files per environment`],1,`Dynamic config values via environment variables are the clean solution — one config, multiple runtime configurations.`],[`R`,`I`,`You want trace viewer files to be stored in a custom location. How?`,null,[`trace: { path: "custom-traces/" }`,`Set outputDir: "custom-traces/" in the config — all artifacts including traces go to this directory. Or use a reporter plugin for custom trace storage.`,`Traces always go to test-results/ and cannot be moved`,`Set PLAYWRIGHT_TRACE_DIR environment variable`],1,`outputDir controls all artifact storage — change it once to redirect all screenshots, videos, and traces.`],[`R`,`B`,`You need to run tests against a self-signed certificate dev server. What config option is required?`,null,[`trustAllCerts: true`,`use: { ignoreHTTPSErrors: true } — this makes every browser context ignore SSL errors`,`Add the cert to the system trust store`,`Set HTTPS_ALLOW_ALL=1 environment variable`],1,`ignoreHTTPSErrors: true in the use block applies to all contexts — the browser proceeds past self-signed cert warnings.`],[`S`,`B`,`After adding a new browser project, the CI run time doubled. How do you address this?`,null,[`Remove the new project from CI`,`Add fullyParallel: true and increase workers, or use sharding across multiple CI machines to distribute the extra project's tests — more browsers is more work, requiring more parallelism`,`Set the new project to run with workers: 0`,`Use retries: 0 to skip the new project's failures`],1,`More projects = more total tests = need more parallelism. fullyParallel + more workers (or sharding) offsets the extra browser coverage cost.`],[`S`,`I`,"`webServer` starts successfully but tests cannot reach the server — `ERR_CONNECTION_REFUSED`. What two things should you check?",null,[`Restart the CI machine`,`1) The server may bind to 127.0.0.1 (localhost) but the URL uses 0.0.0.0 — verify the host matches exactly. 2) A firewall or Docker network may block the port — test the URL from the same environment with curl.`,`Add retry: true to webServer`,`Use a different port`],0,`Host binding and network firewalling are the two most common webServer connection issues in CI/Docker environments.`],[`S`,`A`,`After upgrading Playwright, the config is rejected with "Unknown option: extraHTTPHeaders". What should you do?`,null,[`Roll back Playwright to the previous version`,`Run npx playwright --help or check the changelog — extraHTTPHeaders may have moved to a different option name or been removed. Read the migration guide for breaking changes.`,`Set NODE_ENV=legacy to restore old config parsing`,`Add @ts-ignore above the option`],1,`Breaking config changes happen between minor versions — always read the changelog when upgrading and migrate as documented.`],[`S`,`I`,`You want to debug a specific test without changing the source code or config permanently. What is the fastest approach?`,null,[`Add console.log statements throughout`,`Run PWDEBUG=1 npx playwright test tests/login.spec.ts --headed — this opens the Inspector for that test only with no config changes, and removes itself when you close the terminal`,`Set debug: true in playwright.config.ts`,`Use the --verbose flag`],1,`PWDEBUG=1 is the zero-config debug toggle — no source changes, removed automatically, works with any test.`],[`O`,`I`,"What does `testIdAttribute` change here?",`export default defineConfig({
  use: { testIdAttribute: 'data-qa' },
});`,['`page.getByTestId(\'submit\')` will now look for elements with `data-qa="submit"` instead of the Playwright default `data-testid="submit"` — letting teams use their own existing attribute convention',`It renames every element's data-testid attribute in the DOM to data-qa automatically`,`It disables getByTestId() entirely, forcing use of CSS selectors instead`,`testIdAttribute only affects the HTML report, not actual locator matching`],0,`This is a config-level remapping — teams with an existing data-qa (or similar) convention don't need to change their app's markup to use getByTestId().`],[`L`,`A`,"What are `globalSetup` and `globalTeardown` used for, and how do they differ from `webServer`?",null,[`globalSetup/globalTeardown run arbitrary custom Node.js code once before/after the ENTIRE run (e.g., seeding a database, generating an auth token file) — webServer specifically manages starting/stopping an application server process, a more specialized, common sub-case of the same general need`,`They are exact synonyms for webServer with different names`,`globalSetup can only start a web server, nothing else`,`globalTeardown runs before every single test file, not just once for the whole run`],0,`webServer solves ONE common global-setup need (starting the app); globalSetup/globalTeardown are the general-purpose escape hatch for any other run-wide preparation or cleanup.`],[`O`,`I`,"What does this `expect` config block change?",`export default defineConfig({
  expect: { timeout: 10000 },
});`,[`Every web-first assertion (like toBeVisible(), toHaveText()) will now retry for up to 10 seconds by default before failing, instead of the built-in default (5s) — a project-wide adjustment for a generally slower app or environment`,`It sets the timeout for the entire test file to 10 seconds`,`It disables retries for assertions entirely, failing immediately after 10 seconds without polling`,`This setting only affects visual (toHaveScreenshot) assertions, not text/state ones`],0,`This is the project-wide default for assertion retry duration — distinct from the overall test timeout, and can still be overridden per-assertion when needed.`],[`O`,`B`,"What does `outputDir` control?",`export default defineConfig({
  outputDir: 'test-results/',
});`,[`The directory where run artifacts (screenshots, videos, traces from failures) are written — Playwright typically clears and repopulates it fresh on each run`,`The directory Playwright scans to discover test files`,`Where the compiled TypeScript output for the whole project is written`,`The location of the HTML report only, unrelated to other artifacts`],0,`outputDir is specifically for run artifacts, distinct from testDir (where your test files live) or the HTML reporter's own output folder.`],[`S`,`I`,"Your team wants some tests tagged `@smoke` to be filterable and run separately from the full suite, without maintaining two separate config files. What's the idiomatic Playwright way to achieve this?",null,["Add the tag to test titles/annotations (e.g., `test('login @smoke', ...)` or `test('login', { tag: '@smoke' }, ...)`) and run a filtered subset with `npx playwright test --grep @smoke`",`Maintain a fully duplicated copy of every smoke test in a separate folder`,`Tags are not supported by Playwright; only whole test FILES can be selected`,`Rename every smoke-relevant file to start with "smoke-" and use file globbing only`],0,`Tagging plus --grep filtering is the built-in mechanism for slicing a single suite into different runnable subsets without duplicating test code.`],[`D`,`I`,`After adding a new project to the config for tablet testing, a CI job that previously took 10 minutes for the whole suite now silently takes 3x as long, and nobody remembers changing anything else. What is the most likely explanation?`,null,["Every additional `projects` entry multiplies the ENTIRE test suite by running it again under that project's configuration — adding a tablet project effectively adds a whole extra run of every existing test, tripling total execution if there were 2 projects before and 3 tests-equivalent load after adding a third",`Adding a project has no effect on runtime; the slowdown must be caused by something unrelated`,`projects only affects reporting, never actual test execution count`,`Tablet emulation is inherently 3x slower per test than desktop, regardless of project count`],0,`It's easy to forget that every project in the array re-runs your entire suite — adding a third project alongside two existing ones is effectively a 50% increase in total test executions, not a small addition.`],[`O`,`B`,"What does the `metadata` config option provide?",`export default defineConfig({
  metadata: { environment: 'staging', release: '2.4.0' },
});`,[`Arbitrary custom key-value data attached to the whole test run, surfaced in the HTML report — useful for recording context like which environment or release version a particular run targeted`,`It sets SEO metadata tags for pages the tests visit`,`metadata values are passed as HTTP headers on every request the tests make`,`It configures which test files are treated as metadata-only and excluded from execution`],0,`Run-level metadata is purely informational context attached to a report — handy for distinguishing "which environment/version did this run actually test" after the fact.`],[`O`,`A`,`What does this config option do when running in CI?`,`export default defineConfig({
  shard: { current: 2, total: 4 },
});`,[`It tells this run to execute only the 2nd of 4 evenly-split slices of the total test suite — used to spread one suite across multiple CI machines running in parallel, each with a different current value`,`It limits the run to only 2 out of 4 available workers`,`It repeats every test exactly 2 out of 4 times for flake detection`,`shard controls how many browser projects run simultaneously`],0,`Sharding is orthogonal to workers — workers parallelize within one machine/process, sharding splits the whole suite across separate machines or CI jobs.`],[`O`,`A`,`What does the dependencies field cause here?`,`projects: [
  { name: 'setup', testMatch: /.*\\.setup\\.ts/ },
  { name: 'chromium', dependencies: ['setup'], use: { ...devices['Desktop Chrome'] } },
]`,[`Playwright always runs the "setup" project’s tests to completion first, and only starts the "chromium" project’s tests once setup succeeds — the standard pattern for running an authentication or data-seeding step exactly once before the real suite`,`dependencies installs npm packages listed in the array before the project runs`,`It runs both projects simultaneously but labels their reports together`,`dependencies has no runtime effect and is purely documentation`],0,`Project dependencies let you sequence expensive one-time setup (like logging in and saving storageState) ahead of the projects that actually need it, without manually orchestrating scripts.`],[`O`,`A`,`What is the purpose of the teardown field paired with a setup project?`,`projects: [
  { name: 'setup', testMatch: /global\\.setup\\.ts/, teardown: 'cleanup' },
  { name: 'cleanup', testMatch: /global\\.teardown\\.ts/ },
  { name: 'chromium', dependencies: ['setup'], use: {} },
]`,[`It declares that the "cleanup" project should run once, after every project depending on "setup" has finished — giving you a guaranteed place to tear down whatever the setup project created (e.g. a seeded database record or a test account)`,`teardown deletes the setup project’s test files after the run completes`,`It reverses the setup project’s test execution order`,`teardown only runs if the setup project itself fails`],0,`The setup/teardown project pairing is the built-in mechanism for "run once before everything, clean up once after everything" without needing a separate globalTeardown script.`],[`O`,`I`,`What does forbidOnly accomplish in this config?`,`export default defineConfig({
  forbidOnly: !!process.env.CI,
});`,["In CI, if any test or describe block is accidentally left marked with `.only()`, the whole run fails immediately with an error — preventing a developer’s debugging leftover from silently skipping the rest of the suite in a real CI run",`It disables the .only() modifier everywhere, including locally`,`forbidOnly only affects visual regression tests`,`It forces every test to run in isolation, as if each had .only() applied`],0,"forbidOnly is a safety net: `.only()` is meant for local debugging, and this setting turns forgetting to remove it into a loud CI failure instead of a silent partial test run."],[`O`,`I`,`What does maxFailures do here?`,`export default defineConfig({
  maxFailures: 10,
});`,[`The test run stops early (aborting remaining tests) as soon as 10 tests have failed, instead of always running the entire suite to completion — useful for getting fast feedback on a badly broken build without waiting for hundreds more failures`,`It limits each individual test to at most 10 retry attempts`,`maxFailures caps the number of workers to 10`,`It fails the build if fewer than 10 tests ran`],0,`maxFailures is a "stop digging" safety valve — once a run is clearly broken, there is little value in burning CI minutes running the rest of the suite.`],[`O`,`I`,`What do these use-block options configure for every test context?`,`use: {
  permissions: ['geolocation'],
  geolocation: { latitude: 51.5, longitude: -0.12 },
}`,[`Every browser context is pre-granted the geolocation permission AND given a fixed mock coordinate, so the app under test can query navigator.geolocation without a real permission prompt or real GPS data`,`permissions restricts which Playwright APIs the test itself is allowed to call`,`geolocation only affects the network proxy’s reported IP address, not the browser API`,`These options only take effect when launching in headed mode`],0,`Combining permissions and geolocation in use lets you deterministically test location-aware features without any manual permission-dialog handling.`],[`O`,`I`,`What does setting the channel here change?`,`use: {
  channel: 'chrome',
}`,[`Tests launch the actual installed Google Chrome browser (not Playwright’s bundled Chromium build) — useful for testing against exactly what real users run, including Chrome-specific branding/features/policies`,`It switches the reporter output channel to a Chrome-specific format`,`channel controls which network proxy channel requests are routed through`,`It has no effect unless combined with headless: false`],0,`channel: "chrome" (or "msedge") targets a real, separately-installed browser binary rather than the Chromium build that ships with Playwright — closer to real-world user environments, but requires that browser to be installed on the machine.`],[`O`,`A`,`What debugging capability does this add?`,`use: {
  launchOptions: { slowMo: 250 },
}`,[`Every Playwright-driven action (click, fill, etc.) is artificially delayed by 250ms, making the browser’s behavior easy to follow visually — helpful for live debugging, but generally removed before committing since it slows every run`,`It sets a 250ms timeout for all actions, failing them if they take longer`,`slowMo throttles the network to simulate a slow connection`,`It delays test start by 250ms to let the browser warm up`],0,`slowMo is purely a human-observability tool for headed/debug runs — it is not a reliability or timing fix and should not be left on for CI.`],[`O`,`A`,`What does preserveOutput control?`,`export default defineConfig({
  preserveOutput: 'failures-only',
});`,[`Artifacts (screenshots, videos, traces) in the outputDir are kept on disk only for tests that failed — passing tests’ artifacts are cleaned up, saving disk space across large runs`,`It preserves the previous run’s HTML report instead of overwriting it`,`preserveOutput controls whether console.log output from the test appears in the terminal`,`It determines whether retries reuse the previous attempt’s browser context`],0,`"failures-only" is the common production setting — you rarely need the video/trace of a test that passed, but you always want it for one that failed.`],[`O`,`A`,`What does this snapshotPathTemplate customize?`,`export default defineConfig({
  snapshotPathTemplate: '{testDir}/__snapshots__/{testFilePath}/{arg}{ext}',
});`,[`It changes where and how Playwright names/organizes baseline snapshot files (for toHaveScreenshot/toMatchSnapshot) on disk, instead of using the default nested folder structure — useful for aligning snapshot storage with a team’s existing repo conventions`,`It sets the URL path the app is tested against`,`snapshotPathTemplate defines the output format (PNG vs JPEG) of screenshots`,`It has no effect unless updateSnapshots is also set to 'all'`],0,`Customizing the snapshot path template is useful when a team wants baselines co-located differently than Playwright’s default per-project/per-browser nested folders.`],[`O`,`A`,`What does this nested expect config change for every toHaveScreenshot() call in the suite?`,`export default defineConfig({
  expect: {
    toHaveScreenshot: { maxDiffPixels: 100 },
  },
});`,[`It sets a project-wide default tolerance of up to 100 differing pixels for every visual screenshot assertion, without needing to repeat { maxDiffPixels: 100 } as an inline option on every individual toHaveScreenshot() call`,`It limits every test to a maximum of 100 screenshots total`,`maxDiffPixels only applies to the first screenshot taken in each test file`,`This config key is invalid; maxDiffPixels can only be set inline per-assertion`],0,`Nesting screenshot-specific defaults under expect.toHaveScreenshot lets a team set a sensible tolerance once, while individual assertions can still override it when needed.`],[`O`,`I`,`What is the difference between these two timeout settings?`,`use: {
  actionTimeout: 10000,
  navigationTimeout: 30000,
}`,[`actionTimeout limits how long individual actions like click() or fill() wait to become actionable; navigationTimeout separately limits how long page.goto() and other navigations are allowed to take — a slow page load won’t eat into the budget for a subsequent click, and vice versa`,`They are aliases for the same underlying setting, and only the last one declared takes effect`,`navigationTimeout only applies to the very first page.goto() call in each test`,`actionTimeout overrides navigationTimeout whenever both are set`],0,`Separating action and navigation timeouts lets you tune for a slow-loading app (raise navigationTimeout) without also making every ordinary click wait unnecessarily long if it truly is not actionable.`],[`O`,`I`,`What does setting viewport to null change compared to a fixed size like { width: 1280, height: 720 }?`,`use: {
  viewport: null,
}`,[`Playwright no longer forces a fixed viewport size — the page instead uses the actual size of the (typically maximized, in headed mode) browser window, which can vary by machine/OS rather than being deterministic`,`null defaults to the smallest possible viewport (mobile-sized)`,`viewport: null disables screenshots entirely since there is no fixed size to capture`,`It causes every test to fail with a configuration error, since viewport must be an object`],0,`viewport: null trades deterministic, CI-reproducible sizing for a more "real browser window" feel — mainly useful for exploratory/manual-adjacent debugging runs, not standard CI.`],[`O`,`B`,`What does this option enable for testing dark-mode UI?`,`use: {
  colorScheme: 'dark',
}`,[`It emulates the OS-level "prefers-color-scheme: dark" media feature for every browser context, so CSS and JS relying on that media query render in dark mode automatically, without the test needing to click a theme toggle`,`It changes the color of Playwright’s own console log output`,`colorScheme only affects screenshots, converting them to a dark filter after capture`,`It forces headless mode to use a dark background`],0,`colorScheme emulation lets you test prefers-color-scheme-driven dark mode without relying on the app’s own manual toggle UI, and independent of it too.`],[`O`,`A`,`What does this per-project override achieve?`,`projects: [
  { name: 'guest', use: { storageState: undefined } },
  { name: 'admin', use: { storageState: 'admin.json' } },
]`,[`Tests running under the "guest" project start with a fresh, unauthenticated browser context (no saved cookies/localStorage), while tests under "admin" start already logged in as an admin via the saved storage state — the same test suite can validate both perspectives by running it as two projects`,`storageState: undefined causes the guest project to fail immediately because storageState is a required field`,`Both projects end up sharing the same authenticated session since projects always inherit the global use block`,`This configuration is invalid; storageState cannot vary between projects`],0,`Per-project use overrides are the standard way to run identical test logic against multiple authentication states or personas without duplicating spec files.`],[`O`,`A`,`What does the stdout option control in this webServer config?`,`webServer: {
  command: 'npm run start:test-server',
  url: 'http://localhost:3000',
  stdout: 'pipe',
  stderr: 'pipe',
}`,[`It streams the dev server’s stdout/stderr output into Playwright’s own process output (instead of the default 'ignore'), making it visible in CI logs for diagnosing server startup failures`,`stdout: 'pipe' redirects test console.log output into the server’s process`,`It configures which port the server writes logs to`,`pipe mode disables the server entirely and mocks its responses`],0,`Piping webServer stdout/stderr is invaluable when a CI job fails with "server never became ready" and you need to see WHY the server itself didn’t start.`],[`O`,`A`,`What is the purpose of the blob reporter here in a sharded CI setup?`,`export default defineConfig({
  reporter: process.env.CI ? [['blob']] : [['html']],
});`,["blob produces a compact intermediate report format per shard, meant to later be combined by `npx playwright merge-reports` into one final unified report (e.g. HTML) once all shards finish — a single shard’s HTML report alone would be incomplete",`blob is identical to the html reporter but with a different file extension`,`blob reporting only works when workers is set to 1`,`The blob reporter uploads results directly to a cloud dashboard without any local files`],0,`When sharding splits a suite across machines, each machine only sees its own slice — blob + merge-reports is the standard way to reassemble one coherent report from all the pieces.`],[`O`,`B`,`Why might a team configure the reporter differently for local runs vs CI, as shown here?`,`export default defineConfig({
  reporter: process.env.CI ? [['dot'], ['github']] : [['list']],
});`,[`'dot' is compact and low-noise for scrollable CI logs (plus 'github' annotates PR checks directly), while 'list' gives a developer running locally a more detailed, readable line-by-line view as tests execute`,`Reporters cannot be conditionally configured; this code throws at startup`,`'github' only works when combined with 'html', never with 'dot'`,`This setting has no practical effect since all reporters produce identical output`],0,`Reporter choice is about audience — a human watching a local terminal wants different information density than a CI log viewer or a GitHub PR check annotation.`],[`O`,`I`,`What does this CLI invocation do?`,`npx playwright test --repeat-each=5`,[`It runs every selected test 5 times in a row within the same invocation — a common technique for hunting down flaky tests by seeing if a test fails intermittently across repeated runs`,`It retries only failed tests up to 5 times, identical to the retries config option`,`It runs the suite against 5 different browser projects automatically`,`--repeat-each duplicates the test FILE 5 times on disk`],0,`--repeat-each is specifically for flakiness hunting — running the same test many times in a row surfaces intermittent failures that a single run would miss.`],[`O`,`I`,`What does this CLI invocation do?`,`npx playwright test --last-failed`,[`It re-runs only the tests that failed in the most recent previous run, skipping everything that passed — a fast way to iterate on fixing failures without re-running the whole suite each time`,`It runs the entire suite but reports only the last failure encountered`,`--last-failed re-runs the single most recently modified test file`,`It disables retries and fails fast on the first failing test`],0,`--last-failed reads from Playwright’s cached results of the previous run to scope the next run down to just the known-broken tests.`],[`O`,`A`,`What does this test.describe.configure() call change, compared to running these tests under fullyParallel: true?`,`test.describe.configure({ mode: 'serial' });
test.describe('Checkout flow', () => {
  test('add item', async ({ page }) => { /* ... */ });
  test('apply coupon', async ({ page }) => { /* ... */ });
  test('complete purchase', async ({ page }) => { /* ... */ });
});`,[`It forces the tests inside this specific describe block to run in order, one after another, in the SAME worker — overriding the suite’s general parallelism for this block, which is useful when later tests genuinely depend on state left behind by earlier ones`,`mode: 'serial' disables the tests entirely, treating them as skipped`,`It has no effect if fullyParallel is set globally; global config always wins`,`serial mode requires a separate playwright.config.ts file`],0,`test.describe.configure({ mode: 'serial' }) is a targeted, block-level opt-out of parallelism for the rare case where tests are intentionally stateful and order-dependent.`],[`O`,`A`,`What does this override change for the tests inside the describe block?`,`test.describe('Flaky third-party widget tests', () => {
  test.describe.configure({ retries: 3 });
  test('loads external map widget', async ({ page }) => { /* ... */ });
});`,[`Only tests within this describe block get up to 3 retries on failure, regardless of what retries is set to globally in playwright.config.ts — a scoped override for a known-flaky area without loosening reliability standards for the rest of the suite`,`It sets the global retries config to 3 for the entire run`,`retries at describe level only applies during local runs, never in CI`,`This syntax is invalid; retries can only be set in playwright.config.ts`],0,`Scoped retry overrides let you acknowledge and contain a known-flaky area (like a third-party embed) without hiding real flakiness elsewhere in the suite behind a blanket high retry count.`],[`O`,`A`,`What security-relaxing behavior does this use option introduce?`,`use: {
  bypassCSP: true,
}`,[`It makes the browser ignore the page’s Content-Security-Policy headers/meta tags, allowing Playwright to inject scripts or styles (e.g. via addScriptTag/addStyleTag) that a strict CSP would otherwise block`,`It bypasses the browser’s same-origin policy for all fetch requests`,`bypassCSP disables HTTPS certificate validation, identical to ignoreHTTPSErrors`,`It only affects requests made through the request fixture, not the page`],0,`bypassCSP is specifically useful when a test needs to inject helper scripts/styles into a page whose production CSP would normally block exactly that kind of injection.`],[`O`,`I`,`What does this configuration send with every request from the browser context?`,`use: {
  extraHTTPHeaders: {
    'X-Test-Run': 'nightly-e2e',
  },
}`,[`The given header (and value) is automatically attached to every HTTP request made by that context — useful for tagging test traffic so it can be identified/filtered in backend logs or analytics`,`extraHTTPHeaders only applies to the initial page.goto() navigation, not subsequent requests`,`It overrides the User-Agent header specifically, ignoring the header name given`,`This header is only sent for requests made via the request fixture, never the page`],0,`extraHTTPHeaders is a simple, effective way to make automated test traffic distinguishable from real user traffic in server-side logs or monitoring.`],[`O`,`I`,`What does this configuration allow tests to access without a login form?`,`use: {
  httpCredentials: { username: 'tester', password: 'letmein' },
}`,[`It supplies credentials for HTTP Basic Authentication challenges (the browser-native username/password popup), letting tests reach a Basic-Auth-protected environment like a staging server without any UI-based login flow`,`It stores application-level login credentials that get auto-filled into any form field named username/password`,`httpCredentials is used to authenticate the Playwright test runner itself against a private npm registry`,`It only works when combined with storageState`],0,`HTTP Basic Auth (often used to lock down staging/preview environments) is a browser-level challenge, separate from an app’s own login form — httpCredentials solves specifically that.`],[`O`,`A`,`What scenario does this option let you simulate?`,`use: {
  offline: true,
}`,[`The browser context behaves as if it has no network connectivity at all — useful for testing an app’s offline UI states, service-worker caching fallback, or error handling when requests cannot reach the network`,`offline: true disables the webServer, forcing tests to run against a static build`,`It only blocks WebSocket connections, leaving regular HTTP requests untouched`,`offline mode requires a physical network adapter to be disabled on the test machine`],0,`Testing offline behavior (PWA fallback pages, cached content, retry banners) is exactly the use case offline: true is built for.`],[`O`,`A`,`What workflow does recordHar support here?`,`use: {
  recordHar: { path: 'network-baseline.har' },
}`,[`It captures all network traffic during the test run into a HAR file, which can later be used to replay recorded responses in future runs (via routeFromHAR) — decoupling tests from a live backend’s availability or flakiness`,`recordHar automatically compresses screenshots into the HAR file for smaller storage`,`It records only failed requests, similar to trace on-first-retry`,`HAR files can only be used for manual inspection and have no built-in replay mechanism in Playwright`],0,`Record-once, replay-many with HAR files is a powerful pattern for making tests fast and deterministic against a real backend’s previously-captured responses.`],[`O`,`A`,`What does this option change about how the browser handles service workers?`,`use: {
  serviceWorkers: 'block',
}`,[`Service worker registration is blocked entirely for the context — useful when a real service worker’s caching would interfere with test determinism (e.g. serving stale cached responses instead of the fresh ones a test expects)`,`It blocks all fetch requests originating from the service worker, but allows registration`,`serviceWorkers: 'block' only affects requests made after the first navigation`,`It has no effect unless offline: true is also set`],0,`A real service worker’s caching behavior, while great for production performance, can make tests flaky or hide the very changes you are trying to verify — blocking it trades realism for determinism.`],[`O`,`I`,`What does this proxy configuration route through?`,`use: {
  proxy: { server: 'http://corp-proxy.internal:8080' },
}`,[`All of the browser context’s network traffic is routed through the given proxy server — necessary when testing from inside a network that requires a corporate proxy to reach the target application`,`proxy only affects the Playwright test runner’s own HTTP calls to check for updates`,`It configures a local reverse proxy that Playwright starts automatically for you`,`proxy settings are ignored unless launchOptions.headless is false`],0,`Corporate or restricted network environments often require explicit proxy configuration for any outbound traffic, including from an automated browser.`],[`O`,`A`,`What does this per-project override achieve?`,`projects: [
  { name: 'legacy-suite', testDir: './legacy-tests' },
  { name: 'modern-suite', testDir: './tests' },
]`,[`Each project scans a different directory for its own set of test files, instead of both projects re-running the same global testDir — useful for migrating a legacy test suite gradually alongside a new one without mixing them together`,`testDir at the project level is ignored; only the global testDir applies`,`It causes both projects to run every test file from both directories combined`,`Project-level testDir requires the global testDir to be left unset`],0,`Project-level testDir overrides let each project define an entirely distinct slice of the codebase’s tests, not just different browsers/devices for the SAME tests.`],[`O`,`A`,`What does snapshotDir at the project level control?`,`projects: [
  { name: 'mobile', snapshotDir: './snapshots/mobile', use: { ...devices['iPhone 13'] } },
]`,[`It sets a distinct baseline-image storage location just for this project’s visual snapshot tests, keeping mobile-viewport baselines separate from other projects’ baselines instead of them being intermixed in one shared folder`,`snapshotDir only affects where the HTML report stores screenshots taken on failure, not baselines`,`It has no effect unless toMatchSnapshot() is used instead of toHaveScreenshot()`,`snapshotDir must always match testDir exactly or the config is rejected`],0,`Different projects (mobile vs desktop, different browsers) usually need entirely different visual baselines — snapshotDir keeps those from colliding on disk.`],[`O`,`A`,`What does the stylePath option inject when taking a screenshot?`,`expect: {
  toHaveScreenshot: {
    stylePath: './screenshot.css',
  },
},`,["A custom CSS file is applied to the page right before the screenshot is captured — commonly used to hide known-flaky elements (like animated ads or cursors) with `display: none` without touching the app’s real stylesheets",`stylePath sets the visual theme (light/dark) applied during the whole test, not just the screenshot moment`,`It changes the file format the screenshot is saved as, based on the extension in the path`,`stylePath only works when animations is also set to 'allow'`],0,`stylePath complements mask — mask blanks specific locators at compare-time, while an injected CSS file can hide elements or freeze styles more broadly right before the capture.`],[`O`,`I`,`What files does this testMatch pattern select?`,`export default defineConfig({
  testMatch: ['**/*.e2e.ts', '**/*.smoke.ts'],
});`,[`Only files ending in .e2e.ts or .smoke.ts are treated as test files and discovered by the runner, overriding Playwright’s default *.spec.ts / *.test.ts pattern entirely`,`It excludes .e2e.ts and .smoke.ts files, running everything else`,`testMatch only affects which files appear in the HTML report, not which are executed`,`Both patterns must match the same file simultaneously for it to run`],0,`testMatch is how a team enforces its own file-naming convention for tests instead of relying on Playwright’s default spec/test suffix.`],[`O`,`I`,`What does this command select for execution?`,`npx playwright test --grep-invert "@slow"`,[`It runs every test EXCEPT those whose title contains the tag "@slow" — the inverse of --grep, useful for running a fast subset of the suite while explicitly excluding known long-running tests`,`It runs only the tests tagged @slow, identical to --grep`,`grep-invert reverses the pass/fail result of every matched test`,`It is a shorthand for setting retries to 0 on slow tests only`],0,`--grep and --grep-invert are complementary CLI filters — one includes a subset by pattern, the other excludes it, both without touching the config file or test code.`],[`O`,`B`,`What does this bash command do?`,`npx playwright test --shard=3/4`,[`It runs only the 3rd of 4 equal slices of the total test suite in this invocation — the CLI equivalent of setting shard: { current: 3, total: 4 } in the config, typically used per CI job in a matrix of parallel machines`,`It repeats each test 3 out of 4 times`,`It limits execution to the 3rd project out of 4 defined in the config`,`shard=3/4 sets the worker count to 3 out of a maximum of 4`],0,`The --shard CLI flag is the most common way to configure sharding, since each CI job in a matrix typically only knows its own shard index at invocation time, not at config-authoring time.`],[`L`,`A`,`Why does splitting a suite across CI machines usually use "shard" rather than simply raising "workers" further on one machine?`,null,[`workers parallelizes within a single machine’s CPU/memory budget, which has a ceiling; sharding distributes the ENTIRE suite across multiple separate machines, each running its own workers — the two techniques compose (more workers per shard, more shards across machines) to scale beyond what one machine could handle`,`workers and shard are exact synonyms and either can be used interchangeably`,`Sharding is strictly slower than adding workers and should be avoided when possible`,`shard only works for visual regression tests, not general E2E tests`],0,`For a genuinely large suite, horizontal scaling (more machines via sharding) eventually outpaces vertical scaling (more workers on one machine) which is bounded by that machine’s hardware.`],[`L`,`A`,`Why is a dedicated "setup" project with dependencies generally preferred over doing the same login/seeding work in globalSetup?`,null,[`A setup project is a real Playwright test — it gets full test-runner features (fixtures, retries, tracing, reporting as a normal test result) and its failure is visible and debuggable exactly like any other test failure; globalSetup runs as a bare script outside the fixture/reporting system with much less visibility when something goes wrong`,`globalSetup cannot make network requests, only setup projects can`,`Setup projects run faster because they skip browser launch entirely`,`dependencies is required syntax and globalSetup has been removed from Playwright`],0,`The shift from globalSetup toward project dependencies for auth/seeding reflects wanting that setup work to benefit from the same tooling (fixtures, retries, trace viewer) as the rest of the suite.`],[`L`,`B`,`Why does forbidOnly typically get conditioned on process.env.CI rather than being unconditionally true?`,null,[`Locally, developers legitimately want to use .only() while iterating on one test without the run erroring out; in CI, an accidentally-committed .only() would silently skip most of the suite, which is a much more dangerous, harder-to-notice failure mode that forbidOnly is meant to catch`,`forbidOnly is a CI-only feature and throws an error if enabled locally`,`It has no relationship to .only() and instead controls parallelism`,`Unconditional forbidOnly would prevent any test from ever running`],0,`This mirrors the classic CI-vs-local pattern seen with retries — permissive locally for developer ergonomics, strict in CI for safety.`],[`L`,`I`,`What is the tradeoff of setting maxFailures to a small number like 5 on a very large suite?`,null,[`You get much faster feedback when something is badly broken (the run aborts instead of grinding through hundreds more doomed tests), but you also learn about fewer distinct failures per run — if 5 unrelated bugs exist, you might only discover the first and have to fix-and-rerun repeatedly to surface the rest`,`maxFailures has no real tradeoff; it only ever provides upside with no downside`,`A low maxFailures value causes false negatives, marking passing tests as failed`,`maxFailures only affects the report format, not actual execution`],0,`Fast-fail settings like maxFailures optimize for the common case (one systemic issue causing many failures) at some cost to the less common case (several unrelated failures you’d want to see all at once).`],[`L`,`I`,`Why would a test suite emulate geolocation and permissions via config instead of having the app’s real permission-prompt UI interacted with by Playwright?`,null,[`Native browser permission prompts are OS-level UI outside the page’s DOM and can be unreliable or impossible to interact with consistently across browsers/OSes — pre-granting permissions via config sidesteps that entirely, letting the test focus on the app’s behavior once permission is already granted`,`Playwright cannot interact with any browser dialog under any circumstances`,`Permission emulation is faster only because it skips network requests`,`The app itself would break if a real permission prompt ever appeared during automated testing`],0,`Config-level emulation (geolocation, permissions, colorScheme) exists specifically because these are the kinds of browser/OS-level states that are painful or flaky to drive by simulating real user interaction.`],[`L`,`A`,`Why might a team deliberately test against channel: 'chrome' or 'msedge' in addition to the default bundled Chromium?`,null,[`The bundled Chromium build can differ subtly from the actual, separately-released Chrome or Edge browsers real users run (different build flags, codecs, or enterprise policies) — testing against the real channel catches issues specific to what users actually have installed`,`channel changes have no functional difference; it is purely a cosmetic/branding setting`,`Bundled Chromium is always a strict subset of Chrome’s features with nothing unique to test`,`channel is required for any test that uses the request fixture`],0,`For applications where exact browser-vendor behavior matters (media codecs, PDF viewer, enterprise policies), testing the real channel closes a gap that the generic Chromium engine can leave open.`],[`L`,`B`,`Why is slowMo considered a debugging aid rather than something to leave enabled in a shared config?`,null,[`It artificially delays every single action across every test, multiplying total suite runtime — appropriate for a human watching one test interactively, but a serious, unnecessary cost across hundreds of automated CI runs`,`slowMo makes tests MORE reliable, so removing it before CI is only a stylistic preference`,`slowMo is automatically disabled in headless mode, so leaving it in the shared config has no effect either way`,`It only works with the firefox browser, so it is irrelevant for most suites`],0,`Debugging aids that trade speed for observability (slowMo, headed mode, PWDEBUG) are meant to be applied ad hoc for a specific investigation, not baked into the shared config everyone inherits.`],[`L`,`A`,`Why does preserveOutput: "failures-only" matter more as a suite grows to thousands of tests?`,null,[`Retaining full artifacts (screenshots, videos, traces) for every passing test at scale consumes a large and mostly-useless amount of disk/CI-artifact-storage — since passing tests rarely need post-hoc debugging, discarding their artifacts while keeping failures’ is a straightforward, high-value cost reduction`,`preserveOutput has no measurable effect on storage regardless of suite size`,`Large suites require preserveOutput to be set to 'all' to avoid missing data`,`This setting only affects the terminal reporter, not disk usage`],0,`Artifact retention policy is a scaling concern that only becomes visible once a suite is large enough for "keep everything" to become genuinely expensive.`],[`L`,`A`,`Why does customizing snapshotPathTemplate matter for teams migrating an existing visual-testing setup into Playwright?`,null,[`Playwright’s default nested folder/naming convention for baselines may not match how an existing team already organizes snapshot files (e.g. co-located next to the spec, or in a flat folder) — the template lets you match your migration target’s layout instead of restructuring the whole repo`,`snapshotPathTemplate is required before toHaveScreenshot() can be used at all`,`It changes the actual pixel comparison algorithm used`,`The template only affects file names, never directory structure`],0,`Path template customization is a "fit into an existing convention" tool, not a functional requirement — most teams never need to touch the default.`],[`L`,`A`,`Why does setting a project-wide default under expect.toHaveScreenshot (like maxDiffPixels) scale better than repeating the same option on every individual assertion?`,null,[`A single, centrally-configured default applies consistently everywhere and is trivial to tune later (change one config value vs hunting down every inline occurrence) — inline per-assertion options remain useful only for the exceptions that genuinely need a different tolerance than the norm`,`Inline options are ignored entirely once a project-wide default is set`,`Project-wide defaults only take effect in CI, never locally`,`There is no functional difference; it is purely a style preference with zero practical benefit`],0,`This is the same "set sensible defaults centrally, override only where genuinely needed" principle that applies to config in general — the expect.toHaveScreenshot block is just where it applies to visual assertions specifically.`],[`L`,`I`,`Why can separately tuning actionTimeout and navigationTimeout matter for a slow-loading but otherwise responsive application?`,null,[`If actions themselves (clicks, fills) are normally fast but the app’s initial page load is genuinely slow, raising only navigationTimeout accommodates the slow load without also masking a real bug where an individual click hangs — a single combined timeout would force a bad tradeoff between the two`,`actionTimeout and navigationTimeout must always be set to the same value or Playwright throws a config error`,`navigationTimeout only affects page.goto(), and has no effect on client-side route changes ever`,`Separating the two timeouts has purely cosmetic value with no functional benefit`],0,`Timeout granularity exists so that a legitimate slowness in one area of the app doesn’t force you to loosen the failure threshold for a completely different kind of operation.`],[`L`,`B`,`What is the main downside of setting viewport: null for routine CI test runs?`,null,[`The effective viewport size becomes dependent on the actual browser window/OS environment running the test, which can vary between machines — undermining the deterministic, reproducible layout that a fixed viewport size guarantees for consistent visual and responsive-behavior testing`,`viewport: null makes every test run in headless mode regardless of the headless config setting`,`It disables all locator-based assertions, since they require a known viewport size`,`viewport: null is only valid for mobile device emulation projects`],0,`Determinism is usually more valuable than realism for CI — a fixed viewport ensures every run (and every machine) sees the exact same layout.`],[`L`,`B`,`Why is colorScheme emulation preferable to manually toggling a dark-mode switch in the UI for every dark-mode test?`,null,[`It tests the app’s response to the actual browser-level media query (prefers-color-scheme) directly and consistently across every test that needs it, without depending on a UI toggle existing, working correctly, or being clicked correctly first — decoupling "does dark mode CSS work" from "does the toggle button work"`,`Manually toggling a UI switch is impossible to automate with Playwright`,`colorScheme is the only way dark mode can be tested at all; manual toggling never works`,`colorScheme emulation is deprecated in favor of manual toggling`],0,`Separating concerns — one test for "does the toggle button work", others relying on colorScheme emulation for "does dark-mode CSS render correctly" — avoids conflating two different features in one flaky test.`],[`L`,`A`,`Why might a team run the exact same spec files under two projects with different storageState values, rather than writing separate guest and admin test files?`,null,[`It maximizes reuse — the same assertions and flows are validated under two different authentication states without duplicating test code, and any new test automatically gets both-persona coverage just by being added to the shared file(s) both projects run`,`Playwright requires at least two projects to be defined or it refuses to run`,`Duplicate storageState values across projects cause a config validation error`,`Running the same file under two projects always doubles the reported test count incorrectly`],0,`This "project multiplication" pattern — same tests, different context configuration — is one of the most powerful uses of the projects array beyond just cross-browser coverage.`],[`L`,`A`,`Why does piping webServer stdout/stderr specifically help diagnose CI-only startup failures that never happen locally?`,null,[`A server that fails to start (missing env var, port conflict, build error) usually prints the reason to its own console — with stdout/stderr set to 'ignore' (the default) that diagnostic information is discarded, leaving only a generic "server never became ready" timeout in the Playwright output; piping surfaces the actual root cause`,`Piped output automatically fixes any startup issue by restarting the server`,`stdout/stderr piping only matters for the request fixture, not webServer`,`CI environments cannot start servers at all without piping enabled`],0,`When a failure is "server didn’t come up" rather than a test-logic failure, the most useful diagnostic is the server’s OWN console output — piping is how you get access to it in CI.`],[`L`,`A`,`Why is the blob reporter typically not used for a non-sharded, single-machine local run?`,null,[`blob’s value is specifically as an intermediate format meant to be merged from multiple shards via merge-reports — for a single unsharded run, a directly human-readable reporter like html or list gives an immediately useful result without an extra merge step`,`blob is strictly slower to generate than html regardless of sharding`,`blob reports cannot be viewed at all, even after merging`,`blob is only compatible with the firefox browser project`],0,`Reporter choice should match the actual pipeline shape — blob solves the specific "combine results from N separate machines" problem that a single local run doesn’t have.`],[`L`,`B`,`Why does --repeat-each help catch flaky tests that a single normal run would miss?`,null,[`A test that fails intermittently (e.g. 1 in 10 runs due to a race condition) has a low chance of failing on any single run — repeating it many times in a row within one invocation dramatically increases the odds of observing the failure, surfacing flakiness that a one-shot run would likely pass right through`,`--repeat-each changes the assertion timeout, making flaky timing issues more visible`,`Flaky tests always fail on the very first repetition, so more repeats provide no additional signal`,`--repeat-each only detects flakiness in visual regression tests, not general E2E tests`],0,`Statistically, repeating a suspect test is the most direct way to convert "rare, hard-to-reproduce" flakiness into "reliably reproducible", which is the first step to actually fixing it.`],[`L`,`I`,`How does --last-failed fit into a typical "fix and re-verify" development loop?`,null,[`After a full run reports some failures, a developer can iterate quickly by re-running only those previously-failed tests with --last-failed instead of waiting for the entire suite again — once they all pass, a full run confirms nothing else regressed`,`--last-failed automatically applies a code fix based on the previous failure’s stack trace`,`It re-runs the ENTIRE suite but simply highlights which tests failed last time in a different color`,`--last-failed requires the suite to have zero retries configured to function`],0,`This is purely a local developer-productivity feature — it narrows the feedback loop while iterating, without changing what a full CI run actually verifies.`],[`L`,`A`,`Why would you use test.describe.configure({ mode: 'serial' }) instead of just merging the three dependent steps into one long test?`,null,[`Serial mode keeps the steps as separate, individually-named, individually-reportable tests (each shows its own pass/fail in the report) while still guaranteeing they run in order and share a worker — a single merged test would lose that per-step visibility and make partial-failure diagnosis harder`,`Serial mode and merging into one test produce byte-for-byte identical reports`,`Serial mode is required by Playwright whenever more than one test exists in a describe block`,`test.describe.configure() cannot be combined with any other test.describe options`],0,`Serial mode is a middle ground — you get the granular reporting of separate tests with the ordering guarantee that would otherwise require one giant test.`],[`L`,`A`,`Why should a describe-level retries override for a known-flaky third-party embed generally be paired with an issue/ticket to actually fix the flakiness, rather than left in place indefinitely?`,null,[`A scoped retry override treats a symptom (intermittent failure) without addressing the cause — leaving it indefinitely can mask a real, worsening problem (e.g. the third-party widget getting slower or less reliable over time) behind a growing number of silent retries`,`Retries automatically expire after 30 days if not explicitly renewed`,`describe-level retries only work for exactly 3 CI runs before Playwright disables them`,`There is no risk; scoped retry overrides are a permanent, complete solution with no downside`],0,`Retry overrides are a pragmatic containment tool, not a fix — the underlying flakiness (or the decision to accept it as external/out-of-scope) should still be tracked deliberately.`],[`L`,`A`,`Why does bypassCSP need to be used cautiously rather than as a blanket default across the whole config?`,null,[`CSP is a real production security boundary — bypassing it everywhhere means your tests are no longer exercising the app under the same security constraints real users experience, which could hide a legitimate CSP-related bug (like a genuinely blocked resource) that would only surface in production`,`bypassCSP slows down every test significantly regardless of whether script injection is used`,`It is technically impossible to scope bypassCSP to only specific tests or projects`,`CSP bypass has no relationship to test realism; it is purely a performance optimization`],0,`Like several use-block options, bypassCSP trades test-environment realism for a specific testing convenience — worth doing deliberately and narrowly, not as a global default.`],[`L`,`I`,`What risk does storing httpCredentials directly as plaintext strings in playwright.config.ts introduce?`,null,[`Secrets committed directly into a config file end up in version control history, potentially visible to anyone with repo access (including in public repos or via leaked history) — reading them from environment variables (process.env.STAGING_USER) instead keeps the actual secret out of the codebase`,`httpCredentials are automatically encrypted by Playwright before being written to disk`,`There is no risk since httpCredentials only apply during local test runs, never in CI`,`Config files are excluded from git by default, so this concern does not apply`],0,`Any hardcoded secret in a config file — API keys, basic-auth credentials, tokens — should be sourced from environment variables or a secrets manager instead, exactly like any other credential in a codebase.`],[`L`,`A`,`Why might offline: true tests need to be structured differently from normal E2E tests?`,null,[`With offline: true, the initial page navigation itself may fail (since there is no network to load the page from) unless the page was already loaded/cached before going offline — tests typically need to load the page first, THEN toggle to an offline browser context (or a separate context) to exercise offline-specific behavior`,`offline: true has no effect on page.goto(); only on later requests`,`Offline tests automatically skip themselves if no network is detected on the host machine`,`offline: true requires the webServer config option to be removed entirely`],0,`Simulating "goes offline mid-session" (the realistic scenario for testing offline fallback UI) is different from "starts offline" — test structure needs to account for that ordering.`],[`L`,`A`,`Why is HAR-based replay (recordHar + routeFromHAR) sometimes preferred over live network calls even when the real backend IS available and stable?`,null,[`Replaying from a HAR file removes backend response-time variability and any backend-side flakiness entirely from the test’s critical path, making the test faster and more deterministic — valuable for tests whose real purpose is verifying frontend behavior, not backend integration`,`HAR replay is required by Playwright whenever more than 10 network requests occur in a test`,`Live backends cannot be used in automated tests under any circumstances`,`HAR files automatically stay in sync with backend changes with no manual regeneration needed`],0,`The tradeoff with HAR replay is determinism/speed vs realism/freshness — recorded fixtures need periodic regeneration as the real API evolves, or they silently drift out of sync.`],[`L`,`A`,`Why might blocking service workers (serviceWorkers: 'block') sometimes be the WRONG choice for a specific suite?`,null,[`If the very feature under test IS the service worker’s behavior (offline caching, push notifications, background sync), blocking it removes the thing you actually need to verify — serviceWorkers: 'block' is a determinism convenience for suites where the service worker is incidental, not the suite’s purpose`,`Blocking service workers always causes every navigation to fail entirely`,`serviceWorkers: 'block' is required by Playwright whenever storageState is also used`,`There is no valid reason to ever allow real service workers during testing`],0,`Any config option that trades realism for determinism (bypassCSP, serviceWorkers block, mocked network) needs to be evaluated against what the specific suite is actually trying to verify.`],[`L`,`I`,`Why does a corporate-proxy use.proxy setting typically need to be paired with ignoreHTTPSErrors in some environments?`,null,[`Corporate proxies commonly perform TLS interception (re-signing HTTPS traffic with an internal certificate authority) which the browser doesn’t trust by default — ignoreHTTPSErrors lets the test proceed past the resulting certificate warning rather than failing every single navigation on a cert-trust error`,`proxy and ignoreHTTPSErrors are mutually exclusive and cannot be set together`,`ignoreHTTPSErrors is required for ALL proxy configurations, with no exceptions`,`proxy settings automatically disable HTTPS entirely, requiring no certificate handling`],0,`This is a very common real-world pairing in enterprise environments — proxy for routing, ignoreHTTPSErrors for tolerating the proxy’s own TLS interception certificate.`],[`L`,`A`,`Why does project-level testDir (pointing two projects at entirely different folders) differ conceptually from the more common pattern of multiple projects sharing one testDir but different use options?`,null,[`Shared testDir + different use is "run the SAME tests under different configurations" (e.g. cross-browser); different testDir per project is "run DIFFERENT sets of tests" entirely — the two patterns solve different problems and can even be combined in one config`,`They are functionally identical in every case`,`Different testDir per project is invalid configuration and Playwright will refuse to start`,`testDir can only be set once globally and never overridden per project`],0,`Recognizing which of these two patterns a given multi-project setup is using clarifies whether "more projects" means "more coverage of the same tests" or "more distinct test suites".`],[`L`,`A`,`Why does keeping snapshotDir separate per project (e.g. mobile vs desktop) prevent a subtle class of visual-test bugs?`,null,[`Without separation, two projects with genuinely different expected layouts (mobile vs desktop viewport) could end up comparing against baselines meant for the OTHER project’s viewport, causing constant, confusing false failures that have nothing to do with an actual regression`,`snapshotDir separation only affects file naming, never comparison correctness`,`Playwright automatically infers the correct baseline regardless of snapshotDir configuration`,`This scenario cannot occur because toHaveScreenshot() ignores project context entirely`],0,`Baseline collisions across genuinely different viewport/browser configurations are exactly the failure mode per-project snapshotDir (or Playwright’s own default per-project nesting) is designed to prevent.`],[`L`,`A`,`Why is injecting a stylePath CSS file before a screenshot sometimes preferable to using the mask option for hiding flaky content?`,null,[`stylePath can apply broader, reusable style overrides (like disabling cursor rendering, hiding a whole class of elements via a CSS selector, or freezing hover states) in one shared file across many tests, whereas mask targets specific locators inline per-assertion — the two are complementary rather than competing tools`,`stylePath and mask are functionally identical and interchangeable in every case`,`mask can only target a single element at a time, while stylePath has no such limitation and is therefore strictly superior`,`stylePath disables the actual screenshot comparison, only mask performs real comparisons`],0,`Choosing between mask (per-assertion, locator-targeted) and stylePath (shared CSS file, selector-based) is about scope and reuse — a broad, reusable exclusion rule fits stylePath better; a one-off locator exclusion fits mask better.`],[`L`,`B`,`Why would a team override the default testMatch pattern instead of just renaming all their files to match Playwright’s defaults?`,null,[`A team may be migrating from another test framework, integrating with an existing file-naming convention used elsewhere in the codebase, or simply prefer a different suffix — overriding testMatch avoids a large, disruptive rename of every existing test file`,`Playwright does not allow file renaming as an operation at all`,`testMatch can only be changed by also changing the reporter configuration`,`Renaming files to match Playwright defaults is impossible on Windows filesystems`],0,`Config flexibility like testMatch exists precisely so teams don’t have to conform their entire existing repo structure to match one tool’s defaults.`],[`L`,`I`,`Why is --grep-invert useful even when a team could instead just delete or comment out the tests they want to skip for one run?`,null,[`--grep-invert is a non-destructive, ad hoc, per-invocation filter — it excludes matching tests for just that one run without touching the actual test files, so the excluded tests remain fully intact and will run normally again on the very next invocation without --grep-invert`,`Commenting out tests and using --grep-invert produce identical git history and are equally safe`,`grep-invert permanently disables matched tests until manually re-enabled in the config`,`There is no meaningful difference; grep-invert is purely a performance optimization with the same effect as deletion`],0,`CLI-level filters like --grep and --grep-invert let you reshape what runs for a SPECIFIC invocation without any risk of a "temporary" code change being accidentally left in place.`],[`L`,`I`,`Why does the --shard CLI flag typically get set from a CI matrix variable rather than hardcoded in playwright.config.ts?`,null,[`Each CI job in a parallel matrix needs a DIFFERENT shard index (1/4, 2/4, 3/4, 4/4) at the same time — a single hardcoded value in the shared config file couldn’t vary per job, so the CI system injects the correct index as a CLI argument or environment variable per job instance`,`Hardcoding a shard value in the config file is not technically possible; Playwright rejects config-level shard settings`,`--shard values must always be identical across every job in a CI matrix`,`CI matrix variables and shard configuration are unrelated concepts`],0,`Sharding is fundamentally a per-CI-job concern — the mechanism that assigns each job its own shard index (usually the CI platform’s own matrix feature) naturally lives outside a single static config file.`],[`L`,`A`,`Why does defining a "cleanup" project as the teardown of a "setup" project scale better than manually deleting seeded data at the start of each test run?`,null,[`The teardown project is guaranteed to run exactly once, after everything depending on setup has finished, regardless of how many projects or test files consumed that setup — manual per-test-run deletion logic would need to be duplicated or remembered in multiple places and is easy to forget or get out of sync`,`Manual deletion is always faster because it avoids Playwright’s test infrastructure overhead entirely`,`teardown projects run BEFORE the setup project, guaranteeing a clean slate for every run`,`There is no meaningful difference; both approaches are equally reliable`],0,`The teardown field turns "remember to clean up" into "the framework guarantees cleanup happens exactly once, in the right place" — removing a whole class of easy-to-forget maintenance tasks.`],[`L`,`A`,'How does the project-dependencies feature change the mental model of "what does a single `npx playwright test` invocation actually run", compared to a config with no dependencies at all?',null,[`Without dependencies, all projects’ tests can start immediately and run concurrently; with dependencies declared, Playwright builds an execution order where dependent projects wait for their prerequisite project to fully succeed first — effectively introducing a DAG (directed acyclic graph) of project execution rather than one flat, fully-parallel list`,`Dependencies have no effect on execution order, only on report grouping`,`Dependencies force ALL projects to run strictly one after another regardless of whether they actually depend on each other`,`A project with no dependencies also waits for every other project to finish, by default`],0,`Understanding dependencies as introducing a DAG rather than a flat list explains why a "setup" project failing correctly prevents its dependents from running at all, rather than them running against broken/missing prerequisite state.`],[`L`,`I`,`What is the risk of setting a very generous global expect.timeout (e.g. 30 seconds) suite-wide just to fix one known-slow page, instead of overriding the timeout only for that specific assertion?`,null,[`Every OTHER assertion in the suite now also waits up to 30 seconds before failing on a genuine bug, meaning real regressions elsewhere take far longer to surface as a failure than necessary — a global change to fix a local problem quietly degrades feedback speed everywhere else`,`Generous global timeouts have no downside since Playwright resolves assertions as soon as they pass regardless of the configured maximum`,`A 30-second expect.timeout would prevent the test suite from completing at all`,`expect.timeout can only be set globally, never per-assertion, making this the only option anyway`],0,`This is the config-level version of the same principle as maxDiffPixels defaults — a scoped fix (per-assertion or per-describe-block override) is almost always safer than a broad, blanket change made to accommodate one edge case.`],[`L`,`A`,"Why does forbidOnly not catch a developer who commits a `test.skip()` intended only for local debugging, the way it catches an accidental `.only()`?",null,["forbidOnly specifically targets `.only()` because that pattern silently HIDES most of the suite from running at all (a dangerous false sense of full coverage); `.skip()` is visibly reported as skipped in the test results, so a reviewer or CI dashboard can at least see it happened, even though it is also a real maintenance smell worth catching separately",`forbidOnly actually does block .skip() identically to .only(), contrary to common assumption`,`test.skip() cannot be committed to version control since Playwright removes it automatically before commit`,`There is no meaningful difference in risk between .only() and .skip() being left in code`],0,`The specific danger forbidOnly addresses is silent, easy-to-miss suite narrowing — a lingering .skip() is a different (and generally lower-severity, more visible) code-hygiene issue.`],[`L`,`B`,`What is a key reason a team might keep BOTH a "smoke" project (fast subset) and a "full" project (entire suite) defined in the same playwright.config.ts?`,null,[`Different pipelines/stages have different needs — a pre-merge check might only need the fast smoke subset for quick feedback, while a nightly or pre-release pipeline runs the full project for complete coverage — --project lets CI choose which to run without maintaining two separate config files`,`Playwright requires at least two projects to be defined for any config to be valid`,`Smoke and full projects must always run simultaneously and cannot be selected independently`,`This pattern provides no practical benefit over simply always running the full suite everywhere`],0,`Multiple projects targeting different SPEED/COVERAGE tradeoffs (not just different browsers) is a common and practical use of the projects array in real CI pipelines.`],[`L`,`I`,`Why does reading BASE_URL from an environment variable (use: { baseURL: process.env.BASE_URL }) scale better across dev/staging/prod than maintaining three separate playwright.config.ts files?`,null,[`One config file stays the single source of truth for every OTHER setting (timeouts, retries, projects, reporters) while only the environment-specific piece varies at invocation time — three separate config files would require keeping every non-URL setting manually synchronized across all three copies, an easy source of drift`,`Separate config files execute noticeably faster than a single parameterized one`,`Playwright technically forbids having more than one playwright.config.ts file in a repository`,`Environment variables cannot be read inside a TypeScript config file, forcing the multi-file approach`],0,`This is the general DRY principle applied to configuration — parameterize the one thing that actually varies, rather than duplicating everything to vary that one thing.`],[`L`,`A`,"Why is `workers: '50%'` sometimes preferred over a fixed number like `workers: 4` in a config shared across contributors’ different machines?",null,[`A percentage scales relative to whatever CPU core count is actually available on the machine running the suite — a fixed number tuned for a powerful CI runner might over-subscribe a contributor’s modest laptop (causing resource contention and flaky timing) or under-utilize a very powerful one`,`Percentage-based worker counts run measurably faster than any fixed number due to internal Playwright optimizations`,`Fixed worker counts are deprecated and will be removed in a future Playwright version`,`workers: '50%' is required whenever fullyParallel is also set to true`],0,`Percentage-based settings are a way to write one config value that adapts sensibly to heterogeneous hardware, rather than a single number that is only well-tuned for one specific machine.`],[`L`,`B`,"Why might a team intentionally keep `retries: 0` even in CI for a specific, well-understood project (e.g. a project that only runs fast, deterministic unit-style API checks)?",null,[`Retries exist to absorb genuine UI/timing flakiness; a suite of fast, deterministic checks with no browser timing involved has little to gain from retries and more to lose — a real bug there should fail immediately and clearly rather than being retried and possibly masked as "just flaky"`,`retries: 0 is required by Playwright whenever fullyParallel is also set to false`,`CI environments technically cannot support any retries value other than 0`,`There is never a valid reason to disable retries in CI under any circumstances`],0,`Retries are a tool for a specific problem (timing-related UI flakiness) — applying them uniformly everywhere, including to tests that have no such flakiness risk, can hide genuine bugs instead of protecting against noise.`],[`R`,`A`,`Your suite has 2,000 tests and a single CI machine takes 45 minutes to run them all. You have access to 4 identical CI runners. How do you cut wall-clock time roughly fourfold?`,null,[`Configure shard: { current: N, total: 4 } (or the --shard CLI flag) as a matrix across the 4 runners, each running its own slice, then merge their reports with npx playwright merge-reports`,`Set workers: 16 on the single existing machine instead of using the other 3`,`Reduce retries to 0 across the board to save time`,`Duplicate the exact same full suite on all 4 machines and take whichever finishes first`],0,`Sharding is specifically the mechanism for splitting one suite across multiple independent machines, unlike workers which only parallelizes within one machine.`],[`R`,`A`,`You want every test to authenticate via a fast API call once, save the session, and have all 15 spec files reuse it — without a separate manual script outside the test runner’s visibility.`,null,[`Create a "setup" project with testMatch for a .setup.ts file that logs in via the request fixture and saves storageState; make other projects declare dependencies: ['setup']`,"Write a standalone Node.js script run before `npx playwright test` via a package.json pretest hook",`Add the login API call inside beforeAll in every single spec file`,`Set storageState directly to a hardcoded JSON object in playwright.config.ts`],0,`A project dependency running an actual Playwright test (with fixtures, retries, and tracing) is the modern, more debuggable alternative to a bare external script for one-time setup.`],[`R`,`I`,"A junior developer keeps accidentally pushing branches with `test.only()` left in, and it went unnoticed in CI for a week, silently skipping most of the suite. What config change prevents this going forward?",null,[`forbidOnly: !!process.env.CI`,`retries: process.env.CI ? 2 : 0`,`fullyParallel: true`,`maxFailures: 1`],0,`forbidOnly is precisely the safety net for this exact failure mode — CI errors loudly instead of silently running a fraction of the suite.`],[`R`,`A`,`Your team needs to verify the checkout page renders correctly with both a light and dark OS theme, without adding UI-toggle-clicking steps to every existing test.`,null,[`Add a second project with use: { colorScheme: 'dark' } alongside the existing light-mode project, and run the same spec files under both`,`Insert a manual page.evaluate() call in every test to toggle a CSS class`,`Use the request fixture to send a header indicating theme preference`,`Switch trace to 'on' to visually inspect theme differences after the fact`],0,`colorScheme emulation via a project is the standard way to get theme coverage for free across an existing suite, without touching individual test bodies.`],[`R`,`I`,`A staging environment sits behind an HTTP Basic Auth prompt that must be passed before the app itself even loads. What config option handles this without any UI interaction in the test?`,null,[`use: { httpCredentials: { username: process.env.STAGING_USER, password: process.env.STAGING_PASS } }`,`use: { extraHTTPHeaders: { Authorization: 'Basic' } }`,`use: { storageState: 'staging-auth.json' }`,`use: { ignoreHTTPSErrors: true }`],0,`httpCredentials handles the browser-native Basic Auth challenge directly, and reading from env vars keeps the actual secret out of the committed config.`],[`R`,`A`,`You need the exact same suite to run against Google Chrome (not bundled Chromium) for a client who specifically wants proof of compatibility with the real, branded browser.`,null,[`Add a project with use: { ...devices['Desktop Chrome'], channel: 'chrome' }`,`Set browserName: 'google-chrome' in the use block`,`Install a third-party Chrome plugin for Playwright`,`This is not possible; Playwright can only launch its own bundled browsers`],0,`channel: 'chrome' specifically targets the real, separately-installed Chrome binary rather than Playwright’s bundled Chromium build.`],[`R`,`A`,`A test suite hits a live third-party payment sandbox that is occasionally slow or flaky, unrelated to your own app’s bugs. You want to isolate frontend tests from that instability without mocking the whole payment flow.`,null,[`Record a HAR file of a known-good payment sandbox interaction once, then use routeFromHAR() to replay it in the actual tests, decoupling them from the live sandbox’s availability`,`Set retries: 10 globally to just brute-force through the flakiness`,`Set offline: true to skip network calls entirely during those tests`,`Increase actionTimeout to 60 seconds to wait out any sandbox slowness`],0,`HAR recording/replay is the standard way to get deterministic, fast tests against a known third-party interaction pattern without depending on that third party being consistently available during every CI run.`],[`R`,`B`,`You are debugging one specific failing test locally and want to visually watch each action happen slowly in a real browser window, without changing the committed config.`,null,[`npx playwright test my-test.spec.ts --headed --debug (or set launchOptions.slowMo via an environment-driven config toggle only when debugging locally)`,`Set slowMo: 250 permanently in playwright.config.ts for everyone`,`Add page.waitForTimeout(1000) calls throughout the test file`,`Set workers: 1 in the shared config`],0,`Debugging aids like --headed, --debug, and slowMo are meant to be applied ad hoc per investigation, not committed as permanent shared config changes.`],[`R`,`I`,`Four CI jobs each run a shard of your suite and each produces its own report. Management wants ONE combined HTML report showing the whole run’s results.`,null,[`Configure reporter: [['blob']] for each shard, upload each shard’s blob output as a CI artifact, then run npx playwright merge-reports --reporter=html on the combined blob files in a final job`,`Have each shard generate its own separate HTML report and manually open all four`,`Set reporter: [['html']] on every shard and expect Playwright to merge them automatically`,`Combine the four shards’ JSON reporter output using a spreadsheet`],0,`blob + merge-reports is purpose-built exactly for combining sharded CI results into one coherent final report.`],[`R`,`A`,`A specific describe block tests a flaky embedded third-party chat widget that fails about 1 in 5 runs for reasons outside your control, while the rest of your suite is reliably stable at 0% flake rate.`,null,[`Scope a higher retry count to just that block: test.describe.configure({ retries: 3 }) inside the chat-widget describe, leaving the global retries setting untouched for the rest of the suite`,`Raise the global retries config to 3 for the entire suite`,`Delete the flaky tests entirely since third-party flakiness cannot be tested reliably`,`Set maxFailures: 3 globally to tolerate the flakiness`],0,`A scoped override contains the accommodation to exactly the known problem area, without loosening reliability standards (and hiding potential real regressions) everywhere else.`],[`S`,`A`,`A CI job configured with shard: { current: 3, total: 4 } consistently reports "0 tests found" while the other three shards run fine. What is the most likely cause?`,null,[`The testMatch pattern (or testDir) may be misconfigured such that far fewer tests than expected are discovered overall, and the sharding math for shard 3 of a small total count happens to land on zero — check the total discovered test count before sharding is applied, and verify testMatch/testDir first`,`Shard 3 is a reserved value that Playwright always skips`,`shard: { current: 3 } is invalid syntax and silently no-ops`,`Sharding always requires total to be a power of 2, and 4 shards is invalid`],0,`A "0 tests in one shard" symptom usually traces back to how many tests were discovered in total, not a shard-specific bug — verify what an unsharded run reports first.`],[`S`,`I`,`After introducing dependencies: ['setup'] for authentication, the "setup" project’s login test now ALSO appears as a normal test in the main run’s results, effectively running the login flow twice per invocation. What is misconfigured?`,null,[`The main projects’ testMatch/testDir likely still includes the setup file’s own path (e.g. *.setup.ts matches the default testMatch too) — the setup project needs a distinct, narrowly-scoped testMatch, and other projects should exclude that pattern via testIgnore or a more specific testMatch of their own`,`dependencies always cause the setup project’s tests to run twice by design`,`This is expected behavior and cannot be avoided when using project dependencies`,`The setup project must be renamed to "teardown" to avoid double-execution`],0,`A common project-dependency misconfiguration is failing to exclude the setup-only files from the pattern that OTHER projects also match, causing accidental double execution.`],[`S`,`A`,`A team’s visual regression suite is stable in local development but shows constant, small pixel diffs in CI, concentrated in font rendering and antialiasing — never in actual layout or content. What is the standard remediation path?`,null,[`Run visual tests inside a fixed Docker image matching the exact environment the baselines were generated in (eliminating font/GPU rendering differences between machines), and/or set a small maxDiffPixelRatio at the expect.toHaveScreenshot config level to tolerate genuinely negligible rendering noise`,`Disable visual regression testing entirely since cross-machine rendering can never be made consistent`,`Increase the assertion timeout so the browser has more time to render fonts correctly`,`Switch every toHaveScreenshot() call to toMatchAriaSnapshot() instead, since it ignores rendering`],0,`Cross-environment rendering differences (fonts, subpixel antialiasing, GPU) are the classic visual-testing flakiness cause, and a consistent Docker environment plus a small tolerance threshold are the standard combined fix.`],[`S`,`I`,`A new project.testDir override was added to run a legacy test folder separately, but now some legacy tests appear to run TWICE — once under the new project and once under the original global-testDir project. What went wrong?`,null,[`The legacy folder is likely still nested inside (or overlapping with) the original global testDir, so the default project still discovers those same files in addition to the new project explicitly pointed at them — the legacy folder should be excluded from the original project’s scope (e.g. via testIgnore) or moved outside the global testDir entirely`,`project.testDir overrides are cumulative and always run both the old and new paths`,`This is expected and intentional; legacy tests are meant to always run twice for extra confidence`,`testDir overrides only take effect after the second run of any given day`],0,`Whenever a directory is targeted by more than one project’s discovery pattern, double execution is the predictable result — testIgnore or non-overlapping folder structure resolves it.`],[`S`,`A`,`A describe block genuinely needs serial execution because each test depends on state left by the previous one (adding items to a cart across tests), but the team also wants the rest of the suite to stay fully parallel for speed. What is the right scoping?`,null,[`Apply test.describe.configure({ mode: 'serial' }) ONLY inside that specific describe block, leaving the global fullyParallel setting untouched for everything else`,`Set fullyParallel: false globally to be safe, accepting the suite-wide slowdown`,`Move the dependent tests into a separate playwright.config.ts with different parallelism settings`,`Merge the dependent tests into one long test function to force sequential execution implicitly`],0,`Block-scoped serial mode is exactly the tool for isolating a known ordering-dependency without sacrificing the parallelism benefits everywhere else in the suite.`],[`S`,`I`,`forbidOnly: !!process.env.CI is set, yet a PR with a leftover .only() still merged and CI showed the majority of tests as skipped rather than the run failing outright. What is the most likely explanation?`,null,[`The CI environment variable CI was not actually set to a truthy value in that particular pipeline step/job, so !!process.env.CI evaluated to false and forbidOnly was effectively disabled for that run — verify the CI platform genuinely exports CI=true (most do, but custom runners or certain job types might not)`,`forbidOnly only applies to test.describe.only, never test.only`,`forbidOnly requires retries to be set to 0 to take effect`,`The .only() was inside a fixture file, which forbidOnly does not scan`],0,`A config option gated on process.env.CI is only as reliable as that environment variable actually being set correctly in every relevant pipeline — worth explicitly verifying rather than assuming.`],[`S`,`A`,`HAR-based replay tests for a payment API started failing after the backend team shipped a new required field in the request payload. The recorded HAR file predates that change. What is the underlying issue and fix?`,null,[`The HAR fixture is now stale relative to the real API contract — routeFromHAR() serves the OLD recorded responses/expectations, so it needs to be re-recorded against the updated backend to reflect the new contract; this is the inherent maintenance cost of HAR-based fixtures`,`HAR files automatically detect and adapt to backend schema changes, so this is likely a Playwright bug`,`The fix is to disable HAR replay permanently and always hit the live backend instead`,`routeFromHAR() ignores request payload differences entirely, so this failure must be unrelated to the HAR file`],0,`Recorded fixtures (HAR files, mocked responses, saved storageState) all share the same tradeoff: they need periodic regeneration as the real system they represent evolves, or they silently go stale.`],[`S`,`A`,`A suite with 100,000+ artifacts accumulated over months is filling up CI storage, even though most tests pass reliably. What single config change addresses this most directly?`,null,[`preserveOutput: 'failures-only', so artifacts (screenshots, videos, traces) are only retained on disk for tests that actually failed, rather than every single passing test as well`,`Set video: 'off' and screenshot: 'off' entirely, losing all debugging capability for future failures`,`Reduce workers to 1 to reduce total artifact generation`,`Switch to the 'dot' reporter, since it produces smaller report files`],0,`preserveOutput directly targets artifact retention policy — the specific lever for "stop keeping data we don’t need" without losing debugging capability where it matters (failures).`],[`S`,`I`,`A team accidentally committed httpCredentials with a real staging password directly in playwright.config.ts. What is the correct remediation, beyond just editing the file going forward?`,null,[`Rotate/change the actual staging credential immediately (since it is now exposed in git history even after being removed from the current file), then replace the hardcoded value with process.env.STAGING_PASSWORD read from a secret manager or CI secret store`,`Simply delete the line from the current file; git history does not retain old file contents by default`,`Rename the config file so the old commit history becomes inaccessible`,`No action is needed since httpCredentials are only used locally, never exposed externally`],0,`A leaked secret in version control requires rotating the actual credential — removing it from the current file alone does not undo its exposure in git history.`],[`S`,`A`,"Two teams merge their playwright.config.ts files during a repo consolidation. Team A had `use: { serviceWorkers: 'block' }`, Team B’s suite specifically tests offline-mode service worker caching. After merging, Team B’s offline tests all fail. What is the conflict, and the fix?",null,[`Team A’s global block setting now also disables the service worker Team B’s tests depend on — Team B’s project(s) should override serviceWorkers back to 'allow' at the project level, since project-level use overrides the global default`,`serviceWorkers settings cannot be overridden per-project; the whole config must choose one value for everyone`,`This is unrelated; the tests must be failing for a different, unrelated reason`,`Team B must move their tests into an entirely separate repository to avoid the conflict`],0,`Global use-block defaults are exactly the kind of setting that can silently break another team’s tests during a config merge — project-level overrides are the mechanism to reconcile conflicting needs.`],[`D`,`A`,`Running with --shard=2/4 locally to debug a sharding issue shows a completely different (and much smaller) set of tests than expected, and the numbers don’t add up across manual runs of all 4 shards. What should you check first?`,null,[`Whether testMatch/testDir discovers a stable, deterministic set and ORDER of tests — sharding assigns tests to shards based on the full discovered list, so any source of non-determinism in file discovery (like glob ordering differences) can cause inconsistent shard assignment between runs`,`Shard numbering always starts at 0, so --shard=2/4 is being misinterpreted`,`Local machines cannot run --shard at all; it only works inside actual CI`,`The total must always be a multiple of the worker count or sharding silently breaks`],0,`Sharding correctness depends on consistent test discovery — if the underlying file list order or contents vary between invocations, shard assignment becomes unpredictable too.`],[`D`,`I`,`merge-reports fails with "no blob reports found" even though each of the 4 CI shard jobs completed and reported success. What is the most likely missing step?`,null,[`Each shard’s generated blob-report output directory was likely never uploaded/downloaded as a CI artifact between the shard jobs and the final merge job — merge-reports needs local access to all 4 shards’ blob files, which requires an explicit artifact upload/download step in the CI pipeline definition`,`blob reports expire after 5 minutes and must be merged immediately`,`merge-reports only works when all shards ran on the exact same machine`,`The blob reporter is deprecated and no longer produces any output`],0,`blob + merge-reports across CI jobs is a two-part workflow — the reporter produces the files, but the CI pipeline itself is responsible for getting those files to wherever the merge step runs.`],[`D`,`A`,`A project override sets colorScheme: 'dark', but screenshots taken in that project still show the light theme. What should you investigate?`,null,[`The app may determine its theme via a JavaScript check of localStorage or a manual toggle rather than actually reading the prefers-color-scheme media query — colorScheme emulation only affects what the BROWSER reports for that media feature; if the app ignores it in favor of its own stored preference, emulation alone won’t change the rendered theme`,`colorScheme only applies to the reporter’s own UI theme, not the tested page`,`Dark mode requires headless: false to render correctly`,`colorScheme: 'dark' must be paired with viewport: null or it is ignored`],0,`Config-level emulation only works if the app under test actually reads the emulated signal — if the app’s theme logic checks something else entirely (like a stored cookie), emulation has nothing to hook into.`],[`D`,`I`,"Tests configured with `proxy: { server: 'http://corp-proxy:8080' }` in the global use block are, for one specific project, still making direct (non-proxied) requests. What is the likely cause?",null,[`That specific project’s own use block (or its launchOptions) may set its own proxy value (including an explicit undefined or a different server), which overrides the global use-level proxy setting for just that project — check for a project-level override`,`proxy settings cannot be inherited from the global use block under any circumstances; they must be repeated on every project`,`The corp-proxy server itself is blocking Playwright specifically based on user-agent`,`proxy only applies to the first navigation in each project, never subsequent requests`],0,`Any project-level use/launchOptions value takes precedence over the same key set globally — a silent per-project override is a common cause of "the global setting isn’t working" reports.`],[`D`,`A`,"A `dependencies: ['setup']` chain is configured correctly, but the setup project intermittently reports success in the log while its teardown project (declared via `teardown: 'cleanup'`) never appears to run at all in some CI runs. What should you check?",null,[`Whether the OVERALL Playwright process is being terminated early (e.g. a CI step timeout, a maxFailures abort, or the pipeline killing the job right after the dependent tests finish) before the teardown project gets its turn — teardown runs after ALL dependents finish, so anything that cuts the run short before that point will skip it`,`teardown projects only run if the setup project itself explicitly calls a teardown() function`,`teardown is a Playwright Test feature that requires a paid license to function reliably`,`The cleanup project name must alphabetically follow the setup project name or it is skipped`],0,`Because teardown runs at the very end of a project’s full dependent chain, anything that shortens or aborts the overall run (timeouts, maxFailures, external process kills) can prevent it from ever being reached.`]],fixtures:[[`O`,`B`,`Which of these are built-in Playwright fixtures available in every test?`,null,[`page, context, browser, request, browserName`,`page, dom, fetch, network, storage`,`page, puppeteer, axios, browser, frame`,`page, session, cookie, proxy, device`],0,`Built-in fixtures: page · context · browser · request · browserName — all injected automatically.`],[`O`,`B`,"What does calling `await use(value)` do inside a fixture function?",null,[`Returns the value to the test runner's global scope`,`Hands the value to the test and pauses the fixture until the test finishes`,`Marks the fixture as completed and skips teardown`,`Creates a new browser context with the given value`],1,`use() is the boundary: setup above it, test runs at it, teardown below it.`],[`O`,`I`,"What is the default scope for a custom fixture created with `test.extend()`?",null,[`worker — shared across all tests`,`file — shared within a spec file`,`test — a fresh instance per test`,`global — shared across all workers`],2,`Scope defaults to 'test' — each test gets its own isolated fixture instance.`],[`O`,`B`,"What does `scope: 'worker'` mean for a fixture?",null,[`The fixture runs in a separate Node.js worker thread`,`One fixture instance is created and shared across all tests running in the same worker process`,`The fixture is created for each test file sequentially`,`The fixture runs in the browser worker context`],1,`Worker-scoped fixtures avoid expensive repeated setup (e.g. DB connections) across tests in one worker.`],[`O`,`I`,"When does code placed after `await use()` run inside a fixture?",null,[`Immediately after use() is called, before the test starts`,`After the test finishes — it is the teardown phase`,`It never runs — code after use() is ignored`,`Only when the test fails`],1,`Everything after await use() is teardown — it runs automatically when the test ends, pass or fail.`],[`O`,`B`,`How does a test declare which fixtures it needs?`,null,[`By calling test.request('fixtureName') before the test body`,`By destructuring fixture names from the first argument of the test callback`,`By importing them from the fixture file`,`By listing them in an array passed to test()`],1,"Destructuring in `async ({ page, myFixture }) => { }` tells Playwright which fixtures to set up."],[`O`,`I`,"What does `auto: true` do in a fixture definition?",null,[`Makes the fixture setup run in parallel with other auto fixtures`,`Runs the fixture for every test even if the test doesn't request it by name`,`Automatically tears down the fixture without needing use()`,`Marks the fixture as required — tests fail if it's missing`],1,`auto: true is useful for side-effecting fixtures like clock freezing or request interception.`],[`O`,`I`,"What is the main advantage of fixtures over `beforeEach` hooks for reusable setup?",null,[`Fixtures run faster than hooks`,`Fixtures are lazy — only set up when a test requests them — and compose naturally across files`,`beforeEach cannot be async`,`Hooks cannot access the page object`],1,`Fixtures are tree-shaken and file-portable; hooks are file-local and always run regardless of need.`],[`O`,`B`,"What does `test.extend()` return?",null,[`A new test runner instance with the added fixtures merged in`,`A fixture object you attach to the base test`,`The fixture value directly`,`An array of fixture setup functions`],0,"test.extend() returns a new `test` function — import and use this extended version in your specs."],[`O`,`I`,"What does `test.use({ storageState: 'auth.json' })` do when placed at the top of a spec file?",null,[`Sets storageState as a global default for all projects`,`Overrides the storageState fixture for every test in that file or describe block`,`Imports the auth.json file into the test`,`Sets a cookie named storageState`],1,`test.use() applies fixture overrides to the current file or describe scope.`],[`O`,`I`,`Can a custom fixture depend on another custom fixture?`,null,[`No — fixtures cannot reference each other`,`Yes — list the dependency by name in the fixture's first argument, just like tests do`,`Only built-in fixtures can be dependencies`,`Only if they share the same scope`],1,"Fixtures compose: `adminPage: async ({ page, db }, use) => { … }` depends on both page and db."],[`O`,`A`,"What is the practical difference between `scope: 'worker'` and `scope: 'file'`?",null,[`worker shares across all files in the run; file shares across tests in one spec file only`,`file is faster because it doesn't start a worker process`,`They are identical in behaviour`,`worker is deprecated in favour of file`],0,`Worker scope = one instance per Node.js worker across multiple files; file scope = one instance per spec file.`],[`F`,`B`,"Complete: `const test = base.______({ todoPage: async ({ page }, use) => { … } })`",null,[`extend`,`add`,`create`,`merge`],0,`test.extend() is the API for adding custom fixtures to the test object.`],[`F`,`I`,"Complete the fixture signature: `async ({ page }, ______) => { await use(page) }`",null,[`use`,`done`,`next`,`callback`],0,`use is the second argument — calling it passes the value to the test and marks the setup boundary.`],[`F`,`B`,"Complete: `await ______(myValue)` to hand the fixture value to the test",null,[`use`,`provide`,`inject`,`return`],0,`await use(value) is the canonical pattern; code after it is teardown.`],[`F`,`I`,"Complete: `[async ({}, use) => { … }, { scope: '______' }]` for a shared fixture",null,[`worker`,`global`,`shared`,`process`],0,`scope: 'worker' shares the instance across all tests in a worker process.`],[`F`,`I`,"Complete: `[async ({ page }, use) => { … }, { ______: true }]` to run for every test",null,[`auto`,`always`,`required`,`global`],0,`auto: true makes the fixture run for every test without being explicitly requested.`],[`F`,`I`,"Complete: `test.______({ storageState: 'admin.json' })` to override for a file",null,[`use`,`set`,`configure`,`apply`],0,`test.use() applies fixture overrides at file or describe-block scope.`],[`F`,`A`,"Complete to make a fixture depend on another: `async ({ page, ______ }, use) => { … }` where db is a custom fixture",null,[`db`,`fixtures.db`,`base.db`,`this.db`],0,`Just name the dependency in the destructured first argument — Playwright resolves it automatically.`],[`E`,`B`,`What is wrong?
\`\`\`ts
const test = base.extend({
  loggedIn: async ({ page }, use) => {
    await page.goto('/login');
    use(page);  // no await
  },
});
\`\`\``,null,[`use() does not accept a page argument`,`await is missing before use() — the fixture may tear down before the test starts`,`page.goto() must come after use()`,`extend() requires a type parameter`],1,`Without await, the fixture function returns before the test can use the value — the test runs with an undefined fixture.`],[`E`,`I`,"What is wrong?\n```ts\ntest('my test', async ({ loggedIn }) => { … });\n// imported from base, not the extended test\nimport { test } from '@playwright/test';\n```",null,[`loggedIn cannot be a fixture name — it conflicts with a built-in`,`The test uses the base test object, so loggedIn fixture is not registered and will throw`,`Imports must come before test() calls`,`async is not needed when using fixtures`],1,`Always import and use the extended test — using the base test loses all custom fixtures.`],[`E`,`B`,`What is wrong?
\`\`\`ts
const test = base.extend({
  db: async ({}, use) => {
    const db = await connect();
    await use(db);
    await use(db);  // second use call
  },
});
\`\`\``,null,[`db must be typed before it can be used twice`,`use() must only be called once per fixture — calling it twice is an error`,`The second use() should be close() not use()`,`await is invalid on the second use()`],1,`Each fixture may only call use() once — it marks the single setup/teardown boundary.`],[`E`,`I`,`What is wrong?
\`\`\`ts
const test = base.extend<{}, { page: Page }>({  // page in worker fixtures
  page: [async ({}, use) => {
    …
  }, { scope: 'worker' }],
});
\`\`\``,null,[`page is already a built-in and cannot be overridden`,`page cannot be worker-scoped because it is tied to a single browser context that cannot be shared safely between tests`,`Worker fixtures cannot access built-in fixtures`,`extend does not support type parameters`],1,`page is context-bound and not thread-safe; page should always be test-scoped. Worker scope is for stateless or explicitly thread-safe resources like DB connections.`],[`E`,`I`,`What is wrong?
\`\`\`ts
const test = base.extend({
  myFixture: async ({ page }, use) => {
    const val = setup();
    // no await use() called
  },
});
\`\`\``,null,[`setup() must be awaited`,`use() is never called — the test will hang waiting for the fixture to hand it a value`,`page is not needed if use() is not called`,`The fixture must return a value`],1,`If use() is never called, Playwright waits indefinitely — the test times out.`],[`E`,`A`,`What is wrong?
\`\`\`ts
test.describe('Admin', () => {
  test.use({ storageState: 'admin.json' });
  test.beforeAll(async () => {
    test.use({ storageState: 'superadmin.json' }); // inside beforeAll
  });
});
\`\`\``,null,[`test.use() is not allowed inside test.describe`,`test.use() inside beforeAll is invalid — it only works at the top level of describe or a file`,`storageState cannot be overridden per describe`,`beforeAll cannot call test.use() because it runs before fixtures are resolved`],1,`test.use() must be called at the top level of a describe block or file — not inside a hook.`],[`D`,`B`,'A test fails with "myFixture is not a function". The fixture is defined with `test.extend()`. What is the most likely cause?',null,[`The fixture name must end with Fixture`,"The spec file imports `test` from '@playwright/test' instead of the extended test module",`extend() only works with TypeScript`,`The fixture is missing a return statement`],1,`The spec must import the extended test object — the base test doesn't know about myFixture.`],[`D`,`I`,`A worker-scoped DB fixture is reconnecting between every test, defeating the purpose. What is wrong?`,null,[`worker scope doesn't exist — use file scope instead`,`The fixture is not receiving { scope: 'worker' } in the second element of the array definition — it defaults to test scope`,`Workers share the fixture but each test gets a clone`,`The DB connection is stateless so it reconnects automatically`],1,"The scope must be explicitly set: `[async ({}, use) => { … }, { scope: 'worker' }]` — without the second element it defaults to test scope."],[`D`,`B`,"An `auto: true` fixture intended to freeze time isn't running in one test file. What is the likely cause?",null,[`auto only works in .spec.ts files not .test.ts files`,`The spec imports the base test instead of the extended test — auto fixtures only apply to the extended test object`,`auto: true requires a special flag in the config`,`The fixture must be listed in the test body even with auto: true`],1,`auto: true is relative to the extended test — importing the base test bypasses all custom fixtures including auto ones.`],[`D`,`I`,"Teardown code after `await use()` is not running — the test passes but the DB row is not deleted. What should you check?",null,[`Teardown only runs on test failure`,`The fixture must return a cleanup function separately`,`An unhandled promise rejection in the test body is swallowing the teardown`,`Teardown runs synchronously — remove await`],2,`If an unhandled error bypasses the fixture's use() boundary the teardown may not execute — use try/finally around use() to guarantee cleanup.`],[`D`,`I`,"`test.use({ storageState: 'auth.json' })` is set at the file level but one specific test still hits the login page. What is the likely cause?",null,[`test.use() only applies to the first test in a file`,`The test explicitly overrides storageState with a different value inside a nested test.use()`,`auth.json was not created by globalSetup`,`storageState only works for page fixtures not context fixtures`],1,`A nested test.use() or a project-level override can shadow the file-level storageState.`],[`D`,`A`,"`beforeAll` creates a shared page object but it is undefined in the tests. Why?",null,[`beforeAll cannot be async`,`The page variable is declared with const and is not accessible in closures`,`Variables declared inside beforeAll are scoped to it — share state via a let variable declared in the describe block`,`beforeAll runs after the first test`],2,`Declare variables with let at describe scope, then assign them inside beforeAll so all tests in the block can access them.`],[`C`,`B`,"Complete to create a custom fixture.\n```ts\nconst test = base.______({ myPage: async ({ page }, use) => {\n  await use(page);\n} });\n```",null,[`extend`,`add`,`register`,`create`],0,`base.extend() merges new fixtures into a new test function.`],[`C`,`I`,`Complete the teardown: setup runs before use, teardown after.
\`\`\`ts
async ({ page }, use) => {
  await page.goto('/app');
  await ______(page);
  await page.close();  // teardown
}
\`\`\``,null,[`use`,`done`,`next`,`provide`],0,`await use(page) is where the test runs; page.close() after it is the teardown.`],[`C`,`I`,"Complete the scope for a fixture shared across a worker.\n```ts\n[async ({}, use) => { … }, { scope: '______' }]\n```",null,[`worker`,`global`,`shared`,`process`],0,`scope: 'worker' shares one instance across all tests running in the same worker.`],[`C`,`I`,"Complete to override storageState for an entire spec file.\n```ts\ntest.______({ storageState: 'admin.json' });\n```",null,[`use`,`set`,`configure`,`apply`],0,`test.use() at file level overrides fixtures for every test in the file.`],[`C`,`A`,`Complete to make a fixture run for every test automatically.
\`\`\`ts
[async ({ page }, use) => {
  await page.clock.setFixedTime(new Date('2024-01-01'));
  await use();
}, { ______: true }]
\`\`\``,null,[`auto`,`always`,`required`,`default`],0,`auto: true runs the fixture for every test without needing to be listed in destructuring.`],[`L`,`B`,"Why must you always import the extended `test` object rather than the base one in spec files?",null,[`The base test object is deprecated`,`The base test doesn't know about custom fixtures — using it causes "unknown fixture" errors`,`The extended test has better TypeScript types only`,`The base test cannot be async`],1,`Custom fixtures only exist on the object returned by test.extend() — the base has none of them.`],[`L`,`I`,"When should you use `scope: 'worker'` for a fixture?",null,[`When the fixture must be isolated per test`,`When the setup is expensive (e.g., DB connection, compiled assets) and safe to share across tests in a worker`,`When the fixture needs access to the browser page`,`When auto: true is set`],1,`Worker scope amortises expensive setup across multiple tests — safe only for stateless or thread-safe resources.`],[`L`,`I`,"A fixture has a `try/finally` block. What does putting teardown in `finally` guarantee?",null,[`The teardown runs before the test starts`,`The teardown runs even if the test throws or the page crashes`,`finally blocks are not needed in fixtures`,`finally runs only on test failure`],1,`Without try/finally, an error in the test could cause the fixture to skip teardown and leak resources.`],[`L`,`B`,"What is the key difference between fixtures and `beforeEach` hooks for cross-file reuse?",null,[`Fixtures are faster than hooks`,`Fixtures are defined in a separate module and imported; hooks are file-local and cannot be shared across spec files without manual importing`,`Hooks run before fixtures in the lifecycle`,`beforeEach does not support async operations`],1,`Fixtures live in modules and compose naturally; beforeEach hooks must be manually duplicated or imported into each file.`],[`L`,`A`,"A fixture calls `await use()` with no argument. What does the test receive?",null,[`An error — use() requires a value`,`undefined — the fixture is used for its side effects only`,`null — Playwright normalises missing values`,`The fixture object itself`],1,`use() with no argument is valid — the fixture runs for side effects (e.g. clock freezing, interceptor setup) and the test receives nothing.`],[`R`,`B`,`You need every test in a file to start logged in as an admin. What is the cleanest approach?`,null,[`Add await page.goto('/login') to every test`,`Use beforeEach to log in before each test`,`Add test.use({ storageState: 'admin.json' }) at the top of the file`,`Create a global beforeAll in playwright.config.ts`],2,`test.use() with a pre-saved storageState is the zero-cost approach — no login per test.`],[`R`,`I`,`You have a database that is slow to initialise. How do you connect once per worker and reuse across tests?`,null,[`Use beforeAll in every spec file`,`Create a worker-scoped fixture: [async ({}, use) => { const db = await connect(); await use(db); await db.close(); }, { scope: 'worker' }]`,`Use globalSetup to connect and store on process.env`,`Import the DB module at the top of each spec file`],1,`Worker scope creates one DB connection per worker process — efficient and isolated.`],[`R`,`I`,`You want a fixture that sets up request interception on every test without the test requesting it. Which property do you use?`,null,[`required: true`,`auto: true`,`always: true`,`default: true`],1,`auto: true runs the fixture for every test that uses the extended test object.`],[`R`,`A`,"A custom `adminPage` fixture needs both `page` and a custom `db` fixture. How do you declare the dependency?",null,[`Import db directly and call db.connect() inside the fixture`,`List db in the fixture's first argument: async ({ page, db }, use) => { … }`,`Pass db as a third parameter after use`,`Use test.use({ db }) before the fixture definition`],1,`Fixtures declare dependencies by name in the first argument — Playwright resolves and injects them.`],[`R`,`I`,`You want cleanup (delete test data) to run after every test even if the test crashes. Where do you put the cleanup?`,null,[`In afterAll — it runs after all tests`,`In the fixture after await use(), wrapped in try/finally`,`In the test body in a .catch() block`,`In a separate teardown file pointed to by globalTeardown`],1,`Fixture teardown after use() with try/finally is guaranteed to run regardless of test outcome.`],[`S`,`I`,`A new team member copies the fixture file but their tests get "unknown fixture" errors. What did they miss?`,null,[`They need to register the fixture in playwright.config.ts`,"They imported `test` from '@playwright/test' instead of from the fixture module",`Fixtures must be declared in every spec file`,`The fixture file needs to be in the tests/ directory`],1,`Custom fixtures only exist on the extended test — import from the fixture module, not from @playwright/test.`],[`S`,`A`,`You have 50 spec files that all need to be logged in as different users. How do you avoid duplicating test.use() in every file?`,null,[`Set storageState globally in playwright.config.ts use block, then override per-file with test.use()`,`Use beforeAll in a shared setup file`,`Playwright does not support per-file storageState — use a single admin user`,`Use auto: true on a login fixture`],0,`Set a default in config use, then each file overrides with test.use({ storageState: 'user-X.json' }) only when needed.`],[`S`,`I`,`A worker-scoped fixture for a shared resource is still creating a new instance per test. What is the most likely reason?`,null,[`Worker scope does not work with custom fixtures`,`fullyParallel: true assigns each test its own worker, so 'worker' scope acts like 'test' scope`,`The resource is stateful and Playwright forces a fresh instance`,`Worker scope requires the fixture to be in a separate file`],1,`With fullyParallel: true each test can run in a different worker — worker-scoped fixtures behave like test-scoped when tests don't share a worker.`],[`S`,`A`,`A fixture that creates a temporary directory leaks on test failure. How do you fix this?`,null,[`Use afterAll to clean up temp directories`,`Wrap use() in try/finally: create dir → await use(dir) → in finally: remove dir`,`Add error handling in the test body`,`Set scope: 'worker' so the directory persists for cleanup`],1,`try/finally around use() guarantees cleanup runs even when the test throws.`],[`O`,`B`,"What is the purpose of `use()` in a Playwright fixture definition?",null,[`Sets default values for the fixture`,`Marks the boundary between setup and teardown — code before use() runs before the test, code after runs after the test`,`Provides the fixture value to dependent fixtures and the test`,`Both B and C — use() delivers the value AND marks the setup/teardown boundary`],3,`use() is the pivot point: it delivers the value to the test AND signals where teardown begins.`],[`O`,`I`,'What is the difference between `scope: "test"` and `scope: "worker"` for a fixture?',null,[`test scope is for browser fixtures; worker scope is for Node.js fixtures`,`test scope creates and destroys the fixture for every individual test; worker scope creates it once per worker process and shares it across all tests in that worker`,`worker scope is faster because it uses the same browser session`,`test scope is the default; worker scope must be declared explicitly`],1,`Scope determines lifecycle — test scope means N setups for N tests; worker scope means 1 setup per worker regardless of test count.`],[`O`,`B`,"What does `auto: true` do when added to a fixture options object?",null,[`The fixture automatically calls use()`,`The fixture is automatically used by every test without being listed in the test's argument list — useful for global setup like logging or auth`,`The fixture automatically reruns on failure`,`auto: true enables strict mode for the fixture`],1,`auto fixtures run silently for every test in scope — perfect for things like screenshot-on-failure or request logging that every test should benefit from.`],[`O`,`I`,`Can a test-scoped fixture depend on a worker-scoped fixture?`,null,[`No — scope dependencies must always be equal`,`Yes — a narrower-scoped fixture can depend on a wider-scoped one (test can use worker-scoped). The reverse is not allowed.`,`Yes — but only if both use the same browser type`,`Only if the worker fixture is declared first`],1,`Fixtures can depend on fixtures of equal or wider scope — a test fixture can use a worker fixture, but a worker fixture cannot depend on a test-scoped fixture.`],[`O`,`B`,"What is the return value of `test.extend(fixtures)`?",null,[`A fixture registry object`,`A new test object with all the custom fixtures merged in — used in place of the standard test import`,`A function that registers the fixtures globally`,`The original test object modified in place`],1,`test.extend() returns a NEW test object — import and use this extended test in your spec files to access the custom fixtures.`],[`O`,`I`,`What happens to the fixture teardown if a test throws an error?`,null,[`Teardown is skipped when a test throws`,`Teardown runs regardless — code after await use() always executes, even if the test body threw. This is why fixtures are safer than afterEach hooks that can be skipped.`,`Teardown only runs for the next test`,`The test runner waits for all pending teardowns before reporting the failure`],1,`Fixture teardown is guaranteed — it runs whether the test passed, failed, or threw. This reliability is one of the key advantages over manual beforeEach/afterEach.`],[`O`,`A`,'What does `test.use({ storageState: "admin.json" })` do when called at the describe level?',null,[`Overrides the storageState for all tests globally`,`Overrides the storageState only for tests within that describe block — a scoped configuration override that does not affect tests outside the block`,`Sets storageState as a worker-scoped fixture`,`Reads storageState from admin.json and applies it to the context fixture`],1,`test.use() is scoped — at describe level it applies to tests in that block only; at file level it applies to all tests in the file.`],[`O`,`I`,"What does the `browserName` built-in fixture provide?",null,[`The full browser version string`,`A string identifying the current browser engine: "chromium", "firefox", or "webkit" — useful for browser-specific conditional logic`,`The path to the browser executable`,`The number of open browser instances`],1,`browserName is the engine identifier — use it to skip browser-specific tests: test.skip(browserName === "webkit", "Not supported in WebKit").`],[`O`,`B`,`Which built-in fixture provides an authenticated APIRequestContext scoped to the current test's base URL?`,null,[`fetch`,`http`,`request`,`apiClient`],2,`The request fixture is a built-in APIRequestContext pre-configured with the test's baseURL and context credentials.`],[`O`,`I`,`Can fixture names shadow built-in fixture names?`,null,[`No — built-in names are reserved`,`Yes — a custom fixture named "page" overrides the built-in page fixture for tests that use the extended test object`,`Only if scope is set to "worker"`,`Yes but only for the fixture file, not test files`],1,`Overriding a built-in fixture is a powerful pattern — you can add setup logic to page while still using the standard page API inside the override.`],[`O`,`A`,"What does `test.extend<{}, WorkerFixtures>(workerFixtureDefinitions)` achieve in TypeScript?",null,[`Adds type annotations for documentation only`,`Correctly types worker-scoped fixtures separately from test-scoped fixtures — the second generic type parameter is for worker fixtures, ensuring TypeScript knows their lifecycle`,`Prevents test fixtures from being used in worker scope`,`Creates a separate test object for worker tests`],1,`Correctly typing fixtures with two generics (TestFixtures, WorkerFixtures) helps TypeScript catch scope mismatches at compile time.`],[`O`,`I`,`What is a fixture composition and why is it useful?`,null,[`Combining multiple files into one fixture module`,`Defining a fixture that depends on other custom fixtures — e.g. an adminPage fixture that depends on both page and a logged-in user fixture. Composition builds complex setups from smaller reusable pieces.`,`Using the same fixture in multiple spec files`,`Extending the test object more than once`],1,`Composition avoids duplicating setup logic — each fixture does one thing and complex setups are assembled from simple pieces.`],[`F`,`B`,`Complete the fixture definition structure.
\`\`\`ts
const test = base.extend({
  myFixture: async ({}, ______) => {
    await use("value");
  },
});
\`\`\``,null,[`use`,`inject`,`provide`,`setup`],0,`The second parameter to the fixture function is always use — the function that delivers the value to the test.`],[`F`,`B`,`Complete to mark a fixture as auto-use.
\`\`\`ts
auditLogger: [async ({}, use) => {
  startLog();
  await use();
  stopLog();
}, { ______: true }],
\`\`\``,null,[`auto`,`always`,`global`,`required`],0,`auto: true makes the fixture run automatically for every test without being listed in the test's argument.`],[`F`,`I`,`Complete to declare a worker-scoped fixture.
\`\`\`ts
sharedDb: [async ({}, use) => {
  const db = await connect();
  await use(db);
  await db.disconnect();
}, { scope: "______" }],
\`\`\``,null,[`worker`,`test`,`file`,`global`],0,`scope: "worker" creates the fixture once per worker process — all tests in the worker share the same db connection.`],[`F`,`I`,`Complete to override a built-in fixture.
\`\`\`ts
const test = base.extend({
  page: async ({ page }, ______) => {
    await page.setExtraHTTPHeaders({ "X-Test": "true" });
    await use(page);
  },
});
\`\`\``,null,[`use`,`inject`,`provide`,`return`],0,`Overriding the page fixture: augment the built-in page then pass it through use() — all tests get the enhanced page automatically.`],[`F`,`B`,'Complete to get the current browser name in a test.\n```ts\ntest("check browser", async ({ ______ }) => {\n  console.log(browserName); // "chromium" | "firefox" | "webkit"\n});\n```',null,[`browserName`,`browser.name`,`currentBrowser`,`engineName`],0,`browserName is a built-in fixture that provides the engine name string.`],[`F`,`I`,`Complete to scope a configuration override to a describe block.
\`\`\`ts
describe("admin tests", () => {
  test.______({ storageState: "admin.json" });
  test("can delete", ...);
});
\`\`\``,null,[`use`,`configure`,`set`,`apply`],0,`test.use() at describe level overrides the fixture values for all tests in that block only.`],[`F`,`A`,"Complete to extend an existing extended test object.\n```ts\nconst test = base.extend({ db });\nconst extendedTest = test.______({ adminPage });\n```",null,[`extend`,`add`,`merge`,`compose`],0,`test.extend() can be called on an already-extended test object — composing layers of fixtures.`],[`E`,`B`,`What is wrong?
\`\`\`ts
const test = base.extend({
  myFixture: async ({}, use) => {
    const data = setup();
    // no await use()
  },
});
\`\`\``,null,[`setup() must be async`,`Missing await use() — without calling use(), the test never receives the fixture value and hangs indefinitely waiting for it`,`use() must be the last statement`,`setup() must return a Promise`],1,`Every fixture MUST call await use() — without it the test hangs. The fixture is the test's gate.`],[`E`,`I`,`What is wrong?
\`\`\`ts
const test = base.extend({
  workerData: [async ({}, use) => {
    const d = await heavySetup();
    await use(d);
  }, { scope: "worker" }],
});
// workerData used in 10 parallel tests
// heavySetup is called 10 times
\`\`\``,null,[`worker scope requires auto: true`,`fullyParallel: true assigns each test to a different worker — each worker creates its own instance of the worker-scoped fixture. This is expected behavior, not a bug. To share across all tests, use globalSetup instead.`,`Worker scope is shared across ALL workers`,`This is a Playwright bug with fullyParallel`],1,`Worker scope = one per worker process. With fullyParallel + 10 tests = 10 workers = 10 instances. For truly global shared state, use globalSetup.`],[`E`,`B`,'What is wrong?\n```ts\n// spec.test.ts\nimport { test } from "@playwright/test"; // built-in\ntest("my test", async ({ myFixture }) => { // unknown fixture\n```',null,[`myFixture must be declared in the spec file`,`Importing from "@playwright/test" gives the base test without custom fixtures — import from your extended test module: import { test } from "./fixtures"`,`Fixtures cannot be used in test files directly`,`myFixture must be passed as a parameter to test()`],1,`The most common fixture mistake: importing from the wrong module. Always import the extended test where custom fixtures are defined.`],[`E`,`I`,`What is wrong?
\`\`\`ts
const test = base.extend<{ page: Page }>({
  page: async ({ page, mySetup }, use) => {
    await use(page);
  },
});
\`\`\``,null,[`You cannot override page`,`The TypeScript generic only declares page, but mySetup is also used — TypeScript will complain that mySetup is not in the fixture types. Add mySetup to the generic type parameter.`,`page override must not use other fixtures`,`extend takes only one generic parameter`],1,`All fixtures used in a definition must be declared in the generic type — mySetup needs to be in the types.`],[`E`,`A`,`What is wrong?
\`\`\`ts
const test = base.extend({
  db: [async ({}, use) => {
    await use(await connect());
    await db.disconnect(); // db is not in scope
  }, { scope: 'worker' }],
});
\`\`\``,null,[`worker scope cannot have teardown`,`db is not in scope after await use() — store the connection in a variable BEFORE use(): const conn = await connect(); await use(conn); await conn.disconnect()`,`disconnect must be called before use()`,`Teardown is not supported for worker fixtures`],1,`The value passed to use() must be stored in a local variable to be accessible in teardown.`],[`E`,`I`,`What is wrong?
\`\`\`ts
const test = base.extend({
  screen: async ({ page, screen }, use) => {
    // circular dependency
  },
});
\`\`\``,null,[`screen is a valid built-in fixture`,`A fixture cannot depend on itself — circular dependencies cause a stack overflow. Rename one or remove the self-reference.`,`screen is a reserved name`,`circular dependencies are allowed in worker scope`],1,`Self-referencing fixtures create an infinite dependency loop — Playwright throws a circular dependency error.`],[`D`,`B`,`A fixture is defined but never runs. The test works but the expected setup is missing. Why?`,null,[`The fixture file has a typo`,`The test is importing from @playwright/test instead of the custom test module — the extended test object contains the fixture; the base test does not`,`Fixtures require auto: true to run`,`The fixture is defined after the test`],1,`Wrong import is the most common fixture debugging issue — verify the import path in the spec file.`],[`D`,`I`,`A worker-scoped fixture's teardown is not running. What should you check?`,null,[`Worker fixtures have no teardown`,`Check that code exists after await use() in the fixture — without a teardown block, nothing runs after the test. Also verify the fixture is actually used (not just defined) — unused fixtures do not run.`,`Teardown only runs on test failure`,`Teardown requires finally block`],1,`Teardown code must exist after use() AND the fixture must be consumed by a test — orphaned fixtures (defined but unused) never execute.`],[`D`,`B`,"`test.use({ viewport: { width: 375 } })` has no effect on a test in the same file. What are two possible causes?",null,[`viewport requires a height too`,`1) test.use() must be called at the top level of the describe/file scope — if inside a test function, it has no effect. 2) A project-level viewport override takes precedence over test.use() in some configurations — check the project config.`,`viewport requires headless: false`,`test.use() is deprecated`],0,`test.use() placement and config precedence are the two most common causes — it must be at file/describe scope, not inside test().`],[`D`,`A`,`A fixture that opens a database connection is creating too many connections in a parallel run. How do you fix it?`,null,[`Add connection pooling in the fixture`,`Change scope from "test" to "worker" — one connection per worker instead of one per test dramatically reduces total connections in a parallel run`,`Set workers: 1 to limit concurrency`,`Use globalSetup to create a single connection`],1,`Test scope × N parallel tests = N connections. Worker scope × M workers = M connections (M << N typically). Worker scope is the right fix.`],[`D`,`I`,`A test expects a fixture to provide an array but receives undefined. What is the likely cause?`,null,[`Arrays cannot be fixture values`,`The fixture calls use() without a value: await use() instead of await use(myArray). Always pass the value to use().`,`Arrays must be wrapped in an object`,`use() must be called twice for array fixtures`],1,`use() with no argument delivers undefined — always pass the fixture value as the argument.`],[`C`,`B`,`Complete the minimal fixture definition.
\`\`\`ts
const test = base.extend({
  greeting: async ({}, use) => {
    await ______("Hello");
  },
});
\`\`\``,null,[`use`,`provide`,`inject`,`return`],0,`use() with the value delivers it to the test or dependent fixture.`],[`C`,`I`,`Complete to make a fixture auto-run for every test.
\`\`\`ts
requestLogger: [async ({ request }, use) => {
  const logs = [];
  // intercept requests...
  await use();
  saveLog(logs);
}, { auto: ______ }],
\`\`\``,null,[`true`,`false`,`undefined`,`1`],0,`auto: true makes the fixture run without being listed in any test argument.`],[`C`,`I`,`Complete to add teardown to a fixture.
\`\`\`ts
myFixture: async ({}, use) => {
  const resource = await create();
  await use(resource);
  ______ resource.cleanup();
},
\`\`\``,null,[`await`,`return`,`yield`,`finally`],0,`Code after await use() is the teardown — await the cleanup if it is async.`],[`C`,`B`,'Complete to access the context fixture.\n```ts\ntest("test", async ({ ______ }) => {\n  const cookies = await context.cookies();\n});\n```',null,[`context`,`browser`,`page`,`request`],0,`context is a built-in fixture providing the BrowserContext for the current test.`],[`C`,`A`,`Complete to type a fixture with both test and worker generics.
\`\`\`ts
const test = base.extend<
  { loginPage: LoginPage }, // test scope
  { ______: Database }     // worker scope
>({ ... });
\`\`\``,null,[`sharedDb`,`db`,`database`,`workerDb`],0,`The second generic type parameter lists worker-scoped fixtures — any name matching your fixture definition is valid.`],[`L`,`B`,`Why are fixtures preferred over beforeEach/afterEach for shared setup?`,null,[`Fixtures are faster because they skip setup for tests that don't need them`,`Fixtures are self-contained (setup + teardown in one place), composable (depend on each other), type-safe, scoped (test/worker), and only run when a test actually uses them — beforeEach always runs`,`Fixtures cannot throw errors that affect tests`,`beforeEach is deprecated`],1,`Fixtures compose, scope, and type cleanly — they are the evolution of beforeEach/afterEach into a structured dependency system.`],[`L`,`I`,`Can two different fixture files extend the same base test separately and then be composed?`,null,[`No — the base test can only be extended once`,`Yes — extend the base test in each file to add its fixtures, then extend the RESULT of one in the other: const combined = firstExtended.extend(secondFixtures)`,`Yes — they merge automatically when both are imported`,`Only if they use different fixture names`],1,`Fixture composition by chaining extend() calls allows modular fixture organisation — each domain (auth, db, api) in its own file.`],[`L`,`I`,`What is the execution order of a fixture chain A → B → test → B teardown → A teardown?`,null,[`All setup runs, then the test, then all teardown in reverse`,`Setup A → Setup B → test → Teardown B → Teardown A — like nested LIFO (last in, first out). Innermost fixture tears down first.`,`All setups and teardowns interleave with the test`,`A and B setup in parallel, then the test`],1,`Fixture teardown is LIFO — the last fixture to set up is the first to tear down. This ensures dependencies are still alive during teardown of dependents.`],[`L`,`B`,`When does a worker-scoped fixture's teardown run?`,null,[`After every test in the worker`,`Once after all tests assigned to that worker complete — not between individual tests`,`At the end of the entire test suite`,`Only if the worker encounters a test failure`],1,`Worker-scoped teardown runs once per worker's lifetime — after the last test assigned to it finishes.`],[`L`,`A`,`Can auto fixtures access other fixtures?`,null,[`No — auto fixtures receive no arguments`,`Yes — auto fixtures can depend on any fixture of equal or wider scope in their first argument, just like regular fixtures. { page } in an auto fixture gives access to the test's page.`,`Only built-in fixtures are accessible`,`Only worker-scoped fixtures`],1,`Auto fixtures are regular fixtures with auto: true — they have full access to the fixture system including page, context, and custom fixtures.`],[`R`,`B`,`You want every test to automatically capture a screenshot on failure. How do you implement this?`,null,[`Add screenshot code to every afterEach`,`Create an auto fixture: screenshot: [async ({ page }, use) => { await use(); // after test if (test.info().status !== 'passed') { await page.screenshot({ path: test.info().outputPath("failure.png") }); } }, { auto: true }]`,`Set screenshot: "on-failure" in the config`,`Use test.afterEach globally in conftest`],0,`An auto fixture with teardown that checks testInfo.status is the programmatic screenshot-on-failure pattern — captures exactly what you need when you need it.`],[`R`,`I`,`You need a fixture that provides a unique test user for each test, cleaned up after. How do you structure this?`,null,[`Use beforeAll to create all users upfront`,`Test-scoped fixture: create a user in setup, await use(user), delete the user in teardown. Each test gets its own isolated user with automatic cleanup.`,`Use a global variable for the current user`,`Worker-scoped fixture with user rotation`],1,`Test-scoped fixture with CRUD in setup/teardown is the clean pattern — each test is isolated and cleanup is guaranteed.`],[`R`,`A`,`You need shared expensive computation (e.g. compiled regex patterns) available to all tests across workers. What is the cleanest approach?`,null,[`Worker-scoped fixture — but it runs once per worker, not once globally`,`Compute it in globalSetup and pass via a file or environment variable — worker fixtures run once per worker but globalSetup runs once total. For truly global shared results, use the file system.`,`Test-scoped fixture that memoises the result`,`Page.evaluate() to compute it in the browser`],1,`globalSetup is the only truly once-total execution — write the result to a temp file and read it in a worker fixture.`],[`R`,`I`,`A team has fixtures for db, api, and browser spread across three files. How do you make all three available in every spec?`,null,[`Import all three in every spec file`,`Create a combined fixture index file: const test = dbTest.extend(apiFixtures).extend(browserFixtures) and export it — import from this combined module in all specs`,`Register fixtures in playwright.config.ts`,`Use require() at the top of each spec`],1,`One combined module is the standard pattern — a single import gives access to all fixture domains.`],[`R`,`B`,"You want the `request` fixture to have a custom base URL different from the page. How?",null,[`Override request in test.use()`,`Extend the test with a custom apiRequest fixture: apiRequest: async ({}, use) => { const ctx = await request.newContext({ baseURL: "https://api.example.com" }); await use(ctx); await ctx.dispose(); }`,`Set a second baseURL in the config`,`Use fetch() in tests instead`],1,`A custom request fixture with its own baseURL keeps API and UI base URLs separate — both can coexist.`],[`S`,`B`,`A test says "fixture 'db' is not declared in the fixture configuration". What is the most likely problem?`,null,[`The db fixture does not exist`,`The spec imports from @playwright/test instead of the extended test module that defines db — change the import to the custom test object`,`Fixtures must be declared in playwright.config.ts`,`The db fixture has a typo in its name`],1,`Wrong import is responsible for 90% of "fixture not found" errors — always import from the module where test.extend() defines it.`],[`S`,`I`,`Two spec files both import an extended test with a "page" override. In one file it works; in the other the original page is used. Why?`,null,[`Page override only works once per test run`,`The working file imports from the fixture module; the broken file imports from @playwright/test accidentally — the extended test is in scope in only one file`,`Page override is scoped to the describe block`,`The override requires headless: false`],1,`Import source is the most common cause of inconsistent fixture behavior — audit imports in both files.`],[`S`,`A`,`A fixture has both setup and teardown but you notice teardown never runs in CI. What is the most likely CI-specific cause?`,null,[`CI kills the Node.js process before teardown`,`A SIGTERM from CI is killing the worker process before fixture teardown completes. Increase the CI timeout or ensure graceful shutdown. Alternatively, the teardown code throws synchronously before the async operation completes.`,`Teardown is disabled in headless mode`,`CI has a different Node.js version`],1,`Process termination and synchronous teardown errors are the main CI-specific causes — add logging in teardown to diagnose.`],[`S`,`I`,`You want a fixture that provides a different database depending on the environment (dev vs staging). How?`,null,[`Create two fixtures named devDb and stagingDb`,`Read the environment in the fixture: const dbUrl = process.env.ENV === "staging" ? STAGING_DB : DEV_DB — return a connection to the correct database. One fixture, runtime configuration.`,`Create two playwright.config.ts files`,`Set the DB URL in beforeAll`],1,`Reading process.env inside the fixture is the clean solution — the fixture adapts at runtime without duplicating code.`],[`O`,`A`,`What does this custom fixture provide to every test that uses it?`,`export const test = base.extend({
  loggedInPage: async ({ page }, use) => {
    await page.goto('/login');
    await page.getByLabel('Username').fill('admin');
    await page.getByLabel('Password').fill('secret');
    await page.getByRole('button', { name: 'Log in' }).click();
    await use(page);
  },
});`,["A ready-to-use, already-logged-in `page` object — any test that destructures `{ loggedInPage }` gets this setup run automatically before the test body, without repeating the login steps in every test",`It runs the login flow once for the entire test suite, sharing one session across all tests`,`It only works if imported alongside the built-in @playwright/test module, with no other setup required`,`Fixtures can only wrap existing built-in fixtures, never create genuinely new named ones`],0,"test.extend() is the mechanism for building reusable, named setup/teardown units — the `use(page)` call is where the actual test body executes, with everything before it as setup and (if present) after it as teardown."],[`L`,`A`,'What is the difference between a fixture with `{ scope: "test" }` (the default) and one with `{ scope: "worker" }`?',null,[`A test-scoped fixture is created fresh for EVERY test; a worker-scoped fixture is created ONCE per worker process and reused across all tests that worker runs — ideal for expensive setup (like starting a database connection) that doesn't need to be repeated per test`,`worker-scoped fixtures run once for the entire CI pipeline, across all workers combined`,`scope only affects which browser the fixture runs in, not its lifetime`,`test-scoped fixtures persist across retries; worker-scoped fixtures do not`],0,`Worker scope trades some isolation for performance — appropriate for read-only or expensive-to-create resources that are safe to share across multiple tests within the same worker.`],[`O`,`A`,`What does this "auto" fixture accomplish?`,`export const test = base.extend({
  forEachTest: [async ({ page }, use) => {
    console.log('starting test');
    await use();
    console.log('finished test');
  }, { auto: true }],
});`,["It runs automatically for every test that uses this extended `test`, WITHOUT needing to be explicitly destructured in the test signature — useful for cross-cutting concerns like logging or setup that every test needs implicitly",`It only runs if the test file explicitly imports and calls forEachTest() manually`,`auto: true makes the fixture run in parallel with the test body instead of before it`,`Auto fixtures replace the need for beforeEach/afterEach entirely across the whole codebase`],0,`{ auto: true } is specifically for fixtures that should apply universally without every test needing to remember to request them by name.`],[`O`,`A`,"What does this fixture override change compared to the built-in `page` fixture?",`export const test = base.extend({
  page: async ({ page }, use) => {
    page.on('console', msg => {
      if (msg.type() === 'error') console.log('Browser console error:', msg.text());
    });
    await use(page);
  },
});`,["Every test using this extended `test` automatically gets browser console errors logged to the test output — the override wraps the built-in page fixture with extra behavior, rather than replacing it entirely",`It creates a completely separate, second page object alongside the original`,`Overriding a built-in fixture name like "page" is not allowed by Playwright`,`It disables the actual browser page from loading any real content`],0,`Overriding a built-in fixture by the same name lets you layer additional behavior (like console log capturing) onto every test's page automatically, without touching each individual test file.`],[`O`,`I`,`What runs, and in what order, when this fixture is used?`,`export const test = base.extend({
  tempFile: async ({}, use) => {
    const path = await createTempFile();
    await use(path);
    await deleteTempFile(path);
  },
});
test('writes to temp file', async ({ tempFile }) => {
  await writeToFile(tempFile, 'data');
});`,[`createTempFile() runs first (setup), then the test body runs using that path, then deleteTempFile() runs after the test finishes (teardown) — regardless of whether the test passed or failed`,`deleteTempFile() runs before the test body, cleaning up a PREVIOUS run's file`,`Only the setup half runs; fixtures have no way to express teardown`,`The order is nondeterministic and varies between runs`],0,"Code before `use()` is setup, the test body runs during `use()`, and code after `use()` is teardown — this pattern (like a try/finally) guarantees cleanup runs even if the test fails."],[`R`,`I`,`You need three different tests to each start with a different pre-seeded shopping cart state (empty, one item, ten items). How would you structure this with fixtures rather than repeating setup code in each test?`,null,["Create a parametrized/composable fixture (or a small set of named fixtures like `emptyCart`, `cartWithOneItem`, `cartWithTenItems`) that each test destructures based on which starting state it needs, keeping the setup logic centralized and the test bodies focused on the actual assertions",`Copy-paste the full seeding code at the top of each of the three test bodies`,`Use a single shared fixture and have each test manually mutate it to the state it needs, hoping order does not matter`,`Fixtures cannot vary between tests in the same file; this requires three separate files`],0,`Multiple small, named fixtures (rather than one generic one with manual mutation) keep each test's intent explicit and avoid inter-test coupling through shared mutable setup.`],[`D`,`A`,`A worker-scoped fixture that opens a shared database connection starts producing "connection already closed" errors partway through a large parallel run. What is the most likely design issue?`,`export const test = base.extend({
  db: [async ({}, use) => {
    const conn = await connectDb();
    await use(conn);
    await conn.close();
  }, { scope: 'worker' }],
});`,[`Multiple tests within the same worker share this ONE connection concurrently — if any test (or a bug elsewhere) closes it early, or if the underlying driver isn't safe for concurrent use across tests reusing the same worker, later tests in that worker fail; the fix is ensuring the shared resource is genuinely safe for reuse across the whole worker's lifetime, or narrowing the fixture to test scope if it can't be`,`Worker-scoped fixtures are recreated for every single test, so this error should be impossible`,`The fix is to always use scope: "test" for every fixture without exception`,`Database connections cannot be used inside fixtures under any circumstances`],0,`Worker-scoped sharing is a performance optimization with a real tradeoff: the shared resource's lifetime and thread-safety assumptions must hold for the ENTIRE worker, not just one test.`],[`O`,`A`,`What does the options object here change about this fixture’s behavior?`,`const test = base.extend({
  seedDatabase: [async ({}, use) => {
    await runMigrationsAndSeed();
    await use();
  }, { timeout: 60000 }],
});`,[`It gives THIS specific fixture up to 60 seconds to complete its setup phase, independent of the overall test timeout — useful for a genuinely slow one-time operation that would otherwise be cut off by a shorter default fixture timeout`,`It limits the fixture to running only once every 60 seconds across the whole suite`,`timeout here refers to how long the TEST body itself is allowed to run`,`{ timeout } is not a valid fixture option; this code throws a config error`],0,`Fixtures can have their own timeout separate from the test’s overall timeout — important when one particular fixture (like a slow DB seed) genuinely needs more time than the default budget allows.`],[`O`,`A`,`What does box: true change about how this fixture appears in the report?`,`const test = base.extend({
  authenticatedPage: [async ({ page }, use) => {
    await page.goto('/login');
    await page.getByLabel('Email').fill('user@test.com');
    await page.getByRole('button', { name: 'Log in' }).click();
    await use(page);
  }, { box: true }],
});`,[`The fixture’s internal steps (goto, fill, click) are collapsed/hidden as implementation detail in the trace viewer and HTML report, showing just the fixture as a single named step instead of cluttering the report with every internal action`,`box: true wraps the fixture in a literal sandboxed iframe for isolation`,`It disables retries for this specific fixture only`,`box: true forces the fixture to run before all other fixtures regardless of dependency order`],0,`box: true is a reporting/trace-clarity tool — it lets you build detailed, multi-step fixtures without flooding the report with steps that are really just "how the fixture does its job", not something a test author usually needs to inspect.`],[`O`,`A`,`What pattern does the option:true flag enable here?`,`const test = base.extend({
  defaultLocale: ['en-US', { option: true }],
  page: async ({ page, defaultLocale }, use) => {
    await page.evaluate((l) => localStorage.setItem('locale', l), defaultLocale);
    await use(page);
  },
});`,[`defaultLocale is defined as a plain configurable VALUE (with a default) rather than a resource with setup/teardown — tests or projects can override it via test.use({ defaultLocale: 'fr-FR' }) to parameterize behavior, similar to how built-in options like viewport work`,`option: true means the fixture can only be used by other fixtures, never directly by a test`,`It marks the fixture as required — every test MUST explicitly request defaultLocale by name`,`option: true disables the default value, forcing every test to supply one explicitly`],0,`The option:true pattern is how you build your own configurable, project/test-overridable parameters — modeled after how Playwright’s own built-in options like baseURL and viewport work.`],[`O`,`A`,`What does this test.use() call change, given the defaultLocale option fixture defined above?`,`test.use({ defaultLocale: 'fr-FR' });
test('shows French UI', async ({ page }) => { /* ... */ });`,[`For tests in this file/describe scope, the defaultLocale fixture’s value becomes 'fr-FR' instead of its default 'en-US', so any fixture (like the page override) or test that depends on defaultLocale sees the overridden value`,`test.use() only works for built-in fixtures like storageState and viewport, never custom option fixtures`,`This throws an error because defaultLocale is not a resource fixture with setup/teardown`,`It creates a brand new fixture named defaultLocale scoped only to this test, ignoring the original definition`],0,`test.use() overrides ANY fixture value — built-in or custom — as long as that fixture supports being overridden this way, which option-style fixtures are specifically designed for.`],[`O`,`A`,`What is this fixture doing with testInfo.attach()?`,`const test = base.extend({
  apiLogger: [async ({}, use, testInfo) => {
    const logs = [];
    // ... logic that pushes request/response summaries into logs ...
    await use(logs);
    await testInfo.attach('api-log.json', {
      body: JSON.stringify(logs, null, 2),
      contentType: 'application/json',
    });
  }, { auto: true }],
});`,[`After the test finishes, it saves the collected API log as a named attachment on the test result — visible and downloadable directly from the HTML report, alongside screenshots and traces, for every test since this fixture is auto:true`,`testInfo.attach() only works inside the test body itself, so this code throws when called from a fixture`,`It attaches the log to the NEXT test that runs, not the current one`,`attach() sends the log data to an external logging service automatically`],0,`testInfo (available as a third argument to fixture functions) lets fixtures contribute custom artifacts to the report — extremely useful for auto-attaching diagnostic data to every test without each test having to remember to do it.`],[`O`,`A`,`What problem does testInfo.parallelIndex solve in this worker-scoped fixture?`,`const test = base.extend({
  isolatedDbName: [async ({}, use, workerInfo) => {
    const dbName = \`test_db_\${workerInfo.parallelIndex}\`;
    await createDatabase(dbName);
    await use(dbName);
    await dropDatabase(dbName);
  }, { scope: 'worker' }],
});`,[`Each worker process gets a UNIQUE, deterministic database name derived from its own parallel index — preventing multiple concurrently-running workers from colliding on the same shared database name when tests run in parallel`,`parallelIndex changes on every test within the same worker, giving each test its own database`,`It represents the total number of workers configured, not a per-worker identifier`,`workerInfo.parallelIndex is only available inside globalSetup, not regular fixtures`],0,`workerInfo.parallelIndex gives each worker a stable, unique number for exactly this purpose — allocating per-worker resources (ports, database names, temp directories) without collisions.`],[`O`,`A`,`If fixture B’s setup throws an error, and fixture A (which B depends on) already completed its own setup successfully, what happens to A?`,`const test = base.extend({
  a: async ({}, use) => { const r = await acquire(); await use(r); await release(r); },
  b: async ({ a }, use) => { throw new Error('b setup failed'); },
});
test('uses b', async ({ b }) => { /* never runs */ });`,[`A’s teardown (release(r)) still runs — Playwright tears down every fixture whose setup already succeeded, in reverse order, even if a later dependent fixture’s setup fails and the test itself never runs`,`A’s teardown is skipped entirely since the test never actually started`,`The whole worker process crashes and must be restarted from scratch`,`A must be manually torn down by the next test that uses it`],0,`Playwright guarantees teardown for every fixture that successfully finished setup, regardless of whether a sibling or dependent fixture failed afterward — resource leaks are avoided even on setup failure.`],[`O`,`A`,`What does overriding the built-in context fixture like this change for every test?`,`const test = base.extend({
  context: async ({ context }, use) => {
    await context.grantPermissions(['clipboard-read', 'clipboard-write']);
    await use(context);
  },
});`,[`Every test using this extended test object gets a BrowserContext that already has clipboard permissions granted, layered on top of the default context fixture — no individual test needs to remember to call grantPermissions() itself`,`It creates a second, separate context alongside the original built-in one`,`Overriding the context fixture by this name is disallowed and throws an error at extend() time`,`grantPermissions() only works when called directly in a test body, not inside a fixture override`],0,`Overriding a built-in fixture by name (context, page, browser) is a powerful, common pattern for layering cross-cutting setup onto every test without touching individual test files.`],[`O`,`A`,`Why does this fixture bypass the default browser/context fixtures entirely?`,`const test = base.extend({
  extensionContext: async ({}, use) => {
    const context = await chromium.launchPersistentContext('', {
      headless: false,
      args: ['--load-extension=./my-extension'],
    });
    await use(context);
    await context.close();
  },
});`,[`Testing a browser extension requires a PERSISTENT context launched with specific extension-loading arguments — this cannot be expressed through the default context fixture (which derives from a shared, non-persistent browser instance), so the fixture manages its own browser lifecycle from scratch`,`launchPersistentContext() is just an alias for the default context fixture with no real difference`,`This pattern is unnecessary; extensions can always be loaded through the standard context fixture’s options`,`Persistent contexts cannot be created inside a fixture, only inside globalSetup`],0,`Some advanced scenarios (browser extensions, specific launch flags incompatible with the shared browser fixture) require a custom fixture that manages its own browser/context lifecycle rather than composing the built-in ones.`],[`O`,`A`,`What resource-management problem does this worker-scoped fixture solve?`,`const test = base.extend({
  leasedAccount: [async ({}, use) => {
    const account = await accountPool.lease();
    await use(account);
    await accountPool.release(account);
  }, { scope: 'worker' }],
});`,[`A limited pool of test accounts (e.g. due to a rate-limited staging environment) is safely shared across many parallel tests — each worker leases one account for its own use and releases it back to the pool when the worker finishes, preventing two tests from fighting over the same account concurrently`,`It creates a brand-new account from scratch for every single test that requests it`,`accountPool.lease() blocks all other workers from running until release() is called`,`Worker scope here has no effect since accountPool would behave identically at test scope`],0,`A leasing/pool pattern with worker scope is the standard way to safely share a scarce, shared external resource (accounts, ports, sandbox slots) across a parallel test run without collisions.`],[`O`,`I`,`Why is this login approach typically faster than a UI-based login fixture?`,`const test = base.extend({
  apiAuthenticatedPage: async ({ page, request }, use) => {
    const res = await request.post('/api/login', { data: { user: 'test', pass: 'secret' } });
    const { token } = await res.json();
    await page.context().addCookies([{ name: 'session', value: token, url: 'https://app.example.com' }]);
    await use(page);
  },
});`,[`It skips rendering and interacting with the login form entirely — a single API call retrieves a valid session token which is injected directly as a cookie, avoiding the overhead of page navigation, form filling, and UI-driven waits that a full browser login flow requires`,`It is not actually faster; API calls take the same amount of time as UI interactions in Playwright`,`request.post() runs inside the browser’s JS engine, making it equivalent to a UI login`,`addCookies() triggers a full page reload internally, negating any speed benefit`],0,`API-based authentication fixtures are a common performance optimization — the login FLOW itself usually isn’t what a test cares about verifying, so skip it and start from an already-authenticated state.`],[`O`,`I`,"What happens when two imported fixture modules are spread together into a single extend() call like this, and both happen to define a fixture named `logger`?",`const test = base.extend({
  ...dbFixtures,   // includes logger
  ...apiFixtures,  // also includes logger
});`,[`Standard JavaScript object-spread semantics apply — the LAST spread object’s logger silently overwrites the earlier one’s, so only apiFixtures’ logger definition actually takes effect, with no warning or error`,`Playwright detects the name collision and throws a configuration error at extend() time`,`Both logger definitions are merged into one fixture that runs both implementations`,`The first spread object (dbFixtures) always wins regardless of order`],0,`Because extend() ultimately just receives a plain object, ordinary JS object-key-collision rules apply — spreading multiple fixture modules together requires the same vigilance about naming collisions as merging any other objects.`],[`O`,`A`,`How does chaining .extend() calls handle the same name-collision scenario differently from spreading?`,`const dbTest = base.extend(dbFixtures);   // includes logger
const test = dbTest.extend(apiFixtures);   // also includes logger`,[`The result is the same as spreading — the second extend() call’s logger fixture overrides the first, since each .extend() call layers new/overriding fixture definitions onto the test object returned by the previous one`,`Chaining always throws an error on any name collision, unlike spreading which silently allows it`,`Chained extends cannot both define fixtures with the same name under any circumstances`,`The first .extend() call’s fixtures always take precedence over later ones when chained`],0,`Whether you spread multiple fixture objects into one extend() call or chain multiple extend() calls, later definitions win on a name collision — the difference between the two styles is mostly about whether fixtures in different modules need to reference each other.`],[`O`,`A`,`What does auto:true combined with scope:'worker' achieve here?`,`const test = base.extend({
  mockApiServer: [async ({}, use) => {
    const server = await startMockServer();
    await use(server.url);
    await server.close();
  }, { scope: 'worker', auto: true }],
});`,[`A mock API server is started exactly once per worker process, automatically, without any test needing to explicitly destructure mockApiServer — every test running in that worker implicitly benefits from the mock server being up, and it is torn down once when the worker finishes all its assigned tests`,`auto:true and scope:'worker' cannot be combined; this throws a config error`,`The server starts fresh for every individual test despite the worker scope, because auto:true forces test-scope behavior`,`It starts one shared mock server across ALL workers combined, not one per worker`],0,`This combination is the standard pattern for "start an expensive shared dependency once per worker, and make sure every test automatically gets it without opting in explicitly."`],[`O`,`A`,`What does calling test.skip() from inside this fixture’s setup accomplish?`,`const test = base.extend({
  premiumFeatureFlag: async ({}, use, testInfo) => {
    const enabled = await isFeatureEnabled('premium-ui');
    test.skip(!enabled, 'Premium UI feature flag is disabled in this environment');
    await use(enabled);
  },
});`,[`If the feature flag is off, the test is dynamically skipped (reported as skipped, not failed) BEFORE the test body ever runs, based on a runtime check the fixture performed — centralizing that conditional-skip logic in one fixture rather than repeating it inside every test that needs it`,`test.skip() inside a fixture is not supported and this code throws at runtime`,`It permanently disables the premiumFeatureFlag fixture for all future test runs`,`The test still runs to completion, but its result is relabeled as skipped afterward`],0,`Fixtures can call test.skip() (and other test-control APIs) during their setup, which is a clean way to centralize environment/flag-dependent skip logic instead of duplicating an if-check in every affected test.`],[`O`,`A`,`Does the built-in request fixture automatically share cookies with the built-in page fixture’s browser context?`,`test('checks cookie sharing', async ({ page, request }) => {
  await page.goto('/login');
  // page is now logged in via cookies in its context
  const res = await request.get('/api/profile'); // uses request, not page
});`,[`No — the built-in request fixture is an independent APIRequestContext with its own cookie jar, NOT automatically tied to the page’s BrowserContext; to share cookies you would need to use page.context().request or manually pass cookies between them`,`Yes — request and page always share the exact same underlying browser context and cookies automatically`,`request only shares cookies if storageState is set in the config`,`Cookies are shared, but only for GET requests, never POST/PUT`],0,`This is a common source of confusion: the standalone request fixture is deliberately independent, while page.context().request specifically DOES share the page’s context (including cookies) if that’s what you need.`],[`O`,`A`,`What does branching on testInfo.project.name inside this fixture accomplish?`,`const test = base.extend({
  navPage: async ({ page }, use, testInfo) => {
    const NavPO = testInfo.project.name === 'mobile' ? MobileNavPage : DesktopNavPage;
    await use(new NavPO(page));
  },
});`,["The SAME fixture name (navPage) provides a different Page Object Model implementation depending on which project is currently running — tests can destructure `{ navPage }` without knowing or caring whether they’re running under the 'mobile' or 'desktop' project, since the fixture handles the branching",`testInfo.project.name is only available inside globalSetup, not in individual test fixtures`,`It causes the mobile and desktop projects to always run the exact same navPage implementation, ignoring the ternary`,`This pattern requires two separate playwright.config.ts files, one per project`],0,`Branching a fixture on testInfo.project.name is how you keep a single shared test body working correctly across projects with fundamentally different underlying implementations (like separate mobile vs desktop page objects).`],[`O`,`I`,`What is achieved by this fixture depending on three others at once?`,`const test = base.extend({
  checkoutPage: async ({ page, cartApi, loggedInUser }, use) => {
    await cartApi.seedCart(loggedInUser.id, [{ sku: 'ABC', qty: 2 }]);
    await page.goto('/checkout');
    await use(new CheckoutPage(page));
  },
});`,[`It composes three independent pieces of setup (browser page, an API helper, and an authenticated user) into a single, ready-to-use checkoutPage fixture — Playwright resolves and injects all three dependencies automatically based on the destructured names, before this fixture’s own body runs`,`Fixtures are limited to at most two dependencies; this code throws a runtime error`,`cartApi and loggedInUser must be declared in a specific order matching their internal dependency resolution order, or the fixture fails`,`Each dependency creates a completely separate browser instance, resulting in three browsers per test`],0,`Fixture composition scales naturally — a fixture can depend on as many other fixtures (built-in or custom) as it needs, and Playwright resolves the whole dependency graph automatically.`],[`O`,`A`,`What does checking testInfo.status inside this fixture’s teardown achieve?`,`const test = base.extend({
  tempTestUser: async ({}, use, testInfo) => {
    const user = await createUser();
    await use(user);
    if (testInfo.status === testInfo.expectedStatus) {
      await deleteUser(user.id);
    }
  },
});`,[`The created test user is only deleted if the test finished with its expected outcome (normally 'passed') — if the test unexpectedly failed, the user is deliberately LEFT in place so a developer can inspect its state afterward to help debug the failure`,`testInfo.status is always undefined inside teardown, making this condition permanently false`,`It deletes the user twice: once during the test and once here, causing an error`,`Comparing status to expectedStatus has no special meaning; it is equivalent to always deleting the user`],0,`Conditional cleanup based on test outcome is a deliberate tradeoff — it sacrifices some cleanliness (failed-test leftovers accumulate) in exchange for much better debuggability of exactly what state caused a failure.`],[`O`,`A`,`What safeguard does this pattern add around a potentially slow fixture setup?`,`const test = base.extend({
  slowResource: async ({}, use) => {
    const start = Date.now();
    const resource = await acquireExpensiveResource();
    const elapsed = Date.now() - start;
    if (elapsed > 5000) {
      console.warn(\`slowResource setup took \${elapsed}ms — investigate\`);
    }
    await use(resource);
  },
});`,[`It logs a visible warning whenever the fixture’s setup takes longer than a defined threshold, giving early, low-noise visibility into performance regressions in that setup step over time, without failing the test outright`,`It automatically aborts and retries acquireExpensiveResource() if it takes over 5 seconds`,`console.warn() inside a fixture is not permitted and silently does nothing`,`The 5000ms check replaces the need for a { timeout } option on the fixture`],0,`A simple timing watchdog with a warning (not a hard failure) is a lightweight way to catch a slowly-degrading fixture before it becomes bad enough to start timing out outright.`],[`O`,`A`,`What does re-throwing after cleanup accomplish in this pattern?`,`const test = base.extend({
  transactionalDb: async ({}, use) => {
    const tx = await db.beginTransaction();
    try {
      await use(tx);
    } finally {
      await tx.rollback();
    }
  },
});`,[`If the test body throws, the error propagates up normally to fail the test (finally does not swallow it), while the finally block still guarantees the transaction is always rolled back — combining reliable per-test data isolation with correct error reporting`,`finally always suppresses any error thrown inside use(), causing tests to incorrectly pass`,`try/finally has no effect on fixtures since Playwright already guarantees teardown regardless`,`tx.rollback() only runs if the test explicitly passed, not on failure`],0,`try/finally around use() is a defensive pattern ensuring cleanup runs on EVERY path (pass, fail, or thrown error) — a good habit even though Playwright already guarantees code after use() runs, because it protects against errors thrown by the teardown logic itself interfering with each other.`],[`O`,`A`,`What does this pattern let a test do that a plain worker-scoped connection could not?`,`const test = base.extend({
  db: [async ({}, use) => {
    const conn = await pool.connect();
    await use(conn);
    await conn.release();
  }, { scope: 'worker' }],
  isolatedTx: async ({ db }, use) => {
    await db.query('BEGIN');
    await use(db);
    await db.query('ROLLBACK');
  },
});`,[`It layers a test-scoped transaction fixture on top of a worker-scoped shared connection — each individual test gets full data isolation (via BEGIN/ROLLBACK) while still reusing the same expensive-to-create pooled connection across many tests in the same worker, combining speed and isolation`,`ROLLBACK here permanently disables the connection for any future test in the same worker`,`isolatedTx creates a brand new database connection for every test despite depending on the worker-scoped db fixture`,`This pattern is redundant since worker-scoped fixtures already provide per-test isolation automatically`],0,`This "worker-scoped resource + test-scoped wrapper" pattern is a well-known way to get both the performance of shared setup and the correctness of per-test isolation.`],[`O`,`I`,`What does this fixture provide to every test that requests it?`,`const test = base.extend({
  testData: async ({}, use) => {
    const faker = new TestDataBuilder();
    await use({
      uniqueEmail: () => faker.email(),
      randomAddress: () => faker.address(),
    });
  },
});
test('signs up a new user', async ({ page, testData }) => {
  await page.getByLabel('Email').fill(testData.uniqueEmail());
});`,[`A small utility object with test-data-generation helper methods, ready to use without every test needing to import and instantiate a data-builder library itself — centralizing (and making consistent) how random/unique test data is generated across the suite`,`testData automatically seeds the database with the generated values without the test calling anything else`,`uniqueEmail() and randomAddress() are executed once during fixture setup and return the same fixed values for every test`,`This pattern requires scope: 'worker' to function correctly; test scope would throw an error`],0,`Wrapping a data-generation utility in a fixture is a lightweight, common pattern — it’s less about resource lifecycle and more about giving every test convenient, consistent access to a helper.`],[`O`,`A`,`What is testInfo.retry used for in this fixture?`,`const test = base.extend({
  networkThrottle: async ({ page }, use, testInfo) => {
    if (testInfo.retry > 0) {
      await page.route('**/*', route => route.continue());
      console.log('Retry attempt — running without throttling to rule out network flakiness');
    } else {
      await page.route('**/*', route => setTimeout(() => route.continue(), 200));
    }
    await use();
  },
});`,[`It lets the fixture behave differently on a RETRY attempt versus the original run — here, disabling artificial network throttling on retries to help distinguish "genuinely flaky due to slow network simulation" from "actually broken" behavior`,`testInfo.retry always equals 0 and never changes, making the branch dead code`,`It counts how many times the fixture itself has been retried, unrelated to test retries`,`Playwright automatically disables all fixtures on any retry attempt, making this code redundant`],0,`testInfo.retry (0 on the first attempt, incrementing on each subsequent retry) lets fixtures adapt their own behavior specifically to help diagnose or work around retry-specific conditions.`],[`O`,`A`,`What does pushing to testInfo.annotations accomplish here?`,`const test = base.extend({
  featureFlagState: async ({}, use, testInfo) => {
    const flags = await getActiveFlags();
    testInfo.annotations.push({ type: 'flags', description: JSON.stringify(flags) });
    await use(flags);
  },
});`,[`It records a visible annotation on the test result — surfaced in the HTML report next to that test — documenting which feature flags were active during the run, which is invaluable context when debugging a failure that turns out to be flag-dependent`,`annotations.push() only works when called from the test body, never a fixture`,`It silently discards the flags data since fixtures cannot write to testInfo.annotations`,`This causes the test to be automatically retried once for every flag that was active`],0,`testInfo.annotations is a flexible way for a fixture to attach structured, report-visible metadata to a test — useful for anything a future debugger of a failure would want to know at a glance.`],[`O`,`I`,`What is the practical effect of combining two configurable option fixtures like this?`,`const test = base.extend({
  locale: ['en-US', { option: true }],
  theme: ['light', { option: true }],
});

// playwright.config.ts
projects: [
  { name: 'en-light', use: { locale: 'en-US', theme: 'light' } },
  { name: 'fr-dark', use: { locale: 'fr-FR', theme: 'dark' } },
]`,[`The same test files can now run under a MATRIX of combinations (locale × theme) just by adding more project entries with different overrides — no test code changes are needed to add more combinations, since the option fixtures handle the parameterization`,`Combining two option fixtures in one extend() call is not supported and throws an error`,`Only ONE option fixture can be overridden per project; the second is silently ignored`,`This pattern requires each combination to have its own separate spec files`],0,`Composable option fixtures plus the projects array is how you build a configuration matrix (locale × theme × device, etc.) without multiplying actual test code.`],[`O`,`A`,`What is the significance of storing the connection in a local variable before calling use()?`,`const test = base.extend({
  db: [async ({}, use) => {
    const conn = await connect();  // stored locally
    await use(conn);
    await conn.disconnect();       // still accessible here
  }, { scope: 'worker' }],
});`,["JavaScript closures let the local `conn` variable remain accessible in the code AFTER use() resolves (the teardown phase) — this is the standard, necessary pattern for any fixture that needs to clean up the exact resource it created, rather than trying to look it up again",`Storing it locally has no functional purpose; use(conn) alone would make it accessible afterward too`,`conn must be stored on testInfo, not a local variable, or teardown cannot access it`,`This pattern only works for worker-scoped fixtures, not test-scoped ones`],0,`This is one of the most common fixture-authoring mistakes to get backwards — without keeping a reference in scope, the teardown code has nothing to actually clean up.`],[`O`,`I`,"What does destructuring `{ browserName }` alongside a custom fixture accomplish here?",`const test = base.extend({
  crossBrowserWorkaround: async ({ page, browserName }, use) => {
    if (browserName === 'webkit') {
      await page.addInitScript(() => { window.__isWebkit = true; });
    }
    await use();
  },
});`,[`The custom fixture can depend on and branch its behavior based on the built-in browserName fixture, applying a WebKit-specific workaround only when actually running under that engine — built-in and custom fixtures compose exactly the same way`,`browserName cannot be used inside a custom fixture, only directly inside test bodies`,`This pattern requires a separate playwright.config.ts entry per browser to function`,`addInitScript() only works when called from the test body, not from a fixture`],0,`Built-in fixtures like browserName, page, and context are just as composable as any custom fixture — a custom fixture can freely depend on them to make cross-browser-aware decisions.`],[`O`,`A`,`What does this auto-use step fixture accomplish for report readability?`,`const test = base.extend({
  loggedStep: [async ({}, use) => {
    await test.step('setup', async () => { /* real setup work */ });
    await use();
  }, { auto: true, box: true, title: 'Common test setup' }],
});`,[`It groups this fixture’s work under a friendly, collapsible, named entry ("Common test setup") in the trace/report — combining box:true (hide internal detail) with an explicit title so every test’s report clearly shows a labeled setup phase without exposing its internals by default`,`title only affects the fixture’s internal variable name, with no visible effect in reports`,`auto:true and box:true cannot be combined with a custom title`,`This pattern disables the HTML reporter entirely for tests using this fixture`],0,`Combining auto, box, and a descriptive title is how teams build polished, report-friendly shared setup steps that every test benefits from without cluttering the trace with internals.`],[`O`,`A`,`What does reading testInfo.config.projects accomplish inside this fixture?`,`const test = base.extend({
  siblingBaseURL: async ({}, use, testInfo) => {
    const apiProject = testInfo.config.projects.find(p => p.name === 'api');
    const apiBaseURL = apiProject?.use?.baseURL;
    await use(apiBaseURL);
  },
});`,[`It lets a fixture inspect the FULL resolved config, including OTHER projects’ settings — here, discovering the base URL configured for a differently-named 'api' project, useful when one project’s tests need to reach a service whose address is defined under a sibling project rather than its own`,`testInfo.config only exposes the current project’s own settings, never siblings`,`This code always throws because config is only accessible from playwright.config.ts itself, not fixtures`,`projects.find() runs once per test file rather than once per test`],0,`Because testInfo carries the fully resolved configuration, a fixture can look up information from ANY project’s settings, not just the one the current test happens to be running under.`],[`O`,`A`,`What port-allocation problem does this fixture solve for parallel workers?`,`const test = base.extend({
  devServerPort: [async ({}, use, workerInfo) => {
    const port = 4000 + workerInfo.parallelIndex;
    const server = await startDevServer(port);
    await use(port);
    await server.close();
  }, { scope: 'worker' }],
});`,[`Each worker starts its OWN dev server instance on a unique port derived from its parallel index (4000, 4001, 4002, ...) — avoiding the "port already in use" error that would occur if every worker tried to bind the exact same fixed port simultaneously`,`It reuses the exact same port for every worker, relying on OS-level port sharing`,`devServerPort must be scope: 'test' to work correctly; scope: 'worker' would cause a port leak`,`parallelIndex changes for every test within a worker, causing this to start a new server per test`],0,`Deriving a unique resource identifier (port, database name, temp directory) from workerInfo.parallelIndex is the standard way to give each parallel worker its own isolated instance of something that can’t normally be shared.`],[`O`,`I`,"What does combining `{ page, db, apiContext }` as dependencies of a single fixture demonstrate about Playwright’s fixture resolution?",`const test = base.extend({
  seededCheckout: async ({ page, db, apiContext }, use) => {
    const orderId = await db.insertOrder();
    await apiContext.post(\`/orders/\${orderId}/confirm\`);
    await page.goto(\`/checkout/\${orderId}\`);
    await use(orderId);
  },
});`,[`Playwright resolves the full dependency graph automatically — it does not matter whether page, db, or apiContext are built-in or custom fixtures, or in what order they’re listed; each is set up (respecting ITS OWN dependencies) before seededCheckout’s body runs`,`The three dependencies must be manually initialized in the exact order written, or the fixture fails`,`Only one custom fixture dependency is allowed per fixture definition; this code is invalid`,`db and apiContext must both be scope: 'worker' to be usable alongside the test-scoped page fixture`],0,`This is the core value proposition of the fixture system — declare WHAT you depend on by name, and let Playwright figure out the correct setup order and lifecycle, regardless of how many dependencies or what mix of built-in/custom they are.`],[`O`,`A`,`What race condition risk does this leasing fixture NOT protect against, as written?`,`const test = base.extend({
  leasedAccount: [async ({}, use) => {
    const available = pool.filter(a => !a.inUse);
    const account = available[0];
    account.inUse = true; // not atomic with the check above
    await use(account);
    account.inUse = false;
  }, { scope: 'worker' }],
});`,["If two workers evaluate `pool.filter(...)` and read the SAME available account before either one sets `inUse = true`, both could lease the identical account simultaneously — the check-then-set is not atomic, so a proper lease implementation typically needs a lock, a mutex, or an atomic claim operation (e.g. via a database UPDATE ... WHERE)",`Worker-scoped fixtures are inherently immune to race conditions since JavaScript is single-threaded`,`This code is completely safe because each worker is a separate Node.js process with no shared memory`,`The risk only exists if scope is set to 'test' instead of 'worker'`],0,"Separate worker processes ARE isolated from each other in memory, but if `pool` lives in a shared external store (a file, database, or service) rather than in-process, the classic check-then-act race condition still applies across those processes."],[`O`,`A`,`What does this combination achieve for locale AND currency together?`,`const test = base.extend({
  region: ['us', { option: true }],
  page: async ({ page, region }, use) => {
    const configs = { us: { locale: 'en-US', currency: 'USD' }, uk: { locale: 'en-GB', currency: 'GBP' } };
    await page.addInitScript((c) => { window.__testConfig = c; }, configs[region]);
    await use(page);
  },
});`,["A single configurable `region` option drives BOTH the locale and currency settings together as one coherent bundle — tests can override just `region` via test.use() and get a consistent, correct combination, rather than needing to separately configure locale and currency and risk mismatched combinations",`region and locale must always be set as two entirely separate option fixtures for this to work correctly`,`addInitScript() cannot receive fixture values as arguments, so this code throws`,`This pattern only works if region is scope: 'worker'`],0,`Bundling related configuration behind one higher-level option (region) rather than several independent low-level ones (locale, currency separately) reduces the chance of an invalid or mismatched combination being configured.`],[`O`,`A`,`What subtle bug does this fixture have regarding teardown when the test times out?`,`const test = base.extend({
  slowCleanupResource: async ({}, use) => {
    const r = await acquire();
    await use(r);
    await new Promise(resolve => setTimeout(resolve, 15000)); // slow cleanup
    await release(r);
  },
});`,[`If the fixture’s own teardown (including this artificial 15s delay) takes long enough combined with the test’s remaining time budget, the OVERALL test timeout can be hit DURING teardown itself, potentially reporting the test as timed out even though the test body itself already completed successfully — teardown time counts against the same test timeout budget`,`Teardown code is never subject to the test timeout under any circumstances`,`setTimeout() inside a fixture is disallowed and throws a runtime error`,`This bug can only occur if the fixture is worker-scoped, not test-scoped`],0,`A commonly overlooked detail: the test timeout budget covers the ENTIRE test lifecycle including fixture setup and teardown, not just the test body — a slow teardown can cause an otherwise-passing test to be reported as timed out.`],[`L`,`A`,`Why would a fixture need its own explicit { timeout } option separate from the overall test timeout?`,null,[`A fixture doing genuinely slow one-time work (like seeding a large dataset) could otherwise be cut off by whatever fraction of the overall test timeout happens to remain when it runs — an explicit fixture timeout lets that specific operation get the time IT needs, independent of how the surrounding test timeout budget is allocated`,`Fixtures always default to a timeout of 0 (unlimited) unless one is explicitly set`,`The fixture timeout option only exists for worker-scoped fixtures, never test-scoped ones`,`Setting a fixture timeout disables the overall test timeout entirely for that test`],0,`Per-fixture timeouts exist for the same reason per-assertion timeouts do — to give one known-slow operation appropriate headroom without loosening the budget for everything else.`],[`L`,`A`,`What is the tradeoff of using box: true on a fixture with several meaningful internal steps?`,null,[`Reports become cleaner and easier to scan (internal steps collapse into one labeled entry) at the cost of losing that step-by-step detail when you actually DO need to debug exactly which internal action inside the fixture failed — box:true is a readability/detail tradeoff, not a free win`,`box: true has no tradeoff; it strictly improves every aspect of reporting with zero downside`,`box: true disables the fixture’s teardown, so resources may leak`,`It only affects the terminal reporter, with no effect on the HTML report or trace viewer`],0,`Like most reporting-granularity choices, box:true trades detail for clarity — appropriate once a fixture’s internals are considered "boring, working, implementation detail" rather than something worth surfacing by default.`],[`L`,`A`,`Why is the option:true fixture pattern described as similar to how built-in options like viewport or baseURL work?`,null,[`Both are plain configurable VALUES (not resources needing setup/teardown) with a default, overridable via test.use() at the test/describe/project level — option:true lets you build your OWN parameters that behave exactly like Playwright’s built-in configuration knobs`,`Built-in options like viewport are implemented completely differently, with no relationship to the fixture system at all`,`option:true fixtures cannot have default values, unlike built-in options`,`Only Playwright’s own internal team can create option-style fixtures; option:true is reserved and cannot be used by user code`],0,`Recognizing that Playwright’s own built-in options are themselves implemented as (a special kind of) fixture demystifies why option:true feels so natural to override the same way you’d override viewport or storageState.`],[`L`,`I`,`Why is testInfo.attach() from inside a fixture particularly powerful when combined with auto: true?`,null,[`Every test in the suite automatically gets the attached diagnostic artifact (logs, custom data, etc.) without any individual test needing to remember to call attach() itself — centralizing "make sure X is always captured" logic in one auto-running fixture rather than duplicating it across every test`,`auto:true is required before testInfo.attach() can be called at all; it is otherwise disabled`,`Attachments made via a fixture only appear in the terminal reporter, never the HTML report`,`Combining the two disables screenshots and video for that test`],0,`This mirrors the value of auto fixtures generally — anything that should apply universally, without every test author needing to remember it, belongs in an auto fixture.`],[`L`,`A`,`Why does workerInfo.parallelIndex matter specifically for resources that cannot be safely shared across concurrent processes (like a fixed network port)?`,null,[`Multiple workers run as SEPARATE OS processes potentially at the same time — if two of them both tried to bind the exact same fixed port or use the identical database name, they would collide; deriving a per-worker-unique value from parallelIndex avoids that collision without needing any cross-process coordination logic`,`parallelIndex is only relevant when workers is set to 1, where collisions cannot occur`,`Ports and database names are automatically namespaced by Playwright regardless of parallelIndex`,`parallelIndex changes on every single test, making it unsuitable for worker-scoped resource allocation`],0,`workerInfo.parallelIndex is specifically designed as a stable identifier for exactly this class of problem — safely partitioning genuinely exclusive resources across concurrently-running worker processes.`],[`L`,`A`,`Why does Playwright guarantee teardown of a fixture whose setup succeeded, even if the test that requested it (indirectly, via a failing sibling/dependent fixture) never actually ran?`,null,[`Without this guarantee, resources acquired by lower-level fixtures (database connections, browser contexts, temp files) could leak whenever a LATER fixture in the same dependency chain fails — guaranteeing teardown for anything that successfully completed setup prevents that whole class of leaks regardless of where in the chain a failure occurs`,`Teardown is only guaranteed for worker-scoped fixtures, never test-scoped ones, in this scenario`,`This guarantee only applies when the failing fixture explicitly calls a cleanup() method`,`Fixtures whose dependent test never ran are considered "unused" and are exempt from teardown`],0,`The teardown guarantee is scoped to "did setup succeed", not "did the test ultimately run" — this is precisely what prevents partial-failure resource leaks in a fixture chain.`],[`L`,`A`,"Why would a team override the built-in `context` fixture (rather than only ever overriding `page`) to add default permissions?",null,[`Some settings (like granted permissions, geolocation, or a route handler) apply at the BrowserContext level and should be established before any page in that context is created — overriding context ensures the setting is in place from the very start, rather than being added reactively once a page fixture happens to run`,`page and context fixtures are interchangeable, and either override produces identical results`,`The context fixture cannot be overridden; only page, browser, and request support this pattern`,`Context-level overrides only take effect on the SECOND test in a file, not the first`],0,`Choosing which built-in fixture to override (browser vs context vs page) should match the actual LEVEL at which the setting naturally belongs — context-level concerns belong in a context override.`],[`L`,`A`,"Why can’t the built-in `browser` fixture simply be reused, unmodified, for browser-extension testing that requires launchPersistentContext()?",null,[`The default browser fixture launches a single shared (non-persistent) Browser instance from which multiple lightweight contexts are created — persistent contexts (which extensions require) work fundamentally differently, tying a user-data-directory and extension-loading arguments directly to context creation itself, which requires bypassing the standard browser/context fixture pair entirely with a custom fixture`,`launchPersistentContext() is just a renamed alias for the default browser fixture with no functional difference`,`Extension testing is not supported by Playwright under any configuration`,`The browser fixture already supports loading extensions via a config flag, making a custom fixture unnecessary`],0,`Persistent contexts and standard browser-derived contexts have genuinely different creation models — recognizing when a scenario needs the former, rather than trying to force it through the default fixtures, avoids a lot of wasted effort.`],[`L`,`A`,`Why is a worker-scoped "account pool" fixture a better fit than a test-scoped "create a fresh account per test" fixture when testing against a real, rate-limited staging environment?`,null,[`Creating a brand-new account for every single test could quickly exhaust a rate-limited or quota-constrained environment’s account-creation capacity — a pool of pre-provisioned accounts, leased per worker and reused across that worker’s tests, dramatically reduces the total number of accounts needed while still keeping tests from interfering with each other`,`Worker-scoped account pools are always slower than creating fresh accounts, but are used purely for architectural elegance`,`Test-scoped account creation is impossible against any staging environment`,`There is no meaningful difference; the choice is purely a matter of code style`],0,`This is a direct application of "worker scope for expensive/scarce shared resources" — here the resource being scarce is literally staging-environment account capacity or rate limit headroom.`],[`L`,`I`,`Why is an API-based login fixture generally considered more resilient to UI changes than a UI-based login fixture, in addition to being faster?`,null,[`A UI login fixture depends on the login FORM’s markup, labels, and flow staying stable — if the login page’s design changes (new field labels, an added CAPTCHA step, a redesigned layout), every test relying on that fixture could break, whereas an API-based login only depends on the (typically more stable) backend authentication endpoint`,`API-based fixtures are immune to any kind of backend change, including authentication contract changes`,`UI-based login fixtures never break due to markup changes since Playwright uses accessible role-based locators exclusively`,`There is no resilience difference; the only benefit of API-based login is raw speed`],0,`Decoupling test setup from a UI surface that changes relatively often (marketing/login pages get redesigned) reduces one whole category of test maintenance burden.`],[`L`,`A`,`Why does the fixture DEPENDENCY GRAPH (not the literal source-code order fixtures are written in) determine fixture initialization order?`,null,[`Playwright determines what each fixture actually NEEDS by looking at the names destructured in its first argument, then initializes fixtures in an order that satisfies those dependencies — a fixture written first in the file that happens to depend on one written later still correctly waits for its dependency, regardless of declaration order in the source`,`Fixtures are always initialized in the exact order they appear in the extend() object, with no exceptions`,`Dependency resolution only applies to worker-scoped fixtures, not test-scoped ones`,`The developer must manually order fixture definitions correctly or dependencies silently fail to resolve`],0,`This dependency-graph-based resolution is exactly what lets you organize fixture code by TOPIC (auth.ts, db.ts, api.ts) rather than by a fragile, hand-maintained initialization order.`],[`L`,`A`,`What is the general benefit of Playwright potentially running independent (non-dependent) fixtures concurrently during setup?`,null,[`When two fixtures don’t depend on each other (e.g. a db fixture and an unrelated mock-server fixture), there is no reason to force one to wait for the other — allowing their setup to happen concurrently reduces total setup time compared to always resolving every fixture strictly one-at-a-time regardless of actual dependencies`,`Concurrent fixture setup is disabled by default and must be manually enabled per fixture`,`Independent fixtures are only run concurrently when scope is set to 'worker', never 'test'`,`This behavior only applies to built-in fixtures, never custom ones`],0,`Because the framework already knows the true dependency graph (not just declaration order), it can safely parallelize genuinely independent setup work rather than serializing everything by default.`],[`L`,`B`,`Why should database or API-heavy fixtures typically be kept "thin" (doing one focused thing) rather than one large fixture doing everything a test needs?`,null,[`Small, focused fixtures compose cleanly (a test can mix-and-match exactly the setup it needs) and are individually easier to understand, test, and reuse across different test files — a single monolithic "do everything" fixture forces every consuming test to accept its entire bundle of side effects even when only part of it is actually needed`,`Large fixtures are technically disallowed by Playwright and will throw a configuration error past a certain size`,`Thin fixtures always execute faster than large ones, regardless of what work is actually being done`,`This only matters for worker-scoped fixtures; test-scoped fixtures have no such concern`],0,`This is the same single-responsibility principle applied to fixtures as to functions/classes generally — composability and reuse both benefit from keeping each piece narrowly focused.`],[`L`,`I`,"Why do teams commonly have fixtures return a Page Object Model instance directly (e.g. `checkoutPage: async ({ page }, use) => { await use(new CheckoutPage(page)) }`) instead of manually instantiating the page object at the top of every test?",null,[`It centralizes and DRYs up the instantiation (and any pre-navigation or setup the page object needs) in one place, so every test just destructures the ready-to-use object — adding a new required setup step later to the page object’s constructor updates every test automatically instead of requiring edits everywhere it’s instantiated`,`Manual instantiation of page objects inside test bodies is technically disallowed by Playwright`,`Fixture-provided page objects are automatically type-safe while manually instantiated ones are not, regardless of TypeScript usage`,`This pattern only works if the page object class extends a special PlaywrightPageObject base class`],0,`This is fixtures acting as a dependency-injection mechanism for your own domain objects, not just for raw Playwright primitives like page and context.`],[`L`,`A`,"Why can a fixture calling `test.skip()` during its own setup be a cleaner solution than every test doing its own `test.skip(condition, reason)` check?",null,[`It centralizes the skip CONDITION and its evaluation logic in one place (the fixture) rather than duplicating the same check across every test that happens to need it — if the condition or its meaning changes, only the fixture needs updating, not every individual test`,`Only fixtures are allowed to call test.skip(); calling it from a test body directly is invalid`,`Fixture-based skips are reported differently in the HTML report, showing a special icon unavailable to test-body skips`,`test.skip() from a fixture skips the ENTIRE test file, not just the individual test`],0,`This is the same DRY reasoning behind most fixture patterns — if the same conditional logic would otherwise be copy-pasted into every affected test, a fixture is usually the better home for it.`],[`L`,`I`,`Even though a custom fixture is only actually REQUESTED by one spec file in the whole repo, does its setup cost get "cached" or avoided on subsequent unrelated test runs?`,null,["No — a fixture’s scope determines its lifetime WITHIN a given run (test-scoped: every test; worker-scoped: once per worker in that run) but there is no persistence or caching of fixture results ACROSS separate `npx playwright test` invocations — each full run re-pays the setup cost according to the fixture’s configured scope",`Fixtures used by only one spec file are automatically cached across all future runs indefinitely`,`Unused fixtures (not requested by any test) still run once globally per repository`,`Fixture results are cached in a local .playwright-cache directory and reused across runs by default`],0,`Scope governs lifetime WITHIN one test run, not persistence ACROSS separate runs — there is no built-in cross-invocation caching of fixture setup.`],[`L`,`A`,`What lifecycle guarantee explains why the LIFO (last-in-first-out) teardown order matters for a fixture chain like page → loggedInUser → shoppingCart?`,null,[`Teardown of shoppingCart may still need loggedInUser and page to be fully alive and functional (e.g. to make a final API call using the user’s session, or a page action to verify cleanup) — tearing down in reverse-dependency order ensures nothing a fixture’s teardown needs has already been torn down out from under it`,`LIFO teardown order is purely a performance optimization with no correctness implications`,`Teardown order is actually FIFO (first-in-first-out), not LIFO, contrary to common assumption`,`Teardown order has no relationship to dependency order; it is always alphabetical by fixture name`],0,`LIFO teardown mirrors how stack-based resource cleanup generally works in programming — you can only safely release something once everything that might still depend on it during its own cleanup has already finished.`],[`L`,`I`,`Why does a worker-scoped auto fixture (e.g. starting a mock server once per worker) differ meaningfully from a test-scoped auto fixture (e.g. starting a clock-freeze once per test) in terms of what kind of setup belongs in each?`,null,[`Worker-scoped auto fixtures suit setup that is safe and beneficial to SHARE across many tests in a worker (expensive, stateless, or read-mostly resources); test-scoped auto fixtures suit setup that MUST be fresh and isolated per test (anything with mutable state a test could accidentally leak into the next test if shared)`,`Worker-scoped auto fixtures always run slower than test-scoped ones, making the choice purely a performance tradeoff`,`auto:true has no interaction with scope; both combinations behave identically in every respect`,`Test-scoped auto fixtures cannot depend on the page fixture, unlike worker-scoped ones`],0,`The scope choice for an auto fixture should follow the same "is this state safe to share across tests" reasoning as any other fixture — auto just adds "and it should apply to every test automatically" on top.`],[`L`,`A`,`What does declaring worker fixtures with a separate TypeScript generic parameter (the second type argument to base.extend<TestFixtures, WorkerFixtures>) actually provide at compile time versus runtime?`,null,[`It is a pure TypeScript-level typing aid — it tells the type checker which fixtures are worker-scoped so misuse (like accidentally destructuring a worker fixture where a test-scoped one was expected, or vice versa) can be caught by the compiler; it has NO effect on actual runtime scope, which is determined solely by the { scope: 'worker' } option on the fixture definition`,`The generic parameter is what actually MAKES a fixture worker-scoped at runtime; { scope: 'worker' } is redundant once the type is declared`,`TypeScript generics on extend() are required before ANY fixture (typed or not) can be used`,`Worker fixtures without the correct generic silently fall back to test scope at runtime`],0,`It’s important to separate "what TypeScript checks at compile time" from "what actually determines behavior at runtime" — the generic types are purely for developer tooling/safety, the { scope } option is what Playwright actually reads.`],[`L`,`A`,`Why is it generally considered a fixture design smell if a single fixture both seeds significant test data AND performs UI navigation AND handles authentication, all in one function?`,null,[`Bundling several distinct responsibilities into one fixture makes it hard to reuse just ONE of those pieces independently (e.g. a test that needs authentication but a different data setup), harder to reason about failures (which of the three things actually failed?), and harder to test/maintain in isolation — splitting into composable smaller fixtures (auth, seeding, navigation) that a higher-level fixture then combines is usually more flexible`,`Playwright enforces a hard limit on how many operations a single fixture function may perform`,`Combining responsibilities in one fixture always makes tests run measurably slower`,`This is not actually a design concern; fixture size and responsibility count have no bearing on suite quality`],0,`This is single-responsibility thinking applied specifically to fixtures — favor several small, composable fixtures over one fixture that tries to do everything a particular test scenario happens to need.`],[`L`,`A`,`Why might a worker-scoped fixture intentionally avoid depending on the page fixture, even though Playwright would technically allow a worker fixture to declare a dependency on a wider-or-equal scope?`,null,[`Wait — this is backwards: a WORKER-scoped fixture actually CANNOT depend on the (narrower) test-scoped page fixture at all; dependencies must be of equal-or-WIDER scope than the dependent fixture, so a worker fixture is restricted to depending only on other worker-scoped (or narrower-than-worker doesn’t apply) fixtures, never a test-scoped one like page`,`Worker fixtures can depend on page fixtures without restriction; there is no scope compatibility rule in Playwright`,`The restriction only applies when auto: true is also set on the worker fixture`,`page is itself worker-scoped, so this scenario is a non-issue by definition`],0,`The scope compatibility rule is: a fixture can depend on fixtures of the SAME or a WIDER scope, never a narrower one — since a worker-scoped fixture is set up once while page is recreated per test, there is no consistent single page instance a worker fixture COULD hold onto.`],[`L`,`A`,"Why does a fixture that wraps `test.step()` internally combined with `box: true` provide value beyond just calling test.step() directly inside each test?",null,[`It centralizes the SAME named, collapsible reporting step across every test that uses the fixture, without each test author needing to remember to wrap their own setup in a test.step() call — consistency and DRY-ness in how common setup is presented in the report, not just for one test but for the whole suite`,`test.step() cannot be called directly inside a test body at all; it is fixture-only`,`box:true only has an effect when combined with test.step(), and does nothing otherwise`,`This pattern disables retries for any test using the fixture`],0,`The value here is consistency at scale — every test automatically gets the same polished report presentation for shared setup, rather than depending on each author remembering to add it.`],[`L`,`A`,`Why is globalSetup (a Node.js script run once before ALL workers even start) fundamentally different in lifecycle from a worker-scoped auto fixture (which still runs once PER worker, inside a worker process)?`,null,[`globalSetup runs a single time for the entire test run, in a separate process before any worker is spawned — a worker-scoped fixture, despite sounding "global"-ish, still runs once per EACH worker, meaning with 4 workers it runs 4 times total, not once; genuinely once-for-the-whole-run logic belongs in globalSetup, not a worker fixture`,`They are functionally identical, and either can always be substituted for the other with no behavioral difference`,`Worker-scoped fixtures run before globalSetup in the actual execution order`,`globalSetup can access fixtures like page and context directly, unlike worker-scoped fixtures`],0,`The naming can be misleading — "worker-scoped" sounds close to "global" but is really "once per worker process", which is a very different (and much more frequent) cadence than globalSetup’s true once-per-run.`],[`L`,`A`,`Why is directly using the page fixture inside a worker-scoped fixture’s setup structurally impossible, rather than just discouraged?`,null,[`page is a test-scoped built-in fixture, and Playwright’s scope-compatibility rule forbids a wider-scoped fixture (worker) from depending on a narrower-scoped one (test) — there is no single, stable page instance that could exist for the ENTIRE worker’s lifetime the way there is for a single test, so the dependency simply cannot be declared`,`It is only discouraged as a style guideline; Playwright allows it but logs a deprecation warning`,`page can be used in a worker fixture as long as auto: true is also set`,`The restriction only applies to Chromium-based projects, not Firefox or WebKit`],0,`This scope-compatibility rule is enforced by the framework, not just a best-practice suggestion — it reflects a genuine structural incompatibility between a per-test resource and a per-worker lifetime.`],[`L`,`A`,`Why does wrapping a worker-scoped database connection in a test-scoped "reset" fixture (that runs a transaction rollback, or a lightweight schema reset, before/after each test) address a common downside of pure worker scope?`,null,[`Pure worker scope means all tests in that worker share the SAME live connection/state — without an explicit per-test reset layered on top, one test’s data changes could leak into and affect the next test running in the same worker; the test-scoped reset fixture restores per-test isolation while still amortizing the expensive connection setup at worker scope`,`This pattern is purely about performance and has no bearing on test isolation whatsoever`,`A reset fixture is unnecessary since worker-scoped connections automatically isolate each test’s queries`,`The reset fixture must itself also be scope: 'worker' to have any effect`],0,`This "worker-scoped expensive resource + test-scoped isolation wrapper" pattern directly answers the classic worry about worker scope: "won’t tests then interfere with each other?" — the wrapper is precisely the answer.`],[`L`,`B`,"Why is it useful for a shared fixtures module (e.g. fixtures.ts, re-exporting a combined `test` and `expect`) to be the ONLY place spec files import test/expect from?",null,[`It guarantees every spec file automatically has access to the full set of custom fixtures the team has built (auth, db, api, etc.) through one consistent import, avoiding the common "unknown fixture" error that happens when a file accidentally imports test from @playwright/test directly instead`,`Playwright technically requires all spec files to import from the exact same file path`,`This pattern is only necessary in TypeScript projects; JavaScript projects do not need it`,`A shared fixtures module automatically improves test execution speed compared to per-file custom extends`],0,`Standardizing on one shared import point is a simple convention that eliminates an entire recurring category of "wrong test object imported" bugs across a growing codebase.`],[`L`,`A`,`Why does a fixture’s teardown throwing an error still cause a test that already passed to be reported as failed?`,null,[`Playwright treats the fixture teardown phase as part of the overall test’s execution — an unhandled error anywhere in that lifecycle (including after the test body itself finished successfully) is surfaced as a test failure, since a broken teardown often indicates a real problem (like a resource that couldn’t be released) worth surfacing rather than silently ignoring`,`Teardown errors are always silently swallowed and logged only to the console, never affecting the test result`,`This only happens if the fixture is worker-scoped; test-scoped teardown errors are ignored`,`A teardown error can only affect the NEXT test to run, never the one whose fixture it belongs to`],0,`This is a frequently-surprising but intentional behavior — "the test passed" and "the whole lifecycle around the test completed cleanly" are two different things, and Playwright reports on the latter.`],[`L`,`A`,`Why is fixture-based dependency injection generally considered more testable and maintainable than each test importing and calling setup utility functions directly at the top of the test body?`,null,[`Fixtures make dependencies explicit and declarative (visible right in the test signature), automatically manage teardown/cleanup even on failure, support scoping (test vs worker) and composition — a plain imported utility function called manually has none of that: no guaranteed cleanup, no scoping control, no automatic dependency resolution`,`Utility functions cannot be async, unlike fixtures, making them fundamentally less capable`,`There is no real difference; fixtures are purely syntactic sugar with identical runtime guarantees`,`Fixture-based setup always executes faster than an equivalent utility function call`],0,`The core value fixtures add over plain function calls is the LIFECYCLE guarantees (teardown, scoping) and the DECLARATIVE dependency graph — not just code organization.`],[`L`,`I`,'Why does declaring a dependency by name in a fixture’s first argument (e.g. `async ({ page, db }, use) => {}`) count as "requesting" that fixture, the same way a test does?',null,[`From Playwright’s resolution perspective, a fixture depending on another fixture and a test depending on a fixture are the SAME mechanism — anything that destructures a fixture name in its first argument becomes a consumer in the dependency graph, triggering that fixture’s setup (if not already satisfied for the current scope) before the consumer’s own body runs`,`Fixtures depending on other fixtures use an entirely separate resolution mechanism from how tests request fixtures`,`A fixture can only depend on OTHER fixtures if explicitly marked with a special dependsOn option`,`This mechanism only works for built-in fixtures depending on other built-in fixtures`],0,`Understanding that "tests" and "fixtures" are both just consumers in the same underlying dependency-resolution system explains why composition (fixtures depending on fixtures) works so naturally.`],[`L`,`A`,`Why might reading process.env inside a fixture (to pick dev vs staging config) be preferable to defining two entirely separate fixture files/modules for dev and staging?`,null,[`A single fixture with runtime environment-branching keeps ONE source of truth for the fixture’s logic and API — two separate fixture modules would need to be kept in sync manually as the fixture’s logic evolves, and every consuming spec file would need to know which one to import based on environment`,`Playwright fixtures cannot read process.env at all; this is a common misconception`,`Separate fixture files always execute faster since there is no runtime branching overhead`,`This pattern only works if the two environments use the exact same database schema`],0,`Runtime configuration branching inside one fixture avoids the maintenance burden of keeping multiple near-duplicate fixture implementations in sync as the underlying logic changes over time.`],[`L`,`A`,"Why does a fixture module organized as separate files (auth.fixtures.ts, db.fixtures.ts, api.fixtures.ts) combined via chained `.extend()` calls scale better for a large team than one large fixtures.ts file with everything in it?",null,[`Separate domain-focused files reduce merge conflicts between teams working on different areas (auth vs db vs api), make it easier to find and review a specific fixture’s logic, and let each domain evolve independently — a single monolithic file becomes an increasingly contested, hard-to-navigate bottleneck as more fixtures accumulate`,`Chained extend() calls execute measurably faster than a single extend() call with all fixtures combined`,`Splitting into multiple files is required by Playwright once a project exceeds 10 custom fixtures`,`There is no practical benefit; file organization has no bearing on team scalability`],0,`This is standard software-engineering modularity reasoning applied to fixtures — domain-based file organization scales better with team size than one large shared file, regardless of the specific technology.`],[`L`,`A`,`Why is it important that a custom matcher-like domain assertion (e.g. checking an order’s state) be placed in a page-object method or a shared helper, rather than duplicated inline across every fixture that touches orders?`,null,[`If multiple fixtures independently re-implement similar order-state-checking logic, a change to the business rule (e.g. what counts as a "confirmed" order) requires hunting down and updating every duplicated copy — centralizing it in one shared place means fixtures compose by calling that single source of truth instead of re-deriving the logic themselves`,`Fixtures are technically forbidden from calling helper functions or page-object methods`,`Duplicated logic across fixtures always causes a runtime naming collision error`,`This concern only applies to worker-scoped fixtures, not test-scoped ones`],0,`This is the same DRY principle that motivates centralizing repeated assertions into custom matchers — apply it to fixture-internal logic too, not just to what tests directly call.`],[`L`,`A`,"Why does combining `scope: 'worker'` with a resource pool (leasing pattern) differ meaningfully from simply setting a low `workers` count to avoid resource contention altogether?",null,["Lowering `workers` sacrifices overall test-suite parallelism/speed just to work around a SPECIFIC scarce resource, penalizing every other unrelated test too; a leasing pool instead lets the full worker count be used for everything else, while only the genuinely scarce resource is coordinated/rationed specifically",`They are functionally and performance-wise identical in every scenario`,`Leasing pools are always slower than reducing workers, making the pool pattern strictly worse`,`Reducing workers automatically implements a leasing pool internally, making a custom pool redundant`],0,`A targeted fix (pool the specific scarce resource) usually beats a blunt, suite-wide fix (reduce all parallelism) when only ONE dependency is actually constrained.`],[`L`,`B`,`Why does a fixture that both sets up AND asserts (e.g. verifying seeded data was actually created before calling use()) blur the line between "setup" and "test logic"?`,null,[`Assertions embedded inside fixture setup effectively become invisible, silent preconditions for every test using that fixture — if such an assertion fails, it can be confusing for a developer since the failure appears to originate from fixture "setup" rather than from the test they were actually trying to run, making the actual test intent harder to see in the failure`,`Fixtures are technically not permitted to contain any expect() calls at all`,`This pattern always causes an infinite retry loop`,`Assertions inside fixtures execute twice as slowly as assertions inside test bodies`],0,`A lightweight sanity check inside a fixture (throwing a clear error if setup truly failed) is reasonable, but heavy business-logic assertions are usually better left visible in the test body itself, where a reader expects to find them.`],[`L`,`A`,`Why might a team choose to keep an expensive worker-scoped fixture’s resource creation logged with timing information (even in normal, non-debugging runs)?`,null,[`Passive, always-on visibility into how long expensive shared setup takes lets a team notice a gradual performance regression (e.g. the shared mock server slowly getting slower to start over months) before it becomes severe enough to start causing outright fixture timeouts — proactive rather than reactive detection`,`Logging setup timing has no diagnostic value and only exists for cosmetic terminal output`,`This is only useful when combined with { auto: true }, and has no value otherwise`,`Timing logs are required by Playwright for any fixture using scope: 'worker'`],0,`Lightweight, always-on instrumentation (not just ad hoc debugging) is a good practice for shared, expensive setup precisely because its degradation tends to be gradual and easy to miss until it becomes a real problem.`],[`L`,`A`,'Why is conditionally skipping cleanup based on `testInfo.status` (only deleting created data if the test passed) a deliberate tradeoff rather than a strictly "better" default for every fixture?',null,[`It trades a fully clean environment (all data always deleted regardless of outcome) for better debuggability of failures (data from a failed test is preserved for inspection) — for a fixture where investigating failures matters more than a pristine environment, this is the right call, but for a fixture whose leftover data could interfere with SUBSEQUENT tests, unconditional cleanup may be safer`,`This pattern strictly and unconditionally improves both debuggability and environment cleanliness with no downside`,`testInfo.status is unreliable and often returns an incorrect value, making this pattern risky regardless of intent`,`Conditional cleanup based on status is required by Playwright whenever a fixture creates any data`],0,`Every conditional-cleanup decision should weigh "will leftover data from failures help future debugging" against "could leftover data break or pollute the environment for other tests" — the right answer varies by fixture.`],[`L`,`B`,"Why does a fixture typically receive `testInfo` as a THIRD argument (`async ({ page }, use, testInfo) => {}`), separate from the destructured fixture dependencies?",null,[`testInfo is per-test/per-worker run METADATA (title, status, retry count, project, attachments, etc.) rather than a fixture VALUE that other fixtures depend on — keeping it as a distinct third parameter, rather than lumping it into the destructured fixtures object, reflects that conceptual difference`,`testInfo must be listed as a dependency in the destructured object, and the third-argument form shown is actually invalid syntax`,`The third argument is optional boilerplate with no functional meaning; testInfo works identically if omitted`,`testInfo is only available in worker-scoped fixtures, never test-scoped ones`],0,`The consistent (dependencies, use, testInfo) fixture function signature reflects a real conceptual split — what the fixture NEEDS (dependencies) versus contextual information ABOUT the current run (testInfo).`],[`L`,`A`,"Why is choosing to override the built-in `page` fixture (to add cross-cutting behavior like console-error logging) usually preferred over introducing an entirely new fixture name like `pageWithLogging`?",null,['Overriding `page` directly means every EXISTING test and helper that already destructures `{ page }` automatically gets the new behavior with zero changes anywhere else — introducing a differently-named fixture would require updating every test to destructure the new name instead, and risks two subtly different "page" concepts coexisting in the codebase',`A new fixture name is always technically required; Playwright disallows overriding page under any circumstances`,`pageWithLogging would run measurably faster than an override of the built-in page fixture`,`This choice has no practical difference; either approach yields identical test code with no edits`],0,`Overriding a built-in fixture by the SAME name is specifically valuable because it retrofits new behavior onto the ENTIRE existing suite for free, without any test file needing to change.`],[`L`,`A`,`Why is it risky for a fixture teardown to itself perform substantial new work (e.g. sending a full analytics report) that could itself fail or take a long time?`,null,[`A slow or failing teardown eats into the same overall test timeout budget as the test body, and — since teardown errors are surfaced as test failures — a flaky or slow teardown step can make an OTHERWISE-PASSING test appear to fail or time out, for a reason completely disconnected from what the test was actually verifying`,`Teardown code is exempt from the test timeout and error-reporting rules that apply to the test body`,`This is never actually risky since Playwright automatically retries only the teardown portion on failure`,`Fixture teardown always runs in a separate, isolated process unaffected by test timing`],0,`Because teardown counts toward the same lifecycle (and therefore the same timeout and failure reporting) as the rest of the test, it should generally be kept as fast and reliable as the resource cleanup genuinely requires — not expanded into unrelated, potentially fragile extra work.`],[`L`,`A`,`Why does combining several small option fixtures (locale, theme, region) via the projects array create a genuine "test matrix" rather than just more individual test cases?`,null,[`Every project entry effectively re-runs the SAME underlying test files under a different combination of overridden options — so N option combinations expressed as N projects means the exact same test logic gets validated N times over, without writing N times as much test code`,`Only ONE option fixture can ever be overridden per project; true matrices are not possible with this pattern`,`This pattern requires a separate spec file per matrix cell, multiplying actual test code by N`,`Playwright automatically deduplicates matrix runs that would produce the same result, reducing actual executions`],0,`The "test matrix via projects × option fixtures" pattern is exactly why option-style fixtures are worth learning — they turn configuration variation into free additional coverage rather than additional test-writing effort.`],[`R`,`A`,`Your staging environment only allows 5 concurrent test accounts due to a hard rate limit, but your suite runs 12 workers in parallel. How do you structure fixture-based account access?`,null,[`A worker-scoped "account pool" fixture that leases one of the 5 accounts per worker and releases it when the worker finishes, ensuring at most 5 accounts are ever in use concurrently regardless of worker count`,`A test-scoped fixture that creates a brand-new account for every single test`,`Reduce workers to 5 to match the account limit, sacrificing overall parallelism`,`Share one single hardcoded account across all 12 workers simultaneously`],0,`A worker-scoped leasing pool is the standard way to safely share a hard-capped external resource across more parallel workers than there are resource instances.`],[`R`,`A`,`Your suite of 400 tests each individually logs in through the UI, adding roughly 3 seconds per test just for the login flow. How do you eliminate most of that overhead?`,null,[`Create a fixture that authenticates via a direct API call and injects the resulting session cookie, skipping the UI login form entirely for every test that needs an authenticated page`,`Increase workers to compensate for the extra time each login flow adds`,`Move the login flow into globalSetup and hope the session persists indefinitely across all tests`,`Add retries: 2 so any slow login attempts are simply retried faster`],0,`An API-based auth fixture removes the UI login flow’s time cost entirely by starting from an already-authenticated state.`],[`R`,`A`,`Every test that touches the database wraps its work in a transaction that must roll back afterward, but establishing a fresh database connection per test is measurably slow. What fixture structure balances speed and isolation?`,null,[`A worker-scoped fixture holding one shared pooled connection, with a test-scoped fixture layered on top that runs BEGIN before use() and ROLLBACK after, giving per-test isolation without repeating the expensive connection setup`,`A single test-scoped fixture that creates and closes a brand-new connection for every test`,`A single worker-scoped fixture with no per-test reset, accepting some risk of cross-test data leakage`,`Move all database setup into globalSetup and never reset between tests`],0,`Layering a test-scoped transactional-reset fixture on top of a worker-scoped shared connection is the classic pattern for combining speed and isolation.`],[`R`,`A`,`You need to generate unique, realistic-looking email addresses and shipping addresses for signup tests, without every test file importing and configuring a data-generation library itself.`,null,[`A shared fixture that instantiates a test-data-builder utility once and exposes convenient helper methods (uniqueEmail(), randomAddress()) that any test can destructure and call`,`Hardcode a small fixed list of emails and addresses directly in each test file`,`Generate all test data manually by hand for each individual test case`,`Store pre-generated data in a config file and read it via process.env in every test`],0,`A test-data-builder fixture centralizes and standardizes how the whole suite generates unique/realistic test data, avoiding per-file duplication.`],[`R`,`A`,`Your company is building a browser extension and needs Playwright tests that load it into an actual browser instance, which the default browser/context fixtures cannot express.`,null,[`Create a custom fixture that calls chromium.launchPersistentContext() directly with the --load-extension launch argument, managing its own context lifecycle instead of relying on the default browser/context fixtures`,`Use the standard context fixture with a special extensions: [...] option in the use block`,`Load the extension manually through page.addInitScript() after the default context starts`,`Extensions cannot be tested with Playwright under any configuration`],0,`launchPersistentContext() with extension-loading arguments requires a custom fixture managing its own browser lifecycle, since it doesn’t fit the default browser/context fixture model.`],[`R`,`A`,`A test that fails intermittently needs its diagnostic request/response logs saved as a downloadable artifact directly in the HTML report, for every test in the suite, without any individual test remembering to save logs itself.`,null,[`An auto:true fixture that collects logs during the test and calls testInfo.attach() at the end to save them as a report attachment automatically for every test`,`Add a manual console.log() call at the end of every test body`,`Configure trace: 'on' and rely solely on the trace viewer instead of custom logs`,`Write logs to a local file and manually attach them to Slack after each CI run`],0,`An auto fixture combined with testInfo.attach() is the standard way to guarantee every test contributes the same diagnostic artifact without relying on individual test authors remembering to.`],[`R`,`A`,`You want to validate the SAME checkout flow across en-US/light-theme and fr-FR/dark-theme combinations without writing separate test files for each combination.`,null,[`Define locale and theme as option:true fixtures with defaults, then add project entries in playwright.config.ts overriding those options per combination — the same spec files run under both project configurations`,`Write four nearly-identical copies of the checkout spec file, one per combination`,`Use test.use() inline inside every single test to switch locale and theme manually each time`,`This requires a completely separate playwright.config.ts per locale/theme combination`],0,`Composable option fixtures combined with the projects array is exactly the mechanism for running identical test logic across a configuration matrix without duplicating test code.`],[`R`,`B`,`A failing test needs its created database record preserved for manual inspection, but a passing test’s equivalent record should be deleted to keep the environment clean. How do you structure the fixture teardown?`,null,[`Check testInfo.status against testInfo.expectedStatus in the fixture’s teardown, and only delete the record when they match (i.e. the test passed as expected)`,`Always delete the record regardless of outcome, and rely on database backups for failure investigation`,`Never delete any record, letting the database grow indefinitely for maximum debuggability`,`Delete the record only when the test is retried, leaving first-attempt failures uncleaned but retried ones cleaned`],0,`Conditional cleanup based on testInfo.status is the direct fixture pattern for "keep failure evidence, clean up success runs."`],[`R`,`A`,`A single suite of navigation tests must run against both a mobile project (using a different page object with touch-specific locators) and a desktop project (using a different page object with hover-based locators), sharing the exact same test bodies.`,null,[`A fixture that branches on testInfo.project.name to construct and return the correct page object implementation (MobileNavPage vs DesktopNavPage) under a single shared fixture name like navPage`,`Duplicate every test body once for mobile and once for desktop with hardcoded page objects`,`Use test.skip() to disable the desktop-only page object logic on mobile projects`,`Move the mobile-specific logic into a completely separate spec file with different test titles`],0,`Branching a shared fixture on testInfo.project.name lets identical test code work correctly across projects with fundamentally different underlying implementations.`],[`R`,`I`,`Three teams each maintain their own fixtures (authFixtures, dbFixtures, apiFixtures) in separate files, and every spec file needs access to all three sets combined.`,null,["Create one combined module that chains .extend() calls across all three (or spreads them into one extend() call, watching for name collisions), and export the result as the single `test` object every spec file imports",`Have each spec file individually import and call .extend() three separate times with all three fixture sets`,`Copy all three teams’ fixture code into one shared monolithic file to avoid import complexity`,`Register each team’s fixtures separately in playwright.config.ts under different keys`],0,`A single combined fixtures module (built by chaining or spreading the individual team modules) is the standard way to give every spec file one consistent import point for all available fixtures.`],[`S`,`A`,`Under high parallelism, two tests occasionally report using what appears to be the exact same "leased" account from a worker-scoped pool at the same time, causing data conflicts. What is the most likely root cause?`,null,[`The pool’s lease logic performs a check ("is this account free?") and a claim ("mark it in use") as two separate, non-atomic steps against shared state — under concurrent access from multiple workers, two leases can both pass the check before either completes the claim, both selecting the same account; the fix is an atomic claim operation (a lock, mutex, or an atomic update against a shared store)`,`Worker-scoped fixtures are inherently free of race conditions, so the account pool implementation must be entirely unrelated to the bug`,`This can only happen if the pool has scope: 'test' instead of 'worker', so changing scope alone fixes it`,`The bug indicates a fault in Playwright itself, since fixture leasing is a built-in guaranteed-atomic mechanism`],0,`A shared external pool (file, database, or service) accessed by multiple independent worker processes needs its own explicit atomicity guarantee — Playwright’s fixture system does not automatically make YOUR pool logic thread-safe.`],[`S`,`I`,`A fixture reliably deletes all test data it creates, but this is making debugging failed tests very difficult since the evidence disappears immediately. How should the fixture be restructured?`,null,[`Change the teardown to check testInfo.status and only perform the deletion when the test passed as expected, deliberately leaving a failed test’s data in place for a developer to inspect afterward (with some separate periodic cleanup job for accumulated failure debris)`,`Remove all cleanup logic permanently, letting the environment accumulate data from both passing and failing tests indefinitely`,`Move all cleanup into globalTeardown so it only runs once at the very end of the whole run`,`Disable the fixture entirely and have each test manually decide whether to clean up`],0,`Conditional, outcome-aware cleanup is the standard resolution to this exact tension between "clean environment" and "debuggable failures."`],[`S`,`A`,"A team wants to migrate 200 spec files off manual `beforeEach` login blocks onto a shared fixture, but doing it all in one massive PR feels too risky. What is a safer migration approach?",null,[`Introduce the new fixtures module alongside the existing code, migrate a handful of files first to validate the approach, then incrementally convert the remaining files in smaller batches — each spec file only needs its test/expect import changed to point at the new fixtures module`,`Rewrite all 200 files simultaneously in one PR since fixtures must be adopted suite-wide atomically or not at all`,`Fixtures cannot coexist with beforeEach hooks, so all beforeEach blocks must first be deleted suite-wide before any fixture can be introduced`,`Migration is unnecessary; beforeEach and fixtures are functionally identical, so there is no benefit to switching`],0,`Because a fixtures module is just a different import, incremental file-by-file migration is entirely possible and lower-risk than one large atomic rewrite.`],[`S`,`A`,`A worker-scoped fixture performing a slow database migration and seed occasionally throws "Fixture did not complete within timeout" in CI, but passes fine locally on a faster machine. What is the most direct fix?`,null,[`Add an explicit, more generous { timeout } option to that specific fixture definition, accounting for CI machines being slower than local development hardware, rather than assuming the default fixture timeout is always sufficient`,`Reduce workers to 1 so the fixture never has to compete for CPU with other workers`,`Move the seeding logic into the test body itself, since test bodies have no timeout of their own`,`Disable the fixture entirely and inline the seeding logic directly into every test`],0,`A CI-only timeout on an otherwise-working fixture is a classic sign that the default timeout budget doesn’t account for slower CI hardware — a per-fixture timeout override is the targeted fix.`],[`S`,`A`,"Two teams merge their fixture modules and both happen to define a fixture named `apiContext`, with subtly different behavior. After merging via object spread, tests silently start using the wrong one with no error. How should this be prevented going forward?",null,[`Adopt a naming convention that namespaces or prefixes fixtures by team/domain (e.g. authApiContext vs billingApiContext), and/or add a lightweight lint rule or code-review checklist item specifically checking for fixture name collisions before merging fixture modules together`,`Object spread automatically throws a compile error on any key collision, so this scenario is actually impossible`,`Fixtures cannot be merged from separate files under any circumstances; this indicates a fundamentally broken setup`,`Always prefer the alphabetically-first fixture module when a collision occurs, since that is Playwright’s default resolution rule`],0,`Because plain JavaScript object-key rules apply to fixture merging, avoiding silent collisions is a naming-convention and review-process concern, not something Playwright itself protects against.`],[`S`,`A`,"A custom fixture launches its own persistent context for extension testing, but a separate spec file (unrelated to extensions) still requests the default `browser` fixture. CI logs show two full browser processes running per worker, roughly doubling resource usage. What is happening, and is it a bug?",null,[`This is expected, not a bug — the persistent-context fixture manages an entirely SEPARATE browser lifecycle from the default browser/context/page fixtures, so any worker running both an extension test AND a normal test genuinely launches two independent browser instances; if resource usage is a concern, separate extension tests into their own project/worker group so they don’t co-run with unrelated tests`,`Playwright automatically deduplicates browser instances within a worker regardless of which fixtures request them`,`This indicates the persistent-context fixture has a bug and is leaking a browser process that was supposed to close`,`Only one browser instance can ever exist per worker; this scenario is technically impossible`],0,`A custom fixture managing its OWN browser lifecycle is genuinely independent of the built-in browser fixture — running both kinds of tests in the same worker legitimately means two separate browser processes.`],[`S`,`A`,`Under very high worker counts, a worker-scoped "account pool" fixture sometimes reports "Fixture 'account' timed out waiting for an available lease" even though the pool has more accounts than any single test uses at once. What is the likely capacity issue?`,null,[`The pool’s total account count may simply be smaller than the current WORKER count — since the pool is leased per-worker (one lease held for the whole worker’s lifetime), having fewer accounts than workers means some workers must wait indefinitely for a lease that never frees up until another worker fully finishes; the fix is provisioning enough accounts to cover the actual worker count, or reducing workers to match pool size`,`Account pool fixtures cannot be worker-scoped; this configuration is inherently invalid`,`The timeout indicates a bug in Playwright’s fixture resolution unrelated to the pool’s actual capacity`,`Increasing the fixture’s auto:true setting would resolve the capacity mismatch`],0,`A leasing pool’s capacity needs to be sized relative to the actual concurrency (worker count) that will be requesting leases, not just relative to how many accounts any single test happens to use.`],[`S`,`A`,"A `defaultLocale` option fixture works correctly when overridden via project-level `use`, but a `test.use({ defaultLocale: 'fr-FR' })` call placed at the top of one specific spec file has no effect on that file’s tests. What should be checked first?",null,[`Whether test.use() is actually being called at the top level of the file/describe scope, versus accidentally being placed inside a test() body, a beforeEach, or some other nested location where it has no effect — test.use() overrides only apply when called at file/describe scope`,`option:true fixtures cannot be overridden via test.use(), only via project-level use`,`defaultLocale must be scope: 'worker' for test.use() overrides to take effect`,`The fixture needs to be re-declared with option: true separately in every spec file that wants to override it`],0,`A very common test.use() debugging step is simply verifying WHERE in the file it was called — it only has effect at the top level of a describe block or file, not inside a test or hook.`],[`S`,`A`,'After chaining `.extend()` across three fixture modules built by three different teams, TypeScript starts reporting that a fixture from module A is "not assignable" when used inside a fixture from module C, even though the runtime behavior works fine. What is a likely cause?',null,[`The generic type parameters passed to base.extend() in one or more of the modules may not correctly declare ALL the fixtures actually being used/depended-upon — TypeScript fixture typing must be kept in sync with runtime fixture definitions across every layer of chained extends, or the type system loses track of what is actually available`,`Chained .extend() calls are fundamentally incompatible with TypeScript and only work in plain JavaScript`,`This is a false positive that can be safely ignored since runtime behavior is what actually matters`,`TypeScript limits extend() chains to at most two layers; a third layer is unsupported`],0,`Type-only issues like this usually mean the TypeScript generics describing the fixture shape have drifted out of sync with the actual runtime fixture object being built through the extend() chain.`],[`S`,`A`,`A team wants a single option fixture to control BOTH the API base URL and the expected UI copy language together (since each region has a distinct API endpoint and distinct localized text), without letting someone accidentally configure a mismatched combination.`,null,["Define one higher-level `region` option fixture (e.g. 'us' | 'uk' | 'de') that internally maps to both the correct API base URL and locale as a single bundled configuration, rather than exposing apiBaseURL and locale as two independently-overridable option fixtures that could be set inconsistently",`Expose apiBaseURL and locale as fully independent option fixtures and trust every test author to always set both together correctly`,`This scenario cannot be solved with fixtures and requires separate playwright.config.ts files per region`,`Hardcode the API URL to a single fixed environment and only vary locale as an option fixture`],0,`Bundling related configuration behind one coherent, higher-level option is a good way to prevent invalid or inconsistent combinations that two fully-independent low-level options could otherwise allow.`],[`D`,`A`,`A worker-scoped "account pool" fixture occasionally hands out an account that another currently-running test is still actively using, causing intermittent, hard-to-reproduce data conflicts only under high parallelism. What is the most likely bug?`,null,[`The lease/release logic likely performs a non-atomic "check if free, then mark as in-use" sequence against shared state accessed by multiple worker processes concurrently — a proper fix requires an atomic claim operation (e.g. a database-level UPDATE ... WHERE in_use = false, or a distributed lock), not a plain in-memory array check`,`This is expected, unavoidable behavior for any worker-scoped fixture and cannot be fixed`,`The bug only occurs when auto: true is missing from the fixture definition`,`Switching the pool fixture from worker scope to test scope would automatically eliminate the race condition without any other changes`],0,`This is a classic distributed-systems race condition (TOCTOU — time-of-check to time-of-use) manifesting inside test infrastructure — the fix is the same as it would be anywhere else: make the claim atomic.`],[`D`,`A`,'A fixture defined with `{ timeout: 5000 }` throws "Fixture did not complete within timeout" even though local logs show the underlying setup operation genuinely completing successfully after about 8 seconds. What is the fix?',null,[`Raise the fixture’s explicit timeout value (e.g. to 15000ms) to accommodate how long the setup operation genuinely takes — the fixture is correctly enforcing the timeout that was configured; the configured value was simply too low for the real-world duration of the work`,`Fixture timeouts are advisory only and this error indicates an unrelated Playwright bug`,`Remove the { timeout } option entirely, since fixtures without one never time out`,`The setup function must be converted to synchronous code to avoid the timeout mechanism entirely`],0,`A fixture timing out despite eventually succeeding almost always just means the configured timeout value is lower than the operation’s real duration — measure the real duration and set the timeout with appropriate headroom above it.`],[`D`,`A`,"A `region` option fixture with a default value is overridden via `test.use({ region: 'uk' })` inside a describe block, but the override has no effect — every test in that file still behaves as if using the default region. Config shows a project-level `use: { region: 'us' }` also set. What is the actual precedence issue?",null,[`This should NOT happen — test.use() at describe/file scope takes precedence OVER project-level use for the tests in that scope; if the override truly has no effect, more likely the fixture was not actually declared with { option: true } (making it a plain resource fixture that ignores use()-style overrides) or the test.use() call is misplaced (e.g. inside a test body rather than at file/describe scope)`,`Project-level use ALWAYS overrides test.use(), regardless of where test.use() is called, by design`,`Option fixtures can only be overridden at the project level, never via test.use()`,`The override requires restarting the whole test run for the new value to take effect`],0,`When an override "has no effect," check two things in order: is the fixture actually built as an option:true fixture that supports overriding, and is test.use() being called at a scope where it actually takes effect?`],[`D`,`A`,"A fixture calls `testInfo.attach()` inside its teardown to save a diagnostic log, but the attachment never appears in the generated HTML report, even though the fixture code runs without throwing any error. What should you check?",null,[`Whether the attach() call is actually awaited (a missing await could let the test/worker process move on before the attachment is fully registered) and whether the correct, current test’s testInfo reference is being used rather than a stale one captured from a different test’s scope`,`testInfo.attach() only works when called from inside the test body, never from any fixture, contrary to how it is often used`,`HTML report attachments only display files under 1KB in size, silently dropping larger ones`,`attach() requires the reporter to be explicitly set to [['html', { attachments: true }]] or it is ignored`],0,`A silently "successful but invisible" attach() call is most often a missing-await or stale-reference issue rather than a report configuration problem.`],[`D`,`A`,`A custom fixture launching a persistent context for extension testing works fine when run alone, but running the full suite shows browser processes accumulating and never being cleaned up between test files, eventually exhausting CI resources. What is the likely bug in the fixture?`,null,["The fixture’s teardown is likely missing (or not correctly reaching) the call to close the persistent context — persistent contexts do not close themselves automatically the way the default browser/context fixtures’ managed lifecycle does, so an explicit `await context.close()` after `use()` is required and must actually execute on every path, including error paths",`Persistent contexts always close automatically after exactly one test, making explicit cleanup optional`,`This indicates a Playwright bug in how persistent contexts are managed and has no fixture-level fix`,`Browser process accumulation is expected and unrelated to fixture teardown; it must be addressed at the OS level instead`],0,`Because a custom persistent-context fixture manages its OWN lifecycle entirely, it is fully responsible for its OWN cleanup — nothing built into Playwright will close it automatically the way it does for the default browser/context fixtures.`]],api:[[`O`,`B`,"What is the `request` fixture in Playwright tests?",null,[`A mock for intercepting browser network traffic`,`A built-in HTTP client for making real HTTP requests without opening a browser`,`A wrapper around the Fetch API available in the test browser`,`A fixture that captures and replays network requests`],1,`request is an APIRequestContext pre-configured with baseURL and extraHTTPHeaders from your config.`],[`O`,`B`,"What does `response.ok()` return?",null,[`The response body as a string`,`true if the HTTP status code is in the 200-299 range, false otherwise`,`The response time in milliseconds`,`true only for status 200, false for all others`],1,`ok() is a quick boolean check — use status() when you need the exact code.`],[`O`,`I`,"What is the key difference between `request` and `page.request`?",null,[`page.request is async; request is synchronous`,`request is independent; page.request shares the browser's cookie jar and session`,`request supports JSON; page.request only supports form data`,`They are identical — page.request is just an alias`],1,`page.request authenticates as the logged-in browser user; request has its own independent session.`],[`O`,`B`,`How do you parse a JSON response body from an API call?`,null,[`response.json — no call needed, it's a property`,`await response.json()`,`JSON.parse(response.body())`,`response.asJSON()`],1,`response.json() is async and returns the parsed JavaScript object.`],[`O`,`I`,"What does passing `data: { name: 'Alice' }` to `request.post()` do?",null,[`Sends the object as a URL query string`,`Serialises the object as JSON and sets Content-Type: application/json`,`Encodes the object as form data`,`Appends the object as a URL fragment`],1,`data sends a JSON body with the correct Content-Type automatically.`],[`O`,`B`,"What does `response.status()` return?",null,[`A boolean — true for success, false for error`,`The exact HTTP status code as a number`,`The status text like "OK" or "Not Found"`,`The response time in milliseconds`],1,`status() returns the numeric HTTP status code, e.g. 200, 201, 404, 500.`],[`O`,`I`,"What does `request.newContext({ baseURL, extraHTTPHeaders })` create?",null,[`A new browser context with API interception enabled`,`A standalone HTTP client with its own session, independent of any browser`,`A subclass of the request fixture with additional headers`,`A mock server that intercepts all requests`],1,`newContext() is for API calls outside of tests — e.g. in globalSetup where no browser exists.`],[`O`,`I`,"What does the `params` option do in a GET request?",null,[`Sets POST body parameters`,`Appends key-value pairs as a URL query string`,`Sets request headers`,`Configures timeout parameters for the request`],1,`params: { q: 'playwright' } becomes ?q=playwright in the URL.`],[`O`,`I`,`Where do you set default headers for all API requests in a project?`,null,[`In a beforeAll hook in every spec file`,`In the use: { extraHTTPHeaders: { … } } block in playwright.config.ts`,`By calling request.setHeaders() before each test`,`In a custom APIRequestContext wrapper`],1,`extraHTTPHeaders in the use block applies to all requests made by the request fixture.`],[`O`,`I`,`Why is using the API for test setup faster than navigating the UI?`,null,[`API requests bypass authentication`,`The API skips browser rendering, JavaScript execution, and network overhead of loading full pages`,`API requests are cached between tests`,`The API uses a faster network protocol than the browser`],1,`A direct API call takes milliseconds; loading a UI form, filling it, and submitting takes seconds.`],[`O`,`A`,"What happens to the cookie jar when you use `request` vs `page.request`?",null,[`Both share the same cookie jar — they are the same client`,`request has its own isolated cookie jar; page.request shares the browser context's cookies`,`Neither fixture stores cookies — stateless only`,`page.request deletes cookies after each request`],1,`request is isolated; page.request is cookie-aware and acts as the logged-in browser user.`],[`F`,`B`,"Complete: `const body = await response.______()` to parse JSON",null,[`json`,`parse`,`data`,`body`],0,`response.json() is async and returns the parsed response body.`],[`F`,`I`,"Complete: `await request.______('/api/users', { data: { name: 'Alice' } })`",null,[`post`,`put`,`create`,`send`],0,`request.post() sends a POST with JSON body when using the data option.`],[`F`,`B`,"Complete: `const code = response.______()` to get the HTTP status code",null,[`status`,`code`,`statusCode`,`httpCode`],0,`response.status() returns the numeric HTTP status code.`],[`F`,`I`,"Complete: `request.get('/api/search', { ______: { q: 'playwright' } })`",null,[`params`,`query`,`search`,`qs`],0,`params appends key-value pairs as a URL query string.`],[`F`,`I`,"Complete: `request.get('/api/secure', { headers: { ______: 'Bearer ' + token } })`",null,[`Authorization`,`Auth`,`Token`,`Bearer`],0,`Authorization is the standard HTTP header for bearer tokens.`],[`F`,`I`,"Complete: `const api = await request.______Context({ baseURL: '…' })` for a standalone client",null,[`new`,`create`,`make`,`build`],0,`request.newContext() creates an independent APIRequestContext with its own session.`],[`F`,`A`,"Complete: `const res = await page._______.get('/api/me')` to use the browser's session",null,[`request`,`api`,`fetch`,`http`],0,`page.request is the session-aware HTTP client that shares the browser's cookies.`],[`E`,`B`,"What is wrong?\n```ts\nconst res = request.get('/api/users');\nconst users = await res.json();\n```",null,[`request.get() does not exist — use request.fetch()`,`await is missing on request.get() — res is a Promise, not an APIResponse`,`json() cannot be called after get()`,`users must be typed as any[]`],1,`Without await, res is a Promise object — calling json() on it throws immediately.`],[`E`,`I`,`What is wrong?
\`\`\`ts
const res = await request.post('/api/submit', {
  params: { name: 'Alice', email: 'a@b.com' },
});
\`\`\`
Intended to send a JSON body.`,null,[`POST does not support params`,`params appends to the URL query string, not the body — use data: { … } for a JSON body`,`email in params is not URL-encodable`,`POST requires an explicit Content-Type header`],1,`params builds a query string; data sends a JSON body — mixing them up sends no body to the server.`],[`E`,`B`,"What is wrong?\n```ts\nconst res = await request.get('/api/users');\nconst text = await res.text();\nconst json = await res.json(); // second body read\n```",null,[`text() and json() cannot be called on the same response`,`The response body stream can only be consumed once — calling json() after text() throws`,`await is not needed on the second call`,`res.json() must come before res.text()`],1,`APIResponse body can only be read once — calling a second body-reading method throws an error.`],[`E`,`I`,`What is wrong?
\`\`\`ts
const res = await request.post('/api/login', {
  form: { email: 'a@b.com', password: 'secret' },
});
const { token } = await res.json();
// Server returns HTML on 401
\`\`\``,null,[`form is not a valid request option — use data instead`,`json() should be guarded with res.ok() first — calling json() on an HTML error response throws`,`form data cannot contain passwords`,`post() requires a Content-Type header`],1,`Always check res.ok() or res.status() before calling json() — error pages are often HTML, not JSON.`],[`E`,`I`,`What is wrong?
\`\`\`ts
// global-setup.ts
export default async function globalSetup({ request }) {
  await request.post('/api/seed');
}
\`\`\``,null,[`globalSetup cannot be async`,`The request fixture is not injected into globalSetup — use request.newContext() instead`,`post() is not available in globalSetup`,`global-setup.ts must be a .js file`],1,`globalSetup receives no test fixtures — create a standalone client with request.newContext().`],[`E`,`A`,"What is wrong?\n```ts\nconst api = await request.newContext({ baseURL: 'https://api.example.com' });\nawait api.post('/users', { data: { name: 'Alice' } });\n// context never closed\n```",null,[`newContext() does not accept a baseURL`,`The context must be closed with await api.dispose() to release connections`,`post() requires headers when using newContext()`,`newContext() is only for GET requests`],1,`Not calling dispose() leaks connections and can cause test runner resource warnings.`],[`D`,`B`,"A protected endpoint returns 401. The `request` fixture is used but no Authorization header is set. What is the fix?",null,[`Switch from request to page.request`,`Pass { headers: { Authorization: 'Bearer ' + token } } to the request call`,`Set credentials: 'include' in the request options`,`Add the token to params`],1,`For token-based auth, pass the Authorization header explicitly on each call or set extraHTTPHeaders in the config.`],[`D`,`I`,'`response.json()` throws "Unexpected token < in JSON". What is the likely cause?',null,[`The server returned binary data instead of JSON`,`The endpoint returned an HTML error page (e.g. 404 or 500) — check res.ok() before parsing`,`json() is not supported for POST responses`,`The Content-Type header is missing`],1,`HTML error pages are the most common cause — always guard with res.ok() or res.status() before calling json().`],[`D`,`B`,"A DELETE request returns 204 No Content and `response.json()` throws. What should you do?",null,[`Use response.text() instead — 204 responses always return text`,`Do not call json() — 204 has no body; check status() == 204 instead`,`Switch to response.body() for binary responses`,`Add Content-Type: application/json to the request headers`],1,`204 No Content has an empty body — calling json() on it throws; just check the status code.`],[`D`,`I`,"Tests that use the API in `beforeEach` to seed data are passing but leaving rows in the database, causing later test failures. What is missing?",null,[`The API seed data persists across test runs — use fixtures instead`,`An afterEach or fixture teardown step that calls DELETE on the created resource is missing`,`beforeEach should not be used for API calls`,`Add a database reset in playwright.config.ts globalTeardown`],1,`Always pair API setup with API teardown — delete the created resource in afterEach or a fixture teardown.`],[`D`,`I`,"`page.request.get('/api/profile')` returns 401 even though the browser shows the user is logged in. What is the cause?",null,[`page.request does not share cookies — it is identical to the request fixture`,`The user is logged in via a JWT in localStorage, not a cookie — page.request only shares cookies, not localStorage`,`page.request requires a separate login step`,`The /api/profile route does not accept browser cookies`],1,`page.request shares cookies but not localStorage or sessionStorage — JWT in localStorage is not sent automatically.`],[`D`,`A`,`In parallel test runs, API-seeded data from one test is interfering with another test. How do you fix this?`,null,[`Use workers: 1 to prevent parallel execution`,`Use unique identifiers (e.g. UUID) per test when seeding, so each test's data is isolated`,`Disable retries — they repeat the seed step`,`Set fullyParallel: false`],1,`Unique test data per run (UUIDs, timestamps) is the standard isolation pattern for parallel API tests.`],[`C`,`B`,"Complete a GET and parse the JSON response.\n```ts\nconst res = await request.get('/api/items');\nconst items = await res.______();\n```",null,[`json`,`parse`,`data`,`body`],0,`response.json() parses and returns the JSON body.`],[`C`,`I`,"Complete a POST with a JSON body.\n```ts\nawait request.post('/api/users', {\n  ______: { name: 'Bob', role: 'admin' },\n});\n```",null,[`data`,`body`,`json`,`payload`],0,`data: { … } serialises the object as JSON and sets the Content-Type header.`],[`C`,`I`,"Complete to add an Authorization header to a request.\n```ts\nawait request.get('/api/private', {\n  headers: { ______: 'Bearer abc123' },\n});\n```",null,[`Authorization`,`Auth`,`Token`,`Bearer`],0,`Authorization is the standard HTTP header for bearer tokens.`],[`C`,`I`,"Complete to assert the response is successful.\n```ts\nawait expect(res).toBe______();\n```",null,[`OK`,`Success`,`Valid`,`200`],0,`toBeOK() asserts status 200-299 and integrates with Playwright's test reporter.`],[`C`,`A`,"Complete to create and then dispose a standalone API context.\n```ts\nconst api = await request.newContext({ baseURL: 'https://api.example.com' });\nawait api.post('/seed', { data: payload });\nawait api.______();\n```",null,[`dispose`,`close`,`destroy`,`end`],0,`dispose() releases the context's network connections and resources.`],[`L`,`B`,`When should you use the API for test setup instead of navigating the UI?`,null,[`Only when the UI is broken`,`When creating prerequisite data — the API is faster and the UI rendering is not what's being tested`,`Always — UI interaction should be avoided in tests`,`Only for DELETE operations`],1,`API setup is idiomatic when the creation step is not under test — keep tests focused on what they actually assert.`],[`L`,`I`,"What is the difference between `data`, `form`, and `multipart` request body options?",null,[`They are synonyms — all send JSON`,`data sends JSON (application/json); form sends URL-encoded (application/x-www-form-urlencoded); multipart sends multipart/form-data for file uploads`,`data is for GET; form is for POST; multipart is for PUT`,`form and multipart are deprecated — use data for all body types`],1,`Match the body option to the Content-Type the server expects.`],[`L`,`I`,"Why would you use `page.request` instead of `request`?",null,[`page.request is faster because it reuses the browser's HTTP/2 connection`,`page.request sends requests as the authenticated browser user — sharing cookies set by page.goto() and browser interactions`,`page.request supports streaming responses`,`request does not support POST — only page.request does`],1,`page.request is for session-aware calls where the user is already logged in via the browser.`],[`L`,`B`,"Can `response.json()` be called multiple times on the same response?",null,[`Yes — it caches the result after the first call`,`No — the body stream can only be consumed once; a second call throws`,`Yes — but only if the Content-Type is application/json`,`Yes — each call re-fetches the body from the server`],1,`Body reading methods (json, text, body) consume the response stream — call them once and store the result.`],[`L`,`A`,`What is the right way to pass extraHTTPHeaders to all API requests in every test without repeating them?`,null,[`Call request.setHeaders() in a beforeAll hook`,`Set extraHTTPHeaders in the use block in playwright.config.ts — they apply to all requests made by the request fixture`,`Create a custom request fixture that adds headers`,`Pass headers to every individual request call`],1,`extraHTTPHeaders in config.use are merged into every request automatically.`],[`R`,`B`,`You need to create a product before a UI test and delete it after. Where do you put the API calls?`,null,[`Inside the test body at the top and bottom`,`In beforeEach to create and afterEach to delete, or in a custom fixture with teardown`,`In a separate test file that runs first`,`In globalSetup and globalTeardown`],1,`beforeEach/afterEach or a fixture with teardown after use() keeps setup and cleanup co-located.`],[`R`,`I`,`A REST endpoint requires a Bearer token. How do you add it to all requests without repeating it per test?`,null,[`Pass it in params on every request`,`Set it in extraHTTPHeaders in the config use block: { Authorization: 'Bearer ' + process.env.TOKEN }`,`Call request.authenticate(token) in beforeAll`,`Add it to the baseURL as a query parameter`],1,`extraHTTPHeaders in the config merges the header into every request made by the fixture.`],[`R`,`I`,`After a POST, you get a 201 and want to extract the new resource's ID. How?`,null,[`res.id — Playwright extracts IDs automatically`,`const { id } = await res.json() — parse the JSON body and destructure`,`res.headers()['location'].split('/').pop() — parse the Location header`,`request.lastId — the client tracks the last created ID`],1,`Parse the response JSON and destructure the ID field — the standard REST pattern.`],[`R`,`A`,`You need to authenticate once in globalSetup and save the session for all tests. What is the correct approach?`,null,[`Use the request fixture in globalSetup with an imported token`,`Create a newContext, call the login endpoint, then await api.storageState({ path: 'auth.json' }), then dispose()`,`Store the session cookie in process.env`,`Use page.request in globalSetup — it shares the browser session`],1,`newContext in globalSetup → login → storageState save → dispose → reference in config use block.`],[`R`,`I`,`You need to test that a search endpoint returns results for a specific query. How do you pass the search term?`,null,[`Append it manually: request.get('/api/search?q=playwright')`,`Pass params: { q: 'playwright' } — Playwright builds the query string`,`Use data: { q: 'playwright' } on a GET request`,`Set it as a header: { 'x-search': 'playwright' }`],1,`params is the idiomatic way — Playwright encodes and appends the query string automatically.`],[`S`,`I`,`A test creates a user via POST then checks the UI. The UI shows no user. What should you investigate first?`,null,[`The POST endpoint is broken — test it with curl`,`The response status — if not 201/200 the user was not created; also check if baseURL matches the UI server`,`The UI has caching — add a cache-busting header`,`The test needs a page.reload() before asserting`],1,`Always verify the API call actually succeeded (check status + response body) before debugging the UI layer.`],[`S`,`A`,`In parallel runs, two tests both POST a user with name "TestUser" and then query by name, getting each other's records. How do you fix the isolation?`,null,[`Set workers: 1 to avoid parallel execution`,"Generate unique names per test: `name: 'TestUser-' + crypto.randomUUID()`",`Use page.request instead of request for all POST calls`,`Disable the name uniqueness constraint on the test database`],1,`Unique per-test identifiers (UUID, timestamp) prevent cross-test data contamination in parallel runs.`],[`S`,`I`,'`response.json()` works in one test but throws "SyntaxError: Unexpected token" in another. The only difference is the endpoint. What do you check?',null,[`The second endpoint uses a different API version`,`The second endpoint returns a non-JSON body (HTML, plain text, or empty) for certain status codes`,`json() is not thread-safe in parallel tests`,`The response encoding is different`],1,`Guard with res.ok() check before calling json() — error responses often return HTML, not JSON.`],[`S`,`A`,`You want to test that creating a resource via API and then deleting it removes it from the UI listing. Write the test strategy.`,null,[`Create in beforeAll, delete in afterAll, assert in the test — that's the standard pattern`,`POST to create → capture id → navigate UI → assert present → DELETE → navigate UI → assert absent`,`Use page.evaluate() to make the API calls inside the browser context`,`Use network interception instead of real API calls`],1,`The hybrid pattern: API seed → UI assertion → API cleanup. This tests the full stack while keeping setup fast.`],[`O`,`B`,'What does `request.get("/api/users")` return?',null,[`The parsed JSON body directly`,`A Promise<APIResponse> — an object with status(), json(), text(), headers(), and body() methods`,`A string response body`,`An array of user objects`],1,`request.get() returns an APIResponse — always check ok() or status() before calling json().`],[`O`,`I`,"What does `response.headers()` return?",null,[`A string of raw header bytes`,`An object mapping header names to their values — all header names are lowercase`,`An array of [name, value] tuples`,`An HTTP Headers object`],1,`headers() returns a plain object with lowercase header names — access values like response.headers()["content-type"].`],[`O`,`B`,"What is the difference between `request.post()` and `page.request.post()`?",null,[`page.request.post() is faster`,`request fixture creates an isolated context; page.request uses the page's browser context and shares its session cookies — use page.request when you need session-aware calls`,`They are identical`,`page.request.post() requires authentication`],1,`page.request shares the page's cookies — use it for session-aware API calls. The request fixture is a separate unauthenticated context.`],[`O`,`B`,'What does `request.delete("/api/users/42")` do?',null,[`Deletes the file at that path on the server`,`Sends an HTTP DELETE request to /api/users/42 using the APIRequestContext`,`Removes the API route from Playwright's routing table`,`Closes the request fixture connection`],1,`HTTP DELETE is a standard method — request.delete() sends it just like request.post() sends POST.`],[`O`,`I`,"What option passes a JSON body in `request.post()`?",null,[`body: JSON.stringify(obj)`,`data: obj — Playwright serialises the object to JSON and sets Content-Type: application/json automatically`,`json: obj`,`payload: obj`],1,`data: obj is the shorthand — use it to avoid manual JSON.stringify and Content-Type headers.`],[`O`,`A`,'What does `request.storageState({ path: "api-auth.json" })` save?',null,[`All API responses made so far`,`Cookies and origins from the APIRequestContext — similar to browser context storageState but for the API client`,`The API client configuration`,`The test's request log`],1,`APIRequestContext has its own cookies (from Set-Cookie response headers) — storageState captures them for reuse.`],[`O`,`I`,"What does the `form` option in `request.post()` do?",null,[`Sends the data as JSON with Content-Type: application/json`,`Encodes the data as application/x-www-form-urlencoded — simulates an HTML form POST`,`Encodes as multipart/form-data`,`Sets the form action URL`],1,`form: is for URL-encoded form data; multipart: is for multipart/form-data (file uploads).`],[`O`,`B`,"What does `response.text()` return?",null,[`The Content-Type header value`,`A Promise<string> — the response body as a raw string`,`The response status text (e.g. "OK")`,`The response URL`],1,`text() is the fallback for non-JSON responses — HTML error pages, plain text, etc.`],[`O`,`I`,`How do you pass custom headers in an API request?`,null,[`Set them in the APIRequestContext options once for the whole context`,`Set them per request: request.get(url, { headers: { "Authorization": "Bearer token" } }) — or set extraHTTPHeaders in the context options for all requests`,`Headers cannot be customised in Playwright API tests`,`Use request.setHeader() before each call`],0,`Per-request headers override or extend context-level headers — both approaches are valid depending on whether the header changes per request.`],[`O`,`A`,"What does `apiRequestContext.dispose()` do?",null,[`Cancels all in-flight requests`,`Closes the APIRequestContext, releasing its cookies and connections — call it in fixture teardown to avoid resource leaks`,`Clears all stored cookies`,`Resets the base URL`],1,`dispose() is the cleanup method for manually created API contexts — the request fixture disposes automatically, but manual contexts need explicit disposal.`],[`O`,`I`,`Can an APIRequestContext follow HTTP redirects automatically?`,null,[`No — redirects must be followed manually`,`Yes — by default APIRequestContext follows up to 20 redirects; set maxRedirects: 0 to disable redirect following`,`Only 301 redirects are followed automatically`,`Redirects are only followed in browser contexts`],1,`Redirect following is on by default — configure maxRedirects in newContext options to control this.`],[`F`,`B`,'Complete to make a GET request.\n```ts\nconst response = await request.______("/api/products");\n```',null,[`get`,`fetch`,`read`,`query`],0,`request.get() sends an HTTP GET request and returns a Promise<APIResponse>.`],[`F`,`B`,'Complete to pass a JSON body.\n```ts\nconst response = await request.post("/api/users", {\n  ______: { name: "Alice", role: "admin" },\n});\n```',null,[`data`,`body`,`json`,`payload`],0,`data: serialises the object to JSON and sets the correct Content-Type header automatically.`],[`F`,`I`,'Complete to check the response status.\n```ts\nconst response = await request.post("/api/login", { data: creds });\nexpect(response.______()).toBe(200);\n```',null,[`status()`,`code`,`statusCode`,`ok`],0,`response.status() returns the HTTP status code as a number.`],[`F`,`I`,'Complete to parse the JSON body.\n```ts\nconst response = await request.get("/api/config");\nconst data = await response.______();\n```',null,[`json()`,`parse()`,`body()`,`text()`],0,`json() parses the response body as JSON and returns the object.`],[`F`,`B`,"Complete to make a DELETE request.\n```ts\nawait request.______(`/api/items/${id}`);\n```",null,[`delete`,`remove`,`destroy`,`del`],0,`request.delete() sends the HTTP DELETE method.`],[`F`,`I`,'Complete to set a custom base URL for an API context.\n```ts\nconst apiCtx = await request.newContext({\n  ______: "https://api.example.com",\n});\n```',null,[`baseURL`,`url`,`origin`,`host`],0,`baseURL in newContext sets the base for all relative URL calls on that context.`],[`F`,`A`,`Complete to send multipart form data (file upload).
\`\`\`ts
const response = await request.post("/upload", {
  ______: {
    file: fs.createReadStream("./report.pdf"),
    name: "report",
  },
});
\`\`\``,null,[`multipart`,`form`,`data`,`files`],0,`multipart: sends multipart/form-data — the correct encoding for file uploads.`],[`E`,`B`,'What is wrong?\n```ts\nconst response = await request.get("/api/users");\nconst data = response.json(); // missing await\n```',null,[`json() is synchronous for API responses`,`json() is async — it returns a Promise. Without await, data is a Promise object, not the parsed JSON.`,`response.json() requires Content-Type check first`,`json() is only valid after ok() check`],1,`All response body methods (json, text, body) are async — always await them.`],[`E`,`I`,'What is wrong?\n```ts\nconst response = await request.get("/api/data");\nconsole.log(response.json()); // may throw\n```',null,[`json() throws when called without await`,`json() can throw if the response body is not valid JSON (e.g. an HTML error page). Always check response.ok() before calling json(), or wrap in try/catch.`,`json() only works after status check`,`This is fine — json() handles all content types`],1,`Unexpected non-JSON responses (502 HTML error, empty body) cause json() to throw — always guard with ok() or status check.`],[`E`,`B`,'What is wrong?\n```ts\nconst response = await request.get("/api/users",\n  { params: "name=Alice" } // wrong type\n);\n```',null,[`params is not a valid option`,`params must be an object: { name: "Alice" } — Playwright builds the query string from the object properties`,`params requires URLSearchParams type`,`Params must be part of the URL directly`],1,`params: takes an object, not a pre-encoded string — Playwright handles the encoding.`],[`E`,`I`,`What is wrong?
\`\`\`ts
// request is the test fixture
const apiCtx = await request.newContext({ baseURL: "https://api.example.com" });
// lots of requests...
// no dispose call
\`\`\``,null,[`Manually created contexts dispose automatically at test end`,`Manually created APIRequestContexts must be explicitly disposed with await apiCtx.dispose() — without it, connections and cookies leak for the duration of the test run`,`newContext() is only for browser contexts`,`The request fixture disposes the child context`],1,`Manual context lifecycle management is the responsibility of the test — always dispose in fixture teardown or test afterEach.`],[`E`,`A`,'What is wrong?\n```ts\nconst response = await page.request.get("/api/secure");\n// Used in an unauthenticated test context\nexpect(response.status()).toBe(200);\n```',null,[`page.request cannot be used for GET requests`,`page.request shares the page's browser context cookies — if the page is not authenticated, the request is also unauthenticated and may return 401/403. Load storageState or log in first.`,`page.request only works with POST`,`expect is synchronous so status() needs await`],1,`page.request inherits the page's session — unauthenticated page = unauthenticated API request.`],[`E`,`I`,`What is wrong?
\`\`\`ts
await request.post("/api/users", {
  form: { name: "Alice" },
  data: { role: "admin" }, // both form and data
});
\`\`\``,null,[`form and data can be combined for multipart requests`,`form and data are mutually exclusive — they set different Content-Type headers. Using both is undefined behavior; only the last one takes effect.`,`data overrides form automatically`,`They are merged into a single JSON body`],1,`Choose one: form for URL-encoded, data for JSON, multipart for multipart/form-data — they cannot coexist in one request.`],[`D`,`B`,`API test fails with "ECONNREFUSED" when calling a local endpoint. What is the most likely cause?`,null,[`The request fixture is broken`,`The local server is not running or is on a different port. Verify the server is started (use webServer in config) and the baseURL port matches.`,`CORS is blocking the request`,`The API requires authentication`],1,`ECONNREFUSED means nothing is listening on that port — start the server with webServer config or run it separately.`],[`D`,`I`,"`response.json()` returns `{}` even though the API returns data. What should you check?",null,[`json() always returns an empty object first`,`The response Content-Type may be text/plain or the body may be empty for this status code. Also check if you're hitting the right endpoint (log response.url()).`,`json() requires res.ok() to return true`,`The API is returning gzipped data`],1,`Log response.status() and response.url() first — you may be hitting a redirect or a different endpoint than expected.`],[`D`,`B`,`PUT request returns 200 but the data is not updated when you GET it. What should you investigate?`,null,[`PUT is not supported for partial updates`,`Check that you are hitting the same endpoint with the same ID; the test may use a different environment (dev vs staging) for the PUT and GET calls`,`Retry the GET after 1 second`,`Use PATCH instead of PUT`],1,`Environment mismatch is a common cause — verify both calls use the same baseURL and the ID returned by POST is correctly propagated to the PUT and GET.`],[`D`,`A`,`API tests are flaky — sometimes the response is 429 (rate-limited). How do you handle this?`,null,[`Add retries: 3 to the config`,`Implement exponential backoff in a retry helper: attempt the request, check for 429, wait 2^n seconds, retry up to 3 times. Or use request.newContext with httpCredentials that bypass rate limits in the test environment.`,`Set timeout: 60000 to wait out the rate limit`,`Use page.route() to mock the API rate limit response`],1,`Rate limiting in tests usually indicates hitting a shared environment — use dedicated test environment with relaxed limits, or implement retry logic for 429 responses.`],[`D`,`I`,"`request.delete()` returns 204 No Content but `response.json()` throws. Why is this expected?",null,[`DELETE is not allowed to return a body`,`HTTP 204 means "No Content" — the response body is intentionally empty. Calling json() on an empty body throws. Check for 204 with status() before calling json().`,`json() requires Content-Type header`,`response.ok() must return true before json()`],1,`204 No Content is correct for successful deletes — test for it with expect(response.status()).toBe(204) and skip the json() call.`],[`C`,`B`,'Complete to check if the response is successful.\n```ts\nif (!response.______()) {\n  throw new Error("Request failed: " + response.status());\n}\n```',null,[`ok()`,`success()`,`isOK()`,`valid()`],0,`ok() returns true for 2xx status codes — the idiomatic success check.`],[`C`,`I`,'Complete to send query parameters.\n```ts\nconst resp = await request.get("/api/search", {\n  ______: { q: "playwright", limit: 10 },\n});\n```',null,[`params`,`query`,`qs`,`search`],0,`params: object is encoded and appended as the query string.`],[`C`,`B`,'Complete to send form-encoded data.\n```ts\nawait request.post("/login", {\n  ______: { username: "alice", password: "secret" },\n});\n```',null,[`form`,`data`,`body`,`headers`],0,`form: sends application/x-www-form-urlencoded — matching a standard HTML form POST.`],[`C`,`I`,"Complete to get the response body as text.\n```ts\nconst html = await response.______();\n```",null,[`text()`,`body()`,`content()`,`raw()`],0,`text() returns the response body as a string — use for non-JSON responses.`],[`C`,`A`,'Complete to make a PATCH request.\n```ts\nconst resp = await request.______(`/api/users/${id}`, {\n  data: { name: "Updated" },\n});\n```',null,[`patch`,`update`,`put`,`post`],0,`request.patch() sends the HTTP PATCH method — for partial updates.`],[`L`,`B`,"Why is the `request` fixture preferred over Node.js `fetch` for Playwright API tests?",null,[`fetch is not available in Node.js`,`request integrates with Playwright's baseURL config, shares cookies via page.request, participates in the fixture lifecycle (auto-dispose), and has a Playwright-idiomatic API — fetch works but requires manual URL building and cookie management`,`request is faster because it uses the browser's network stack`,`fetch cannot send DELETE requests`],1,`Integration with config, cookies, and fixture lifecycle makes request the better choice for Playwright API testing.`],[`L`,`I`,"When should you use `page.request` instead of the `request` fixture?",null,[`Always — page.request is more powerful`,`When the API call needs to share the page's session (cookies, auth tokens) — e.g. calling a session-authenticated endpoint that the logged-in user can access. The request fixture is a separate isolated context.`,`When testing a POST endpoint`,`When the test uses storageState`],1,`Session sharing is the key distinction — page.request is session-aware, the request fixture is isolated.`],[`L`,`I`,"Can `response.json()` be called multiple times?",null,[`Yes — it caches the result after the first call`,`No — the response body stream can only be consumed once. Calling json() a second time throws. Store the result in a variable.`,`Yes — each call re-parses the body`,`Only if ok() returned true`],1,`Response bodies are streams — consume them exactly once. Store the result: const body = await response.json()`],[`L`,`B`,"What HTTP method does `request.put()` use?",null,[`POST with a _method=PUT field`,`HTTP PUT — used for full resource replacement`,`HTTP PATCH — same as put()`,`HTTP POST with the PUT header`],1,`put() sends HTTP PUT — for complete resource replacement. patch() is for partial updates.`],[`L`,`A`,"Can the `request` fixture be used in `globalSetup`?",null,[`Yes — import request from @playwright/test`,`No — globalSetup does not have access to Playwright fixtures. Use playwright.request.newContext() directly in globalSetup and dispose it manually.`,`Yes — globalSetup automatically imports all fixtures`,`Only if globalSetup is async`],1,`Fixtures are test-runner constructs — they are not available in globalSetup. Use the low-level playwright.request API directly.`],[`R`,`B`,`You need to create 10 test users before a test and delete them all after. What is the most efficient pattern?`,null,[`Create them one by one with 10 request.post() calls`,`Create them in parallel with Promise.all([...Array(10).keys()].map(i => request.post("/api/users", { data: { name: "User" + i } }))) then delete in the same way in fixture teardown`,`Use a single batch API endpoint to create all 10`,`Create 1 user and clone the record 9 times`],1,`Promise.all for parallel API seeding is dramatically faster than sequential calls for independent resources.`],[`R`,`I`,`You need to verify a complex JSON object returned by the API. How do you assert nested fields?`,null,[`Convert to string and use toContain`,`const body = await response.json(); expect(body.user.roles).toContain("admin") — access nested paths directly and use appropriate Jest/expect matchers for each field`,`Use toMatchObject on the full body`,`Compare as string with exact JSON`],1,`Destructure the JSON and assert specific fields with type-appropriate matchers — toEqual for exact deep equality, toContain for array membership.`],[`R`,`A`,`You need to test pagination: first page returns 20 items, second page returns the next 20. How do you write this test?`,null,[`GET both pages simultaneously and merge results`,`GET page=1, assert 20 items and a next link, GET page=2 using the next link from the response, assert 20 items and correct offset`,`Use websocket to stream all results`,`Mock the pagination response`],1,`Follow the actual API contract: first response drives the second request — this tests the pagination metadata too.`],[`R`,`I`,`You want to test that an API endpoint rejects requests without an Authorization header. How?`,null,[`Remove the header from the config and retry`,`Create a request context without credentials: await request.newContext() (no extraHTTPHeaders), call the endpoint, expect 401`,`Use route() to strip the header`,`Toggle authentication in the test environment`],1,`A separate unauthenticated context cleanly tests the 401 scenario without affecting the authenticated request fixture.`],[`R`,`B`,"After a successful POST, the response includes an `id` field. How do you use it in a subsequent GET?",null,[`Access it from page.lastResponse`,`const body = await response.json(); const { id } = body; then use id in the next request URL`,`Store it in process.env`,`Extract from the Location header with headers()["location"].split("/").pop()`],1,`Capture the id from the POST response body and use it in subsequent request URLs — standard CRUD test pattern.`],[`S`,`B`,`An API test passes individually but fails in a parallel run with "resource already exists". What is the problem?`,null,[`API tests cannot run in parallel`,`Multiple tests are creating a resource with the same name/ID — use UUID: name: "user-" + crypto.randomUUID() to guarantee uniqueness across parallel tests`,`The API has a bug with concurrent requests`,`Add retries to handle the conflict`],1,`Static test data in parallel tests is a classic isolation failure — always generate unique identifiers.`],[`S`,`I`,`You need to verify that creating a resource via UI stores it correctly in the database. How?`,null,[`Inspect the database directly from the test`,`Use the UI to create the resource, then call the read API to verify the stored data matches what was entered — no direct DB access needed`,`Compare UI screenshots before and after`,`Check the network request payload`],1,`API read verification is the clean alternative to direct DB access — test the full stack through the public API.`],[`S`,`A`,`An API returns different response shapes for different user roles. How do you test both shapes?`,null,[`Use one test with conditional assertions`,`Separate tests: one authenticated as a regular user and one as an admin (using separate request contexts or page.request with different storageState) — each asserts its expected shape`,`Mock the API to return both shapes`,`Use expect.objectContaining for flexible matching`],1,`Separate contexts per role give clean, independent tests with clear assertions about each role's response shape.`],[`S`,`I`,`A test creates API data in setup but the cleanup DELETE sometimes fails with 404 (already deleted). How do you make cleanup robust?`,null,[`Skip the cleanup if it might fail`,`In fixture teardown: const resp = await request.delete(url); if (resp.status() !== 404) { expect(resp.ok()).toBeTruthy() } — accept 404 as already-cleaned but fail on other errors`,`Wrap delete in try/catch and ignore all errors`,`Re-create the resource if deleted to keep state consistent`],1,`Idempotent cleanup accepts 404 (already gone) as success — prevents test failures from double-cleanup while still catching unexpected errors.`],[`L`,`A`,"What is the advantage of using Playwright's `request` fixture (APIRequestContext) to set up test data via direct API calls, instead of driving the UI to create that data?",null,[`API calls are dramatically faster and more reliable than UI interactions, and decouple test setup from unrelated UI flows — so a test focused on 'editing a user' doesn't also depend on the 'create user' UI form working correctly`,`The request fixture can interact with the DOM the same way page.locator() does`,`API calls automatically update the currently open page's UI in real time`,`The request fixture only works for GraphQL APIs, not REST`],0,`Common best practice: use the API for test data setup/teardown (fast, focused) and reserve UI automation for what you're actually testing.`],[`O`,`I`,`What is this pattern accomplishing?`,`const response = await request.post('/api/login', {
  data: { username: 'test', password: 'pass' },
});
const { token } = await response.json();
await context.addCookies([{ name: 'session', value: token, url: baseURL }]);`,[`Logging in via a direct API call (bypassing the UI login form entirely) and injecting the resulting session token as a cookie, so the browser context starts already authenticated`,`It mocks the login response without ever making a real network call`,`It logs in through the UI and then also calls the API redundantly`,`It creates a new user account, ignoring any existing session`],0,`This is the API-based login pattern — much faster than filling in a login form via the UI for every test that needs an authenticated session.`],[`R`,`I`,`You want to mock a third-party payment API entirely so tests never hit the real (paid, rate-limited) payment provider, while still exercising your app's real checkout UI. What is the right tool?`,null,["`page.route()` to intercept requests to the payment provider's domain and `route.fulfill()` with a synthetic response — the UI runs for real, only the external network call is mocked",`Use the request fixture to call the real payment API once and cache the response for all future tests`,`Skip testing checkout entirely since third-party APIs cannot be tested`,`Set the payment provider's API key to a fake value and expect the real API to still respond correctly`],0,`Route-level mocking replaces only the risky/costly external dependency while keeping everything else (your own app's UI and logic) genuinely exercised.`],[`D`,`I`,"A test mocks `**/api/orders` to return a fixed JSON array, but the page still shows a loading spinner forever and never renders the mocked orders. What's a likely cause?",null,["The mock response's shape doesn't match what the frontend expects (e.g., missing a field, wrong content-type, or the app expects `{ data: [...] }` but the mock returns a bare array) — the frontend may be silently failing to parse/handle it",`route.fulfill() cannot return JSON, only plain text`,`Mocked responses are always delayed by exactly 30 seconds by Playwright`,`The spinner is a CSS-only animation and is entirely unrelated to network behavior`],0,`When a mock is set up but the UI mysteriously doesn't reflect it, verify the actual response body/shape exactly matches the contract the frontend code expects.`],[`L`,`A`,`What does "contract testing" mean in the context of a UI test suite that also makes direct API calls, and what problem does it guard against?`,null,[`Contract testing verifies that the API's actual response SHAPE (fields, types) matches what the frontend code expects — guarding against silent breakage when a backend team changes a field name or type without the frontend being updated in lockstep`,`It means legally validating the API's terms of service before testing`,`It refers to testing the UI's visual contract (design system compliance) rather than any API behavior`,`Contract testing is a synonym for load testing the API under contract-specified traffic volumes`],0,`This becomes especially valuable in larger organizations where frontend and backend teams deploy independently — a contract test catches a breaking API change before it reaches production.`],[`O`,`A`,`What does this test verify beyond a simple 200 status check?`,`const res = await request.get('/api/users/1');
const body = await res.json();
expect(res.status()).toBe(200);
expect(body).toMatchObject({
  id: expect.any(Number),
  email: expect.stringContaining('@'),
});`,[`It validates the SHAPE and basic type/format correctness of the response body (id is a number, email contains "@"), not just that the request succeeded — catching cases where a 200 response has malformed or missing data`,`It ensures the response arrives in under 200ms`,`It confirms the endpoint requires no authentication`,`toMatchObject requires an EXACT match with no extra fields allowed`],0,`A 200 status alone says nothing about whether the payload is actually well-formed — schema/shape assertions catch a whole class of bugs that a bare status check would miss.`],[`R`,`I`,`You want to verify that creating a resource via the UI form and then immediately fetching it via a direct API call return consistent data (same values, correctly persisted). What is a robust test structure for this?`,null,[`Perform the creation through the UI, capture the created resource's ID (e.g., from a success toast or redirect URL), then make a direct API GET request for that ID and assert its fields match what was entered in the form`,`Only test via the UI; direct API verification is redundant and never adds value`,`Only test via the API; the UI creation flow does not need separate coverage`,`Compare two unrelated resources created independently by the UI and the API to check general consistency`],0,`Combining a UI action with an API-level verification of its actual persisted effect is a strong way to catch bugs where the UI "looks" successful but the backend state is subtly wrong.`],[`O`,`I`,"What does the `timeout` option do for a direct API request, and how does it relate to the global test timeout?",`const res = await request.get('/api/slow-report', { timeout: 5000 });`,[`It bounds how long this SPECIFIC request is allowed to take before Playwright throws a timeout error for it — independent of (and typically much shorter than) the overall test timeout, letting you fail fast on a specific slow call`,`It sets how long the ENTIRE test file has to complete all its requests combined`,`timeout on a request has no real effect; requests always wait indefinitely regardless of this option`,`It only applies to POST/PUT requests, never GET`],0,`A tight per-request timeout on top of the global test timeout gives faster, more specific failure signals — "this particular slow-report endpoint timed out" instead of "the whole test eventually timed out".`],[`O`,`A`,`What does this accomplish?`,`const res = await request.post('/api/upload', {
  multipart: {
    file: { name: 'photo.png', mimeType: 'image/png', buffer: fileBuffer },
    description: 'Profile photo',
  },
});`,['It sends a multipart/form-data request containing both a binary file field and a regular text field — exactly matching how a real `<form enctype="multipart/form-data">` file upload would submit to the server',`It sends the file as a base64 string inside a plain JSON body`,`multipart uploads are not supported by the request fixture; only page-driven file inputs work`,`It uploads the file to a CDN automatically before sending the API request`],0,`The multipart option lets you construct realistic file-upload requests entirely at the API level, without ever driving an actual file input through the UI.`],[`D`,`I`,`A suite of API tests passes locally but intermittently fails in CI with "ECONNRESET" or similar transient network errors against a shared staging environment. What is a reasonable mitigation, separate from fixing the environment itself?`,null,[`Add a small, bounded retry specifically around the flaky network call (or use Playwright's built-in test-level retries for that spec), while also flagging the shared environment's reliability as a separate infrastructure concern to track and fix`,`Silently ignore all network errors in every API test going forward`,`Switch every API test to mock the network entirely, removing all real backend verification`,`Increase the request timeout to 10 minutes so the connection eventually succeeds no matter what`],0,`A bounded retry treats transient infrastructure flakiness pragmatically without masking genuine, reproducible bugs — but it should come with a parallel effort to actually stabilize the shared environment.`],[`L`,`A`,"How do you test a GraphQL endpoint using Playwright's `request` fixture, since GraphQL typically exposes a single URL for all operations?",null,["POST to the GraphQL endpoint with a JSON body containing `query` and `variables` fields, then parse the `data`/`errors` fields from the JSON response",`Only page.route() can intercept GraphQL, request cannot call it directly`,`GraphQL requires a dedicated Playwright plugin to be installed`,`Send the query as URL query parameters on a GET request`],0,"GraphQL servers usually return HTTP 200 even when the query itself failed, so you must inspect the `errors` array in the body rather than trusting status() alone."],[`L`,`I`,"What does passing `ignoreHTTPSErrors: true` to `request.newContext()` allow?",null,[`It disables JSON parsing for HTTPS responses`,`It lets the API client accept self-signed or otherwise untrusted TLS certificates when calling a staging/internal server`,`It skips authentication for HTTPS endpoints`,`It forces all requests to downgrade to plain HTTP`],1,`Common for internal staging environments with self-signed certs; you would never need or want this for real production traffic with valid certificates.`],[`L`,`I`,"What does the `httpCredentials` option do when creating an API request context?",null,[`It stores the app's login form field names`,`It encrypts the request body`,`It automatically adds an HTTP Basic Authorization header to every request from that context`,`It generates a new OAuth token per request`],2,`httpCredentials answers HTTP Basic Auth challenges (often set up at a reverse proxy) — a different mechanism from your application's own login flow.`],[`L`,`A`,"Why might you configure a `proxy` option on `request.newContext()` in a corporate CI environment?",null,[`To cache API responses between test runs`,`To bypass rate limiting on third-party APIs`,`To encrypt request headers`,`To route outbound API calls through a required corporate proxy/firewall so they can reach external hosts, mirroring how the app's real traffic is routed`],3,`Without matching the network path real users/servers take, tests calling external hosts directly might fail in a locked-down CI network.`],[`L`,`I`,"Why would an API test send an `Idempotency-Key` header when retrying a failed POST request?",null,[`To guarantee retrying a request that may have already been processed doesn't create a duplicate resource`,`To speed up the retry by skipping validation`,`To tell the server which test is running`,`To disable authentication on retry`],0,`The server recognizes the same key on a retried request and returns the original result instead of creating a second resource.`],[`L`,`A`,"Your app receives webhooks from a third-party service (e.g. a payment provider) at an endpoint like /webhooks/payment. How can Playwright's `request` fixture help test this flow without needing the real third party to fire the webhook?",null,[`It cannot — webhook testing requires the real provider`,`Use request.post() to POST a synthetic payload with a valid signature directly to your app's webhook endpoint, simulating what the third party would send`,`Use page.route() to intercept the outgoing webhook`,`Mock the entire third-party service using routeFromHAR()`],1,`Since your webhook handler is just another endpoint on your own server, you can drive it directly with a crafted request instead of waiting on an external trigger.`],[`L`,`A`,`You need to test that your app correctly refreshes an expired OAuth2 access token. What's an effective API-level testing approach?`,null,[`Manually edit the OAuth provider's database`,`Disable token expiry entirely for tests`,`Seed a request context with a token that's already expired/near expiry, make an API call through the app's normal flow, and assert a refresh request fires and the retried call succeeds`,`Always use a token that never expires in every environment`],2,`Forcing the expired-token condition deterministically lets you assert on the refresh behavior itself, rather than waiting for a real token to expire.`],[`L`,`I`,"An API supports both `/v1/users` and `/v2/users` (different response shapes). What's a maintainable way to test both versions with Playwright?",null,[`Only test v2 since v1 will eventually be removed`,`Copy-paste every test file once per version`,`Manually run the suite twice with different global find-and-replace edits`,`Parametrize tests (e.g. loop over versions or use a fixture that injects the version) so both are exercised with the same assertion structure`],3,`Parametrization avoids duplicated test logic while still giving each version's contract independent coverage.`],[`L`,`A`,"If you use the `request` fixture to call an API on a completely different origin than your app, will a CORS error occur the way it would from browser JS?",null,[`No — APIRequestContext isn't a browser page, so it doesn't enforce the browser's same-origin policy or CORS at all; the request goes through regardless of CORS headers`,`Yes, exactly the same as in a browser`,`Only if the response is missing Access-Control-Allow-Origin`,`Only for POST requests, not GET`],0,`CORS is a browser-side security enforcement mechanism — a plain Node-level HTTP client like APIRequestContext has no such restriction.`],[`L`,`I`,"What's an advantage of validating API responses with a schema library (e.g. zod, ajv) over `expect(body).toMatchObject({...})` in Playwright API tests?",null,[`Schema libraries are always faster to run`,`A schema validates types/shape comprehensively (required/optional fields, enums, formats) in one reusable definition, catching more classes of contract drift than a handful of manual matcher assertions`,`toMatchObject cannot be used with JSON at all`,`Schema libraries automatically generate mock servers`],1,`A single schema definition can be reused across many tests and enforces format/type rules that ad-hoc matchers would need to spell out individually.`],[`L`,`I`,"What's a downside of using `expect(body).toMatchSnapshot()` to assert an entire API response body, compared to targeted field assertions?",null,[`toMatchSnapshot only works with images`,`It cannot compare JSON data at all`,`Snapshots capture every field including ones expected to vary (timestamps, generated IDs), so they become brittle and require frequent, low-value snapshot updates`,`It is slower than a real HTTP request`],2,`Any legitimately-changing field forces a snapshot update even when nothing is actually broken, which trains reviewers to rubber-stamp diffs.`],[`L`,`B`,`How do you verify via the API that an endpoint returns a downloadable file with the correct filename?`,null,[`Download the file and open it manually`,`Check the response status code only`,`Assert the URL contains the filename`,'Inspect the Content-Disposition response header (e.g. `attachment; filename="report.pdf"`) and assert its contents'],3,`The Content-Disposition header is the standard place the filename is communicated, without needing to actually parse the downloaded bytes.`],[`L`,`B`,"Why would you wrap several related `request` calls inside `test.step()` in an API test?",null,[`It groups the calls under a named, collapsible step in the HTML report and trace, making a multi-call API workflow easier to read and debug when it fails`,`test.step() is required for request calls to work`,`It automatically retries failed calls`,`It disables parallelism for those calls`],0,`Named steps give a failing multi-request test a much more navigable trace than an unlabeled sequence of calls.`],[`L`,`I`,"How can Playwright's `projects` config be used to run the same API test suite against staging and production with different base URLs?",null,[`It cannot — a separate repo is required per environment`,"Define separate projects each with their own `use: { baseURL }` (and other environment settings), then run a project by name via --project",`Use environment variables inside every test file individually`,`Rename the spec files per environment`],1,`Projects let the exact same spec files run against multiple environments just by switching the --project flag, with no code duplication.`],[`L`,`A`,"Why would a team create a custom `apiContext` fixture that wraps `request.newContext()` with preset baseURL, auth headers, and automatic disposal, instead of using the built-in `request` fixture directly in every test?",null,[`The built-in request fixture cannot make POST calls`,`Custom fixtures run faster than built-in ones`,`It centralizes environment/auth setup and teardown in one place, so tests stay focused on behavior instead of repeating boilerplate`,`It is required for parallel test execution`],2,`Changes to auth or environment config then only need to happen in one fixture definition instead of every test file.`],[`L`,`I`,"What does sending `headers: { Accept: 'application/xml' }` on a `request.get()` call test?",null,[`Whether the server encrypts the response`,`Whether the endpoint accepts file uploads`,`Whether the server supports HTTP/2`,`Whether the server performs content negotiation and returns the response in the requested format instead of its default`],3,`Content negotiation lets a single endpoint serve multiple representations of the same resource based on the Accept header.`],[`L`,`A`,"When would you use `expect.poll()` instead of a single `request.get()` + assertion in an API test?",null,[`When the backend is eventually consistent (e.g. an async job or search index) and the expected state may not be immediately visible after triggering it`,`Never — expect.poll() only works with page locators`,`When you need to make exactly one request`,`When testing DELETE endpoints specifically`],0,`expect.poll() re-runs the callback and re-checks the matcher on an interval until it passes or times out, which suits eventually-consistent backends.`],[`L`,`I`,"What's the difference between `expect.poll(callback)` and `expect(async () => {...}).toPass()`?",null,[`They are identical aliases`,`expect.poll() re-runs a callback and compares its return value against a matcher each interval; toPass() re-runs an entire block of code (including its own assertions) until none of them throw`,`toPass() only works for API tests, expect.poll() only for UI tests`,`expect.poll() cannot have a timeout`],1,`toPass() wraps arbitrary retryable logic with internal assertions, while expect.poll() is built specifically around retrying a value-returning function against one matcher.`],[`L`,`I`,`You trigger a report-generation endpoint that returns 202 Accepted with a jobId, and the report becomes ready later at a status endpoint. What's the idiomatic Playwright pattern to wait for completion?`,null,[`await page.waitForTimeout(30000) then assume it is ready`,`Retry the POST request until it succeeds`,`Use expect.poll() to repeatedly GET the job status endpoint until it reports done, instead of a fixed sleep`,`Use routeFromHAR to skip the wait entirely`],2,`Polling with a bounded timeout adapts to however long the job actually takes, instead of guessing a fixed delay that's either too short (flaky) or too long (slow).`],[`L`,`B`,`How would you configure Playwright so API tests (e.g. files ending in .api.spec.ts) run without launching a browser, separately from UI tests?`,null,[`It is impossible to run Playwright tests without a browser`,`Set headless: true globally`,`Delete the playwright.config.ts file`,`Define a project with a testMatch pattern for those files and no browser device settings in its use block`],3,`API-only tests never need a launched browser at all — a project scoped by testMatch keeps them lightweight and fast.`],[`L`,`A`,"Why might a test suite define a custom Playwright matcher like `expect(response).toBeAPISuccess()` instead of always writing `expect(response.ok()).toBeTruthy()`?",null,[`A custom matcher can produce a much more informative failure message (e.g. including status, URL, and body) and centralizes the "what counts as success" logic`,`Custom matchers execute requests faster`,`toBeTruthy() cannot be used with booleans`,`It removes the need for the request fixture`],0,`A bare boolean assertion tells you nothing about WHY the response wasn't ok — a custom matcher can surface exactly that in the failure output.`],[`L`,`I`,`In a hybrid test that logs in via API and then also needs page.request calls to share that session, what must happen after the API login succeeds?`,null,[`Nothing — page.request automatically detects any prior API login`,`The auth cookie/token obtained from the API call must be added to the browser context (e.g. via context.addCookies()) so page.request, which uses the page's context, is also authenticated`,`The test must call request.newContext() again`,`The browser must be relaunched`],1,`The standalone request fixture is an isolated session — its cookies do not automatically flow into the page's browser context.`],[`L`,`I`,"If a server responds with `Content-Encoding: gzip`, does Playwright's `response.text()`/`response.json()` return the compressed bytes or the decompressed content?",null,[`The raw compressed bytes, requiring manual decompression`,`It throws an error on any compressed response`,`Decompressed content — APIRequestContext automatically decompresses gzip/deflate/brotli responses before you read the body`,`Only text() decompresses; json() does not`],2,`Automatic decompression means tests never have to special-case a server that happens to compress its responses.`],[`L`,`I`,"Why does it not matter whether you write `response.headers()['Content-Type']` or `response.headers()['content-type']`?",null,[`Both keys always coexist in the object`,`JavaScript object keys are case-insensitive`,`Content-Type is a special-cased header exempt from case rules`,`HTTP header names are case-insensitive, and Playwright normalizes all header keys returned by headers() to lowercase`],3,`Only the lowercase key form actually exists on the returned object — accessing a mixed-case key like "Content-Type" would return undefined.`],[`L`,`I`,"Can the `data` option in `request.post()` be a plain array instead of an object, e.g. `data: [1, 2, 3]`?",null,[`Yes — data accepts any JSON-serializable value, and it is serialized with the correct Content-Type just like an object would be`,`No, data must always be an object`,`Only if the array is first converted to a Map`,`Arrays require the multipart option instead`],0,`JSON itself allows top-level arrays, and Playwright's data option simply JSON-serializes whatever value you give it.`],[`L`,`B`,"What happens if you call `request.patch(url)` with no `data` option at all?",null,[`Playwright throws a validation error before sending anything`,`The request is sent with an empty body — useful for endpoints where the PATCH itself (not a payload) triggers an action`,`The request automatically becomes a GET`,`It defaults to sending an empty JSON array`],1,`Some action-style endpoints (like "cancel this order") don't need any fields in the body — the HTTP method and URL alone convey the intent.`],[`L`,`A`,"A response sets multiple cookies via several `Set-Cookie` headers. Why might `response.headers()['set-cookie']` be insufficient to inspect them all?",null,[`set-cookie is never included in headers() at all`,`Cookies can only be read via context.cookies()`,`headers() folds same-named headers into a single combined value/string, potentially losing individual entries — use response.headersArray() to get every header instance separately`,`Playwright silently drops all but the first Set-Cookie header from the network layer`],2,`headersArray() returns every header instance as a separate {name, value} pair, preserving duplicates that a flattened object representation would merge or lose.`],[`L`,`A`,"Can Playwright's `request` fixture be used to consume a Server-Sent Events (SSE) stream and assert on individual events as they arrive?",null,[`Yes, it natively parses SSE event boundaries`,`SSE is not HTTP-based so Playwright cannot connect at all`,`Only if the stream uses WebSocket internally`,`Not well — APIRequestContext is built around a request/response model and buffers the whole body, making long-lived streaming responses awkward to test this way`],3,`For SSE-driven UI behavior, testing through the real page and its EventSource-driven updates is usually more practical than trying to consume the raw stream via the request fixture.`],[`L`,`I`,"Can `request.newContext()` (APIRequestContext) open and interact with a WebSocket connection?",null,[`No — APIRequestContext is HTTP request/response only; WebSocket testing requires a Page and page.waitForEvent('websocket')`,`Yes, via request.newContext({ protocol: 'ws' })`,`Yes, but only for read-only WebSocket monitoring`,`Only in Chromium-based projects`],0,`APIRequestContext models a plain HTTP client; WebSocket connections are a page-level browser feature Playwright exposes separately.`],[`L`,`I`,`Beyond the 429 status code itself, what else should a robust API test check when verifying rate-limiting behavior?`,null,[`The response body length only`,`Rate-limit-related response headers such as X-RateLimit-Remaining and Retry-After, to confirm the server communicates its limits/backoff guidance correctly`,`The request's User-Agent header`,`Nothing else is meaningful to assert`],1,`A client needs those headers to implement correct backoff — verifying just the 429 status misses whether the server actually tells clients how long to wait.`],[`L`,`B`,"If `baseURL` is set in the Playwright config's `use` block, what happens if a test calls `request.get('https://other-service.com/health')` (a full absolute URL)?",null,[`Playwright throws an error because it conflicts with baseURL`,`The request is sent to baseURL + the absolute URL concatenated`,`baseURL is ignored for that call — an absolute URL always overrides the configured baseURL`,`It silently uses baseURL instead of the given URL`],2,`baseURL only matters for relative paths — a fully-qualified URL always wins, letting a single test reach a different host when needed.`],[`L`,`A`,`What's the performance benefit of making a custom API context fixture worker-scoped rather than test-scoped, when tests only read data and don't mutate shared state?`,null,[`Worker-scoped fixtures automatically parallelize individual requests`,`Worker scoping makes assertions run faster`,`It disables retries, which speeds up the suite`,`The APIRequestContext (and its connections) is created once per worker and reused across many tests in that worker, avoiding new-context overhead per test`],3,`Establishing a fresh context (and its TLS/TCP handshakes) has real overhead — reusing one per worker amortizes that cost across many tests.`],[`L`,`I`,`How would you test that an endpoint correctly returns HTTP 304 Not Modified for a conditional GET request?`,null,["Make an initial GET, capture the ETag response header, then make a second GET passing `headers: { 'If-None-Match': etag }` and assert the second response's status() is 304",`Send two identical GET requests and expect both to return 304`,`Set a very short timeout on the second request`,`Compare response.text() lengths between two calls`],0,`304 responses are only returned when the client tells the server, via If-None-Match, which version it already has cached.`],[`L`,`A`,`For a cursor-based (not page-number-based) paginated API, what does a correct test need to do differently from testing offset/page pagination?`,null,[`Nothing — increment a page number the same way`,`Extract the nextCursor/cursor value from each response and pass it into the next request's params — the test cannot simply increment a page number`,`Always request page=1 repeatedly`,`Use routeFromHAR instead of real requests`],1,`Cursor tokens are opaque and server-defined, so the test must follow the chain the API actually hands back rather than guessing sequential numbers.`],[`L`,`I`,"Why would a security-conscious test suite specifically assert that a user API response does NOT contain a `password` or `passwordHash` field?",null,[`To improve response parsing performance`,`Because the field name is reserved by Playwright`,`To catch a regression where a backend change accidentally serializes and leaks a sensitive internal field to API consumers`,`To satisfy a linter rule`],2,`This is a genuine and common class of security bug — a serializer change that forgets to exclude an internal field.`],[`L`,`A`,"Why can issuing many parallel `request.get()` calls against the same host be efficient even without special test code to batch them?",null,[`Playwright automatically batches requests into one HTTP call`,`Node.js caches all GET requests automatically`,`APIRequestContext deduplicates identical URLs`,`If the server and client negotiate HTTP/2, a single connection multiplexes many concurrent requests, avoiding full new-connection overhead per call`],3,`HTTP/2 multiplexing means Promise.all()-style parallel calls to the same host don't each pay the cost of a brand-new TCP/TLS handshake.`],[`L`,`I`,`What's a risk of sharing one long-lived APIRequestContext (e.g. worker-scoped) across many tests that also perform authentication?`,null,[`Cookies set by one test's login call persist in that shared context and can leak into later tests, causing them to unexpectedly run as an already-authenticated user`,`The context runs out of memory after 10 requests`,`Worker-scoped contexts cannot make POST requests`,`It forces all tests in that worker to run sequentially`],0,`Without resetting cookies between logical sessions, a shared context can silently carry authentication state across unrelated tests.`],[`L`,`A`,`If you never call any body-reading method (json/text/body) on a response, does Playwright still handle cleanup properly?`,null,[`No — you must always call response.dispose() manually or it leaks`,`Yes — an unread response is automatically cleaned up when the underlying context is disposed`,`No — the test process will hang indefinitely`,`Yes, but only for GET responses`],1,`You only need to read the body when you actually need its contents; Playwright still manages resource cleanup regardless.`],[`L`,`I`,"Why is storing an auth token in an environment variable and reading it via `process.env.API_TOKEN` in a fixture preferred over hardcoding it in the test file?",null,[`Environment variables run faster than string literals`,`process.env is required syntax for Playwright fixtures`,`It keeps secrets out of source control and lets different environments inject different values without changing test code`,`Hardcoded tokens cannot be used with the request fixture`],2,`Hardcoding a real token in a committed file is both a security risk and inflexible across local/CI/staging environments.`],[`L`,`A`,"Why is it generally safe to retry a failed `request.get()` automatically, but risky to blindly retry a failed `request.post()` for creating a resource?",null,[`GET requests are always faster to retry`,`POST requests cannot be retried by design`,`Retrying POST always returns a cached response`,`GET is idempotent — repeating it has no side effects — while a naive POST retry risks creating a duplicate resource if the first request actually succeeded server-side before the client saw a timeout`],3,`A client-observed timeout doesn't guarantee the server-side effect didn't happen — that ambiguity is exactly what makes blind POST retries dangerous.`],[`O`,`A`,`What must this test check to properly validate the GraphQL call succeeded?`,`const res = await request.post('/graphql', {
  data: {
    query: \`query { user(id: 1) { id name } }\`,
    variables: {},
  },
});
const { data, errors } = await res.json();`,[`errors should be checked/undefined and data.user inspected — GraphQL servers commonly return HTTP 200 even for query-level errors`,`Only res.status(), since GraphQL always fails with a non-200 status on errors`,`Only that data is not null`,`Nothing further — a successful POST guarantees a valid result`],0,`status() alone doesn't indicate success for GraphQL — the errors array in the body is the real signal.`],[`O`,`I`,"What problem does `ignoreHTTPSErrors: true` solve here?",`const api = await request.newContext({
  baseURL: 'https://staging.internal.example.com',
  ignoreHTTPSErrors: true,
});`,[`It speeds up TLS handshakes`,`It allows the client to connect even though staging.internal presents a self-signed or otherwise untrusted certificate that would otherwise cause a TLS error`,`It disables cookies for this context`,`It forces requests to use HTTP/1.1`],1,`Internal staging environments frequently run without a certificate signed by a public CA, which browsers/HTTP clients reject by default.`],[`O`,`I`,`What is httpCredentials authenticating against here?`,`const api = await request.newContext({
  baseURL: 'https://staging.example.com',
  httpCredentials: { username: 'ci', password: 'secret' },
});
const res = await api.get('/api/status');`,[`The application's own login form`,`A database connection`,`An HTTP Basic Auth gate in front of the staging environment (e.g. set up at a reverse proxy), separate from any application-level login the API itself might require`,`A CI pipeline secret store`],2,`Many staging environments are protected at the infrastructure layer with Basic Auth, entirely independent of the app's own authentication.`],[`O`,`A`,`What does this configure?`,`const api = await request.newContext({
  proxy: { server: 'http://corp-proxy:8080' },
});`,[`A retry policy for failed requests`,`A caching layer for API responses`,`Automatic load balancing across multiple servers`,`Routes all requests made through this API context via the specified corporate proxy server`],3,`Some corporate/CI networks require all outbound traffic to pass through a designated proxy before reaching the internet.`],[`O`,`I`,`What is the purpose of the Idempotency-Key header here?`,`await request.post('/api/orders', {
  headers: { 'Idempotency-Key': orderKey },
  data: { items },
});`,[`It prevents the server from creating a duplicate order if this exact request is retried after a network timeout`,`It authenticates the request`,`It compresses the request body`,`It logs the request for analytics`],0,`The server recognizes the same key on a retried call and returns the original order rather than creating a second one.`],[`O`,`A`,`What is being tested here?`,`const payload = { event: 'payment.succeeded', amount: 500 };
const signature = createHmac('sha256', secret).update(JSON.stringify(payload)).digest('hex');
await request.post('/webhooks/payment', {
  headers: { 'X-Signature': signature },
  data: payload,
});`,[`The real third-party payment provider's uptime`,`The app's webhook handler, by simulating an authentic-looking incoming webhook call (including a valid HMAC signature) without needing the real third-party service to send one`,`Whether HMAC-SHA256 is a secure algorithm`,`The app's outgoing HTTP client`],1,`Since your webhook handler is just another endpoint you own, you can construct a realistic, correctly-signed request directly against it.`],[`O`,`A`,`What does this verify?`,`await context.addCookies([{ name: 'session', value: expiredToken, url: baseURL }]);
await page.goto('/dashboard');
const [refreshReq] = await Promise.all([
  page.waitForRequest('**/oauth/refresh'),
  page.reload(),
]);`,[`That the dashboard loads without any network requests`,`That reloading the page always clears cookies`,`That the app detects an expired token and automatically triggers a refresh request rather than failing or logging the user out`,`That waitForRequest works with any URL pattern`],2,`Seeding an already-expired token forces the refresh-on-expiry code path to run deterministically.`],[`O`,`I`,`What pattern is this?`,"for (const version of ['v1', 'v2']) {\n  test(`GET /${version}/users`, async ({ request }) => {\n    const res = await request.get(`/${version}/users`);\n    expect(res.ok()).toBeTruthy();\n  });\n}",[`A retry loop for flaky tests`,`A way to run tests in parallel workers`,`An anti-pattern that should be avoided`,`Dynamically generating one test per API version so both are covered without duplicating test code`],3,`Parametrized test generation avoids copy-pasting the same assertions once per version.`],[`O`,`I`,`What advantage does the zod validation have over toMatchObject here?`,`const UserSchema = z.object({ id: z.number(), email: z.string().email() });
const body = await res.json();
expect(() => UserSchema.parse(body)).not.toThrow();`,[`It validates types and formats (email must actually look like an email) comprehensively via one schema definition, not just presence of keys`,`It runs faster than toMatchObject`,`It only checks that the keys exist, nothing more`,`zod cannot be used with async functions`],0,`toMatchObject checks structural presence; a schema enforces actual value formats and types as well.`],[`O`,`I`,`What is the risk with this assertion?`,`const body = await res.json();
expect(JSON.stringify(body)).toMatchSnapshot();`,[`toMatchSnapshot cannot serialize JSON`,`Any field that legitimately changes between runs (timestamps, generated IDs) will cause this snapshot to fail even though nothing is actually broken`,`It will always pass regardless of the response`,`It only compares the first key of the object`],1,`Whole-body snapshotting is brittle against any naturally-varying field, unlike targeted field assertions.`],[`O`,`I`,`What is verified by this test?`,`const res = await request.get('/api/reports/42/download');
const disposition = res.headers()['content-disposition'];
expect(disposition).toContain('filename="report-42.pdf"');`,[`That the download API is fast`,`That the file exists on disk`,`That the download response correctly names the file report-42.pdf via the Content-Disposition header`,`That the PDF content is valid`],2,`Content-Disposition is the standard header a server uses to tell the client the intended filename for a download.`],[`O`,`B`,`What is the benefit of wrapping the API call in test.step() here?`,`await test.step('seed test user via API', async () => {
  await request.post('/api/users', { data: userPayload });
});`,[`It makes the request run faster`,`It automatically retries the request on failure`,`It disables the request fixture's isolation`,`The API call appears as a clearly labeled, collapsible step in the trace/report instead of an unlabeled line inside the test`],3,`Named steps make a failing multi-call test far easier to navigate in the trace viewer.`],[`O`,`I`,`What is the purpose of this config?`,`// playwright.config.ts
projects: [
  { name: 'staging', use: { baseURL: 'https://staging.example.com' } },
  { name: 'prod', use: { baseURL: 'https://example.com' } },
]`,[`To run the exact same API test suite against two different environments by selecting --project=staging or --project=prod`,`To run tests twice as fast`,`To enable parallel workers`,`To disable retries on staging only`],0,`Each project supplies its own baseURL, so the identical spec files can target either environment on demand.`],[`O`,`A`,`What does this custom fixture centralize?`,`export const test = base.extend({
  apiContext: async ({}, use) => {
    const ctx = await request.newContext({ baseURL: process.env.API_URL, extraHTTPHeaders: { Authorization: \`Bearer \${process.env.TOKEN}\` } });
    await use(ctx);
    await ctx.dispose();
  },
});`,[`Nothing — it duplicates the built-in request fixture exactly`,`Base URL, auth header setup, and disposal for every test that uses apiContext, so individual tests don't repeat that boilerplate`,`Test parallelism settings`,`Browser launch configuration`],1,`Centralizing setup/teardown in one fixture keeps individual tests focused purely on the behavior being verified.`],[`O`,`I`,`What is tested here?`,`const res = await request.get('/api/report', { headers: { Accept: 'application/xml' } });
expect(res.headers()['content-type']).toContain('xml');`,[`Whether the server rate-limits XML requests`,`Whether XML parsing is faster than JSON`,`The server's content negotiation — it should honor the requested Accept type instead of always returning JSON`,`Whether the endpoint requires authentication`],2,`This checks that the server actually varies its response format based on the client's stated preference.`],[`O`,`A`,`What does this replace?`,`await expect.poll(async () => {
  const res = await request.get(\`/jobs/\${jobId}\`);
  return (await res.json()).status;
}, { timeout: 15000 }).toBe('completed');`,[`A single synchronous request`,`A WebSocket connection to the job service`,`A retry configured in playwright.config.ts`,`A fixed sleep-then-check pattern — it repeatedly calls the job-status endpoint until it reports completed or the timeout elapses`],3,`Polling adapts to however long the job actually takes rather than guessing a static delay.`],[`O`,`I`,`What is the behavior of this code?`,`await expect(async () => {
  const res = await request.get('/api/search?q=new-item');
  const body = await res.json();
  expect(body.results.length).toBeGreaterThan(0);
}).toPass({ timeout: 10000 });`,[`The entire block (request + assertion) re-runs on an interval until the assertion inside it stops throwing, or the timeout is reached`,`It runs the block exactly once and fails immediately on any error`,`It skips the test if results.length is 0`,`It disables retries for the rest of the test`],0,`toPass() retries the whole enclosed function, unlike expect.poll() which only retries a single value-returning callback against one matcher.`],[`O`,`B`,`What effect does this project config have?`,`// playwright.config.ts
projects: [
  { name: 'api', testMatch: /.*\\.api\\.spec\\.ts/ },
  { name: 'ui', testMatch: /.*\\.ui\\.spec\\.ts/, use: { ...devices['Desktop Chrome'] } },
]`,[`Both projects run in the same browser instance`,`API tests run in their own project without browser device settings, keeping API-only test files from launching a browser`,`It disables the API project entirely`,`It merges API and UI tests into a single suite`],1,`Since the api project has no browser device configuration, files matched by it never trigger a browser launch.`],[`O`,`A`,`What is the benefit of this custom matcher over expect(res.ok()).toBeTruthy()?`,`expect.extend({
  async toBeAPISuccess(response) {
    const ok = response.ok();
    return { pass: ok, message: () => \`Expected \${response.url()} to succeed but got \${response.status()}: \${await response.text()}\` };
  },
});
await expect(res).toBeAPISuccess();`,[`It runs faster than a plain boolean check`,`It automatically retries the request on failure`,`On failure, the error message automatically includes the URL, status, and response body — far more actionable than a bare "expected true, got false"`,`It bypasses the need for the request fixture`],2,`A custom matcher can surface exactly the diagnostic details a developer needs the moment a failure occurs.`],[`O`,`I`,`Why is addCookies necessary here?`,`const loginRes = await request.post('/api/login', { data: creds });
const { token } = await loginRes.json();
await context.addCookies([{ name: 'session', value: token, url: baseURL }]);
await page.goto('/dashboard');`,[`request.post() automatically updates the page's cookies`,`addCookies is required before any navigation regardless of auth`,`It prevents the request from being retried`,`The request fixture used for login has its own isolated session — the token must be manually injected into the browser context's cookie jar before page.goto() will be treated as logged in`],3,`The standalone request fixture and the page's browser context maintain completely separate cookie jars.`],[`O`,`I`,`What happens to the gzip encoding here?`,`const res = await request.get('/api/large-dataset');
// Server sends Content-Encoding: gzip
const body = await res.json();`,[`It is transparently decompressed by Playwright before json() parses it — the test never has to handle compression manually`,`json() throws because it cannot handle compressed bodies`,`The test must manually call zlib.gunzip() first`,`Only the first chunk of the gzip stream is read`],0,`APIRequestContext automatically decompresses standard content encodings before you ever touch the body.`],[`O`,`B`,`Why does the first console.log here log undefined?`,`console.log(response.headers()['Content-Type']); // undefined
console.log(response.headers()['content-type']); // 'application/json'`,[`Content-Type is a reserved word in JavaScript`,`headers() normalizes all header names to lowercase, so a mixed-case key lookup misses`,`The response never actually set a Content-Type header`,`headers() only returns the first header alphabetically`],1,`Only the lowercase key exists on the object headers() returns — HTTP header names are case-insensitive on the wire but the returned object is strictly lowercase.`],[`O`,`I`,`What content-type/body does this send?`,`await request.post('/api/bulk-tags', { data: ['urgent', 'reviewed', 'archived'] });`,[`multipart/form-data with three separate fields`,`application/x-www-form-urlencoded`,`application/json with the array serialized as the JSON body, e.g. ["urgent","reviewed","archived"]`,`text/plain with a comma-separated list`],2,`The data option accepts any JSON-serializable value, arrays included, and sets the correct Content-Type automatically.`],[`O`,`B`,`What is this test verifying?`,`const res = await request.patch('/api/orders/42/cancel');
expect(res.status()).toBe(200);`,[`That PATCH always requires a body`,`That the server rejects bodyless PATCH requests`,`That order 42 does not exist`,`An action-style PATCH endpoint that performs a side effect (cancelling order 42) purely from the URL/method — no request body is needed`],3,`Some endpoints model an action rather than a partial field update, so no payload is required.`],[`O`,`A`,`Why use headersArray() instead of headers() here?`,`const res = await request.post('/api/login', { data: creds });
const allHeaders = res.headersArray();
const cookies = allHeaders.filter(h => h.name.toLowerCase() === 'set-cookie');`,[`headers() would collapse multiple Set-Cookie headers into one merged/lossy value; headersArray() preserves each header instance separately`,`headersArray() is faster to call`,`headers() does not include cookies at all`,`headersArray() is required for POST responses specifically`],0,`When a response sets several cookies, only headersArray() reliably exposes each Set-Cookie header individually.`],[`O`,`I`,`What is asserted beyond the status code here?`,`const res = await request.get('/api/data');
expect(res.status()).toBe(429);
expect(Number(res.headers()['retry-after'])).toBeGreaterThan(0);`,[`That the request took longer than expected`,`That the server also tells the client how long to wait before retrying, via the Retry-After header`,`That the API is completely down`,`That rate limiting is disabled in this environment`],1,`A complete rate-limit test checks not just that the limit was hit, but that the server communicates proper backoff guidance.`],[`O`,`B`,`Which URL does this call actually hit?`,`// playwright.config.ts use: { baseURL: 'https://app.example.com' }
await request.get('https://status.example.com/health');`,[`https://app.example.com/health`,`It throws because the URL conflicts with baseURL`,`https://status.example.com/health — an absolute URL bypasses the configured baseURL entirely`,`https://app.example.com/status.example.com/health`],2,`baseURL only applies when a relative path is given; a full absolute URL always takes precedence.`],[`O`,`A`,`What does { scope: 'worker' } change here?`,`export const test = base.extend({
  api: [async ({ playwright }, use) => {
    const ctx = await playwright.request.newContext({ baseURL: process.env.API_URL });
    await use(ctx);
    await ctx.dispose();
  }, { scope: 'worker' }],
});`,[`It makes the fixture available only in the last test of the worker`,`It disables the fixture's automatic disposal`,`It forces the fixture to run before every describe block, not just once`,`The fixture (and its APIRequestContext) is created once per worker process and shared across all tests that worker runs, instead of being recreated for every single test`],3,`Worker scope trades per-test isolation for reduced setup overhead — appropriate when tests don't mutate shared context state.`],[`O`,`I`,`What does this conditional-GET test verify?`,`const first = await request.get('/api/config');
const etag = first.headers()['etag'];
const second = await request.get('/api/config', { headers: { 'If-None-Match': etag } });
expect(second.status()).toBe(304);`,[`That the server implements conditional GET correctly, returning 304 Not Modified (with no body) when the client's cached version is still current`,`That the config endpoint caches responses client-side`,`That ETag headers are optional`,`That the config never changes`],0,`Conditional requests save bandwidth by letting the server confirm "your cached copy is still valid" instead of resending the whole body.`],[`O`,`A`,`What does this loop accomplish?`,`let cursor = null;
const allItems = [];
do {
  const res = await request.get('/api/items', { params: cursor ? { cursor } : {} });
  const body = await res.json();
  allItems.push(...body.items);
  cursor = body.nextCursor;
} while (cursor);`,[`It fetches only the first page of results`,`It walks through every page of a cursor-paginated endpoint by feeding each response's nextCursor into the next request, until the server stops returning one`,`It creates a new cursor resource on the server`,`It deletes items as it paginates`],1,`Cursor pagination requires chaining the server-provided token rather than guessing sequential page numbers.`],[`O`,`I`,`What regression does this guard against?`,`const res = await request.get('/api/users/1');
const user = await res.json();
expect(user).not.toHaveProperty('passwordHash');`,[`A performance regression in the users endpoint`,`A missing id field`,`A backend change that accidentally includes the password hash field in the public user-serialization response`,`An incorrect HTTP status code`],2,`This kind of test catches a real and common class of security bug: an internal field leaking through a serializer.`],[`O`,`A`,`What efficiency note applies to this code?`,"const results = await Promise.all(\n  ids.map(id => request.get(`/api/items/${id}`))\n);",[`Each request always opens a brand-new TCP connection regardless of protocol`,`Promise.all() forces requests to run sequentially`,`This pattern is only valid for POST requests`,`If the connection uses HTTP/2, these concurrent requests can be multiplexed over a single TCP connection rather than each needing its own`],3,`HTTP/2 multiplexing allows many logically-parallel requests to share one underlying connection efficiently.`],[`O`,`I`,`What is wrong with this test setup?`,`// worker-scoped apiContext, reused across tests
test('logs in as admin', async ({ apiContext }) => {
  await apiContext.post('/api/login', { data: adminCreds });
});
test('checks anonymous access', async ({ apiContext }) => {
  const res = await apiContext.get('/api/private');
  expect(res.status()).toBe(401); // unexpectedly passes as admin instead
});`,[`The admin login's session cookie persisted in the shared worker-scoped context, so the "anonymous" test is actually still authenticated as admin`,`apiContext cannot make GET requests after a POST`,`401 is the wrong expected status for anonymous access`,`Worker-scoped fixtures cannot be used across multiple test() blocks`],0,`A shared context carries cookie state between tests unless it's explicitly reset, breaking the assumption of a clean "anonymous" session.`],[`O`,`I`,`What does storageState do here?`,`const api = await request.newContext({ storageState: 'admin-auth.json' });
const res = await api.get('/api/admin/settings');`,[`It replays a HAR file of recorded requests`,`It pre-loads cookies/origin storage previously saved (e.g. after a real login), so this API context starts already authenticated as that saved user`,`It records the requests made by this context to a file`,`It disables authentication checks entirely`],1,`storageState lets an API context skip performing a live login by reusing a previously captured authenticated session.`],[`O`,`B`,`Why use process.env.API_TOKEN instead of a literal string here?`,"const api = await request.newContext({\n  extraHTTPHeaders: { Authorization: `Bearer ${process.env.API_TOKEN}` },\n});",[`Literal strings are not valid JavaScript in this position`,`process.env is required syntax for extraHTTPHeaders`,`It keeps the real credential out of source control and lets CI/local/staging each supply their own token value`,`It makes requests run faster`],2,`Hardcoding a real secret token in a test file would commit it to version control, which is a security risk.`],[`R`,`I`,`Your payment provider signs webhooks with HMAC-SHA256 in an X-Signature header. How do you write a realistic Playwright test for the webhook handler?`,null,[`Only test that the endpoint returns 200 for any POST body`,`Wait for a real webhook to arrive naturally in staging`,`Disable signature verification for tests`,`Compute the HMAC over a synthetic payload using the same shared secret the app uses, POST it to the webhook endpoint with that header, and assert the app processes it correctly — also test that a request with a bad/missing signature is rejected`],3,`Testing both the valid-signature success path and the invalid-signature rejection path covers the handler's actual security behavior.`],[`R`,`I`,`Your staging environment uses a self-signed certificate, causing real API tests to throw TLS errors. What's the correct fix (not a workaround)?`,null,[`Create the API context with ignoreHTTPSErrors: true for the staging project specifically, without changing anything for production`,`Disable TLS entirely in your app's production config`,`Add the self-signed cert to every developer's OS trust store manually`,`Skip all API tests against staging`],0,`Scoping the relaxed TLS check to the staging project keeps production tests strict while unblocking staging.`],[`R`,`A`,`You want to prevent frontend/backend contract drift. How would you structure automated contract tests using a shared schema?`,null,[`Manually re-read the backend source code before every release`,`Check a schema (ideally derived from the backend's actual code or OpenAPI spec) into a shared location, then validate real API responses against it in CI so a schema change without corresponding contract test updates fails the build`,`Rely entirely on manual QA sign-off`,`Only test the UI, since it indirectly exercises the API`],1,`A shared, versioned schema turns silent contract drift into a loud, automated CI failure.`],[`R`,`A`,`Tests hitting a third-party payment sandbox are intermittently slow/flaky, but you still want SOME coverage against the real thing. What's a pragmatic approach?`,null,[`Remove all sandbox-based tests entirely`,`Increase the timeout to 5 minutes for every test`,`Keep a small number of true end-to-end tests against the sandbox (tagged, possibly run nightly) wrapped in test.step with a bounded retry, while the bulk of CI uses mocked page.route() responses for speed and reliability`,`Run the sandbox tests in an infinite retry loop until they pass`],2,`This balances genuine third-party coverage against the reliability cost of depending on an external, less-controlled system in every CI run.`],[`R`,`I`,`QA needs to run the same API regression suite nightly against three environments (dev, staging, prod-readonly). How do you avoid triplicating test files?`,null,[`Copy the spec files into three folders with find-and-replace on the URL`,`Use a shell script to sed the base URL before each run`,`Hardcode all three URLs in every test and loop manually`,`Define three Playwright projects, each with a different use.baseURL (and env-specific credentials), and run the same spec files against each via --project`],3,`Projects are the built-in mechanism for running identical spec files against multiple environment configurations.`],[`R`,`A`,`You need to seed 50 related records (with foreign-key dependencies) before a UI test, and the backend only exposes GraphQL. What's an efficient approach?`,null,[`Write a single GraphQL mutation (or a small number of batched mutations) that creates the dependent records server-side in one round trip, rather than looping 50 separate REST-style calls`,`Loop 50 separate individual mutation calls sequentially`,`Seed the data by driving the UI 50 times`,`Skip seeding and rely on existing production data`],0,`A batched server-side mutation avoids 50 round trips and lets the server handle foreign-key ordering correctly.`],[`R`,`I`,`An "export to CSV" feature kicks off a background job and the file becomes available minutes later at a URL. How do you test this end-to-end via API?`,null,[`Sleep for exactly 5 minutes then GET the file`,`POST to trigger the export, poll the job-status endpoint with expect.poll() until it reports ready, then GET the resulting file URL and assert its content — avoiding a long fixed sleep`,`Only test that the POST returns 202`,`Mock the entire export process instead of testing it`],1,`Polling adapts to the actual job duration instead of guessing a fixed wait time that risks being too short or unnecessarily long.`],[`R`,`I`,`In production, a POST to create an order sometimes appears to fail client-side due to network flakiness, but the order may have actually been created server-side. How should the checkout flow (and its test) handle client-side retries safely?`,null,[`Never retry — always show an error to the user`,`Retry with a brand-new order ID each time`,`The client should send an Idempotency-Key with the request so a retry is recognized as the same operation server-side; the test should verify retrying the same key does not create a second order`,`Disable retries and rely on the user to manually resubmit`],2,`Idempotency keys let the server safely deduplicate retried requests instead of creating duplicate side effects.`],[`R`,`A`,`You log in via a fast API call in a fixture, then the test both drives UI actions AND makes page.request calls that must reflect the logged-in user. What must the fixture do to make both work?`,null,[`Nothing extra — page.request automatically shares the request fixture's session`,`Call page.reload() twice`,`Use a separate browser instance for the UI actions`,`Obtain the auth token/cookie from the API login and inject it into the browser context (e.g. context.addCookies), since page.request shares the page's context — the standalone request fixture used for login does not`],3,`page.request is session-aware through the page's context, but the standalone request fixture used for the fast login is an entirely separate session.`],[`R`,`I`,`QA reports that page 2 of search results sometimes duplicates items from page 1 in production. How would an API test catch this class of bug?`,null,[`Request several consecutive pages via the cursor, collect all returned item IDs across pages, and assert there are no duplicate IDs across the full paginated result set`,`Only assert that each page returns the expected count`,`Compare page 1 and page 2 response times`,`Test only the first page since that's what most users see`],0,`Duplicate detection requires actually aggregating IDs across multiple pages, not just validating each page in isolation.`],[`S`,`A`,`A test that seeds an authenticated session works locally but fails in CI with 401s. Locally you use a long-lived personal token; CI uses a service-account token that expires hourly. What's the robust fix?`,null,[`Increase the token's expiry time in the service account config only`,`Have the test/fixture perform a real login (or token exchange) at the start of the run instead of relying on a static long-lived token, so a fresh, valid token is always used regardless of environment`,`Hardcode the CI token directly into the test file`,`Disable authentication checks specifically in CI`],1,`A dynamic login at test-time is robust to any token lifetime, while a static stored token will eventually expire and break unpredictably.`],[`S`,`I`,`A zod schema validation test starts failing after a backend deploy, but manual QA sees no behavior change. The failing field is middleName, which the backend now omits for users without one. What's the likely fix?`,null,[`Report it as a backend bug and revert the deploy`,`Delete the schema validation test entirely`,`Change the field from required to optional/nullable in the zod schema to reflect that middleName is genuinely allowed to be absent`,`Force the backend to always return an empty string for middleName`],2,`The schema was too strict relative to a legitimately optional field — updating the schema, not the backend, is the correct fix here.`],[`S`,`A`,`A backend team renames a field from full_name to fullName without telling the frontend team. What kind of automated test is most likely to catch this before it reaches production, and why?`,null,[`A pure UI test asserting on rendered text, since it always fails on any backend change`,`A visual regression screenshot test`,`Load testing the renamed endpoint`,`A contract/schema test that validates the actual API response shape against the shape the frontend expects — a UI test might still incidentally pass if a fallback masks the missing field`],3,`Contract tests directly validate response shape, while a UI test could mask the break behind default/fallback rendering.`],[`S`,`I`,`After creating a resource via POST, an immediate GET-by-search sometimes doesn't find it, but a GET-by-ID does. What does this suggest, and how should the "searchable after creation" test be written?`,null,[`The search index update is asynchronous/eventually consistent relative to the write path; the test should use expect.poll() against the search endpoint rather than asserting immediately after creation`,`The POST endpoint is broken and should be fixed`,`GET-by-ID is unreliable and should not be trusted`,`The test should always sleep for exactly 10 seconds before searching`],0,`A poll-based assertion tolerates the natural lag of an eventually-consistent search index without hardcoding a guessed delay.`],[`S`,`A`,`API tests against a shared staging environment start intermittently returning 429 once more teams began using it. What's a reasonable two-part fix?`,null,[`Just increase the test timeout to 5 minutes`,`Implement a bounded retry-with-backoff specifically for 429 responses in the test's HTTP helper, AND push for a dedicated/isolated test environment or per-team rate-limit allowance as the real long-term fix`,`Ignore all 429 responses silently in every test`,`Switch every test to run sequentially forever`],1,`A short-term retry mitigates flakiness while the actual root cause — environment contention — gets addressed separately.`],[`S`,`I`,`A login test asserts on response.headers()['set-cookie'] and expects to find both a session and a csrf cookie in it, but only ever sees one. What's the likely mistake and the fix?`,null,[`The server only ever sets one cookie, which is a backend bug`,`Cookies cannot be read from API responses at all`,`headers() merges multiple same-named headers into one string, losing one of the two Set-Cookie entries — use response.headersArray() to inspect them individually`,`The test needs to make two separate login calls`],2,`headersArray() preserves every header instance, whereas headers() can lossily collapse duplicates.`],[`S`,`A`,`Two different checkout attempts by the same user accidentally reuse the same Idempotency-Key value (a client bug), causing the second, legitimately different order to be silently ignored and return the first order's data. What does this reveal about how idempotency keys should be generated?`,null,[`Idempotency keys should be removed from the API entirely`,`The server should ignore idempotency keys after the first use`,`Idempotency keys should be based on the user ID alone`,`Idempotency keys must be generated fresh and unique per logical operation (e.g. a new UUID per checkout attempt), not reused across genuinely different requests`],3,`Reusing a key for a different operation defeats the purpose — the server has no way to distinguish "same retry" from "different, intentional request".`],[`S`,`I`,`A test manually sets headers: { 'Accept-Encoding': 'identity' } to disable compression for debugging, but afterward some unrelated tests running in the same worker start behaving oddly with slow response times. What's a likely explanation?`,null,[`If that header was set via extraHTTPHeaders on a shared/worker-scoped context rather than per-request, it now applies to every subsequent request in that context, forcing uncompressed responses for all of them`,`This header has no possible side effect on other tests`,`Accept-Encoding always applies globally across all workers`,`The test framework caches Accept-Encoding permanently once set`],0,`Context-level header configuration is shared state — setting it for one debugging purpose can leak into unrelated later requests in the same context.`],[`S`,`A`,`During code review, someone points out the webhook test always uses a fresh, unique payload/signature per test run, but never tests what happens if the SAME previously-valid signed payload is replayed twice. Why might this matter for the actual app, not just the test?`,null,[`It doesn't matter — signatures are only checked once ever`,`Without replay protection (e.g. a nonce/timestamp check plus deduplication), a captured valid webhook payload could be resent by an attacker to trigger the same effect twice — the test gap mirrors a real potential security gap in the handler itself`,`Replayed webhooks are always automatically rejected by HTTP itself`,`This only matters for GET-based webhooks`],1,`A missing replay-attack test can indicate a corresponding missing defense in the actual handler.`],[`S`,`I`,`Your app calls /v1/users but the backend team is quietly planning to deprecate v1 in favor of v2, which has a different response shape. How should the test suite be structured to catch v1 breaking before the deprecation happens unannounced?`,null,[`Delete the v1 tests since v1 is going away anyway`,`Only test v2 endpoints from now on`,`Keep explicit contract/schema tests for /v1/users that will fail loudly the moment the backend team stops maintaining or begins altering v1's shape, rather than only testing via v2 or via UI flows that might silently adapt`,`Rely on production error monitoring instead of tests`],2,`Explicit, targeted contract tests give an early, unambiguous signal the moment the still-relied-upon v1 contract changes.`],[`D`,`I`,`response.text() returns garbled/binary-looking text for an endpoint that should return plain UTF-8 text. What's a likely cause?`,null,[`The endpoint is simply broken and always was`,`text() cannot handle UTF-8 by default`,`The request used the wrong HTTP method`,`A manually set Accept-Encoding or Content-Encoding header (or a proxy) is causing the body to arrive still compressed and not be auto-decompressed as expected — check for anything overriding normal encoding negotiation`],3,`Automatic decompression relies on normal encoding negotiation — an overridden header can defeat it and leave the raw compressed bytes.`],[`D`,`I`,`A conditional-GET test (expecting 304) fails intermittently — sometimes 304, sometimes 200 with a slightly different ETag. What's the likely cause?`,null,[`The resource is being modified by another concurrent test (or a background process) between the first and second GET, so the ETag legitimately changes — the test needs isolated/dedicated test data`,`The If-None-Match header is malformed`,`ETags are inherently random and non-deterministic`,`304 responses are inherently flaky by design`],0,`Shared, mutable test data is a common source of this kind of intermittent conditional-request failure.`],[`D`,`A`,`A worker-scoped API fixture causes test B to unexpectedly run as the user that test A logged in as, even though B never calls login. What's the fix?`,null,[`Switch to test-scoped fixtures for everything, always`,`Make the authenticated session part of a test-scoped fixture (or explicitly reset/clear cookies on the shared context between tests) instead of relying on a single worker-scoped context for both unauthenticated and authenticated scenarios`,`Disable worker parallelism entirely`,`Rename test B so it runs in a different file`],1,`A worker-scoped context that also handles login needs explicit cookie resets, or the login itself needs to move to a narrower fixture scope.`],[`D`,`I`,`API tests pass locally but fail in CI with ECONNREFUSED to an internal API host, even though the URL and code are unchanged. What's a likely CI-specific cause?`,null,[`The test code has a syntax error only visible in CI`,`CI always disables networking by default`,`CI runners require an outbound proxy or are on a different network segment that can't resolve/reach the internal host directly — the request context needs a proxy option (or CI needs network access) that local machines don't`,`The internal API only accepts requests from Windows machines`],2,`Local developer machines and CI runners can sit on very different networks with different access to internal hosts.`],[`D`,`A`,`A zod-based contract test fails with "Expected string, received null" for a field the backend has always returned as null for guest users, and this is expected/valid app behavior. What's the schema fix?`,null,[`Remove the field from the schema entirely`,`Change the field type to z.any()`,`Have the backend stop returning null and use an empty string instead`,`Change the field's schema type to z.string().nullable() (or .nullish()) so null is accepted as a valid value alongside strings, instead of leaving it defined as a bare required string`],3,`The schema should accurately model a genuinely valid state (null for guests) rather than being stricter than the real contract.`]],network:[[`O`,`B`,"What does `page.route(pattern, handler)` do?",null,[`Records all network requests matching the pattern for later inspection`,`Intercepts browser requests matching the pattern and passes them to the handler`,`Blocks all requests except those matching the pattern`,`Adds a Content Security Policy header to matching responses`],1,`page.route() is the entry point for network interception — the handler decides what to do with each matched request.`],[`O`,`B`,"What does `route.fulfill({ status: 200, json: data })` do?",null,[`Forwards the request to the server with the given body`,`Returns a mocked response to the browser without hitting the real server`,`Modifies the real response before delivering it`,`Logs the response and passes it through unchanged`],1,`fulfill() short-circuits the real network call and returns whatever you specify directly to the browser.`],[`O`,`I`,"What does `route.abort()` do?",null,[`Cancels the route handler and lets the request continue normally`,`Causes the browser to see a network error for that request`,`Returns a 503 status to the browser`,`Removes the route handler permanently`],1,`abort() simulates a network failure — useful for testing offline states and error UI.`],[`O`,`B`,"What does `route.continue()` do?",null,[`Mocks the response with an empty body`,`Forwards the request to the real server, optionally modifying headers or body`,`Records the request and retries it after 1 second`,`Signals that the route handler is finished`],1,`continue() passes the request through — optionally letting you modify headers, method, or body before sending.`],[`O`,`I`,"What does `await route.fetch()` return?",null,[`The real network response from the server — you can then modify it before calling fulfill()`,`A Promise that resolves once the browser receives the response`,`The request headers as an object`,`The URL that was actually fetched after any redirects`],1,`route.fetch() sends the request to the real server and gives you the response to inspect or modify.`],[`O`,`B`,"Which of these URL patterns is valid for `page.route()`?",null,[`Only exact string URLs`,`Glob strings like **/api/**, exact strings, or RegExp`,`Only RegExp patterns`,`Only URLs starting with https://`],1,`page.route() accepts glob strings, exact strings, and RegExp — giving flexible matching options.`],[`O`,`I`,"What is the difference between `page.route()` and `context.route()`?",null,[`page.route() intercepts GET requests; context.route() intercepts POST requests`,`context.route() applies to all pages in the browser context including new ones; page.route() only covers that specific page`,`They are identical — context.route() is an alias`,`context.route() runs before page.route() in the handler chain`],1,`context.route() covers every page opened in that context — useful for popups and new tabs.`],[`O`,`I`,"What does `page.on('request', fn)` do differently from `page.route()`?",null,[`It intercepts and can modify requests`,`It is read-only — it observes requests but cannot fulfill, abort, or continue them`,`It only fires for XHR requests, not fetch()`,`It requires await unlike page.route()`],1,`Event listeners are passive monitors — use them to collect data without changing request behaviour.`],[`O`,`I`,"What does `page.unroute(pattern)` do?",null,[`Aborts all pending requests matching the pattern`,`Removes the route handler previously registered for that pattern`,`Clears the browser cache for matching URLs`,`Disables network interception entirely`],1,`unroute() removes a specific handler — useful in fixtures when teardown should restore normal network behaviour.`],[`O`,`A`,"What does `page.routeFromHAR('fixtures/api.har')` do?",null,[`Records all network traffic to a HAR file`,`Replays a previously recorded HTTP Archive to mock an entire API surface`,`Blocks all requests not in the HAR file`,`Converts HAR entries to route.fulfill() calls in the test file`],1,`routeFromHAR() lets you mock a complex API by replaying a real recorded session.`],[`O`,`I`,"When should you use `route.fetch()` instead of `route.fulfill()` directly?",null,[`When the URL is too long for fulfill() to handle`,`When you want the real server response but need to add or change a field before the browser receives it`,`When the request uses POST — fulfill() only works for GET`,`When the network is too slow and fetch() uses a cache`],1,`fetch() + fulfill() is the spy-and-modify pattern — real data with surgical changes.`],[`O`,`B`,"What does `route.request().url()` return?",null,[`The page URL at the time of the request`,`The full URL of the intercepted network request`,`The base URL from playwright.config.ts`,`The redirect chain as an array of URLs`],1,`route.request() gives you the Request object; .url() returns its full URL string.`],[`F`,`B`,"Complete: `route.______({ status: 404, body: 'Not Found' })` to return a mock response",null,[`fulfill`,`mock`,`respond`,`reply`],0,`fulfill() returns the specified response to the browser without hitting the real server.`],[`F`,`B`,"Complete: `route.______()` to block the request so the browser sees a network error",null,[`abort`,`block`,`deny`,`cancel`],0,`abort() causes a network error — use it to test offline states or blocked resources.`],[`F`,`B`,"Complete: `route.______()` to forward the request to the real server unchanged",null,[`continue`,`pass`,`forward`,`allow`],0,`continue() passes the request through — optionally with modified headers or body.`],[`F`,`I`,"Complete: `'______/api/users'` — the glob prefix that matches any path prefix",null,[`**`,`*`,`?`,`%`],0,`** matches any number of path segments — e.g. **/api/users matches /v1/api/users.`],[`F`,`I`,"Complete: `await page.______( '**/api/**', handler )` to register a route",null,[`route`,`intercept`,`mock`,`trap`],0,`page.route() is the API for registering network interception handlers.`],[`F`,`I`,"Complete: `await page.______( '**/api/users' )` to remove a specific handler",null,[`unroute`,`removeRoute`,`clearRoute`,`cancelRoute`],0,`unroute() removes the route handler registered for that pattern.`],[`F`,`A`,"Complete: `const res = await route.______()` to get the real server response for modification",null,[`fetch`,`forward`,`request`,`get`],0,`route.fetch() forwards to the real server and returns the APIResponse for inspection.`],[`E`,`B`,"What is wrong?\n```ts\nawait page.goto('/');\nawait page.route('**/api/users', route => route.fulfill({ json: [] }));\n```",null,[`fulfill() cannot receive a json option`,`page.route() must use RegExp not a glob string`,`page.route() was registered after page.goto() — the first page load's API request was not intercepted`,`route.fulfill() requires await`],2,`Always register routes before navigation — requests fired during goto() will miss handlers set up afterward.`],[`E`,`I`,"What is wrong?\n```ts\nawait page.route('**/api/data', route => {\n  // handler body — no fulfill/abort/continue call\n});\n```",null,[`page.route() requires an async handler`,`The handler must call route.fulfill(), route.abort(), or route.continue() — omitting it causes the request to hang until timeout`,`page.route() does not support arrow functions`,`The pattern must start with https://`],1,`Every route handler must resolve the request with exactly one of the three methods — silence causes a permanent hang.`],[`E`,`B`,`What is wrong?
\`\`\`ts
await page.route('**/api/**', route => route.fulfill({
  body: JSON.stringify({ items: [] }),
}));
// UI never parses the response
\`\`\``,null,[`body must be a Buffer not a string`,`Content-Type defaults to text/plain — the browser parses it as text, not JSON. Use json: { items: [] } instead`,`fulfill() does not support the body key`,`JSON.stringify is not available in route handlers`],1,`Without contentType: 'application/json' or using the json: shorthand, the browser treats the body as plain text.`],[`E`,`I`,"What is wrong?\n```ts\npage.on('request', req => {\n  req.fulfill({ json: { mocked: true } }); // modify in listener\n});\n```",null,[`page.on('request') is not a valid event`,`Event listeners are read-only — req has no fulfill() method; use page.route() to intercept and modify`,`json must be a string not an object here`,`The listener must be async to call fulfill()`],1,`Event listeners observe only — page.route() is needed to fulfill, abort, or continue requests.`],[`E`,`I`,`What is wrong?
\`\`\`ts
await page.route('**/api/products', async route => {
  const res  = route.fetch();  // no await
  const json = await res.json();
  await route.fulfill({ json });
});
\`\`\``,null,[`route.fetch() does not return a Promise`,`await is missing on route.fetch() — res is a Promise, so res.json() will throw`,`json() must be called synchronously`,`route.fulfill() cannot follow route.fetch()`],1,`route.fetch() is async — without await, res is a pending Promise and res.json() throws immediately.`],[`E`,`A`,"What is wrong?\n```ts\nawait page.route('**/api/**', route => route.abort());\nawait page.route('**/api/health', route => route.continue()); // health should pass through\n```",null,[`Two routes cannot match the same URL`,`The second route is registered last and fires first (LIFO order) — health passes through correctly. Actually this is CORRECT behaviour`,`continue() and abort() cannot both be registered for overlapping patterns`,`Route handlers cannot share the same page`],1,`LIFO means the last-registered handler fires first — health matches **/api/health** first via the second handler (continue), so this setup is actually correct.`],[`D`,`B`,`A test hangs with "Request timed out". No assertion ever fires. What is the most likely cause?`,null,[`The page.goto() URL is wrong`,`The route handler neither fulfills, aborts, nor continues the request — the browser waits indefinitely`,`The assertion timeout is too short`,`The test has too many route handlers`],1,`A handler that does nothing leaves the request permanently pending — always call one of the three resolution methods.`],[`D`,`I`,`The mock route is registered but the UI still shows real API data. What should you check first?`,null,[`The mock json is formatted incorrectly`,`The route pattern may not match the actual request URL — log req.url() inside the handler to verify`,`The UI is caching the previous response`,`route.fulfill() is async and needs await`],1,`URL pattern mismatches are the most common cause — glob syntax is easy to get wrong; log the actual URL first.`],[`D`,`B`,"`route.abort()` causes an unhandled page error that fails the test. How do you suppress it?",null,[`Use route.fulfill({ status: 499 }) instead of abort()`,`Listen for the pageerror event with page.on('pageerror', () => {}) or add page.on('requestfailed', () => {})`,`Add try/catch around route.abort()`,`abort() should not be used in tests — it always fails`],1,`Aborted requests trigger requestfailed events and can throw uncaught page errors — add a listener to ignore expected failures.`],[`D`,`I`,"You registered a route to add a header with `route.continue({ headers: { 'x-test': '1' } })` but the original headers are gone. Why?",null,[`continue() always sends an empty headers object`,`Passing headers to continue() replaces all headers — merge with route.request().headers() to preserve the original ones`,`x-test is a reserved header name`,`The server strips custom headers`],1,`continue({ headers: newHeaders }) replaces the entire headers object — spread route.request().headers() first to preserve originals.`],[`D`,`I`,"`page.routeFromHAR()` is set up but some API calls return 404 instead of mocked data. What is the cause?",null,[`routeFromHAR() only works in headed mode`,`The HAR file does not contain an entry for those specific URLs — they fall through to the real network`,`HAR files cannot be used for POST requests`,`The HAR file must be re-recorded every test run`],1,`routeFromHAR only intercepts URLs present in the recorded file — unrecorded requests reach the real network.`],[`D`,`A`,"Two route handlers are registered for `**/api/items**`. The second one was meant to override the first but the first one still fires. What went wrong?",null,[`Multiple handlers for the same pattern are not allowed`,`Only the first registered handler fires — LIFO order means the second one registered fires first, not the first. Re-check the registration order`,`Both handlers run and both call fulfill() — the second is ignored`,`The handlers need different handler references to stack`],1,`LIFO = last in, first out. If the second registration is truly last, it fires first. Double-check the actual registration order in your code.`],[`C`,`B`,"Complete to mock an empty list response.\n```ts\nawait page.route('**/api/items', route => route.______({ status: 200, json: [] }));\n```",null,[`fulfill`,`mock`,`reply`,`respond`],0,`fulfill() returns the mocked response to the browser.`],[`C`,`I`,`Complete to simulate a 503 server error.
\`\`\`ts
await page.route('**/api/orders', route => route.fulfill({
  status: ______,
  body: 'Service Unavailable',
}));
\`\`\``,null,[`503`,`'503'`,`500`,`404`],0,`status is a number — 503 Service Unavailable is the standard "server down" code.`],[`C`,`I`,"Complete to block all image requests.\n```ts\nawait page.route('**/*.{png,jpg,gif,svg}', route => route.______());\n```",null,[`abort`,`block`,`deny`,`cancel`],0,`abort() simulates a network error — the browser skips loading the matched resources.`],[`C`,`I`,`Complete to pass headers through while adding one.
\`\`\`ts
await page.route('**/api/**', route =>
  route.continue({
    headers: { ...route.request().______(), 'x-test': '1' },
  })
);
\`\`\``,null,[`headers`,`getHeaders`,`allHeaders`,`requestHeaders`],0,`route.request().headers() returns the original request headers as a plain object.`],[`C`,`A`,`Complete the fetch-and-modify pattern.
\`\`\`ts
await page.route('**/api/users', async route => {
  const res  = await route.______();
  const json = await res.json();
  json.push({ id: 99, name: 'Bot' });
  await route.fulfill({ json });
});
\`\`\``,null,[`fetch`,`forward`,`get`,`request`],0,`route.fetch() sends the real request and returns the server response for modification.`],[`L`,`B`,"Why must `page.route()` be called before `page.goto()`?",null,[`page.goto() clears all route handlers`,`Requests fired during page load happen before route handlers registered after goto() are in place`,`page.route() is synchronous and must precede async calls`,`goto() and route() cannot share the same await chain`],1,`The very first network requests happen during navigation — handlers must be in place before goto() fires them.`],[`L`,`I`,`When multiple route handlers match the same URL, which one fires first?`,null,[`First registered (FIFO)`,`Last registered (LIFO)`,`The most specific pattern wins`,`They all fire in alphabetical pattern order`],1,`LIFO order — the last handler registered runs first. Use this to layer overrides on top of base handlers.`],[`L`,`I`,"What is the advantage of using `route.fetch()` over a separate `request.get()` call in the handler?",null,[`route.fetch() is 10x faster because it reuses a connection`,`route.fetch() forwards the original request including method, headers, and body — a separate request.get() would miss POST bodies and auth headers`,`request.get() is not available inside route handlers`,`route.fetch() bypasses CORS restrictions`],1,`route.fetch() preserves the original request intact — crucial when the real request has POST bodies or auth headers you want forwarded.`],[`L`,`B`,"What is the difference between `page.route()` and `page.on('request')`?",null,[`page.route() only works for XHR; page.on works for all requests`,`page.route() intercepts and can fulfill/abort/continue; page.on('request') is read-only observation`,`page.on('request') fires before page.route() in the pipeline`,`They are identical — page.route is just syntactic sugar`],1,`route handlers can modify traffic; event listeners can only observe it.`],[`L`,`A`,"`context.route()` vs `page.route()` — when does context-level routing matter?",null,[`context.route() is faster because it runs outside the page JS engine`,`context.route() intercepts requests from all pages in the context, including popups and newly opened tabs`,`page.route() cannot use RegExp patterns — context.route() can`,`They are identical — context.route() is deprecated`],1,`context.route() is essential when actions like Ctrl+Click open a new page — page.route() would not cover the new tab.`],[`R`,`B`,`You want to test the "no results" empty state of a search page. How do you set it up?`,null,[`Clear the database before the test`,`Use page.route() to mock the search API to return an empty array`,`Add a query parameter that triggers an empty response on the server`,`Use page.evaluate() to clear the search results DOM`],1,`Mocking the API response is faster and more reliable than manipulating the real data layer.`],[`R`,`I`,`You need to test how the app handles a 503 from the checkout API. What do you set up?`,null,[`Take the real server offline`,`await page.route('**/api/checkout', route => route.fulfill({ status: 503, body: 'Down' }))`,`Use network throttling to simulate slowness`,`Set retries: 0 in the config`],1,`route.fulfill({ status: 503 }) simulates the error without touching the real server.`],[`R`,`I`,`You want to block all Google Analytics and Hotjar requests to keep tests fast. What is the correct approach?`,null,[`Add a Content Security Policy header in the config`,`await page.route(/google-analytics|hotjar/, route => route.abort())`,`Set headless: true — analytics only load in headed mode`,`Use extraHTTPHeaders to block those domains`],1,`route.abort() on a regex pattern covering all analytics domains blocks them efficiently.`],[`R`,`I`,'During a test you want to verify that the app called `/api/orders` after clicking "Place Order". How?',null,[`Use page.route() to intercept and log it`,`Collect URLs with page.on('request', req => apiCalls.push(req.url())) and assert after the action`,`Check the Network tab in DevTools`,`Add a console.log() to the app source`],1,`page.on('request') collects request URLs passively — then assert the expected URL appears in the list.`],[`R`,`A`,`You want to test how the app handles slow API responses (2 s delay). How do you implement this?`,null,["Use page.route() to fulfill after a delay: `async route => { await new Promise(r => setTimeout(r, 2000)); await route.fulfill({ json: data }); }`",`Use network throttling in the config`,`Set timeout: 2000 in the route handler`,`Use route.continue() with a delay option`],0,`A setTimeout inside an async route handler adds the delay before fulfilling — clean and reliable.`],[`S`,`I`,"A mock route is set up but the real API is still being called. Logging shows the intercepted URL differs from the pattern. The real URL is `/v2/api/users`. The pattern is `**/api/users`. Does it match?",null,[`Yes — ** matches any prefix including /v2`,`No — ** only matches one level; use **/* to match v2`,`Yes — glob patterns ignore URL path depth`,`No — ** requires the pattern to start with a slash`],0,`** matches zero or more path segments — **/api/users matches /v2/api/users, /api/users, and /any/depth/api/users.`],[`S`,`A`,"You need the first call to `/api/feed` to return 10 items and the second to return 0 items (pagination). How do you implement this?",null,[`Register two separate routes — they fire in order`,`Use a call counter in a closure: increment on each intercept and return different data based on the count`,`Use routeFromHAR() which handles pagination automatically`,`Register one route per page number using params`],1,`A closure counter lets the same handler return different responses on successive calls.`],[`S`,`I`,"`route.continue()` is called but the request headers sent to the server are missing the original Authorization header. What went wrong?",null,[`continue() always strips Authorization for security`,`The headers option in continue() replaced all headers — the original Authorization was not merged in`,`Authorization headers cannot be forwarded — use fetch() instead`,`The server strips Authorization on HTTPS requests`],1,"Spread route.request().headers() into the continue() headers to preserve all originals: `{ ...route.request().headers(), 'x-test': '1' }`."],[`S`,`A`,"A test opens a popup window via window.open() and the popup makes an API call. The route handler on `page` does not intercept the popup's request. How do you fix this?",null,[`Add page.route() inside the popup's load event`,`Use context.route() instead of page.route() — it covers all pages including popups`,`Pass the route handler to waitForEvent('popup')`,`Set fullyParallel: false so popups share the parent page's routes`],1,`context.route() intercepts requests from all pages in the browser context — the only way to cover popups opened by the main page.`],[`O`,`B`,'What does `page.route("**/*.png", route => route.abort())` do?',null,[`Delays all PNG requests by 1 second`,`Cancels all requests matching *.png — the browser receives a network error for those requests`,`Redirects PNG requests to a placeholder image`,`Blocks only the first PNG request`],1,`route.abort() terminates the request — the browser sees it as a network failure, not a 404.`],[`O`,`I`,"What is the return type of `route.request().url()`?",null,[`URL object`,`string — the full URL of the intercepted request`,`Request object`,`Promise<string>`],1,`url() is synchronous and returns the request URL as a plain string — no await needed.`],[`O`,`B`,"What does `route.fulfill({ status: 503 })` do?",null,[`Pauses the route for 503 milliseconds`,`Responds to the intercepted request with HTTP 503 Service Unavailable — no real network call is made`,`Retries the request 503 times`,`Passes the request through with a 503 header added`],1,`fulfill() returns a synthetic response from Playwright without hitting the network — use it to simulate server errors.`],[`O`,`I`,"What does `page.route(url, handler, { times: 1 })` do?",null,[`Fires the handler at most once — subsequent matching requests are passed through normally`,`Fires the handler for 1 second then removes itself`,`Fires the handler for the first 1 ms only`,`Limits the handler to 1 active interception at a time`],0,`times: 1 auto-removes the route after the first match — useful for mocking a one-time bootstrap response.`],[`O`,`A`,'What does `page.unroute("**/api/**", handler)` do?',null,[`Removes all routes for the pattern`,`Removes only the specific handler function for that pattern — other handlers on the same pattern remain active`,`Pauses the route handler temporarily`,`Resets the route to pass-through mode`],1,`unroute() is handler-specific — pass the same function reference to remove exactly that handler.`],[`O`,`I`,"What does `request.postDataJSON()` return inside a route handler?",null,[`The raw request body as a Buffer`,`The request body parsed as JSON — or null if the body is empty or not JSON`,`A Promise<object> — must be awaited`,`The multipart form fields as an object`],1,`postDataJSON() is synchronous and parses the request body as JSON — convenient for inspecting what the app sent.`],[`O`,`B`,"What does `route.continue()` do with no options?",null,[`Forwards the request to the real server unchanged`,`Fulfills the request with an empty 200 response`,`Aborts the request`,`Marks the request as handled without forwarding it`],0,`continue() with no arguments forwards the request exactly as-is — use it to observe without modifying.`],[`O`,`A`,'What does `page.routeFromHAR("fixtures/api.har", { update: true })` do?',null,[`Plays back responses from the HAR file`,`Records new HAR entries from live network traffic and UPDATES the file — useful for refreshing stale HAR fixtures`,`Validates the HAR file format`,`Uploads the HAR to Playwright's servers`],1,`update: true switches routeFromHAR to recording mode — requests are forwarded live and the HAR is rewritten with fresh data.`],[`O`,`I`,"What happens when multiple `page.route()` handlers match the same URL?",null,[`All handlers run in parallel`,`Handlers run in LIFO order — the last-registered handler runs first. If it calls route.continue(), the next handler in the stack gets a chance to run.`,`The first-registered handler always wins`,`A "duplicate route" error is thrown`],1,`LIFO stacking lets you override a context-level route with a page-level one, or chain handlers.`],[`O`,`B`,'What does `response.request().method()` return inside a `page.on("response", ...)` listener?',null,[`The HTTP verb (e.g. "GET", "POST") of the original request that triggered this response`,`The response method — always "GET"`,`The redirect method used`,`The method of the next request in the chain`],0,`response.request() gives back the original Request object — .method() on it returns the HTTP verb.`],[`O`,`I`,'What does `page.waitForRequest(url => url.includes("/api/data"), { timeout: 5000 })` return?',null,[`A boolean — true if the request is made within 5000 ms`,`A Promise<Request> — resolves with the first matching Request object, or rejects on timeout`,`The response for that request`,`The number of matching requests`],1,`waitForRequest() resolves with the Request object — you can then inspect its headers, body, and method.`],[`O`,`A`,'What does `context.route("**", route => route.fulfill({ body: "", status: 200 }))` do when set on the context?',null,[`It intercepts all requests from all pages in the context and responds with an empty 200`,`It only intercepts requests from the first page`,`It intercepts only XHR, not fetch requests`,`It requires fullyParallel: false to work correctly`],0,`context.route() covers every page — combined with ** it intercepts all network traffic, useful for fully offline tests.`],[`F`,`B`,'Complete to abort requests matching the pattern.\n```ts\nawait page.route("**/ads/**", route => route.______());\n```',null,[`abort`,`cancel`,`block`,`drop`],0,`route.abort() signals a network error to the browser for that request.`],[`F`,`I`,`Complete to modify a request header before forwarding.
\`\`\`ts
await page.route("**/api/**", async route => {
  await route.continue({
    headers: { ...route.request().headers(), ______: "Bearer mytoken" },
  });
});
\`\`\``,null,[`"Authorization"`,`"Auth"`,`"X-Token"`,`"Bearer"`],0,`"Authorization" is the standard HTTP header for auth tokens — injected here without the test having to log in via the UI.`],[`F`,`B`,`Complete to respond with a JSON body.
\`\`\`ts
await page.route("**/api/user", route => route.fulfill({
  status: 200,
  contentType: "application/json",
  ______: JSON.stringify({ name: "Alice" }),
}));
\`\`\``,null,[`body`,`data`,`response`,`content`],0,`body: accepts a string, Buffer, or ReadableStream — JSON.stringify converts the object to a string.`],[`F`,`I`,'Complete to use the HAR file for responses.\n```ts\nawait page.______("fixtures/api.har", { url: "**/api/**" });\n```',null,[`routeFromHAR`,`loadHAR`,`replayHAR`,`useHAR`],0,`routeFromHAR() replays matching responses from the HAR file without hitting the network.`],[`F`,`B`,'Complete to wait for a response matching a URL.\n```ts\nconst response = await page.______("**/api/login");\n```',null,[`waitForResponse`,`awaitResponse`,`onResponse`,`captureResponse`],0,`waitForResponse() resolves with the Response object once a matching response arrives.`],[`F`,`I`,`Complete to read the intercepted request body as a string.
\`\`\`ts
await page.route("**/api/data", route => {
  const body = route.request().______();
  console.log(body);
  route.continue();
});
\`\`\``,null,[`postData()`,`body()`,`text()`,`data()`],0,`postData() returns the request body as a string — postDataJSON() parses it as JSON.`],[`F`,`A`,'Complete to intercept only once.\n```ts\nawait page.route("**/api/bootstrap", handler, { ______: 1 });\n```',null,[`times`,`count`,`once`,`limit`],0,`times: N auto-removes the handler after N matches.`],[`F`,`I`,`Complete to pass through a response but inspect it first.
\`\`\`ts
await page.route("**/api/**", async route => {
  const response = await route.fetch();
  const json = await response.json();
  console.log(json);
  await route.______( response);
});
\`\`\``,null,[`fulfill`,`continue`,`pass`,`forward`],0,`route.fulfill(response) passes the already-fetched response to the browser — used after route.fetch() to inspect-and-forward.`],[`E`,`B`,`What is wrong?
\`\`\`ts
await page.route("**/api/users", route => {
  route.fulfill({ status: 200, body: { users: [] } });
  // no await on fulfill
});
\`\`\``,null,[`fulfill() does not accept objects as body — it must be a string or Buffer`,`fulfill() is async — missing await means the route promise resolves before the response is sent, causing "Request was already handled"`,`Route handlers cannot be async`,`body must always be JSON.stringify'd`],1,`fulfill() and continue() are both async — always await them inside async route handlers.`],[`E`,`I`,'What is wrong?\n```ts\npage.route("**/api/**", route => route.continue());\n// later — never removed\n// second test accidentally intercepts real requests\n```',null,[`Page routes are automatically removed between tests`,`page.route() routes persist for the lifetime of the page. If the page is reused across tests (unusual), routes accumulate. Always call page.unroute() or use a test-scoped page fixture that creates a fresh page.`,`Routes only match once by default`,`fullyParallel: true clears routes between tests`],1,`Routes live as long as the page — ensure fresh pages per test or explicitly unroute to avoid cross-test contamination.`],[`E`,`B`,`What is wrong?
\`\`\`ts
await page.route("**/*.json", async route => {
  await route.fulfill({ body: '{"mocked": true}' });
  await route.continue(); // both fulfill AND continue
});
\`\`\``,null,[`This is valid — fulfill and continue can coexist`,`Once a route is handled (via fulfill, continue, or abort), it cannot be handled again. The second call throws "Request is already handled."`,`continue() overrides fulfill() silently`,`fulfill must come after continue`],1,`Handle a route exactly once — pick fulfill OR continue OR abort, never two in the same handler.`],[`E`,`I`,`What is wrong?
\`\`\`ts
await page.route("**/api/user", route => {
  const data = route.request().postDataJSON();
  route.fulfill({ body: JSON.stringify({ echo: data }) });
});
\`\`\``,null,[`postDataJSON() cannot be called in a route handler`,`route.fulfill() is async — the handler must be async and await fulfill(). Without await, the route may complete before the response is sent.`,`postDataJSON() is not available for POST requests`,`JSON.stringify is not allowed in fulfill`],1,`fulfill() and continue() must be awaited — otherwise the event loop moves on before Playwright sends the response.`],[`E`,`A`,`What is wrong?
\`\`\`ts
await page.route("**/api/**", async route => {
  const resp = route.fetch(); // no await
  await route.fulfill(resp);
});
\`\`\``,null,[`route.fetch() is not a valid method`,`route.fetch() is async — without await, resp is a Promise, not an APIResponse. fulfill() receives a Promise object rather than the response, leading to incorrect behavior.`,`fulfill() cannot accept a route.fetch() result`,`route.fetch() automatically fulfills the route`],1,`Always await route.fetch() — the returned Promise must resolve to an APIResponse before passing to fulfill().`],[`D`,`B`,"`page.route()` is not intercepting requests. What is the most common first thing to check?",null,[`The test has to opt in to network interception`,`Verify the URL pattern matches the actual request URL. Log the request URL in the handler: route => { console.log(route.request().url()); route.continue(); } to see what is actually being called.`,`Network interception only works in Chromium`,`Reload the page before adding the route`],1,`Pattern mismatch is the #1 cause — log the actual URL to confirm the glob pattern matches.`],[`D`,`I`,"`route.fulfill()` is called but the UI still shows an error. Network tab shows a 200 response with correct data. What else could cause this?",null,[`fulfill() requires the correct Content-Type header`,`The app may be checking a field in the response body that differs from the mock. Inspect the JSON in fulfill() against the real API response shape — the app code may expect a field the mock is missing.`,`The browser cached the old response`,`fulfill() only works on the first request per route`],1,`JSON shape mismatch is subtle — the network shows 200 but the app logic fails on a missing field. Always mirror the real API response structure.`],[`D`,`B`,'`waitForResponse("**/api/data")` times out even though the request is made. What should you check?',null,[`waitForResponse requires a context-level route`,`The waitForResponse call must be placed BEFORE the action that triggers the request — if the response arrives before waitForResponse starts listening, the promise never resolves.`,`waitForResponse cannot use glob patterns`,`Add timeout: 30000 to extend the default`],1,`Race condition: set up waitForResponse before navigating or clicking — classic Playwright ordering mistake.`],[`D`,`I`,"A `routeFromHAR()` test passes in CI but fails locally with different data. What is the likely cause?",null,[`HAR files are environment-specific`,`The local HAR file is stale (recorded against an older API) while CI uses the checked-in version. Re-record the HAR with update: true or synchronise the local file with CI.`,`routeFromHAR() only works in Chromium`,`HAR playback is non-deterministic`],1,`HAR fixtures must be committed and refreshed when the API changes — divergence between local and CI HAR files is the cause.`],[`D`,`A`,"Modifying request headers with `route.continue({ headers: newHeaders })` works, but the original cookies are gone from the request. How do you fix it?",null,[`Cookies cannot be forwarded with continue()`,`The headers option REPLACES all headers — spread the originals first: { ...route.request().headers(), ...newHeaders }. Without spread, cookie and other headers are dropped.`,`Use context.setExtraHTTPHeaders() instead`,`Cookies must be set separately via context.addCookies()`],1,`Merging with spread preserves all original headers while adding or overriding specific ones.`],[`C`,`B`,`Complete to inspect request headers in a route handler.
\`\`\`ts
await page.route("**/api/**", route => {
  const headers = route.request().______();
  console.log(headers["content-type"]);
  route.continue();
});
\`\`\``,null,[`headers()`,`getHeaders()`,`requestHeaders()`,`allHeaders()`],0,`headers() returns a plain object with lowercase header names from the intercepted request.`],[`C`,`I`,`Complete to capture both the request and response for an API call.
\`\`\`ts
const [request, response] = await Promise.all([
  page.______("**/api/data"),
  page.______("**/api/data"),
  page.click("#load"),
]);
\`\`\``,null,[`waitForRequest, waitForResponse`,`onRequest, onResponse`,`captureRequest, captureResponse`,`expectRequest, expectResponse`],0,`Promise.all with waitForRequest and waitForResponse started before the click captures both sides of the network call.`],[`C`,`A`,`Complete to serve a static JSON file as the mocked response.
\`\`\`ts
await page.route("**/api/config", async route => {
  await route.fulfill({
    path: ______,
  });
});
\`\`\``,null,[`"fixtures/config.json"`,`"./config.json"`,`__dirname + "/config.json"`,`new URL("config.json", import.meta.url).pathname`],0,`path: accepts a file path — Playwright reads the file and infers Content-Type from the extension.`],[`C`,`I`,`Complete to modify the response body while preserving the status code.
\`\`\`ts
await page.route("**/api/user", async route => {
  const resp = await route.fetch();
  const body = await resp.json();
  body.extra = "injected";
  await route.fulfill({
    ______: resp.status(),
    body: JSON.stringify(body),
    contentType: "application/json",
  });
});
\`\`\``,null,[`status`,`statusCode`,`code`,`httpStatus`],0,`status: sets the HTTP status code in the synthetic fulfill response.`],[`C`,`B`,`Complete to count how many times an API is called.
\`\`\`ts
let callCount = 0;
await page.route("**/api/track", route => {
  ______++;
  route.continue();
});
\`\`\``,null,[`callCount`,`count`,`calls`,`requests`],0,`The closure variable callCount increments on each intercept — assert it after the test actions.`],[`L`,`B`,"Why is `page.route()` preferred over setting up a mock server for API mocking?",null,[`It is faster because it skips the network`,`It is simpler — no extra process to start, stop, or configure. Routes run in-process and can be set up per test. A mock server adds infra complexity and port management.`,`page.route() supports HTTP/2 while mock servers do not`,`Mock servers cannot handle dynamic responses`],1,`In-process route mocking is the simplest approach for most API stubbing scenarios in Playwright tests.`],[`L`,`I`,'What is the difference between `page.on("request", fn)` and `page.route("**", fn)`?',null,[`They are identical`,`on("request") is read-only — it observes requests without intercepting them. route() intercepts — the request is suspended until route.continue/fulfill/abort is called.`,`on("request") only fires for GET requests`,`route() only fires once per URL`],1,`Observation vs. interception — use on("request") for logging and route() when you need to modify or mock.`],[`L`,`I`,"Can `route.fulfill()` be used to simulate slow API responses?",null,[`No — fulfill is synchronous`,`Yes — wrap route.fulfill() in a setTimeout or use a helper: await new Promise(r => setTimeout(r, 2000)); then await route.fulfill({...}); to simulate a 2 second delay.`,`Only by setting timeout in playwright config`,`Yes — pass delay: 2000 as an option to fulfill`],1,`Adding a deliberate delay before fulfill() lets you test loading states and timeout handling.`],[`L`,`A`,"What is the advantage of `routeFromHAR()` over manual `route.fulfill()` for complex APIs?",null,[`HAR files are faster to replay`,`HAR files capture real API responses including headers, timing, and body — no need to hand-craft JSON fixtures for every endpoint. They can be updated with update: true when the API changes.`,`routeFromHAR() supports WebSocket mocking`,`HAR files compress better`],1,`HAR replay eliminates fixture maintenance for complex APIs — re-record when the API changes instead of manually updating JSON mocks.`],[`L`,`I`,'Why should you NOT use `page.route("**", route => route.abort())` in most tests?',null,[`It has a memory leak`,`Aborting all requests breaks page navigation, static asset loading, and any real endpoint the test needs. Restrict route patterns to specific URLs or API paths you want to intercept.`,`fullyParallel requires routes to be allowed`,`abort() is only valid for XHR, not fetch`],1,`Be specific with patterns — ** catches everything including the page HTML itself, causing navigation failures.`],[`R`,`B`,`You want to test the app's behavior when the API is down. How do you simulate this?`,null,[`Shut down the API server during the test`,`await page.route("**/api/**", route => route.fulfill({ status: 503, body: "Service Unavailable" })) — then trigger the action that calls the API and assert the error state in the UI`,`Use a proxy to block the request`,`Set network conditions to offline`],1,`Simulating a 503 is the cleanest approach — no infrastructure changes, no side effects on other tests.`],[`R`,`I`,`You want to verify the correct payload is sent when a form is submitted. How?`,null,[`Check the form's value attribute in the DOM`,`await page.route("**/api/submit", async route => { const body = route.request().postDataJSON(); expect(body.name).toBe("Alice"); await route.continue(); }) — assert the payload before forwarding.`,`Use page.on("request") and call request.postDataJSON()`,`Log the request in the browser console`],1,`Intercepting with route.continue() lets you assert the request payload while still allowing the real request to proceed.`],[`R`,`A`,`You need to test that the app retries a failed request exactly 3 times. How?`,null,[`page.route() with a retry counter in a closure: let count = 0; route handler returns 503 for the first 2 calls and 200 on the 3rd. Assert count === 3 after the action.`,`Configure retries: 3 in playwright config`,`Use waitForRequest 3 times in sequence`,`Add a network condition that fails every third request`],0,`A closure counter with conditional fulfill responses precisely simulates N failures followed by success — the cleanest way to test retry logic.`],[`R`,`I`,`You need to test offline functionality. How do you simulate offline mode?`,null,[`Disconnect the network adapter during the test`,`Use page.route("**", route => route.abort()) to abort all requests, then interact with the UI and assert the offline state message`,`Set headless: false and disable WiFi manually`,`Use context.setOffline(true) if available, otherwise abort all routes`],3,`context.setOffline(true) is the correct Playwright API — it puts the entire context offline without pattern matching.`],[`R`,`B`,`You want to inject a custom header into every outgoing API request. How?`,null,[`Set headers in the request fixture`,`Use context.setExtraHTTPHeaders({ "x-test-env": "ci" }) — it adds the header to every request made by pages in that context, including API calls`,`Intercept every request with page.route() and add the header via continue()`,`Both B and C are valid; B is simpler`],3,`setExtraHTTPHeaders() is the simplest approach for global header injection — route-based injection is needed only when headers differ per request.`],[`S`,`B`,`A test shows different data depending on when it runs because the API returns real time-dependent data. How do you fix it?`,null,[`Add retries to make the test eventually consistent`,`Mock the API endpoint with route.fulfill() to return fixed, deterministic data — the test becomes independent of time or external state`,`Use a fixed system clock via Date mock`,`Freeze the network layer`],1,`Deterministic mocks are the standard solution for time-sensitive or externally-dependent API data in tests.`],[`S`,`I`,`You need to test that a UI element appears only when a specific feature flag is set in the API response. How?`,null,[`Toggle the feature flag in the database before the test`,`Use page.route("**/api/config", route => route.fulfill({ body: JSON.stringify({ featureFlag: true }) })) — the UI reads the flag from the API response and the test controls it precisely`,`Set the flag in localStorage before navigation`,`Add the flag as a query parameter`],1,`Route mocking to control API response content is exactly the right tool for feature-flag UI testing — no backend changes needed.`],[`S`,`A`,"Tests that use `routeFromHAR()` start failing after an API schema change. What is the correct remediation?",null,[`Delete the HAR file and let the tests skip`,`Re-record the HAR: add update: true to the routeFromHAR call, run the tests once (live network), remove update: true, commit the updated HAR file`,`Manually edit the HAR JSON to match the new schema`,`Add a route handler to transform the old HAR response to the new schema`],1,`Refreshing the HAR with update: true is the intended workflow — commit the new HAR and the tests return to passing.`],[`S`,`I`,`A route mock causes a CORS error in the browser console. The real API sets Access-Control-Allow-Origin. How do you fix the mock?`,null,[`CORS errors are expected in mocked tests`,`Add the CORS header in fulfill: route.fulfill({ headers: { "access-control-allow-origin": "*" }, ... }) — the browser sees the header on the synthetic response and allows the cross-origin request`,`Disable CORS in Playwright config`,`Use context.route() instead of page.route()`],1,`Synthetic responses must include any headers the browser enforces — CORS headers are needed if the app is cross-origin from the mock server.`],[`L`,`A`,"What is the difference between `page.route()` and listening to `page.on('request')` / `page.on('response')`?",null,["`page.route()` lets you intercept and actively modify, abort, or fulfill a request before it's sent; the request/response events are read-only observers that fire after the fact and cannot change the traffic",`They are interchangeable; route() is just newer syntax for the same events`,`on('request') can modify headers; route() cannot`,`route() only works for GET requests`],0,`Use route() when you need to control network behavior (mock, block, modify); use the events when you only need to observe/log traffic.`],[`O`,`I`,"What happens to the real `/api/users` network request?",`await page.route('**/api/users', route => {
  route.fulfill({
    status: 200,
    contentType: 'application/json',
    body: JSON.stringify([{ id: 1, name: 'Mock User' }]),
  });
});
await page.goto('/users');`,[`It is intercepted and never reaches the actual server — Playwright responds with the fake body directly, and the app renders as if the server returned that data`,`It is sent to the server as normal, and fulfill() just logs the response afterward`,`route() blocks all requests matching the pattern, including unrelated ones like images`,`The request is sent twice — once real, once mocked — and the app uses whichever resolves first`],0,"`route.fulfill()` short-circuits the request entirely — nothing is sent over the network; the browser receives your synthetic response instead."],[`R`,`I`,`You want to test how your app behaves when a specific API call is slow (e.g., a loading spinner appears and disappears correctly), without waiting for a real slow server. What is the Playwright approach?`,null,["Use `page.route()` on that endpoint, and inside the handler `await new Promise(r => setTimeout(r, delayMs))` before calling `route.continue()` or `route.fulfill()` — simulating latency deterministically","Use `page.waitForTimeout(delayMs)` before every assertion in the test",`Lower the test's global timeout so it fails fast instead`,`Simulate slowness by throttling your own machine's CPU`],0,`Injecting an artificial delay inside a route handler gives you full, deterministic control over network timing without depending on a real slow backend.`],[`D`,`A`,"A test intercepts `**/api/*` to mock all API calls, but one specific test that needs to hit the REAL `/api/health` endpoint keeps getting the mock response instead. What's the fix?",null,["Register a more specific `page.route('**/api/health', route => route.continue())` handler — Playwright runs the most-recently-registered matching handler first, so a later, narrower route can override the broader mock",`page.route() can only be used once per test; remove the broad route entirely`,`Use page.unroute() before every request, then re-add it after`,"The broad pattern `**/api/*` does not match `/api/health` at all, so the bug must be elsewhere"],0,"When multiple route handlers match a URL, Playwright runs the most recently registered one first — call `route.fallback()`/`route.continue()` to pass control to the next matching handler."],[`O`,`A`,`What does this wait for, precisely?`,`const [response] = await Promise.all([
  page.waitForResponse(res => res.url().includes('/api/search') && res.status() === 200),
  page.getByRole('searchbox').fill('playwright'),
]);`,[`A specific network response matching BOTH a URL pattern and a 200 status — using a predicate function for precise matching, rather than just any response from that endpoint (which might include an earlier failed or in-flight request)`,`The very first network response the page has ever made, regardless of URL`,`It waits for ALL pending network requests to finish, not just one specific one`,`fill() triggers the response itself; waitForResponse is redundant here`],0,`A predicate function on waitForResponse gives fine-grained control — important when multiple requests to similar URLs might fire, and only a specific one (matching status too) is the one you care about.`],[`O`,`I`,`What does this test?`,`await context.setOffline(true);
await page.reload();
await expect(page.getByText('You are offline')).toBeVisible();`,[`The app's offline-handling UI — simulating a complete loss of network connectivity and verifying the appropriate offline message/state is shown, without physically disconnecting the test machine's network`,`It only slows down the network, not fully disconnecting it`,`setOffline only affects images and stylesheets, not the page's own navigation`,`It permanently disables the context for all future tests in the file`],0,`context.setOffline() is a deterministic, repeatable way to test offline behavior (like a service worker fallback UI) without relying on flaky real network manipulation.`],[`L`,`A`,`What is a HAR (HTTP Archive) file used for in Playwright, and what workflow does it enable?`,null,[`Playwright can RECORD all network traffic during a real run into a HAR file, then later REPLAY that exact recorded traffic for a route — enabling deterministic, offline test runs against a snapshot of real backend responses, without needing the live backend at all`,`HAR files are only used for storing screenshots in a compressed archive format`,`HAR recording requires a paid Playwright add-on, unlike the rest of network interception`,`HAR files can only be viewed, never replayed back into a test`],0,`HAR-based routing is a middle ground between fully live network calls and fully hand-written mocks — real captured traffic, replayed deterministically.`],[`O`,`A`,`What does this route handler accomplish?`,`await page.route('**/api/profile', async route => {
  const response = await route.fetch();
  const json = await response.json();
  json.isPremium = true;
  await route.fulfill({ response, json });
});`,[`It lets the REAL request go through, then modifies the actual response (forcing isPremium to true) before handing it to the page — useful for testing a feature-flag-like state without needing the real backend to support it`,`It blocks the request entirely, never reaching the real server`,`It duplicates the request, sending it twice to the server`,`route.fetch() and route.fulfill() cannot be used together in the same handler`],0,`route.fetch() followed by a modified route.fulfill() is the pattern for "let the real call happen, but tweak the response" — different from fully faking a response with a hard-coded fulfill().`],[`O`,`I`,`What does this accomplish?`,`await page.route('**/*.{png,jpg,jpeg}', route => route.abort());
await page.goto('/gallery');`,[`It blocks all image requests entirely, letting the page load faster for a test that does not care about actual image content — commonly used to speed up unrelated tests or simulate slow/failed image loading`,`It compresses all images to a smaller size automatically`,`route.abort() only works for the FIRST matching request, not subsequent ones`,`It redirects all image requests to a placeholder CDN`],0,`route.abort() is the tool for deliberately blocking specific request types — a common performance/focus optimization when image content is irrelevant to what's being tested.`],[`L`,`A`,`How can Playwright be used to test an application under simulated slow/throttled network conditions (e.g., "Slow 3G"), similar to Chrome DevTools' network throttling?`,null,[`Via a CDP (Chrome DevTools Protocol) session obtained from the context/page, calling Network.emulateNetworkConditions with custom latency/throughput values — this is a Chromium-specific capability, not exposed as a simple cross-browser Playwright API option`,'Playwright has a built-in `use: { networkSpeed: "3g" }` config option that works identically across all browsers',`Network throttling is impossible to test with Playwright under any circumstances`,`Setting context.setOffline(true) with a delay parameter achieves realistic throttling`],0,`True network throttling (as opposed to just blocking/mocking) leans on the underlying browser's DevTools protocol — available for Chromium via a CDP session, since it isn't a universal cross-browser primitive.`],[`S`,`I`,`Your app uses a WebSocket connection for live chat updates. What is the general strategy for testing WebSocket-dependent features in Playwright?`,null,['Use `page.waitForEvent("websocket")` to get a handle on the WebSocket connection, then listen for its "framereceived"/"framesent" events to observe messages — or, for full control, intercept/mock at a higher level (like mocking the server-side behavior the socket depends on)',`WebSocket traffic cannot be observed or tested by Playwright in any way`,`Only page.route() works for WebSocket traffic, exactly like HTTP requests`,`You must fully disable WebSocket support and fall back to polling for any test coverage`],0,`Playwright exposes WebSocket-specific events (distinct from HTTP request/response) precisely because WebSocket traffic doesn't fit the regular route()/fulfill() request-response model.`],[`L`,`A`,"What does `page.routeWebSocket()` allow that manually listening to `page.on('websocket')` frame events does not?",null,[`It lets you intercept a WebSocket connection and actively mock/modify messages (simulate server pushes, block sends) rather than just passively observing frames as they occur`,`It records frames to a HAR file automatically`,`It works across multiple browser projects simultaneously`,`It is only available for secure wss:// connections`],0,`routeWebSocket gives you an active control point over the connection, unlike the read-only frame events.`],[`L`,`I`,"Why might `page.route()` fail to intercept a request that's actually being served from a service worker's cache/fetch handler?",null,[`page.route() only works for the first request on a page`,`Requests handled entirely within the service worker (which runs in its own execution context) can bypass the page-level network stack Playwright hooks into, unless the context blocks service workers`,`Service workers always disable network interception globally`,`page.route() cannot match any URL containing /sw.js`],1,`A service worker's own fetch handler can serve a response without the request ever reaching the normal page-level network path that route() intercepts.`],[`L`,`A`,"What does setting `serviceWorkers: 'block'` in browser context options accomplish for network testing?",null,[`It blocks all WebSocket connections instead of HTTP`,`It disables JavaScript execution entirely`,`It prevents any service worker from registering/running for pages in that context, so all requests flow through the normal network path where page.route() can reliably intercept them`,`It forces every request through a mock proxy automatically`],2,`Removing the service worker from the equation avoids a class of requests that could otherwise be served outside Playwright's interception path.`],[`L`,`I`,"What does `request.timing()` give you access to inside a `page.on('response')` handler?",null,[`The number of bytes transferred`,`The list of all requests on the page`,`A cached copy of the response body`,`A timing breakdown (DNS lookup, connect, TLS, request start, response start/end, etc.) for that specific request`],3,`Fine-grained timing data lets you assert on performance budgets for individual requests, not just overall page load time.`],[`L`,`I`,"What do `request.redirectedFrom()` and `request.redirectedTo()` let you inspect?",null,[`The full chain of a redirected request — redirectedFrom() gives the original request that led to this one, redirectedTo() gives the request this one was redirected into`,`The list of all cookies set during a redirect`,`The HTTP status code of a redirect only`,`Whether a request used HTTPS`],0,`These methods let you walk the redirect chain in either direction from any single request in it.`],[`L`,`I`,"When would `request.failure()` return a non-null value?",null,[`For any response with a status code 400 or higher`,`For a request that failed at the network level (e.g. aborted, DNS failure, connection refused) rather than completing with an HTTP response`,`For every request that took longer than 1 second`,`Only for requests blocked by CORS`],1,`A 404 or 500 is still a completed HTTP response — failure() is reserved for requests that never got that far.`],[`L`,`A`,"How do you mock a binary response (e.g. an image) with `route.fulfill()`?",null,[`It is not possible — fulfill() only supports text bodies`,`Base64-encode the image and pass it as the status option`,`Pass a Buffer to the body option along with the correct contentType (e.g. image/png) — string bodies are for text content, Buffers preserve binary data correctly`,`Use route.continue() with a binary flag`],2,`Buffers preserve raw binary bytes exactly, whereas a plain string body would corrupt binary content.`],[`L`,`I`,"What does the `bypassCSP: true` context option do, and why might it matter for network-dependent tests?",null,[`It disables all network requests for the context`,`It forces all requests to use plain HTTP`,`It automatically approves all cross-origin requests`,`It disables the page's Content-Security-Policy enforcement, which can otherwise block certain injected scripts or resources from loading, useful when a test needs to inject something the real CSP would forbid`],3,`A strict CSP can prevent Playwright-injected test hooks/scripts from executing unless it's explicitly bypassed for the test context.`],[`L`,`I`,"If a route pattern is registered on both `context.route()` and `page.route()`, and both would match a request, which one takes effect?",null,[`The most-recently-registered matching handler runs first regardless of whether it was registered on the context or the page`,`context.route() always wins over page.route()`,`page.route() always wins over context.route()`,`Both run simultaneously and the responses are merged`],0,`Handler precedence is purely about registration order (LIFO), not the level (context vs page) at which the handler was registered.`],[`L`,`A`,"What's the difference between recording a HAR via `browser.newContext({ recordHar: { path } })` and using `page.routeFromHAR()` with an update mode?",null,[`They are functionally identical`,`recordHar passively records ALL real network traffic for that context to a HAR file as the test runs live; routeFromHAR's update mode instead intercepts matching routes and records/replays only what matches that specific route pattern`,`recordHar only works for GET requests`,`routeFromHAR update mode never actually reaches the real network`],1,`recordHar is a blanket, context-wide recorder; routeFromHAR in update mode is scoped to whatever URL pattern you register the route for.`],[`L`,`I`,"What's the difference between `recordHar: { mode: 'minimal' }` and the default 'full' mode?",null,[`minimal mode only records failed requests`,`minimal mode cannot record POST bodies`,`minimal mode omits information not used for replay (like detailed timing) producing a smaller HAR file optimized purely for later route mocking, while full mode captures complete details useful for general network analysis`,`full mode is required for routeFromHAR to work at all`],2,`Choose minimal when the HAR's only purpose is future replay via routeFromHAR, and full when you need it for broader network inspection.`],[`L`,`A`,"What happens if a `page.route()` handler throws an uncaught error instead of calling fulfill/abort/continue?",null,[`The test suite crashes entirely`,`The request silently succeeds with an empty 200 response`,`Playwright automatically retries the handler up to 3 times`,`The request effectively fails (the page sees a failed request), but it's best practice to explicitly handle errors and still resolve the route rather than relying on this fallback behavior`],3,`Relying on an uncaught throw to resolve a route is fragile — explicit error handling that still calls fulfill/abort is more predictable.`],[`L`,`I`,"What's different about aborting a request where `request.isNavigationRequest()` is true, compared to aborting an image subresource request?",null,[`Aborting a navigation request typically causes the entire page load/navigation to fail, whereas aborting a subresource just leaves that one resource missing while the page otherwise loads`,`There is no functional difference between the two`,`Navigation requests cannot be aborted at all`,`Aborting a subresource always crashes the browser tab`],0,`A navigation request is the document itself — aborting it prevents the page from loading at all, unlike a missing image or script.`],[`L`,`B`,"Why is waiting for `page.waitForLoadState('networkidle')` generally discouraged as a synchronization strategy in modern Playwright tests?",null,[`It is deprecated and throws an error`,`Pages with polling, analytics beacons, or websockets may never truly go network-idle, causing flaky timeouts; explicit waits for a specific element/response are more reliable`,`It only works in Chromium`,`It waits longer than the default test timeout by design`],1,`Continuous background network activity on many real apps means "idle" may simply never occur, making this wait unreliable.`],[`L`,`I`,`How do you make Playwright automatically pass HTTP Basic Auth credentials for a site protected at that level?`,null,[`Set them as extraHTTPHeaders manually on every request`,`Use page.route() to inject an Authorization header on every response`,`Set httpCredentials: { username, password } when creating the browser context — Playwright answers basic-auth challenges automatically for all requests in that context`,`Basic Auth cannot be automated in Playwright`],2,`httpCredentials handles the Basic Auth handshake transparently, without needing manual header injection via route handlers.`],[`L`,`I`,"What does `request.serviceWorker()` tell you about a given intercepted request?",null,[`Whether the request used HTTPS`,`The total number of registered service workers`,`Whether the request was cached`,`Whether (and which) service worker was involved in initiating or handling that particular request, distinguishing service-worker-originated requests from ones that came directly from the page`],3,`This helps distinguish requests the page made directly from requests a service worker generated or intercepted on its own.`],[`L`,`I`,"What's the difference between `route.fulfill({ json: obj })` and `route.fulfill({ body: JSON.stringify(obj), contentType: 'application/json' })`?",null,[`They are functionally equivalent — json: is a convenience shorthand that automatically serializes the object and sets the correct Content-Type, saving you from doing both manually`,`json: is slower because it validates the schema first`,`json: only works for arrays, not objects`,`body: cannot be combined with contentType`],0,`json: exists purely for convenience — under the hood it performs the same serialization and header-setting.`],[`L`,`A`,"Why might a glob pattern like `**/api/search?q=test` fail to match the intended request even though the URL genuinely contains that query string?",null,[`Glob patterns cannot contain the word "search"`,`? is a glob wildcard character (matches any single character) in Playwright's URL glob syntax, not a literal question mark — the query string needs different handling (RegExp, predicate, or escaping)`,`Query strings are never matched by page.route() under any circumstances`,`Glob patterns require the full URL including the protocol`],1,`Because ? has special meaning in glob syntax, a literal query-string question mark needs to be handled with RegExp or a predicate function instead.`],[`L`,`I`,"How would a single `page.route()` handler behave differently for GET vs POST requests to the same URL pattern?",null,[`Playwright automatically splits GET and POST into separate handlers`,`POST requests are never intercepted by page.route()`,`Inspect route.request().method() inside the handler and branch your logic (e.g. fulfill GET with cached data, continue POST to the real server) — the pattern match alone doesn't distinguish methods`,`GET and POST always require two separate route() calls with different patterns`],2,`The URL pattern only filters by URL — method-specific behavior requires checking request.method() inside the single handler.`],[`L`,`A`,"Why can't `route.fulfill()` alone realistically simulate a slow DOWNLOAD (e.g. a large file trickling in over 10 seconds), even though it can simulate slow time-to-first-byte via a setTimeout delay before fulfilling?",null,[`fulfill() cannot delay a response at all`,`fulfill() only works for responses under 1KB`,`Delaying fulfill() always causes the test to time out`,`fulfill() delivers the entire response body at once as a complete unit — delaying before calling it only affects when the response starts, not how it streams; true throttled download speed requires lower-level throttling (e.g. CDP network conditions)`],3,`A setTimeout before fulfill() delays the START of the response, not the rate at which bytes arrive afterward.`],[`L`,`I`,"When mocking a 503 response with `route.fulfill()`, why might you also want to set a `Retry-After` header on the mocked response?",null,[`To test that the app actually reads and respects the server's suggested retry delay, rather than only testing that a bare 503 triggers some generic error UI`,`Retry-After is required by fulfill() or it throws`,`It automatically retries the request for you`,`It has no real effect and is purely cosmetic`],0,`Including realistic headers on a mock lets you test the app's handling of that specific header, not just its generic error path.`],[`L`,`B`,"Why must every `page.route()` handler eventually call exactly one of fulfill(), abort(), or continue()?",null,[`It is only a style convention, not a requirement`,`The intercepted request is suspended in a pending state until one of those is called — omitting all three leaves the request hanging until it times out`,`Playwright automatically calls continue() after 1 second if none are called`,`Handlers that don't call one of these are simply skipped`],1,`A route handler holds the request open until it's explicitly resolved one way or another.`],[`L`,`I`,"Playwright doesn't offer a `browser.route()` — why does routing exist only at the page and context level?",null,[`Because browser-level routing would be too slow`,`browser.route() exists but is deprecated`,`Routing is inherently about a specific browsing session's network traffic; a single browser instance can host many unrelated contexts (e.g. different logged-in users) that shouldn't share the same interception rules`,`It is a current limitation that will be fixed in a future version`],2,`Contexts model isolated sessions — sharing routing rules across all of them at the browser level would break that isolation.`],[`L`,`A`,"Could a route pattern of `'https://example.com/api'` fail to match a request to `'https://example.com/api/'` (trailing slash)?",null,[`No, trailing slashes are always ignored`,`No, but only for GET requests`,`Yes, but only in Firefox`,`Yes — an exact string pattern must match the URL exactly character-for-character, so a trailing slash difference causes a mismatch; use a glob or RegExp to be tolerant of it`],3,`Exact string patterns require exact character-for-character matches, unlike glob or RegExp patterns which can tolerate variation.`],[`L`,`I`,"When should you prefer `context.setExtraHTTPHeaders()` over adding headers via `route.continue({ headers })` on every matched request?",null,[`When the same header(s) should apply to literally every request the context makes (e.g. a test environment marker) — it's simpler and avoids needing a route handler at all`,`Never — route-based header injection is always preferred`,`setExtraHTTPHeaders only works for GET requests`,`route.continue() cannot set headers`],0,`A blanket, unconditional header applies to all requests without needing any pattern matching or handler logic.`],[`L`,`A`,'Why might you fulfill a request with just `{ status: 204 }` and no body when testing a "fire and forget" analytics-style POST?',null,[`204 is required for all mocked responses`,`To let the app's success-handling logic proceed normally (some apps only check status, not body) while suppressing any real analytics call from actually reaching a third-party service during tests`,`It automatically disables all future analytics calls in the test run`,`204 responses are cached more aggressively by the browser`],1,`A minimal, status-only mock is enough for the app to treat the fire-and-forget call as successful without contacting the real third party.`],[`L`,`I`,`If you use CDP to set network throttling conditions on one page, do subsequently opened pages in the same context automatically inherit those conditions?`,null,[`Yes, all pages in the context share one CDP session automatically`,`Yes, but only for the first 5 new pages`,`No — CDP network emulation is applied per CDP session (effectively per page), so each new page typically needs its own session and its own explicit emulateNetworkConditions call`,`No, throttling can only ever be applied once per browser instance`],2,`Each page generally needs its own newCDPSession() and its own explicit call to reapply the desired network conditions.`],[`L`,`B`,`Are Playwright's glob route patterns case-sensitive when matching URL paths?`,null,[`No, matching is always case-insensitive`,`Only on Windows machines`,`Case sensitivity depends on the browser being used`,"Yes — `**/API/users` would not match a request to `/api/users`; the pattern must match the actual casing of the URL"],3,`Glob matching in Playwright treats the URL path literally, casing included.`],[`L`,`I`,"If a test opens two separate `browser.newContext()` instances and registers a route only on the first context, does it also apply to pages opened in the second context?",null,[`No — routes are scoped to the context (or page) they were registered on; a second, independent context has no routing rules unless you explicitly register them there too`,`Yes, all contexts from the same browser share routes`,`Yes, but only for requests to the same origin`,`It depends on whether fullyParallel is enabled`],0,`Contexts are isolated browsing sessions — routing configuration does not carry over between them automatically.`],[`L`,`A`,"Why might you prefer `route.fulfill({ path: 'fixtures/large-response.json' })` over `route.fulfill({ body: largeJsonString })` for a large mock payload?",null,[`path: is required for any response over 1MB`,`path: streams the file from disk and lets Playwright infer the Content-Type from its extension, keeping the test file itself free of a huge inline JSON string and easier to maintain as a separate fixture file`,`body: does not support JSON at all`,`path: is faster because it skips network layers entirely`],1,`Keeping large fixture data in its own file improves readability and lets multiple tests reuse the same fixture.`],[`L`,`I`,"What's the difference between `page.on('requestfinished')` and `page.on('requestfailed')`?",null,[`They fire for the exact same set of requests`,`requestfailed only fires for requests to third-party domains`,`requestfinished fires when a request completes successfully (received a full response, even an HTTP error status like 404); requestfailed fires when the request could not complete at the network level at all (e.g. aborted, DNS error, connection reset)`,`requestfinished only fires for GET requests`],2,`A completed-but-erroneous HTTP response (like a 500) still counts as "finished" — failed is reserved for network-level breakdowns.`],[`L`,`A`,"Why might a test author register a broad `context.route()` mock for ALL API calls as a baseline in a fixture, then register a narrower `page.route()` override inside a specific test for one endpoint?",null,[`This pattern is invalid and Playwright will throw an error`,`Context-level routes always override page-level routes so this achieves nothing`,`It doubles the number of network requests made`,`It lets most endpoints use a shared, reusable set of default mock responses (reducing per-test boilerplate) while still allowing an individual test to precisely control the one endpoint it cares about, since the more recently registered page-level route takes precedence`],3,`Layering broad defaults with a narrower, later-registered override is a clean way to share setup while keeping per-test control.`],[`L`,`I`,"When would you use a predicate function with `page.waitForRequest(req => ...)` instead of a simple glob string?",null,[`When matching requires more than URL pattern matching alone — e.g. checking the HTTP method, a header value, or the POST body content — a predicate function has access to the full Request object to make that decision`,`Predicate functions are always required, glob strings are deprecated`,`Glob strings cannot match any URL containing "api"`,`Predicate functions run faster than glob matching`],0,`A predicate lets you inspect anything about the Request object, not just its URL string.`],[`L`,`A`,"Do `page.on('request')` listeners fire before or after a matching `page.route()` handler resolves the request?",null,[`Always strictly after the route handler calls fulfill()`,`The request event fires as soon as the request is intercepted/initiated, independent of when the route handler eventually resolves it — the event reflects the request being made, not the route decision being finalized`,`Only if no route handler is registered at all`,`request events never fire when routing is active`],1,`The request event reflects the request being issued, not the eventual outcome of any route handler processing it.`],[`L`,`I`,"What can `page.routeWebSocket()`'s handler do that lets you simulate the server pushing an unsolicited message to the client?",null,[`It cannot send messages, only observe them`,`It automatically replays a HAR file of WebSocket frames`,`Call .send() on the mocked WebSocket route object at any time (not just in response to a client message) to inject a synthetic server-pushed frame into the page's WebSocket handling code`,`It requires the client to send a message first before send() becomes available`],2,`Because you control the mock server side of the connection, you can push messages proactively at any time, not just in reply to client sends.`],[`L`,`A`,"How would you test that your app respects a `Cache-Control: no-store` response header by not caching sensitive data?",null,[`This cannot be tested with Playwright, only with browser DevTools manually`,`Check that the response body contains the word "no-store"`,`Assert the response arrives in under 100ms`,`Mock the endpoint's response with that header via route.fulfill(), then trigger a scenario where the app might otherwise cache it (e.g. navigate away and back), and assert the app makes a fresh request rather than reusing stale/cached data`],3,`The real test is behavioral: does the app actually re-fetch instead of relying on a cached copy after the header instructs it not to store one?`],[`L`,`I`,"Why is `await route.fetch()` generally safer than manually reconstructing the request with a separate `request.get(route.request().url())` inside a handler?",null,[`route.fetch() automatically forwards the original request's method, headers (including auth/cookies), and body exactly as received, whereas manually rebuilding the request risks accidentally dropping some of that information`,`route.fetch() is always faster`,`request.get() cannot be called from inside a route handler`,`route.fetch() bypasses TLS certificate checks`],0,`Manually reconstructing a request is error-prone — route.fetch() guarantees fidelity to the original request.`],[`L`,`A`,"For a mock that should only apply to the very first matching request and let all subsequent identical requests hit the real server, is `{ times: 1 }` or calling `page.unroute()` inside the handler after one match more idiomatic?",null,[`Calling page.unroute() from inside the handler is the only valid approach`,`{ times: 1 } — it's the built-in, declarative way to express a one-time interception, avoiding the extra bookkeeping of manually unrouting from within the handler`,`Neither works; you must register and remove the route from a separate test.step`,`Both approaches are invalid because a route cannot self-remove`],1,`The times option expresses one-shot interception declaratively without needing manual cleanup logic inside the handler.`],[`L`,`I`,"Why does `context.setOffline(true)` more accurately simulate real offline conditions than `page.route('**', route => route.abort())`?",null,[`They are functionally identical in every respect`,`abort() only works for GET requests, not POST`,`setOffline actually changes the browser's reported network connectivity state (affecting APIs like navigator.onLine and triggering real 'offline' events the app may listen for), whereas aborting every route just makes requests fail while the browser still thinks it's online`,`setOffline is slower to apply than route-based aborting`],2,`setOffline changes what the browser itself reports as connectivity state, which a blanket abort() cannot replicate.`],[`L`,`A`,`Why is CDP-based network throttling (Network.emulateNetworkConditions) not something you can use uniformly across all Playwright browser projects?`,null,[`It works identically in all browsers but is disabled by default`,`CDP throttling requires a paid Playwright license`,`It only works when running in headless mode`,`CDP (Chrome DevTools Protocol) is specific to Chromium-based browsers — Firefox and WebKit don't expose the same protocol, so throttling via CDP only works when testing against Chromium`],3,`CDP is a Chromium-specific debugging protocol; Firefox and WebKit have no equivalent Playwright can hook into the same way.`],[`O`,`A`,`What does this code do?`,`await page.routeWebSocket('wss://example.com/chat', ws => {
  ws.onMessage(message => {
    ws.send(JSON.stringify({ type: 'ack' }));
  });
});`,[`It intercepts the WebSocket connection and, whenever the page sends a message, replies with a synthetic 'ack' message instead of talking to the real server`,`It logs every WebSocket message to the console without modifying anything`,`It closes the WebSocket connection immediately`,`It records the WebSocket traffic to a HAR file`],0,`routeWebSocket lets the test act as a fake server, responding to client sends with fully synthetic messages.`],[`O`,`I`,`Why block service workers in this setup?`,`const context = await browser.newContext({ serviceWorkers: 'block' });
const page = await context.newPage();
await page.route('**/api/**', route => route.fulfill({ json: [] }));`,[`To make the test run faster by skipping caching`,`To guarantee the mocked route actually intercepts the request, since a service worker's own fetch handler could otherwise serve the request from cache without ever going through the page's normal network path`,`Because service workers are not supported in test contexts at all`,`To disable JavaScript execution for security`],1,`Blocking the service worker removes a possible bypass of the normal request path that page.route() hooks into.`],[`O`,`I`,`What is asserted here?`,`page.on('response', async response => {
  const timing = response.request().timing();
  expect(timing.responseEnd - timing.requestStart).toBeLessThan(1000);
});`,[`That the page loaded in under 1 second overall`,`That the response body is smaller than 1KB`,`That a specific request completed (from being sent to fully receiving the response) in under one second`,`That fewer than 1000 requests were made`],2,`timing() gives per-request breakdown fields you can use to assert on individual request performance.`],[`O`,`I`,`What does this log?`,`page.on('request', request => {
  if (request.redirectedFrom()) {
    console.log(request.redirectedFrom().url(), '->', request.url());
  }
});`,[`Every single request made by the page`,`Only requests that failed`,`The response status codes for every redirect`,`For any request that resulted from a redirect, the original URL and the URL it was redirected to`],3,`redirectedFrom() is only non-null when the current request is itself the result of following a redirect.`],[`O`,`I`,`What gets logged for aborted png requests?`,`page.on('requestfailed', request => {
  console.log(request.url(), request.failure()?.errorText);
});
await page.route('**/*.png', route => route.abort());`,[`The URL of each aborted image request along with a network-level error description, since abort() causes the request to fail rather than complete`,`Nothing, because aborted requests never fire requestfailed`,`The full response body of each png request`,`A count of how many png files exist on the page`],0,`Aborting a request results in a network-level failure, which triggers the requestfailed event with an errorText.`],[`O`,`A`,`What does this do?`,`const imgBuffer = fs.readFileSync('fixtures/placeholder.png');
await page.route('**/*.jpg', route => route.fulfill({
  status: 200,
  contentType: 'image/png',
  body: imgBuffer,
}));`,[`It converts every .jpg file on the server to .png`,`It replaces every .jpg request's response with the bytes of a local placeholder PNG file, served as if it were the requested image`,`It blocks all .jpg requests entirely`,`It resizes real .jpg images before returning them`],1,`route.fulfill() with a Buffer body lets you swap in real binary content, including a different image format than requested.`],[`O`,`I`,`Why is bypassCSP needed here?`,`const context = await browser.newContext({ bypassCSP: true });
const page = await context.newPage();
await page.addScriptTag({ content: 'window.__testHook = true;' });`,[`addScriptTag never works without bypassCSP`,`It speeds up script execution`,`The target site's Content-Security-Policy would otherwise block this inline script injection; bypassing CSP for the test context allows the injected script to actually execute`,`It is needed to enable network interception at all`],2,`A strict CSP can specifically forbid inline scripts, which is exactly what addScriptTag with inline content would otherwise trigger.`],[`O`,`A`,`Which mock wins for /api/users?`,`await context.route('**/api/**', route => route.fulfill({ json: { source: 'context' } }));
await page.route('**/api/users', route => route.fulfill({ json: { source: 'page' } }));
const res = await page.request.get('/api/users');`,[`The context-level route always wins regardless of order`,`Neither — both run and the responses are merged`,`It is non-deterministic which one wins`,`The page-level route wins because it was registered more recently than the context-level route, regardless of which level it was registered at — LIFO order applies across both`],3,`Precedence follows registration order (last registered, first to run), not the level at which a route was registered.`],[`O`,`I`,`What happens during the test?`,`const context = await browser.newContext({
  recordHar: { path: 'network.har', mode: 'minimal' },
});`,[`All real network traffic in this context is passively recorded to network.har as the test runs, ready to be used later with routeFromHAR — no route mocking happens now`,`All routes are automatically mocked from an existing HAR file`,`The test fails if any network request occurs`,`It replays a previous test run's network activity`],0,`recordHar is a recorder, not a mocker — it captures live traffic for later replay.`],[`O`,`B`,`Why does this time out?`,`await page.goto('/dashboard');
await page.waitForLoadState('networkidle'); // times out
// Dashboard has a live polling widget hitting /api/metrics every 2s`,[`networkidle only works on the homepage`,`The polling widget keeps making periodic requests, so the network never truly goes idle — an explicit wait for a specific element or response is needed instead`,`waitForLoadState requires an explicit timeout argument`,`The dashboard route is not registered correctly`],1,`Continuous polling means the page is never actually "idle" from a network perspective.`],[`O`,`I`,`What problem does this solve?`,`const context = await browser.newContext({
  httpCredentials: { username: 'tester', password: 'pass123' },
});
const page = await context.newPage();
await page.goto('https://staging.example.com');`,[`It logs into the application as user "tester"`,`It disables TLS verification`,`It automatically answers the HTTP Basic Auth prompt that staging.example.com presents before serving any page, which would otherwise block navigation entirely`,`It sets a cookie named httpCredentials`],2,`HTTP Basic Auth is a browser-level challenge separate from any in-app login form.`],[`O`,`I`,`What is the relationship between these two calls?`,`await route.fulfill({ json: { ok: true } });
// vs
await route.fulfill({
  body: JSON.stringify({ ok: true }),
  contentType: 'application/json',
});`,[`They produce different Content-Type headers`,`json: is deprecated in favor of body:`,`body: is faster because it skips serialization`,`They produce an equivalent response — json: is shorthand that does the stringify and content-type setting for you`],3,`json: is purely a convenience wrapper around manual stringify + contentType.`],[`O`,`A`,`Why doesn't the route fire here?`,`await page.route('**/api/search?q=test', route => route.fulfill({ json: [] }));
await page.goto('/search?q=test');
// route never fires`,[`? in the glob pattern matches any single character, not a literal question mark, so the pattern doesn't actually match a URL containing a real '?' as written — it needs escaping or RegExp/predicate matching instead`,`page.route() cannot match query strings under any circumstances`,`The route was registered after goto() started`,`fulfill() requires an explicit status code to activate`],0,`Glob wildcards treat ? specially, which breaks naive attempts to match a literal query string character.`],[`O`,`I`,`What is the behavior of this handler?`,`await page.route('**/api/items', route => {
  if (route.request().method() === 'GET') {
    return route.fulfill({ json: cachedItems });
  }
  return route.continue();
});`,[`GET and POST both get the cached mock response`,`GET requests to /api/items are served from a mocked cache while any other method (POST, PUT, etc.) passes through to the real server`,`It blocks all requests to /api/items regardless of method`,`It only intercepts requests with a query string`],1,`Branching on request.method() inside a single handler lets one route pattern behave differently per HTTP verb.`],[`O`,`A`,`What does this let you verify?`,`await page.route('**/api/orders', route => route.fulfill({
  status: 503,
  headers: { 'Retry-After': '5' },
  body: 'Service Unavailable',
}));`,[`That the server automatically retries after 5 seconds`,`That 503 responses are cached for 5 seconds`,`That the app reads the Retry-After header and waits roughly 5 seconds before retrying, rather than retrying immediately or not at all`,`That the mock always takes exactly 5 seconds to resolve`],2,`This mock lets you test the app's actual handling of the Retry-After guidance, beyond just detecting a generic error.`],[`O`,`B`,`What is the outcome of this code?`,`await page.route('**/api/data', route => {
  console.log('intercepted');
  // handler ends here
});
await page.goto('/');`,[`The request to /api/data is fulfilled with an empty 200 response automatically`,`The console.log call throws an error`,`The route is silently ignored and the real server is used`,`The request to /api/data hangs indefinitely (until timeout) because the handler never calls fulfill, abort, or continue`],3,`A handler that never resolves the route leaves the intercepted request permanently pending.`],[`O`,`I`,`Why isn't this route intercepted?`,`await page.route('https://api.example.com/health', route => route.fulfill({ json: { ok: true } }));
await page.request.get('https://api.example.com/health/');
// not intercepted`,[`An exact string pattern requires an exact match, and the trailing slash makes the actual URL different from the pattern — use a glob or regex to tolerate it`,`Exact string patterns automatically ignore trailing slashes`,`page.request calls are never intercepted by page.route()`,`The route only applies to page.goto(), not page.request`],0,`An exact string pattern is character-for-character strict, unlike glob or regex patterns.`],[`O`,`I`,`When is context.setExtraHTTPHeaders preferable to per-request header injection via route.continue here?`,`await context.setExtraHTTPHeaders({ 'x-test-run-id': runId });
// applies to every subsequent request from this context`,[`Only when testing POST requests`,`When the same header should be present on literally all requests regardless of URL — no route pattern matching or handler logic is needed at all`,`It is never preferable to route-based injection`,`Only when the header value never changes across the whole test suite`],1,`A blanket header requirement is simpler to express at the context level than via a route handler.`],[`O`,`B`,`What is the effect of this route?`,`await page.route('**/analytics/collect', route => route.fulfill({ status: 204 }));`,[`It blocks all analytics scripts from loading on the page`,`It causes the app to throw an error whenever analytics fires`,`Any call to the analytics collection endpoint gets a fake "no content" success response instead of actually reaching the real third-party analytics service`,`It records the analytics payload to a local file`],2,`A minimal 204 mock lets the app's fire-and-forget analytics call "succeed" without any real third-party traffic.`],[`O`,`A`,`Why isn't page2 throttled here?`,`const client = await context.newCDPSession(page);
await client.send('Network.emulateNetworkConditions', {
  offline: false, latency: 400, downloadThroughput: 50 * 1024, uploadThroughput: 20 * 1024,
});
const page2 = await context.newPage();
// page2 not throttled`,[`CDP throttling always applies to the whole browser instance`,`page2 inherits throttling only after a reload`,`CDP sessions expire after 30 seconds`,`The CDP session and its emulated network conditions were established for the original page only — a newly opened page needs its own CDP session and its own emulateNetworkConditions call`],3,`CDP sessions are tied to a specific page target, not shared automatically with sibling pages in the same context.`],[`O`,`B`,`Why doesn't this route intercept the request?`,`await page.route('**/API/users', route => route.fulfill({ json: [] }));
await page.request.get('/api/users');
// not intercepted`,[`Glob patterns match URL paths case-sensitively, and 'API' does not match the actual lowercase 'api' in the request URL`,`route() cannot match relative URLs`,`page.request bypasses page.route() entirely`,`The pattern is missing a leading slash`],0,`Case mismatches between the pattern and the actual URL path prevent a match, since matching is case-sensitive.`],[`O`,`I`,`Why does context2 hit the real API?`,`const context1 = await browser.newContext();
await context1.route('**/api/**', route => route.fulfill({ json: [] }));
const context2 = await browser.newContext();
const page2 = await context2.newPage();
await page2.goto('/'); // hits the real API`,[`Routes registered on context1 apply globally to the whole browser`,`Routes registered on context1 are scoped only to that context — context2 is an entirely separate browsing session with no routing rules of its own`,`context2 must call unroute() first to see any behavior`,`It is a bug that should be reported to Playwright`],1,`Each browser context is an isolated session, and routing rules do not automatically propagate between contexts.`],[`O`,`A`,`What's the advantage over inlining the JSON as a body string?`,`await page.route('**/api/config', route => route.fulfill({
  path: 'fixtures/large-config.json',
}));`,[`path: responses load faster over the network`,`path: automatically validates the JSON schema`,`The fixture lives in its own file (easier to review/maintain, and Content-Type is inferred from the .json extension) rather than being embedded as a large string literal in the test file`,`body: cannot handle files larger than 1KB`],2,`Keeping fixture data in a separate file improves maintainability and lets it be reused across multiple tests.`],[`O`,`I`,`Which event fires for each route in this scenario?`,`page.on('requestfinished', r => console.log('finished:', r.url()));
page.on('requestfailed', r => console.log('failed:', r.url()));
await page.route('**/broken', route => route.abort());
await page.route('**/api/data', route => route.fulfill({ status: 404, body: 'nf' }));`,[`Both requests fire requestfailed since neither returns a 2xx status`,`Both requests fire requestfinished because fulfill() and abort() both "complete" the route`,`Neither event fires because both requests were intercepted`,`The aborted '/broken' request fires requestfailed; the /api/data request (which got a real, if unsuccessful, 404 response) fires requestfinished — a completed response is not a failure`],3,`requestfailed is reserved for network-level failures like abort() — an HTTP error status still counts as a finished request.`],[`O`,`A`,`What does this layering achieve?`,`// fixture-level default mocks
await context.route('**/api/**', route => route.fulfill({ json: { default: true } }));
// test-specific override
test('shows error state', async ({ page }) => {
  await page.route('**/api/orders', route => route.fulfill({ status: 500 }));
  ...
});`,[`Most endpoints keep using the shared baseline mock from the fixture, while this specific test cleanly overrides just the /api/orders behavior it needs, without touching the shared fixture`,`It causes a routing conflict that Playwright rejects at runtime`,`Both mocks apply simultaneously, merging their responses`,`The test-specific route silently does nothing because it was registered after the fixture route`],0,`The later-registered, narrower page-level route takes precedence over the earlier, broader context-level default.`],[`O`,`I`,`What makes a predicate necessary here instead of a plain glob?`,`const req = await page.waitForRequest(req =>
  req.url().includes('/api/search') && req.postDataJSON()?.query === 'shoes'
);`,[`Glob strings cannot contain the word "search"`,`The match depends on the POST body content ('query' being 'shoes'), which a simple URL glob pattern has no way to inspect`,`waitForRequest never accepts glob strings, only predicates`,`Predicates are required whenever more than one request matches the URL`],1,`A URL-only glob has no visibility into the request body, which is exactly what this match condition depends on.`],[`O`,`B`,`When does 'sent:' log relative to the 500ms delay?`,`page.on('request', req => console.log('sent:', req.url()));
await page.route('**/api/data', async route => {
  await new Promise(r => setTimeout(r, 500));
  await route.fulfill({ json: [] });
});`,[`After the 500ms delay, right before fulfill() resolves`,`It never logs because route() suppresses the request event`,`Immediately, before the delay — the request event fires when the request is made, independent of how long the route handler takes to eventually resolve it`,`Exactly at the 250ms midpoint of the delay`],2,`The request event reflects the request being issued, not when its route handler eventually finishes.`],[`O`,`A`,`What does this simulate?`,`await page.routeWebSocket('wss://example.com/live', ws => {
  setTimeout(() => ws.send(JSON.stringify({ type: 'priceUpdate', price: 42 })), 1000);
});`,[`The client requesting a price update from the server`,`A connection timeout after 1 second`,`The WebSocket connection closing after 1 second`,`The server pushing an unsolicited 'priceUpdate' message to the client one second after the WebSocket connects, without the client having sent anything first`],3,`Since the mock controls the server side of the socket, it can push messages proactively at any time, not only in reply to client sends.`],[`O`,`I`,`What is being verified here?`,`await page.route('**/api/account', route => route.fulfill({
  headers: { 'Cache-Control': 'no-store' },
  json: accountData,
}));
await page.goto('/account');
await page.goto('/other');
await page.goBack();
// assert a fresh request was made`,[`That the app doesn't cache the no-store-marked sensitive account data and instead re-fetches it when the page is revisited`,`That the account endpoint responds quickly`,`That goBack() always triggers a full page reload`,`That accountData contains no sensitive fields`],0,`The real test is behavioral — checking that a fresh network request actually happens rather than a cached response being silently reused.`],[`O`,`I`,`What would be lost if this used request.post(route.request().url()) manually instead of route.fetch()?`,`await page.route('**/api/upload', async route => {
  const response = await route.fetch(); // forwards original method, headers, body
  await route.fulfill({ response });
});`,[`Nothing — the two approaches are always equivalent`,`The original multipart body, auth headers, and exact method would need to be manually reconstructed and could easily be dropped or gotten wrong — route.fetch() forwards all of it automatically`,`request.post() is faster than route.fetch()`,`The response status code would be lost either way`],1,`route.fetch() guarantees fidelity to the original request; manual reconstruction risks silently dropping details like the body or auth headers.`],[`O`,`B`,`What is the behavior on the second matching request?`,`let calls = 0;
await page.route('**/api/bootstrap', route => {
  calls++;
  route.fulfill({ json: { bootstrap: true } });
}, { times: 1 });`,[`The handler intercepts every matching request forever`,`The second request throws an error`,`The route handler no longer intercepts — the second and later requests to that URL go to the real server, since times: 1 auto-removed the handler after the first match`,`calls increments but the response is never sent`],2,`times: 1 automatically removes the handler after exactly one match, letting subsequent requests reach the real server.`],[`O`,`A`,"Would `page.route('**', route => route.abort())` also make this assertion pass?",`await context.setOffline(true);
const isOnline = await page.evaluate(() => navigator.onLine);
expect(isOnline).toBe(false);`,[`Yes, they are functionally equivalent for this purpose`,`Yes, but only in Chromium`,`No, because abort() cannot be combined with page.evaluate()`,`No — aborting requests doesn't change what navigator.onLine reports; only setOffline() actually flips the browser's reported connectivity state`],3,`navigator.onLine reflects the browser's reported connectivity state, which only setOffline() actually changes.`],[`O`,`I`,`Why the test.skip here?`,`// playwright.config.ts – webkit project
test('throttled load', async ({ page, browserName }) => {
  test.skip(browserName !== 'chromium', 'CDP throttling is Chromium-only');
  const client = await page.context().newCDPSession(page);
  ...
});`,[`newCDPSession and Network.emulateNetworkConditions rely on the Chrome DevTools Protocol, which WebKit and Firefox don't implement — the test can only meaningfully run against Chromium`,`newCDPSession is deprecated in all browsers`,`WebKit does not support any form of network testing`,`The test is flaky in WebKit for unrelated reasons`],0,`CDP is a Chromium-specific protocol, so any test depending on it must be scoped to Chromium projects only.`],[`O`,`I`,`What does urlFilter combined with minimal mode accomplish here?`,`const context = await browser.newContext({
  recordHar: { path: 'api.har', mode: 'minimal', urlFilter: '**/api/**' },
});`,[`It records every request but compresses the resulting HAR file`,`Only requests matching the api/** pattern are recorded (ignoring page assets like images/CSS), and the recorded entries omit extra timing/detail fields not needed for later replay — producing a small, replay-focused HAR`,`It blocks all non-matching requests during the test`,`It automatically replays the recorded requests on future runs`],1,`Combining a URL filter with minimal mode keeps the HAR small and focused purely on what you intend to replay later.`],[`O`,`A`,`What happens to the intercepted request here?`,`await page.route('**/api/data', async route => {
  const data = JSON.parse(badJsonString); // throws SyntaxError
  await route.fulfill({ json: data });
});`,[`Playwright automatically retries the handler with a fixed empty response`,`The request silently succeeds with a 200 status`,`Since the handler throws before resolving the route, the request effectively fails (similar to an aborted request) — it's better practice to wrap handler logic in try/catch and still resolve explicitly rather than let it throw`,`The test suite crashes and all subsequent tests are skipped`],2,`An unresolved, throwing handler leaves the request in a failed state — explicit error handling is the more predictable approach.`],[`R`,`I`,`You need to test that a live price ticker UI updates correctly when the server pushes a new price over an existing WebSocket connection, without needing a real market data feed. How?`,null,[`Poll a REST endpoint every second instead of using WebSockets`,`Use page.route() the same way you would for HTTP requests`,`This cannot be tested without a real market data feed`,`Use page.routeWebSocket() to intercept the WebSocket connection and call ws.send() with a synthetic price-update message, then assert the UI reflects the new price`],3,`routeWebSocket lets the test act as the mock server, deterministically pushing whatever message it wants.`],[`R`,`A`,`You want to verify a sensitive settings page is never served from cache after logout (a real security requirement). How would you test this at the network level?`,null,[`Mock or inspect the response headers for Cache-Control: no-store on the settings endpoint, log out, then navigate back and assert (e.g. via page.on('response')) that a fresh network request was made rather than a cached one being reused`,`Only check that logout redirects to the login page`,`Clear the browser's cache manually before every test`,`Assert the settings page returns a 401 status forever after logout`],0,`The real security-relevant behavior is whether a fresh request happens post-logout, not just whether a header is present.`],[`R`,`I`,`Analytics and ad scripts are slowing down every test run and occasionally cause flaky waits. What's the standard Playwright fix, applied broadly across the suite?`,null,[`Disable JavaScript entirely for all tests`,`Register a shared context-level route (e.g. in a fixture) that aborts requests to known analytics/ad domains for every test, so the app loads faster and deterministically without those third parties involved`,`Increase every test's timeout to compensate`,`Run the whole suite with network throttling disabled`],1,`A shared baseline mock/abort rule in a fixture keeps every test fast and free of third-party network noise without per-test repetition.`],[`R`,`A`,`Your app is supposed to retry a failed API call up to 3 times with exponential backoff before showing an error. How do you verify the actual timing/backoff behavior, not just that it eventually shows an error?`,null,[`Just assert the error message eventually appears`,`Set retries: 3 in the Playwright config`,`Use a route handler with a closure counter that fails the first two attempts and records timestamps of each intercepted request, then assert the app retried exactly 3 times AND that the delay between attempts roughly follows the expected backoff pattern`,`Mock a single 500 response and assume retries happened`],2,`Recording timestamps of each intercepted attempt lets you verify the actual retry count and backoff timing, not just the final outcome.`],[`R`,`I`,"A GraphQL endpoint can return HTTP 200 with a `data: null, errors: [...]` body on failure. How do you mock this scenario to test your app's error handling?",null,[`Mock a 500 status code since that always represents GraphQL errors`,`GraphQL errors cannot be simulated via route mocking`,`Use route.abort() to simulate a GraphQL error`,`Use page.route() on the GraphQL endpoint and route.fulfill() with a 200 status and a body containing { data: null, errors: [...] }, since a real GraphQL error doesn't necessarily change the HTTP status code`],3,`Mocking the actual GraphQL error shape (still HTTP 200) tests the app's real error-parsing logic accurately.`],[`R`,`A`,`You want to verify the app shows a helpful message when a third-party payment iframe is slow to load, without relying on the real vendor's servers being slow. How?`,null,[`Use page.route() scoped to the iframe's domain pattern and delay fulfillment (or use context-level CDP throttling if Chromium) to simulate slow loading, then assert the app's timeout/fallback messaging appears`,`Wait for the real vendor to actually be slow during a scheduled test window`,`Skip testing this scenario since third parties cannot be controlled`,`Block the iframe's domain entirely and expect the exact same message`],0,`A deliberate delay via route mocking or CDP throttling deterministically recreates a "slow third party" condition.`],[`R`,`I`,`How do you verify via network monitoring that clicking "Place Order" twice quickly doesn't send two separate order-creation requests?`,null,[`Assert the button is disabled after the first click, and nothing else`,`Collect all matching POST requests to the orders endpoint via page.on('request') during the double-click, then assert the array contains exactly one such request`,`Check the final order count in the database`,`Mock the endpoint to always return the same order ID regardless of call count`],1,`Directly counting the actual network calls made is the most reliable way to verify no duplicate request was sent.`],[`R`,`A`,`You want new team members to be able to run the full test suite on a plane with no internet, against a complex multi-endpoint third-party API. What's the best Playwright tool for this?`,null,[`Have them hand-write mocks for every endpoint from scratch`,`Ask them to use a mobile hotspot instead`,`Record a HAR file once against the real API (browser.newContext({ recordHar })) while online, commit it, then use page.routeFromHAR() to replay it deterministically offline`,`This scenario is not supported by Playwright`],2,`HAR record-once, replay-forever is exactly the workflow designed for offline, deterministic testing against a complex real API surface.`],[`R`,`I`,`Your ops team requires every outgoing API request to include an X-Correlation-Id header for tracing. How do you verify the frontend always sends it?`,null,[`Manually inspect a few requests in DevTools once`,`Trust that the backend logs will catch any missing header`,`Only check the first request made on page load`,`Use page.on('request') (or a route handler with continue()) to inspect req.headers()['x-correlation-id'] on every request during a full user flow, and assert it's present and non-empty on each one`],3,`Comprehensive coverage means checking every request in the flow, not spot-checking a single one.`],[`R`,`A`,`You want to distinguish, in your app's error handling tests, between "the server responded with a 500" and "the network request couldn't even reach the server." How do you simulate each distinctly with Playwright?`,null,[`Use route.fulfill({ status: 500 }) to simulate a real server error response, and route.abort() (which triggers request.failure()) to simulate a connection-level failure — testing both ensures the app's error UI correctly handles both cases`,`Both can only be simulated by taking the real server offline`,`Use route.continue() for one case and route.fulfill() for the other, with no other distinction needed`,`Only route.abort() is needed since it covers both scenarios`],0,`fulfill() and abort() model genuinely different failure classes that an app's error handling should distinguish between.`],[`S`,`A`,`A test mocks '**/api/**' via page.route() on the main page, but a "Print receipt" link opens a new tab (window.open) whose network calls are NOT mocked, breaking the test. What's the fix and why does it happen?`,null,[`Register the same route again inside a page.on('popup') handler manually every time`,`Register the mock on context.route() instead of page.route() — routes registered on the page only apply to that specific page, not to new pages/popups opened from it`,`Disable window.open() in the app for tests`,`Use page.waitForEvent('popup') to skip the print flow entirely`],1,`context.route() automatically covers every page in the context, including ones opened later via window.open.`],[`S`,`I`,"After adding `page.route('**', route => route.abort())` intended to block only third-party ads, the entire app fails to load in the test. What went wrong?",null,[`abort() cannot be used with any pattern besides an exact URL`,`The route was registered after page.goto() so it fired too late`,`The pattern ** matches literally everything, including the page's own HTML, scripts, and stylesheets — the pattern needs to be scoped to the actual ad/analytics domains, not left as a catch-all`,`abort() only works for image requests`],2,`An overly broad glob pattern intercepts and blocks the app's own critical resources along with the intended third-party traffic.`],[`S`,`A`,`A previously reliable routeFromHAR-based test starts intermittently asserting on stale field values after the backend team deployed changes to that endpoint. What happened and what's the fix?`,null,[`The HAR file is corrupted and needs to be deleted entirely`,`routeFromHAR always returns random data after enough runs`,`The test needs a longer timeout to account for the backend change`,`The committed HAR file still reflects the OLD API response shape/data from when it was recorded; it needs to be re-recorded (e.g. via update mode) against the new backend and re-committed`],3,`A HAR fixture is a frozen snapshot — it goes stale whenever the real API it was recorded from changes.`],[`S`,`I`,`A required X-Correlation-Id header is present on the first attempt of an API call but missing on automatic client-side retries. What class of bug does this suggest, and how would network-level testing catch it?`,null,[`The retry logic likely reconstructs the request from scratch rather than cloning the original request's headers — a test collecting all requests (including retries) via page.on('request') and asserting the header on EVERY one would catch this`,`This is expected behavior and not worth testing`,`The header is randomly dropped by the network layer`,`Retries never include any custom headers by design`],0,`Checking only the first request in a flow would miss a bug that only manifests on retries — comprehensive assertion across every request catches it.`],[`S`,`A`,`A test calls page.routeWebSocket() after the page has already navigated and the chat widget already opened its WebSocket connection. The mock never applies. Why, and what's the fix?`,null,[`routeWebSocket() only works for wss:// URLs, not ws://`,`Like page.route() with HTTP requests, WebSocket routing must be registered before the connection is initiated — register routeWebSocket() before navigating to the page that opens the socket, not after`,`The chat widget must be reloaded manually after registering the route`,`routeWebSocket() requires context-level registration, not page-level`],1,`Interception, whether HTTP or WebSocket, only affects connections established after the handler is registered.`],[`S`,`I`,"A test occasionally times out on `await page.waitForResponse('**/api/data')` even though the network tab shows the response arrived. What's the likely ordering bug?",null,[`waitForResponse cannot use glob patterns reliably`,`The response body is too large for waitForResponse to capture`,`The action that triggers the request (e.g. a click) ran BEFORE waitForResponse started listening, so the response already resolved and was missed — waitForResponse must be set up (e.g. via Promise.all) before the triggering action`,`The default timeout is too short for any network call`],2,`This is a classic race condition — the waiter must be armed before the action that triggers the awaited event.`],[`S`,`A`,`A network-throttling test using CDP passes on your machine but errors out in the CI matrix's WebKit project. What's the root cause and the correct long-term fix?`,null,[`CI machines are simply too slow to run CDP sessions`,`WebKit needs a special CDP proxy configured in CI only`,`The test needs a longer timeout specifically in the WebKit project`,`CDP-based throttling is Chromium-specific; the test should explicitly skip (or use an alternative approach) for non-Chromium projects rather than assuming CDP is universally available`],3,`CDP simply doesn't exist as a protocol in WebKit, so the test needs to be conditionally skipped or restructured for non-Chromium projects.`],[`S`,`I`,`A route mock for a JSON API returns the correct body but omits Content-Type: application/json. The app's fetch wrapper checks the content-type before parsing and silently treats the response as an error. What's the fix?`,null,[`Add contentType: 'application/json' to the route.fulfill() call (or use the json: shorthand which sets it automatically) — the mock's headers must match what a real server response would include, not just the body`,`Change the app code to never check Content-Type`,`Switch the app to always assume JSON regardless of headers`,`Increase the response body size to trigger correct parsing`],0,`A realistic mock needs to include the headers a real server would send, not just a correct body.`],[`S`,`A`,`A shared fixture and an individual test both register a \`page.route('**/api/**', ...)\` handler with different behavior, and a developer is confused why logs show the request being "handled" twice. What's actually happening?`,null,[`Both handlers always fully execute in parallel on every request`,`Both handlers exist in the LIFO stack — the most recently registered one runs first, and if it calls route.fallback()/continue() to pass through rather than resolving, the next (earlier-registered) handler in the stack also runs and logs, giving the appearance of double-handling`,`This indicates a memory leak in the browser context`,`page.route() automatically deduplicates identical patterns, so this should be impossible`],1,`A handler that calls continue()/fallback() explicitly passes control to the next handler in the LIFO stack rather than fully resolving the request itself.`],[`S`,`I`,`routeFromHAR successfully mocks GET requests, but a seemingly identical POST endpoint still hits the real network. What's a likely reason specific to how HAR matching works?`,null,[`routeFromHAR never supports POST requests at all, by design`,`POST requests always bypass all route() mechanisms`,`HAR matching typically requires the request body (not just the URL) to match what was recorded for non-GET requests, so if the POST body differs even slightly from what was captured, the entry won't be considered a match and falls through to the real network`,`The HAR file has a maximum of one entry per URL`],2,`Body-sensitive matching for non-GET methods means even a minor difference from the recorded payload causes a miss.`],[`D`,`I`,`page.routeWebSocket() mock doesn't seem to intercept anything, and the real WebSocket server receives all traffic. What should you check first?`,null,[`Whether the WebSocket URL uses wss:// instead of ws://`,`Whether the page has more than one WebSocket connection open`,`Whether the context was created with serviceWorkers: 'block'`,`Whether routeWebSocket() was registered before the page navigated/connected the socket — like HTTP routing, it must be set up before the connection is initiated`],3,`Interception for WebSockets, like HTTP requests, only affects connections that are established after the handler is registered.`],[`D`,`A`,`Network throttling set up via a CDP session stops applying after the test calls page.reload(). Why?`,null,[`A reload can involve operations that invalidate assumptions tied to the specific page/session state the CDP client was attached to — the safest approach is to re-verify/re-apply the emulateNetworkConditions call after any navigation-level operation`,`reload() always creates an entirely new, unrelated page object`,`CDP sessions expire automatically after any navigation by design, with no workaround`,`Throttling is only ever applied to the very first request on a page`],0,`Treating CDP-applied conditions as needing reapplication after major navigation events avoids assuming they persist unconditionally.`],[`D`,`I`,`A test throws "Request is already handled" inside a route handler that calls both route.fetch() + route.fulfill() AND, due to a bug, also route.continue() afterward. What's the fix?`,null,[`Call route.fulfill() twice instead of once`,`Only one of fulfill/continue/abort should be called per route resolution — remove the redundant route.continue() call after fulfill() has already resolved the route`,`Wrap the extra call in a try/catch and ignore the error`,`Switch from route.fetch() to request.get() to avoid the conflict`],1,`A route can only be resolved once — calling a second resolution method after the first throws exactly this error.`],[`D`,`A`,`A URL that's definitely present in the recorded HAR file still returns 404 when replayed via routeFromHAR(). What's a subtle cause worth checking?`,null,[`HAR files can only store up to 100 entries`,`404 always means the HAR file itself is corrupted`,`routeFromHAR only intercepts requests matching the url filter option (if one was passed) — if the filter is narrower than expected, or if query-string ordering/values differ from what was recorded, the entry won't be matched even though it's technically present in the file`,`routeFromHAR requires the HAR file to be re-encoded as JSON first`],2,`Both a mismatched url filter and subtle query-string differences from the recorded request can silently prevent a technically-present entry from matching.`],[`D`,`I`,"After adding `page.route(/analytics|tracking/, route => route.abort())`, an unrelated first-party feature (a script hosted at /js/user-tracking-preferences.js) stops working in tests. Why?",null,[`Regex patterns cannot be used with page.route() at all`,`The abort() call has a bug that blocks all first-party scripts`,`page.route() applies regex patterns to the whole page, not individual URLs`,`The regex /tracking/ matches that legitimate script's URL too, since it happens to contain the substring 'tracking' — the pattern is too broad and needs to be scoped more precisely to actual third-party analytics domains/paths`],3,`An unanchored substring match in a regex pattern can accidentally catch unrelated URLs that happen to contain the same text.`]],visual:[[`O`,`B`,"What does `page.screenshot({ path: 'debug.png' })` do?",null,[`Opens a file picker to choose where to save`,`Saves a PNG of the current viewport to the specified path`,`Takes a screenshot and attaches it to the test report automatically`,`Records a short video clip named debug.png`],1,`page.screenshot() captures the viewport (or full page with fullPage: true) as a PNG.`],[`O`,`I`,"What does `expect(page).toHaveScreenshot('home.png')` do on the very first run?",null,[`Fails immediately — the baseline does not exist yet`,`Creates a baseline PNG file in a __snapshots__ directory and passes the test`,`Throws a "golden file missing" error and stops the suite`,`Downloads home.png from the Playwright CDN`],1,`On first run Playwright creates the golden baseline — subsequent runs compare against it.`],[`O`,`I`,"What does `expect(page).toHaveScreenshot('home.png')` do on subsequent runs?",null,[`Overwrites the baseline with the current screenshot`,`Compares the current screenshot pixel-by-pixel against the stored baseline and fails if they differ beyond the threshold`,`Records a video of the comparison`,`Skips the comparison if the baseline is older than 24 hours`],1,`Visual regression: the test fails if the rendered page differs from the committed baseline.`],[`O`,`B`,"What does `video: 'on-first-retry'` in the config `use` block do?",null,[`Records a video only for the first test in the suite`,`Records a video when a test is retried — capturing evidence of failures without recording every passing test`,`Limits video recording to a maximum of one retry`,`Records a video of the first browser launch only`],1,`on-first-retry is the recommended setting — low disk usage while still capturing failing test evidence.`],[`O`,`I`,`What does a Playwright trace file contain?`,null,[`Only a video recording of the test`,`DOM snapshots at every action, network request waterfall, console messages, and timestamped screenshots`,`Only the test source code and assertion results`,`A list of all locators used in the test`],1,`Traces are the richest debugging artifact — they combine DOM, network, console, and screenshots in one zip.`],[`O`,`B`,`How do you open a Playwright trace file after a test run?`,null,[`Double-click the .zip file in your file explorer`,"Run `npx playwright show-trace trace.zip` or drag it onto trace.playwright.dev",`Open it with npx playwright open --trace`,`Import it into the Playwright config as a HAR file`],1,`show-trace is the CLI command; trace.playwright.dev is the online viewer.`],[`O`,`I`,"What is the difference between `trace: 'on-first-retry'` and `trace: 'retain-on-failure'`?",null,[`They are identical — both only save traces for failed tests`,`on-first-retry records only when a retry occurs; retain-on-failure records always but deletes the zip if the test passes`,`retain-on-failure is deprecated — use on-first-retry`,`on-first-retry requires retries: 1 or higher to ever produce a trace`],1,`retain-on-failure records every test but discards passing ones — useful when you want a trace regardless of whether a retry happened.`],[`O`,`B`,"What does `page.screenshot({ fullPage: true })` do differently from the default?",null,[`Captures the page at 2× device pixel ratio`,`Scrolls and stitches the entire page including content below the fold`,`Captures all open browser tabs in one image`,`Removes the browser chrome (address bar, tabs) from the screenshot`],1,`Without fullPage, only the visible viewport is captured; fullPage: true stitches the entire scrollable page.`],[`O`,`I`,"What does the `--update-snapshots` CLI flag do?",null,[`Clears all test results and re-runs from scratch`,`Regenerates and overwrites existing visual baseline files with current screenshots`,`Updates the Playwright package to the latest version`,`Resets the browser profile between tests`],1,`--update-snapshots regenerates baselines — run it after intentional UI changes, then commit the new files.`],[`O`,`I`,"What does `locator.screenshot()` capture compared to `page.screenshot()`?",null,[`It captures the same viewport but with the locator highlighted`,`It captures only the bounding box of the matched element, not the full page`,`It is identical to page.screenshot() but faster`,`It requires the element to be in the viewport — it does not scroll`],1,`locator.screenshot() crops to the element's bounding box — ideal for component-level visual regression.`],[`O`,`A`,"What does `context.tracing.start({ screenshots: true, snapshots: true })` enable?",null,[`Automatic screenshots on every assertion failure`,`Records screenshots at each action step and captures DOM snapshots for the hover-to-inspect feature in the Trace Viewer`,`Starts continuous video recording for the context`,`Enables network HAR recording alongside the trace`],1,`screenshots adds action thumbnails; snapshots enables the Trace Viewer's interactive DOM inspector.`],[`O`,`I`,"Where does Playwright store `toHaveScreenshot()` baseline images?",null,[`In the test-results directory, deleted after each run`,`In a __snapshots__ subdirectory next to the spec file — should be committed to version control`,`In ~/.playwright/snapshots on the local machine`,`Uploaded to the Playwright cloud automatically`],1,`Baselines live in __snapshots__ beside the spec — commit them so CI can compare against the same reference.`],[`F`,`B`,"Complete: `await page.______({ path: 'debug.png' })` to save a screenshot",null,[`screenshot`,`capture`,`snap`,`photo`],0,`page.screenshot() is the method for saving a PNG of the current viewport.`],[`F`,`I`,"Complete: `await expect(page).______Screenshot('home.png')` for visual regression",null,[`toHave`,`match`,`compare`,`baseline`],0,`toHaveScreenshot() is the visual regression assertion — creates or compares the baseline.`],[`F`,`B`,"Complete: `use: { ______: 'on-first-retry' }` to capture video on retry",null,[`video`,`record`,`capture`,`media`],0,`video is the config key for browser session recording.`],[`F`,`I`,"Complete: `use: { ______: 'retain-on-failure' }` to capture traces on failure",null,[`trace`,`tracing`,`debug`,`record`],0,`trace is the config key for Playwright's trace recording mode.`],[`F`,`I`,"Complete: `await context.tracing.______({ path: 'trace.zip' })` to save the trace",null,[`stop`,`save`,`end`,`export`],0,`tracing.stop() finalises and writes the trace to the specified path.`],[`F`,`I`,"Complete: `npx playwright ______-trace trace.zip` to open the viewer",null,[`show`,`view`,`open`,`display`],0,`show-trace is the Playwright CLI command for opening the Trace Viewer.`],[`F`,`A`,"Complete: `await video.______As('recording.webm')` to copy video to a custom path",null,[`save`,`copy`,`export`,`move`],0,`video.saveAs() copies the recorded WebM to the specified destination path.`],[`E`,`B`,"What is wrong?\n```ts\nawait page.screenshot({ fullPage: 'true' });\n```",null,[`fullPage is not a valid option — use clip instead`,`fullPage must be a boolean true, not the string 'true'`,`screenshot() cannot be awaited`,`'true' is treated as falsy in Playwright`],1,`fullPage is a boolean option — passing a string causes TypeScript to reject it and the behaviour is undefined at runtime.`],[`E`,`I`,"What is wrong?\n```ts\n// Inside the test body:\nconst path = await page.video()?.path();\nconsole.log(path); // undefined\n```",null,[`page.video() is only available in headed mode`,`video.path() returns undefined until the test ends and the video is finalised — call it in afterEach or after the test`,`path() must be called without await`,`page.video() requires video: 'on' in config`],1,`The video file is not written until the browser context closes at the end of the test — access path in afterEach or a fixture teardown.`],[`E`,`I`,"What is wrong?\n```ts\nawait expect(page).toHaveScreenshot('home.png', { maxDiffPixels: 0 });\n// Fails on every run despite no visible change\n```",null,[`maxDiffPixels: 0 is invalid — the minimum is 1`,`maxDiffPixels: 0 means zero pixels can differ — sub-pixel rendering variations between runs will always fail it`,`maxDiffPixels should be a percentage not a count`,`toHaveScreenshot does not accept an options object`],1,`maxDiffPixels: 0 is too strict for real-world rendering — use a small tolerance like 50-200 pixels to absorb antialiasing differences.`],[`E`,`A`,`What is wrong?
\`\`\`ts
await context.tracing.start({ screenshots: true, snapshots: true });
await page.goto('/');
await page.getByRole('button').click();
// test ends — no tracing.stop() call
\`\`\``,null,[`tracing stops automatically when the test ends`,`tracing.stop() must be called to write the trace to disk — without it the zip is never created`,`start() already writes the trace incrementally so stop() is optional`,`The trace is saved to the default path automatically`],1,`tracing.stop({ path }) is required to flush and write the trace — always call it, preferably in a finally block.`],[`E`,`I`,"What is wrong?\n```ts\n// CI baseline comparison fails — different font rendering on Linux vs macOS\nawait expect(page).toHaveScreenshot('home.png');\n```",null,[`toHaveScreenshot is not supported in CI`,`Baselines are OS-specific — generate the baseline on the same OS/browser as CI, or use threshold/maxDiffPixels to absorb rendering differences`,`CI requires the --ci flag with toHaveScreenshot`,`Fonts are not a factor in Playwright screenshots`],1,`Cross-OS rendering differences are the most common visual regression headache — generate baselines in the same environment as CI, or use Docker for consistency.`],[`E`,`B`,"What is wrong?\n```ts\n// Baselines are stored in __snapshots__ but not committed to Git\n// CI always creates a new baseline and the test always passes\n```",null,[`__snapshots__ should never be committed — they are auto-generated artifacts`,`Without committed baselines, CI has nothing to compare against — it creates a new baseline each run and always passes, defeating regression detection`,`Baselines should be stored in test-results not __snapshots__`,`CI cannot write to __snapshots__ — use the --snapshot-dir flag`],1,`Baseline files must be committed to VCS — otherwise CI compares against itself and regressions are never caught.`],[`D`,`B`,"`toHaveScreenshot()` fails with hundreds of pixel differences after a valid CSS change. What should you do?",null,[`Increase maxDiffPixels to a very high number`,"Run `npx playwright test --update-snapshots` to regenerate the baselines, review the diff, then commit the new files",`Delete the __snapshots__ directory and re-run`,`Switch from toHaveScreenshot to a functional assertion`],1,`After intentional UI changes, regenerate baselines with --update-snapshots, visually review the diffs, then commit.`],[`D`,`I`,`The trace zip is 0 bytes after a test run. What is the most likely cause?`,null,[`Traces require headed mode`,`tracing.stop() was called but tracing.start() was never called`,`The trace file path must end in .zip`,`Tracing is not compatible with fullyParallel: true`],1,`If start() is never called, stop() writes an empty or invalid file — ensure start() runs before any actions you want to capture.`],[`D`,`B`,`Visual regression screenshots differ between local and CI even though the code hasn't changed. What is the root cause?`,null,[`The --update-snapshots flag was not run in CI`,`Different OS, browser engine version, font rendering, or DPI between environments produce slightly different pixels`,`toHaveScreenshot is non-deterministic by design`,`CI runs at a different viewport size`],1,`Sub-pixel rendering differs across OS/GPU combinations — pin the environment (use Docker), or generate baselines on CI itself.`],[`D`,`I`,"`page.video()` returns `null` even though `video: 'on'` is set in the config. What should you check?",null,[`video: 'on' only works in headed mode`,`The test may be using a different browser context than expected — ensure the page was created from the configured context`,`video requires a writeable test-results directory`,`null is the default before the video starts recording`],1,`Video is attached to the browser context — a page created from a manually launched browser won't have video unless explicitly configured.`],[`D`,`I`,"Trace Viewer shows actions but no network requests. What option was missing from `tracing.start()`?",null,[`network: true must be passed to tracing.start()`,`Network is always included — something else is wrong`,`The network tab requires the --trace-network CLI flag`,`Traces record network only for XHR requests, not fetch()`],0,`Network recording is automatic in Playwright traces — if it's missing, check that tracing.start() ran before page.goto(), since requests before start() are not captured.`],[`D`,`A`,`Visual regression tests produce different baselines per browser project (Chromium vs WebKit). How are the baselines organised?`,null,[`All projects share a single baseline — differences are expected and ignored`,`Playwright automatically namespaces baselines per project — e.g. __snapshots__/home-chromium.png vs home-webkit.png`,`You must run --update-snapshots once per project separately`,`Baselines are identical across projects — rendering differences are handled by threshold`],1,`Playwright includes the project name in the snapshot filename, so each browser project has its own independent baseline.`],[`C`,`B`,"Complete to take a full-page screenshot.\n```ts\nawait page.screenshot({ path: 'full.png', ______: true });\n```",null,[`fullPage`,`full`,`scrollable`,`entire`],0,`fullPage: true stitches the entire scrollable page including below-fold content.`],[`C`,`I`,"Complete visual regression with pixel tolerance.\n```ts\nawait expect(page).toHaveScreenshot('home.png', { ______: 100 });\n```",null,[`maxDiffPixels`,`threshold`,`tolerance`,`pixelDiff`],0,`maxDiffPixels allows up to N pixels to differ before failing the comparison.`],[`C`,`I`,"Complete to start a manual trace.\n```ts\nawait context.tracing.______({ screenshots: true, snapshots: true });\n```",null,[`start`,`begin`,`record`,`init`],0,`tracing.start() initialises trace recording with the specified options.`],[`C`,`I`,"Complete to save the trace to disk.\n```ts\nawait context.tracing.______({ path: 'trace.zip' });\n```",null,[`stop`,`save`,`end`,`write`],0,`tracing.stop() finalises and writes the trace zip to the path.`],[`C`,`A`,"Complete to capture a single element screenshot.\n```ts\nconst chart = page.getByTestId('chart');\nawait chart.______({ path: 'chart.png' });\n```",null,[`screenshot`,`capture`,`snap`,`toHaveScreenshot`],0,`locator.screenshot() crops the output to the element's bounding box.`],[`L`,`B`,`What is the main difference between a screenshot and a Playwright trace?`,null,[`A screenshot is larger than a trace`,`A screenshot captures a single visual moment; a trace captures the full timeline including DOM snapshots, network, console, and action sequence`,`Traces are only available in headed mode; screenshots work in headless`,`Screenshots are for debugging; traces are for visual regression`],1,`Traces are the richest artifact — they let you step through the test's entire history in the Trace Viewer.`],[`L`,`I`,"When should you use `toHaveScreenshot()` instead of functional assertions like `toHaveText()`?",null,[`Always — screenshots are more reliable than text assertions`,`For visual layout regressions (CSS, spacing, colours) that functional assertions cannot detect — text assertions handle content correctness`,`When the element is not locatable by role or text`,`Only for full-page checks, not individual components`],1,`Visual regression catches layout regressions; functional assertions verify content and state — use both together.`],[`L`,`I`,"What is the advantage of `retain-on-failure` over `on-first-retry` for traces?",null,[`retain-on-failure is smaller in file size`,`retain-on-failure captures a trace even for tests that fail on the first attempt without a retry — on-first-retry requires a retry to record`,`on-first-retry has better DOM snapshots`,`They are identical — the names are just aliases`],1,`With retries: 0, on-first-retry never fires — retain-on-failure always captures and discards only passing tests.`],[`L`,`B`,`Why must baseline PNG files be committed to version control?`,null,[`Playwright refuses to run without committed baselines`,`Without committed baselines CI compares against a freshly created baseline each run — regressions are never detected`,`Baselines are binary and should not be in VCS`,`The --update-snapshots flag only works when baselines are in VCS`],1,`Baselines must be in VCS so CI checks the current render against the approved reference, detecting any unexpected changes.`],[`L`,`A`,`A component has a live timestamp that changes every second. How do you prevent it from causing visual regression false failures?`,null,[`Freeze JavaScript time with page.clock.setFixedTime() before the screenshot`,`Increase maxDiffPixels to cover the entire timestamp area`,`Use toContainText() instead of toHaveScreenshot()`,`All three approaches work — mask is the cleanest`],3,`mask: [locator] (blackouts the element), page.clock.setFixedTime() (freezes it), or maxDiffPixels (tolerates it) all work — mask is cleanest for stable visual tests.`],[`R`,`B`,`You want to verify a product card looks correct after a CSS change. What assertion do you use?`,null,[`await expect(card).toHaveCSS('color', '…') for each property`,`await expect(page.getByTestId('product-card')).toHaveScreenshot('card.png')`,`await page.screenshot({ clip: cardBounds })`,`Use a visual diff tool outside of Playwright`],1,`locator.toHaveScreenshot() captures just the card and compares against the committed baseline.`],[`R`,`I`,`You want traces only for failed tests to keep CI artifact storage manageable. Which config option do you use?`,null,[`trace: 'on-first-retry'`,`trace: 'retain-on-failure'`,`trace: 'on'`,`trace: 'off' and add manual tracing to each test`],1,`retain-on-failure records all tests but deletes the trace zip if the test passes — minimises storage.`],[`R`,`I`,`A test has a date picker that shows today's date, breaking visual regression daily. How do you handle it?`,null,[`Run visual tests only on the day the baseline was created`,`Pass mask: [page.getByTestId('datepicker')] to toHaveScreenshot() to exclude it from comparison`,`Use threshold: 1 to ignore all differences`,`Regenerate baselines every day in CI`],1,`mask blackouts the dynamic element so its pixel values don't contribute to the diff.`],[`R`,`A`,`After a complex checkout failure you want to see every DOM state and network call step by step. What is the workflow?`,null,[`Add console.log() throughout the test and read the output`,`Open the trace zip from test-results/ in the Trace Viewer — it shows DOM snapshots, network waterfall, and console for each action`,`Rerun the test in headed mode and watch manually`,`Use page.on('request') to log network calls`],1,`The Trace Viewer is purpose-built for this: click any action in the timeline and see the exact DOM state and network calls at that moment.`],[`R`,`I`,`You want to verify the app's dark mode renders correctly. How do you structure the visual regression test?`,null,[`Set colorScheme: 'dark' in the test and take a screenshot`,`Use page.emulateMedia({ colorScheme: 'dark' }) or set colorScheme in the project config, then run toHaveScreenshot with a dark-specific baseline name`,`Dark mode cannot be tested with visual regression`,`Use CSS assertions for every dark mode colour`],1,`emulateMedia({ colorScheme: 'dark' }) or a project-level colorScheme forces the dark theme; name the snapshot to distinguish it from the light baseline.`],[`S`,`I`,`A new team member runs the test suite and all visual regression tests fail immediately. The baseline files are in the repo. What is the likely cause?`,null,[`The baselines are corrupt`,`The member is running on a different OS/browser version producing slightly different rendering — they need to run tests in the same environment or use Docker`,`The --update-snapshots flag must be run once on first clone`,`Playwright requires re-creating baselines per machine`],1,`Cross-environment rendering differences are the #1 cause of immediate visual regression failures — align the environment or add threshold tolerance.`],[`S`,`A`,`You need to trace only the payment flow in a long test suite without tracing every test. How?`,null,[`Set trace: 'on' globally — all tests are traced`,`Set trace: 'off' globally and add manual context.tracing.start/stop only around the payment test steps`,`Use --grep to run only the payment test with tracing enabled`,`Tracing cannot be limited to specific tests — it's all-or-nothing`],1,`Manual tracing gives surgical control — start before the first payment action and stop (in a finally) after the last one.`],[`S`,`I`,`CI visual regression tests pass locally but fail with "X pixels different" in CI. The difference is exactly in text rendering. What is the most practical fix?`,null,[`Increase maxDiffPixels to a very high number to silence all failures`,`Generate baselines in CI using --update-snapshots on the first CI run, commit them, and use those CI-generated files as the reference`,`Switch to functional text assertions for all visual tests`,`Disable anti-aliasing in the browser config`],1,`Generating baselines in CI on a dedicated "update" run ensures the reference matches the exact rendering environment used for comparison.`],[`S`,`A`,`After a redesign, 40 visual regression tests fail. You want to approve all of them efficiently. What is the process?`,null,[`Delete all __snapshots__ directories and re-run`,"Run `npx playwright test --update-snapshots`, review the HTML report's diff view to confirm each change is intentional, then commit the updated files",`Update each baseline manually by copying test-results images to __snapshots__`,`There is no bulk update — each test must be updated individually`],1,`--update-snapshots regenerates all baselines in one command; the HTML report's diff view lets you review every change before committing.`],[`O`,`B`,"What is stored in the `__snapshots__` directory?",null,[`Playwright trace files`,`Baseline PNG images used for visual regression comparison — one file per toHaveScreenshot() name, per browser/OS/viewport combination`,`HTML reports for failed tests`,`Video recordings of test runs`],1,`__snapshots__ holds the reference images — committed to source control and compared against actual screenshots on every test run.`],[`O`,`I`,"What does `toHaveScreenshot({ maxDiffPixels: 50 })` do?",null,[`The test fails if more than 50% of pixels differ`,`The test passes if the actual screenshot differs by at most 50 pixels from the baseline`,`The test captures 50 screenshots and picks the best match`,`50 is the screenshot quality setting`],1,`maxDiffPixels sets an absolute pixel tolerance — useful for minor rendering differences like anti-aliasing.`],[`O`,`B`,"What does `page.screenshot({ fullPage: true })` capture?",null,[`Only the viewport — same as the default`,`The entire scrollable content of the page — taller than the viewport if the page is long`,`A screenshot of every tab`,`A PDF rendition of the full page`],1,`fullPage: true scrolls and stitches the full document — ideal for capturing long pages in one image.`],[`O`,`I`,"What does `expect(locator).toHaveScreenshot()` do differently than `expect(page).toHaveScreenshot()`?",null,[`They are identical — locator and page screenshots use the same area`,`locator.toHaveScreenshot() clips the screenshot to the locator's bounding box — only that element is captured, not the full viewport`,`locator variant captures in higher resolution`,`page variant requires fullPage: true`],1,`Element-scoped screenshots reduce noise — a change elsewhere on the page won't fail the test for that component.`],[`O`,`A`,"What does `page.screenshot({ clip: { x: 0, y: 0, width: 300, height: 200 } })` do?",null,[`Resizes the viewport to 300×200`,`Captures only the 300×200 pixel region from the top-left corner of the viewport`,`Crops the existing full-page screenshot`,`Captures the element at coordinates (300, 200)`],1,`clip: defines a precise viewport sub-region to capture — useful when only a specific area is relevant.`],[`O`,`I`,"What file format does `toHaveScreenshot()` use by default?",null,[`JPEG`,`PNG — lossless compression, correct for pixel-perfect comparison`,`WebP`,`SVG`],1,`PNG is lossless — JPEG compression would introduce artifacts that break exact pixel comparison.`],[`O`,`B`,"What does `--update-snapshots` do when passed to `npx playwright test`?",null,[`Deletes all existing snapshots and leaves them empty`,`Regenerates all baseline snapshot files with the current actual screenshots — new baselines replace old ones`,`Updates the Playwright version`,`Updates only failing snapshots`],1,`--update-snapshots is the "approve changes" command — run it after intentional UI changes, then commit the new baselines.`],[`O`,`I`,"What does `toHaveScreenshot({ threshold: 0.1 })` do?",null,[`Allows up to 10 pixels difference`,`Allows a perceptual difference ratio of up to 10% per pixel (using SSIM/color-distance) — combines with maxDiffPixels for full control`,`Sets JPEG quality to 10%`,`Waits 100ms before comparing`],1,`threshold is per-pixel color distance tolerance (0–1) — distinct from maxDiffPixels which counts total differing pixels.`],[`O`,`A`,'What does `page.screenshot({ animations: "disabled" })` do?',null,[`Pauses JavaScript but allows CSS animations`,`Freezes CSS animations and transitions before capturing — prevents animated elements from appearing mid-transition in screenshots`,`Disables all event handlers`,`Skips elements with animation attributes`],1,`animations: "disabled" ensures deterministic screenshots — animated carousels, spinners, and transitions appear in their rest state.`],[`O`,`I`,"What is the purpose of `expect.soft(locator).toHaveScreenshot()` in visual testing?",null,[`It skips flaky screenshot comparisons`,`The screenshot assertion is non-fatal — the test continues even if it fails, and all soft assertion failures are reported at the end`,`It takes a lower-quality screenshot`,`It compares only soft (blurry) areas of the screenshot`],1,`Soft assertions let you collect all visual differences in one run rather than stopping at the first failure.`],[`O`,`B`,"What does `context.tracing.start({ screenshots: true, snapshots: true })` do?",null,[`Starts recording a video`,`Begins a Playwright trace recording that captures screenshots at each step and DOM/network snapshots — viewable in the Playwright Trace Viewer`,`Starts the screenshot comparison baseline`,`Enables continuous screenshot mode`],1,`Tracing with screenshots:true and snapshots:true creates a full timeline — every action has a before/after screenshot in the trace.`],[`O`,`A`,'What does `await context.tracing.stop({ path: "trace.zip" })` do?',null,[`Saves a video recording as trace.zip`,"Finalises and writes the trace to trace.zip — the archive contains screenshots, network logs, and DOM snapshots viewable with `playwright show-trace trace.zip`",`Stops the browser context and exports state`,`Stops the test runner`],1,`tracing.stop() with a path saves the complete trace archive — share it with teammates or attach it to CI artifacts.`],[`F`,`I`,'Complete to assert an element\'s screenshot matches a baseline.\n```ts\nconst card = page.locator(".product-card");\nawait expect(card).______("card.png");\n```',null,[`toHaveScreenshot`,`toMatchSnapshot`,`toEqualScreenshot`,`toBeVisually`],0,`toHaveScreenshot() compares against a stored PNG baseline, creating it on first run.`],[`F`,`B`,'Complete to set pixel tolerance on a screenshot assertion.\n```ts\nawait expect(page).toHaveScreenshot("hero.png", {\n  ______: 20,\n});\n```',null,[`maxDiffPixels`,`tolerance`,`pixelThreshold`,`diffPixels`],0,`maxDiffPixels: N allows up to N pixels to differ before the assertion fails.`],[`F`,`A`,`Complete to start tracing.
\`\`\`ts
await context.tracing.start({
  screenshots: true,
  ______: true,
});
\`\`\``,null,[`snapshots`,`dom`,`trace`,`network`],0,`snapshots: true captures DOM and network state at each trace step — essential for the Trace Viewer timeline.`],[`F`,`I`,'Complete to disable animations before a screenshot.\n```ts\nawait page.screenshot({ path: "stable.png", ______: "disabled" });\n```',null,[`animations`,`transitions`,`motion`,`css`],0,`animations: "disabled" freezes CSS animations and transitions for a stable capture.`],[`F`,`B`,'Complete to save the trace to a file.\n```ts\nawait context.tracing.stop({ ______: "trace.zip" });\n```',null,[`path`,`file`,`output`,`name`],0,"path: is where the trace archive is written — pass it to `playwright show-trace` to inspect."],[`F`,`I`,`Complete to clip the screenshot to a region.
\`\`\`ts
await page.screenshot({
  path: "region.png",
  ______: { x: 100, y: 50, width: 400, height: 300 },
});
\`\`\``,null,[`clip`,`region`,`crop`,`bounds`],0,`clip: { x, y, width, height } captures only that viewport sub-region.`],[`F`,`A`,'Complete to mask a dynamic element before comparison.\n```ts\nawait expect(page).toHaveScreenshot("page.png", {\n  ______: [page.locator(".timestamp")],\n});\n```',null,[`mask`,`exclude`,`ignore`,`hide`],0,`mask: [locators] overlays a pink rectangle over dynamic content before comparison — timestamps, avatars, and ads are common masks.`],[`F`,`I`,'Complete to set a perceptual color-distance threshold.\n```ts\nawait expect(page).toHaveScreenshot("logo.png", {\n  ______: 0.2,\n});\n```',null,[`threshold`,`tolerance`,`maxDiff`,`sensitivity`],0,`threshold: 0–1 sets the per-pixel color-distance tolerance using a perceptual metric.`],[`E`,`B`,'What is wrong?\n```ts\n// First run — no baseline exists\nawait expect(page).toHaveScreenshot("home.png");\n// This is run in CI without --update-snapshots first\n```',null,[`toHaveScreenshot always passes on first run`,`If no baseline exists and --update-snapshots was not run to generate one, the test fails with "screenshot is missing". Generate baselines locally first, commit them, then run in CI.`,`CI auto-generates baselines`,`toHaveScreenshot requires a baseline path option`],1,`Baselines must be generated and committed before CI can compare — run locally, commit, then CI compares.`],[`E`,`I`,`What is wrong?
\`\`\`ts
await context.tracing.start({ screenshots: true });
await page.goto("/");
await page.click("button");
// no tracing.stop() — test ends
\`\`\``,null,[`tracing auto-saves on test end`,`Without tracing.stop(), the trace is never written to disk — always call stop() in an afterEach or finally block to ensure the trace is saved even on test failure.`,`screenshots: true makes tracing.stop() optional`,`The trace is saved when the context closes`],1,`Always stop tracing in a finally or afterEach — test failures skip code after the failing assertion, so stop() must be in cleanup.`],[`E`,`B`,'What is wrong?\n```ts\nawait expect(page).toHaveScreenshot("snapshot");\n// Extension omitted\n```',null,[`Extensions are optional — Playwright adds .png automatically`,`The name must include the .png extension explicitly: "snapshot.png". Without it, Playwright throws a "name must include extension" error.`,`.jpg is the required extension`,`Snapshot names cannot contain dots`],1,`Always include the .png extension in toHaveScreenshot names.`],[`E`,`I`,`What is wrong?
\`\`\`ts
// playwright.config.ts
use: {
  screenshot: "on",
}
\`\`\``,null,[`"on" is the correct value for always capturing screenshots`,`The screenshot option is "only-on-failure" for failure screenshots or "on" for all. This is valid config — however this only captures test result screenshots, NOT visual regression baselines. toHaveScreenshot() is separate.`,`Screenshots cannot be configured globally`,`The option belongs under reporter, not use`],1,`use.screenshot is for CI artifact screenshots; toHaveScreenshot() is the separate visual regression API — they serve different purposes.`],[`E`,`A`,'What is wrong?\n```ts\nawait expect(page).toHaveScreenshot("header.png");\n// Baseline was captured on a 1280×800 viewport\n// Test now runs at 1920×1080\n```',null,[`Viewport size does not affect screenshots`,`The dimensions differ — the comparison will fail because the image sizes don't match. Baselines must be regenerated after viewport changes, or set a fixed viewport in the test config.`,`toHaveScreenshot scales images before comparing`,`The baseline PNG is resized automatically`],1,`Screenshot dimensions are part of the baseline — any viewport change requires new baselines.`],[`D`,`B`,'`toHaveScreenshot()` fails with "2 pixels are different". The diff shows a 1-pixel border rendering difference. How do you fix it without updating the baseline?',null,[`Disable border CSS for tests`,`Add maxDiffPixels: 5 or threshold: 0.1 to allow minor rendering differences without failing`,`Re-run the test — it will pass on retry`,`Switch from PNG to JPEG`],1,`Small rendering differences (sub-pixel, anti-aliasing) are handled with tolerance options — not by updating the baseline.`],[`D`,`I`,`Visual tests fail in CI but pass locally. The diff shows text looks slightly different. What is the most likely cause?`,null,[`The CI network is blocking fonts`,`Font rendering differs between OSes — CI runs Linux while local runs macOS/Windows. Use a Docker image that matches CI's OS, or set a threshold to tolerate font rendering differences.`,`The test viewport is different in CI`,`CI doesn't support screenshot assertions`],1,`Cross-OS font rendering is the most common cause of visual test environment mismatch — align environments using Docker.`],[`D`,`B`,`The trace viewer shows a blank "before" screenshot for a step. What is the likely cause?`,null,[`The browser crashed before the step`,`tracing.start() was called without screenshots: true — enable it to see before/after screenshots in the trace viewer`,`The trace file is corrupt`,`The page was not loaded before tracing started`],1,`screenshots: true is required to see visual snapshots in the trace — without it, only events and DOM snapshots are recorded.`],[`D`,`I`,"`page.screenshot()` captures an element partially off-screen. How do you ensure the element is fully visible?",null,[`Use fullPage: true`,`Call await locator.scrollIntoViewIfNeeded() before taking the screenshot — or use locator.screenshot() which automatically scrolls the element into view`,`Increase the viewport height`,`Use clip: to capture a larger area`],1,`locator.screenshot() handles scroll-into-view automatically — use it for element-scoped captures instead of page.screenshot() with manual scrolling.`],[`D`,`A`,`A visual test is flaky — it fails 1 in 5 runs due to a loading spinner still visible at assertion time. How do you fix it?`,null,[`Add retries: 3 to the test`,`Wait for the spinner to disappear before taking the screenshot: await page.locator(".spinner").waitFor({ state: "hidden" }); then take the screenshot`,`Use animations: "disabled" to freeze the spinner`,`Add a 2 second sleep before the screenshot`],1,`Explicit waitFor on the spinner ensures it is gone before capture — deterministic and fast.`],[`C`,`B`,'Complete to capture a screenshot of a single element.\n```ts\nconst header = page.locator("header");\nawait header.______({ path: "header.png" });\n```',null,[`screenshot`,`capture`,`save`,`export`],0,`locator.screenshot() clips to the element's bounding box and scrolls it into view automatically.`],[`C`,`I`,'Complete to mask dynamic content in a visual assertion.\n```ts\nawait expect(page).toHaveScreenshot("profile.png", {\n  mask: [______],\n});\n```',null,[`page.locator(".avatar")`,`".avatar"`,`{ selector: ".avatar" }`,`page.getByClass("avatar")`],0,`mask: accepts an array of Locator objects — not CSS strings.`],[`C`,`A`,`Complete to stop tracing on test failure only.
\`\`\`ts
test.afterEach(async ({ context }, testInfo) => {
  if (testInfo.______) {
    await context.tracing.stop({ path: "trace.zip" });
  }
});
\`\`\``,null,[`status === "failed"`,`failed`,`didFail`,`error`],0,`testInfo.status === "failed" is the idiomatic check — trace only on failure to save storage.`],[`C`,`I`,`Complete to assert element screenshot within tolerance.
\`\`\`ts
const btn = page.locator(".cta");
await expect(btn).toHaveScreenshot("cta.png", {
  maxDiffPixels: ______,
});
\`\`\``,null,[`10`,`0`,`100`,`1`],0,`10 pixels is a common starting tolerance for button screenshots — adjust based on your rendering environment.`],[`L`,`B`,`Why should visual regression baselines be committed to version control?`,null,[`So Playwright can access them from the CDN`,`Committed baselines make visual changes reviewable in PRs — teammates can see exact pixel diffs in the diff view and intentional changes can be approved as part of code review`,`Playwright requires them to be in the repo root`,`Local-only baselines are faster`],1,`Source-controlled baselines turn visual regressions into a code-review concern — reviewers see exactly what changed in the UI.`],[`L`,`I`,"Why is `locator.screenshot()` preferred over `page.screenshot({ clip: ... })` for element screenshots?",null,[`locator.screenshot() is faster`,`locator.screenshot() automatically scrolls the element into view, clips to its bounding box, and does not require manual coordinate calculation — clip: requires knowing exact pixel positions which change if the layout changes.`,`clip: requires a fixed viewport`,`locator variant uses a higher DPI`],1,`Auto-scroll and auto-clip make locator.screenshot() far more robust than manual coordinate-based clipping.`],[`L`,`A`,"What is the difference between `screenshots: true` and `snapshots: true` in tracing options?",null,[`They are the same option`,`screenshots: true captures a pixel screenshot at each action (before/after). snapshots: true captures DOM and network state (the source for the DOM inspector and network tab in Trace Viewer) — both together give the full timeline experience.`,`snapshots: true is for video`,`screenshots refers to test-result screenshots`],1,`Both are needed for a fully usable trace — screenshots for visual timeline, snapshots for DOM/network inspection.`],[`L`,`I`,"Why does `toHaveScreenshot()` create separate baselines per OS and browser?",null,[`It is a Playwright limitation to be fixed`,`Font rendering, sub-pixel anti-aliasing, and scrollbar styles differ between OS/browser combinations — separate baselines prevent false failures from rendering environment differences.`,`It stores multiple quality levels`,`Each browser uses different screenshot APIs`],1,`Environment-specific baselines are the correct approach — you cannot have one PNG that matches identical rendering on Windows, macOS, and Linux.`],[`L`,`I`,'What is the difference between `trace: "on"` and `trace: "on-first-retry"` in playwright config?',null,[`They produce identical traces`,`on traces every test run (large storage cost). on-first-retry traces only when a test is retried for the first time (failing tests are the ones you need to debug) — a better default for CI.`,`on-first-retry requires retries: 1`,`trace: "on" only works in headful mode`],1,`on-first-retry is the recommended CI setting — you get traces for failures without the storage cost of tracing every passing test.`],[`R`,`B`,`You want to add visual regression testing to an existing test suite without modifying every test. What is the quickest approach?`,null,[`Convert all tests to toHaveScreenshot`,`Add toHaveScreenshot() calls at the end of your most critical end-to-end tests — start with the golden path and expand coverage iteratively`,`Set screenshot: "on" in config — it auto-generates baselines`,`Use Playwright's visual mode flag`],1,`Incremental adoption — add toHaveScreenshot to high-value tests first, build confidence, then expand coverage.`],[`R`,`I`,`A teammate asks for a trace of a specific failing test. How do you generate one?`,null,[`Send them the test-results video folder`,'Set trace: "on" in playwright.config.ts, re-run the failing test, find the trace.zip in test-results/, and share it — they open it with `playwright show-trace trace.zip`',`Traces are only available in cloud CI`,`Use --debug flag to generate a trace`],1,`The standard workflow: enable trace, run the test, share the zip — teammate runs show-trace to inspect the full timeline.`],[`R`,`A`,`You need to capture before/after screenshots of a form submission for a visual changelog. How?`,null,[`Use two toHaveScreenshot calls`,`await page.screenshot({ path: "before.png" }); then fill and submit the form; await page.screenshot({ path: "after.png" }); — store both in the test-results or fixtures directory`,`Use page.on("response") to trigger screenshots`,`Use Playwright's diff mode`],1,`Manual page.screenshot() calls give you precise before/after images — name them descriptively and save to a known path for easy retrieval.`],[`R`,`I`,`You need to test the dark mode appearance of a component. How?`,null,[`Manually set body class in the test`,`colorScheme option: await page.emulate({ colorScheme: "dark" }) before navigating, then use toHaveScreenshot("component-dark.png") — run both light and dark assertions in the same test or separate tests`,`Use a CSS override in the test`,`Change the OS theme before running`],1,`page.emulate({ colorScheme: "dark" }) or context creation with colorScheme: "dark" is the clean way to test dark mode.`],[`S`,`B`,`You add a new component and want to baseline its screenshot for the first time. What is the workflow?`,null,[`Write the toHaveScreenshot assertion, let it fail, then find the actual screenshot in test-results and copy it to __snapshots__`,"Write the toHaveScreenshot assertion, run `npx playwright test --update-snapshots`, confirm the generated PNG looks correct, then commit it",`Playwright auto-commits baselines on first run`,`Run the test twice — second run auto-commits`],1,`--update-snapshots creates new baselines (and updates changed ones) — always inspect before committing.`],[`S`,`I`,`CI visual tests fail because a banner ad renders dynamic content. You cannot remove the ad. How do you fix the test?`,null,[`Accept the flakiness and add retries`,`Add the ad container to the mask list: toHaveScreenshot({ mask: [page.locator(".ad-banner")] }) — Playwright overlays it with a solid color before comparison`,`Use threshold: 1 to ignore all differences`,`Screenshot only the part of the page below the ad`],1,`mask: is designed exactly for this — dynamic content like ads, timestamps, and user avatars that legitimately change between runs.`],[`S`,`A`,`You want to run visual regression tests only in CI, not on every local run. How?`,null,[`Add an environment check: if (process.env.CI) { await expect(page).toHaveScreenshot(...) } — visual assertions only run in CI`,`Use test.skip(!process.env.CI) at the top of each visual test file — the tests are skipped locally but run in CI`,`Visual tests always run everywhere — Playwright does not support conditional execution`,`Add --grep visual to the CI command`],1,`test.skip(!process.env.CI) is a clean pattern for CI-only tests — local developers skip the visual suite for speed.`],[`L`,`A`,"Why do visual regression tests with `toHaveScreenshot()` typically need a `maxDiffPixels` or `threshold` tolerance instead of requiring pixel-perfect equality?",null,[`Minor, expected rendering differences (anti-aliasing, font hinting, sub-pixel rendering) vary slightly even across runs on the same machine/browser, so a small tolerance avoids false failures for non-meaningful differences`,`Playwright screenshots are always compressed lossily, so exact matches are technically impossible`,`Tolerance is only needed when testing on mobile viewports`,`toHaveScreenshot() ignores color entirely; tolerance only affects layout`],0,`Zero tolerance is usually too strict in practice — a small, deliberate tolerance filters out rendering noise while still catching real visual regressions.`],[`O`,`I`,"What does the `mask` option do here?",`await expect(page).toHaveScreenshot('homepage.png', { fullPage: true, mask: [page.locator('.timestamp')] });`,[`It covers the matched element(s) with a solid box before taking/comparing the screenshot, so dynamic content like a live timestamp doesn't cause false-positive diffs`,`It hides the element entirely, removing it from the page layout`,`It blurs the rest of the page except the masked element`,`It excludes the masked element's network requests from the trace`],0,`Masking is the standard fix for visual tests with unavoidably dynamic content — it neutralizes that region instead of trying to keep it deterministic.`],[`R`,`I`,`Your team runs visual regression tests locally on macOS but CI runs on Linux, and screenshots render slightly differently (font rendering), causing constant false failures in CI. What is the recommended fix?`,null,[`Generate and commit the baseline screenshots by running the update command inside the same environment CI uses (e.g., the Playwright Docker image), so baselines and comparisons render on matching OS/font stacks`,`Disable visual tests in CI entirely and only run them locally`,`Increase maxDiffPixels to an arbitrarily large number until failures stop`,`Convert all screenshots to grayscale before comparing to eliminate font rendering issues`],0,`Cross-OS font/rendering differences are a common visual-testing pitfall — generate baselines in the same environment (often a Docker container) that will run the comparisons.`],[`D`,`I`,`A visual test that was passing suddenly fails after an unrelated code change, and the diff image shows the entire page shifted down by exactly the height of a new banner added above the header. Is this a legitimate visual regression?`,null,[`It's an intentional layout change caused by a deliberate feature addition — the correct action is to review the diff, confirm it's expected, and update the baseline screenshot, not treat it as a bug`,`No — this must be a flaky test; simply re-run it until it passes`,`Yes — any pixel diff regardless of cause always indicates a genuine bug`,`The test should be deleted since visual tests cannot handle layout changes`],0,`Visual regression failures aren't automatically bugs — distinguishing an intentional design change (update the baseline) from an unintended regression (fix the code) is part of the workflow.`],[`L`,`A`,`Why do visual regression baselines typically need to be generated separately per browser AND per operating system, rather than one universal baseline set?`,null,[`Font rendering, anti-aliasing, and even subtle layout engine differences vary across browsers (Chromium/Firefox/WebKit) and operating systems — a screenshot taken on one combination will legitimately differ, pixel-for-pixel, from the same page on another`,`Playwright requires a separate license key per OS/browser combination for visual testing`,`This is only a theoretical concern; in practice all engines render identically`,`Baselines are OS/browser-specific purely due to file path naming conventions, not actual rendering differences`],0,`This is why Playwright's snapshot naming automatically includes the project/browser name — a Chromium baseline is never expected to match a WebKit render pixel-for-pixel.`],[`O`,`I`,`What does this achieve compared to a full-page screenshot comparison?`,`await expect(page.locator('.pricing-card')).toHaveScreenshot('pricing-card.png');`,["It compares only the specific `.pricing-card` element's rendered appearance, ignoring the rest of the page entirely — more stable against unrelated changes elsewhere on the page and faster to diff/review",`It automatically crops the full-page screenshot down to that element after the fact, with no functional difference`,`Element-scoped screenshots cannot use masking or tolerance options, unlike full-page ones`,`It captures the element's HTML source instead of a rendered image`],0,`Scoping a visual assertion to just the relevant element reduces false positives from unrelated page changes and keeps the diff focused on what the test actually cares about.`],[`O`,`A`,'What does the `animations: "disabled"` option do here, and why is it commonly paired with visual testing?',`await expect(page).toHaveScreenshot('modal-open.png', { animations: 'disabled' });`,[`It freezes CSS animations/transitions to their final state (and disables infinite animations) before capturing the screenshot — preventing flaky, timing-dependent diffs caused by catching an animation mid-frame`,`It permanently disables all CSS animations in the application for every future test`,`It only disables JavaScript-driven animations, not CSS ones`,`This option has no effect unless combined with a manual waitForTimeout`],0,`A screenshot taken mid-animation is inherently non-deterministic — animations: "disabled" removes that entire source of visual test flakiness.`],[`O`,`I`,"What does the `clip` option accomplish?",`await page.screenshot({ path: 'header-only.png', clip: { x: 0, y: 0, width: 1280, height: 80 } });`,[`It captures only the specified pixel region of the page (here, the top 80px header strip) rather than the full viewport or full page`,`It resizes the browser viewport to exactly 1280x80 before taking the screenshot`,`clip only works with toHaveScreenshot(), not plain page.screenshot()`,`It crops the resulting PNG file after the fact using an external image tool`],0,`clip lets you screenshot an exact pixel rectangle directly — useful when you want a fixed region regardless of the full page's layout.`],[`R`,`I`,"A visual test flags a diff, and a reviewer needs to confirm whether it is an intentional design change before approving an `--update-snapshots` run. What is a good team workflow for this review step?",null,[`Attach the generated diff image (showing old vs. new vs. highlighted differences) as a PR artifact or comment for a human reviewer to explicitly approve before the updated baseline is committed — treating baseline updates as a reviewed code change, not an automatic action`,`Anyone can run --update-snapshots at any time without review, since visual diffs are rarely meaningful`,`Automatically auto-approve any diff below a certain percentage threshold with no human review at all`,`Skip visual tests in CI permanently to avoid needing this workflow`],0,`Since baseline updates effectively "bless" a new appearance as correct, treating them like any other reviewable code change (with the diff visible) prevents accidental or unnoticed regressions from being silently accepted.`],[`D`,`I`,`A visual test passes locally every time but fails in CI with a diff showing text rendered at a very slightly different position, even though nothing in the code changed. What is a likely cause specific to CI environments?`,null,[`CI runners often use different installed system fonts than local developer machines, causing subtly different font metrics/rendering — the standard fix is generating and comparing baselines inside the SAME containerized environment (e.g., the official Playwright Docker image) used by CI`,`Visual tests are fundamentally incompatible with CI and should never be run there`,`The screenshot comparison algorithm is randomized and gives different results each run by design`,`This can only be fixed by disabling visual testing on text-containing elements entirely`],0,`Font availability/rendering differences between local machines and CI containers are one of the most common causes of "phantom" visual diffs — generating baselines in the same environment that runs the comparison eliminates this class of flakiness.`],[`L`,`I`,"What does `await expect(page.getByRole('navigation')).toMatchAriaSnapshot()` test, and how does it differ fundamentally from `toHaveScreenshot()`?",null,[`It captures/compares the element's accessibility tree (roles, names, structure) as text, not pixels — robust to purely visual restyling but sensitive to structural/semantic changes, the opposite tradeoff from pixel screenshots`,`It is a synonym for toHaveScreenshot() scoped to one element`,`It only checks color contrast ratios for accessibility`,`It records a video of the navigation element`],0,`ARIA snapshots and pixel screenshots catch entirely different classes of regressions — structural versus visual.`],[`L`,`A`,"What does the `expect: { toHaveScreenshot: { stylePath: './screenshot.css' } } ` config option let you do?",null,[`It only works with locator.screenshot(), never expect(page)`,`It injects an extra CSS file into the page specifically during screenshot capture (e.g. to hide scrollbars or blinking carets consistently) without affecting the app's real styling during normal test execution`,`It sets the default viewport size for all screenshots`,`It compresses screenshots using the specified CSS-driven algorithm`],1,`stylePath applies only at capture time, letting you neutralize known sources of rendering noise without touching production styles.`],[`L`,`I`,"What problem does the `caret: 'hide'` screenshot option solve?",null,[`It hides the mouse cursor from the screenshot`,`It removes all form inputs from the screenshot`,`It hides the blinking text cursor in a focused input field before capturing, preventing flaky diffs caused by whether the cursor happened to be visible or blinking at the exact capture instant`,`It disables keyboard input during the test`],2,`A blinking caret is inherently non-deterministic at any single capture moment, making it a classic source of flaky visual diffs.`],[`L`,`I`,"What's the difference between `scale: 'css'` and `scale: 'device'` in a screenshot option?",null,[`They only affect JPEG screenshots, not PNG`,`scale: 'device' is only valid on mobile viewport projects`,`They produce identical output on all devices`,`'css' captures at 1 CSS pixel per pixel in the output image regardless of device pixel ratio, while 'device' captures at the actual device pixel ratio, producing a larger, higher-resolution image`],3,`On a high-DPI (Retina-style) device, 'device' scale produces a noticeably larger image than 'css' scale for the same viewport.`],[`L`,`A`,"Why would a team set `expect: { toHaveScreenshot: { maxDiffPixelRatio: 0.01, animations: 'disabled' } }` once in playwright.config.ts instead of repeating those options on every call?",null,[`It establishes consistent defaults for every toHaveScreenshot() call in the suite, so individual tests don't need to remember to pass the same tolerance/animation settings each time, and defaults stay easy to tune in one place`,`Because per-call options are not supported at all`,`It makes screenshots capture faster`,`It is required for CI environments specifically`],0,`Centralizing shared defaults avoids repetitive per-call options and keeps future tuning to a single location.`],[`L`,`I`,"What's the difference between `maxDiffPixelRatio` and `maxDiffPixels`?",null,[`They are exact synonyms with different names`,`maxDiffPixels is an absolute count of differing pixels allowed; maxDiffPixelRatio is a fraction of the total image (e.g. 0.01 = 1%) — the ratio option scales naturally across screenshots of very different sizes`,`maxDiffPixelRatio only applies to full-page screenshots`,`maxDiffPixels is deprecated in favor of maxDiffPixelRatio`],1,`A fixed pixel count can be too strict for a huge image or too loose for a tiny one — a ratio scales proportionally instead.`],[`L`,`B`,"Why might `page.screenshot({ type: 'jpeg', quality: 80 })` be preferred over PNG for a screenshot attached purely as debugging evidence in CI (not for pixel comparison)?",null,[`JPEG is required for any screenshot larger than 1MB`,`JPEG captures higher resolution than PNG`,`JPEG produces a much smaller file at slightly reduced quality, saving CI artifact storage/bandwidth — fine for a human to glance at, but unsuitable for toHaveScreenshot()'s exact pixel comparison, which needs lossless PNG`,`PNG cannot be viewed in most browsers`],2,`Lossy compression is an acceptable tradeoff for a debugging artifact but would break exact-pixel regression comparison.`],[`L`,`I`,"What does `page.screenshot({ omitBackground: true })` do for an element with a transparent CSS background?",null,[`It fills the background with black instead of white`,`It removes the element entirely from the capture`,`It only works for full-page screenshots`,`It captures the screenshot with a transparent background (alpha channel) instead of forcing a default white/opaque background behind the content`],3,`This is useful for verifying elements meant to overlay other content, where a default opaque backdrop would misrepresent the real rendering.`],[`L`,`A`,"What does configuring `video: { mode: 'on', size: { width: 640, height: 360 } }` in the config control?",null,[`The resolution of the recorded video file — it can be set independently of the actual test viewport size, useful for keeping video artifact file sizes down`,`The actual browser viewport used during the test`,`The frame rate of the recorded video`,`The screenshot resolution used for toHaveScreenshot()`],0,`Video resolution and the test's actual viewport are independent settings — you can record a smaller video than the real viewport.`],[`L`,`I`,"Why would wrapping UI actions in `test.step('fill checkout form', async () => {...})` make a failing visual regression test easier to debug in the Trace Viewer?",null,[`test.step() automatically fixes flaky visual assertions`,`Named steps appear as distinct, collapsible rows in the trace timeline, letting you jump straight to the relevant portion of a long test instead of scrubbing through every individual action`,`It disables animations during that step automatically`,`It causes the screenshot baseline to be regenerated automatically`],1,`Named steps organize a long test's timeline into navigable chunks in the trace viewer.`],[`L`,`B`,"How does Playwright's UI mode (`npx playwright test --ui`) help with visual debugging while a test is actively running or after it finishes?",null,[`It automatically updates all snapshot baselines for you`,`It only works for tests that have already failed`,`It shows a live, scrubbable timeline of screenshots/DOM states for each action, letting you visually step through what happened without needing to separately open a trace file afterward`,`It replaces the need for toHaveScreenshot() entirely`],2,`UI mode gives an interactive, trace-like view integrated directly into the test-running experience.`],[`L`,`A`,"Besides images, what else can `toMatchSnapshot()` compare, and how is it more general-purpose than `toHaveScreenshot()`?",null,[`It can only compare images, identically to toHaveScreenshot()`,`It can compare live network traffic`,`It can compare CSS stylesheets specifically`,`It can compare arbitrary text, JSON, or binary buffers against a stored snapshot file, not just PNG screenshots — toHaveScreenshot() is really a specialized, image-focused wrapper around similar snapshot machinery`],3,`toMatchSnapshot() is the more general primitive; toHaveScreenshot() specializes it for the image-comparison use case.`],[`L`,`I`,"How does the newer `toMatchAriaSnapshot()` differ in purpose from the older, now-deprecated `page.accessibility.snapshot()` API?",null,[`toMatchAriaSnapshot() is designed specifically as a test assertion with baseline comparison and update workflow (like toHaveScreenshot but for the a11y tree), whereas accessibility.snapshot() was a lower-level API that just returned the tree for manual inspection`,`They are functionally identical, just renamed`,`accessibility.snapshot() only worked in Chromium`,`toMatchAriaSnapshot() cannot be used with getByRole()`],0,`The new matcher brings a baseline-and-compare workflow to the accessibility tree, similar in spirit to visual regression but text-based.`],[`L`,`A`,"Why can a `fullPage: true` screenshot sometimes show a sticky/fixed-position header appearing to repeat multiple times down the page?",null,[`This never actually happens with fullPage captures`,`fullPage capture works by scrolling and stitching multiple viewport-sized captures together, and a sticky/fixed element re-renders at the top of the viewport in each of those captures, producing visible duplication artifacts in the stitched result`,`Sticky headers are automatically removed during capture`,`It only happens when deviceScaleFactor is greater than 1`],1,`The stitching process is essentially several viewport screenshots glued together, and a pinned element appears fresh in each one.`],[`L`,`I`,`How would you set up visual regression tests to cover a responsive design at mobile, tablet, and desktop breakpoints?`,null,[`Use a single baseline and rely on maxDiffPixelRatio to absorb layout differences`,`Only test the desktop breakpoint since it has the most content`,`Define separate Playwright projects (or use test.use({ viewport })) for each breakpoint's viewport size, each producing its own independent baseline images, since the correct 'expected' screenshot genuinely differs per breakpoint`,`Resize the viewport mid-test and reuse the same baseline for all sizes`],2,`Each breakpoint has a genuinely different correct layout, so each needs its own independently maintained baseline.`],[`L`,`B`,"Why might a full-page visual regression screenshot be flaky on a page with several lazily-loaded images, even after `page.waitForLoadState('load')`?",null,[`waitForLoadState('load') always waits for lazy images too`,`fullPage screenshots skip images entirely`,`Lazy loading is disabled automatically in test environments`,`Lazy-loaded images (loaded via IntersectionObserver as they scroll into view) may not have started loading yet at the 'load' event, since 'load' only guarantees initially-requested resources are done`],3,`The load event only covers resources requested up front — images gated behind scroll-triggered lazy loading aren't part of that guarantee.`],[`L`,`I`,`Why can text rendered with a custom web font cause visual regression flakiness even on the exact same machine and browser across runs?`,null,[`If the screenshot is taken before the web font finishes downloading and applying (a FOUT/FOIT race), the fallback system font may still be showing in some runs, producing pixel differences unrelated to any real code change`,`Web fonts render identically every time with no possible variance`,`Web fonts are incompatible with toHaveScreenshot() entirely`,`This only happens on the very first test run ever`],0,`A font-loading race condition means the exact same code can render differently depending on timing alone.`],[`L`,`A`,"What does waiting on `page.waitForFunction(() => document.fonts.status === 'loaded')` before a screenshot accomplish?",null,[`It preloads all fonts used anywhere on the site`,`It ensures all web fonts have finished loading and are actively applied before the screenshot is captured, eliminating a font-loading race as a source of visual test flakiness`,`It disables custom fonts entirely for the test`,`It forces the page to use only system fonts`],1,`Waiting on document.fonts.status directly addresses the timing race between font loading and screenshot capture.`],[`L`,`I`,"Why might a screenshot unexpectedly show a button in its `:hover` styled state, even though the test never intentionally hovers over it?",null,[`toHaveScreenshot() randomly applies hover styles for visual testing purposes`,`Hover states are always captured regardless of cursor position`,`The mouse cursor's last position from a previous action may still be resting over that element when the screenshot is taken, triggering its real :hover CSS — moving the mouse away before capturing avoids this`,`This only happens in headless mode`],2,`The browser applies real :hover CSS based on wherever the mouse cursor last was, independent of test intent.`],[`L`,`A`,"Why might a visual test suite globally override `scroll-behavior: auto` (disabling smooth scrolling) via injected CSS during tests?",null,[`Smooth scrolling always breaks page.screenshot() with an error`,`It is required for fullPage: true to work at all`,`It speeds up the browser's rendering engine globally`,`Smooth scrolling animates over time, so a screenshot taken right after a programmatic scroll could catch the page mid-animation at an unpredictable position — disabling it makes scrolls resolve instantly and deterministically before capture`],3,`An animated scroll introduces timing-dependent variance into exactly where the page has settled at capture time.`],[`L`,`I`,"When a `toHaveScreenshot()` assertion fails, what does Playwright's built-in HTML reporter typically show for that test, without any extra configuration?",null,[`A visual diff view with the expected baseline, the actual screenshot, and a highlighted difference image side by side, generated automatically because the failure carries these as attachments`,`Only a text message saying "screenshot mismatch"`,`A video of the entire test run`,`Nothing — you must manually attach the images yourself`],0,`toHaveScreenshot() failures automatically attach expected/actual/diff images that the HTML reporter renders for review.`],[`L`,`B`,"Why might a test call `testInfo.attach('manual-review.png', { path, contentType: 'image/png' })` in addition to a normal toHaveScreenshot() assertion?",null,[`It is required for toHaveScreenshot() to function at all`,`To include additional custom evidence (e.g. a screenshot of a different region or state) in the HTML report/trace beyond what the automatic pass/fail visual diff already provides`,`It replaces the need for any assertion in the test`,`It forces the test to always pass regardless of the screenshot comparison`],1,`Manual attachments supplement the automatic diff artifacts with any extra evidence you find useful.`],[`L`,`A`,"What's a key tradeoff of using Playwright's built-in `toHaveScreenshot()` for visual regression versus a dedicated third-party visual-testing service?",null,[`toHaveScreenshot() is always strictly worse in every respect`,`Third-party services cannot integrate with Playwright at all`,`The built-in approach requires you to manage baseline storage, cross-environment consistency, and review workflow yourself, whereas a dedicated service typically handles perceptual diffing, cross-browser baseline management, and review UI as a hosted feature — at the cost of an external dependency and often a subscription`,`toHaveScreenshot() only works with Chromium`],2,`The tradeoff is self-managed simplicity and cost versus hosted convenience and additional external dependency.`],[`L`,`I`,"What does passing an array like `toHaveScreenshot(['checkout', 'step-2', 'summary.png'])` do differently from a single string name?",null,[`It compares against three separate baselines and requires all to match`,`It captures three screenshots in sequence`,`It is invalid syntax and throws an error`,`It builds a nested path under __snapshots__ (e.g. __snapshots__/checkout/step-2/summary-chromium.png), letting you organize a large number of related baselines into subfolders instead of one flat directory`],3,`Array-form names build a folder hierarchy for baseline organization rather than a flat naming scheme.`],[`L`,`A`,`Why might a visual regression suite need separate baselines per locale (e.g. English vs German) for the same page, beyond just per-browser/OS baselines?`,null,[`Translated text often has different lengths, which can shift layout, wrap differently, or resize containers — so the correct rendered appearance genuinely differs by locale, not just due to rendering-engine noise`,`Locale differences never affect rendered layout`,`Locale settings are automatically normalized before capture`,`Only right-to-left locales need separate baselines`],0,`A layout that fits perfectly in English may overflow or wrap differently once translated into a longer language.`],[`L`,`I`,`A designer asks whether visual regression tests can confirm the implementation exactly matches their Figma mockup pixel-for-pixel. What's the accurate answer?`,null,[`Yes, toHaveScreenshot() compares directly against Figma files`,`No — visual regression tests only detect CHANGE relative to a previously approved baseline; the very first baseline still has to be manually reviewed and approved as matching the design before it becomes the reference for catching future drift`,`No, visual regression cannot be used for design work at all`,`Yes, but only for full-page screenshots, not components`],1,`The tests catch drift from an approved reference — they don't independently validate that the reference itself matches a design mockup.`],[`L`,`B`,`In what format does Playwright save its recorded test videos, and what's a practical implication of that?`,null,[`MP4 — compatible with virtually every tool by default`,`AVI — an older format requiring a codec pack`,`WebM — which plays natively in most browsers and video players, but if a stakeholder needs an MP4 for a tool that doesn't support WebM, an external conversion step (e.g. ffmpeg) is required since Playwright doesn't do that conversion itself`,`GIF — which limits color depth and frame rate`],2,`WebM works fine in most modern contexts, but converting to MP4 for a specific tool requires an external step Playwright doesn't handle.`],[`L`,`A`,"Why does Playwright hide scrollbars by default when taking screenshots (a `hideScrollbars` behavior), and when might you want to explicitly show them instead?",null,[`Scrollbars are always invisible in every browser by default already`,`It is a performance optimization unrelated to visual consistency`,`Scrollbars cannot technically be captured in a screenshot at all`,`Scrollbar appearance/width differs across OSes, so hiding them by default removes a common source of cross-environment diff noise; you'd disable this only if the test specifically needs to verify scrollbar-related UI`],3,`OS-level scrollbar rendering differences are exactly the kind of environmental noise visual regression testing tries to eliminate.`],[`L`,`I`,"If you call `expect(page).toHaveScreenshot()` with no name argument at all, how does Playwright decide what to name the baseline file?",null,[`It derives a name automatically from the test's title (and an incrementing index if called multiple times in the same test), rather than requiring an explicit name every time`,`It throws an error requiring an explicit name`,`It always names the file "screenshot.png"`,`It prompts interactively for a filename`],0,`Automatic naming based on the test title is a convenience for simple cases with one or a few screenshots per test.`],[`L`,`A`,"In a test that checks five different components' screenshots in sequence, what's the benefit of using `expect.soft(component).toHaveScreenshot(...)` for each instead of the regular `expect`?",null,[`Soft assertions run components' screenshots in parallel`,`If the first component's screenshot fails, the test continues checking the remaining four rather than aborting immediately, so a single run reports every component that changed instead of just the first one encountered`,`expect.soft() automatically retries failed comparisons`,`It disables the visual diff generation for speed`],1,`Soft assertions collect multiple failures in one run instead of stopping at the very first one.`],[`L`,`I`,"Why would changing a test project's `deviceScaleFactor` from 1 to 2 break all of that project's existing visual regression baselines?",null,[`It only affects video recording, not screenshots`,`deviceScaleFactor has no effect on toHaveScreenshot()`,`A higher device scale factor produces a larger output image (more physical pixels per CSS pixel), so the new screenshot's dimensions no longer match the previously committed baseline's dimensions`,`It only affects mobile emulation projects`],2,`A dimension mismatch alone is enough to fail every comparison, regardless of whether the actual visual content changed.`],[`L`,`A`,"Why do some teams require that a PR containing `--update-snapshots` changes be reviewed by a second person before merging, treating it almost like a design sign-off?",null,[`Because --update-snapshots is technically risky to run at all`,`Because only senior engineers are permitted to run CLI commands`,`Because baseline files require special file permissions to edit`,`Updating a baseline effectively declares "this new appearance is correct going forward" — without review, an accidental visual regression could get silently baked in as the new accepted baseline instead of being caught`],3,`A baseline update is a meaningful decision about what counts as "correct" — it deserves the same scrutiny as any other code change.`],[`L`,`I`,`When reviewing a visual regression failure, what's a useful heuristic for telling apart "a few scattered single pixels differ" from "a real layout regression"?`,null,[`Scattered, tiny, sub-pixel-level differences across the whole image usually indicate rendering noise (anti-aliasing, font hinting) needing a tolerance adjustment; a contiguous, structural block of difference usually indicates an actual layout or content change worth investigating`,`There is no reliable way to distinguish the two`,`Any diff larger than 10 pixels is automatically a real bug`,`Scattered diffs always mean the baseline itself is corrupted`],0,`The SHAPE of the diff (scattered noise vs. a solid contiguous block) is a strong practical signal for triage.`],[`L`,`A`,"Why might you want to customize `maskColor` (instead of using the default) when masking a dynamic region in a screenshot?",null,[`maskColor changes how strict the comparison tolerance is`,`Mainly cosmetic/clarity in the reviewed diff image — a distinct, easily recognizable mask color makes it visually obvious in a PR review which regions were deliberately excluded from comparison, versus an area that might be a real difference`,`It is required for mask to function at all`,`It determines which locator gets masked`],1,`A visually distinct mask color helps a human reviewer immediately recognize deliberately-excluded regions in a diff.`],[`L`,`I`,"What's the functional difference between the config option `use: { screenshot: 'only-on-failure' }` and calling `expect(page).toHaveScreenshot()` inside a test?",null,[`They are two names for the exact same mechanism`,`toHaveScreenshot() only works when use.screenshot is also set`,`use.screenshot automatically captures a plain debugging screenshot attached to the report when ANY test fails, with no baseline comparison involved; toHaveScreenshot() is an explicit assertion that compares against a committed baseline and can itself cause the test to fail`,`use.screenshot only works for full-page captures`],2,`One is a generic failure-evidence capture; the other is a pass/fail assertion against a committed reference image.`],[`L`,`A`,"If a `toHaveScreenshot()` assertion fails and the test is configured to retry, does each retry attempt get compared against a fresh capture, or does Playwright reuse the first attempt's actual screenshot?",null,[`It always reuses the first attempt's screenshot for subsequent retries`,`Retries are not supported for toHaveScreenshot() at all`,`Retries only re-run the assertion, not the whole test`,`Each retry re-runs the test from scratch, including taking a brand-new screenshot to compare — a flaky visual assertion could legitimately pass on a later retry if the transient issue doesn't recur`],3,`A full test re-run means a transient timing issue (like a mid-animation capture) may simply not recur on retry.`],[`L`,`I`,"Why might a checkout confirmation test include both `toHaveScreenshot()` AND `toHaveText('Order confirmed')`, rather than relying on the screenshot alone?",null,[`A screenshot assertion alone can't say WHY it failed in a way that's immediately actionable, whereas pairing it with a targeted functional assertion gives a much clearer, faster signal about whether the actual content/state is wrong versus a purely cosmetic rendering difference`,`toHaveText() is required syntax before any screenshot assertion`,`toHaveScreenshot() cannot detect text content at all under any circumstances`,`It is purely a stylistic convention with no practical benefit`],0,`A functional assertion gives a specific, actionable failure reason, while a screenshot failure alone just says "pixels differ somewhere."`],[`L`,`A`,"What's an advantage of generating brand-new visual baselines by running `--update-snapshots` as a dedicated CI job (then committing the artifact back), rather than always generating them on a developer's local machine?",null,[`CI jobs run faster than any local machine`,`The CI job runs in the exact same environment (OS, browser build, fonts) that will later perform the comparisons, eliminating any local-machine-vs-CI rendering mismatch as a source of false failures for baselines generated that way`,`It automatically gets design team approval`,`It is the only way to commit files from CI`],1,`Generating and comparing in the same environment removes an entire class of cross-environment rendering differences.`],[`L`,`I`,"Why do most teams apply visual regression testing selectively (key pages/components) rather than adding `toHaveScreenshot()` to literally every test?",null,[`Playwright technically limits the total number of toHaveScreenshot() calls per suite`,`Visual assertions are always slower than functional ones by a fixed large factor`,`Every additional visual assertion adds baseline files to maintain, review overhead when legitimate changes happen, and additional flakiness surface area — the value is highest on stable, high-traffic UI, with diminishing returns as coverage becomes exhaustive`,`It is only a licensing restriction`],2,`The cost of maintaining and reviewing baselines scales with coverage, while the marginal value of covering yet another minor page is often low.`],[`L`,`A`,'Is the DOM/network "snapshot" data captured by `context.tracing.start({ snapshots: true })` the same mechanism used by `toMatchAriaSnapshot()`?',null,[`Yes, they share the exact same underlying data and files`,`Yes, but only when both are enabled together in the same config`,`No, tracing snapshots only exist for screenshots, not DOM`,`No — tracing snapshots are captured for the interactive Trace Viewer's DOM inspector (a debugging tool with no pass/fail comparison), while toMatchAriaSnapshot() is a distinct test assertion that compares the accessibility tree against a committed baseline file`],3,`One is a debugging aid with no comparison logic; the other is a genuine pass/fail test assertion with its own baseline files.`],[`O`,`A`,`What does this assert?`,`await expect(page.getByRole('navigation')).toMatchAriaSnapshot(\`
  - navigation:
    - link "Home"
    - link "Products"
    - link "Contact"
\`);`,[`That the navigation region's accessibility tree contains exactly those roles/names in that structure — a purely CSS/color restyle of the nav would NOT fail this, but removing or renaming a link would`,`That the navigation element is exactly 3 pixels tall`,`That clicking each link navigates correctly`,`That the navigation loads within 3 seconds`],0,`ARIA snapshot assertions are sensitive to structural/semantic changes, not cosmetic ones.`],[`O`,`I`,`What is the effect during screenshot capture here?`,`// playwright.config.ts
expect: { toHaveScreenshot: { stylePath: './hide-scrollbar.css' } }
// hide-scrollbar.css: ::-webkit-scrollbar { display: none; }`,[`The scrollbar is permanently removed from the app for all users`,`This CSS is injected only while capturing the screenshot for comparison, hiding scrollbars consistently across environments without changing how the app actually looks to real users`,`It disables scrolling entirely during the test`,`It only applies to WebKit-based browser projects`],1,`stylePath applies purely at capture time, leaving the real application styling untouched.`],[`O`,`I`,`Why does caret:'hide' matter here?`,`const input = page.getByLabel('Email');
await input.fill('test@example.com');
await expect(input).toHaveScreenshot('email-input.png', { caret: 'hide' });`,[`fill() always leaves the caret invisible already, so this is a no-op`,`caret: 'hide' hides the entire input field from the screenshot`,`Without it, the blinking text cursor in the now-focused input might or might not be visible at the exact capture moment, causing an intermittent, non-meaningful diff`,`It prevents the fill() call from working correctly`],2,`A blinking caret's on/off state at capture time is essentially random, making it a classic flakiness source.`],[`O`,`A`,`How do a.png and b.png differ here?`,`// deviceScaleFactor: 2 in project config
await page.screenshot({ path: 'a.png', scale: 'css' });
await page.screenshot({ path: 'b.png', scale: 'device' });`,[`They are pixel-identical regardless of deviceScaleFactor`,`a.png is larger because css scale always doubles resolution`,`scale only affects JPEG output, not PNG`,`b.png is captured at 2x resolution (twice the pixel dimensions of a.png) because scale: 'device' honors the 2x device pixel ratio, while scale: 'css' normalizes to 1 image pixel per CSS pixel`],3,`scale: 'device' respects the configured device pixel ratio, producing a higher-resolution image than scale: 'css'.`],[`O`,`I`,`What is the effect on individual test files here?`,`// playwright.config.ts
expect: {
  toHaveScreenshot: { maxDiffPixelRatio: 0.02, animations: 'disabled' },
},`,[`Every toHaveScreenshot() call anywhere in the suite automatically applies a 2% pixel-difference tolerance and disables animations, unless a specific call explicitly overrides those options`,`Individual toHaveScreenshot() calls can never override these values`,`Only the first test file in the suite is affected`,`It applies only to full-page screenshots, not element screenshots`],0,`Config-level defaults apply suite-wide but can still be overridden per assertion when needed.`],[`O`,`B`,`What does maxDiffPixelRatio mean for a 1000x800 image here?`,`await expect(page).toHaveScreenshot('dashboard.png', { maxDiffPixelRatio: 0.01 });`,[`Only 1 pixel total may differ`,`Up to roughly 8,000 pixels (1% of the 800,000 total pixels) may differ before the assertion fails — the tolerance scales with image size rather than being a fixed count`,`1% of the image must differ or the test fails`,`It limits the screenshot to 1% of the original resolution`],1,`The ratio is computed against the total pixel count of the image being compared.`],[`O`,`B`,`Why not use this JPEG screenshot for a toHaveScreenshot baseline?`,`await page.screenshot({ path: 'debug-evidence.jpg', type: 'jpeg', quality: 60, fullPage: true });`,[`JPEG screenshots cannot be attached to test reports`,`JPEG only supports full-page captures`,`JPEG's lossy compression introduces subtle artifacts that would make exact/tolerant pixel comparison unreliable — toHaveScreenshot() always compares lossless PNGs for that reason`,`fullPage: true is incompatible with the jpeg type`],2,`Lossy JPEG compression can vary slightly between encodes, undermining precise pixel-level comparison.`],[`O`,`I`,`What does badge.png look like here?`,`const badge = page.getByTestId('status-badge'); // has transparent background
await badge.screenshot({ path: 'badge.png', omitBackground: true });`,[`A solid white square regardless of the badge's actual background`,`An error, since locators cannot use omitBackground`,`The badge cropped to a fixed 100x100 size`,`The badge's own rendered content on a transparent background, rather than the default opaque white fill Playwright would otherwise composite behind transparent areas`],3,`omitBackground preserves the element's actual transparency instead of compositing a default opaque backdrop.`],[`O`,`A`,`What is the purpose of size here?`,`// playwright.config.ts
use: { video: { mode: 'retain-on-failure', size: { width: 320, height: 240 } } }`,[`To record smaller, lower-resolution videos for failed tests specifically to reduce CI storage/upload time, independent of the actual browser viewport size used during the test`,`It sets the actual browser viewport to 320x240`,`It crops the video after recording using external software`,`It is required for retain-on-failure to work at all`],0,`Video resolution can be tuned independently of the real test viewport purely to manage artifact size.`],[`O`,`B`,`What benefit does test.step give here if the screenshot assertion fails?`,`await test.step('open settings and switch to dark mode', async () => {
  await page.getByRole('button', { name: 'Settings' }).click();
  await page.getByLabel('Dark mode').check();
});
await expect(page).toHaveScreenshot('dark-mode.png');`,[`It automatically fixes any visual mismatch found`,`You can jump directly to the labeled 'open settings and switch to dark mode' step in the timeline instead of scanning through every low-level click/check action individually`,`It disables the toHaveScreenshot comparison inside the step`,`It causes the screenshot to be taken twice for redundancy`],1,`Named steps make a long test's trace timeline much easier to navigate when debugging a failure.`],[`O`,`I`,`What kind of snapshot is this?`,`const summary = await page.locator('.order-summary').innerText();
expect(summary).toMatchSnapshot('order-summary.txt');`,[`A pixel screenshot saved with a .txt extension`,`An accessibility tree snapshot`,`A plain-text snapshot of the element's rendered text content, compared against a stored .txt baseline — no pixels or accessibility tree involved, just text`,`A HAR-based network snapshot`],2,`toMatchSnapshot() is a general-purpose snapshot mechanism, not limited to images.`],[`O`,`A`,`What visual artifact might appear in long-page.png here?`,`// header has position: sticky; top: 0
await page.screenshot({ path: 'long-page.png', fullPage: true });`,[`The header disappears entirely from the screenshot`,`The page is truncated at the header's height`,`The screenshot fails to generate at all`,`The sticky header may appear to repeat at multiple vertical positions down the stitched image, since it re-renders pinned to the top of each viewport-sized capture segment used to build the full-page image`],3,`fullPage stitches multiple viewport captures together, and a pinned element re-appears fresh in each segment.`],[`O`,`I`,`What happens to toHaveScreenshot baselines across these projects?`,`// playwright.config.ts
projects: [
  { name: 'mobile', use: { viewport: { width: 375, height: 812 } } },
  { name: 'desktop', use: { viewport: { width: 1440, height: 900 } } },
]`,[`Each project gets its own independently named baseline file (e.g. home-mobile.png, home-desktop.png), since the two viewports legitimately render different layouts that shouldn't be compared against each other`,`Both projects share one baseline and compare against each other`,`Only the desktop project generates baselines`,`It causes a naming conflict that must be resolved manually`],0,`Playwright's project name is automatically included in the baseline filename, keeping different breakpoints independent.`],[`O`,`B`,`What is the likely fix for this flaky gallery screenshot?`,`await page.goto('/gallery');
await page.waitForLoadState('load');
await expect(page).toHaveScreenshot('gallery.png'); // flaky: some images blank`,[`Add a fixed 10-second sleep before the screenshot`,`Scroll through the page (or otherwise trigger the lazy-loading IntersectionObserver) and wait for all images' load events before taking the screenshot`,`Switch to fullPage: false`,`Increase maxDiffPixels to a very high number`],1,`The load event alone doesn't guarantee lazily-loaded images have started or finished loading.`],[`O`,`I`,`What flakiness does the waitForFunction call prevent here?`,`await page.goto('/');
await page.waitForFunction(() => document.fonts.status === 'loaded');
await expect(page).toHaveScreenshot('home.png');`,[`It prevents images from failing to load`,`It prevents the page from navigating before load`,`Capturing the screenshot while a custom web font is still loading, which would show the fallback system font instead of the intended typeface in some runs but not others`,`It has no meaningful effect on the screenshot`],2,`Waiting on document.fonts.status removes the font-loading race as a source of flakiness.`],[`O`,`A`,`Why might the Confirm button (now gone) or another element show unexpected hover styling in the screenshot?`,`await page.getByRole('button', { name: 'Delete' }).click();
await page.getByRole('dialog').getByRole('button', { name: 'Confirm' }).click();
await expect(page).toHaveScreenshot('after-delete.png');
// an unrelated element appears highlighted/hovered in the screenshot`,[`toHaveScreenshot() always highlights the last-clicked element`,`This indicates a real CSS bug that must be fixed in the app`,`click() automatically applies a permanent hover class`,`The mouse cursor's last position from the click may still be resting over an element in the final layout, triggering its real CSS :hover state at capture time — moving the mouse elsewhere before the screenshot avoids this`],3,`The browser applies genuine :hover styling based on cursor position, regardless of test intent.`],[`O`,`B`,`Why override scroll-behavior for this test?`,`// injected before tests: html { scroll-behavior: auto !important; }
await page.getByText('Jump to section 5').click();
await expect(page).toHaveScreenshot('section-5.png');`,[`Without it, the click could trigger a smooth (animated) scroll, and the screenshot might capture the page mid-scroll at an unpredictable position instead of settled at section 5`,`scroll-behavior has no effect on screenshots at all`,`It is required for the click() call to succeed`,`It disables all animations on the page, not just scrolling`],0,`Instant scrolling removes the timing-dependent variance a smooth animated scroll would introduce.`],[`O`,`I`,`What does the generated HTML report show for this failure by default?`,`// no special config needed
await expect(page).toHaveScreenshot('home.png'); // fails`,[`Just a plain text error message with no images`,`An automatic side-by-side (or slider) view of the expected baseline, the actual screenshot, and a highlighted diff image — all attached automatically because toHaveScreenshot() failures carry these artifacts`,`A video recording of the entire test suite`,`Nothing until --update-snapshots is run`],1,`toHaveScreenshot() failures automatically attach the comparison images the reporter needs to render a diff view.`],[`O`,`B`,`What is the purpose of this testInfo.attach call?`,`test('profile page', async ({ page }, testInfo) => {
  await page.goto('/profile');
  const buf = await page.screenshot();
  await testInfo.attach('profile-manual', { body: buf, contentType: 'image/png' });
});`,[`It is required before any assertion can run`,`It replaces the need for expect() assertions entirely`,`To include a supplementary screenshot in the HTML report as evidence, independent of and in addition to any toHaveScreenshot() pass/fail comparisons in the test`,`It automatically fails the test if the screenshot looks wrong`],2,`Manual attachments add extra evidence to the report beyond the automatic pass/fail comparison artifacts.`],[`O`,`A`,`What is the resulting file location here?`,`await expect(page).toHaveScreenshot(['checkout', 'step-2', 'summary.png']);`,[`A single flat file named checkout-step-2-summary.png`,`It throws a runtime error since arrays are not valid names`,`Three separate baseline files, one per array element`,`A nested path like __snapshots__/checkout/step-2/summary-chromium.png, organizing related baselines into subfolders instead of a single flat directory`],3,`Array-form names map to a nested directory structure under __snapshots__.`],[`O`,`I`,`Why separate baseline names per locale here?`,`for (const locale of ['en-US', 'de-DE']) {
  test(\`homepage - \${locale}\`, async ({ browser }) => {
    const context = await browser.newContext({ locale });
    const page = await context.newPage();
    await page.goto('/');
    await expect(page).toHaveScreenshot(\`home-\${locale}.png\`);
  });
}`,[`Translated text can be longer/shorter and wrap or resize elements differently, so the genuinely correct rendered appearance differs by locale — comparing German against an English baseline would produce constant false failures`,`Because Playwright requires a unique name per test regardless of content`,`Locale has no effect on the DOM at all`,`It is only a naming convention with no functional purpose`],0,`Different locales can legitimately produce different, equally-correct layouts.`],[`O`,`B`,`What must happen to get an .mp4 version for a stakeholder who needs one?`,`// use: { video: 'on' }
// after test run:
// test-results/.../video.webm`,[`Nothing — Playwright automatically produces both formats`,`Convert the .webm file with an external tool (e.g. ffmpeg) — Playwright records and saves video only in WebM format, with no built-in conversion step`,`Rename the file extension from .webm to .mp4`,`Change video: 'on' to video: 'mp4' in the config`],1,`Playwright has no built-in transcoding — converting formats requires an external tool.`],[`O`,`A`,`What does a.png look like regarding scrollbars, by default?`,`// default behavior, no special config
await page.setViewportSize({ width: 800, height: 400 });
await page.goto('/long-content-page');
await page.screenshot({ path: 'a.png' });`,[`Scrollbars are always visible in the captured image`,`The screenshot fails because the content overflows the viewport`,`Scrollbars are hidden in the captured screenshot by default (hideScrollbars behavior), even though the page visibly has scrollable overflow`,`It captures only the visible scrollbar, not the page content`],2,`hideScrollbars is a default behavior specifically meant to reduce OS-specific rendering noise in screenshots.`],[`O`,`B`,`What baseline filenames are generated for these two calls?`,`test('renders the pricing table correctly', async ({ page }) => {
  await page.goto('/pricing');
  await expect(page).toHaveScreenshot();
  await expect(page).toHaveScreenshot();
});`,[`Both calls overwrite the same file, named after the test`,`It throws an error because a name is required`,`Randomly generated UUID filenames`,`Names derived from the test title with an incrementing suffix, e.g. renders-the-pricing-table-correctly-1.png and renders-the-pricing-table-correctly-2.png`],3,`Playwright appends an incrementing index when multiple unnamed toHaveScreenshot() calls occur in the same test.`],[`O`,`I`,`What is the benefit of expect.soft here?`,`for (const name of ['header', 'sidebar', 'footer']) {
  await expect.soft(page.getByTestId(name)).toHaveScreenshot(\`\${name}.png\`);
}
// test continues even if header.png fails`,[`If header.png's comparison fails, the loop still checks sidebar and footer in the same run, so a single test execution reports every component that changed instead of stopping at the first failure`,`It makes the screenshots capture faster`,`It automatically retries only the failed component`,`It disables the diff image generation for soft failures`],0,`Soft assertions accumulate failures instead of aborting the test at the first one.`],[`O`,`A`,`Why does simply changing deviceScaleFactor break every existing baseline here?`,`// before: use: { deviceScaleFactor: 1 }
// after:  use: { deviceScaleFactor: 2 }
await expect(page).toHaveScreenshot('logo.png'); // now fails for every test`,[`deviceScaleFactor only affects video recordings, not screenshots`,`The captured image's pixel dimensions double at deviceScaleFactor: 2, so it no longer matches the previously committed baseline's dimensions at all — this requires regenerating every baseline, not a rendering bug`,`It is a bug in Playwright that needs a patch`,`It only affects screenshots on mobile viewport projects`],1,`A pure dimension mismatch alone is enough to fail comparison, independent of any actual visual content change.`],[`O`,`I`,`Is raising maxDiffPixels to 60 here a reasonable response?`,`// Diff report shows: 40 pixels differ, scattered randomly across anti-aliased text edges
await expect(page).toHaveScreenshot('label.png', { maxDiffPixels: 60 });`,[`No, any pixel difference always indicates a real bug that must be fixed in the app`,`No, maxDiffPixels can never be safely increased`,`Yes — scattered single-pixel differences concentrated on text edges are classic anti-aliasing noise, not a real regression, so a modest, deliberate tolerance increase is appropriate rather than treating it as a bug`,`Yes, but only if the tolerance is set to at least 1000`],2,`The scattered, text-edge nature of the diff is a strong signal this is rendering noise rather than a real layout regression.`],[`O`,`B`,`What is the purpose of maskColor here?`,`await expect(page).toHaveScreenshot('profile.png', {
  mask: [page.getByTestId('avatar')],
  maskColor: '#FF00FF',
});`,[`It changes the pixel tolerance for the masked region`,`It applies a magenta filter to the entire page`,`It is required for mask to function at all`,`It makes the masked avatar region render as an obvious, easily-recognized magenta block in the diff/baseline image, so reviewers can clearly see it was intentionally excluded rather than mistaking it for an actual visual bug`],3,`A distinct mask color is primarily about reviewer clarity, not comparison behavior.`],[`O`,`I`,`If the test fails only because of the toHaveScreenshot mismatch, what artifacts does the report contain?`,`// playwright.config.ts
use: { screenshot: 'only-on-failure' },
// in a test:
await expect(page).toHaveScreenshot('hero.png');`,[`Both — a generic "on failure" debugging screenshot from the use.screenshot config, AND the toHaveScreenshot expected/actual/diff comparison images, since they're two independent mechanisms both triggered by this failing test`,`Only the toHaveScreenshot diff images, since use.screenshot is unrelated`,`Only the use.screenshot debugging image`,`Neither, since only one screenshot mechanism can be active at a time`],0,`use.screenshot and toHaveScreenshot() are separate, independently-triggered mechanisms that both attach evidence on failure.`],[`O`,`A`,`What happened on the retry here?`,`// playwright.config.ts: retries: 1
test('hero renders correctly', async ({ page }) => {
  await page.goto('/');
  await expect(page).toHaveScreenshot('hero.png'); // fails first, passes on retry
});`,[`Playwright reused the first attempt's failing screenshot and just re-compared it`,`The entire test re-ran from scratch, including navigating again and capturing a brand-new screenshot to compare — if the first failure was caused by a transient rendering timing issue that didn't recur, the retry can legitimately pass`,`Retries always automatically update the baseline`,`The second attempt used a cached DOM snapshot instead of a fresh render`],1,`A full re-run means whatever transient condition caused the first failure may simply not happen again.`],[`O`,`I`,`Why include both assertions instead of only the screenshot here?`,`await page.getByRole('button', { name: 'Place order' }).click();
await expect(page.getByText('Order confirmed')).toBeVisible();
await expect(page).toHaveScreenshot('confirmation.png');`,[`toHaveScreenshot() requires a preceding functional assertion to run`,`It makes the test run in parallel across browsers`,`If something breaks, the text assertion immediately tells you WHETHER the confirmation actually appeared (a content/logic bug) versus the screenshot only telling you pixels changed somewhere, which is far less actionable on its own`,`It is required to avoid a Playwright warning`],2,`Pairing functional and visual assertions gives a much more diagnosable failure than either alone.`],[`O`,`B`,`Why run this as a CI job rather than on a developer's laptop?`,`# CI job, run once intentionally
npx playwright test --update-snapshots
git add __snapshots__ && git commit -m 'update visual baselines'`,[`Because --update-snapshots does not work on local machines`,`Because it is faster to run in CI than locally`,`Because CI has more storage available than local disks`,`The baselines end up rendered in the exact OS/browser/font environment that regular CI runs will later compare against, eliminating any local-machine-vs-CI rendering mismatch for those specific baselines`],3,`Matching the generation environment to the comparison environment removes an entire class of false failures.`],[`O`,`I`,`Why might a team deliberately limit visual regression coverage to a small subset of high-traffic pages like this?`,`// Only these 3 of 200 spec files include toHaveScreenshot assertions:
// homepage.spec.ts, checkout.spec.ts, pricing.spec.ts`,[`Visual assertions carry ongoing maintenance cost (baseline review, occasional flakiness) that isn't worth paying on every page — concentrating coverage on the highest-value, most stable pages gives good ROI without the overhead of maintaining hundreds of baselines`,`Playwright technically limits how many files can use toHaveScreenshot()`,`It is required because toHaveScreenshot() is slow to run`,`Visual regression is legally restricted to 3 pages per project`],0,`Selective coverage trades exhaustiveness for a better cost/value ratio on the pages that matter most.`],[`O`,`A`,`Are the "snapshots" from tracing.start and the aria snapshot assertion related here?`,`await context.tracing.start({ snapshots: true });
...
await expect(nav).toMatchAriaSnapshot();
await context.tracing.stop({ path: 'trace.zip' });`,[`Yes, toMatchAriaSnapshot() reads its baseline from the trace file`,`No — tracing's snapshots:true powers the Trace Viewer's interactive DOM inspector for manual debugging with no pass/fail concept, while toMatchAriaSnapshot() is a separate, independent test assertion comparing the accessibility tree to a committed baseline`,`Yes, they must always be used together or both fail`,`No, using both together causes a config conflict`],1,`These are two entirely separate mechanisms that happen to share the word "snapshot" in their names.`],[`O`,`I`,`How do these two mechanisms complement each other here?`,`// playwright.config.ts
expect: { toHaveScreenshot: { stylePath: './test-overrides.css' } }
// test-overrides.css hides scrollbars and carets
await expect(page).toHaveScreenshot('form.png', { mask: [page.getByTestId('timestamp')] });`,[`They both do the exact same thing, so one is redundant`,`mask disables stylePath for that specific assertion`,`stylePath globally neutralizes cosmetic rendering noise (scrollbars, carets) across all screenshots via CSS, while mask handles content-level dynamism (a specific timestamp value) on a per-assertion basis — together they remove two different classes of false-positive sources`,`stylePath only works when combined with a mask option`],2,`stylePath addresses global rendering-noise sources, while mask targets specific dynamic content per assertion — different problems, complementary fixes.`],[`R`,`I`,`A design system component gets restyled frequently (new colors/spacing each sprint) but its structure/content rarely changes. Pixel-based visual regression keeps needing baseline updates. What's a lower-maintenance alternative?`,null,[`Remove all automated testing for that component`,`Increase maxDiffPixelRatio to 0.5 to tolerate any change`,`Only test the component manually before each release`,`Use toMatchAriaSnapshot() to assert on the component's structural/semantic content instead of (or alongside) pixel screenshots, since it won't fail on pure restyling and only catches structural/content regressions`],3,`An ARIA snapshot assertion is immune to purely cosmetic restyles, unlike a pixel comparison.`],[`R`,`A`,`A dashboard shows both a live-updating "last refreshed" timestamp AND several charts whose exact pixel rendering depends on the current time of day. What's a robust combination of techniques to visually test this dashboard reliably?`,null,[`Use page.clock.setFixedTime() to freeze the app's notion of the current time before navigating (so time-dependent chart rendering is deterministic), and additionally mask the timestamp element in toHaveScreenshot() as a belt-and-suspenders safeguard`,`Only run the test at the exact same time of day every day`,`Delete the timestamp element from the DOM before the test`,`Rely solely on a very large maxDiffPixels tolerance`],0,`Freezing time addresses time-dependent rendering broadly, while masking the timestamp specifically guards against anything freezing time doesn't fully cover.`],[`R`,`I`,`After a large redesign touching 60 pages, how would you set up a controlled process to regenerate all visual baselines without accidentally approving unrelated bugs introduced in the same release?`,null,[`Run --update-snapshots locally and push without review`,`Run --update-snapshots on a dedicated branch/CI job against the redesign code, then have reviewers inspect the generated diff images in the HTML report (comparing old vs new) before merging, treating it as a reviewed step rather than an automatic bulk approval`,`Delete all baselines and let the first CI run silently create fresh ones`,`Manually take 60 screenshots by hand and copy them into __snapshots__`],1,`Treating bulk baseline regeneration as a reviewed step catches any unintended regression hidden among the intentional redesign changes.`],[`R`,`A`,`Your app supports dark mode and right-to-left (RTL) locales, and you want visual coverage of the combinations without an unmanageable number of baseline files. What's a pragmatic scoping approach?`,null,[`Test every page in every combination for completeness`,`Skip dark mode and RTL testing entirely`,`Apply full visual coverage to only the highest-value pages across the combination matrix (e.g. homepage in light/dark x LTR/RTL), rather than every page in every combination, accepting that exhaustive coverage isn't worth the maintenance cost`,`Test only the light mode, LTR combination since it is the default`],2,`Scoping to the highest-value pages balances real coverage against the maintenance cost of an exponentially growing combination matrix.`],[`R`,`I`,`You want to catch visual regressions in a transactional email's HTML template, which isn't rendered inside your normal app pages. How can Playwright still help?`,null,[`Playwright cannot test email templates at all`,`Send a real email and manually inspect it each time`,`Convert the email template to a PDF and diff the PDF bytes`,`Load the rendered email HTML directly into a page via page.setContent() (or serve it locally), then run the normal toHaveScreenshot() assertion against that page just like any other UI`],3,`page.setContent() lets you render arbitrary HTML (including an email template) as a normal page for the purposes of visual testing.`],[`R`,`A`,`Design and engineering are on different teams. What review workflow best ensures a --update-snapshots baseline change actually reflects an approved design decision, not just "the code did something different"?`,null,[`Attach the generated diff images to the PR (or a design-review tool) and require explicit sign-off from a designer or design-system owner before the updated baseline is merged`,`Let any engineer approve baseline changes unilaterally to move fast`,`Skip design review entirely for visual changes under 5% diff`,`Auto-merge baseline updates nightly without any review`],0,`Making the diff visible to design stakeholders keeps baseline updates from silently drifting away from intended design.`],[`R`,`I`,`Your team wants component-level visual regression tests that don't depend on the full app being deployed. How could Playwright be used against components rendered in isolation (e.g. via a Storybook-style harness)?`,null,[`This requires a different tool entirely; Playwright only works against full apps`,`Point Playwright at the isolated component-rendering harness's URL for each component/story, and use locator.screenshot() or toHaveScreenshot() scoped to that component, keeping these tests fast and independent of full-app state`,`Mock the entire component tree using page.route()`,`Manually screenshot each component in a design tool instead`],1,`Playwright works against any URL — an isolated component harness is just another page to navigate to and assert on.`],[`R`,`A`,"A chart is rendered entirely on an HTML `<canvas>`, so there's no meaningful DOM structure to assert against with functional locators. How do you meaningfully test that it renders correctly?",null,[`Canvas content cannot be tested with Playwright at all`,`Assert on the canvas element's width and height attributes only`,`Visual regression (toHaveScreenshot() scoped to the canvas element) is one of the few practical ways to verify canvas-rendered content, since the pixels themselves are effectively the only observable output`,`Extract the chart's underlying data via page.evaluate() and skip visual testing entirely`],2,`For canvas content, there's no DOM structure to inspect functionally — pixel comparison is one of the few meaningful checks available.`],[`R`,`I`,`A page briefly shows a loading spinner before real content renders, and the visual regression screenshot is flaky depending on exact timing. What's the most robust fix?`,null,[`Add a fixed 3-second sleep before every screenshot in the suite`,`Increase maxDiffPixels to tolerate the spinner's presence`,`Disable the spinner in the app code specifically for tests`,`Explicitly wait for the spinner to be hidden (or the real content to be visible) before calling toHaveScreenshot(), rather than relying on a fixed delay or hoping the timing works out`],3,`An explicit wait on the actual completion signal is more robust than any fixed-duration guess.`],[`R`,`A`,`For your most business-critical flow (checkout), would you rely primarily on toHaveScreenshot() assertions or functional assertions (toHaveText, toBeVisible, etc.) to catch regressions, and why?`,null,[`Functional assertions should be primary, since they directly verify the flow actually works; a small number of targeted visual assertions can supplement them to catch cosmetic regressions, but visual checks alone wouldn't catch a functional bug that happens to render pixel-identical to success`,`Rely purely on toHaveScreenshot() since it catches everything`,`Rely purely on manual QA for checkout, skip automation entirely`,`Neither — checkout should only be tested via API calls`],0,`A pixel-perfect screenshot says nothing about whether the underlying business logic (totals, confirmation) is actually correct.`],[`S`,`A`,`A toMatchAriaSnapshot() test starts failing after a developer says they "only changed some CSS classes." What does this failure most likely reveal?`,null,[`ARIA snapshot tests are simply unreliable and should be ignored`,`The change wasn't purely cosmetic — something about the accessible role, name, or structure of the element actually changed (e.g. a div became a button, or an aria-label changed), which the CSS-only framing in the PR description missed`,`The baseline file itself is corrupted`,`toMatchAriaSnapshot() always fails after any deploy regardless of changes`],1,`ARIA snapshots specifically catch structural/semantic drift, so a failure here suggests the change wasn't as purely cosmetic as described.`],[`S`,`I`,`A visual regression test fails showing dozens of scattered single-pixel differences across text throughout the page, right after upgrading the OS's font rendering library in CI. Is this most likely a code regression?`,null,[`Yes, any diff at all always indicates a genuine application bug`,`Yes, but only if more than 100 pixels differ`,`No — this pattern (widespread, tiny, text-edge differences with no code change) is a classic sign of an environment/font-rendering shift, not an actual application bug; the fix is regenerating baselines in the updated environment, not investigating app code`,`No, but the test should simply be deleted rather than fixed`],2,`The pattern (widespread, tiny, text-edge, correlated with an environment change) is the signature of rendering noise, not a real bug.`],[`S`,`A`,`A visual regression diff shows one clean, solid rectangular block of difference exactly matching a component's dimensions, and a recent PR touched that exact component's layout code. What should the reviewer conclude?`,null,[`This is definitely rendering noise and can be safely ignored`,`The baseline file must be corrupted`,`It should be dismissed since scattered pixel diffs are always noise`,`This is very likely a genuine, deliberate (or accidental) layout change from that PR — a solid contiguous diff block, especially correlated with a code change to that exact area, should be reviewed as a real change and either approved or flagged as a bug`],3,`A contiguous, structural diff correlated with a matching code change is a strong signal of a genuine, reviewable change.`],[`S`,`I`,`A newly added German locale variant of a page immediately fails toHaveScreenshot() because no baseline exists for it yet, and a teammate assumes this means the German translation broke the layout. What's actually going on?`,null,[`Nothing is necessarily broken — a baseline simply hasn't been generated for this new locale/page combination yet; the correct step is to run --update-snapshots for it, review the result, and commit it as the new reference, not to assume a regression`,`The German translation is indeed broken and must be fixed immediately`,`toHaveScreenshot() cannot support multiple locales at all`,`The test file itself has a syntax error`],0,`A missing baseline for a genuinely new scenario is expected and simply needs a first-time approval, not investigation as a bug.`],[`S`,`A`,`QA files a bug saying "the header appears twice on the page" based on a fullPage screenshot from an automated test, but manually scrolling the real page shows the header only once, correctly pinned at the top. What's the likely explanation?`,null,[`The real production page genuinely has a duplicate header that manual testing missed`,`This is very likely a fullPage screenshot stitching artifact caused by a position: sticky/fixed header re-appearing in each stitched viewport segment — not a real rendering bug in the actual live page`,`fullPage screenshots always duplicate the entire page content`,`The automated test's browser version renders differently from the real one`],1,`fullPage stitching artifacts with sticky/fixed elements are a well-known false-positive source, distinct from genuine live-page bugs.`],[`S`,`I`,"A visual test fails roughly 1 in 10 runs with a spinner still visible in the screenshot, and the team's response has been to just add `retries: 2` to make it pass reliably in CI. What risk does this masking introduce?",null,[`There is no risk — retries are always the correct fix for any flaky test`,`Retries always slow down the entire CI pipeline unacceptably`,`It hides a real, unaddressed timing issue (the spinner sometimes not finishing before the screenshot) rather than fixing it — the underlying flakiness could also indicate an actual intermittent slow-loading bug worth investigating`,`It permanently corrupts the baseline file`],2,`Papering over flakiness with retries can mask a genuine, intermittent performance issue rather than fixing the actual root cause.`],[`S`,`A`,`A new dark-mode visual test is failing with a huge diff (nearly the whole page), but a screenshot review shows the actual capture looks correct in dark mode. What misconfiguration is likely?`,null,[`toHaveScreenshot() does not support dark mode under any configuration`,`The screenshot library has a rendering bug specific to dark themes`,`maxDiffPixelRatio must always be set higher for dark mode tests`,`The test is comparing against a baseline that was actually generated in light mode (e.g. wrong/reused snapshot name, or colorScheme wasn't actually applied when the original baseline was captured) — the fix is regenerating the correctly-named dark-mode-specific baseline`],3,`A near-total-page diff despite a visually correct capture points to comparing against the wrong (mismatched-mode) baseline.`],[`S`,`I`,`A screenshot occasionally shows an unrelated navigation link in its hover-highlighted state, and a developer suspects a CSS bug causing random highlighting in production. How would you determine whether this is a real production issue or a test artifact?`,null,[`Check whether the test's own mouse actions from a prior step left the cursor resting over that link — if so, it's a test-only artifact from real hover CSS being triggered by leftover cursor position, not a production bug; manually reproducing without that residual mouse position would confirm this`,`Assume it is a production bug and file it immediately without further checking`,`This can never be a test artifact since hover requires real user interaction`,`Rerun the test with headless: false and expect the bug to disappear automatically`],0,`Checking the test's own prior mouse movements is the direct way to distinguish a genuine app bug from a self-inflicted test artifact.`],[`S`,`A`,`A team added toHaveScreenshot() to nearly every one of 500 test files, and now the suite takes hours and requires constant baseline maintenance after every minor design tweak. What's the recommended course correction?`,null,[`Keep the current coverage but run the suite less frequently`,`Scale back visual regression coverage to a curated set of high-value, stable pages/components, relying on functional assertions elsewhere — exhaustive visual coverage has diminishing returns relative to its growing maintenance and runtime cost`,`Switch to comparing screenshots as raw byte-for-byte file diffs to speed things up`,`Disable all visual tests permanently since they became too costly`],1,`Selective, high-value visual coverage balances regression detection against runtime and maintenance cost far better than exhaustive coverage.`],[`S`,`I`,`Someone configured the suite to save \`type: 'jpeg'\` screenshots and then wired a homemade diffing script to compare them run-to-run, and it reports "differences" even when nothing visibly changed. Why?`,null,[`JPEG files cannot be compared programmatically at all`,`The diffing script has a bug unrelated to image format`,`JPEG's lossy compression introduces small, non-deterministic-looking artifacts on each encode, so even pixel-identical renders can produce slightly different JPEG bytes — lossless PNG (as toHaveScreenshot() uses internally) avoids this problem entirely`,`JPEG images are always exactly 50% the size of PNG, causing false diffs`],2,`Lossy compression artifacts are exactly why toHaveScreenshot() always uses lossless PNG for comparison, not JPEG.`],[`D`,`I`,"A `fullPage: true` screenshot is shorter than the actual page and cuts off content near the bottom, even though scrolling manually shows more content below. What should you check?",null,[`Whether fullPage: true is spelled correctly in the code`,`Whether the viewport height is set too small`,`Whether the PNG format has a maximum height limit`,`Content lower on the page may be lazily rendered/inserted after scroll (infinite scroll or lazy sections) and hadn't loaded yet when fullPage capture measured the page's height — trigger/wait for that content to load before capturing`],3,`fullPage capture measures the page height at a point in time — content that lazily appears afterward won't be included.`],[`D`,`A`,"A `mask: [page.getByTestId('timestamp')]` option doesn't seem to hide anything, and the dynamic timestamp still causes diff failures. What's a likely cause?",null,[`The locator may not be matching any element at the moment the screenshot is taken (e.g. wrong test id, or the element hasn't rendered yet) — masking silently does nothing for a locator that resolves to zero elements`,`mask only works with CSS selector strings, not locators`,`mask requires the element to be off-screen to work`,`mask only applies to full-page screenshots, not element screenshots`],0,`A mask locator that matches nothing has nothing to mask — verify the locator actually resolves to the intended element first.`],[`D`,`I`,"A `toMatchAriaSnapshot()` assertion fails with what looks like an identical tree when visually compared to the expected snapshot string. What's a subtle cause worth checking?",null,[`toMatchAriaSnapshot() is inherently unreliable and should not be trusted`,`Minor differences in indentation, ordering, or exact wording of accessible names/roles in the snapshot string can cause a mismatch even when the tree looks equivalent at a glance — regenerate the snapshot with the update flag and diff carefully rather than hand-editing the expected string`,`The test must be run in headed mode for ARIA snapshots to work`,`ARIA snapshots ignore all whitespace differences automatically`],1,`Subtle textual differences in a hand-written expected snapshot are easy to miss but will still cause a mismatch.`],[`D`,`A`,"`video: 'on'` is set, but the test-results folder has no video file for a test that ran and passed. What's the most likely explanation?",null,[`Video files are only saved for failed tests regardless of config`,`video: 'on' only works with headed mode`,`Video files are only finalized and written when the browser context closes — if the test (or a custom fixture) doesn't properly close the context at the end, or an unusual crash prevents cleanup, the video may never be flushed to disk`,`The video was saved but with a hidden file attribute`],2,`Video finalization happens at context close — anything preventing a clean context shutdown can prevent the video from ever being written.`],[`D`,`I`,"The `caret: 'hide'` option doesn't seem to remove a visible blinking cursor artifact from a screenshot. What should you verify?",null,[`That the screenshot is being saved as PNG, not JPEG`,`That caret: 'hide' is spelled with capital letters`,`That the input field has a maxlength attribute set`,`Whether the target element (or the right one) actually has focus at the moment of the screenshot — caret: 'hide' only affects the caret in a genuinely focused, editable element; if focus moved elsewhere, there's nothing for it to hide, and the artifact may be something else entirely`],3,`caret: 'hide' only suppresses a real text caret in a focused editable field — an unrelated blinking artifact needs a different explanation.`]],pom:[[`O`,`B`,`What problem does the Page Object Model primarily solve?`,null,[`Slow test execution due to repeated browser launches`,`DOM locators and interactions scattered across many tests — a UI change requires updating every test`,`Playwright lacking built-in assertion methods`,`Tests not running in parallel`],1,`POM centralises locators and actions in one class — a UI change is fixed in one place, not across every test.`],[`O`,`B`,`What does the constructor of a page object receive?`,null,[`The test name and assertion count`,`The Playwright Page object (and optionally context or request)`,`The URL of the page to navigate to`,`The list of locators to use`],1,`The page object wraps a Page instance — the constructor is how it gets access to browser interactions.`],[`O`,`I`,"Why are locators declared as `readonly` properties rather than computed inside each method?",null,[`readonly prevents tests from accidentally overwriting them`,`They are lazy — evaluated on each access so they always reflect the current DOM — and declaring them as properties keeps them DRY`,`readonly properties are faster than method-computed locators`,`They enable TypeScript to infer types automatically`],1,`Playwright locators are lazy references — a readonly property re-queries the DOM every time it is used.`],[`O`,`B`,`What should page object action methods return?`,null,[`The expect() result so tests can chain assertions`,`Usually nothing (void) or the next page object for chained navigation — assertions should stay in the test`,`A boolean: true if the action succeeded`,`The locator they interacted with`],1,`Page objects are action libraries — they execute interactions and leave assertions to the test.`],[`O`,`I`,`What is the "actions in PO, assertions in test" rule?`,null,[`Page objects must call at least one expect() to verify the action succeeded`,`Assertions (expect()) belong in the test; page objects only perform interactions — this keeps POs reusable and assertions visible`,`Page objects should throw errors instead of using expect()`,`Actions and assertions can be mixed freely`],1,`Mixing expect() into page objects makes them context-dependent and harder to reuse across different test scenarios.`],[`O`,`B`,`What is a component object in the POM pattern?`,null,[`A page object that only tests React components`,`A class representing a reusable UI component (header, modal, table) that is composed into full page objects`,`A Playwright built-in for testing web components`,`A fixture that provides DOM elements`],1,`Component objects mirror the UI's component architecture — reusable across multiple page objects.`],[`O`,`I`,"How does the fixture system improve POM usage compared to `new LoginPage(page)` in every test?",null,[`Fixtures are faster because they cache the page object instance`,`Fixtures handle instantiation and teardown centrally — tests request the page object by name without knowing how to create it`,`new LoginPage(page) is deprecated in favour of fixtures`,`Fixtures provide TypeScript types automatically`],1,"Fixture-based POM means tests just destructure `{ loginPage }` — the fixture handles construction and any teardown."],[`O`,`I`,`When a UI element's label changes from "Sign in" to "Log in", how many files need updating with POM?`,null,[`Every test that clicks the sign-in button`,`Only the page object class that defines the submitButton locator`,`The playwright.config.ts and every spec file`,`Both the page object and a central locator config file`],1,`This is POM's core benefit — one change propagates automatically to all tests.`],[`O`,`B`,"What TypeScript modifier is conventionally used for the `page` parameter stored in a page object class?",null,[`public readonly`,`protected`,`private readonly`,`static`],2,`private readonly ensures the page reference is not accessible outside the class and cannot be reassigned.`],[`O`,`I`,`What does composing component objects inside a page object look like?`,null,[`Importing and calling the component object's static methods`,"Declaring the component as a readonly property: `readonly header = new Header(this.page)`","Extending the component class with `extends`",`Passing the component class as a constructor parameter`],1,`Composition (has-a) is preferred over inheritance (is-a) — a DashboardPage has a Header, it is not a Header.`],[`O`,`I`,`What is the recommended directory structure for page objects?`,null,[`Place all page objects inside the tests/ directory alongside specs`,`Place them in a pages/ or page-objects/ directory separate from specs — import them into tests or fixtures`,`Name them the same as the spec files they are used in`,`Store them in playwright.config.ts as named exports`],0,`A dedicated pages/ directory keeps page objects separate from tests and easy to discover.`],[`O`,`A`,`Can a page object method navigate to a different page and return a new page object for that page?`,null,[`No — page objects can only interact with their own page`,"Yes — a method can navigate and return `new NextPage(this.page)` enabling chained fluent-style usage",`Only if the page object extends a base NavigablePage class`,`Only if the test re-instantiates the page object after navigation`],1,"Returning the next page object from a navigation action enables fluent chaining: `await loginPage.signIn(…)` returns a `DashboardPage`."],[`F`,`B`,"Complete the constructor to store the page reference.\n```ts\nconstructor(______ readonly page: Page) {}\n```",null,[`private`,`public`,`protected`,`readonly`],0,`private readonly is the idiomatic TypeScript shorthand for a private constructor parameter stored as a property.`],[`F`,`I`,"Complete the locator declaration.\n```ts\n______ submitButton = this.page.getByRole('button', { name: 'Sign in' });\n```",null,[`readonly`,`private`,`const`,`let`],0,`readonly prevents accidental reassignment; the locator is re-evaluated lazily on each access.`],[`F`,`B`,`Complete the action method signature.
\`\`\`ts
______ signIn(email: string, pw: string) {
  await this.emailInput.fill(email);
  await this.passwordInput.fill(pw);
  await this.submitButton.click();
}
\`\`\``,null,[`async`,`public async`,`private async`,`static async`],0,`async is required because the method calls await on Playwright actions.`],[`F`,`I`,"Complete the fixture to provide the page object.\n```ts\nloginPage: async ({ page }, use) => {\n  await ______(new LoginPage(page));\n},\n```",null,[`use`,`provide`,`inject`,`return`],0,`use() hands the page object to the test and marks the boundary between setup and teardown.`],[`F`,`I`,"Complete the goto method.\n```ts\nasync goto() {\n  await this.page.______('/login');\n}\n```",null,[`goto`,`navigate`,`open`,`visit`],0,`page.goto() is the Playwright navigation method.`],[`F`,`I`,"Complete to compose a component object inside a page object.\n```ts\nreadonly header = new ________(this.page);\n```",null,[`Header`,`HeaderComponent`,`page.Header`,`Component`],0,`Instantiate the component class with the same page reference — composition over inheritance.`],[`F`,`A`,`Complete to return a new page object after navigation.
\`\`\`ts
async signIn(email: string, pw: string): Promise<______> {
  await this.signInForm.fill(email, pw);
  return new DashboardPage(this.page);
}
\`\`\``,null,[`DashboardPage`,`Page`,`void`,`LoginPage`],0,`Returning the next page object enables fluent chaining in tests.`],[`E`,`B`,`What is wrong?
\`\`\`ts
export class LoginPage {
  async signIn(email: string, pw: string) {
    const emailInput = this.page.getByLabel('Email');
    await emailInput.fill(email);
    await expect(emailInput).toBeVisible(); // assertion in PO
  }
}
\`\`\``,null,[`getByLabel is not allowed inside page objects`,`Assertions inside page objects couple them to a specific test scenario and make them harder to reuse — move expect() to the test`,`emailInput must be a readonly class property`,`async methods cannot contain expect()`],1,`Assertions in page objects reduce reusability — a method that asserts visibility can't be used when you expect the element to be hidden.`],[`E`,`I`,`What is wrong?
\`\`\`ts
export class LoginPage {
  emailInput = this.page.getByLabel('Email'); // no readonly
}
// Test:
page.loginPage.emailInput = page.getByLabel('Password'); // accidentally overwritten
\`\`\``,null,[`Locators cannot be class properties — use methods instead`,`Missing readonly allows the locator to be accidentally reassigned from outside the class`,`page.loginPage is not valid TypeScript`,`getByLabel cannot be used as a class property`],1,`readonly prevents accidental overwrites — always declare locator properties as readonly.`],[`E`,`B`,"What is wrong?\n```ts\n// In every test file:\nconst loginPage = new LoginPage(page);\n```\nInstead of using a fixture.",null,[`new LoginPage(page) is not supported — page objects must be created with test.extend()`,`This works but is repetitive — the fixture pattern centralises construction and makes the page object available by destructuring`,`page objects must be singletons`,`The page argument cannot be passed to a constructor`],1,`Direct instantiation is repetitive — a fixture creates and provides the page object once, shared by all tests that request it.`],[`E`,`I`,"What is wrong?\n```ts\nexport class DashboardPage extends LoginPage {\n  // Dashboard inherits login functionality\n}\n```",null,[`TypeScript classes cannot extend Playwright page objects`,`DashboardPage is not a LoginPage — inheritance is the wrong relationship; prefer composition (DashboardPage has a LoginPage helper) or keep them separate`,`extends requires calling super() with the page argument`,`Inheritance breaks Playwright's page lifecycle`],1,`Page objects model pages, not roles — a DashboardPage is not a LoginPage. Use composition or a shared base class only for truly shared utilities.`],[`E`,`I`,`What is wrong?
\`\`\`ts
export class ProductPage {
  readonly addToCart = this.page.getByRole('button', { name: 'Add to cart' });
  constructor(private readonly page: Page) {}
}
\`\`\``,null,[`readonly properties must come after the constructor in TypeScript`,`The readonly property initialiser runs before the constructor body — this.page is undefined at that point in this order`,`getByRole must be called inside a method, not as a property`,`Page objects cannot have both a constructor and readonly properties`],1,"Class property initialisers run after the constructor parameters are bound — this.page is available. However TypeScript requires field declarations before the constructor or after — this specific order is fine; the real gotcha is if page is not yet assigned. Actually in TypeScript with constructor shorthand (`constructor(private readonly page: Page)`), the property initializers that reference `this.page` DO have access because the parameter property is assigned before field initializers run. So this is actually valid TypeScript. Let me reconsider this error question... Actually I think this is fine code in TypeScript. The constructor shorthand initializes the property, then field initializers run. So `this.page` IS available. This is actually correct code. Let me change this error question to something else."],[`E`,`A`,`What is wrong?
\`\`\`ts
export class CheckoutPage {
  constructor(private readonly page: Page) {}
  async fillCard(number: string) {
    const cardInput = await this.page.waitForSelector('input[name=card]');
    await cardInput.type(number);
  }
}
\`\`\``,null,[`type() is deprecated in page objects`,`waitForSelector and type() are legacy Playwright APIs — use locator-based approach: this.page.getByLabel('Card').fill(number)`,`Page objects cannot use async methods`,`waitForSelector must not be awaited`],1,`waitForSelector returns an ElementHandle and type() is the legacy API — use getByLabel/getByPlaceholder and fill() for resilient, auto-waiting locators.`],[`D`,`B`,`A page object method was working but now throws "strict mode violation — found 2 elements". The UI was changed. What should you fix?`,null,[`Disable strict mode globally in the config`,`Update the locator in the page object to be more specific — this is exactly the maintenance benefit POM provides`,`Add .first() to every locator in the page object`,`Create a new page object class for the updated page`],1,`The fix is in one page object class — all tests using it benefit automatically.`],[`D`,`I`,"Tests using `loginPage.signIn()` pass but a new test that checks the error message fails. It can't access `loginPage.errorAlert`. Why?",null,[`errorAlert must be declared inside signIn()`,`errorAlert is not a declared property on the LoginPage class — add it as a readonly locator property`,`Page objects cannot expose locators to tests`,`The error alert locator must be passed as a constructor argument`],1,"Add `readonly errorAlert = this.page.getByRole('alert')` to the class — tests can then use `loginPage.errorAlert` in assertions."],[`D`,`B`,"A `goto()` method navigates to `/login` but the test starts failing because the path changed to `/auth/login`. What is the maintenance process?",null,[`Update the URL in every test that calls loginPage.goto()`,`Update the URL in the single goto() method in LoginPage — all tests immediately use the new path`,`Create a new page object for the new path`,`Use baseURL to avoid hardcoded paths`],1,`This is POM's maintenance payoff — one file, one change, all tests fixed.`],[`D`,`I`,"A component object `Modal` is instantiated with `new Modal(this.page)` inside a page object method. The modal's locator is not found. Why?",null,[`Modals cannot be wrapped in component objects`,`The modal must be opened before the component is instantiated — create the Modal instance as a readonly property so it is always available, or instantiate it inside the method after opening the modal`,`Component objects must be passed the locator, not the page`,`new Modal(this.page) requires an async constructor`],0,`Creating Modal as a readonly property means it is always available regardless of modal state — locators are lazy and won't fail until you actually try to interact with them.`],[`D`,`I`,"Test says `loginPage` is undefined even though the fixture is registered. What is the likely cause?",null,[`Page object fixtures require async: true in the config`,`The test imports from @playwright/test instead of from the extended test module that defines the fixture`,`loginPage must be declared with let before the test`,`Page object fixtures need to be registered in playwright.config.ts`],1,"Always import the extended `test` function — importing from @playwright/test skips all custom fixtures."],[`D`,`A`,"A page object action method calls `await this.page.goto('/checkout')` then returns `new CheckoutPage(this.page)`. But the returned page object's locators don't find elements. What's the timing issue?",null,[`goto() must be called from the test, not from a page object`,`The navigation is async but the CheckoutPage is returned before the page finishes loading — ensure navigation completes before returning`,`this.page is different inside the returned CheckoutPage`,`Page objects cannot navigate — use fixtures for navigation`],1,`await page.goto() waits for load by default, so this should work — but if the page uses client-side routing, ensure you wait for a stable element or URL before returning the new page object.`],[`C`,`B`,"Complete the page object class skeleton.\n```ts\nexport class ProductPage {\n  constructor(______ readonly page: Page) {}\n}\n```",null,[`private`,`public`,`protected`,`readonly`],0,`private readonly is the idiomatic shorthand that stores page as a private, immutable property.`],[`C`,`I`,"Complete to declare a button locator.\n```ts\n______ addToCartBtn = this.page.getByRole('button', { name: 'Add to cart' });\n```",null,[`readonly`,`private`,`const`,`var`],0,`readonly locator properties are lazy and prevent accidental reassignment.`],[`C`,`I`,`Complete the action method.
\`\`\`ts
______ addToCart() {
  await this.addToCartBtn.click();
  await this.page.getByText('Added to cart').waitFor();
}
\`\`\``,null,[`async`,`public async`,`static async`,`private async`],0,`async is required for methods that use await.`],[`C`,`I`,`Complete to register the page object as a fixture.
\`\`\`ts
const test = base.extend<{ productPage: ProductPage }>({
  productPage: async ({ page }, use) => {
    await use(______ ProductPage(page));
  },
});
\`\`\``,null,[`new`,`create`,`make`,`build`],0,`new creates the page object instance — the fixture then hands it to the test via use().`],[`C`,`A`,`Complete the fluent navigation pattern.
\`\`\`ts
async signIn(email: string, pw: string): Promise<______> {
  await this.signInForm.fill(email, pw);
  await this.submitButton.click();
  return new DashboardPage(this.page);
}
\`\`\``,null,[`DashboardPage`,`Page`,`LoginPage`,`void`],0,`The return type declares which page object the caller receives after navigation.`],[`L`,`B`,`Why are Playwright locators suitable as class property initialisers (evaluated once at construction)?`,null,[`They are not — locators must be created fresh inside each method`,`Locators are lazy references — they are re-evaluated against the live DOM each time they are used, so a class property defined once is always up-to-date`,`Locators cache the element at creation time for performance`,`Property initialisers run before the DOM loads, so they need to be lazy`],1,`A locator is a query descriptor, not a stored DOM element — every interaction re-queries the DOM, so a property initialiser is perfectly safe.`],[`L`,`I`,`What is the benefit of returning a new page object from a navigation action?`,null,[`It is required — Playwright throws if you don't return the next page`,"It enables fluent chaining: `const dashboard = await loginPage.signIn(…)` giving the test the correct page object for the next step",`It prevents the original page object from being garbage collected`,`It is a Playwright best practice that improves performance`],1,`Fluent page object chaining models the user journey naturally — each step produces the object you need for the next step.`],[`L`,`I`,`Why is composition (has-a) preferred over inheritance (is-a) for page objects?`,null,[`TypeScript does not support multiple inheritance`,`Pages don't share a meaningful type hierarchy — a DashboardPage is not a LoginPage; Header is a component of both and should be composed in`,`Inheritance breaks Playwright's locator lazy evaluation`,`Component objects cannot extend page object classes`],1,`Inheritance couples unrelated pages together; composition keeps them independent and the shared component reusable.`],[`L`,`B`,`What happens when a page object locator is accessed but the element is not yet in the DOM?`,null,[`It throws immediately — page objects do not auto-wait`,`Nothing — the locator is lazy; it only fails when you perform an action or assertion on it, and Playwright auto-waits at that point`,`It returns null, which the action method must handle`,`The page object re-instantiates itself to retry`],1,`Locators are lazy — accessing the property doesn't query the DOM; the auto-wait happens at the point of interaction.`],[`L`,`A`,`Is it ever appropriate to put assertions inside a page object method?`,null,[`Never — assertions always belong in tests`,`Yes, for "navigation assertions" like \`await expect(page).toHaveURL('/dashboard')\` inside a signInAndNavigate() helper method that is intended to always succeed`,`Only for visual regression assertions`,`Only when using expect.soft()`],1,`A high-level convenience method like signInAndExpectDashboard() can include navigation assertions — it is a complete step, not a reusable building block. The key is intentionality: is this method always expected to succeed?`],[`R`,`B`,`You have 30 tests that all call \`page.getByRole('button', { name: 'Submit' }).click()\`. The button label changes to "Send". What is the POM approach?`,null,[`Use find-and-replace across all 30 test files`,"Extract the locator into a FormPage class as `readonly submitBtn = this.page.getByRole('button', { name: 'Submit' })` and fix it once when it changes",`Use a shared constant for the locator string`,`Add an alias attribute to the button so both locators work`],1,`POM centralises the locator — one update in the page object fixes all 30 tests automatically.`],[`R`,`I`,`A checkout test needs to fill in address, payment, and confirm — each on a separate page. How do you structure the page objects?`,null,[`One giant CheckoutPage class with all locators`,`Three separate page objects (AddressPage, PaymentPage, ConfirmPage) chained via navigation return values`,`One page object per test file`,`Inline all interactions without page objects for complex multi-page flows`],1,`Separate page objects per step model the actual page transitions cleanly and keep each class focused.`],[`R`,`I`,`Multiple page objects (Dashboard, Profile, Settings) all have a navigation header. How do you avoid duplicating the header locators?`,null,[`Copy the header locators into each page object`,"Create a Header component object and compose it: `readonly header = new Header(this.page)` in each page class",`Create a base class with the header locators and have all pages extend it`,`Store header locators in playwright.config.ts`],0,`Composing a Header component avoids duplication and ensures a header change is fixed in one class.`],[`R`,`A`,`You want page objects to be available in all tests without importing them in every file. How?`,null,[`Export them from playwright.config.ts`,`Register each page object as a fixture in a custom test module; import that test object in spec files`,`Use globalSetup to instantiate all page objects`,`Add them to the Playwright test context using test.use()`],1,`A fixture module that exports the extended test object with all page object fixtures is the standard pattern.`],[`R`,`I`,"A page object's `goto()` method navigates to a page that requires authentication. How do you handle it?",null,[`Add login logic inside goto()`,`Use test.use({ storageState: 'auth.json' }) to pre-authenticate the browser context, so goto() navigates to the authenticated page without extra login steps`,`Create a separate AuthPage that goto() calls internally`,`Pass credentials to the page object constructor`],1,`Pre-loading storageState in the config or fixture keeps page object methods clean — they navigate, not authenticate.`],[`S`,`I`,'A junior dev adds `await expect(this.submitButton).toBeVisible()` inside the page object\'s `submit()` method "to be safe". What problem does this introduce?',null,[`expect() cannot be called inside a page object`,`The assertion makes the page object non-reusable — a test that expects submit to be disabled cannot use this method without it failing`,`Visible assertions must be in beforeEach hooks`,`It is correct practice — page objects should self-validate`],1,`Embedding assertions restricts reuse — a submit() that asserts visibility can't be called in a test that verifies the button is disabled before filling the form.`],[`S`,`A`,`A POM class has grown to 600 lines covering a complex product page: images, reviews, pricing, variants, and add-to-cart. How do you refactor this?`,null,[`Split it into multiple spec files — one per feature`,`Break it into component objects (ProductImages, ReviewsSection, PricingBlock, VariantPicker) and compose them inside ProductPage`,`Extract all locators to a separate constants file`,`Use inheritance to split methods across a hierarchy of product page classes`],1,`Component objects mirror the UI's component structure — ProductPage composes them, each component owns its own locators and actions.`],[`S`,`I`,"After enabling `fullyParallel: true`, tests sharing the same page object instance start failing with unexpected state. What is the cause?",null,[`Page objects are not thread-safe and cannot be used with fullyParallel`,`Each parallel test gets its own browser page — as long as each test creates its own page object instance (via fixture), there is no sharing. If a singleton is shared, that causes the issue`,`Page objects must use scope: 'worker' to be thread-safe`,`fullyParallel is incompatible with the fixture pattern`],1,`Fixtures with scope: 'test' create a fresh page object per test — no sharing, no conflicts. A shared singleton outside the fixture system would cause issues.`],[`S`,`A`,`You want to write tests that read like plain English user stories: "Given the user is logged in, When they add an item to cart, Then the cart count increases." How does POM help?`,null,[`POM does not improve test readability — it only reduces maintenance`,"Page objects with well-named action methods make tests read like user stories: `await loginPage.signIn(…); await productPage.addToCart(…); await expect(header.cartCount).toHaveText('1')`",`Use BDD frameworks like Cucumber instead — POM is for unit tests`,`Write the steps as comments alongside raw Playwright calls`],1,`Meaningful page object method names (signIn, addToCart, placeOrder) make tests self-documenting — they describe user intent, not DOM structure.`],[`O`,`B`,`What is the key principle of the Page Object Model regarding test assertions?`,null,[`Page objects must contain all assertions`,`Page objects should NOT contain assertions — they expose locators and actions; the test file is responsible for asserting outcomes. This keeps page objects reusable across tests with different expected outcomes.`,`Assertions go in afterEach hooks only`,`Page objects only handle navigation, not assertions`],1,`Assertion-free page objects are reusable — the same click() method works in both a "should succeed" and "should fail" test.`],[`O`,`I`,"What is the benefit of returning `this` from page object methods?",null,[`It is required by TypeScript`,`Returning this enables method chaining: await loginPage.enterEmail(e).enterPassword(p).submit() — makes tests more readable and fluent`,`It caches the method for performance`,`It allows the page object to be cloned`],1,`Fluent interfaces via method chaining make test steps read naturally in sequence.`],[`O`,`B`,`Should page object constructors navigate to the page URL?`,null,[`Yes — the constructor should call page.goto() for convenience`,`No — the constructor should only store the page reference and initialise locators. Navigation should be an explicit method like goto() — callers control when navigation happens.`,`It depends on whether the test uses fixtures`,`Only if the page object extends BasePage`],1,`Separating construction from navigation gives callers full control — a test that is already on the page won't need to navigate again.`],[`O`,`I`,'What does `this.page.locator(".menu").locator("a")` achieve in a page object?',null,[`A query error — locators cannot be chained`,`Chained locators scope the second selector within the first — "a" tags that are descendants of ".menu". This is cleaner and more robust than CSS combinator strings.`,`It creates two separate locators`,`It selects the first "a" only`],1,`Locator chaining scopes the inner selector within the outer — equivalent to ".menu a" but composable and easier to read.`],[`O`,`A`,`What is the difference between a "page object" and a "component object" in POM architecture?`,null,[`They are the same concept`,`A page object represents a full page/route; a component object represents a reusable UI element (modal, nav, form) that appears on multiple pages — component objects are composed inside page objects to avoid duplicating locators across pages.`,`Component objects extend page objects via inheritance`,`Component objects handle network requests`],1,`Page/component decomposition mirrors the UI architecture — components encapsulate shared UI, pages compose them.`],[`O`,`I`,"Should page object methods be `async`?",null,[`No — only constructors can be async`,`Yes — methods that call Playwright APIs (click, fill, goto, waitFor) must be async/await`,`Only if they navigate between pages`,`Only if they return locators`],1,`Playwright APIs are Promise-based — page object methods that interact with the page must be async and awaited by callers.`],[`O`,`B`,"What is the purpose of a `BasePage` class in a POM architecture?",null,[`It provides default assertions for all pages`,`It holds shared setup (storing the page reference, common locators like nav and header, shared utility methods) — concrete page objects extend BasePage to inherit these without duplicating code.`,`It is required by Playwright to enable POM`,`It contains the globalSetup logic`],1,`BasePage centralises shared boilerplate — nav links, header, footer — while concrete pages add their specific interactions.`],[`O`,`A`,`What TypeScript feature makes page object locators type-safe?`,null,[`Locators are always typed as any`,`Declaring locators as readonly properties of type Locator in the class body: readonly submitBtn: Locator = this.page.locator("button[type=submit]") — TypeScript enforces Locator usage at call sites.`,`Locators are untyped at runtime`,`TypeScript generics on the Page class`],1,`Typed readonly Locator properties give IDE autocomplete and compiler checks on locator usage throughout the test suite.`],[`O`,`I`,`When a UI element appears on every page (e.g. a nav bar), where should its locators live in a POM hierarchy?`,null,[`Duplicate them in every page object for clarity`,`In a shared NavComponent or HeaderComponent object — composed into each page object as a property (this.nav = new NavComponent(page)). Changes to the nav update one file.`,`In a global constants file as CSS strings`,`In the test helper file`],1,`Component objects for shared UI prevent duplication — a single NavComponent serves every page that includes the navigation.`],[`O`,`B`,`What is wrong with storing locators as CSS strings in test files instead of page objects?`,null,[`CSS strings are not valid in TypeScript`,`Each CSS selector is duplicated across every test that uses it — a class rename requires updating every test file. Page objects make selectors single-source-of-truth.`,`CSS selectors cannot be used in Playwright`,`Strings cannot be passed to expect()`],1,`DRY principle: centralise selectors in page objects so a DOM change requires updating one file, not every test.`],[`O`,`I`,`How should a page object method handle a multi-step interaction like "fill and submit a form"?`,null,[`Each field should be a separate public method; submitForm is a fourth method`,`The page object can provide both granular methods (fillEmail, fillPassword) and a convenience method (login(email, password)) that calls them in sequence — this supports both fine-grained and coarse-grained test usage.`,`All form interactions must be in one method`,`Each step should be a separate class`],1,`Both granular and convenience methods serve different test needs — login() for standard tests, fillEmail() for testing partial form state.`],[`O`,`A`,`What is the purpose of the "factory" pattern in POM?`,null,[`Factories create fake test data`,`A factory function or class creates page object instances pre-configured for a specific context (logged-in, specific locale, mobile viewport) — tests call the factory and receive a ready-to-use page object`,`Factories generate locators from CSS paths`,`Factory pattern is for mocking, not POM`],1,`Factories encapsulate context setup — tests receive a ready page object without knowing the initialisation details.`],[`F`,`B`,`Complete the page object constructor.
\`\`\`ts
class SearchPage {
  readonly searchInput: Locator;
  constructor(______: Page) {
    this.searchInput = page.getByRole("searchbox");
  }
}
\`\`\``,null,[`private page`,`readonly page`,`public page`,`private readonly page`],3,`private readonly page: Page — private to prevent external mutation, readonly to signal the reference never changes.`],[`F`,`I`,"Complete to return `this` for chaining.\n```ts\nasync fillEmail(email: string) {\n  await this.emailInput.fill(email);\n  return ______;\n}\n```",null,[`this`,`self`,`page`,`new this()`],0,`return this enables fluent chaining — await page.fillEmail(e).fillPassword(p).submit()`],[`F`,`B`,'Complete to navigate to the page from within the page object.\n```ts\nasync ______() {\n  await this.page.goto("/login");\n}\n```',null,[`goto`,`navigate`,`open`,`visit`],0,`goto() is the conventional name for the page object's navigation method — tests call await loginPage.goto() to navigate.`],[`F`,`I`,`Complete to compose a component object inside a page object.
\`\`\`ts
class DashboardPage {
  readonly nav: NavComponent;
  constructor(private readonly page: Page) {
    this.nav = ______(page);
  }
}
\`\`\``,null,[`new NavComponent`,`NavComponent.create`,`page.component`,`NavComponent.init`],0,`Composition via new NavComponent(page) — the page reference flows from the page object into its component objects.`],[`F`,`A`,`Complete to type-annotate a locator property.
\`\`\`ts
class CartPage {
  readonly checkoutBtn: ______;
  constructor(private readonly page: Page) {
    this.checkoutBtn = page.getByRole("button", { name: "Checkout" });
  }
}
\`\`\``,null,[`Locator`,`ElementHandle`,`HTMLElement`,`PageLocator`],0,`Locator is the Playwright type for lazy element references — import { Locator } from "@playwright/test".`],[`F`,`B`,`Complete to use a page object in a test.
\`\`\`ts
test("login", async ({ page }) => {
  const loginPage = ______(page);
  await loginPage.goto();
});
\`\`\``,null,[`new LoginPage`,`LoginPage.create`,`page.object`,`createPage`],0,`new LoginPage(page) instantiates the page object — the test passes its page fixture to the constructor.`],[`F`,`I`,"Complete to extend a base page.\n```ts\nclass ProductPage ______ BasePage {\n  // ...\n}\n```",null,[`extends`,`implements`,`inherits`,`uses`],0,`extends is the TypeScript keyword for class inheritance.`],[`F`,`A`,`Complete to expose a page object via a fixture.
\`\`\`ts
export const test = base.extend<{ loginPage: LoginPage }>({
  loginPage: async ({ page }, use) => {
    await use(______(page));
  },
});
\`\`\``,null,[`new LoginPage`,`LoginPage`,`createLoginPage`,`page.as(LoginPage)`],0,`new LoginPage(page) creates the instance — the fixture injects it into tests like any other Playwright fixture.`],[`F`,`I`,`Complete to chain page object methods.
\`\`\`ts
await loginPage
  .fillEmail("alice@test.com")
  .______("secret123")
  .______();
\`\`\``,null,[`fillPassword, submit`,`enterPassword, clickSubmit`,`setPassword, confirm`,`typePassword, login`],0,`Method names should match the action intent — fillPassword and submit() follow the fill+submit convention.`],[`E`,`B`,`What is wrong with this page object?
\`\`\`ts
class CartPage {
  async addToCart(item: string) {
    await this.page.locator(".product").filter({ hasText: item }).click();
    expect(await this.page.locator(".cart-count").textContent()).toBe("1");
  }
}
\`\`\``,null,[`filter() is not a valid Locator method`,`The assertion makes addToCart() only usable in tests where the cart has exactly 1 item — a test starting with 2 items in the cart cannot use this method. Remove the assertion from the page object.`,`textContent() requires await`,`Page objects cannot use filter`],1,`Embedding assertions in page objects breaks reusability — move assertions to the test.`],[`E`,`I`,`What is wrong?
\`\`\`ts
class LoginPage {
  constructor(private page: Page) {}
  async goto() { await this.page.goto("/login"); }
  async login() {
    await this.goto(); // navigates in the middle of a session test
    await this.page.fill("#email", "user@test.com");
  }
}
\`\`\``,null,[`page.fill() is deprecated`,`Calling goto() inside login() navigates away from any existing page state — tests that are already on /login and just need to fill credentials will get a double navigation. Separate navigation from interaction.`,`fill() requires getByLabel instead`,`async functions cannot call other async methods directly`],1,`Call goto() explicitly in tests that need it — don't bake navigation into interaction methods.`],[`E`,`B`,`What is wrong?
\`\`\`ts
const loginPage = new LoginPage(page);
await loginPage.login();
const dashPage = new DashboardPage(page);
// different instance but same page — OK?
\`\`\``,null,[`Two page objects cannot share the same page`,`This is fine — creating a new DashboardPage(page) on the same page reference is correct and expected when the app has navigated to the dashboard after login. Each page object is a view model, not an exclusive owner.`,`dashPage will reset the page state`,`Only one page object instance can exist per test`],1,`Multiple page objects sharing the same page reference is correct POM usage — each models a different route/screen.`],[`E`,`I`,`What is wrong?
\`\`\`ts
class NavComponent {
  constructor(private page: Page) {}
  async clickHome() {
    await this.page.getByRole("link", { name: "Home" }).click();
    await this.page.waitForURL("/");
    return new HomePage(this.page); // page object creates another page object
  }
}
\`\`\``,null,[`getByRole cannot be used in component objects`,`Returning a new page object from a component method creates tight coupling — NavComponent now depends on HomePage. Caller tests should create the next page object after navigation instead.`,`waitForURL cannot be called from a component object`,`This is the recommended fluent navigation pattern`],1,`Page object creation belongs in tests or fixtures — component objects should not instantiate other page objects.`],[`E`,`A`,`What is wrong?
\`\`\`ts
export const test = base.extend<{ loginPage: LoginPage }>({
  loginPage: async ({ page }, use) => {
    const lp = new LoginPage(page);
    await lp.login("user", "pass"); // auto-login in fixture
    await use(lp); // passes authenticated loginPage
  },
});
\`\`\``,null,[`login() cannot be called in a fixture`,`The fixture name loginPage implies a login page object, but it's actually an authenticated session fixture — the naming is misleading. Better to name it authenticatedPage and return a different page object or the plain page.`,`base.extend does not support page objects`,`use() must be called before login()`],1,`Fixture names should reflect what they provide — a fixture that authenticates and returns a page object should be named for what the test receives, not the mechanism.`],[`D`,`B`,`Tests using a page object fail after a UI refactor. The button selector changed. How do you fix it?`,null,[`Update every test file that clicks the button`,`Update the selector in the page object's constructor (one change) — all tests using that method get the fix automatically. This is the POM payoff.`,`Re-run the tests with --update-snapshots`,`Use a generic click method that accepts any selector`],1,`Single point of change is the core POM benefit — update the locator in one place and all tests are fixed.`],[`D`,`I`,"A page object method is throwing `locator.click: Error: strict mode violation — X elements match`. The locator is too broad. How do you fix it?",null,[`Call .first() on the locator to pick the first match`,`Make the locator more specific: add a filter, a role name option, or scope it within a parent container. Use Playwright's locator inspector to find a unique selector.`,`Wrap the click in a try/catch`,`Use page.$$eval() instead`],1,`Strict mode enforces uniqueness — tighten the locator in the page object to match exactly one element.`],[`D`,`B`,"A page object fixture is not available in a test file. The test imports from `@playwright/test` instead of the custom fixture file. How do you fix it?",null,[`Add the page object to playwright.config.ts`,`Change the import to the custom fixture file: import { test, expect } from "./fixtures" (or the correct relative path). Custom fixtures are only available when imported from their definition file.`,`Re-run with --fixture flag`,`Page objects do not require custom imports`],1,`Custom test fixtures must be imported from their definition — the standard @playwright/test import does not include them.`],[`D`,`A`,'A page object uses `this.page.locator()` in the constructor, but calling the method throws "Target closed". The page fixture was already disposed. What went wrong?',null,[`Locators capture a reference to the DOM — if the DOM changes, they throw`,`The page object instance was kept beyond the fixture scope — e.g. stored in a module-level variable or closure. Page objects must not outlive their page fixture's lifecycle.`,`Locators must be re-created on each method call`,`The page fixture closes after the first test step`],1,`Page objects live exactly as long as the page fixture — never store them outside the fixture/test lifecycle.`],[`C`,`B`,`Complete the page object to encapsulate the login action.
\`\`\`ts
class LoginPage {
  constructor(private readonly page: Page) {}
  async login(email: string, password: string) {
    await this.page.getByLabel("Email").______( email);
    await this.page.getByLabel("Password").fill(password);
    await this.page.getByRole("button", { name: "Sign in" }).click();
  }
}
\`\`\``,null,[`fill`,`type`,`setValue`,`input`],0,`fill() is the Playwright method for setting input field values — replaces any existing content.`],[`C`,`I`,`Complete to add a method that waits for navigation after clicking.
\`\`\`ts
async clickAndWait(locator: Locator, url: string) {
  await Promise.all([
    this.page.______( url),
    locator.click(),
  ]);
}
\`\`\``,null,[`waitForURL`,`awaitNavigation`,`expectURL`,`navigate`],0,`Promise.all with waitForURL + click handles the race between the click and the navigation — standard pattern.`],[`C`,`A`,`Complete to make the page object injectable via a test fixture.
\`\`\`ts
export const test = base.extend<{ cart: CartPage }>({
  cart: async ({ page }, ______ ) => {
    await use(new CartPage(page));
  },
});
\`\`\``,null,[`use`,`inject`,`provide`,`setup`],0,`use() is the Playwright fixture callback — call it with the fixture value and the test receives it.`],[`C`,`I`,`Complete to compose a header component in the page object.
\`\`\`ts
class ProductPage {
  readonly header: HeaderComponent;
  constructor(private readonly page: Page) {
    this.header = ______;
  }
}
\`\`\``,null,[`new HeaderComponent(page)`,`HeaderComponent(page)`,`page.header`,`createHeaderComponent(page)`],0,`new HeaderComponent(page) — composition via constructor injection of the page reference.`],[`C`,`B`,'Complete to check if a page object is on the correct URL.\n```ts\nasync isOn() {\n  return this.page.url().______("/login");\n}\n```',null,[`includes`,`contains`,`startsWith`,`equals`],0,`includes() — or endsWith() depending on how specific you need the URL check to be.`],[`L`,`B`,`Why is the Page Object Model considered a pattern and not a Playwright-specific feature?`,null,[`Playwright has built-in POM support via a flag`,`POM is a general test architecture pattern applicable to any UI automation framework (Selenium, Cypress, WebdriverIO) — Playwright supports it natively because it is just a class wrapping Playwright APIs. The pattern predates Playwright.`,`POM is enforced by the TypeScript compiler`,`POM only works with class-based tests`],1,`POM is framework-agnostic — the principle (encapsulate locators and actions in a class) works in any testing tool.`],[`L`,`I`,`What is the tradeoff of having large page objects vs. many small component objects?`,null,[`Large page objects are always better — fewer files to maintain`,`Large page objects grow monolithic and hard to maintain; many small component objects can over-engineer simple UIs. Balance: decompose when a section has more than ~15 locators or repeats across pages; keep small pages as single objects.`,`Component objects cannot access the page fixture`,`Small component objects cannot be nested`],1,`Right-size your decomposition to the UI complexity — over-engineering a simple CRUD form is as harmful as a 1000-line page object.`],[`L`,`A`,"Should page objects use Playwright's `Locator` type or the older `ElementHandle` type?",null,[`ElementHandle is more powerful and preferred`,`Locator is strongly preferred — it is lazy (evaluated at use time), auto-waits, and is compatible with expect() matchers. ElementHandle is an eager snapshot (no auto-wait) and is considered legacy.`,`They are interchangeable`,`Locator only works inside page object methods`],1,`Locators are the modern Playwright API — auto-waiting and strict mode make them far more robust than eager ElementHandle references.`],[`L`,`I`,'Should fixture-based page objects use `scope: "worker"` or `scope: "test"`?',null,[`Worker scope is always better for performance`,`Use test scope (default) — each test gets a fresh page object with a fresh page fixture. Worker scope is only appropriate for stateless utilities that are expensive to create and safe to share across tests.`,`Test scope causes performance issues with parallel tests`,`Scope does not apply to page object fixtures`],1,`Test scope is the safe default — page objects carry state (the page reference) so sharing them across tests in worker scope would leak state.`],[`L`,`I`,'What is the advantage of using `getByRole` in page objects over `locator(".class")`?',null,[`getByRole is faster to evaluate`,`Role-based selectors are resilient to CSS and HTML refactoring — as long as the element's semantic role and name stay the same, the test passes even if classes are renamed or the HTML structure changes.`,`getByRole selectors are cached by the browser`,`locator() requires strict mode to be disabled`],1,`Semantic selectors (role, label, placeholder, text) survive UI refactoring better than CSS selectors tied to implementation details.`],[`R`,`B`,`You have 20 tests that all start with the same 5 lines of login UI actions. How does POM fix this?`,null,[`Extract the login steps into a beforeAll hook`,`Create a LoginPage with a login(email, password) method and call it from each test in one line — the 5 lines collapse to 1`,`Add the login steps to globalSetup`,`Use test.describe with a shared beforeEach`],1,`A LoginPage.login() method removes duplication — one method call replaces 5 repeated lines in every test.`],[`R`,`I`,`You need to test the same user flow on mobile and desktop. How does POM help?`,null,[`Create separate page objects per viewport`,`Use the same page objects but run the tests with different viewport configurations — POM separates the "what" (actions) from the "how" (viewport, device) — the same login() method works on both viewports`,`Duplicate the page objects with Mobile prefix`,`Page objects only work on desktop viewports`],1,`POM's abstraction layer is viewport-agnostic — actions stay the same while configuration (viewport, device) changes.`],[`R`,`A`,`You want to generate a usage report showing which page object methods are most frequently called. How could you implement this?`,null,[`Enable verbose logging in playwright.config.ts`,`Add a proxy around each method that logs calls to a shared counter, or use a base class that overrides method calls via Proxy — this is advanced but shows the extensibility of class-based page objects`,`Playwright has a built-in method call counter`,`Use page.on("request") to count interactions`],1,`Class-based page objects are plain TypeScript — you can instrument them with any logging, tracing, or counting logic you need.`],[`S`,`B`,`A page object's submit button locator starts failing after the button is renamed from "Submit" to "Save". How do you fix it?`,null,[`Search and replace across all test files`,`Update the locator once in the page object class: this.submitBtn = page.getByRole("button", { name: "Save" }) — all tests using submit() get the fix automatically`,`Add an alias in the HTML for the old name`,`Create a new page object for the renamed button`],1,`One change in the page object fixes all tests — this is the POM maintenance payoff.`],[`S`,`I`,`A team of 5 writes tests for a large e-commerce app. Without POM, multiple developers have created different selectors for the same "Add to Cart" button. How does POM solve this?`,null,[`Add a code review step to standardise selectors`,`A shared CartComponent page object defines the single authoritative selector — all tests import and use CartComponent.addToCart() instead of their own selector. One selector, one truth.`,`Use Playwright codegen to standardise selectors`,`Run a linter that flags duplicate selectors`],1,`Shared page objects create a single source of truth for selectors — collaboration scales cleanly.`],[`S`,`A`,`You need page objects to support multi-language testing (English, French, German). How do you design this?`,null,[`Duplicate each page object per language`,`Use getByRole, getByLabel, and getByPlaceholder selectors that are inherently language-sensitive — pass a locale fixture to the browser context and let Playwright's locators find the correct translated text`,`Store all UI strings in constants and pass the locale to each method`,`Create a language wrapper around each page object`],1,`Role and label selectors combined with locale-specific browser context is the clean approach — the page object methods stay unchanged, only the context locale differs.`],[`L`,`A`,`What core problem does the Page Object Model (POM) pattern solve compared to writing raw locators directly inside every test?`,null,[`It centralizes each page's locators and common interactions into one reusable class, so when the UI changes, only the Page Object needs updating — instead of hunting through every test file that happens to reference that element`,`POM makes tests run measurably faster by caching DOM queries permanently`,`POM is required by Playwright; tests cannot run without a Page Object class`,`POM eliminates the need for any assertions inside test files`],0,`POM is fundamentally about maintainability and DRY — a single source of truth for "how do I find/interact with X on this page" instead of scattering that knowledge across dozens of tests.`],[`O`,`I`,`What is the benefit of defining locators as class GETTERS (as shown) rather than as one-time constructor-assigned properties?`,`class LoginPage {
  constructor(page) { this.page = page; }
  get usernameInput() { return this.page.getByLabel('Username'); }
  get submitButton() { return this.page.getByRole('button', { name: 'Log in' }); }
}`,[`Getters re-evaluate the locator query each time they're accessed, staying consistent with Playwright's lazy, auto-retrying locator philosophy — a constructor-assigned locator would just be a regular property, which works too, but getters make the 'always fresh, always retried' intent explicit`,`Getters run faster than regular properties because they are compiled differently`,`Only getters can be used inside a class in modern JavaScript`,`Constructor-assigned locators would immediately query the DOM at construction time, before the page has loaded`],0,`Since Playwright Locators are already lazy (they don't query anything until used), both patterns work — getters are simply a stylistic way of keeping that lazy, declarative feel consistent within a class.`],[`L`,`A`,"What is considered a Page Object anti-pattern regarding exposing the raw `page` object to test files?",null,["If tests reach into `loginPage.page.locator(...)` directly to bypass the Page Object's own methods, the encapsulation POM is meant to provide breaks down — tests become coupled to raw selectors again, defeating the whole purpose of centralizing them in the Page Object","Exposing `page` is required for POM to function; there is no anti-pattern here","Page Objects should never accept a `page` parameter in their constructor at all","Only exposing `page` as a private field (never public) makes POM inherently broken"],0,`A well-designed Page Object's methods should be the ONLY way tests interact with that page — if tests still write raw locators against the exposed page, the abstraction isn't actually being used.`],[`O`,`A`,`What benefit does this composed structure provide over one giant DashboardPage class with every locator inlined?`,`class DashboardPage {
  constructor(page) {
    this.page = page;
    this.header = new HeaderComponent(page);
    this.sidebar = new SidebarComponent(page);
  }
}`,[`Shared UI widgets (header, sidebar) that appear on MANY pages get their own reusable component objects, avoiding duplicated locator definitions across every page class that includes them — DashboardPage composes these rather than redefining header/sidebar locators itself`,`Composition makes the page load faster at runtime`,`Component objects can only be used within a single page class, never shared elsewhere`,`This pattern requires a completely different testing framework than Playwright Test`],0,`Composing smaller component objects mirrors how the actual UI is likely built (shared header/sidebar across pages) — avoiding the same locators being copy-pasted into every page class.`],[`O`,`I`,`What does method chaining, as used here, provide stylistically?`,`class LoginPage {
  async login(username, password) {
    await this.usernameInput.fill(username);
    await this.passwordInput.fill(password);
    await this.submitButton.click();
    return new DashboardPage(this.page);
  }
}
// usage:
const dashboard = await new LoginPage(page).login('admin', 'secret');`,[`A fluent API where completing one page's flow (login) directly returns the NEXT page object the user would land on, making the test read like a natural sequence of page-to-page navigation`,`Method chaining makes the underlying Playwright actions execute faster`,`Chaining requires converting every method to a synchronous (non-async) function`,`This pattern only works if the login always succeeds; failed logins crash the chain unrecoverably`],0,`Returning the next logical Page Object from an action method is a popular POM refinement — it makes the resulting test code mirror the actual user journey step by step.`],[`S`,`I`,`A small project has only 8 total tests across 2 simple pages. A new team member proposes building a full Page Object Model layer with base classes and component composition before writing any tests. Is this the right call?`,null,[`Probably not yet — POM's main payoff (avoiding duplicated locators/maintenance pain across MANY tests) is proportional to suite size and complexity; for 8 tests, simple inline locators or lightweight helper functions may be sufficient, with POM introduced later if the suite grows`,`Yes — POM must always be used from test 1, regardless of suite size, with no exceptions`,`No — POM is fundamentally incompatible with small projects and should never be introduced`,`It doesn't matter either way; POM has no tradeoffs to consider`],0,`Like most abstractions, POM has a break-even point — the upfront structure cost is worth it once a suite is large/complex enough that locator duplication becomes a real maintenance burden.`],[`D`,`A`,"A team's Page Object methods each individually call `expect()` assertions deep inside them (e.g., `loginPage.login()` internally asserts the dashboard loaded), making test failures hard to trace back to which specific test intention failed. What is the recommended POM discipline being violated?",null,[`Page Objects should generally encapsulate ACTIONS and locators, not assertions — assertions belong in the test body itself, where the specific expectation being verified is clear from context; burying them inside reusable action methods obscures what each individual test is actually checking`,`Page Objects must never contain any methods at all, only raw locator properties`,`Assertions are only allowed inside beforeEach hooks, never inside Page Object methods or tests`,`This is not a real problem; assertions belong wherever is most convenient`],0,`Keeping 'what does the page let me DO' (Page Object) separate from 'what am I actually CHECKING in this specific test' (test body) keeps both reusable and readable.`],[`L`,`A`,`How does the "Screenplay Pattern" (actors, tasks, abilities, questions) differ philosophically from the Page Object Model?`,null,[`They are unrelated to test automation and only apply to unit testing`,`Screenplay models tests around user goals and reusable tasks/interactions composed of low-level "abilities", decoupling business intent from page structure; POM organizes around pages/components. Screenplay can compose across pages more fluidly, at the cost of more upfront abstraction`,`Screenplay is just another name for the fluent chaining style of POM`,`Screenplay requires a different test runner than Playwright Test`],1,`Screenplay asks "what is the actor trying to do" while POM asks "what page am I on" — both are valid, but Screenplay scales better for cross-page user journeys at higher design cost.`],[`L`,`A`,`What is the "App Actions" idea sometimes layered on top of POM, and how does it differ from calling page object methods directly in a test?`,null,[`App Actions replace Playwright entirely with a REST client`,`App Actions are business-level helper functions (e.g. loginAsAdmin(), createOrderWithItems()) that internally orchestrate one or more page objects (or even API calls) so tests read as user stories rather than sequences of individual page-object calls`,`App Actions are a Playwright config option for enabling actionability checks`,`App Actions is another name for fixtures with scope: "worker"`],1,`App Actions sit one layer above page objects — they compose multiple POM calls (or bypass the UI via API) into a single reusable, intention-revealing step for tests.`],[`L`,`B`,`When declaring locators inside a page object, what selector priority does Playwright recommend, from most to least preferred?`,null,[`CSS class names, then IDs, then XPath`,`User-facing locators (getByRole, getByLabel, getByPlaceholder, getByText) first, then getByTestId as a fallback, and raw CSS/XPath locators last`,`XPath first because it is the most precise`,`getByTestId always first regardless of the element`],1,`Playwright's own guidance is to mirror how a user or assistive technology finds the element — role/label/text — reserving test IDs and CSS as fallbacks for elements with no accessible identity.`],[`L`,`I`,"Why is branching logic like `if (await this.banner.isVisible()) { await this.banner.close(); }` inside a page object action method considered risky?",null,[`isVisible() is deprecated in Playwright`,`It performs a single, immediate, non-retrying check — if the banner is about to appear (race condition) the check can return false right before it shows up, silently skipping a needed interaction and making the method's behavior timing-dependent`,`Page objects cannot call isVisible() at all`,`It requires an await inside an if statement which is invalid syntax`],1,`isVisible() reports the DOM at that instant with no auto-wait; conditional logic based on it is a common source of flakiness that auto-waiting locator actions avoid.`],[`L`,`I`,`How is POM analogous to the Repository pattern from application architecture?`,null,[`They share no conceptual similarity`,`Just as a Repository hides SQL/ORM details behind domain-friendly methods (findUser, saveOrder), a Page Object hides locator/DOM details behind user-intent methods (login, addToCart) — both create a stable interface over a volatile implementation detail`,`Repositories and Page Objects are both required to be singletons`,`POM is literally implemented using the Repository interface from TypeScript`],1,`Both patterns exist to insulate callers from an implementation detail (database schema vs DOM structure) that changes more often than the business operations built on top of it.`],[`L`,`I`,"Why is `data-testid` generally treated as a fallback locator strategy rather than the default choice inside page objects?",null,[`data-testid attributes are not supported by Playwright`,`Test IDs require the application team to add and maintain attributes with no functional purpose, and they do not verify anything about accessibility; role/label locators double as an accessibility check and usually need no extra markup`,`data-testid locators are slower to evaluate than CSS selectors`,`Only one data-testid is allowed per page`],1,`Using accessible, user-facing locators means your tests also implicitly assert the app is accessible — a benefit test IDs don't provide.`],[`L`,`A`,"When a page object needs asynchronous setup before it is usable (e.g. waiting for a config API call before locators are meaningful), what pattern avoids putting `async` logic directly in the constructor?",null,[`Make the constructor async — JavaScript allows this for page object classes`,"Use a static async factory method, e.g. `static async create(page) { const po = new SettingsPage(page); await po.waitForReady(); return po; }`, since JS/TS constructors cannot be async",`Call await inside the constructor body without declaring it async`,`Use a Promise as the return value of the constructor`],1,`Constructors can never be async in JavaScript, so any required async setup has to live in a separate factory function or an explicit method the caller awaits after construction.`],[`L`,`A`,"What is the benefit of defining a TypeScript interface like `interface Authenticatable { login(user: string, pw: string): Promise<void>; }` and having multiple unrelated page objects implement it?",null,[`Interfaces make Playwright run tests faster`,`It lets shared test helpers or fixtures operate polymorphically on any page object that can authenticate, without caring which concrete class it is — useful when different apps in a monorepo have differently-shaped login pages but a common contract`,`TypeScript interfaces automatically generate locators`,`Only one class in a file may implement an interface`],1,`Programming to an interface, not a concrete class, lets shared logic (like a generic loginAs helper) work across multiple unrelated page objects.`],[`L`,`I`,"Why should a component object (e.g. a `ReviewsWidget`) avoid re-implementing business logic that already exists in the page object that composes it?",null,[`Component objects cannot contain any methods at all`,`Duplicated logic in both the component and its parent page means a behavior change has to be fixed in two places, defeating the whole purpose of decomposition — each piece of behavior should have exactly one owner`,`Component objects can only be used for styling, not behavior`,`JavaScript does not allow classes to call methods on other classes`],1,`The point of decomposing into component objects is a single owner per responsibility — logic duplicated across the composition tree reintroduces the maintenance problem POM was meant to solve.`],[`L`,`I`,"For a table with dozens of rows, when is a parameterized method like `getRow(name: string): Locator` on the table's page object preferable to creating a separate page object class per row?",null,[`Never — every row must be its own class`,'Almost always for simple rows — a method that returns a scoped Locator (e.g. `this.page.getByRole("row").filter({ hasText: name })`) is lightweight and handles an arbitrary, dynamic number of rows; a full row class is only worth it if each row has many distinct interactive sub-elements',`Only if the table has fewer than 3 rows`,`Row-level classes are required by Playwright's TypeScript types`],1,`A parameterized locator method scales to any row count with almost no code; a dedicated row class is reserved for rows complex enough to need their own encapsulated behavior.`],[`L`,`B`,"Should a page object method ever call `expect(locator).toHaveScreenshot()` internally?",null,[`Yes, always — visual checks belong exclusively inside page objects`,`No — like other assertions, visual regression checks are the test's responsibility; baking a screenshot comparison into a reusable action method means every caller is forced into that comparison even when it is not relevant to what they are testing`,`Screenshots can only be taken from the test file, never from a class method, due to a Playwright restriction`,`toHaveScreenshot() cannot be awaited inside a class method`],1,`The "actions in page objects, assertions in tests" rule applies to visual assertions exactly the same way it applies to any other expect() call.`],[`L`,`A`,"What is a real tradeoff of encapsulating `page.route()` network-mocking setup inside a page object method (e.g. `productPage.mockOutOfStock()`)?",null,[`page.route() cannot be called from inside a class`,`It couples a page object — meant to represent DOM structure and user actions — to specific network/API behavior, meaning changes to backend response shapes now also require touching a UI-focused class; some teams prefer keeping route mocks in dedicated fixtures or mock modules instead`,`Route interception only works when called directly from a test file`,`Doing this permanently disables Playwright's auto-waiting for that page object`],1,`It can be convenient, but it blurs the boundary between "how this page behaves" and "what the network returns" — a legitimate design choice as long as the team is consistent about it.`],[`L`,`A`,`What is a "workflow" or "domain" object in a POM architecture, and how does it differ from a Page Object?`,null,[`They are the same thing under a different name`,`A workflow/domain object spans and orchestrates MULTIPLE page objects to accomplish an end-to-end business process (e.g. "PlaceOrderWorkflow" driving cart, checkout, and confirmation pages), whereas a single Page Object is scoped to one route`,`Domain objects replace the need for a Playwright Page entirely`,`A workflow object can only be used in API tests, never UI tests`],1,`Workflow objects live one level above page objects — they compose several of them to express a complete user journey as a single reusable call.`],[`L`,`I`,'Why does a locator chain like `this.page.locator(".card").locator(".card-body").locator("button")` inside a page object often indicate a missing component object?',null,[`Locator chaining is invalid in Playwright and will throw`,'Three levels of nested chaining to reach one element usually means ".card" represents a reusable UI unit that deserves its own component class exposing a clean `.addButton` locator, rather than every consumer re-deriving the same deep chain',`Playwright limits locator chains to two levels`,`Chained locators always match zero elements`],1,`Deeply nested ad-hoc chains are a maintenance smell — if that structure repeats, wrap it once in a component object instead of re-deriving it everywhere.`],[`O`,`I`,`What does this page object property let callers do?`,`class CheckoutPage {
  constructor(page) { this.page = page; }
  get paymentFrame() {
    return this.page.frameLocator('iframe[title="Payment"]');
  }
  get cardNumberInput() {
    return this.paymentFrame.getByLabel('Card number');
  }
}`,[`It queries the top-level document only; iframe content is ignored by Playwright entirely`,"It scopes locators inside a cross-origin or same-origin iframe (e.g. a third-party payment widget) using frameLocator(), so `cardNumberInput` transparently finds an element that lives inside the embedded frame",`frameLocator() only works for same-origin frames, so this code always throws for payment widgets`,`This code requires manually switching Playwright's active frame context before each locator call`],1,`frameLocator() lets a page object expose iframe-nested elements through the exact same fluent, auto-waiting Locator API used for the rest of the page.`],[`L`,`I`,"Why is a generic, catch-all page object method like `clickButton(name: string)` that accepts any button label often worse than several specifically named methods?",null,[`Playwright does not allow methods with string parameters`,"A generic method exposes DOM-shaped concerns (labels) to the test again, defeating the point of POM — specifically named methods like `submitOrder()` or `applyCoupon()` describe user intent, are searchable, and get renamed automatically if the underlying label changes without touching call sites",`Generic methods run measurably slower at runtime`,`clickButton(name) cannot be typed with TypeScript`],1,`A page object's value comes from encoding intent (submitOrder) not mechanism (click a button called X) — the latter just pushes the raw selector string back into the test.`],[`L`,`A`,"Where should a helper like `async expectAccessible() { await expect(this.page).toMatchAriaSnapshot(...); }` live, and why?",null,[`It must live in playwright.config.ts as a global hook`,`Since it is an assertion, it belongs in the test file per the "actions in POM, assertions in test" rule — the page object can still expose the accessibility tree or a relevant locator, but should not itself decide pass/fail`,`ARIA snapshots cannot be generated for any Locator, only for the whole page`,`toMatchAriaSnapshot must be called before the page object is constructed`],1,`ARIA snapshot matching is still an expect() call — the same reusability argument that keeps other assertions out of page objects applies here too.`],[`L`,`A`,'Why is it a "leaky abstraction" for a page object method to accept a raw CSS selector string as a parameter, e.g. `async clickCustom(selector: string)`?',null,[`CSS selector strings cannot be passed as function parameters in TypeScript`,`It pushes DOM implementation details (selector syntax) back out to the test caller, meaning the test now has to know the page's markup — exactly what POM was meant to hide. A well-designed method should accept domain-meaningful parameters (like an item name) and resolve the selector internally`,`Playwright throws a compile error for string parameters`,`This pattern is required whenever a page has more than 10 elements`],1,`A leaky abstraction is one where callers must still know the underlying implementation to use it correctly — a selector parameter reintroduces exactly the coupling POM tries to remove.`],[`L`,`I`,`When an action opens a new browser tab (e.g. clicking "View invoice" opens a PDF viewer in a new tab), how should a page object handle it?`,null,[`Page objects cannot handle multi-tab flows; this must be done directly in the test`,'The triggering method should await `page.waitForEvent("popup")` alongside the click (or use the `browserContext.on("page", ...)` handler), then construct and return a new page object wrapping the resulting Page instance, so the caller receives a properly typed object for the new tab',`New tabs automatically inherit the same page object instance as the original tab`,`Each new tab requires re-authenticating from scratch, invalidating any page object usage`],1,`A popup/new-tab is just another Page — wrap it in the appropriate page object and return it, exactly like returning a page object after in-tab navigation.`],[`L`,`A`,`What is a practical signal that a page object has become a "God Object" that needs decomposition?`,null,[`It has more than one public method`,`It mixes multiple unrelated UI regions (e.g. header nav, product grid, filters sidebar, and footer) in one class with dozens of locators and methods that have little to do with each other — a sign each region deserves its own component object composed into a slimmer page class`,`It imports the Page type from @playwright/test`,`It has a constructor that takes a Page parameter`],1,`Size alone isn't the problem — a class doing many UNRELATED things is. Group related locators/actions into their own component objects and compose them.`],[`L`,`A`,"What is a valid use of a generic base class like `abstract class ListPage<TRow>` in a POM hierarchy for an app with many similar CRUD list screens (users, products, orders)?",null,[`Generics are not supported in Playwright page objects`,"It lets a single base class define shared behavior (pagination, sorting, `getRow(name): TRow`) once, parameterized by the specific row type for each concrete list page — reducing duplication across near-identical screens while keeping each row's specific fields strongly typed",`Generic base classes eliminate the need for a Page reference entirely`,`abstract classes cannot be instantiated by Playwright fixtures under any circumstances`],1,`Generics let you share the boilerplate every list page needs (pagination, search, common table interactions) once, while keeping row-level typing specific to each screen.`],[`L`,`A`,"Is it accurate to say `getByTestId` locators are always more brittle than role-based locators?",null,[`Yes, test IDs should never be used under any circumstances`,`Not necessarily — test IDs are actually very stable against CSS/class refactors since they are dedicated attributes; the real risk is churn in the APPLICATION's test-id naming conventions or reuse of the same component (and its test-id) in multiple visually different contexts, causing ambiguity. Role-based locators are preferred mainly for their added accessibility guarantee, not because test IDs are unstable per se`,`Role-based locators are always slower to resolve than test IDs`,`getByTestId locators cannot be combined with .filter()`],1,`Both strategies are reasonably stable to CSS churn; the real differentiator is that role/label locators double-check accessibility, while test IDs need disciplined naming to avoid collisions.`],[`L`,`I`,`What convention helps distinguish "action" methods from "query" methods on a page object?`,null,[`Actions must be static; queries must be instance methods`,'Action methods (fill, click, submit) typically return `Promise<void>` or the next page object, while query methods (getTotalPrice, getRowCount) return `Promise<string>`, `Promise<number>`, or a Locator for the test to assert on — the naming (verb vs "get"/"is") signals which kind a method is',`All methods on a page object must return the same type for consistency`,`Query methods cannot be async`],1,`A consistent naming and return-type convention makes it obvious at the call site whether a method performs an action or hands back data for the test to assert on.`],[`L`,`I`,'What is the benefit of pairing page objects with a "test data builder" (e.g. `UserBuilder.aStandardUser().withEmail(...).build()`) rather than hardcoding literal values inside page object methods?',null,[`Builders make Playwright locators resolve faster`,`Data builders keep test data construction flexible and readable, separate from UI interaction logic — page objects stay focused purely on "how to interact with the DOM" while builders own "what data to use", and both can evolve independently`,`Page objects cannot accept objects as method parameters, only builders can`,`Builders are required for storageState to work correctly`],1,`Separating "what data" (builder) from "how to interact" (page object) keeps each concern simple and independently testable.`],[`L`,`I`,"Why is it discouraged for a page object to read `process.env.SOME_FLAG` directly inside a method to change its behavior?",null,[`process.env is not accessible from TypeScript classes`,`It hides an environment dependency inside a class that is supposed to represent DOM structure, making the page object's behavior implicit and harder to reason about or test in isolation — environment-specific behavior is better handled via fixture/config parameters passed in explicitly`,`Reading process.env inside any class throws a runtime TypeError in Node.js`,`Environment variables can only be read inside playwright.config.ts`],1,`Implicit, hidden dependencies on global environment state make a class harder to reuse and reason about — prefer passing configuration in explicitly.`],[`L`,`A`,"Why is subclassing a page object per deployment environment (e.g. `StagingLoginPage extends LoginPage`, `ProdLoginPage extends LoginPage`) usually a poor way to handle small DOM differences between environments?",null,[`Subclassing across environments is impossible in TypeScript`,`It multiplies the number of classes to maintain and scatters environment logic across the class hierarchy; a single class with environment-driven configuration (e.g. a locator or base URL passed via fixture/config) usually scales better and keeps the "shape" of the page object consistent`,`Environments cannot have different DOM structures by design`,`Subclasses cannot override locator getters in TypeScript`],1,`Composition/configuration (inject what differs) usually beats a growing class hierarchy (one subclass per variation) as environments multiply.`],[`L`,`A`,"What do the `.and()` and `.or()` Locator combinators let a page object express?",null,[`They are used only for combining two separate Page instances into one`,"`.and()` builds a locator matching elements satisfying both conditions (e.g. a button that is both enabled AND has specific text); `.or()` matches either of two alternative locators — useful for an A/B-tested UI where the button could render as one of two possible variants",`They combine two different browser contexts into a shared session`,`They can only be used with getByRole locators, never with CSS locators`],1,`.and()/.or() let a single page object locator gracefully handle variation (feature flags, A/B tests, or compound conditions) without branching logic in the method itself.`],[`L`,`I`,`Why can reusing the exact same component object class across pages cause visual regression baseline collisions?`,null,[`Component objects cannot generate screenshots`,"If the test names its `toHaveScreenshot()` snapshot using only the component's locator without including which page it appears on, two different pages that reuse the same Header component could accidentally generate/compare against the same baseline file name, even though the header renders slightly differently on each page",`Screenshots are stored inside the component class file itself`,`toHaveScreenshot always uses a fixed global filename regardless of test name`],1,`Baseline filenames are usually derived from the test title/location, not the component — but ad hoc custom naming schemes can accidentally collide if the page context isn't part of the name.`],[`L`,`I`,'Why is it good practice to encapsulate `await page.waitForEvent("download")` inside a page object\'s `exportReport()` method rather than repeating it in every test that downloads something?',null,[`waitForEvent cannot be called more than once per test file`,`Download handling has a specific two-step pattern (start the event listener before triggering the action, then await the resulting Download object) that is easy to get wrong; centralizing it in one method means every caller gets the correct pattern and receives a ready-to-use Download object or saved path`,`Downloads are automatically saved without needing waitForEvent in modern Playwright`,`Page objects are not permitted to interact with the Download API`],1,`The event-then-action ordering for waitForEvent is a common source of race-condition bugs — wrapping it once in a page object method removes that risk from every test.`],[`O`,`I`,`What pattern does this code demonstrate?`,`class ProductPage {
  constructor(page) { this.page = page; }
  getRow(productName) {
    return this.page.getByRole('row').filter({ hasText: productName });
  }
  async addToCart(productName) {
    await this.getRow(productName).getByRole('button', { name: 'Add' }).click();
  }
}`,[`A parameterized locator method that scopes a query to a specific dynamic row by visible text, then chains a further locator within that scoped row — avoids creating a class per row while remaining precise even with many rows`,`A hardcoded locator that only works for a single product named "productName"`,`An anti-pattern because getRow() cannot be called more than once per test`,`A locator that matches every row on the page regardless of the productName argument`],0,`.filter({ hasText }) scopes the row locator to the matching row, and chaining a nested getByRole finds the button only within that specific row — precise and reusable for any row count.`],[`O`,`A`,`What problem does this code correctly avoid, and how?`,`class CheckoutPage {
  static async create(page) {
    const instance = new CheckoutPage(page);
    await instance.page.waitForLoadState('networkidle');
    return instance;
  }
  constructor(page) { this.page = page; }
}
// usage:
const checkout = await CheckoutPage.create(page);`,["It avoids a syntax error, since `async` cannot appear on a constructor — the static factory method performs the async setup and returns a ready instance instead","It has no benefit over `new CheckoutPage(page)`; both are functionally identical",`It prevents the CheckoutPage from ever needing a page reference`,`It replaces the need for Playwright fixtures entirely`],0,`JavaScript/TypeScript constructors cannot be declared async — a static async factory method is the standard workaround when a page object needs asynchronous setup before use.`],[`O`,`I`,`What issue does this component object design have?`,`class Modal {
  constructor(page) {
    this.closeBtn = page.locator('.modal .close-btn'); // scoped to page, not a modal root
  }
}
// Two different modals ("EditModal", "ConfirmModal") both use class="modal"`,['Locator scoping from `page` using a generic ".modal .close-btn" CSS selector will match the close button of WHICHEVER modal element(s) are currently in the DOM — if both modals can coexist or the class name is reused, this closeBtn locator is ambiguous and may click the wrong modal\'s button',`Modal classes cannot use CSS selectors, only getByRole`,`This code is fully correct with no issues`,`page.locator() cannot be used inside a constructor`],0,`Scoping from the page root instead of a specific modal root locator makes the component ambiguous whenever more than one matching element could exist — pass in or derive a specific root locator per modal instance.`],[`O`,`I`,`What is the risk in this locator declaration?`,`class ProfilePage {
  constructor(page) {
    this.page = page;
    this.isPremiumBadgeVisible = page.locator('.badge-premium').isVisible(); // stored as a Promise
  }
}`,[`This is correct and idiomatic Playwright usage`,"`isVisible()` is invoked immediately in the constructor and its resulting Promise (a one-time snapshot at construction time) is stored on the instance — the property will never reflect any later DOM changes; it should be an async method or getter that queries fresh, e.g. `async isPremiumBadgeVisible() { return this.page.locator(...).isVisible(); }`",`isVisible() cannot be called from a constructor, so this throws`,`Promises assigned to class properties are automatically re-evaluated on each access`],1,`isVisible() is an action, not a lazy Locator — calling it in the constructor captures a single stale snapshot forever, unlike a Locator property which stays live.`],[`O`,`A`,`What is wrong with encapsulating this assertion inside the page object?`,`class SearchResultsPage {
  constructor(page) { this.page = page; }
  async verifyAccessible() {
    await expect(this.page).toMatchAriaSnapshot(\`
      - heading "Search results"
      - list
    \`);
  }
}`,[`toMatchAriaSnapshot cannot be called with a template literal`,`Like any other expect() call, this bakes a specific expected outcome into a reusable class — any test that reaches a different result set (empty search, different heading) cannot reuse this page object method without failing on an unrelated ARIA structure mismatch. The snapshot assertion belongs in the test itself`,`ARIA snapshots are not supported for full-page targets, only individual Locators`,`This code will throw a TypeError because expect() cannot accept a Page argument`],1,`The exact same "assertions belong in tests" principle applies to ARIA snapshot assertions as to any other expect() call — otherwise the method is only reusable for the one scenario it was written for.`],[`O`,`B`,`What does this base class provide to its subclasses?`,`abstract class BasePage {
  constructor(protected page) {}
  abstract goto(): Promise<void>;
  async waitForPageLoad() {
    await this.page.waitForLoadState('domcontentloaded');
  }
}
class DashboardPage extends BasePage {
  async goto() { await this.page.goto('/dashboard'); }
}`,[`An abstract base class that stores the shared page reference and a shared waitForPageLoad() helper, while forcing every subclass to implement its own goto() — sharing common behavior while requiring page-specific navigation logic`,`A base class that automatically navigates to every page on construction`,`A pattern that is invalid because abstract classes cannot be extended in TypeScript`,`A pattern where DashboardPage inherits a fixed URL from BasePage`],0,`abstract methods force each concrete page to supply its own implementation (here, its specific URL) while concrete methods on the base class (waitForPageLoad) are inherited and shared.`],[`O`,`I`,'What does `.filter({ hasNotText: "Out of stock" })` achieve when used inside this page object method?',`class CatalogPage {
  constructor(page) { this.page = page; }
  availableProducts() {
    return this.page.getByRole('listitem').filter({ hasNotText: 'Out of stock' });
  }
}`,[`It removes out-of-stock elements from the DOM entirely`,`It scopes the returned Locator to only listitem elements that do NOT contain the text "Out of stock", useful for exposing a "currently purchasable products" query without the test needing to know the exclusion text itself`,`hasNotText is not a valid Locator filter option`,`It throws if any listitem contains that text`],0,`.filter({ hasNotText }) is the negative counterpart to hasText — narrowing a locator set by excluding elements that match a given text.`],[`O`,`A`,`What outdated pattern does this page object method use, and what should replace it?`,`class SearchPage {
  async search(term) {
    await Promise.all([
      this.page.waitForNavigation(),
      this.page.fill('#q', term),
    ]);
    await this.page.click('#search-btn');
  }
}`,[`This is the current best practice and needs no change`,'waitForNavigation() combined with Promise.all is a legacy pattern from before Playwright\'s auto-waiting locators; modern code should use `page.getByRole(...).fill(term)` then `page.getByRole("button", ...).click()` and rely on `page.waitForURL()` (awaited after the click, not raced against fill) if a specific navigation must be confirmed',`fill() cannot be used inside Promise.all under any circumstances`,`click() must always come before fill() in a search flow`],1,`Playwright's locators auto-wait for actionability, and page.waitForURL() after the triggering action is now the clearer way to await navigation — the old race-based Promise.all pattern is largely unnecessary.`],[`O`,`A`,`What is the fluent-chaining bug in this code?`,`class LoginPage {
  async login(email, pw) {
    await this.emailInput.fill(email);
    await this.passwordInput.fill(pw);
    await this.submitBtn.click();
    // forgot to return new DashboardPage(this.page)
  }
}
// test:
const dashboard = await new LoginPage(page).login('a@b.com', 'pw');
await dashboard.header.logout(); // TypeError: dashboard is undefined`,["login() is missing a return statement — since it has no explicit return, it implicitly returns undefined, so `dashboard` is undefined and cannot be used as a DashboardPage",`logout() is misspelled`,`DashboardPage cannot have a header property`,`await cannot be used with a method that has no return type`],0,"A fluent method promising to return the next page object must actually `return new DashboardPage(this.page)` — omitting the return silently breaks every caller expecting to chain off the result."],[`O`,`I`,`What does composing a drag-and-drop helper this way achieve?`,`class KanbanBoardPage {
  constructor(page) { this.page = page; }
  getCard(title) { return this.page.getByText(title, { exact: true }); }
  getColumn(name) { return this.page.getByRole('region', { name }); }
  async moveCard(title, toColumn) {
    await this.getCard(title).dragTo(this.getColumn(toColumn));
  }
}`,[`It hardcodes a single drag-and-drop interaction that cannot be reused for any other card or column`,"It exposes a reusable, intention-revealing `moveCard(title, toColumn)` action built from two parameterized locator getters and Playwright's built-in `dragTo()`, keeping drag-and-drop mechanics out of the test file entirely",`dragTo() requires manually simulating mousedown/mousemove/mouseup events; this code is incomplete`,`getByText with exact:true cannot be combined with dragTo`],1,`Wrapping dragTo() in a named, parameterized method turns a fiddly interaction into a one-line, readable call for any card/column combination.`],[`O`,`I`,`What does this method correctly encapsulate for tests that need to upload files?`,`class DocumentUploadPage {
  constructor(page) { this.page = page; }
  async uploadFile(filePath) {
    await this.page.getByLabel('Upload document').setInputFiles(filePath);
  }
}`,[`A native OS file picker dialog that Playwright automatically clicks through pixel by pixel`,'The `setInputFiles()` call which sets a file (or array of files) on an `<input type="file">` element directly, bypassing the native OS file-picker dialog entirely — the standard Playwright approach for testing uploads',`A workaround that only works in headed browser mode, never headless`,`A method that requires the file to already be open in another browser tab`],1,`setInputFiles() programmatically sets the file input's value without needing to interact with (and Playwright cannot interact with) a native OS dialog.`],[`O`,`A`,`What does wrapping a page object in a Proxy like this enable?`,`function withLogging(pageObject) {
  return new Proxy(pageObject, {
    get(target, prop) {
      const orig = target[prop];
      if (typeof orig === 'function') {
        return (...args) => {
          console.log(\`Calling \${String(prop)}\`);
          return orig.apply(target, args);
        };
      }
      return orig;
    },
  });
}`,[`It permanently disables all methods on the page object`,`It transparently intercepts every method call on the wrapped page object to log which method was invoked, without modifying the page object's own class — a form of cross-cutting instrumentation (logging, timing, call counting) applied externally`,`It converts the page object into a plain JSON object`,`Proxy objects cannot wrap class instances, only plain objects`],1,`JavaScript's Proxy lets you intercept property/method access on an object from the outside — a lightweight way to add logging or metrics to any page object without touching its class definition.`],[`O`,`I`,`What bug does this component object introduce when two card instances exist on the same page?`,`class ProductCard {
  constructor(page) {
    this.page = page;
    this.addToCartBtn = page.getByRole('button', { name: 'Add to cart' }); // not scoped per-card
  }
}
// usage: new ProductCard(page).addToCartBtn.click() when 5 cards are rendered`,[`This code is correct as long as fewer than 10 cards exist`,"The locator is derived from the whole page instead of a specific card's root element, so with multiple product cards rendered, `addToCartBtn` matches every card's button — clicking it either throws a strict-mode violation or (with .first()) silently clicks the wrong card's button",`getByRole cannot be used more than once per class`,`Playwright automatically scopes getByRole calls to the nearest visible card`],1,`A component object representing ONE of several repeated instances must be constructed with (and scope its locators from) a specific root Locator for that instance, not the whole page.`],[`L`,`A`,`You are integrating a third-party embedded chat widget (rendered in a cross-origin iframe) into several pages of your app. How should the corresponding page objects model this?`,null,[`Duplicate the same iframe-scoped locators inside every page object that includes the widget`,"Create one `ChatWidget` component object whose constructor scopes all of its locators via `page.frameLocator(...)`, and compose an instance of it into every page object that includes the widget — a single class owns the widget's DOM knowledge",`Chat widgets in cross-origin iframes cannot be tested with Playwright at all`,`Use only page.frame() by name, since frameLocator does not support cross-origin frames`],1,`frameLocator() does work across origins for locating elements inside same-process iframes, and a single reusable widget component avoids duplicating iframe-scoping logic on every page that embeds it.`],[`R`,`I`,`A payment step embeds Stripe's hosted card-input iframe. How should the checkout page object expose it to tests?`,null,["Tests should reach into `page.frame()` directly since page objects cannot handle third-party iframes","Expose a `cardNumberInput` (and similarly named fields) as getters that internally use `this.page.frameLocator('iframe[title=\"Secure card number...\"]').getByRole(...)`, so callers use it exactly like any other page object field without knowing it is implemented via an iframe",`Disable the Stripe iframe using page.route() and replace it with a mock before every test`,`Stripe iframes require switching Playwright to a legacy WebDriver-based mode`],1,`The whole point of a page object is that callers never need to know the field is inside an iframe — the frameLocator plumbing stays entirely inside the class.`],[`R`,`A`,`Your team is migrating a suite from raw Playwright codegen scripts (hundreds of tests with inline selectors) to POM. What is the most pragmatic incremental strategy?`,null,[`Rewrite every existing test file from scratch on day one before merging any change`,`Start by extracting page objects for the highest-traffic/most-duplicated flows (e.g. login, navigation), refactor the tests that already touch those flows to use the new classes, and repeat for the next most-duplicated area — treating POM adoption as an ongoing refactor rather than a one-time rewrite`,`Introduce POM only for new tests and never touch the old raw scripts again, permanently`,`Delete all existing tests and only keep the ones that already match a POM template`],1,`Incremental extraction targeting the most duplicated flows first gives the biggest maintenance payoff early and avoids a risky big-bang rewrite.`],[`R`,`A`,`Your product renders a data grid with virtualized rows (only visible rows exist in the DOM; scrolling swaps them in and out). How should the page object expose row access?`,null,[`Query all rows up front in the constructor and cache them as a fixed array`,'Provide a method like `async getRow(name) { await this.page.getByText(name).scrollIntoViewIfNeeded(); return this.page.getByRole("row").filter({ hasText: name }); }` so the row is scrolled into the DOM before being queried, since virtualization means a row that is not currently rendered simply does not exist yet',`Disable virtualization globally in the test environment via playwright.config.ts`,`Virtualized grids cannot be tested with Playwright`],1,`With virtualized lists, a row genuinely does not exist in the DOM until scrolled near — the page object method must account for that by scrolling before querying, rather than assuming every row is always present.`],[`R`,`A`,`Multiple internal apps in a monorepo share the same design-system component library (buttons, modals, date pickers). How should page/component objects for that shared library be organized?`,null,[`Copy-paste the same component object classes into every app's test folder`,`Publish a shared package (or shared workspace folder) of component objects for the design-system pieces, imported by each app's test suite — a change to how the shared DatePicker renders is fixed once and picked up everywhere it is used`,`Component objects cannot be shared across separate Playwright projects`,`Only page objects can be reused across apps; component objects must be duplicated per app`],1,`Design-system components are, by definition, reused across apps — their component objects should be too, published as a shared internal package.`],[`R`,`I`,`A feature is behind an A/B test: 50% of users see "Buy now", the other 50% see "Purchase" for the exact same button. How should the page object model this without branching logic in every test?`,null,[`Write two entirely separate page object classes, one per variant, and have every test decide which one to instantiate`,'Expose a single locator built with `.or()`: `this.page.getByRole("button", { name: "Buy now" }).or(this.page.getByRole("button", { name: "Purchase" }))`, so the method works regardless of which variant is currently rendered',`Disable the A/B test in every single test run via an environment variable`,`Use .first() on a list of all buttons on the page to guess the right one`],1,`.or() lets one locator (and therefore one method) gracefully accept either rendering, keeping the variation invisible to the rest of the page object and to tests.`],[`R`,`I`,`Your accessibility test suite needs to verify several pages are fully keyboard-navigable (Tab order, Enter/Space activation) without a mouse. How should this be reflected in your page objects?`,null,[`Accessibility/keyboard testing cannot be expressed through page objects, only raw scripts`,'Add explicit keyboard-driven action methods alongside the existing pointer-driven ones, e.g. `async submitViaKeyboard() { await this.page.keyboard.press("Tab".repeat(3)); await this.page.keyboard.press("Enter"); }`, so keyboard-only flows are named, reusable, and easy to distinguish from mouse-driven ones in test reports',`Keyboard interactions must always be inlined directly in the test file, never in a page object`,`page.keyboard is not accessible from inside a class method`],1,`Treating keyboard-only interaction as its own first-class, named action method (rather than an inline afterthought) makes accessibility coverage visible and maintainable.`],[`R`,`A`,`Your app migrated its UI library from Angular Material to a custom component set, changing most CSS classes and some ARIA roles. What POM structure minimizes the blast radius of future library migrations like this?`,null,[`Inline every selector directly in each test so a migration only requires updating that one library’s tests`,`Concentrate all library-specific selectors inside a thin layer of low-level component objects (buttons, inputs, dialogs) that pages compose; higher-level page objects and all tests only call semantic methods on those components — a future library swap only requires updating the low-level component layer, not every page or test`,`Rewrite every page object and every test from scratch after each UI library migration`,`Avoid using any UI library-specific selectors ever, relying only on plain HTML tags`],1,`Isolating library-specific selector knowledge in one thin layer means a future library swap is a localized change instead of a suite-wide rewrite.`],[`R`,`I`,`Your organization wants new teams to be productive writing Playwright tests immediately, without relearning POM conventions each time. What structural investment pays off across many teams?`,null,[`A wiki page describing POM in the abstract, with no actual shared code`,"A shared, versioned internal package providing a `BasePage` (common navigation/waiting helpers) and common component objects (nav bar, toast, pagination) that every team's app-specific page objects extend or compose — new teams inherit conventions and working code, not just documentation",`Mandating every team write their page objects completely independently for full flexibility`,`A single, giant shared page object file containing every team's locators`],1,`Shared, working base classes and common components onboard new teams faster than documentation alone, and keep conventions consistent across the org.`],[`R`,`A`,`Your app increasingly uses native Web Components with real Shadow DOM (not just React/Vue virtual DOM). How does this affect page object locator design?`,null,[`Shadow DOM makes elements completely inaccessible to Playwright locators`,`Playwright's locators (getByRole, getByText, CSS) pierce open shadow roots automatically by default, so page objects can generally use the same locator strategies as with regular DOM — the main added consideration is that some legacy XPath-based selectors do NOT pierce shadow boundaries and should be avoided`,"You must manually call `.shadowRoot()` on every locator before continuing the chain",`Web Components require a completely separate testing framework from Playwright`],1,`Playwright locators pierce open shadow roots by default, which is one of the reasons XPath (which historically does not) is discouraged in favor of the built-in locator engines.`],[`S`,`I`,"A page object's `dismissPromoBanner()` uses `if (await this.banner.isVisible()) { await this.banner.close(); }` and the calling test is intermittently flaky depending on network speed. How should this be redesigned?",null,['Add a fixed `page.waitForTimeout(2000)` before the isVisible() check to "give it time"',"Replace the one-shot check with an approach that tolerates the banner's absence without a race: e.g. `await this.banner.click({ timeout: 3000 }).catch(() => {})`, or better, only call dismissPromoBanner() from tests where the banner is a guaranteed precondition and assert its presence first rather than branching on it",`Retry the whole test up to 5 times using test-level retries to average out the flakiness`,`Remove dismissPromoBanner() and require every test to handle the banner manually`],1,`The core issue is a non-retrying instantaneous check racing against an element that appears asynchronously — either make the precondition explicit and asserted, or use an approach that tolerates the element never appearing instead of guessing at its timing.`],[`S`,`A`,'A `Tooltip` component object is instantiated as `new Tooltip(page)` instead of being scoped to the specific icon that triggers it, and clicking on "help icon B" ends up asserting text that belongs to "help icon A"\'s tooltip. What is the fix?',null,[`Tooltip components cannot be tested with Playwright since tooltips are transient`,'Scope the Tooltip constructor to a specific trigger element\'s locator (e.g. `new Tooltip(page, page.getByTestId("help-icon-b"))`) so its internal locators are derived relative to that specific trigger, instead of matching whichever tooltip happens to be open anywhere on the page',`Add a 5-second wait before reading the tooltip text to let the correct one load`,`Only allow one tooltip to exist per page across the whole application`],1,`Any component representing one of several repeated widget instances needs to be constructed with, and scope its locators from, the specific instance it represents — never the page as a whole.`],[`S`,`I`,`Your team wants both automated Playwright tests and a manual QA checklist document to stay in sync about which user actions are available on the Checkout page. Is reusing the POM class directly for both purposes realistic?`,null,[`Yes, page objects can be directly rendered as human-readable documents with zero extra work`,`Not directly — a page object's method names and doc comments (e.g. via TSDoc) can be used to auto-generate a readable checklist as a build step, but the class itself is code meant for Playwright, not a document; treat the generated checklist as a byproduct of well-named methods rather than expecting the class to serve two purposes unmodified`,`Manual QA checklists are obsolete once POM exists and should be deleted entirely`,`Page objects must be rewritten in plain English before QA can reference them`],1,`Well-named, documented page object methods can seed a generated checklist, but the class remains code for automation — bridging to a human document usually needs a small doc-generation step, not a shared artifact.`],[`S`,`A`,'A page object method silently swallows errors: `async save() { try { await this.saveBtn.click(); } catch (e) { console.log("save failed", e); } }`. Tests using `save()` never fail even when saving is broken. What is wrong and what should replace it?',null,[`This is correct — page objects should never let errors propagate to tests`,`Swallowing the error hides real failures from the test, defeating the purpose of automated testing; the method should simply let click() throw naturally (Playwright already produces clear timeout/actionability errors) so the calling test fails loudly, or the test itself should assert the outcome after calling save()`,`console.log statements are not permitted inside page object methods`,`click() cannot be wrapped in a try/catch under any circumstances`],1,`A page object action method should propagate failures, not hide them — swallowing exceptions turns every test using that method into a false-positive machine.`],[`S`,`A`,`You have 15 nearly identical page object classes, one per product category page, differing only in the category name used in a couple of locators. How should you consolidate them?`,null,[`Keep all 15 classes since each represents a distinct URL`,'Replace the 15 classes with a single `CategoryPage` class whose constructor takes the category name (or is instantiated via `new CategoryPage(page, "electronics")`), parameterizing the few differing locators, and derive the goto() URL from that parameter — one class serves all 15 categories',`Merge all 15 classes into one giant class with 15 sets of duplicated methods, one per category`,`Use multiple inheritance so each category class extends all the others`],1,`When classes differ only by a parameter (like a category name), parameterizing a single class is far more maintainable than duplicating near-identical classes.`],[`S`,`I`,`A new requirement asks for RTL (right-to-left, e.g. Arabic) locale test coverage without duplicating every page object's locators. What is the right approach?`,null,[`Duplicate every page object into an "Rtl" variant with mirrored locator strings`,'Keep using semantic locators (getByRole, getByLabel, getByText) which are layout-direction agnostic, and drive the actual RTL behavior through the browser context\'s locale/direction settings (e.g. `browser.newContext({ locale: "ar-SA" })`) — the same page object classes and methods work unchanged for both directions',`RTL testing requires a completely separate testing tool, not Playwright`,`Playwright cannot render RTL layouts under any circumstances`],1,`Because role/label/text locators describe elements by meaning, not visual position, they remain correct regardless of text direction — only the context locale needs to change.`],[`S`,`A`,'A flaky "Add to cart" test was traced to a page object using `.first()` on a locator that matches multiple buttons to sidestep a strict-mode violation. What is the more durable fix?',null,[`Keep .first() permanently — it is the recommended way to resolve any strict-mode violation`,`Investigate WHY multiple elements match: likely the locator needs to be scoped to a specific product card (e.g. via .filter({ hasText: productName }) or a properly rooted component object) rather than blindly taking the first match, which may silently interact with the wrong product as the page changes`,`Disable strict mode globally in playwright.config.ts to avoid the violation entirely`,`Replace getByRole with an XPath selector using an absolute index`],1,`.first() as a strict-mode workaround often just relocates the bug — it silently picks an arbitrary match instead of the intended one; proper scoping fixes the ambiguity at its root.`],[`S`,`I`,`Tests need to reliably wait for a loading skeleton to disappear before interacting with a page's content. Where should this waiting logic live?`,null,[`Inside each individual test, repeated every time content needs to be accessed`,'Inside the relevant page object\'s navigation/ready method (e.g. `async goto() { await this.page.goto("/dashboard"); await this.skeleton.waitFor({ state: "hidden" }); }`), so every test that navigates via that page object automatically waits without repeating the logic',`Nowhere — Playwright's auto-waiting makes explicit skeleton waiting always unnecessary`,`In playwright.config.ts as a global timeout override`],1,`A recurring precondition like "wait for the skeleton to disappear" belongs in the page object's own navigation/setup method so every caller benefits automatically.`],[`S`,`A`,'A page object has accumulated a `waitForReady()` method using `await this.page.waitForTimeout(3000)` to "make sure everything loaded" before other methods are called. How should this be improved?',null,[`Increase the timeout to 5000ms to be extra safe`,'Replace the fixed timeout with a condition-based wait on something meaningful — e.g. `await expect(this.mainHeading).toBeVisible()` or `await this.page.waitForLoadState("networkidle")` if appropriate — so the method waits exactly as long as needed rather than a hardcoded guess that is both slow on fast runs and unreliable on slow ones',`waitForTimeout is required before any locator can be used`,`Remove waitForReady() entirely since Playwright never needs explicit waiting`],1,`Fixed timeouts are both wasteful (slow when unnecessary) and unreliable (too short when the app is slow) — condition-based waits on a specific, meaningful signal are the resilient replacement.`],[`S`,`A`,"You want a single `login(user, pw)` action reusable in shared test helpers across several internal apps, but each app's login form has different DOM structure. What design achieves this?",null,[`Force every app to use identical login page markup so one class works everywhere`,"Define a shared `LoginPage` interface (e.g. `login(user, pw): Promise<void>`) and have each app supply its own concrete class implementing it with app-specific locators; shared helpers depend only on the interface, not any specific implementation",`Copy the exact same LoginPage class into each app's repo and edit the locators by hand each time a shared helper changes`,`Shared test helpers cannot work across apps with different login markup, period`],1,`Programming shared helpers against an interface (a contract) rather than a concrete class lets each app supply its own implementation while the shared logic stays the same.`],[`D`,`A`,"Two tests running in different browser contexts both instantiate `new HeaderComponent(page)`, but somehow one test's assertion inspects the other test's header state. What should you check first?",null,[`HeaderComponent has a bug in Playwright itself`,"Verify that the `page` reference passed into each HeaderComponent genuinely belongs to that test's own context/fixture and was not accidentally captured from a shared module-level variable or a fixture with the wrong scope (e.g. mistakenly worker-scoped) — cross-context bleed almost always traces back to a shared reference somewhere",`Restart the Playwright test runner to clear its internal cache`,`HeaderComponent must be marked as a singleton to prevent this`],1,"If two logically separate tests appear to share state, the most common cause is a `page` (or other fixture) reference being unintentionally shared — often via a wrongly-scoped fixture or a module-level variable."],[`D`,`A`,"A `Modal` component object's locators were captured relative to a `rootLocator` passed in when the modal first opened. After the modal is closed and reopened, methods on the same Modal instance stop finding elements. What is happening?",null,[`Locators are eagerly cached DOM references that go stale when the modal closes`,"Since Playwright Locators are lazy query descriptors (not cached elements), this should actually keep working automatically after reopening — the real bug is more likely that `rootLocator` was itself derived from an ElementHandle or a specific DOM node reference captured at construction time rather than as a live, re-evaluating Locator",`Modal components must be re-instantiated every single time they open`,`page.locator() only works the first time it is called`],1,`True Locators re-query on every use and should survive open/close cycles fine — if a component object breaks after reopening, look for an ElementHandle or non-Locator reference sneaking into its root instead.`],[`D`,`I`,"Chained page object navigation stops working after adding a new method: `async continueToPayment() { await this.continueBtn.click(); }` — callers expecting a returned `PaymentPage` get `undefined`. What is the fix?",null,[`click() must be called twice for navigation to register`,"The method never returns the next page object — add `return new PaymentPage(this.page);` after the click completes (and after confirming navigation, e.g. with waitForURL if needed) so callers can chain off the result",`PaymentPage must extend CheckoutPage for chaining to work`,`Playwright automatically infers and returns the next page object based on the URL`],1,`Any method meant to support fluent chaining must explicitly construct and return the next page object — Playwright has no automatic mechanism for this.`],[`D`,`A`,"A test fails only when run after a specific other test in the same file, never in isolation. The shared page object has `constructor(page) { this.page = page; this.selectedIds = []; }` and `selectedIds` accumulates across calls to `selectItem()`. What is wrong?",null,[`Playwright test files must never define more than one test`,"If the SAME page object instance is being reused across multiple tests (e.g. created once at module scope instead of freshly per test via a fixture or in each test body), its `selectedIds` array persists state from the previous test — page objects should be constructed fresh per test, typically via a fixture",`selectedIds must be declared with const instead of an array literal`,`This can only be caused by a Playwright fixture-caching bug`],1,`A page object holding mutable instance state that outlives a single test is a sign it is being shared across tests — fixtures (test-scoped by default) are the fix, ensuring a fresh instance every test.`],[`D`,`A`,"A `PaymentFrame` component object built with `page.frameLocator(...)` cannot find any elements immediately after the page object is constructed, but works fine a moment later inside an action method. Why?",null,[`frameLocator() itself does not query anything at construction time — it is lazy, exactly like Locator — so this should not error at construction; the real issue is likely that the iframe has not been added to the DOM yet by the time an action method is called too early, before the triggering step (e.g. clicking "Pay with card") that causes the payment iframe to render`,`frameLocator must be called only after the iframe has fully loaded, requiring an explicit page.waitForFrame() before construction`,`frameLocator can only be used once per page object instance`,`This is a Playwright bug present in all versions`],0,`frameLocator(), like Locator, is lazy and does not query the DOM until used — a "not found" error means the underlying iframe genuinely was not attached yet when the action was attempted, not a construction-time problem.`],[`O`,`B`,`What does exposing this getter allow a test to do without knowing any selector details?`,`class InboxPage {
  constructor(page) { this.page = page; }
  get unreadCount() {
    return this.page.getByTestId('unread-badge');
  }
}
// test:
await expect(inboxPage.unreadCount).toHaveText('3');`,[`The test must still know the CSS selector for the badge`,"The test can assert on `inboxPage.unreadCount` using Playwright's auto-retrying `toHaveText` matcher directly, with zero knowledge of how the badge is actually implemented in the DOM",`This code requires calling .click() before the assertion can run`,`getByTestId locators cannot be used with toHaveText`],1,`Exposing a plain Locator (not the queried value) lets the test use Playwright's auto-retrying expect() matchers directly, which is more robust than the page object resolving the value itself.`],[`O`,`I`,`What is the effect of scoping this locator relative to a specific card root instead of the page?`,`class ProductCard {
  constructor(rootLocator) {
    this.root = rootLocator;
    this.price = this.root.getByTestId('price');
    this.addBtn = this.root.getByRole('button', { name: 'Add' });
  }
}
// usage:
const card = new ProductCard(page.getByTestId('product-42'));`,[`It has no effect; getByTestId always searches the whole document regardless of scope`,"Because `price` and `addBtn` are derived from `this.root` (a Locator scoped to one specific product's container), they will only ever match elements within that one card — safe to use even when many identical cards are rendered on the same page",`It causes an error since Locators cannot be chained from another Locator`,`It only works if there is exactly one product card on the page`],1,`Deriving child locators from a specific root Locator (rather than the page) is exactly how component objects avoid ambiguity when multiple instances of the same UI piece exist.`],[`L`,`I`,'Why is passing a scoped root Locator into a component object\'s constructor (e.g. `new ProductCard(page.getByTestId("product-42"))`) generally better than passing the raw Page for a component representing one of several repeated instances?',null,[`Passing a Page instead is impossible; TypeScript forbids it`,`A root-scoped constructor guarantees every locator derived inside the component only matches within that specific instance, eliminating an entire class of "which one?" ambiguity bugs that arise when multiple identical widgets exist on the page`,`Root Locators execute faster than Page-derived locators`,`Component objects can only accept a Page, never a Locator, per Playwright's API design`],1,"Scoping at construction time bakes correctness in structurally — every method built on `this.root` is automatically safe for repeated widget instances."],[`L`,`B`,`What is a simple heuristic for deciding whether a reusable UI element deserves its own component object versus just being a couple of locator properties directly on the page object?`,null,[`Every UI element, no matter how trivial, must always get its own component class`,`If the element (a) appears on multiple distinct pages, or (b) has enough internal locators/behavior that it would otherwise clutter the page object, it is a good candidate for a component object; a single one-off label or icon used on only one page usually does not need one`,`Component objects are only for elements with more than 100 pixels of screen space`,`Only elements inside a <form> tag qualify for component objects`],1,`The two classic signals for extracting a component object are reuse across pages and enough internal complexity to warrant its own file — neither alone is a hard rule, but together they are a good practical heuristic.`],[`L`,`I`,"What is the tradeoff of a page object exposing both a low-level method (`fillEmail(value)`) and a high-level convenience method (`login(email, pw)`) that calls it internally?",null,[`Exposing both is not allowed; a class can only expose one level of granularity`,`It gives callers flexibility — most tests use the convenient login() for brevity, while a small number of tests that specifically need partial-form-state control (e.g. testing field-level validation) can drop down to fillEmail() directly — at the small cost of a slightly larger public API to maintain`,`High-level methods always internally duplicate the low-level method's code rather than calling it`,`Playwright fixtures require exactly one public method per page object`],1,`Layering convenience methods on top of granular ones is a common, low-cost way to serve both common-case brevity and edge-case flexibility.`],[`L`,`A`,`Why might a team deliberately choose NOT to introduce component objects for a small marketing site with 5 static pages and 12 total tests, even though the pattern is technically applicable?`,null,[`Component objects are incompatible with static (non-SPA) sites`,`With so few pages and no meaningfully repeated complex widgets, the upfront cost of decomposing into component objects likely exceeds the maintenance savings it would provide — a handful of flat page objects (or even inline locators) may be the pragmatically simpler choice until the suite grows`,`Playwright refuses to run more than 12 tests without full POM decomposition`,`Marketing sites cannot be tested with Playwright at all`],1,`POM (and its finer-grained component decomposition) pays off in proportion to duplication and suite size — applying heavy structure to a tiny, simple suite is over-engineering.`],[`L`,`I`,"How does exposing a page object method's return type as `Promise<DashboardPage>` (rather than `Promise<void>`) help catch bugs at compile time?",null,[`It has no compile-time benefit, only runtime documentation value`,"If a caller tries to call `.header` on the result but the method's declared return type is `Promise<void>`, TypeScript will flag the error before the test ever runs; declaring the precise return type lets the compiler verify chained calls are valid for whatever page object type is actually returned",`TypeScript ignores return type annotations on async methods`,`Promise<DashboardPage> and Promise<void> are treated identically by TypeScript`],1,`Precise return types turn a class of chaining mistakes (forgetting to return the next page object, or assuming the wrong one) into compile-time errors instead of runtime surprises.`],[`L`,`A`,"What is a reasonable way to keep a `NotificationToast` component object useful despite the toast auto-dismissing after a few seconds?",null,[`Toasts cannot be tested since they disappear before Playwright can interact with them`,'Expose a method like `async expectMessage(text) { await expect(this.page.getByRole("status")).toHaveText(text); }` that leans on Playwright\'s auto-retrying expect() rather than a one-shot check — as long as the assertion is set up before or shortly after the action that triggers the toast, auto-waiting captures it even though it is transient',`Increase the toast's auto-dismiss duration in test environments to 60 seconds so it never disappears`,`Use page.waitForTimeout(100) immediately followed by a synchronous DOM read`],1,`Playwright's auto-retrying assertions actively poll rather than checking once, so as long as the assertion starts polling before the toast disappears, transient UI elements are testable without special handling.`],[`L`,`I`,"Why does exposing `get errorMessages(): Locator` (returning a Locator that may match zero or more elements) tend to be more useful to tests than a method returning `Promise<string[]>` of the current error texts?",null,[`Locator objects cannot be asserted on with expect()`,"The Locator version lets the test use Playwright's auto-retrying matchers like `toHaveText([...])` or `toHaveCount(n)`, which wait for the DOM to settle; a pre-resolved `string[]` is a one-time snapshot that could be stale or incomplete if errors are still rendering when it was captured",`string[] return values execute faster since they avoid the browser round-trip`,`Locators can only be returned from getters, never from async methods`],1,`Returning a live Locator (rather than an already-resolved value) preserves Playwright's auto-waiting benefits all the way through to the test's assertion.`],[`L`,`A`,"What is the risk of a page object exposing a `waitForNetworkIdle()` method that every test is expected to call before interacting with dynamic content?",null,[`There is no risk; this is always the best practice for any dynamic content`,`Network-idle-based waiting can be unreliable for apps using polling, websockets, or analytics beacons that keep the network "busy" indefinitely, causing the wait to time out even though the UI is actually ready; a condition-based wait on a specific visible element or state is usually more robust than waiting for network idle`,`waitForNetworkIdle is not a real Playwright API and this code would not compile`,`This method must be called exactly once per test file, not per navigation`],1,`Network-idle heuristics are notoriously unreliable in modern apps with background polling — waiting on a specific, meaningful UI signal is usually a better bet than a generic network-quiet-period check.`],[`L`,`I`,"Why is it useful for a page object's `goto()` method to also assert (via waiting, not expect) that navigation actually landed on the right page, e.g. `await this.page.waitForURL(/\\/dashboard/)`?",null,[`waitForURL() is required syntax and has no functional purpose`,`It converts a silent failure mode (goto() succeeding at the network level but the app redirecting elsewhere, e.g. back to login due to an expired session) into an early, clear failure right at the point of navigation, rather than a confusing failure several steps later when a locator is not found`,`waitForURL always throws if called after page.goto()`,`This pattern disables Playwright's auto-waiting for the rest of the test`],1,`Confirming the landed URL right after navigation surfaces problems (like an unexpected redirect) immediately and with a much clearer error than a downstream locator timeout would give.`],[`D`,`B`,"A page object's `submitBtn` locator used to find one button but now, after a UI change, actually matches two (a mobile and a desktop variant rendered simultaneously and hidden via CSS). What is the correct fix?",null,[`Add .first() permanently to always pick the visible one, regardless of which one that is`,"Narrow the locator to specifically target the one that matters for the current viewport, e.g. by filtering on visibility (`.filter({ visible: true })` conceptually, or scoping to a container unique to that viewport's markup) so the locator remains unambiguous and correct rather than picking an arbitrary match",`Widen the test viewport until only one button renders in the DOM at all`,`Ignore the issue since Playwright always picks the correct one automatically`],1,`A locator matching multiple elements for different responsive variants needs to be scoped precisely to the one that is actually relevant, not resolved by blindly picking the first match.`],[`O`,`I`,"What does this pattern let the test do that plain `new DashboardPage(page)` in the test body would not?",`export const test = base.extend({
  dashboardPage: async ({ page }, use) => {
    await use(new DashboardPage(page));
  },
});
// test file:
test('shows widgets', async ({ dashboardPage }) => {
  await dashboardPage.goto();
});`,[`Nothing different; both approaches behave identically in every respect`,"It centralizes page object creation in one fixture definition, so every test simply destructures `{ dashboardPage }` without repeating the constructor call, and any future setup/teardown added to the fixture applies to all tests automatically",`Fixture-provided page objects cannot use async methods`,`This pattern requires disabling fullyParallel`],1,"Fixture-based instantiation is the standard way to remove repeated `new X(page)` calls from every test file while giving you one place to add shared setup or teardown later."],[`O`,`A`,`What subtle issue exists in this attempt to expose a reusable component from a fixture?`,`export const test = base.extend({
  header: async ({ page }, use) => {
    await use(new HeaderComponent(page));
  },
}, { scope: 'worker' }); // header fixture is worker-scoped`,["Worker-scoped fixtures cannot use the page fixture, so this throws at fixture-resolution time — `page` is inherently test-scoped, and a worker-scoped fixture cannot depend on a narrower-scoped fixture like page, since the header would otherwise outlive the specific page it was built from",`This code is completely correct with no issues`,"HeaderComponent must be declared with the `async` keyword in its class definition",`Fixtures cannot be passed to the second argument of test.extend`],0,"Fixture scope rules require a fixture to only depend on fixtures of equal or broader scope — a worker-scoped fixture depending on the inherently test-scoped `page` fixture is a lifecycle mismatch."],[`O`,`I`,"What does chaining `.filter({ has: ... })` inside this page object method achieve?",`class OrderHistoryPage {
  constructor(page) { this.page = page; }
  ordersWithRefundButton() {
    return this.page.getByRole('listitem').filter({
      has: this.page.getByRole('button', { name: 'Refund' }),
    });
  }
}`,[`It filters to only listitem elements that themselves contain a "Refund" button as a descendant — useful for finding orders that are currently refundable without the test needing to know the row/button DOM relationship`,`It removes all buttons from the returned listitems`,`has: only works with CSS selectors, not other Locators, so this throws`,`It matches every listitem on the page regardless of whether it has a Refund button`],0,`.filter({ has: <locator> }) scopes to elements that contain a descendant matching the given locator — a powerful way to query rows by whether they include a specific interactive child.`],[`S`,`I`,"You need a `logoutEverywhere()` action that must work whether it is triggered from the header menu (desktop) or a bottom sheet (mobile viewport), depending on which is currently rendered. How do you design this in the page object?",null,[`Two totally separate test suites, one for each viewport, with no shared page object code`,"A single method that first checks which trigger is present with a resilient combined locator (`.or()`), or two internal private helper methods selected based on a viewport check passed into the page object at construction, exposing one consistent public `logoutEverywhere()` to callers regardless of layout",`Playwright cannot test responsive layouts that change interaction patterns`,`Hardcode the desktop version only, since mobile is out of scope for automation`],1,"Keeping the public API (`logoutEverywhere()`) stable while handling layout-dependent variation internally (via `.or()` or viewport-aware branching inside the class) keeps tests simple regardless of which layout is active."],[`D`,`I`,`A test that used to pass now fails with "Element is not attached to the DOM" immediately after a page object method triggers a client-side route change (SPA navigation, no full page reload). What is the most likely cause?`,null,[`Client-side routed SPAs cannot be tested with Playwright`,`A Locator obtained BEFORE the SPA navigation was stored and reused after the navigation replaced the DOM subtree it pointed at; since Locators re-query lazily, using the SAME Locator instance after navigation should typically still work, so check instead whether an ElementHandle (not a Locator) was captured and reused across the navigation, which would go stale`,`goto() must always be used for SPA navigation, never a client-side link click`,`This error means the click() call itself failed`],1,`Locators recover automatically across DOM replacement because they re-query on demand — this specific error more often points to an eagerly-captured ElementHandle reference being reused after the node it pointed to was removed.`],[`R`,`B`,"You need the same `SearchPage` page object to work against both a QA environment and a staging environment with different base URLs. What is the cleanest way to parameterize this?",null,[`Hardcode the QA URL in goto() and manually edit the file before running against staging`,'Read the base URL from Playwright\'s `baseURL` config option (set per environment via env vars or separate playwright.config.ts projects) and call `this.page.goto("/search")` with a relative path — the page object itself never needs to know which environment it is running against',`Create SearchPageQA and SearchPageStaging as separate classes`,`Environment differences must be handled by launching a different browser per environment`],1,`Relying on Playwright's baseURL config for relative navigation keeps environment concerns entirely out of the page object, which only ever deals in relative paths.`],[`S`,`I`,'A `SettingsPage` component needs to expose a toggle whose accessible name changes based on state ("Enable notifications" vs "Disable notifications" — same control, different label per state). How should the page object model the single toggle?',null,["Two separate locators, `enableToggle` and `disableToggle`, that tests must manually pick between based on current state",'A single locator using `.or()` for both possible names, or better, a role/name-independent locator (like getByTestId or a stable parent-scoped role query) plus a `getByRole("switch")` combined with `isChecked()`/`toBeChecked()` for state, so one property models the one real toggle regardless of its current label',`Rename the actual UI control so it never changes its accessible name, and treat this as a product bug rather than a test design problem`,`It is impossible to locate a toggle whose accessible name changes dynamically`],1,`Toggling controls with state-dependent accessible names are best modeled by a state-independent locator (role/testid) plus Playwright's toBeChecked() for state, rather than juggling two separate name-based locators.`],[`L`,`B`,'Why is it usually better for a page object\'s `goto()` method to navigate with a relative path (`this.page.goto("/login")`) rather than a hardcoded absolute URL?',null,[`Relative paths are required by TypeScript's type system`,"A relative path automatically resolves against Playwright's configured `baseURL`, so the same page object works across every environment (local, staging, production) the config points it at, without any code changes",`Absolute URLs are not supported by page.goto() at all`,`Relative paths execute faster than absolute URLs`],1,`Relying on baseURL for relative navigation is what makes a single page object portable across environments with zero hardcoding.`],[`L`,`A`,'What is the argument for keeping `waitFor({ state: "visible" })` calls inside page object action methods (as a precondition) even though Playwright locator actions already auto-wait for actionability?',null,[`There is never a valid reason; auto-waiting always makes this redundant`,'An explicit `waitFor` can express a distinct, meaningful precondition (e.g. "this whole section must render before I touch any field in it") with a clearer failure message than a generic actionability timeout on an unrelated later action — useful for surfacing exactly which step of a multi-field form failed to load',`waitFor and auto-waiting are the same mechanism and cannot both exist in the same method`,`Explicit waitFor calls disable auto-waiting for every subsequent locator in the method`],1,`While auto-waiting handles the common case well, an explicit precondition wait can give a much clearer, earlier failure point in a multi-step method than waiting for the eventual failure of an unrelated later action.`],[`O`,`A`,`What is the design intent of exposing this pair of methods on a table page object?`,`class UsersTable {
  constructor(page) { this.page = page; }
  async sortBy(column) {
    await this.page.getByRole('columnheader', { name: column }).click();
  }
  async rowValues(rowName) {
    const row = this.page.getByRole('row').filter({ hasText: rowName });
    return row.getByRole('cell').allTextContents();
  }
}`,[`sortBy() is an action method (returns void, triggers a state change) while rowValues() is a query method (returns resolved data for the test to assert on) — the pairing follows the action/query separation convention that keeps each method's purpose unambiguous`,`Both methods are functionally identical and one is redundant`,`rowValues() is an anti-pattern because it returns an array instead of a Locator`,`sortBy() should be renamed to getSortBy() to follow proper naming conventions`],0,`sortBy (an action, imperative verb, no return value of interest) and rowValues (a query, noun-like name, returns data) are named and shaped according to the action/query convention.`],[`D`,`I`,"A page object test suite that was previously reliable starts failing right after the team enables `fullyParallel: true`, with different tests intermittently seeing each other's cart contents. The `CartPage` class itself has no bugs. What should you investigate?",null,[`CartPage locators must be redeclared as readonly to fix this`,`Whether cart state is backed by a shared backend resource (e.g. a fixed test account, or a global cart keyed by IP rather than session) that multiple concurrently-running tests are mutating simultaneously — the fix is test-level data isolation (unique test accounts/sessions per test), not a change to the page object itself`,`fullyParallel is incompatible with any page object that has more than 5 methods`,`Page objects must be marked with a special @parallel-safe decorator`],1,`When newly-parallel tests start interfering, the root cause is almost always shared backend state (not the page object code) — each test needs its own isolated data/session.`],[`R`,`I`,`You need to verify a "Save draft" button becomes enabled only after a required field is filled, across five different forms that each have their own page object. How do you avoid duplicating this check five times?`,null,[`Copy the same expect() assertion into all five test files`,"Add a small shared assertion helper (a plain function, not tied to any one page object) like `async function expectEnabledAfterFill(input, submitBtn, page) {...}`, or give each form's page object a consistent `saveDraftBtn` property so a single generic test helper can operate on any of the five using the same property name",`Merge the five separate forms into one giant page object`,`This scenario cannot be handled without rewriting all five forms to be identical`],1,`Either a small shared, page-object-agnostic assertion helper, or a consistent property naming convention across page objects, avoids repeating the same check's logic five times.`],[`S`,`A`,'A component object for a "Confirm delete" dialog is composed as a property on 8 different page objects, each instantiating their own `new ConfirmDialog(page)`. The dialog\'s locators recently changed. How many files need updating, and why?',null,[`All 8 page object files, since each has its own copy of the dialog's locators`,"Just the single `ConfirmDialog` class file — since all 8 page objects compose the SAME shared class rather than duplicating its locators, the fix in one file automatically propagates to every page object that uses it",`None — Playwright automatically detects and adapts to DOM changes`,"Only the page objects that call `.confirm()` need updating, not the ones that call `.cancel()`"],1,`This is exactly the payoff of composing a shared component object instead of duplicating its locators in every page object that uses it — one fix, propagated everywhere automatically.`],[`L`,`I`,`Why do many teams prefer page object methods to accept plain values (strings, numbers, small typed objects) rather than entire Locator objects as parameters?`,null,[`Locators cannot be passed as function parameters in TypeScript`,`Accepting plain, domain-meaningful values keeps the method's public API decoupled from any specific DOM element, whereas accepting a Locator parameter would let a caller pass in an arbitrary, possibly unrelated element, reintroducing the same DOM-knowledge leakage that POM is meant to prevent`,`Plain values execute measurably faster than Locator parameters`,`Locator objects cannot be serialized across the Node.js/browser boundary`],1,`A parameter type of "value the user would recognize" (a name, an amount) keeps the method's contract about intent; a parameter type of "some Locator" reopens the door to callers needing DOM knowledge.`],[`L`,`A`,`What is a reasonable way to keep component objects discoverable in a large codebase with 50+ of them?`,null,[`There is no way to organize this many classes; teams should avoid ever having more than 10 component objects`,`Organize them into a directory structure mirroring the app's UI structure (e.g. components/navigation/, components/forms/, components/data-display/), use a consistent naming suffix (Component or Widget), and consider a barrel/index file re-exporting them for straightforward imports`,`Put all 50+ classes into a single file to guarantee they are always found via search`,`Component objects must always be nested inside the page object that uses them, never in separate files`],1,`At scale, a predictable folder structure plus a consistent naming convention is what keeps dozens of component objects navigable — the same organizational hygiene you would apply to any growing codebase.`],[`L`,`I`,"Why might a team choose to return `Promise<this>` from a chainable action method instead of `Promise<void>`, even when the method does not navigate anywhere?",null,[`Promise<this> is required syntax for any async method`,"Returning `this` (the same page object instance) lets multiple actions on the SAME page be chained fluently in one statement (`await page.fillEmail(e).then(p => p.fillPassword(pw))` or with await-per-step chaining), which some teams find more readable for multi-step same-page forms, though it is a stylistic choice rather than a requirement",`Promise<void> methods cannot be awaited`,`This pattern is mandatory whenever a class has more than one method`],1,"Returning `this` supports optional same-page fluent chaining as a readability choice — it is not required, since sequential awaited calls on separate lines achieve the same result."],[`L`,`A`,"A team debates whether `LoginPage.login()` should live as one method that fills both fields and clicks submit, or be split into `fillCredentials()` + `submit()` as two separate calls always used together. What is the actual tradeoff?",null,[`There is no tradeoff; splitting into two methods is always strictly better`,`A single combined method is more convenient for the overwhelming common case (successful login), while splitting is only worth the extra API surface if tests genuinely need to observe or interact with the intermediate state (e.g. asserting a validation message appears before submitting) — many teams offer both, with the combined method calling the granular ones internally`,`Playwright requires exactly one public method per page object class`,`Combined methods cannot be typed with TypeScript`],1,`The choice mirrors the general granular-vs-convenience tradeoff — provide the convenient combined method for the common path, and expose the granular pieces only if a real testing need for the intermediate state exists.`],[`O`,`I`,`What does the return type annotation communicate to other developers about this method, beyond what TypeScript enforces at compile time?`,`class LoginPage {
  async login(email: string, pw: string): Promise<DashboardPage | LoginPage> {
    await this.emailInput.fill(email);
    await this.passwordInput.fill(pw);
    await this.submitBtn.click();
    if (await this.errorBanner.isVisible()) return this;
    return new DashboardPage(this.page);
  }
}`,[`Nothing beyond compilation — return type annotations have no documentation value`,`It documents that login() can fail (returning the same LoginPage on a validation error) as well as succeed (returning DashboardPage), signaling to callers that they must handle both outcomes — a union return type makes an otherwise-implicit possibility explicit and force TypeScript to check both branches at call sites that narrow the type`,`It forces the method to always throw instead of returning on failure`,`Union return types are not supported by the Playwright Locator API`],1,"A union return type like `Promise<DashboardPage | LoginPage>` makes a fallible action's two possible outcomes explicit in the type system, nudging callers to handle both."],[`O`,`B`,`What is the purpose of this method on a base class shared by many page objects?`,`abstract class BasePage {
  constructor(protected page) {}
  async currentUrlContains(fragment) {
    return this.page.url().includes(fragment);
  }
}`,[`It performs an assertion and will fail the test if the fragment is missing`,"It is a query helper returning a plain boolean for the CURRENT url — useful as a lightweight sanity check inside page object logic (e.g. deciding which of two flows to take) but not itself an assertion; a test wanting to assert on the URL should use `expect(page).toHaveURL(...)` for auto-retrying behavior instead",`It navigates the page to a URL containing the given fragment`,`It only works if fullyParallel is disabled`],1,"A plain synchronous string check like `page.url().includes(...)` has no retrying behavior — fine as an internal query, but tests wanting a resilient URL assertion should prefer expect(page).toHaveURL()."],[`D`,`B`,'A page object method calls `this.page.locator(".btn-primary").click()` and it used to work, but after a design system update, EVERY button on the page now has the class "btn-primary" (styling was consolidated). What should replace this locator?',null,["Keep the CSS class locator but add `.last()` to hope it picks the right one",'Replace the brittle styling-class-based locator with a semantic one, e.g. `page.getByRole("button", { name: "Save" })`, which identifies the element by its accessible role and name rather than a class that is now shared by every button on the page',`Rename the class in the application source code back to something unique`,`Switch to an XPath selector using the button's pixel coordinates`],1,`Styling classes are a poor locator strategy precisely because a design-system consolidation (shared classes across many elements) can silently make a previously-unique locator ambiguous — role/name locators are immune to this kind of change.`]],auth:[[`O`,`B`,'What does `page.context().storageState({ path: "auth.json" })` save?',null,[`Only browser cookies`,`Cookies, localStorage, and sessionStorage of the current browser context`,`The full browser profile including extensions and history`,`Only the URL and viewport size`],1,`storageState serialises the complete session state — cookies AND storage — so tests can resume an authenticated context.`],[`O`,`B`,`After running an auth setup project, what must test projects declare to reuse the saved state?`,null,[`globalSetup pointing to the JSON file`,`storageState: "playwright/.auth/user.json" inside the project's use: block, plus dependencies: ["setup"]`,`A beforeAll hook that calls page.reload()`,`An environment variable TEST_AUTH_FILE`],1,`storageState in use: injects the session into every new context; dependencies: ensures the file exists before tests start.`],[`O`,`I`,"What is the purpose of the `dependencies` field in a Playwright project config?",null,[`Lists npm packages the project requires`,`Declares other projects that must complete successfully before this project starts — used to ensure auth state is generated before test projects run`,`Controls which test files are included in the project`,`Specifies which browsers to use in order`],1,`dependencies enforces project ordering — the setup project runs and saves auth state before any test project that lists it as a dependency.`],[`O`,`B`,`Where is it conventional to store the auth state JSON file?`,null,[`.env file in the project root`,`playwright/.auth/ directory (and add it to .gitignore)`,`src/fixtures/ directory`,`node_modules/.cache/`],1,`playwright/.auth/ keeps auth files near the playwright config; .gitignore prevents committing real tokens to version control.`],[`O`,`I`,`What is the advantage of logging in via the API rather than the UI in auth.setup.ts?`,null,[`API login bypasses Playwright's auto-wait making tests run faster`,`API requests complete in ~100ms vs 1-2 seconds for full UI login — with 100 test workers this adds up to significant time savings`,`API login stores more data in storageState`,`API login is the only way to handle 2FA`],1,`API-based auth eliminates rendering overhead — the session is established programmatically and injected into the browser context.`],[`O`,`B`,'What happens to all tests in a project that sets `storageState: "playwright/.auth/user.json"` in its `use` block?',null,[`Only the first test in each file gets the state`,`Every new browser context in that project automatically starts with the saved cookies and storage — tests start pre-authenticated`,`The state is loaded once per worker process`,`Only tests tagged @auth get the state`],1,`Playwright creates a fresh context per test (by default) from the stored state — every test in the project begins authenticated.`],[`O`,`I`,'What does `httpCredentials: { username: "u", password: "p" }` configure?',null,[`The login form credentials for UI-based authentication`,`HTTP Basic Authentication headers sent with every request from the browser context`,`The credentials used to fetch the storageState file`,`Two-factor authentication bypass credentials`],1,`httpCredentials configures the browser context to send Basic Auth headers automatically — no UI interaction needed for HTTP Basic Auth protected resources.`],[`O`,`B`,"Why should `playwright/.auth/` be added to `.gitignore`?",null,[`Playwright fails to load storageState if it is tracked by git`,`Auth JSON files contain real session tokens and cookies — committing them exposes live credentials in version control`,`Git cannot handle binary files like storageState`,`The file changes too frequently to track`],1,`Auth state files contain real session tokens that grant authenticated access — they must never be committed to source control.`],[`O`,`I`,"What is the return value of `context.cookies()`?",null,[`A Promise<string> containing all cookie headers`,`A Promise<Cookie[]> — an array of cookie objects with name, value, domain, path, expires, etc.`,`A Map of cookie name to value`,`A string in Set-Cookie header format`],1,`cookies() returns the full cookie objects including metadata (domain, path, secure, httpOnly, expires) not just name/value pairs.`],[`O`,`I`,"What does `test.use({ storageState: { cookies: [], origins: [] } })` do when set in a specific test?",null,[`Clears all cookies and storage for that test — useful for testing unauthenticated flows within an authenticated project`,`Disables cookies for the entire test suite`,`Resets the browser to its default profile`,`Sets the storageState to an empty file path`],1,`Passing an empty storageState object overrides the project-level state — the test runs without any pre-loaded session, simulating a logged-out user.`],[`O`,`I`,`What method saves a request API context's cookies back to the browser context for storageState capture?`,null,[`request.saveState(context)`,`context.addCookies(cookies) after extracting them from the API response`,`page.reload() triggers automatic cookie sync`,`storageState() merges request and page contexts automatically`],1,`After API login, extract the session cookie from the response and inject it into the page context via context.addCookies() before calling storageState().`],[`O`,`A`,`Can worker-scoped fixtures share authentication state across tests in the same worker without re-running auth setup?`,null,[`No — authentication must always be set per test`,`Yes — a fixture with scope: "worker" creates the authenticated context once per worker; all tests in that worker reuse it`,`Only if the tests are in the same file`,`Only if fullyParallel is false`],1,`Worker-scoped fixtures live for the duration of the worker — a signed-in browser context created once is shared by all tests assigned to that worker.`],[`F`,`B`,'Complete the call to save the authentication state.\n```ts\nawait page.context().______({ path: "playwright/.auth/user.json" });\n```',null,[`storageState`,`saveState`,`saveAuth`,`exportCookies`],0,`storageState() serialises cookies and storage to the specified JSON file.`],[`F`,`B`,'Complete the config to pre-load saved auth in a project.\n```ts\nuse: {\n  ______: "playwright/.auth/user.json",\n}\n```',null,[`storageState`,`authFile`,`cookieFile`,`sessionFile`],0,`The storageState option in the use block loads the JSON into every new browser context.`],[`F`,`I`,`Complete to declare a setup project dependency.
\`\`\`ts
{
  name: "chromium",
  use: { storageState: "playwright/.auth/user.json" },
  ______: ["setup"],
}
\`\`\``,null,[`dependencies`,`requires`,`after`,`waitFor`],0,`dependencies ensures the named projects complete before this project starts.`],[`F`,`I`,'Complete to configure HTTP Basic Auth.\n```ts\nuse: {\n  httpCredentials: { username: "user", ______: "pass" },\n}\n```',null,[`password`,`secret`,`token`,`pass`],0,`httpCredentials takes { username, password } — matching the HTTP Basic Auth challenge fields.`],[`F`,`B`,'Complete to set a localStorage value before a test.\n```ts\nawait page.evaluate(() => localStorage.______("featureFlag", "enabled"));\n```',null,[`setItem`,`set`,`put`,`add`],0,`localStorage.setItem(key, value) is the standard Web Storage API method.`],[`F`,`I`,`Complete to add a cookie to the context.
\`\`\`ts
await context.______([{
  name: "session",
  value: "tok123",
  domain: "example.com",
  path: "/",
}]);
\`\`\``,null,[`addCookies`,`setCookies`,`pushCookies`,`injectCookies`],0,`context.addCookies() accepts an array of cookie objects and adds them to the context.`],[`F`,`I`,'Complete to import the base test as setup in an auth setup file.\n```ts\nimport { ______ as setup } from "@playwright/test";\n```',null,[`test`,`expect`,`request`,`chromium`],0,`Aliasing test as setup is a readability convention for auth setup files — setup("authenticate", …)`],[`E`,`B`,`What is wrong?
\`\`\`ts
// auth.setup.ts
setup("authenticate", async ({ page }) => {
  await page.goto("/login");
  await page.fill("#email", "user@test.com");
  await page.fill("#pwd", "secret");
  // saved immediately — before login completes
  await page.context().storageState({ path: "auth.json" });
  await page.click("button[type=submit]");
});
\`\`\``,null,[`page.fill() is not allowed in setup files`,`storageState() is called before the login form is submitted — the saved state will not contain a valid session`,`page.context() is undefined before navigation`,`auth.json must have a .json extension`],1,`Always save storageState after the login succeeds — confirmed by a URL change or a visible authenticated element.`],[`E`,`I`,`What is wrong?
\`\`\`ts
// playwright.config.ts
projects: [
  { name: "setup", testMatch: /auth\\.setup\\.ts/ },
  {
    name: "chromium",
    use: { storageState: "playwright/.auth/user.json" },
    // missing dependencies
  },
],
\`\`\``,null,[`testMatch regex is wrong`,`Missing dependencies: ["setup"] — Playwright may run the chromium project before setup creates the state file, causing "file not found" errors`,`storageState must use an absolute path`,`The setup project must come last in the array`],1,`Without dependencies, Playwright can run projects in any order — the state file may not exist yet when chromium tests start.`],[`E`,`B`,`What is wrong?
\`\`\`ts
await context.addCookies({
  name: "session",
  value: "abc",
  domain: "example.com",
  path: "/"
});
\`\`\``,null,[`addCookies requires httpOnly: true`,`addCookies expects an array, not a plain object — wrap the cookie in square brackets: addCookies([{ … }])`,`domain must include the protocol (https://)`,`path must be omitted for session cookies`],1,`addCookies always takes an array — even when adding a single cookie.`],[`E`,`I`,"What is wrong?\n```ts\n// playwright/.auth/user.json is committed to git\n```",null,[`JSON files cannot be committed to git`,`auth JSON files contain live session tokens — committing them exposes real credentials in git history even after later deletion`,`JSON files must be binary encoded before committing`,`Playwright refuses to load storageState from tracked files`],1,`Once a secret is committed to git history it is exposed — always add playwright/.auth/ to .gitignore before creating any state files.`],[`E`,`I`,`What is wrong?
\`\`\`ts
test("unauthenticated flow", async ({ page }) => {
  await page.goto("/login");
  // expects redirect to /login but gets /dashboard
});
// project has storageState: "playwright/.auth/user.json"
\`\`\``,null,[`goto() ignores storageState`,`The test runs in an authenticated context — to test unauthenticated flows, override storageState: { cookies: [], origins: [] } for this test or put it in a project without storageState`,`Authentication state expires so the test should handle both outcomes`,`page.goto() must be called after clearing cookies`],1,`Override storageState with an empty object at the test or describe level to strip the session for unauthenticated scenarios.`],[`E`,`A`,`What is wrong?
\`\`\`ts
setup("authenticate", async ({ page }) => {
  await page.goto("/login");
  await page.getByLabel("Email").fill("user@test.com");
  await page.getByLabel("Password").fill("hunter2");
  await page.getByRole("button", { name: "Sign in" }).click();
  await page.context().storageState({ path: "playwright/.auth/user.json" });
});
// No verification that login succeeded
\`\`\``,null,[`storageState() automatically checks for a valid session`,`Missing confirmation (waitForURL / expect) that login succeeded before saving state — if the login fails silently, an unauthenticated state is saved and all tests will fail mysteriously`,`setup files do not support async functions`,`The password should be loaded from process.env`],1,`Always confirm successful authentication (URL change, heading, or cookie presence) before calling storageState() — otherwise you persist a failed login state.`],[`D`,`B`,`All tests fail with "You are not logged in" even though the auth setup project passes. What should you check first?`,null,[`Re-run the auth setup manually`,`Verify the storageState path in the use block matches the path used in auth.setup.ts — a typo means the state file is never found`,`Increase the test timeout`,`Clear the Playwright cache`],1,`A mismatched file path means tests start with an empty context even though the state file was written correctly.`],[`D`,`I`,`Auth setup passes locally but fails in CI with "ENOENT: playwright/.auth/user.json". What is the likely cause?`,null,[`CI uses a different version of Playwright`,`The playwright/.auth/ directory does not exist in CI — create it in globalSetup or via a CI script step before running tests`,`The JSON file is too large for CI environments`,`CI blocks file system writes`],1,`The directory must exist before storageState() tries to write to it — create it in setup or as a CI step (mkdir -p playwright/.auth).`],[`D`,`I`,`Tests that use storageState are randomly failing mid-suite with auth errors. What is the most likely cause?`,null,[`storageState files are corrupted by parallel test runs`,`The session token in the state file expired during the test run — sessions with short TTLs (< test run duration) need a refresh strategy or per-test re-auth`,`Playwright has a bug with storageState in parallel mode`,`The auth file path must be absolute in CI`],1,`Short-lived sessions expire mid-run — increase TTL on the test environment, use cookies with longer expiry, or implement periodic re-auth in a worker fixture.`],[`D`,`B`,`A test in an "admin-tests" project is behaving like a regular user. What should you verify?`,null,[`Verify the test file has the .test.ts extension`,`Verify the admin-tests project references the admin storageState file (admin.json) and that admin.setup.ts ran and populated that file`,`Restart the Playwright test runner`,`Check that the admin user exists in the database`],1,`Each role needs its own state file and its own setup project — a typo in the path silently loads the wrong session.`],[`D`,`I`,"`context.cookies()` returns an empty array even after login. Why?",null,[`Playwright does not capture HttpOnly cookies`,`The login response may set cookies via JavaScript (document.cookie) which context.cookies() does capture, OR the cookies are set on a different domain/path — check the domain filter`,`cookies() requires await on every access`,`Cookies are only visible after page.reload()`],1,`context.cookies() supports a URLs filter — without it, all context cookies are returned including HttpOnly ones. Check if the server sets cookies on a subdomain not matched by the current page URL.`],[`D`,`A`,`Two workers are running setup projects simultaneously and intermittently one writes a corrupted auth.json. What is the architectural fix?`,null,[`Set workers: 1 to disable parallelism entirely`,`Give each role its own state file path — never have two setup projects write to the same file concurrently. Use unique paths: admin.json, user.json, editor.json`,`Use file locking in globalSetup`,`Store auth state in a database instead`],1,`Concurrent writes to the same file cause corruption — every role must have a distinct output path. One setup project = one state file.`],[`C`,`B`,'Complete to confirm login before saving state.\n```ts\nawait page.getByRole("button", { name: "Sign in" }).click();\nawait page.______("/dashboard");\nawait page.context().storageState({ path: "auth.json" });\n```',null,[`waitForURL`,`goto`,`assertURL`,`checkURL`],0,`waitForURL confirms the navigation succeeded — proof that authentication worked before saving state.`],[`C`,`I`,"Complete the config for an unauthenticated test within an auth project.\n```ts\ntest.use({ storageState: { ______: [], origins: [] } });\n```",null,[`cookies`,`headers`,`sessions`,`tokens`],0,`An empty cookies array with empty origins clears the pre-loaded session for this test.`],[`C`,`I`,'Complete to read a cookie value.\n```ts\nconst allCookies = await context.______();\nconst sessionCookie = allCookies.find(c => c.name === "session");\n```',null,[`cookies`,`getCookies`,`readCookies`,`fetchCookies`],0,`context.cookies() returns the full array of cookie objects for the context.`],[`C`,`B`,'Complete to set a value in localStorage before the page loads.\n```ts\nawait page.goto("/app");\nawait page.evaluate(() => localStorage.______("theme", "dark"));\nawait page.reload();\n```',null,[`setItem`,`set`,`put`,`insert`],0,`localStorage.setItem(key, value) is the standard Web API to write a value.`],[`C`,`A`,'Complete to dispose the API context after saving auth state.\n```ts\nawait page.context().storageState({ path: "auth.json" });\nawait apiCtx.______();\n```',null,[`dispose`,`close`,`destroy`,`end`],0,`apiCtx.dispose() releases the API request context and its underlying connections.`],[`L`,`B`,`Why does Playwright recommend a separate auth setup project rather than a beforeAll hook in every spec file?`,null,[`beforeAll hooks cannot call page.goto()`,`A dedicated setup project runs once and saves state to disk; beforeAll in each file would log in once per file — multiplicative overhead with many spec files`,`Setup projects run faster because they use a headless browser`,`beforeAll does not support async functions`],1,`One setup project = one login across the entire suite. beforeAll per file = one login per file = unnecessary repetition.`],[`L`,`I`,"If a project sets `storageState` at the project level but a single test uses `test.use({ storageState: … })` to override it, which value wins?",null,[`The project-level setting always wins`,`The test.use() override wins — it is the innermost scope and takes precedence over project-level use`,`They are merged, combining both state files`,`An error is thrown when both are set`],1,`Playwright uses the innermost scope — test.use() inside a file or describe overrides the project-level setting.`],[`L`,`I`,`Can you have multiple browser contexts with different auth states within the same test?`,null,[`No — a test can only have one browser context`,`Yes — use browser.newContext({ storageState: "admin.json" }) and browser.newContext({ storageState: "user.json" }) to create contexts with different sessions in one test`,`Only if the contexts use different browsers`,`Only in API tests, not in UI tests`],1,`Multiple contexts in one test enable testing interactions between different authenticated users — e.g. an admin posting and a user receiving.`],[`L`,`B`,"What is the difference between `context.clearCookies()` and `test.use({ storageState: { cookies: [], origins: [] } })`?",null,[`They are identical`,`clearCookies() removes cookies from an already-running context at runtime; the test.use override prevents storageState from being loaded in the first place — before the test starts`,`clearCookies() only removes session cookies; test.use removes all cookies`,`test.use only works for the first test in a file`],1,`test.use with empty storageState prevents loading — clearCookies() removes them after context creation. Both achieve "no session" but at different points in the lifecycle.`],[`L`,`A`,`Why might API-based auth setup fail to authenticate the browser even when the API call returns 200?`,null,[`Playwright API contexts are always unauthenticated`,`The API sets a cookie, but if you only inspect the response body for a token and do not explicitly add the cookie to the page context via context.addCookies(), the browser has no session`,`API responses are cached and do not apply to new contexts`,`The browser ignores cookies set by request.newContext()`],1,`Request contexts and page contexts are isolated — cookies from an API response do not automatically transfer; you must extract and inject them explicitly.`],[`R`,`B`,`You have 50 tests that all need a logged-in user. How do you avoid running the login flow 50 times?`,null,[`Use beforeAll in one file and export the page object`,`Create an auth.setup.ts that logs in and saves storageState; configure test projects to depend on setup and load the state file`,`Use a shared global variable for the session token`,`Run all tests sequentially so only the first test logs in`],1,`One auth setup project + storageState in the project use block = single login for all 50 tests, regardless of parallelism.`],[`R`,`I`,`Your app has three roles: admin, editor, viewer. How do you structure the auth setup?`,null,[`One setup file that logs in as all three and saves to one JSON`,`Three separate setup projects (admin.setup.ts, editor.setup.ts, viewer.setup.ts) each saving to its own state file; three test projects each with a different storageState`,`One setup file with a loop that saves different JSON based on role`,`Use environment variables to switch roles between test runs`],1,`One state file per role keeps sessions isolated and allows each test project to run concurrently with the correct identity.`],[`R`,`I`,`You need to test the login page itself (unauthenticated) in a project that normally uses storageState. How?`,null,[`Create a separate project with no storageState just for login tests`,`Override storageState per test: test.use({ storageState: { cookies: [], origins: [] } }) — or put login tests in a separate project without storageState`,`Delete the auth.json file before the login test runs`,`Use page.context().clearCookies() at the start of the test`],1,`Both approaches work; a dedicated project is cleaner for many unauthenticated tests; test.use override is simpler for one or two cases.`],[`R`,`A`,`Your test suite runs on CI with short-lived JWT sessions (1-hour TTL) and a 2-hour test run. How do you handle session expiry?`,null,[`Reduce the number of tests to fit in one hour`,`Implement a worker-scoped fixture that re-authenticates when the session nears expiry, or split the suite into two CI jobs each under one hour`,`Disable session expiry in the test environment`,`Use persistent cookies instead of JWTs`],1,`A worker-scoped fixture can check session validity before each test batch and re-authenticate if needed — keeping the suite running within TTL constraints.`],[`R`,`B`,`You want to verify that a non-admin user cannot see the admin panel. How do you write this test?`,null,[`Check the page source for admin-related HTML`,`Load the user storageState, navigate to /admin, and assert that either a 403 page is shown or the admin-specific element is not visible`,`Log out and back in as a non-admin before checking`,`Check the API response headers for the admin route`],1,`The test reuses pre-authenticated user state, navigates to the restricted route, and asserts the expected access-denied outcome.`],[`S`,`I`,`Your auth.setup.ts runs in the "setup" project but your storageState file sometimes is empty (0 bytes). What are two likely causes?`,null,[`storageState() always writes an empty file on first run`,`1) Login failed silently and storageState was called on an unauthenticated context; 2) The playwright/.auth/ directory does not exist and the file was created empty instead of throwing. Fix: verify login success before saving, and ensure the directory exists.`,`Playwright has a race condition when writing JSON`,`The JSON file must be seeded with { cookies: [], origins: [] } before storageState() can write to it`],1,`Both silent login failure and missing directory produce deceptive "empty state" bugs — always validate auth and pre-create the directory.`],[`S`,`A`,`A test that creates two contexts (admin and user) to simulate a permission handoff is flaky. The admin action sometimes appears in the user context. What is the architectural issue?`,null,[`Two contexts cannot share the same browser instance`,`The test is sharing a single Page instance between contexts accidentally — each context must navigate its own page independently; actions on admin's page should never reference user's locators and vice versa`,`Contexts share cookies by default — add context isolation in the config`,`The test needs await page.waitForLoadState() between context switches`],1,`Context isolation only holds if you keep pages separate — accidental cross-context page references break isolation. Name your pages clearly: adminPage and userPage.`],[`S`,`I`,`You need to test a feature that is only visible for 24 hours after a user's first login. How do you simulate this without waiting a day?`,null,[`Fast-forward the system clock using process.env.NODE_ENV`,`Inject a fake "firstLoginAt" timestamp directly into localStorage or a cookie before the test using page.evaluate() or context.addCookies() — then verify the feature is visible`,`Use Playwright's time mocking API`,`Create a test user that always reports first-login state`],1,`Direct state injection skips real-time dependencies — set the timestamp programmatically so the feature condition is met without time travel.`],[`S`,`A`,`Your storageState approach works for cookie-based auth, but your app uses short-lived tokens refreshed by a service worker. storageState does not capture service workers. What is your strategy?`,null,[`Disable the service worker in tests using test.use({ serviceWorkers: "block" }) and use a long-lived test token in localStorage instead`,`Use a different browser for service-worker tests`,`storageState does capture service worker state — no change needed`,`Add the token to playwright.config.ts use block`],0,`Blocking service workers and providing a long-lived test token via localStorage or a cookie bypasses the refresh cycle entirely — suitable for test environments where the token TTL can be configured.`],[`O`,`B`,"What is `playwright/.auth/` used for?",null,[`Playwright's own authentication with its cloud service`,`A recommended directory for storing storageState JSON files (e.g. user.json, admin.json) — committed to .gitignore since these contain session tokens`,`The browser's auth certificate store`,`The configuration for HTTP Basic Auth credentials`],1,`playwright/.auth/ is the conventional location — add it to .gitignore so session tokens are never committed to source control.`],[`O`,`I`,'What happens if you call `browser.newContext({ storageState: "nonexistent.json" })`?',null,[`Returns an empty context`,`Throws a file-not-found error — the storageState path must exist. Run the setup project first to generate it.`,`Downloads a fresh session`,`Creates the file automatically`],1,`A missing storageState file throws at context creation — ensure the setup project runs first (use dependencies in playwright.config.ts).`],[`O`,`B`,`Can two tests in the same browser context run simultaneously?`,null,[`Yes — contexts support concurrent test access`,`No — each test should have its own browser context. Sharing a context between tests causes state leakage (cookies, storage, navigation history).`,`Yes if fullyParallel: true is set`,`Only read-only tests can share a context`],1,`One context per test is the fundamental isolation rule — contexts are cheap to create and state leakage between shared contexts causes flaky tests.`],[`O`,`I`,"What does `context.clearCookies()` do during auth testing?",null,[`Logs the user out on the server side`,`Removes all cookies from the browser context — the next request will be unauthenticated. Useful for testing session expiry scenarios.`,`Clears the storageState file`,`Resets localStorage and sessionStorage`],1,`clearCookies() simulates cookie expiry or manual logout — combine with a subsequent navigation to test the unauthenticated state.`],[`O`,`B`,"What is the purpose of `httpCredentials` in `browser.newContext()`?",null,[`Sets the HTTPS certificate for MITM testing`,`Provides username and password for HTTP Basic Authentication — the browser sends these credentials automatically on every request to the protected origin`,`Stores API tokens for the request fixture`,`Sets the form-based login credentials`],1,`httpCredentials: { username, password } handles HTTP Basic Auth — the browser includes the Authorization: Basic header automatically.`],[`O`,`I`,"What does `page.context().addCookies([...])` do?",null,[`Creates a new browser context with those cookies`,`Adds cookies to the current browser context — subsequent requests include them. Use this to inject auth cookies without going through the login UI.`,`Sets cookies in localStorage`,`Replaces all existing cookies`],1,`addCookies() is the programmatic way to seed a session — faster than UI login for tests that only need cookie-based auth.`],[`O`,`A`,"What does `storageState` return when called on an unauthenticated context?",null,[`null or undefined`,`A valid JSON object with empty arrays: { cookies: [], origins: [] } — no error is thrown. This is the trap: an unauthenticated save looks like success but the state is empty.`,`An error: "Not authenticated"`,`The default browser session state`],1,`An empty storageState is valid JSON — always verify login succeeded before calling storageState() to avoid saving an empty session.`],[`O`,`I`,'When should you use `test.use({ storageState: "auth.json" })` at the describe level vs. the project level?',null,[`Always use the project level — describe-level overrides are not supported`,`Describe-level is for a subset of tests in one file that need a different auth state (e.g. admin tests mixed with user tests). Project-level applies the storageState to every test in that project.`,`They are identical in behavior`,`Describe-level is only valid for worker-scoped fixtures`],1,`Granular storageState application: project-level for homogeneous suites, describe-level when one file contains multiple roles.`],[`O`,`B`,'What does `page.evaluate(() => localStorage.getItem("token"))` allow you to test?',null,[`Server-side token storage`,`Reads the token from the browser's localStorage — useful for asserting that login stored the token correctly, or for injecting a token programmatically in a test setup`,`Reads from the Playwright token store`,`Reads the auth cookie value`],1,`page.evaluate() runs in the browser context — full access to localStorage, sessionStorage, and document properties.`],[`O`,`I`,"How does the `dependencies` key in a project config relate to auth?",null,[`It lists NPM packages required by the project`,`It ensures the listed setup projects complete before this project's tests run — used to guarantee auth.setup.ts (which creates storageState files) finishes before authenticated tests start`,`It declares which browser the project uses`,`It is an alias for testMatch`],1,`dependencies: ["setup"] is the mechanism that makes the setup → test project ordering deterministic in Playwright config.`],[`O`,`A`,'What does `context.grantPermissions(["geolocation"])` do in an auth context?',null,[`Adds geolocation to the storageState`,`Grants browser permissions to the context — if the app requests geolocation access, the browser auto-approves. Combine with context.setGeolocation({ latitude, longitude }) to set the position.`,`Requests geolocation from the OS`,`Requires the page to reload to take effect`],1,`Permissions can be pre-granted in the context — no dialog to dismiss, and the position is set programmatically for deterministic tests.`],[`O`,`I`,"What is the difference between `page.context().cookies()` and `document.cookie` accessed via `page.evaluate()`?",null,[`They return identical results`,`page.context().cookies() returns ALL cookies including HttpOnly ones. document.cookie is the browser's JS API and omits HttpOnly cookies — it cannot be used to read session cookies that have HttpOnly set.`,`page.context().cookies() only returns session cookies`,`document.cookie returns more cookies than cookies()`],1,`HttpOnly cookies are invisible to JavaScript — Playwright's cookies() API reads them directly from the browser's cookie store.`],[`F`,`B`,'Complete to save storageState to a file.\n```ts\nawait page.context().storageState({\n  ______: "playwright/.auth/user.json"\n});\n```',null,[`path`,`file`,`output`,`save`],0,`path: specifies where the JSON file is written — this is the file consumed by subsequent test contexts.`],[`F`,`I`,'Complete to load a storageState in a new browser context.\n```ts\nconst context = await browser.newContext({\n  ______: "playwright/.auth/admin.json",\n});\n```',null,[`storageState`,`authState`,`session`,`auth`],0,`storageState: path loads cookies and storage into the new context — the app sees it as an already-authenticated session.`],[`F`,`B`,`Complete to add a cookie to the current context.
\`\`\`ts
await page.context().addCookies([
  {
    name: "session_id",
    value: "abc123",
    domain: "localhost",
    ______: "/",
  },
]);
\`\`\``,null,[`path`,`url`,`route`,`origin`],0,`path: "/" means the cookie is sent on all paths under the domain.`],[`F`,`I`,`Complete to apply storageState to all tests in a describe block.
\`\`\`ts
test.describe("Admin suite", () => {
  test.______({ storageState: "playwright/.auth/admin.json" });
  test("see admin panel", async ({ page }) => { ... });
});
\`\`\``,null,[`use`,`setup`,`configure`,`apply`],0,`test.use() at the describe level applies fixture overrides to all tests in that describe block.`],[`F`,`A`,`Complete to verify no auth state leaks between tests.
\`\`\`ts
test.afterEach(async ({ context }) => {
  const cookies = await context.______();
  expect(cookies).toHaveLength(0); // fresh context has no cookies
});
\`\`\``,null,[`cookies()`,`getCookies()`,`allCookies()`,`readCookies()`],0,`context.cookies() returns all cookies — asserting length 0 confirms the test context started clean.`],[`F`,`B`,'Complete to set HTTP Basic Auth credentials.\n```ts\nconst context = await browser.newContext({\n  ______: { username: "admin", password: "secret" },\n});\n```',null,[`httpCredentials`,`basicAuth`,`credentials`,`auth`],0,`httpCredentials: sets the Basic Auth username/password for all requests in the context.`],[`F`,`I`,"Complete to clear all cookies from the context.\n```ts\nawait page.context().______();\n```",null,[`clearCookies()`,`deleteCookies()`,`resetCookies()`,`removeAllCookies()`],0,`clearCookies() removes all cookies from the browser context — the session is effectively destroyed.`],[`F`,`A`,'Complete to inject a JWT into localStorage before the test.\n```ts\nawait page.evaluate(token => {\n  localStorage.setItem("auth_token", ______);\n}, jwtToken);\n```',null,[`token`,`jwtToken`,`auth`,`value`],0,`token is the parameter received from the evaluate() callback — the second argument to evaluate() is passed as the callback's parameter.`],[`F`,`I`,"Complete to read the storageState from the current context.\n```ts\nconst state = await page.context().______();\nconsole.log(state.cookies);\n```",null,[`storageState()`,`getState()`,`exportState()`,`serialize()`],0,`storageState() without a path argument returns the state as an in-memory object rather than writing to a file.`],[`E`,`B`,`What is wrong?
\`\`\`ts
test("admin test", async ({ page }) => {
  await page.goto("/admin");
  // expects admin auth — but context has no storageState
});
\`\`\``,null,[`goto requires awaiting the page load`,`The test context has no auth state — add storageState to the project config or use test.use({ storageState: "playwright/.auth/admin.json" })`,`Admin tests cannot use goto`,`The context must call login() before goto`],1,`Without storageState the context is unauthenticated — admin routes will redirect to login or return 403.`],[`E`,`I`,`What is wrong?
\`\`\`ts
// auth.setup.ts
await page.fill("#email", user.email);
await page.fill("#password", user.password);
await page.click("button[type=submit]");
await page.context().storageState({ path: "auth.json" });
// No wait after login click
\`\`\``,null,[`storageState() must be called before click()`,`The click triggers navigation/API calls to log in — without waiting for the session to be established (await page.waitForURL("/dashboard") or await expect(page.getByRole("heading")).toBeVisible()), storageState() may be called before the session cookie is set.`,`fill() is deprecated`,`storageState cannot be called on a context`],1,`Always wait for login to complete before capturing storageState — a premature save captures pre-login cookies.`],[`E`,`B`,`What is wrong?
\`\`\`ts
// playwright.config.ts
projects: [
  { name: "tests", use: { storageState: "playwright/.auth/user.json" } },
  // No setup project defined
]
\`\`\``,null,[`projects cannot use storageState`,`The "user.json" file is referenced but there's no setup project to create it. Add a setup project with auth.setup.ts that generates the file, and declare dependencies: ["setup"] in the tests project.`,`storageState must be in globalSetup`,`The file path must be absolute`],1,`storageState files must be generated before use — missing the setup project means the file either doesn't exist or is stale.`],[`E`,`I`,`What is wrong?
\`\`\`ts
const page = await context.newPage();
await page.evaluate(() => {
  document.cookie = "session=abc; HttpOnly";
});
\`\`\``,null,[`document.cookie is not accessible via evaluate`,`JavaScript cannot set HttpOnly cookies — the HttpOnly flag can only be set by the server in a Set-Cookie response header. To set an HttpOnly cookie in tests, use context.addCookies() with httpOnly: true.`,`evaluate() does not support cookie setting`,`document.cookie only works with HTTPS`],1,`HttpOnly cookies are server-only — use Playwright's context.addCookies() to set them programmatically in tests.`],[`E`,`A`,`What is wrong?
\`\`\`ts
export default async function globalSetup(config: FullConfig) {
  const browser = await chromium.launch();
  const page = await browser.newPage();
  await page.goto(config.use!.baseURL! + "/login");
  // ... login steps ...
  await page.context().storageState({ path: "playwright/.auth/user.json" });
  // no browser.close()
}
\`\`\``,null,[`globalSetup must use webkit`,`browser.close() is missing — the browser process leaks and remains running after setup. Always call browser.close() in a finally block in globalSetup.`,`storageState cannot be used in globalSetup`,`The FullConfig type is not importable in globalSetup`],1,`Always close the browser in globalSetup — open browser processes accumulate and slow down CI if left uncleaned.`],[`D`,`B`,`Auth setup runs successfully (exit 0) but tests are failing with "not authenticated". What should you check first?`,null,[`Increase the test timeout`,`Check that storageState file exists and is non-empty. Open it in a text editor and confirm cookies/origins arrays are populated. If empty, the login in auth.setup.ts failed silently.`,`Re-run the setup project manually`,`The storageState file is too large`],1,`An empty storageState is the most common auth setup failure — inspect the file content and add assertions after login to catch silent failures.`],[`D`,`I`,`Tests using storageState pass for the first hour then start failing. The app uses 1-hour session tokens. What is the fix?`,null,[`Add retries to handle token expiry`,`Re-run auth.setup.ts as a dependency before each test run (keep it in dependencies: ["setup"]) so the token is refreshed on every CI run. Alternatively, configure the test environment to use longer-lived tokens.`,`Cache the storageState for 24 hours`,`Use keepAlive: true in the context options`],1,`Session expiry is a CI environment issue — short-lived tokens need fresh auth on each run. Use longer test tokens or re-run setup on each CI run.`],[`D`,`B`,`Multiple-role tests (admin + user) pass individually but fail in parallel because auth files overwrite each other. How do you fix it?`,null,[`Disable parallel execution for auth tests`,`Use separate file paths per role: playwright/.auth/admin.json and playwright/.auth/user.json — never write multiple roles to the same file`,`Use a mutex to coordinate file writes`,`Store both roles in one JSON file using an object`],1,`Role isolation requires separate files — one file per role prevents race conditions when parallel setup workers save state simultaneously.`],[`D`,`A`,"`page.context().cookies()` returns cookies but the app still redirects to login. How do you debug?",null,[`Cookies are not supported by the test server`,`Check the cookie domain, path, and sameSite attributes — a cookie with domain: "api.example.com" won't be sent to "www.example.com". Also verify the session token is valid server-side (not expired or revoked).`,`Clear the cookies and try again`,`Use page.reload() to apply the cookies`],1,`Cookie scoping is the most common cause — domain/path mismatch means the browser doesn't send the cookie to the right endpoint.`],[`C`,`B`,'Complete to save the auth state after login.\n```ts\nawait page.context().storageState({\n  ______: "playwright/.auth/user.json",\n});\n```',null,[`path`,`file`,`dest`,`output`],0,`path: is the destination for the storageState JSON file.`],[`C`,`I`,"Complete to set geolocation in an authenticated context.\n```ts\nawait context.______({ latitude: 37.7749, longitude: -122.4194 });\n```",null,[`setGeolocation`,`grantGeolocation`,`setLocation`,`updateLocation`],0,`setGeolocation({ latitude, longitude }) sets the browser's reported location — combine with grantPermissions(["geolocation"]).`],[`C`,`A`,"Complete to read a cookie value from the context.\n```ts\nconst cookies = await context.cookies();\nconst session = cookies.find(c => c.name === ______);\n```",null,[`"session_id"`,`"session"`,`"token"`,`"auth"`],0,`Find the cookie by name — the exact name depends on your app's session cookie name.`],[`C`,`B`,'Complete to grant camera permission before the test.\n```ts\nawait context.______( ["camera"]);\n```',null,[`grantPermissions`,`setPermissions`,`allowPermissions`,`addPermissions`],0,`grantPermissions(["camera"]) pre-approves the permission — no dialog appears when the app requests it.`],[`C`,`I`,'Complete to check if the user is authenticated by verifying a page element.\n```ts\nawait page.goto("/dashboard");\nawait expect(page.getByRole("heading", { name: ______ })).toBeVisible();\n```',null,[`"Dashboard"`,`"Welcome"`,`"Home"`,`"Login"`],0,`"Dashboard" confirms the app navigated to the authenticated page rather than redirecting to login.`],[`L`,`B`,`Why is API-based login in auth.setup.ts faster than UI-based login?`,null,[`API calls use a different network stack`,`API login bypasses the browser rendering engine — no page loads, no selectors, no waitForURL. A single request.post("/api/login") takes milliseconds vs. seconds for the full UI login flow.`,`Playwright skips validation for API calls`,`API calls are cached by the browser`],1,`Skipping the UI login flow saves 2-5 seconds per worker — critical when you have many auth contexts to set up.`],[`L`,`I`,`Why should you NOT commit playwright/.auth/ files to version control?`,null,[`The files are too large for git`,`Auth files contain live session tokens — committing them exposes valid credentials to anyone with repo access, including CI logs. Add playwright/.auth/ to .gitignore.`,`Git cannot diff JSON files`,`The setup project regenerates them from .gitignore entries`],1,`Session tokens in source control are a credential leak — .gitignore is the right fence, and CI regenerates fresh tokens on each run.`],[`L`,`I`,"What is the difference between `test.use({ storageState })` and loading storageState in a fixture?",null,[`They are identical`,`test.use({ storageState }) applies the state to ALL tests in the scope and cannot be dynamic. A fixture can conditionally load different storageState files based on fixture parameters — more flexible for multi-role suites.`,`Fixtures cannot access storageState`,`test.use() requires a file path; fixtures require an object`],1,`Fixture-based storageState loading enables dynamic selection — the fixture picks the right file based on the test's declared role or parameter.`],[`L`,`A`,`Can a single Playwright test use two authenticated sessions simultaneously?`,null,[`No — each test can only have one authenticated context`,`Yes — create two browser contexts (admin and user), each with its own storageState. Run both pages in parallel within the test to simulate multi-user interactions (e.g. admin posts a message, user receives it).`,`Only if using two different browsers`,`Two contexts share the same session in the same test`],1,`Multi-context tests are powerful for testing real-time features, collaboration workflows, and permission scenarios — each context is fully isolated.`],[`L`,`I`,"Why is the `setup` project given a separate `testMatch` pattern (e.g. `**/auth.setup.ts`) in playwright.config.ts?",null,[`Setup projects must use a specific file format`,`A separate testMatch ensures the setup project only runs auth.setup.ts — not all *.spec.ts files. Without it, setup would also pick up test files and potentially run the wrong tests in the wrong order.`,`testMatch filters by browser type`,`setup testMatch must match *.setup.js files only`],1,`Scoped testMatch keeps the setup project's scope narrow — it runs exactly the auth setup files and nothing else.`],[`R`,`B`,`You need to test an app that uses Microsoft SSO. The redirect flows through an external domain. How do you handle login in setup?`,null,[`Use page.route() to mock the SSO redirect`,`Use a test user with username/password configured for direct login (not SSO), or use the app's API to create a session token directly — bypass the SSO flow entirely in test setup`,`Set up an LDAP mock server`,`Configure httpCredentials with the SSO token`],1,`Test environments should support direct auth (API or test credentials) to avoid external SSO dependency in automated tests.`],[`R`,`I`,`You need to test an app that shows different content based on the user's timezone. How do you set the timezone in a test?`,null,[`Set process.env.TZ before the test`,`Create the context with: browser.newContext({ timezoneId: "America/New_York" }) — the browser's JS Date and Intl APIs report this timezone`,`Use page.evaluate to override Date`,`Set timezone in playwright.config.ts use block`],1,`timezoneId in the context options sets the browser's timezone — the app's timezone-dependent rendering is fully controllable.`],[`R`,`A`,`You have 100 tests split across 3 roles. How do you structure the auth setup to avoid redundant logins?`,null,[`Login in a beforeEach for each role`,`Three setup projects (admin.setup.ts, user.setup.ts, editor.setup.ts) each save their own storageState file. Three test projects (one per role) each declare their respective setup as a dependency — 3 logins total regardless of how many tests run.`,`One shared setup project that logs in as all 3 roles`,`Use globalSetup with 3 concurrent browser instances`],1,`N roles = N setup projects and N storageState files — each test project depends on its role's setup and inherits the auth. Scales linearly with roles, not with test count.`],[`S`,`B`,`Auth state becomes stale after 30 minutes in CI. Tests start failing mid-run. What is the architecture fix?`,null,[`Add retries to every test`,`Renegotiate auth: set up a worker-scoped fixture that refreshes the storageState at the start of each worker process, or configure the test environment to use non-expiring session tokens`,`Add keepAlive ping requests to maintain the session`,`Split the test run into 25-minute chunks`],1,`Worker-scoped auth refresh or long-lived test tokens are the architectural solutions — workarounds like retries hide the root cause.`],[`S`,`I`,`You want to test that a user cannot access an admin endpoint, and that the 403 response is handled gracefully. How do you write this?`,null,[`Mock the 403 with route.fulfill()`,`Load user storageState (non-admin) in the context, navigate to the admin endpoint or call it via page.request, assert the 403 status, then verify the app shows the correct "Access denied" message in the UI`,`Use the admin storageState and expect the route to be accessible`,`Add the user role to the admin group temporarily`],1,`Using real auth state for the non-admin user tests the actual permission boundary — no mocking needed when the real session is available.`],[`S`,`A`,`A test logs in via UI in beforeEach — the suite takes 45 minutes. How do you reduce it to 5 minutes?`,null,[`Increase workers to 10`,`Convert to setup-project pattern: login once in auth.setup.ts, save storageState, configure all tests to use it. Remove the UI login from beforeEach — tests start pre-authenticated and skip the entire login flow.`,`Add trace: "on" to diagnose the slowness`,`Cache the login page in a route handler`],1,`Setup-project auth is the standard solution to login-in-beforeEach anti-pattern — login once per CI run, not once per test.`],[`O`,`A`,`What does this globalSetup accomplish for the rest of the suite?`,`// global-setup.ts
async function globalSetup() {
  const browser = await chromium.launch();
  const page = await browser.newPage();
  await page.goto('/login');
  await page.getByLabel('Username').fill('admin');
  await page.getByLabel('Password').fill('secret');
  await page.getByRole('button', { name: 'Log in' }).click();
  await page.context().storageState({ path: 'auth.json' });
  await browser.close();
}`,['It logs in ONCE before the entire test run and saves the resulting session (cookies/localStorage) to a file, which other tests can then load via `storageState: "auth.json"` to start already authenticated — avoiding a repeated login flow in every single test',`It logs in fresh for every single test file automatically, with no file caching`,`auth.json is regenerated automatically before every individual test, ignoring any caching benefit`,`It only works for a single test file, not the whole suite`],0,`This globalSetup + storageState pattern is the standard way to pay the "login cost" exactly once per run instead of once per test.`],[`R`,`I`,`Your app has distinct admin and regular-user roles with different UI/permissions. How do you structure Playwright tests that need to run as different authenticated roles?`,null,["Generate separate storageState files per role (e.g., admin.json, user.json) via separate login flows, then configure separate `projects` (or use `test.use({ storageState: 'admin.json' })` per describe block) so each test group runs authenticated as the correct role",`Only one storageState file can exist per project; role-based testing requires manually logging in inside every single test`,`Use the same storageState for all roles and manually elevate permissions via direct database writes before each test`,`Role-based access cannot be tested with Playwright; it requires a separate manual QA process`],0,`Multiple storageState files, one per role, scoped via projects or test.use(), is the standard multi-role authentication testing pattern.`],[`L`,`I`,`What is a tradeoff of authenticating via a direct API call (login endpoint + cookie injection) versus authenticating by driving the actual login UI form?`,null,[`API-based login is much faster and decouples unrelated tests from the login UI's own correctness, but it means the login FORM itself is never exercised by those tests — a small number of dedicated tests should still drive the real login UI to cover that flow directly`,`API-based login is strictly worse in every way and should never be used`,`UI-based login is always faster than an API call because it avoids network round-trips`,`There is no meaningful difference; both approaches test exactly the same thing`],0,`This is the classic speed-vs-coverage tradeoff — use API login for setup convenience in most tests, but keep a small, explicit set of tests that verify the actual login UI works.`],[`S`,`I`,`A test suite uses a shared storageState file that was generated weeks ago, and a subset of tests start failing with 'session expired' errors after the backend's token TTL was shortened. What's the durable fix?`,null,[`Regenerate storageState as part of routine CI setup (e.g., via globalSetup running on every CI job, not a stale committed file) so the session is always freshly authenticated for that specific run, immune to TTL changes`,`Extend the token TTL on the backend permanently to match how often the file happens to be regenerated`,`Commit a new storageState.json file manually every single day as a scheduled task`,`Disable session expiry entirely in the test environment as a permanent workaround`],0,`A storageState file is a snapshot of a session at a point in time — regenerating it fresh as part of every CI run (rather than relying on a stale, committed file) avoids the whole class of "session eventually expired" bugs.`],[`O`,`I`,`What does this test setup verify?`,`test.use({ storageState: { cookies: [], origins: [] } });
test('redirects to login when unauthenticated', async ({ page }) => {
  await page.goto('/dashboard');
  await expect(page).toHaveURL(/\\/login/);
});`,[`That a genuinely unauthenticated user (explicitly empty cookies/storage, overriding any shared storageState default) is redirected to the login page when trying to access a protected route`,`It uses the previously logged-in session, just with an empty page title`,`storageState cannot be overridden per test; this code has no effect`,`It logs the user out via the login redirect itself, as a side effect`],0,`Explicitly passing an empty storageState is the way to override a shared authenticated default for the specific tests that need to verify UNauthenticated behavior.`],[`D`,`A`,"Tests using a shared `admin.json` storageState start intermittently failing because one test in the suite logs the admin user out (testing the logout flow itself), corrupting the session for tests that run afterward in the same worker. What's the design issue?",null,["A test that intentionally invalidates session state (like testing logout) is mutating a SHARED resource other tests depend on — it should run in its own isolated context/project (e.g., with `test.use({ storageState: undefined })` and its own fresh login), not reuse the shared authenticated session file",`Logout functionality should never be tested since it inherently conflicts with other tests`,`The fix is to run the entire suite serially so logout always runs last`,`storageState files cannot be invalidated by any test action, so this scenario is impossible`],0,`Tests that deliberately change auth state (login, logout, session expiry) should isolate their OWN session rather than share one that other unrelated tests depend on staying valid.`],[`L`,`I`,`How does testing time-based one-time-password (TOTP) 2FA differ from testing a typical password login in Playwright?`,null,[`2FA cannot be automated at all and must always be tested manually`,`Since a real authenticator app cannot be driven directly, tests typically generate the current TOTP code programmatically (using the same shared secret configured for the test account, via a library that implements the TOTP algorithm) and fill it into the code input like any other field — the login flow itself is otherwise automated normally`,`Playwright has a built-in TOTP generator accessible via context.generateTotp()`,`2FA testing requires disabling all Playwright timeouts`],1,`TOTP is just a deterministic algorithm keyed by a shared secret and the current time — generating the code programmatically in test code (given the test account's secret) is the standard approach.`],[`L`,`I`,`What is a practical approach to testing a "Remember me" checkbox that is supposed to keep a user logged in across browser restarts?`,null,[`"Remember me" cannot be tested since it depends on browser restarts`,`Log in with the checkbox checked, save storageState, then verify the resulting cookie has a long expiry (via context.cookies() and checking the expires field) rather than a session cookie (expires: -1) — or use launchPersistentContext to more closely simulate an actual browser restart with a persisted profile`,`Manually restart the physical test machine between assertions`,`Use test.setTimeout(0) to keep the browser open indefinitely between test runs`],1,`The functional contract of "remember me" shows up as cookie expiry/persistence — inspecting the cookie's expires value (or using a persistent context) verifies the behavior without literally restarting a browser.`],[`L`,`A`,"Why does `storageState` not capture data stored in IndexedDB, and what does this mean for apps that keep session/auth data there?",null,[`storageState only serializes cookies and the origin's localStorage/sessionStorage entries — it does not include IndexedDB. Apps relying on IndexedDB for auth-relevant data need a different strategy, such as re-creating that data via context.addInitScript() or accepting a slower per-test login for those specific flows`,`storageState captures IndexedDB automatically starting in recent Playwright versions`,`IndexedDB-based apps cannot be tested with Playwright at all`,`storageState fails with an error whenever IndexedDB is used by the app`],0,`storageState's documented scope is cookies plus localStorage/sessionStorage per origin — IndexedDB, Cache Storage, and Service Worker state are outside what it captures.`],[`L`,`A`,"What is the practical difference between authenticating a test via `httpCredentials`/`storageState` versus setting an `Authorization: Bearer <token>` header via `extraHTTPHeaders` in the context options?",null,[`They are exactly the same mechanism under a different name`,`extraHTTPHeaders attaches a fixed header to every request from that context — suitable for token-based APIs that check an Authorization header rather than cookies; storageState instead seeds cookies/localStorage that the app's own client-side code reads, which is the right choice for cookie-session or client-rendered-token apps`,`extraHTTPHeaders can only be used for testing API endpoints, never for UI navigation`,`storageState and extraHTTPHeaders cannot be used in the same browser context`],1,`Choose the mechanism that matches how the app actually authenticates requests — a static header for bearer-token APIs, storageState for cookie/localStorage-driven sessions.`],[`L`,`I`,"Why might a test suite prefer `launchPersistentContext(userDataDir, options)` over `storageState` for a specific subset of auth-related tests?",null,[`launchPersistentContext is strictly deprecated and should never be used`,`launchPersistentContext keeps an actual on-disk browser profile (including IndexedDB, service worker registrations, and extensions) between runs, closely simulating a real returning user across sessions — storageState only snapshots cookies and Web Storage, so it is lighter-weight but less complete for apps that depend on other browser-persisted state`,`Persistent contexts do not support cookies at all`,`storageState only works with Chromium, while launchPersistentContext works with all browsers`],1,`A persistent context is a fuller simulation of a returning real user's browser profile, while storageState is a fast, portable snapshot of just cookies and Web Storage.`],[`L`,`A`,`Why is testing an "account lockout after 5 failed login attempts" feature tricky when using a shared storageState-driven auth setup for the rest of the suite?`,null,[`Account lockout cannot be triggered by automated tests under any circumstances`,`The lockout test must deliberately create its OWN isolated test user (or reset the target account's failure counter first) rather than reuse the shared login credentials that other, unrelated tests also rely on — triggering a lockout on a shared account would break every other test using that account`,`storageState automatically resets any account lockout state before each test`,`Lockout tests must always run with workers: 1 to succeed`],1,`A test that deliberately puts an account into a bad state (locked out) must not do so to an account other tests depend on being usable — use a dedicated, disposable test user for this scenario.`],[`L`,`I`,`Why would a team choose to extract a CSRF token from a login page's HTML/cookie before performing an API-based login in auth.setup.ts, rather than skipping straight to a POST request?`,null,[`CSRF tokens are only relevant for GET requests, never POST`,`Many apps validate a CSRF token on state-changing requests (like login); the API-based login must first fetch the page (or a dedicated endpoint) to obtain the current token and include it in the login POST body/headers, or the server will reject the request even with correct credentials`,`Playwright automatically attaches valid CSRF tokens to every request`,`CSRF protection is disabled automatically when Playwright's request context is used`],1,`API-based login has to replicate whatever the real login flow does under the hood, including any CSRF token dance — skipping it means the server legitimately rejects the request.`],[`L`,`A`,`What extra step does an API-based auth setup need when your app additionally maintains session-relevant state in IndexedDB (e.g. an offline-first app caching the logged-in user profile)?`,null,[`None — storageState automatically captures IndexedDB in every Playwright version`,"Since storageState cannot capture IndexedDB, the setup would need to also seed that data separately, e.g. via `context.addInitScript()` that writes the expected IndexedDB records before the app's scripts run, or by loading the actual app once via the UI so its own code populates IndexedDB normally",`IndexedDB data is irrelevant to authentication and can always be ignored`,`addInitScript cannot interact with IndexedDB`],1,`When part of the session lives outside cookies/Web Storage, an API-only shortcut has to be supplemented with another mechanism (init scripts, or a partial UI pass) to reproduce that state.`],[`L`,`I`,"Why might running each role's auth.setup.ts login (admin, editor, viewer) via `Promise.all` in a single globalSetup function be faster than three separate setup PROJECTS run sequentially?",null,[`Promise.all cannot be used with Playwright APIs`,`Separate setup projects are typically also allowed to run concurrently by Playwright's scheduler if nothing depends on them serially — but if a team's config forces them sequentially, explicitly running the three independent logins concurrently inside one function (e.g. three separate browser contexts logging in at once) removes that artificial serialization and can meaningfully cut setup time`,`Promise.all always runs slower than sequential awaits due to Node.js event loop limits`,`globalSetup cannot open more than one browser context`],1,`Independent logins have no reason to run one after another — concurrent execution (via separate contexts, awaited together) removes unnecessary serialized setup time.`],[`L`,`A`,"Why is it important that a shared `storageState`-driven test account never be used by a test that intentionally changes the account's password or triggers a forced logout?",null,[`Playwright automatically detects and prevents password changes in tests`,`Any test that mutates the credentials or invalidates the session behind a SHARED storageState file corrupts that saved state for every other test relying on it — such tests should use their own dedicated, isolated user/session rather than the one everyone else depends on staying valid`,`Password changes are not possible from within a test at all`,`storageState files cannot be affected by anything the test does after loading`],1,`A shared piece of pre-baked state (like storageState) is implicitly a contract that nothing should invalidate it — tests that intentionally break that contract need their own isolated resource.`],[`L`,`I`,`What is the key difference between an "authentication" test and an "authorization" test, and why does the distinction matter for how storageState is used?`,null,[`They are the same category of test and the distinction is purely academic`,`Authentication tests verify that logging in/out itself works correctly (and typically must NOT rely on a pre-authenticated storageState, since they are testing the login process); authorization tests verify what an already-logged-in user of a given role is or is not permitted to do (and SHOULD use the relevant role's storageState to start pre-authenticated)`,`Authorization tests never need a browser context`,`Authentication tests can only be written using the request fixture, never the page fixture`],1,`Whether a test should start pre-authenticated (via storageState) depends on whether it is exercising the authentication mechanism itself versus exercising permissions of an already-authenticated user.`],[`L`,`A`,"Why might tests occasionally need to distinguish `testInfo's` per-test isolation from the fact that a `storageState`-backed browser context is otherwise identical for every test in a project?",null,[`storageState makes every test share the exact same context object, so isolation is impossible`,`Even though every test in the project loads the SAME storageState file, Playwright still creates a brand-new browser context per test by default — so each test gets its own independent copy of that initial cookie/storage snapshot; mutations one test makes (like adding an item to a cart) do not leak into another test's context even though both started from identical data`,`Playwright reuses one physical browser context across every test in a storageState-based project`,`Only the first test in a project actually receives the storageState data`],1,`storageState determines the STARTING data for each new context, not a single shared context — Playwright's default per-test context isolation is unaffected by whether that starting data came from a file.`],[`S`,`A`,"Your backend enforces `SameSite=Strict` on its session cookie, and a test flow requires simulating a user arriving via a cross-origin link (e.g. an external referral) before completing an authenticated action. What issue might this cause, and how do you address it in tests?",null,[`SameSite=Strict cookies are never sent on any cross-origin navigation, which is expected browser behavior and mirrors production — the test should simply structure its navigation flow (e.g. navigate to the app's own origin first) the same way a real user would, rather than trying to force the cookie through where the browser genuinely would not send it`,`SameSite=Strict is a Playwright-specific setting that can be disabled via playwright.config.ts`,`Playwright automatically converts Strict cookies to Lax during test runs`,`This scenario cannot occur since Playwright ignores SameSite entirely`],0,`SameSite=Strict cookie behavior is a real browser security mechanism Playwright faithfully reproduces — a test relying on it being sent cross-site would fail in production too, so the test should mirror the real navigation path instead of forcing an unrealistic exception.`],[`L`,`I`,`Why might WebKit (Safari engine) sometimes behave differently from Chromium regarding storageState-loaded cookies, especially around third-party or cross-site cookies?`,null,[`WebKit does not support cookies at all`,`Different browser engines implement varying levels of cross-site tracking prevention (e.g. WebKit's Intelligent Tracking Prevention), which can restrict or expire certain cookies differently than Chromium — a storageState snapshot that behaves correctly in Chromium tests may need verification against WebKit if the app sets cross-site or third-party cookies`,`WebKit requires a completely different storageState file format`,`Playwright does not support running authenticated tests on WebKit`],1,`Cross-browser auth testing needs to account for engine-specific cookie policies (like ITP in WebKit) that can behave more restrictively than Chromium for cross-site cookies.`],[`L`,`A`,`What is a reasonable strategy for testing SAML-based SSO login without depending on a real external Identity Provider (IdP) during CI runs?`,null,[`SAML SSO cannot be tested in CI under any circumstances and must be skipped entirely`,`Stand up a mock/test IdP (many SAML libraries and dedicated test IdPs exist for this) configured with test users, point the test environment's SSO configuration at it, and drive the login redirect flow through the mock IdP just as a real user would — keeping the real, external IdP out of the automated test loop entirely`,`Bypass SSO by hardcoding a valid session cookie copied from a production login, committed to the repo`,`Disable SAML entirely in the test environment and always use plain username/password instead, silently skipping SSO-specific coverage`],1,`A controlled mock IdP lets you exercise the real SAML redirect/assertion flow deterministically in CI, without depending on (or hammering) a real third-party identity provider.`],[`L`,`I`,"What does storing a decoded JWT's `exp` claim check give you that simply trusting storageState blindly does not?",null,[`Nothing; JWTs cannot be decoded outside of the server that issued them`,'Explicitly decoding the token (e.g. from a cookie or localStorage value) and comparing its `exp` timestamp to the current time lets a worker-scoped fixture proactively detect an about-to-expire or already-expired session and refresh it BEFORE tests start failing with confusing "not authenticated" errors',`JWTs are always valid indefinitely once issued, making exp irrelevant`,`Playwright automatically validates JWT expiry before every test`],1,`Proactively checking a token's expiry claim lets you re-authenticate ahead of time, rather than reactively debugging mysterious auth failures partway through a long test run.`],[`L`,`A`,"Why can mocking the login API response via `page.route()` be a reasonable strategy for tests that only need to reach an authenticated app STATE quickly, without a real backend login round-trip?",null,[`page.route() cannot intercept authentication-related requests`,`Intercepting the login request/response entirely and fulfilling it with a canned success response (plus manually setting the resulting cookie/localStorage) removes any dependency on the real backend's login behavior or latency for tests that only care about the authenticated app itself — though this sacrifices coverage of the real login endpoint, which dedicated login tests should still exercise separately`,`page.route() automatically bypasses all authentication checks in the browser`,`This approach is identical in effect to using a real storageState file, with no tradeoffs`],1,`Route mocking a login endpoint is a valid speed/isolation optimization for tests that are not actually about login itself — as long as a smaller set of dedicated tests still exercises the real backend login.`],[`L`,`I`,`Why should a test that verifies "session already active elsewhere" (single-session-per-user enforcement) behavior be structured carefully with storageState?`,null,[`This behavior cannot be triggered from Playwright since it requires two physical devices`,`The test needs to open TWO separate browser contexts (or sequential logins for the same dedicated test account) to simulate two concurrent sessions, and must use a test account not shared with other tests, since triggering this feature will likely invalidate one of the two sessions — an isolated account avoids disrupting unrelated tests that assume their storageState always stays valid`,`Single-session enforcement is purely a UI-only feature that never involves the backend`,`storageState files automatically merge when two contexts use the same file simultaneously`],1,`Simulating "logged in elsewhere" needs two independent sessions for the same account, deliberately created in a way (dedicated account, two contexts) that will not corrupt storageState files other tests depend on.`],[`L`,`A`,'Why is combining `page.clock` (Playwright\'s clock-mocking API) with a stored session useful for testing "your session will expire in 5 minutes" warning banners?',null,[`page.clock only affects animations, not JavaScript timers or Date()`,`page.clock lets a test fast-forward the browser's notion of time deterministically, so a warning that is supposed to appear "5 minutes before session expiry" (computed via Date.now() or setTimeout in the app) can be triggered and asserted on immediately, without a real 5-minute wait or genuinely waiting for a token to expire`,`page.clock cannot be combined with storageState in the same test`,`Session expiry warnings cannot be tested since they depend on server-side timers`],1,`page.clock mocks the browser's time-related APIs (Date, setTimeout, etc.), letting client-side, time-driven UI logic like expiry warnings be tested instantly and deterministically.`],[`L`,`I`,"Why does passing `storageState` as an inline object (`{ cookies: [...], origins: [...] }`) directly in `test.use()`, rather than a file path, sometimes make sense for a specific test?",null,[`Inline objects are required; file paths are deprecated`,`An inline object lets a test construct a very specific, minimal, or deliberately partial auth state programmatically (e.g. only one cookie, or a state that simulates a corrupted session) without needing a corresponding on-disk JSON file — useful for edge-case tests that do not represent a normal, reusable login state`,`Inline storageState objects execute faster because they skip JSON parsing entirely`,`File-path-based storageState cannot be combined with project-level configuration`],1,`An inline object is convenient for one-off, deliberately unusual auth states that do not warrant creating (and maintaining) a dedicated JSON file for a single edge-case test.`],[`L`,`A`,`Why is caching a locally-generated storageState file based on its file modification time (skip regenerating auth.json if it is less than N minutes old) a reasonable LOCAL DEVELOPMENT optimization, but risky as a blanket CI strategy?`,null,[`File modification time checks are not possible in Node.js`,`Locally, skipping a redundant login on every test run saves developer iteration time and short-lived local sessions are rarely an issue within a single working session; in CI, each run is typically a fresh environment/container where a cached file from a previous run may not even exist, and relying on a stale file risks masking real login regressions that a fresh run would have caught`,`This optimization applies equally well to both contexts with no meaningful difference`,`CI environments cannot check file modification times`],1,`A dev-convenience shortcut (skip re-login if recent) trades a small chance of a stale session for faster iteration — that tradeoff usually is not worth it in CI, where fresh state and catching regressions matter more than speed.`],[`L`,`I`,"Why does the `request` fixture's underlying `APIRequestContext` also accept a `storageState` option, and what does this enable?",null,[`It has no relation to browser-based storageState; the option name is purely coincidental`,"It lets pure API tests (that never open a browser page) reuse the same cookies captured from a UI or API login, so authenticated API calls can be made directly via `request.get(...)` without needing a full browser context — useful for fast API-level assertions that still require an authenticated session",`storageState on APIRequestContext only works for GET requests`,`APIRequestContext ignores the storageState option and always starts unauthenticated`],1,`Sharing the same storageState format between browser contexts and APIRequestContext lets API-only tests reuse a session captured elsewhere, without spinning up a browser.`],[`L`,`A`,`In a multi-tenant SaaS app where each tenant is accessed via a subdomain (tenant1.app.com, tenant2.app.com), what extra consideration applies to storageState-based auth setup?`,null,[`Subdomains share cookies automatically by default in every browser, so no extra consideration is needed`,`Cookies are typically scoped by domain, so a session cookie set for tenant1.app.com will generally not authenticate a request to tenant2.app.com unless the cookie is explicitly set with a shared parent domain — each tenant likely needs its OWN storageState file (or the cookie's domain attribute needs deliberate configuration) rather than assuming one file works across tenants`,`Multi-tenant apps cannot be tested with storageState under any configuration`,`storageState files must contain a tenantId field for Playwright to route them correctly`],1,`Cookie domain scoping means a session valid on one subdomain is not automatically valid on a sibling subdomain — multi-tenant auth setups usually need one storageState per tenant.`],[`O`,`I`,`What does this auth setup accomplish, and what is the security consideration around the generated file?`,`import { test as setup } from '@playwright/test';
const authFile = 'playwright/.auth/user.json';
setup('authenticate', async ({ page }) => {
  await page.goto('/login');
  await page.getByLabel('Username').fill(process.env.TEST_USER!);
  await page.getByLabel('Password').fill(process.env.TEST_PASS!);
  await page.getByRole('button', { name: 'Log in' }).click();
  await page.waitForURL('/dashboard');
  await page.context().storageState({ path: authFile });
});`,[`It hardcodes plaintext credentials into the repository, which is a security risk`,`It reads credentials from environment variables (kept out of source control, typically injected as CI secrets) rather than hardcoding them, confirms successful login via waitForURL before saving state, and writes the resulting session to a gitignored file — the credentials themselves are never committed, only the resulting session artifact which should also stay out of version control`,`process.env values are not accessible inside a Playwright test file`,`waitForURL is unnecessary here since storageState() implicitly waits for navigation`],1,`Sourcing credentials from environment variables (rather than literals in the file) keeps secrets out of the codebase, while the resulting storageState file still needs .gitignore protection since it contains live session data.`],[`O`,`A`,`What does generating a TOTP code this way inside an auth setup accomplish?`,`import { authenticator } from 'otplib';
setup('authenticate with 2FA', async ({ page }) => {
  await page.goto('/login');
  await page.getByLabel('Username').fill('testuser');
  await page.getByLabel('Password').fill('secret');
  await page.getByRole('button', { name: 'Continue' }).click();
  const code = authenticator.generate(process.env.TOTP_SECRET!);
  await page.getByLabel('Authentication code').fill(code);
  await page.getByRole('button', { name: 'Verify' }).click();
  await page.context().storageState({ path: 'auth.json' });
});`,[`It bypasses 2FA entirely using a Playwright-specific flag`,`It programmatically computes the current valid TOTP code from the test account's shared secret (using the same algorithm a real authenticator app implements), then submits it just like a human would type it from their phone — enabling full end-to-end 2FA login automation without any human interaction`,`TOTP secrets are single-use and this code will only work once ever`,`authenticator.generate() requires a live network connection to Google Authenticator's servers`],1,`TOTP is a deterministic, time-based algorithm — given the shared secret, any correct implementation (including a Node.js library) computes the same code a phone app would show at that moment.`],[`O`,`I`,`What does inspecting the cookie this way let you verify about "Remember me" behavior?`,`const cookies = await page.context().cookies();
const session = cookies.find(c => c.name === 'session_id');
expect(session?.expires).toBeGreaterThan(Date.now() / 1000 + 86400 * 25); // ~25+ days out`,[`That the session cookie will be deleted the moment the browser closes`,`That the session cookie has a long-lived expiry (roughly 25+ days in the future) rather than being a session-only cookie (which Playwright reports with expires: -1), confirming the "remember me" checkbox actually resulted in persistent, not session-scoped, authentication`,`That the user's password was stored in the cookie`,`That the cookie will automatically renew itself every 25 days indefinitely`],1,`A session-only cookie reports expires: -1 in Playwright's cookie objects; a genuinely persistent "remember me" cookie should show a real future timestamp far out from now.`],[`O`,`A`,`What issue does this code have when trying to set an httpOnly session cookie for a test?`,"await page.evaluate((value) => {\n  document.cookie = `session=${value}; HttpOnly`;\n}, sessionToken);",[`document.cookie cannot accept template literals`,`JavaScript running in the page (including via page.evaluate) can never actually set the HttpOnly flag — browsers silently ignore that attribute when set via document.cookie, so this cookie will be set WITHOUT HttpOnly protection rather than throwing an error; to truly set an httpOnly cookie, use context.addCookies([{ ..., httpOnly: true }]) instead`,`page.evaluate() cannot access the document object`,`This code will throw a SecurityError at runtime`],1,`HttpOnly is a server-only protection browsers enforce by ignoring attempts to set it from client-side script — Playwright's context.addCookies() is the correct API-level way to create a cookie with that flag for tests.`],[`O`,`I`,`What does adding this header to the browser context achieve for a token-authenticated API-driven SPA?`,`const context = await browser.newContext({
  extraHTTPHeaders: {
    Authorization: \`Bearer \${accessToken}\`,
  },
});`,[`It sets a cookie named Authorization visible to document.cookie`,`Every HTTP request made from this context (including the app's own XHR/fetch calls to its API) will automatically include the given Authorization header, simulating an already-authenticated session for apps that check a bearer token rather than a cookie for auth`,`It only affects requests made through the Playwright request fixture, not the browser's own network calls`,`extraHTTPHeaders can only be set at the test.use() level, never on browser.newContext()`],1,`extraHTTPHeaders applies to every network request the context makes, including the ones the app itself issues via its own JavaScript — useful for header-based (rather than cookie-based) authentication schemes.`],[`O`,`A`,`What does this worker-scoped fixture accomplish for a suite with expiring tokens?`,`let cachedState: any;
let fetchedAt = 0;
export const test = base.extend({
  freshStorageState: [async ({}, use) => {
    if (!cachedState || Date.now() - fetchedAt > 20 * 60 * 1000) {
      cachedState = await loginAndGetStorageState();
      fetchedAt = Date.now();
    }
    await use(cachedState);
  }, { scope: 'worker' }],
});`,[`It re-authenticates on every single test regardless of token age, guaranteeing freshness at the cost of speed`,`It amortizes login cost within a worker by reusing a cached authenticated state for up to 20 minutes, but proactively re-authenticates once that window has passed — balancing setup speed against the risk of using an expired session for tests that run late in a long CI job`,`It permanently disables authentication for that worker after the first login`,`loginAndGetStorageState() is called once per test file, not once per worker`],1,`Caching with a time-based freshness check inside a worker-scoped fixture is a common way to get the speed benefit of shared auth state while still refreshing before a token would actually expire.`],[`O`,`I`,`What is the purpose of decoding the JWT in this helper before running tests that depend on a valid session?`,`function isTokenExpiringSoon(jwt: string, bufferSeconds = 60) {
  const payload = JSON.parse(Buffer.from(jwt.split('.')[1], 'base64').toString());
  return payload.exp * 1000 < Date.now() + bufferSeconds * 1000;
}`,[`It verifies the JWT's cryptographic signature to ensure it has not been tampered with`,`It only decodes the token's payload (without verifying its signature) to read the \`exp\` (expiry) claim, then checks whether the token will expire within the given buffer window — useful as a proactive "should I re-authenticate before running more tests" check, though it does NOT validate the token's authenticity`,`Buffer.from cannot be used to decode base64 strings in Node.js`,`JWTs do not have a standard exp claim, so payload.exp is always undefined`],1,`This is a lightweight decode-only check for expiry timing, not a security validation — its purpose is proactive test-suite housekeeping (refresh before expiry), not verifying the token's trustworthiness.`],[`O`,`A`,"What does using `context.addInitScript()` this way achieve for testing an app that reads a feature-relevant timestamp from IndexedDB on startup?",`await context.addInitScript(() => {
  indexedDB.open('app-db').onsuccess = (event) => {
    const db = (event.target as any).result;
    const tx = db.transaction('session', 'readwrite');
    tx.objectStore('session').put({ firstLoginAt: Date.now() - 23 * 60 * 60 * 1000 });
  };
});`,[`addInitScript only runs inside Node.js and cannot touch browser APIs like indexedDB`,`It injects a script that runs before any of the page's own scripts on every navigation in that context, pre-seeding IndexedDB with a firstLoginAt timestamp from 23 hours ago — letting a test simulate "this user's 24-hour welcome window is about to close" without storageState (which cannot capture IndexedDB) or waiting a real 23 hours`,`This script only runs once, on the very first page load, never on subsequent navigations`,`IndexedDB writes inside addInitScript are asynchronous and Playwright cannot guarantee they complete before the test proceeds`],1,`addInitScript runs before the page's own scripts on every document in that context — a good place to pre-seed browser-only storage (like IndexedDB) that storageState cannot capture.`],[`O`,`I`,`What does blocking service workers this way achieve for auth testing?`,`const context = await browser.newContext({
  serviceWorkers: 'block',
  storageState: 'playwright/.auth/user.json',
});`,[`It disables cookies entirely for the context`,`It prevents any service worker (which might otherwise intercept requests to silently refresh a short-lived token in the background) from registering or running in that context, so a test relying on the storageState-provided token stays stable for the duration of the test instead of having its session unexpectedly refreshed or replaced mid-run by service-worker logic`,`serviceWorkers: "block" also disables all JavaScript execution on the page`,`This option has no effect unless combined with httpCredentials`],1,`Blocking service workers removes a source of nondeterministic, background session-refresh behavior that could otherwise interfere with a test relying on a specific, stable token from storageState.`],[`O`,`A`,`What does this project configuration achieve for testing an app with SAML SSO in CI?`,`projects: [
  { name: 'setup', testMatch: /sso\\.setup\\.ts/ },
  {
    name: 'chromium',
    use: { storageState: 'playwright/.auth/sso-user.json' },
    dependencies: ['setup'],
  },
],
// sso.setup.ts drives the login redirect through a mock IdP configured for the test environment`,[`It requires a live connection to the production identity provider during every CI run`,`The setup project drives the full SSO redirect handshake against a MOCK IdP once, saves the resulting authenticated session, and the chromium project (declared dependent on setup) starts every test pre-authenticated without repeating the SSO redirect dance for each test`,`SSO logins cannot be captured by storageState because they involve a redirect to another origin`,`dependencies is only usable for non-SSO auth flows`],1,`Even a multi-redirect SSO flow ultimately results in cookies/storage on the app's origin, which storageState can capture just like any other login — the setup project pays that cost once for the whole suite.`],[`R`,`I`,`Your app supports WebAuthn/passkey login as well as password login, and Playwright does not provide first-class WebAuthn automation. How do you get auth-dependent tests running without manually completing a WebAuthn ceremony each time?`,null,[`WebAuthn-protected apps cannot be tested with Playwright under any circumstances`,`Keep password-based (or API-based) login as the mechanism your test SETUP uses to produce a valid session and storageState file, and cover the actual WebAuthn ceremony itself with a small, separate, more manual/CDP-based test (or accept that flow as a manual QA gap) rather than blocking the whole suite on full WebAuthn automation`,`Use context.addInitScript() to fully simulate a hardware security key's cryptographic responses in every test`,`Disable WebAuthn entirely in the test environment permanently, discarding it from any coverage plan`],1,`When a niche auth mechanism lacks first-class automation support, a pragmatic split is common: use whatever mechanism IS automatable for setup/session generation, and handle the unsupported mechanism's own correctness with narrower, more manual coverage.`],[`R`,`A`,`Your app has an "impersonate user" admin feature (support staff can act as a customer to debug their account). How should you test that impersonated actions are properly scoped and auditable?`,null,[`Impersonation is too complex to test reliably and should be excluded from automation`,`Load the ADMIN's storageState, drive the UI (or API) action that starts impersonation, then assert the resulting session reflects the target customer's permissions/data (not the admin's), and separately verify an audit log entry was created — a second context with the customer's own storageState can be used to independently confirm the customer's real permissions for comparison`,`Use the customer's storageState directly since impersonation and normal login produce identical sessions`,`Impersonation cannot be triggered without a real support staff member logging in manually first`],1,`Testing impersonation means verifying the resulting session's effective permissions/scope match the impersonated user, not the impersonator — comparing against a real customer session is a good independent check.`],[`R`,`I`,`Your CI secrets manager rotates the test account's password weekly, but your auth.setup.ts has the password hardcoded as a literal string for convenience during initial development. What should change before this reaches CI?`,null,[`Nothing — hardcoded test credentials are fine as long as the repo is private`,"Replace the hardcoded literal with `process.env.TEST_PASSWORD` (and similarly for the username), injected by the CI secrets manager at run time, so the setup always uses the current rotated credential instead of a stale value baked into the source code",`Commit the current password to a comment above the hardcoded value so it is easy to find and update manually each week`,`Disable password rotation for the test account permanently to avoid this issue`],1,`Any credential that can change (especially one rotated by policy) belongs in an environment variable sourced from a secrets manager, not a literal in version-controlled source code.`],[`L`,`I`,"Why is calling `context.clearCookies()` at the start of every test in a describe block generally a weaker isolation strategy than simply letting Playwright create a fresh browser context per test?",null,[`clearCookies() is deprecated and no longer functions`,`clearCookies() only removes cookies — it leaves localStorage, sessionStorage, and any in-memory app state from the previous test untouched, whereas a fresh context (Playwright's default, one per test) starts completely clean across all of those; explicit clearing is a partial, easy-to-forget workaround for something the default architecture already solves fully`,`clearCookies() cannot be called from inside a test, only from a fixture`,`A fresh context is slower to create than clearing cookies on an existing one, so clearCookies() is the recommended optimization`],1,`Relying on the default one-context-per-test isolation is more complete than manually clearing cookies, since manual clearing easily misses other kinds of leftover state (storage, service workers, in-memory JS state).`],[`L`,`B`,"What does a cookie object's `expires: -1` value mean when returned by `context.cookies()`?",null,[`The cookie already expired one second ago and is invalid`,`The cookie has no fixed expiry date and is a "session cookie" — it is intended to be deleted when the browser closes, as opposed to a persistent cookie which reports a real future Unix timestamp in that field`,`The cookie will never expire under any circumstances`,`The value -1 indicates a corrupted or malformed cookie`],1,`Playwright reports session cookies (no Expires/Max-Age attribute) with expires: -1 — a is a good way to programmatically distinguish "logged in for this browser session only" from "remembered" logins.`],[`L`,`A`,"Why does providing `storageState` at the project level (in `use`) create a stronger ordering requirement with `dependencies` than overriding `storageState` inline with `test.use()` inside a single test file?",null,[`There is no difference between the two in terms of ordering requirements`,"Project-level storageState is a file PATH resolved when Playwright creates a context for every test in that project, so the file must already exist on disk before any test starts — requiring a `dependencies` project to generate it first; an inline object passed to test.use() is just in-memory data with nothing to generate beforehand, so it has no such ordering requirement",`test.use() overrides can never reference a file path, only inline objects`,`Project-level storageState is resolved lazily per test, so file-existence timing never matters`],1,'A file-path storageState needs the file to exist before Playwright reads it to build a context — that dependency on prior generation is exactly what `dependencies: ["setup"]` exists to guarantee.'],[`L`,`I`,`Why is "deleting the session cookie from the browser" not equivalent to "logging the user out" from the application's perspective?`,null,[`They are always equivalent; deleting the cookie always invalidates the server-side session immediately`,`Deleting a cookie only removes the CLIENT's ability to present that session token — if the server does not also invalidate the corresponding server-side session record, the same token (if somehow replayed, e.g. from a backup or another device) could still be accepted as valid; a real logout test should verify the server-side session is actually revoked, not just that the browser no longer sends the cookie`,`Servers cannot maintain session state; all sessions are purely client-side`,`Cookies cannot be deleted by test automation, only by the server`],1,`True logout testing should verify server-side invalidation (e.g. reusing the old token now fails) not just that the client no longer holds the cookie — the two are related but not the same guarantee.`],[`L`,`I`,"Why might an app need BOTH `httpCredentials` (HTTP Basic Auth at the network/infrastructure layer) and a normal application login form configured in the same test setup?",null,[`These two mechanisms are mutually exclusive and cannot coexist`,`Some environments (often staging/QA) put an extra HTTP Basic Auth gate in front of the entire site (e.g. via a reverse proxy) purely to keep it away from the public internet, which is a completely separate concern from the application's own user login — a test may need to satisfy the infrastructure-level gate via httpCredentials AND still perform the application-level login via storageState or a UI flow`,`httpCredentials automatically also logs the user into the application`,`Only one of the two can be configured per browser context in Playwright`],1,`An infrastructure-level Basic Auth wall and the application's own login system are two independent security layers — a test environment gated by both needs both configured.`],[`L`,`A`,`Why is testing a "maximum 3 concurrent active sessions per account" feature different from testing "logging in elsewhere invalidates the previous session"?`,null,[`They test exactly the same underlying behavior with no meaningful difference`,"The concurrency-limit feature requires creating several valid, simultaneously-active sessions (e.g. via several `browser.newContext({ storageState })` calls or repeated logins) and asserting that the Nth login is REJECTED or the oldest is evicted, whereas the single-session-invalidation feature asserts that only ONE session can ever be valid and a new login always kills the previous one — different assertions about different business rules",`Concurrent session limits cannot be tested since Playwright only supports one browser context at a time`,`Both features are only testable via manual QA, not Playwright`],1,`One feature is about a cap with graceful multi-session tolerance up to N; the other is strict single-session exclusivity — the test needs to assert the specific behavior the feature actually claims.`],[`L`,`I`,`Why might a team keep the "setup" project's test (e.g. named "authenticate") visible as its own row in HTML test reports rather than hiding it, even though it is not a "real" feature test?`,null,[`Playwright provides no way to distinguish setup projects from regular test projects in reports`,`Seeing the setup step's pass/fail status directly in the report gives an immediate, unambiguous signal when login itself is broken — if it fails, every dependent test project's failures are explained by that one row rather than being mistaken for many unrelated failures, saving debugging time`,`Hiding project rows from reports is technically impossible in any Playwright version`,`Setup projects are automatically excluded from all reporters regardless of configuration`],1,`A visible, clearly-named setup step acts as an early diagnostic signal — a red "authenticate" row immediately explains a wave of downstream failures instead of looking like unrelated breakage.`],[`L`,`A`,`For an extremely long-running (multi-hour) overnight test suite with short-lived tokens, why might a server-side or scheduled token-refresh mechanism be more robust than a Playwright worker-scoped fixture that only checks/refreshes at the start of each worker's lifecycle?`,null,[`Worker-scoped fixtures always refresh continuously throughout every test, so there is no gap to worry about`,`A worker-scoped fixture only runs its setup once at worker start — if that worker then runs tests for several hours without restarting, a token could still expire mid-way through, well after the fixture's one-time check; a scheduled external refresh mechanism (or a check inside a shared, more granular fixture invoked per test or per batch) closes that gap more reliably`,`Server-side refresh mechanisms cannot be used in test environments`,`Worker-scoped fixtures automatically re-run before every single test regardless of scope`],1,`"Worker-scoped" means "runs once per worker's lifetime", not "runs periodically" — a single check at worker start does not protect against expiry hours later in a long-lived worker.`],[`L`,`A`,"Why is it risky for multiple CI branches/pipelines to reuse the exact same cache key (and therefore the same cached `playwright/.auth/user.json`) across unrelated branches?",null,[`Playwright refuses to load a storageState file that came from a CI cache`,`A stale cached auth file from one branch could be silently reused by a different branch's run, potentially masking an auth-related regression introduced by that branch's own changes, or causing confusing failures if the underlying backend/test-account state has since diverged; cache keys for such files usually should be scoped and invalidated based on inputs likely to affect the resulting session (login flow, environment)`,`CI caches are always branch-isolated automatically with no configuration needed`,`Auth files cannot be cached in CI under any circumstances`],1,`A cache is only safe when its key reflects everything that could invalidate its contents — an auth file cached without regard to which branch/commit produced it can silently paper over real regressions.`],[`L`,`I`,"Why does Playwright's ability to fill a login field via `getByLabel(...).fill(value)` not depend on the field's `autocomplete` HTML attribute, unlike a real user relying on browser autofill?",null,[`Playwright cannot fill fields that have an autocomplete attribute set`,`fill() sets the input's value directly (and dispatches the appropriate input/change events) regardless of autocomplete hints — autocomplete only affects the BROWSER's own autofill suggestion UI for a real human, which is irrelevant to Playwright's programmatic interaction`,`autocomplete="off" causes fill() to silently no-op`,`Playwright requires autocomplete="on" to locate any form field at all`],1,`Autocomplete attributes are a browser-UI convenience for real humans; Playwright's fill() bypasses that entirely by setting the value programmatically, so it works the same regardless of the attribute.`],[`L`,`A`,"What is the difference in trust and coverage between authenticating tests via a dedicated test-only backdoor endpoint (e.g. `POST /test/login-as`) versus driving the real production login flow?",null,[`They provide identical coverage and no meaningful tradeoff exists`,`A backdoor endpoint is fast and simple but exists ONLY in test/non-production builds, meaning it provides zero coverage of the real login flow's correctness (validation, redirects, cookie-setting logic) — it should be reserved for tests that are not actually about login, while a smaller set of tests still needs to exercise the genuine production login path`,`Backdoor endpoints are exposed in production for all users, so there is no security concern`,`The real login flow cannot be automated at all, making a backdoor endpoint the only option`],1,`A test-only shortcut trades coverage of the real mechanism for speed and simplicity — it is only appropriate for the majority of tests that are not specifically verifying login itself.`],[`L`,`I`,"Why should a role's `storageState` file (e.g. admin.json) typically be regenerated whenever that role's underlying permission set changes materially in the application, rather than being treated as a long-lived static artifact?",null,[`storageState files automatically adapt to permission changes without regeneration`,`A storageState file only captures session/cookie data, not permissions themselves — permissions are usually re-evaluated server-side on each request based on the account's CURRENT role, so an old file should generally still reflect new permissions automatically; the real risk is if the session token itself encodes stale role/permission claims (e.g. a JWT with an outdated roles array) that would need a fresh login to pick up the change`,`Regenerating storageState files is required by Playwright's license terms`,`storageState files expire automatically after any permission change is deployed`],1,`Whether stale storageState reflects the new permissions correctly depends on whether the session token itself encodes role data (like some JWTs do) versus permissions being looked up fresh server-side on every request.`],[`L`,`A`,`What is the tradeoff of "N roles = N logins via storageState" versus "every test logs in individually via a fixture" as the number of TESTS per role grows very large (e.g. 5,000 tests for one role)?`,null,[`They scale identically since both eventually run the same total number of tests`,`storageState decouples login cost from test count entirely — one login produces a file reused by all 5,000 tests in that role's project, while per-test fixture login would mean 5,000 separate logins; the gap between the two approaches widens as the test count per role grows, making storageState increasingly valuable at scale`,`Per-test fixture login is always faster because it avoids reading a file from disk`,`storageState files have a hard limit of 1,000 reuses before requiring regeneration`],1,`The key scaling property of storageState is that login cost is paid once per ROLE, not once per TEST — the larger the test count per role, the bigger the relative win.`],[`L`,`A`,`Why might a team deliberately keep one small "canary" test that always drives the real UI login form, even after migrating the rest of the suite to fast storageState-based setup?`,null,[`Canary tests are required by Playwright's configuration schema`,`Once the whole suite relies on a pre-generated storageState file, nothing else in the suite would ever notice if the actual login FORM broke (wrong field name, broken submit button, backend regression) since no other test drives it — a small canary test dedicated to the real login flow closes that blind spot`,`Canary tests replace the need for a setup project entirely`,`UI-driven login tests run faster than storageState-based tests, which is why canaries exist`],1,`If storageState generation itself silently starts failing or the real login UI regresses, a suite that never exercises the actual login form again would not catch it — a canary test is a deliberate guard against that blind spot.`],[`L`,`I`,"Why might a login flow that first requires accepting a cookie-consent banner cause an otherwise-correct `auth.setup.ts` to intermittently fail?",null,[`Cookie consent banners are automatically dismissed by Playwright before any test runs`,`If the login form's fields are only truly interactive (or not obscured) after the consent banner is dismissed, and the setup script tries to fill them immediately without first handling the banner, it can race against the banner's appearance — the fix is to explicitly wait for and dismiss the banner (or configure the test environment to skip it) as a first step in the setup, before touching the login form`,`Cookie consent banners only appear in production and never in test environments`,`This scenario cannot cause flakiness since Playwright always waits for full page stability before any interaction`],1,`A cookie-consent interstitial is just another precondition the setup script needs to handle explicitly and in the right order — skipping it (or racing it) is a common, easily overlooked source of setup flakiness.`],[`O`,`I`,`What ordering problem does this code have, and how would you fix it?`,`test.beforeEach(async ({ page, context }) => {
  await context.clearCookies();
});
test.describe('guest browsing', () => {
  test('shows sign-in prompt', async ({ page }) => {
    await page.goto('/account');
    await expect(page.getByText('Sign in')).toBeVisible();
  });
});
// project has storageState: 'playwright/.auth/user.json' at the project level`,[`clearCookies() runs correctly before every test in every describe block automatically scoped to only that block`,`clearCookies() in a top-level (or misplaced) beforeEach applies broadly and can accidentally strip authentication from OTHER tests in the same file that expect to be logged in; the fix is to scope the clearCookies() call specifically to the "guest browsing" describe block (or better, use test.use({ storageState: { cookies: [], origins: [] } }) at that describe level instead) so only the intended tests lose their session`,`This code is correct and requires no changes`,`clearCookies() cannot be called from inside beforeEach, only from a fixture`],1,`A cookie-clearing hook placed too broadly silently strips auth from tests that were never meant to be affected — scope such overrides as narrowly as the scenario that needs them.`],[`O`,`I`,`What tradeoff does this test-only login endpoint usage represent?`,`setup('fast authenticate', async ({ request }) => {
  const response = await request.post('/test-only/login-as', {
    data: { username: 'qa_user_42' },
  });
  const { token } = await response.json();
  // ... inject token into storage/cookie, then save storageState ...
});
// /test-only/login-as endpoint exists only in the staging build, disabled in production`,[`It provides full coverage of the real login form's behavior`,`It trades full login-flow coverage for setup speed and simplicity — this endpoint only exists in non-production builds specifically to give tests a shortcut, so it is appropriate for setup but should not be the ONLY thing exercising authentication; a separate test should still drive the real login form`,`This pattern is a security vulnerability that must never be implemented in any environment`,`request.post() cannot be used inside a setup project`],1,`A test-only backdoor is a legitimate speed optimization for SETUP, precisely because it deliberately does not exist in production — but it means nothing else in the suite is checking the real login form unless something else specifically does.`],[`O`,`A`,`What does this test verify about a "max 3 concurrent sessions" feature?`,`const sessions = await Promise.all(
  [1, 2, 3, 4].map(() => browser.newContext())
);
for (const [i, ctx] of sessions.entries()) {
  const page = await ctx.newPage();
  await page.goto('/login');
  await loginAs(page, 'shared_test_user');
}
const fourthPage = await sessions[3].pages()[0];
await expect(fourthPage.getByText(/session limit reached/i)).toBeVisible();`,[`That exactly one session can ever be active for this account at a time`,`That when a 4th concurrent login is attempted for an account already at its 3-session limit, the login is rejected with a visible "session limit reached" message — verifying the cap is enforced rather than silently allowing unlimited sessions`,`That all 4 contexts will share the exact same session token`,`That Playwright cannot open more than 3 browser contexts simultaneously`],1,`This test specifically targets a numeric CAP (reject the Nth login past the limit), which is a different assertion than "logging in elsewhere always kills the previous session".`],[`O`,`B`,`What does this code check about the cookie-consent-gated login flow?`,`await page.goto('/login');
const consentBanner = page.getByRole('dialog', { name: 'Cookie preferences' });
if (await consentBanner.isVisible().catch(() => false)) {
  await consentBanner.getByRole('button', { name: 'Accept all' }).click();
}
await page.getByLabel('Email').fill('user@test.com');`,[`It permanently disables cookies for the browser context`,`It tolerantly dismisses a cookie-consent banner if one happens to appear before proceeding to fill the login form, handling environments where the banner may or may not show (e.g. due to a prior consent cookie) without failing either way`,`It will throw if the consent banner is not present`,`isVisible().catch(() => false) is invalid Playwright syntax`],0,`The pattern conditionally handles an optional interstitial (present or not) before continuing — a reasonable use of a one-shot visibility check specifically because the banner's ABSENCE is also a valid, expected state.`],[`O`,`I`,"What does asserting on `expires` here confirm?",`const cookies = await context.cookies();
const sess = cookies.find(c => c.name === 'sid');
expect(sess?.expires).toBe(-1);`,[`That the cookie has already expired and the session is invalid`,`That the cookie is a session-only cookie (no Max-Age/Expires attribute), which is expected behavior for a login WITHOUT "remember me" checked — the session should end when the browser closes rather than persisting`,`That the cookie will expire in exactly negative one second`,`That the login attempt failed entirely`],1,`expires: -1 specifically signals "no persistent expiry set" — the right assertion for confirming a plain (non-remembered) login produced a session-only cookie.`],[`O`,`A`,"What does using two separate contexts with different `extraHTTPHeaders` accomplish in this test?",`const adminCtx = await browser.newContext({
  extraHTTPHeaders: { Authorization: \`Bearer \${adminToken}\` },
});
const userCtx = await browser.newContext({
  extraHTTPHeaders: { Authorization: \`Bearer \${userToken}\` },
});
const adminPage = await adminCtx.newPage();
const userPage = await userCtx.newPage();
await adminPage.goto('/admin/announcements/new');
// admin creates an announcement via the UI
await userPage.goto('/dashboard');
await expect(userPage.getByText('New announcement')).toBeVisible();`,[`It tests that a single user session can act as both admin and regular user simultaneously`,`It simulates two independently-authenticated, concurrently-running sessions (admin and regular user) via distinct bearer tokens on two contexts, letting the test verify a real-time cross-user interaction (admin publishes, user sees it) end-to-end`,`extraHTTPHeaders cannot differ between two contexts created from the same browser instance`,`This code requires storageState files instead of extraHTTPHeaders`],1,`Two contexts with different auth headers simulate two truly independent, differently-authenticated users interacting with the same backend at the same time — useful for cross-user or real-time feature testing.`],[`O`,`A`,`What bug does this project configuration introduce?`,`projects: [
  { name: 'setup-admin', testMatch: /admin\\.setup\\.ts/ },
  { name: 'setup-editor', testMatch: /editor\\.setup\\.ts/ },
  {
    name: 'editor-tests',
    use: { storageState: 'playwright/.auth/admin.json' }, // should be editor.json
    dependencies: ['setup-editor'],
  },
],`,[`dependencies cannot reference a project named "setup-editor"`,`The editor-tests project loads admin.json instead of editor.json — a copy-paste path mistake means every "editor" test actually runs with ADMIN permissions, which could mask permission-boundary bugs (editor tests would pass even if the app incorrectly grants editor-level access to admin-only features, since the session genuinely IS an admin)`,`testMatch regexes cannot use backslash-escaped dots`,`This configuration will fail to start because two setup projects exist`],1,`A storageState path mismatch silently runs tests under the WRONG identity — "editor" tests actually exercising admin permissions can hide real authorization bugs rather than catching them.`],[`O`,`A`,`What does this auto fixture accomplish for a suite with a short-lived token?`,`export const test = base.extend({
  ensureFreshAuth: [async ({ page }, use) => {
    const cookies = await page.context().cookies();
    const sess = cookies.find(c => c.name === 'sid');
    if (sess && sess.expires * 1000 < Date.now() + 60_000) {
      await reauthenticate(page);
    }
    await use();
  }, { auto: true }],
});`,[`It only runs when a test explicitly requests the ensureFreshAuth fixture by name`,"Declaring `auto: true` makes this fixture run automatically for EVERY test in scope without needing to be destructured, checking the session's expiry before each test and re-authenticating if it is within 60 seconds of expiring — a transparent safety net against mid-suite token expiry",`auto: true disables the fixture entirely`,`This fixture can only check expiry once per worker, not once per test`],1,`auto: true fixtures run for every test automatically, making them a good place for transparent, cross-cutting checks like proactive session-freshness that every test should benefit from without opting in explicitly.`],[`O`,`I`,"What does deriving an `APIRequestContext` from an existing authenticated `BrowserContext` like this let the test do?",`const apiContext = context.request;
const response = await apiContext.get('/api/profile');
expect(response.ok()).toBeTruthy();`,[`context.request creates a brand-new, unauthenticated API client unrelated to the browser context`,"`context.request` is an APIRequestContext that shares the SAME cookies as its parent browser context, so API calls made through it are automatically authenticated exactly as the browser is — useful for quickly verifying a backend endpoint's response without navigating any UI",`This code requires manually copying cookies from context to apiContext first`,`context.request only works for POST requests, not GET`],1,"A browser context's built-in `request` property is pre-wired to share that context's cookies, letting authenticated API assertions piggyback on whatever session the context already has."],[`O`,`I`,"What does pairing `globalSetup` and `globalTeardown` this way accomplish?",`// playwright.config.ts
export default defineConfig({
  globalSetup: require.resolve('./global-setup'),
  globalTeardown: require.resolve('./global-teardown'),
});
// global-teardown.ts
export default async function globalTeardown() {
  await fs.promises.rm('playwright/.auth', { recursive: true, force: true });
}`,[`It deletes the auth files before tests run, forcing every test to fail`,`It ensures the generated storageState files (created once by globalSetup for the whole run) are cleaned up after ALL tests finish, preventing a stale artifact from lingering into unrelated future local runs or being accidentally left behind for a CI cache to pick up`,`globalTeardown runs before globalSetup by design`,`This pattern is required for storageState to work at all`],1,`globalTeardown mirrors globalSetup — auth artifacts created once for a run should generally also be cleaned up once, at the end of that run, rather than persisting indefinitely.`],[`O`,`A`,`What is this test deliberately checking about account lockout, and why does it use a disposable account?`,`test('locks account after 5 failed attempts', async ({ page }) => {
  const throwawayUser = await createDisposableTestUser();
  await page.goto('/login');
  for (let i = 0; i < 5; i++) {
    await page.getByLabel('Email').fill(throwawayUser.email);
    await page.getByLabel('Password').fill('wrong-password');
    await page.getByRole('button', { name: 'Log in' }).click();
  }
  await expect(page.getByText(/account locked/i)).toBeVisible();
});`,[`It reuses the shared storageState account, since lockout does not affect other tests`,`It deliberately creates a fresh, disposable test user via an API call BEFORE triggering 5 failed logins, so the lockout this test intentionally causes never affects the shared account other tests rely on being usable`,`createDisposableTestUser() is unnecessary since account lockout is impossible to trigger from Playwright`,`This test will also lock out every other test in the suite regardless of the account used`],0,`A test that deliberately puts an account into a bad state (locked out) needs its own dedicated, disposable account precisely so it does not corrupt the shared account other, unrelated tests depend on staying usable.`],[`R`,`I`,`Your app occasionally shows a "we've updated our Terms of Service, please re-accept" interstitial right after login, but only for accounts that haven't seen the current version. How should your storageState-based auth setup account for this?`,null,[`Ignore the interstitial entirely; it will never appear in test environments`,`Ensure the dedicated test account used to generate storageState has already accepted the current Terms version (e.g. via a one-time API call or UI step in auth.setup.ts before saving state), and add a SEPARATE test verifying the interstitial itself for an account that has not yet accepted it`,`Delete the Terms of Service feature from the test environment permanently`,`Every single test in the suite must handle the interstitial individually`],1,`Keep the shared, reused storageState account in a state where routine tests are not interrupted by the interstitial, and cover the interstitial itself with its own dedicated, narrowly-scoped test.`],[`R`,`A`,`Your app is deployed per-region (eu.app.com, us.app.com) with completely separate user databases and session cookies scoped to each region's domain. How do you structure auth setup for a suite that must run against all regions?`,null,[`One storageState file that magically works across all region domains`,`One storageState file PER region (eu.json, us.json, ...), each generated by logging into that region's own domain, with the corresponding test project's baseURL and storageState pointed at the matching region — cookies scoped to one region's domain will not authenticate a different region's domain`,`Use httpCredentials instead of storageState for all regions uniformly`,`Regional deployments cannot be tested with Playwright since domains differ`],1,`Since session cookies are scoped per domain, each independently-domained regional deployment needs its own storageState file generated against that specific domain.`],[`R`,`A`,"Your organization wants test credentials to rotate automatically via a secrets vault (e.g. HashiCorp Vault) rather than being manually updated. How does this integrate with `auth.setup.ts`?",null,[`auth.setup.ts cannot read from anything other than hardcoded strings`,"CI injects the current secret (fetched from the vault by the pipeline itself, e.g. via a vault-integration step) into environment variables before the test run starts; auth.setup.ts reads `process.env.TEST_PASSWORD` as usual — the rotation happens entirely OUTSIDE the Playwright config, in the pipeline's secret-fetching step",`Playwright has a built-in HashiCorp Vault client accessible via playwright.config.ts`,`Secrets vaults are incompatible with Playwright test runs`],1,`The vault integration lives in the CI pipeline's setup step, not in Playwright itself — Playwright just consumes whatever the pipeline already placed into environment variables.`],[`R`,`I`,`A new engineer joins the team and needs to run the authenticated test suite locally on day one, but generating storageState requires a slow VPN-gated staging login. How can you speed up their onboarding?`,null,[`Require every new hire to manually complete the slow login before their first local test run, with no alternative`,`Provide a pre-generated, short-lived storageState file (clearly documented as for local dev only, refreshed periodically, and never committed to a public location) via a shared, access-controlled internal artifact store, or script the one-time slow setup so it only needs to run once and is then cached locally`,`Store a permanently-valid storageState file directly in the git repository for convenience`,`Disable authentication entirely in the local development environment`],1,`The goal is removing REPEATED friction, not skipping the setup process security-lessly — a short-lived, access-controlled shared artifact (or a one-time scripted setup) balances convenience with not leaking long-lived credentials.`],[`R`,`A`,`Your app must show a GDPR consent flow only for EU-region test traffic, and login behaves slightly differently as a result (an extra consent step). How do you structure test coverage for both regions without duplicating every test?`,null,[`Duplicate every single test file, once for EU and once for non-EU`,`Keep the core suite region-agnostic using a shared login page object whose goto()/login() methods internally handle the consent step ONLY when present (e.g. via a tolerant conditional or a region-aware auth setup project), and add a small number of EU-specific tests that specifically verify the consent flow itself`,`GDPR-related features cannot be tested with Playwright`,`Only test the non-EU flow and skip EU entirely since it is more complex`],1,`Keep the bulk of shared test logic region-agnostic (handling the extra step gracefully when present) and add a focused, small set of tests specifically for the region-specific behavior itself.`],[`R`,`A`,`Your app supports "Sign in with Google" via real OAuth2, and your team wants to automate a login test without depending on Google's actual production login page (which also has bot-detection that would block automation). What is a reasonable approach?`,null,[`Attempt to fully automate the real accounts.google.com login form despite the bot-detection risk`,`Use a dedicated OAuth2 test/sandbox mode if the identity provider offers one (many do, with test accounts exempt from bot-detection), or mock the OAuth callback entirely by directly hitting your own app's OAuth callback endpoint with a valid-looking authorization code/token for a test account — keeping real third-party login pages out of the automated loop`,`OAuth-based login cannot be tested with any degree of automation`,`Disable Google sign-in in the test environment and rely purely on manual QA for that flow forever`],1,`Real third-party login pages are both fragile to automate and often actively hostile to automation (CAPTCHAs, bot detection) — sandbox modes or mocking the OAuth callback keep coverage without fighting a third party's anti-bot measures.`],[`R`,`I`,`Your frontend and backend live in separate repositories with separate CI pipelines, but Playwright E2E tests (which need a valid session) run in the frontend repo's pipeline against a shared staging backend. How do you keep auth setup working reliably across this split?`,null,[`Auth setup must always live in the same repo as the backend`,`Keep auth.setup.ts in the frontend repo (where the E2E tests live) but treat the staging backend's login endpoint as a stable, versioned contract; if the backend team changes login behavior, they should coordinate with the frontend E2E suite (e.g. via a shared changelog or a canary test) since the frontend repo's CI has no visibility into backend-repo changes otherwise`,`E2E tests cannot run across a repo split under any circumstances`,`The two repositories must be merged into a monorepo before E2E testing is possible`],1,`A cross-repo split means auth setup has an implicit contract with a backend it cannot see changes to directly — communication/versioning discipline (or a canary test) is what keeps that contract from silently breaking.`],[`S`,`A`,"`auth.setup.ts` passes reliably locally but intermittently fails in CI with a saved storageState that is missing the app's expected localStorage entry, even though login itself succeeded (URL changed to /dashboard). What is the likely cause and fix?",null,[`storageState() has a known bug that randomly omits localStorage entries`,`The app likely writes that localStorage entry asynchronously (e.g. after a client-side redirect completes and some post-login JS runs), and storageState() was called before that write finished — the fix is to wait for a specific, reliable signal that the post-login JS has completed (e.g. a UI element that only appears after the write) before calling storageState(), rather than relying on the URL change alone`,`CI environments do not support localStorage`,`This is expected behavior and no fix is needed since localStorage is never captured by storageState`],1,`A URL change confirms navigation, not that every piece of post-login client-side setup (like writing to localStorage) has finished — wait for a more specific, later signal before saving state.`],[`S`,`I`,`A single test needs to show an authenticated user's dashboard and an unauthenticated visitor's public preview SIDE BY SIDE (e.g. to compare rendered content). How do you set this up in one test?`,null,[`This cannot be done in a single test; it requires two separate test files`,`Create two browser contexts within the same test — one loaded with the authenticated storageState, one with no storageState (or an explicitly empty one) — open a page in each, and assert on both pages within the same test body`,`Toggle storageState on and off using test.use() partway through a running test`,`Use test.step() to switch the same page between authenticated and unauthenticated states instantly`],1,`Multiple browser contexts within a single test are exactly how Playwright supports comparing two independently-authenticated (or one authenticated, one not) views side by side.`],[`S`,`A`,`QA occasionally sees "wrong user's data" in a staging-based CI run. Investigation shows the storageState.json used was several days old, restored from a CI cache keyed only by a lockfile hash (which had not changed across the affected branches). What is the fix?`,null,[`Disable CI caching entirely for all Playwright-related state`,`Change the cache key for the auth artifact to include something that reflects when it should actually be considered stale (e.g. a date-based component or the git commit of the auth setup script itself), or simply stop caching the generated storageState file across runs and always regenerate it fresh each CI run — auth state is cheap to regenerate and risky to cache indiscriminately`,`Increase the lockfile hash's cache TTL so it changes less often`,`This is expected behavior; storageState is meant to be reused indefinitely across all branches`],1,`Caching should only reuse an artifact when nothing relevant to its correctness could have changed — an auth state cached under a key that never reflects staleness is a recipe for exactly this kind of cross-branch data leakage.`],[`S`,`A`,`A security audit discovers an old CI log accidentally printed a test account's plaintext password months ago (now indexed by a log aggregator). What should the test suite's remediation include?`,null,[`Nothing needs to change in the test suite itself, only the leaked account's password needs rotating`,`Rotate the leaked credential (and any storageState/token derived from it) immediately, audit auth.setup.ts and any custom logging to ensure credentials are never printed (e.g. avoid console.log of full request/response bodies during login, use masked logging), and consider whether the log aggregator retention policy needs a manual purge for the exposed entries`,`Delete the entire test suite and start over from scratch`,`Leaked test credentials pose no real risk since they only grant access to a test account`],1,`Remediation covers both the immediate fix (rotate the credential and anything derived from it) and the systemic fix (stop logging secrets in the first place) — a leaked test credential can still be a real risk if the test account has meaningful access.`],[`S`,`I`,`Your app rolls out a mandatory re-consent requirement (privacy policy v2) overnight, and every test in the suite that relies on a pre-generated storageState starts failing at a consent interstitial the next morning. What is the most robust fix, both immediate and long-term?`,null,[`Immediately: regenerate storageState using an account/flow that already accepts v2 (or have auth.setup.ts explicitly accept it as part of setup). Long-term: consider adding a lightweight check in CI (or a canary test) that would have caught this kind of breaking interstitial change earlier, and make auth.setup.ts resilient to optional post-login interstitials in general`,`Immediately: disable the privacy policy feature in production to unblock tests`,`Immediately: mark all failing tests as expected failures indefinitely`,`There is no way to prevent this class of breakage from recurring`],0,`The immediate fix is making the setup account/flow compliant with the new requirement; the durable fix is designing setup to tolerate this category of change (optional interstitials) and catching such breaking changes earlier via a canary.`],[`S`,`B`,`Production requires a CAPTCHA on login, but your test environment has a feature flag to disable it so automation is possible. How do you keep this from becoming a blind spot for CAPTCHA-related bugs?`,null,[`Ignore the CAPTCHA entirely; it is not testable and does not need coverage`,`Disable the CAPTCHA for the bulk of automated tests via the flag (as is standard practice), but keep a small number of dedicated tests — manual, exploratory, or automated against a CAPTCHA-friendly test mode/sandbox if the provider offers one — that specifically verify the CAPTCHA integration itself still works end-to-end`,`Automate solving the real CAPTCHA using OCR in every single test`,`Remove CAPTCHA from production entirely since it conflicts with automated testing`],1,`Disabling an anti-bot mechanism for routine automation is standard practice — the risk it creates (an untested integration) should be covered by a small, separate, deliberate check rather than left completely uncovered.`],[`S`,`I`,"Two independent teams in the same monorepo both configured their Playwright suites to write to `playwright/.auth/user.json`, and their CI runs (which happen to run concurrently on the same shared runner pool) intermittently corrupt each other's auth file. What is the fix?",null,[`Have both teams coordinate to never run tests at the same time`,"Give each team's suite its own distinct output path (e.g. `playwright/.auth/team-a-user.json` and `playwright/.auth/team-b-user.json`), scoped by team or app, so there is no shared file for concurrent runs to collide on — the same principle as giving each ROLE its own file, applied across teams/apps instead",`Merge both teams' auth setups into a single shared setup project permanently`,`Disable parallelism for both teams' suites entirely`],1,`The fix mirrors per-role file isolation: any two things that could run concurrently and write to the same auth artifact path need their own distinct paths instead.`],[`S`,`A`,"You discover that `auth.setup.ts` was written to log in using a REAL support engineer's personal account (borrowed for convenience during a demo) instead of a dedicated test account, and that account now shows up with elevated real-world permissions in every test run. What should change?",null,[`Nothing — as long as the tests pass, the account used does not matter`,`Replace the real personal account with a dedicated, purpose-built test account that has exactly the permissions needed for the tests it supports (no more, no less), and audit whether the real account's credentials were exposed anywhere in the process (CI logs, storageState files) that now need rotating or revoking`,`Keep using the real account since creating a new test account requires extra setup effort`,`Disable the affected tests permanently rather than fixing the underlying account issue`],1,`Using a real individual's account for automated testing is both a security risk (broader access than needed, tied to a real person) and a maintenance hazard (breaks if that person's access changes) — a dedicated, minimally-scoped test account is the correct fix.`],[`S`,`A`,"Your app's login requires completing an email-based one-time passcode sent to a real inbox, with no API shortcut available. How do you automate `auth.setup.ts` for this?",null,[`This flow cannot be automated under any circumstances and must always be done manually`,`Use a test mailbox provider with an API (e.g. a disposable-inbox service or your own test SMTP catcher) to programmatically fetch the OTP email's contents right after triggering the login, extract the code, and submit it — treating "read the latest email's OTP code via API" as just another step in the setup script`,`Hardcode a single OTP value that is expected to always work in every environment forever`,`Disable OTP requirements permanently in the test environment configuration file`],1,`Email-based OTP flows are automatable as long as the test environment's inbox is reachable via some API — fetching and parsing the OTP programmatically is the standard way to close this loop.`],[`D`,`I`,"`auth.setup.ts` completes successfully and the saved `storageState.json` contains cookies, but tests still behave as if unauthenticated because the app actually reads its logged-in state from `localStorage`, not cookies. Inspecting the file shows an empty `origins` array. What is the root cause?",null,[`storageState() never captures localStorage under any circumstances`,"The setup script likely called `storageState()` from a context/page whose ORIGIN did not match where the app actually wrote to localStorage (e.g. saved right after a cross-origin OAuth redirect before returning to the app's real origin), or the login's localStorage write happens asynchronously after the point storageState() was called — check both the origin and the timing",`origins arrays are only used for cookies, not localStorage, so this is expected and not a bug`,`localStorage requires special encryption before storageState can capture it`],1,`storageState does capture localStorage per-origin — an empty origins array despite the app clearly using localStorage points to either capturing from the wrong origin or capturing before the write happened.`],[`D`,`A`,"A test explicitly overrides `test.use({ storageState: { cookies: [], origins: [] } })` at the top of a describe block to test unauthenticated behavior, but the test still appears to render the authenticated dashboard. What should you check?",null,[`test.use() overrides never actually take effect`,`Verify the override is placed correctly relative to the describe block it should apply to (a common mistake is placing it inside a nested describe or after tests have already been declared, where it has no effect on already-declared tests), and confirm the app is not authenticating via a mechanism other than cookies/storage (e.g. an IP allowlist, a separate always-on dev-mode bypass, or a cached service worker response) that an empty storageState would not remove`,`storageState overrides only work for the FIRST test in a describe block`,`This is expected; storageState overrides never remove existing sessions`],1,`Two common causes: the override is scoped incorrectly relative to the tests it should apply to, or the app is authenticating via something storageState genuinely cannot touch (like a service worker cache or a non-cookie mechanism).`],[`D`,`A`,"A storageState.json has a session cookie with `secure: true`, but tests running against `http://localhost:3000` (no TLS) fail to authenticate even though the cookie is present in the file. What is happening?",null,[`Playwright ignores the secure flag entirely, so this cannot be the cause`,"Browsers refuse to send a cookie marked `secure: true` over a plain (non-HTTPS) connection — since the local test environment serves over http://, the browser will never actually attach that cookie to requests, regardless of what the storageState file says; either serve the local environment over https:// or ensure the cookie is generated without the secure flag for that environment",`secure cookies can only be read by the request fixture, never by page navigation`,`The storageState file must be re-encoded in base64 for secure cookies to work`],1,`The secure cookie attribute is enforced by the BROWSER at request time based on the current connection's scheme — a mismatched local http:// environment means the cookie is present in the file but never actually sent.`],[`D`,`I`,"The \"editor\" role's tests started passing that should have failed (e.g. an editor now appears able to access an admin-only settings page). Investigation shows `editor-tests` project's `use.storageState` path was recently changed during a refactor. What is the most likely bug?",null,[`Playwright silently swaps role permissions during fullyParallel runs`,'A copy-paste or refactor mistake likely pointed the editor-tests project at `admin.json` instead of `editor.json` — the tests are unintentionally running as an admin, so admin-only pages "pass" simply because the session genuinely has admin rights, not because the app\'s authorization is broken',`The application itself is broken and grants editor role admin access`,`storageState files merge permissions from all roles by default`],1,`When a role's tests unexpectedly gain access they should not have, check the storageState PATH configured for that project first — a swapped or mistyped path silently runs tests under the wrong identity.`],[`D`,`A`,"A worker-scoped auth-refresh fixture works fine with `workers: 4` but starts timing out when the suite is run with `workers: 12` on the same CI machine. What should you investigate?",null,[`Worker-scoped fixtures have a hardcoded limit of 4 concurrent instances`,`With more workers, more of them may be hitting the login endpoint (or its underlying dependencies, like a shared database or rate limiter) concurrently at worker-startup time, causing contention/slowness or triggering rate-limiting that was not visible at lower concurrency — check the login backend's behavior and response times under N simultaneous logins, and consider staggering or rate-limiting the fixture's own login calls`,`Increasing workers always causes fixtures to be skipped entirely`,`This is unrelated to worker count and must be a Playwright version issue`],1,`A resource (login endpoint, DB, rate limiter) that comfortably handles a handful of concurrent worker-startup logins can become a bottleneck (or trigger rate limiting) once significantly more workers try to authenticate at once.`],[`L`,`A`,"How does a `SameSite=Lax` session cookie differ from `SameSite=Strict` specifically for a test that simulates a user arriving via a top-level link from an external site?",null,[`They behave identically in every scenario`,`Lax cookies ARE sent on a top-level (full-page) navigation from an external site, even though they are withheld on cross-site subrequests (like XHR/fetch or iframes) — Strict cookies are withheld even on that top-level cross-site navigation; a test verifying "user clicks an external link and lands already logged in" would only pass with Lax, never with Strict`,`SameSite=Lax cookies are never sent under any cross-origin circumstance`,`SameSite=Strict is only enforced in Playwright's test environment, not in real browsers`],1,`Lax specifically carves out an exception for top-level navigations (like clicking a link), which is exactly the scenario where Strict and Lax cookies behave differently and a test needs to pick the right one to simulate correctly.`],[`L`,`I`,"Why might a very small suite (say, 10 tests) reasonably choose to log in via the real UI in a `beforeEach` for every test, rather than adopting the storageState + setup-project pattern?",null,[`storageState is technically incompatible with suites under 50 tests`,`For a small enough suite, the total time cost of repeated UI logins may be genuinely negligible, and the added structure (a setup project, dependencies, gitignored auth files) may not be worth the complexity it introduces for a team that values simplicity over the marginal speed gain — the payoff of storageState scales with suite size and login cost`,`Playwright requires a minimum of 50 tests before storageState() can be used`,`UI-based login in beforeEach is strictly faster than storageState in every case`],1,`Like most performance optimizations, storageState's value is proportional to how much it saves — for a small suite where a few extra seconds of login time genuinely does not matter, the simpler always-log-in-in-beforeEach approach can be a reasonable, deliberate choice.`],[`L`,`A`,`Why might an app enforcing HSTS (HTTP Strict Transport Security) behave differently for a locally-run Playwright suite (http://localhost) versus a CI environment hitting a real HTTPS staging URL, with respect to cookie behavior?`,null,[`HSTS has no effect on cookies at all`,`HSTS only actively upgrades and enforces HTTPS for domains the browser has previously seen the HSTS header from over a real HTTPS connection; a fresh local Playwright browser profile hitting plain http://localhost has typically never received that header for localhost, so HSTS-driven redirect/cookie behavior differs from a CI run against a real HTTPS staging domain that legitimately sends the header`,`HSTS is automatically disabled for all Playwright test runs regardless of environment`,`HSTS only applies to WebSocket connections, not regular HTTP requests`],1,`HSTS enforcement is tied to a browser having previously observed the header from a specific HTTPS host — a fresh local profile against localhost over plain HTTP is a fundamentally different starting point than CI hitting a real HTTPS-enforcing staging host.`],[`O`,`A`,`What will this login-redirect test actually verify, and what could make it fail even when auth itself works?`,`test('redirects back to originally requested page after login', async ({ page }) => {
  await page.goto('/settings/billing'); // unauthenticated, redirects to /login?redirect=/settings/billing
  await page.getByLabel('Email').fill('user@test.com');
  await page.getByLabel('Password').fill('secret');
  await page.getByRole('button', { name: 'Log in' }).click();
  await expect(page).toHaveURL('/settings/billing');
});`,[`It only verifies that login succeeds, not the redirect-back behavior`,`It verifies the app correctly preserves and honors the originally-requested deep link (via a redirect query param) through the login flow, landing the user back on /settings/billing rather than a generic /dashboard — it could fail even with working auth if the redirect param is dropped, mismatched, or overridden by a default post-login destination`,`toHaveURL cannot check for a specific path, only a full URL including protocol`,`This test requires storageState and cannot use a fresh unauthenticated context`],1,`This test specifically targets deep-link preservation through login, a distinct behavior from authentication itself — a working login that always redirects to a generic dashboard would fail this specific assertion.`],[`O`,`I`,`What debugging technique does this code demonstrate?`,`const before = await context.storageState();
await page.getByRole('button', { name: 'Enable 2FA' }).click();
const after = await context.storageState();
console.log('New cookies:', after.cookies.filter(c => !before.cookies.some(b => b.name === c.name)));`,[`It permanently disables 2FA for the test`,`It captures storageState as an in-memory object BEFORE and AFTER an action, then diffs the cookie lists to see exactly what new cookies (if any) the action introduced — a useful ad hoc technique for understanding what a specific UI action actually changes about the session`,`storageState() can only be called once per context, so this code throws on the second call`,`This pattern automatically merges the two states into one`],1,`Calling storageState() without a path returns an in-memory snapshot you can inspect or diff — a handy debugging technique for understanding exactly what an action changes about the session.`],[`O`,`I`,`Why does this auth setup produce an empty storageState file?`,`setup('authenticate', async ({ browser }) => {
  const context = await browser.newContext();
  await context.storageState({ path: 'auth.json' }); // saved before any page/login
});`,[`storageState() requires the context to already have a page created, or it throws`,`storageState() is called on a freshly-created context that has never navigated anywhere or logged in — there is nothing to capture yet, so the resulting file legitimately contains empty cookies/origins arrays; the login steps (navigate, fill, submit, confirm) must happen before this call`,`browser.newContext() automatically logs in using default credentials`,`This code is correct, and the resulting file will contain a valid session`],1,`storageState() only captures whatever is currently true for that context — calling it before doing anything (like logging in) simply reflects the still-blank starting state.`],[`O`,`I`,`What does this setup provide for a suite made entirely of API-level tests with no browser UI involved?`,`const apiContext = await request.newContext({
  baseURL: 'https://staging.example.com',
  storageState: 'playwright/.auth/user.json',
});
const res = await apiContext.get('/api/orders');
expect(res.status()).toBe(200);`,[`request.newContext() ignores the storageState option entirely`,`It creates a pure APIRequestContext (no browser/page needed at all) pre-loaded with the same cookies captured by a prior UI or API login, letting fast, browser-free API tests make authenticated requests directly`,`This code requires a page fixture to already be open`,`storageState files generated from a browser context cannot be reused by an APIRequestContext`],0,`APIRequestContext accepts the exact same storageState format as browser contexts, letting browser-free API test suites reuse a session captured however it was generated.`],[`O`,`B`,"What does applying `test.use()` at two different describe levels in the same file achieve?",`test.describe('as admin', () => {
  test.use({ storageState: 'playwright/.auth/admin.json' });
  test('sees admin panel', async ({ page }) => { /* ... */ });
});
test.describe('as viewer', () => {
  test.use({ storageState: 'playwright/.auth/viewer.json' });
  test('cannot see admin panel', async ({ page }) => { /* ... */ });
});`,[`This is invalid; test.use() can only be called once per file`,`It lets a single spec file mix multiple roles by scoping a different storageState to each describe block — tests under "as admin" run with admin's session and tests under "as viewer" run with viewer's, without needing separate files or projects`,`Both describe blocks will use whichever storageState was declared last, applied to the whole file`,`storageState can only be set at the project level, never inside a test file`],1,`test.use() scoping to a specific describe block is exactly how a single file can mix multiple roles cleanly, each isolated to the tests that need it.`],[`O`,`A`,`What race condition does this pattern risk when multiple setup projects run concurrently?`,`// admin.setup.ts and editor.setup.ts both run concurrently as separate projects
// each does:
await page.context().storageState({ path: 'playwright/.auth/shared.json' }); // SAME path for both roles`,[`There is no risk since Playwright serializes all file writes automatically`,`Two setup projects running concurrently and writing to the SAME file path can race — whichever finishes last "wins", meaning the file may end up containing the wrong role's session, or in rare cases a corrupted partial write; each role must write to its OWN distinct file path`,`JSON files cannot be corrupted by concurrent writes under any circumstances`,`storageState() automatically merges two different sessions into one file safely`],1,`Any time two independent processes might write to the same file concurrently, correctness requires either distinct paths (the standard fix here) or explicit coordination — sharing one path between two roles' setup projects is the actual bug.`],[`O`,`I`,"What does wrapping the login steps in `test.step()` provide here that plain sequential awaits would not?",`await test.step('log in as standard user', async () => {
  await page.goto('/login');
  await page.getByLabel('Email').fill('user@test.com');
  await page.getByLabel('Password').fill('secret');
  await page.getByRole('button', { name: 'Log in' }).click();
  await page.waitForURL('/dashboard');
});`,[`It makes the login run measurably faster than sequential awaits`,`It groups the individual actions into a single named, collapsible step in the HTML report and trace viewer, making it much faster to skip past routine login boilerplate when reviewing a failing test's trace, since the step collapses to one line instead of five`,`test.step() is required syntax; plain awaits are not valid inside a test body`,`It disables auto-waiting for every action inside the step`],1,`test.step() is purely about reporting/trace ergonomics — bundling routine setup like login into one named, collapsible block makes traces much faster to scan when debugging failures elsewhere in the test.`],[`O`,`B`,`What bug does this code have?`,`setup('authenticate', async ({ page }) => {
  await page.goto('/login');
  await page.getByLabel('Email').fill('user@test.com');
  await page.getByLabel('Password').fill('secret');
  await page.getByRole('button', { name: 'Log in' }).click();
  await page.waitForURL('/dashboard');
  page.context().storageState({ path: 'auth.json' }); // missing await
});`,[`storageState() does not accept a path argument`,"The missing `await` means the setup function may finish (and the worker/process may proceed or exit) before the asynchronous write to auth.json actually completes, risking a missing, empty, or partially-written file — always await storageState()",`This code is correct since storageState() is synchronous`,`page.context() cannot be called without first awaiting page.goto()`],1,`storageState() is asynchronous (it involves I/O to write the file) — omitting await risks the setup function completing before the file is actually fully written.`],[`O`,`A`,`What conflict does this configuration risk creating?`,`const context = await browser.newContext({
  storageState: 'playwright/.auth/user.json',
});
await context.route('**/api/login', route => route.fulfill({ status: 200, body: '{}' }));
// test also navigates to /login and submits the form for an unrelated reason`,[`route() cannot be combined with storageState in the same context`,`The context starts already authenticated via storageState, but ALSO intercepts and fakes a response for the login endpoint — if a test unexpectedly triggers a real login attempt (e.g. testing a "log in as different user" flow), it will silently get the FAKE fulfilled response instead of hitting the real backend, likely producing confusing, inconsistent test behavior`,`This configuration guarantees the fastest possible test execution with no downsides`,`storageState always takes priority and the route handler will simply never be triggered`],1,`Combining a pre-authenticated context with a blanket route fake for the login endpoint can create surprising behavior if any test path in that context legitimately tries to log in again for a real reason.`],[`O`,`A`,`What does this fixture design let tests do that a fixed, single storageState path could not?`,`export const test = base.extend<{ role: string; roleAuthedPage: Page }>({
  role: ['viewer', { option: true }],
  roleAuthedPage: async ({ browser, role }, use) => {
    const context = await browser.newContext({ storageState: \`playwright/.auth/\${role}.json\` });
    await use(await context.newPage());
    await context.close();
  },
});
// test.use({ role: 'admin' }) in a specific file`,[`role must always be "viewer" and cannot be changed per test file`,"The `role` option fixture lets individual test files (or describe blocks) select WHICH role's storageState file to load at runtime via test.use({ role: ... }), instead of being locked into whichever storageState was hardcoded at the project level — a single flexible fixture serves any number of roles",`Option fixtures cannot be combined with browser.newContext()`,`This pattern requires a separate project per role and therefore provides no benefit over the standard approach`],1,`Declaring role as an option fixture ({ option: true }) turns the storageState PATH into something individual test files can select dynamically, rather than being fixed once at the project level.`],[`O`,`I`,`What will happen when this code runs?`,`await context.addCookies([{
  name: 'session',
  value: 'abc123',
  domain: 'not a valid domain!!',
  path: '/',
}]);`,[`The cookie is silently ignored and the rest of the test continues normally`,`Playwright validates cookie fields and will throw an error for a malformed domain value, failing fast at the addCookies() call rather than silently producing an unusable or ambiguous cookie`,`The invalid domain is automatically corrected to match the current page's origin`,`addCookies() ignores the domain field entirely and applies the cookie to every domain`],1,`Playwright validates the shape of cookie objects passed to addCookies() — a malformed domain value throws immediately rather than being silently accepted and producing confusing downstream behavior.`],[`L`,`B`,`Why is it a good habit to name role-based storageState files after the ROLE (e.g. admin.json, editor.json) rather than after the specific test account's username (e.g. jsmith.json)?`,null,[`Playwright requires storageState filenames to match a specific naming pattern or it will refuse to load them`,"Naming by role keeps the config and setup scripts stable even if the underlying test account changes (e.g. jsmith@test.com is swapped for a different account) — code that references `admin.json` never needs to change, whereas code referencing a specific username would need updating whenever that particular account changes",`Filenames including a real username are required for Playwright to associate the cookie with the correct account`,`Role-based naming makes the file load faster than username-based naming`],1,`Naming by role rather than by the specific underlying account is a small but effective decoupling — the config stays stable even as the concrete test account behind that role changes over time.`],[`O`,`A`,`What does this pattern accomplish for verifying that a session was actually revoked server-side after logout?`,`await page.getByRole('button', { name: 'Log out' }).click();
await page.waitForURL('/login');
const apiContext = await request.newContext({ storageState: { cookies: staleCookies, origins: [] } });
const res = await apiContext.get('/api/profile');
expect(res.status()).toBe(401);`,[`It merely checks that the UI redirected to /login, which only proves client-side behavior`,`After capturing the OLD (pre-logout) cookies, it constructs a fresh APIRequestContext seeded with those now-stale cookies and makes a direct API call — asserting a 401 proves the server actually invalidated the session, not just that the browser stopped showing authenticated UI`,`request.newContext() cannot accept cookies directly, only a file path`,`This code will always return 200 regardless of whether logout worked correctly`],1,`Replaying the pre-logout cookies via a fresh API context after logout is a good way to verify server-side revocation specifically, rather than just trusting the UI's redirect to /login.`],[`O`,`I`,`What does this project setup achieve for a suite that needs both a Basic-Auth-gated staging environment AND per-role application logins?`,`use: {
  httpCredentials: { username: 'staging', password: process.env.STAGING_GATE_PASSWORD! },
  storageState: 'playwright/.auth/admin.json',
},`,[`httpCredentials and storageState cannot both be set in the same use block`,`The two settings operate at different layers and compose cleanly: httpCredentials satisfies an infrastructure-level Basic Auth gate on every request to the staging environment, while storageState separately provides the application-level session — the browser context ends up passing the network gate AND already being logged into the app`,`storageState silently overrides httpCredentials, so only one will actually take effect`,`This configuration will cause every request to fail with a 401 due to conflicting auth headers`],1,`Basic Auth (a network/infrastructure gate) and application session cookies (storageState) are independent layers that can and often do need to be configured together for gated staging environments.`]],parallel:[[`O`,`B`,"What does `workers: 4` in playwright.config.ts control?",null,[`The number of browsers opened per test`,`The number of parallel Node.js worker processes that run tests concurrently`,`The number of retries per failing test`,`The number of CPU cores allocated to the browser`],1,`Each worker is an independent Node.js process with its own browser instance — workers: 4 runs up to 4 tests simultaneously.`],[`O`,`B`,`In the default Playwright mode (without fullyParallel), what runs in parallel and what runs serially?`,null,[`All tests run serially in a single worker`,`Test files run in parallel (one worker per file); tests within a single file run serially in sequence`,`All tests run in parallel — each in its own worker`,`Tests run in parallel only if they are in different describe blocks`],1,`The default behaviour parallelises at the file level — within a file, tests are sequential.`],[`O`,`I`,"What does `test.describe.serial()` do when `fullyParallel: true` is set globally?",null,[`It is ignored — fullyParallel overrides all serial declarations`,`It forces the tests inside that describe block to run in sequential order within a single worker, even when the rest of the suite is fully parallel`,`It causes those tests to be skipped`,`It disables retries for the tests in the block`],1,`describe.serial is a targeted override — the ordered group runs sequentially while everything outside it is still fully parallel.`],[`O`,`I`,"What does `--shard=2/3` do when passed to `npx playwright test`?",null,[`Runs tests at 2/3 speed to reduce CPU usage`,`Splits the test suite into 3 equal buckets and runs only the second bucket on this machine`,`Retries each test 2 times out of a maximum of 3`,`Runs only the second project defined in playwright.config.ts`],1,`Sharding distributes tests across CI machines — machine 2 of 3 runs roughly the middle third of all tests.`],[`O`,`I`,`What reporter should each sharded machine use to enable later merging?`,null,[`html`,`json`,`blob`,`dot`],2,`The blob reporter writes a binary artifact per shard — npx playwright merge-reports then combines all blobs into a single unified report.`],[`O`,`B`,"What is the default value for `workers` when not specified in playwright.config.ts?",null,[`1 (fully serial)`,`The exact number of logical CPU cores on the machine`,`Half the number of logical CPU cores (with a minimum of 1)`,`4, regardless of hardware`],2,`Playwright defaults to half the available CPU cores to leave headroom for the browser processes themselves.`],[`O`,`I`,'What does a worker-scoped fixture (`scope: "worker"`) guarantee?',null,[`The fixture is recreated for every test in the worker`,`The fixture is created once per worker process and shared by all tests assigned to that worker — setup and teardown happen once per worker, not per test`,`The fixture is created once for the entire test suite across all workers`,`The fixture only runs on worker index 0`],1,`Worker scope amortises expensive setup (database seeding, authenticated context) across all tests in the worker.`],[`O`,`B`,"What does `maxFailures: 10` in playwright.config.ts do?",null,[`Allows each test to fail 10 times before being marked failed`,`Stops the entire test run after 10 total test failures across all workers — prevents wasting CI time when something is seriously broken`,`Sets the maximum number of retries to 10`,`Limits parallel workers to 10`],1,`maxFailures is a circuit breaker — if 10 tests fail the suite aborts rather than running hundreds more doomed tests.`],[`O`,`I`,`What command merges blob reports from multiple shards into one HTML report?`,null,[`npx playwright combine-shards`,`npx playwright merge-reports --reporter html ./blob-report`,`npx playwright report --merge`,`npx playwright test --merge-output`],1,`merge-reports is the dedicated subcommand — it accepts the directory containing blob artifacts and emits the chosen reporter format.`],[`O`,`I`,"What happens to shared mutable module-level state (e.g., `let counter = 0`) between tests running in different workers?",null,[`Workers share memory — counter increments are visible across all workers`,`Workers are separate processes — each has its own copy of the module; changes in one worker are invisible to others`,`Playwright automatically resets module state between workers`,`Shared state is locked using Playwright's built-in mutex`],1,`Worker isolation is process-level — shared state bugs only appear within a single worker, not across workers.`],[`O`,`A`,"When using `--workers=50%`, what does Playwright use as the base for the percentage?",null,[`50% of the configured project count`,`50% of the logical CPU cores on the machine, rounded down`,`50% of the number of test files`,`50% of the number of tests in the suite`],1,`Percentage workers are relative to logical CPU count — --workers=50% on a 16-core machine gives 8 workers.`],[`F`,`B`,`Complete to enable fully parallel mode.
\`\`\`ts
export default defineConfig({
  ______: true,
  workers: 4,
});
\`\`\``,null,[`fullyParallel`,`parallel`,`allParallel`,`parallelMode`],0,`fullyParallel: true is the config key that runs every test in its own worker.`],[`F`,`B`,`Complete to run a describe block in parallel within a file.
\`\`\`ts
test.describe.______("search tests", () => {
  test("by title", async ({ page }) => { /* … */ });
  test("by author", async ({ page }) => { /* … */ });
});
\`\`\``,null,[`parallel`,`concurrent`,`async`,`fast`],0,`test.describe.parallel() opts that block into concurrent execution even without fullyParallel.`],[`F`,`B`,`Complete to force sequential order within a describe block.
\`\`\`ts
test.describe.______("user flow", () => {
  test("register", …);
  test("login", …);
  test("logout", …);
});
\`\`\``,null,[`serial`,`sequence`,`ordered`,`sync`],0,`test.describe.serial() guarantees the tests run in declaration order on a single worker.`],[`F`,`I`,"Complete the shard CLI argument for machine 3 of 4.\n```\nnpx playwright test --shard=______\n```",null,[`3/4`,`3:4`,`3-4`,`shard3`],0,`The shard argument format is M/N — shard index / total shard count.`],[`F`,`I`,'Complete the reporter setting for shard-compatible output.\n```ts\nreporter: [["______"], ["github"]]\n```',null,[`blob`,`html`,`json`,`dot`],0,`blob reporter writes binary artifacts that merge-reports can later combine into any format.`],[`F`,`I`,`Complete to declare a worker-scoped fixture.
\`\`\`ts
myClient: [async ({}, use) => {
  const c = await createClient();
  await use(c);
  await c.close();
}, { scope: "______" }],
\`\`\``,null,[`worker`,`test`,`file`,`global`],0,`scope: "worker" creates the fixture once per worker process — shared across all tests in that worker.`],[`F`,`B`,"Complete the CLI flag to set a specific number of workers.\n```\nnpx playwright test ______=8\n```",null,[`--workers`,`--parallel`,`--concurrency`,`--threads`],0,`--workers overrides the config value from the command line.`],[`E`,`B`,`What is wrong?
\`\`\`ts
let sharedData: string;

test("sets data", async ({ page }) => {
  sharedData = await page.textContent("h1");
});

test("reads data", async ({ page }) => {
  expect(sharedData).toBe("Hello"); // may be undefined
});
\`\`\``,null,[`textContent() cannot be assigned to a variable`,`With fullyParallel, tests run in separate workers — module-level sharedData is not shared between workers; "reads data" may run before "sets data" and always sees undefined`,`expect() cannot reference module variables`,`The tests must be in a describe block to share state`],1,`Tests in parallel mode must be self-contained — use fixtures or describe.serial for ordered state sharing.`],[`E`,`I`,"What is wrong with this sharding command on the last machine?\n```\nnpx playwright test --shard=4/3\n```",null,[`The reporter flag is missing`,`Shard index 4 is greater than the total 3 — valid shards are 1/3, 2/3, 3/3. Playwright will error or run no tests`,`--shard requires --workers to be set`,`The total must always be a power of 2`],1,`The shard index must be between 1 and N inclusive — 4/3 is invalid.`],[`E`,`B`,`What is wrong?
\`\`\`ts
export default defineConfig({
  workers: 16,
  fullyParallel: true,
  webServer: {
    command: "npm run dev",
    port: 3000,
  },
});
\`\`\`
All 16 workers try to start the dev server.`,null,[`webServer does not work with fullyParallel`,`Playwright's webServer starts once and all workers share it — the issue is not multiple starts but a fixed port: if workers each try to bind to port 3000 individually they conflict. Actually Playwright starts webServer once globally. This is fine in this config.`,`workers must be 1 when using webServer`,`webServer requires reuseExistingServer: true`],1,`Playwright starts the webServer process once before any workers launch — all workers share the single server. This configuration is actually correct. The real issue to watch for is workers trying to bind their own ports inside tests (e.g., mock servers). Add reuseExistingServer: true to avoid errors if the port is already occupied.`],[`E`,`I`,`What is wrong?
\`\`\`ts
test.describe.serial("checkout flow", () => {
  test.describe.parallel("payment options", () => {
    test("credit card", …);
    test("paypal", …);
  });
});
\`\`\``,null,[`describe cannot be nested`,`parallel inside serial is ignored — the outer serial constraint dominates; all tests in the serial block run in order on a single worker regardless of inner parallel declarations`,`paypal cannot be tested in parallel with credit card`,`You must use test.describe.configure instead of nesting`],1,`serial is an outer constraint — inner parallel declarations have no effect inside a serial block.`],[`E`,`B`,`What is wrong?
\`\`\`ts
test.describe.serial("flow", () => {
  test("step 1", …);
  test("step 2", …);
});
// fullyParallel: true in config
\`\`\`
Both tests are still running simultaneously.`,null,[`serial is ignored in fullyParallel mode — use test.only instead`,`describe.serial does work with fullyParallel but you must import test from the correct module — if using a custom test extension make sure describe.serial is available on it`,`serial requires workers: 1 to work`,`You need test.describe.configure({ mode: "serial" }) instead`],1,`The most common cause is importing from @playwright/test instead of a custom extended test object — but also verify via the HTML report that they show as sequential. describe.serial and fullyParallel do coexist correctly in standard usage.`],[`E`,`A`,`What is wrong?
\`\`\`ts
// Worker-scoped fixture opens an authenticated browser context
myContext: [async ({ browser }, use) => {
  const context = await browser.newContext({
    storageState: "playwright/.auth/user.json"
  });
  await use(context);
  await context.close();
}, { scope: "worker" }],
\`\`\`
Tests get intermittent auth failures.`,null,[`browser fixture cannot be used in worker-scoped fixtures`,`The browser fixture is test-scoped by default — using it in a worker-scoped fixture creates a dependency scope mismatch. Use the browserType worker-scoped fixture (browserName) or launch a browser directly with chromium.launch() to keep everything at worker scope`,`context.close() must not be awaited in teardown`,`storageState is incompatible with worker-scoped contexts`],1,`Fixtures can only depend on fixtures of equal or wider scope — a worker fixture depending on a test-scoped browser causes lifecycle issues. Use playwright.chromium or pass the browser type explicitly.`],[`D`,`B`,`Tests pass locally but fail in CI with "too many open files". What is the likely cause?`,null,[`CI does not support Playwright`,`CI machines have lower ulimit for file descriptors — each worker opens browser processes and files. Reduce workers in CI: workers: process.env.CI ? 2 : undefined`,`Tests must be run serially in CI`,`The CI machine needs more RAM`],1,`High worker counts in CI can exhaust file descriptor limits — tune workers based on the CI machine's ulimit and available memory.`],[`D`,`I`,`A test in a serial describe block sometimes sees stale state from the previous test. What should you check?`,null,[`Serial describe blocks have a memory isolation bug`,`Verify that each test in the serial block uses the same page fixture — the page is recreated between tests by default even in serial mode. If state must persist, use a worker-scoped context fixture shared across the serial block`,`Re-enable retries for the serial block`,`Add await page.reload() at the start of each test`],1,`Serial blocks guarantee order but not state persistence — each test still gets a fresh page unless you explicitly share context via a worker fixture.`],[`D`,`B`,`Sharded CI runs produce separate HTML reports but you cannot see all results together. What is the fix?`,null,[`Download all HTML reports and open them simultaneously`,`Switch each shard to --reporter=blob, upload the blob artifacts, then run npx playwright merge-reports --reporter html on the collected blobs to produce one unified report`,`Use --reporter=json and manually concatenate the files`,`Run without sharding to get a single report`],1,`The blob + merge-reports workflow is the canonical way to aggregate sharded results into a single HTML report.`],[`D`,`I`,`Two tests that used to pass are now failing with race conditions after enabling fullyParallel. What is the root cause?`,null,[`fullyParallel has a known bug with async tests`,`The tests share side-effectful state (a database record, a file, a global variable) that was harmless when they ran serially but causes conflicts when concurrent — make each test self-contained or serialise them with describe.serial`,`The tests need retries: 3 to handle race conditions`,`Playwright workers have a bug with shared memory`],1,`Race conditions on shared resources only surface under parallelism — the fix is isolation (each test owns its data) or explicit serialisation.`],[`D`,`I`,"`workers: 100%` is set but only 4 workers start on a 32-core machine. Why?",null,[`Playwright caps workers at 4`,`The browser process count also consumes cores — Playwright may also be considering available memory, or your OS limits the number of child processes. Additionally 100% should give 32 workers; check that the config key is fullyParallel: true is also set, otherwise fewer tests are assigned per worker and excess workers idle`,`workers: 100% is not valid syntax`,`Playwright limits shard sizes to 4`],1,`workers: "100%" gives 32 workers on 32 cores — if fewer start, check OS limits (ulimit -u), available memory, or whether tests are fewer than the worker count (Playwright won't start idle workers).`],[`D`,`A`,`merge-reports fails with "incompatible blob version". What happened?`,null,[`The blob files are corrupted during upload`,`The shards were run with different versions of Playwright — blob format is version-specific. Ensure all CI machines use the exact same @playwright/test version`,`Blob files must be decompressed before merging`,`merge-reports requires Node 18+`],1,`Always pin the exact Playwright version across all CI machines — even a patch version difference can produce incompatible blob formats.`],[`C`,`B`,"Complete the config to use fewer workers in CI.\n```ts\nworkers: process.env.CI ? 2 : ______,\n```",null,[`undefined`,`4`,`8`,`1`],0,`undefined uses Playwright's default (half the CPU cores) locally, while CI gets a fixed safe count.`],[`C`,`I`,`Complete the describe block to guarantee sequential order.
\`\`\`ts
test.describe.______("delete flow", () => {
  test("create item", …);
  test("delete item", …);
});
\`\`\``,null,[`serial`,`parallel`,`ordered`,`sync`],0,`describe.serial enforces sequential execution on a single worker.`],[`C`,`I`,"Complete the CLI shard for the second of four machines.\n```\nnpx playwright test --shard=______\n```",null,[`2/4`,`2:4`,`2-4`,`2`],0,`Shard syntax is index/total — 2/4 means "run the second quarter of the suite".`],[`C`,`I`,"Complete the merge command.\n```\nnpx playwright ______ --reporter html ./blob-report\n```",null,[`merge-reports`,`merge`,`combine`,`aggregate`],0,`merge-reports is the Playwright subcommand for combining shard blob artifacts.`],[`C`,`A`,`Complete to set worker scope on the fixture.
\`\`\`ts
myDb: [async ({}, use) => {
  const db = await connectDb();
  await use(db);
  await db.close();
}, { ______: "worker" }],
\`\`\``,null,[`scope`,`level`,`lifecycle`,`mode`],0,`The scope option in the fixture options object controls the fixture lifecycle: "test", "worker", or "file".`],[`L`,`B`,`If you have 10 test files and workers: 4, how many files can run at the same time?`,null,[`10 — all at once`,`4 — one per worker. The remaining 6 wait in the queue until a worker finishes`,`2 — Playwright always reserves half`,`1 — files always run one at a time`],1,`Workers process files concurrently up to the worker limit — 4 files in parallel, 6 waiting.`],[`L`,`I`,`What is the key difference between a test-scoped and a worker-scoped fixture?`,null,[`Test-scoped fixtures are faster`,`Test-scoped fixtures are created and destroyed for every test; worker-scoped fixtures are created once per worker and shared by all tests assigned to that worker — ideal for expensive setup`,`Worker-scoped fixtures are shared across all workers`,`There is no practical difference — scope is cosmetic`],1,`Scope determines lifecycle: test scope = N creations for N tests; worker scope = 1 creation per worker regardless of how many tests run in it.`],[`L`,`B`,`Why should tests be self-contained (no shared mutable state) when using parallel execution?`,null,[`Playwright requires self-contained tests for TypeScript compilation`,`Parallel workers execute tests in any order and timing — tests that depend on shared state created by another test become order-dependent and flaky`,`Self-contained tests load faster`,`Playwright crashes if tests share state`],1,`Shared state between parallel tests creates hidden dependencies — the test that runs first may leave state the second test needs, but "first" is non-deterministic.`],[`L`,`I`,"How does sharding interact with the `--grep` flag to filter tests?",null,[`You cannot use --grep with sharding`,`--grep filters tests first, then sharding splits the filtered set — only matching tests are distributed across shards`,`Sharding always runs all tests regardless of --grep`,`--grep must list test titles in JSON format when used with sharding`],1,`Filtering happens before sharding — each shard receives its fraction of the already-filtered test set.`],[`L`,`A`,`If a serial describe block is in a file and fullyParallel is true, does the block run concurrently with tests from other files?`,null,[`No — serial blocks always run last, after all parallel tests`,`Yes — the serial block runs as a unit on one worker, but that worker runs concurrently with workers handling other files. Serial only constrains ordering within the block, not across the suite`,`Serial blocks disable parallelism for the entire worker`,`fullyParallel: true has no effect on files containing serial blocks`],1,`describe.serial controls order within the block — the block itself is one task among many workers and runs concurrently with other tests globally.`],[`R`,`B`,`You have 200 tests and CI takes 12 minutes. How do you cut that to ~4 minutes?`,null,[`Use retries: 0 to skip failed tests faster`,`Enable fullyParallel: true and increase workers to match available CI cores, or add sharding across 3 CI machines to run 3× the tests simultaneously`,`Combine all tests into one file so they share setup`,`Use the dot reporter to reduce output overhead`],1,`Parallelism is the primary lever for CI speed — more workers (or more machines via sharding) is the direct solution.`],[`R`,`I`,`Your app has a "create → verify → cleanup" test that must run in order but other tests are fully parallel. How do you structure this?`,null,[`Put all tests in a single file to ensure order`,`Put the ordered tests inside test.describe.serial() — they run sequentially on one worker while all other describe blocks or files run in parallel`,`Use retries: 0 only for those tests`,`Mark the tests with test.slow() to run them last`],1,`describe.serial targets exactly the tests that need ordering without affecting anything else in the suite.`],[`R`,`A`,`You want to run your 500-test suite across 5 GitHub Actions VMs to get 5× faster CI. What is the complete approach?`,null,[`Create 5 separate test suites with different testDir values`,`Use a matrix strategy with shardIndex 1-5 and shardTotal 5; each job runs npx playwright test --shard=$INDEX/$TOTAL --reporter=blob; a merge job downloads all blobs and runs npx playwright merge-reports --reporter html`,`Use 5 separate playwright.config.ts files and merge results manually`,`Increase workers: 100 on a single VM`],1,`Matrix + sharding + blob reporter + merge-reports is the canonical GitHub Actions pattern for distributed Playwright runs.`],[`R`,`I`,`A worker-scoped database fixture is seeding 10 000 rows before tests run. With 8 workers this means 80 000 rows inserted per run. How do you fix it?`,null,[`Use retries: 0 to avoid re-seeding`,`Seed once in globalSetup (which runs before any workers start) and have tests read from shared, immutable seed data — or use transactions that roll back after each test instead of truncate/insert`,`Worker scope is wrong — switch to test scope`,`Use a mock database instead of a real one`],0,`globalSetup runs once before all workers — it is the right place for one-time expensive seeding. Tests then read from or transact against the pre-seeded data.`],[`R`,`B`,`After adding fullyParallel, your login test fails with "storageState file not found" even though the auth setup project is listed as a dependency. What should you check?`,null,[`fullyParallel breaks dependency ordering — use serial instead`,`Verify the storageState path matches exactly between auth.setup.ts and the test project use block, and that the playwright/.auth/ directory exists. With fullyParallel, tests start as soon as the dependency project passes — the file must be written before the first test launches`,`Restart the test runner to clear the dependency cache`,`fullyParallel requires each test to re-authenticate`],1,`File path typos and missing directories are the most common auth + parallel failures — the dependency only ensures the setup project ran, not that the correct file was created at the expected path.`],[`S`,`I`,`With fullyParallel: true and workers: 8, your test suite runs in 3 minutes locally. On CI (4 cores) it takes 11 minutes. Why and how do you fix it?`,null,[`CI uses a slower browser — switch to Chromium headless`,`8 workers on a 4-core CI machine causes extreme context switching overhead — cap CI workers: process.env.CI ? 4 : undefined, or use sharding across multiple CI machines instead of over-provisioning a single one`,`fullyParallel causes more overhead in headless mode`,`CI blocks parallel browser processes for security`],1,`More workers than CPU cores causes thrashing — tune workers to match the CI machine's actual capacity, or scale out with sharding.`],[`S`,`A`,`You need to test a multi-step purchase flow (cart → checkout → confirmation → email) but also have 300 independent product tests running in parallel. How do you structure this?`,null,[`Run all tests serially to avoid complexity`,`Put the purchase flow in test.describe.serial() — it runs as a self-contained sequential unit on one worker. The 300 product tests use fullyParallel: true and run concurrently across all other workers simultaneously`,`Split into two separate test suites with different configs`,`Use retries: 0 only for the purchase flow`],1,`describe.serial isolates the ordered flow on one worker while fullyParallel maximises parallelism everywhere else — both concerns are satisfied simultaneously.`],[`S`,`I`,`Your team wants to ensure that no individual test takes longer than 30 seconds, and the total suite does not exceed 5 minutes. How do you configure this?`,null,[`Use retries: 1 and hope tests finish faster`,`Set timeout: 30000 (per-test timeout) and globalTimeout: 300000 (total suite limit) in playwright.config.ts — combine with enough workers to realistically hit the suite limit`,`Use maxFailures: 1 and retries: 0`,`Set expect.timeout globally to 30 seconds`],1,`timeout controls individual test runtime; globalTimeout kills the entire suite if it exceeds the wall-clock limit — both are needed for strict SLA enforcement.`],[`S`,`A`,`After switching to 4-shard CI, some tests that check sequential record IDs (1, 2, 3…) are failing because records from parallel shards collide. What is the architectural fix?`,null,[`Run database tests in shard 1 only`,`Each shard should use an isolated database (separate schema, separate instance, or a unique prefix per shard using process.env.TEST_SHARD_INDEX) — sequential IDs are inherently global state and break under parallel shards sharing a database`,`Disable sharding for database tests`,`Use random IDs and assert "is a number" instead of exact values`],0,`Isolated data namespaces (by shard index, worker index, or random prefix) are the correct fix — shared global state (including auto-increment IDs) is incompatible with parallel test runs.`],[`O`,`B`,'What does `workers: "50%"` in playwright.config.ts do?',null,[`Runs 50% of the test suite in parallel`,`Uses 50% of available CPU cores as the worker count — if the machine has 8 cores, workers is set to 4. Keeps the machine responsive while still parallelising.`,`Limits test execution to 50% of the timeout`,`Retries 50% of failing tests`],0,`Percentage workers scale with the host machine — useful for configs shared between developer laptops and CI servers with different core counts.`],[`O`,`I`,"What does `maxFailures: 5` in playwright.config.ts do?",null,[`Stops the suite after any single test fails`,`Stops the entire test run once 5 tests have failed — prevents a bad commit from burning through all workers when many tests are expected to fail`,`Retries each test up to 5 times before marking it as failed`,`Limits per-file failures to 5`],1,`maxFailures is a fast-fail ceiling — useful for large suites where you want early signal without waiting for every test to run.`],[`O`,`B`,"What does `test.describe.parallel()` do?",null,[`Marks a describe block to run in a separate browser`,`Overrides the default test order within the describe block to allow its tests to run in parallel on different workers — requires fullyParallel or the parent describe to allow parallelism`,`Disables serial execution for the block`,`Runs the describe block on all configured browsers simultaneously`],0,`describe.parallel() is the counterpart to describe.serial() — it explicitly opts the block into parallel execution even if the file-level default is serial.`],[`O`,`I`,`What is a "shard" in Playwright terminology?`,null,[`A shared browser context between tests`,`A partition of the test suite run on a separate machine or CI node — e.g. --shard=1/4 runs the first quarter of tests. Combine all shards' blob reports with merge-reports for a unified result.`,`A single test file`,`A group of tests from the same project`],1,`Sharding splits the suite across machines — total parallelism = workers per machine × number of shards.`],[`O`,`A`,'What does `test.describe.configure({ mode: "parallel", retries: 2 })` do?',null,[`Configures the entire test file to run in parallel with 2 retries`,`Applies the configuration only to the enclosing describe block — its tests run in parallel and failing ones are retried up to 2 times independently of the file-level config`,`Sets global parallelism and retry options`,`Overrides only the retry setting`],1,`describe.configure() scopes configuration to the describe block — a fine-grained override for mixed-mode test files.`],[`O`,`I`,"What does `PLAYWRIGHT_WORKER_INDEX` environment variable contain during test execution?",null,[`The total number of workers`,`The 0-based index of the current worker process — useful for namespacing test data (e.g. database schema per worker: "test_worker_" + process.env.PLAYWRIGHT_WORKER_INDEX)`,`The CI job number`,`The current shard index`],1,`Worker index is the per-run isolation key — use it to create worker-scoped data stores that don't collide with other parallel workers.`],[`O`,`B`,"What does `--reporter=blob` save during a sharded run?",null,[`A Blob storage connection string for uploads`,"A compact binary report file in blob-report/ directory — each shard produces one, then `playwright merge-reports` combines them into a single HTML report",`A screenshot blob for each test`,`The test source code in binary format`],1,`Blob reporter is the inter-shard report format — small, efficient, and mergeable.`],[`O`,`I`,"What does `globalSetup` run in relation to workers?",null,[`Once per worker — each worker runs its own globalSetup`,`Once before any workers are launched — a single Node.js process runs globalSetup to perform shared pre-test setup (auth, DB seed) before parallelism begins`,`Simultaneously with the first worker`,`After all workers complete`],1,`globalSetup is a single-execution pre-run hook — its purpose is one-time setup that shouldn't be repeated per worker.`],[`O`,`B`,"What does `globalTeardown` do?",null,[`Tears down each test after it fails`,`Runs once after all workers have finished — used to clean up global state created in globalSetup (delete temp databases, close connections, clean up auth files)`,`Runs after each worker completes`,`Resets the browser profile between runs`],1,`globalTeardown mirrors globalSetup — anything created globally should be cleaned up globally.`],[`O`,`A`,"What does `--shard=2/3` mean when passed to `npx playwright test`?",null,[`Run tests 2 and 3 only`,`Run the second partition of 3 equal shards — Playwright deterministically assigns tests to shards so the distribution is consistent across runs`,`Run tests at 2/3 speed for stability`,`Run the second third of the test files alphabetically`],1,`Sharding assigns tests to shards based on a stable hash — the same test always lands on the same shard for the same total shard count.`],[`O`,`I`,"What does `webServer.reuseExistingServer: true` achieve in a parallel context?",null,[`Allows multiple workers to reuse the same port`,`Tells Playwright to use an already-running dev server instead of starting a new one — prevents port conflicts when multiple test runs or workers try to start the same server concurrently`,`Enables server-side session sharing`,`Forces all workers to use localhost`],1,`reuseExistingServer: true is essential in watch mode and CI where a server may already be running — prevents EADDRINUSE errors.`],[`O`,`B`,"What does `test.setTimeout(60_000)` inside a test function do?",null,[`Sets the global timeout to 60 seconds`,`Overrides the timeout for that specific test only — 60 seconds instead of the configured default. Other tests are unaffected.`,`Pauses the test for 60 seconds`,`Retries the test for 60 seconds`],1,`Per-test timeout override is useful for known slow tests — set it explicitly rather than raising the global timeout.`],[`F`,`B`,"Complete to set the worker count based on environment.\n```ts\nworkers: process.env.CI ? ______ : undefined,\n```",null,[`4`,`1`,`8`,`2`],0,`4 is a common CI worker count for machines with 4+ cores — undefined lets Playwright use its default (half CPU cores) locally.`],[`F`,`I`,`Complete to run a describe block serially.
\`\`\`ts
test.describe.______("purchase flow", () => {
  test("add to cart", ...);
  test("checkout", ...);
  test("confirmation", ...);
});
\`\`\``,null,[`serial`,`sequential`,`ordered`,`sync`],0,`describe.serial() runs its tests in order on a single worker — the rest of the suite can still run in parallel.`],[`F`,`B`,"Complete to set a global suite timeout.\n```ts\n// playwright.config.ts\nglobalTimeout: ______,\n```",null,[`5 * 60 * 1000`,`60_000`,`300`,`"5m"`],0,`5 * 60 * 1000 = 300,000 ms = 5 minutes — Playwright timeouts are always in milliseconds.`],[`F`,`I`,"Complete to stop after 10 failures.\n```ts\n// playwright.config.ts\n______: 10,\n```",null,[`maxFailures`,`stopAfter`,`failLimit`,`earlyExit`],0,`maxFailures: N stops the run when N tests have failed — fast-fail for large suites.`],[`F`,`A`,"Complete to run only the second of four shards.\n```sh\nnpx playwright test --shard=______\n```",null,[`2/4`,`2,4`,`2-4`,`shard:2/4`],0,`--shard=X/Y: X is the shard index (1-based), Y is the total number of shards.`],[`F`,`B`,"Complete to merge shard reports.\n```sh\nnpx playwright ______ blob-report/ --reporter=html\n```",null,[`merge-reports`,`combine-reports`,`join-reports`,`aggregate-reports`],0,`merge-reports reads blob report directories from all shards and produces a single combined report.`],[`F`,`I`,'Complete to configure the blob reporter.\n```ts\n// playwright.config.ts\nreporter: process.env.CI ? [["______", { outputDir: "blob-report" }]] : [["html"]],\n```',null,[`blob`,`blob-reporter`,`json`,`ci`],0,`"blob" is the reporter name — lowercase, as registered in Playwright's reporter list.`],[`F`,`A`,`Complete to set the test timeout per test.
\`\`\`ts
test("slow test", async ({ page }) => {
  test.______( 90_000);
  // ...
});
\`\`\``,null,[`setTimeout`,`timeout`,`setLimit`,`slow`],0,`test.setTimeout() overrides the timeout for the currently running test only.`],[`F`,`I`,`Complete to configure per-project parallelism.
\`\`\`ts
projects: [
  {
    name: "chromium",
    ______: { fullyParallel: true },
  },
]
\`\`\``,null,[`use`,`config`,`settings`,`options`],0,`use: { fullyParallel: true } applies the parallel setting to the specific project — overrides the global setting.`],[`E`,`B`,`What is wrong?
\`\`\`ts
test.describe.serial("flow", () => {
  test("step 1", async ({ page }) => { ... });
  test("step 2", async ({ page }) => { ... });
});
// Each test creates a fresh page fixture
\`\`\``,null,[`describe.serial is not a valid Playwright API`,`describe.serial ensures ordering but each test still gets its own fresh page fixture — state does not automatically flow between tests. Share state via module-level variables or worker-scoped fixtures if step 2 depends on step 1's state.`,`Tests inside describe.serial share the same page`,`describe.serial requires all tests to pass to run the next one`],1,`Serial execution guarantees order but not state continuity — fresh fixtures reset state between tests. Use module variables or shared fixtures for inter-test state.`],[`E`,`I`,"What is wrong?\n```ts\n// playwright.config.ts\nfullyParallel: true,\nworkers: 1,\n```",null,[`fullyParallel requires workers: undefined`,`This config is valid but self-defeating — fullyParallel with workers: 1 runs tests sequentially (1 worker = no parallelism). Set workers to 2+ to gain any benefit from fullyParallel.`,`workers must be at least equal to fullyParallel's value`,`fullyParallel overrides workers automatically`],1,`1 worker negates parallelism — remove the workers override or set it higher than 1.`],[`E`,`B`,"What is wrong with sharing a module-level array between parallel tests?\n```ts\n// tests/shared.ts\nexport const ids: string[] = [];\n```",null,[`Arrays cannot be used in TypeScript test files`,`Module-level variables in Node.js are NOT shared between worker processes — each worker gets its own copy of the module. Cross-worker data sharing requires external mechanisms (file, DB, IPC). The array in worker 1 is invisible to worker 2.`,`export arrays are always read-only`,`Worker processes share module memory`],1,`Workers are separate processes — module state is process-local. Use files, environment variables, or databases for cross-worker data.`],[`E`,`A`,"What is wrong?\n```sh\nnpx playwright merge-reports --reporter=html blob-report/\n```",null,[`merge-reports does not accept a reporter flag`,"The argument order is wrong — the directory comes before the flags in some versions, but the correct syntax is: `npx playwright merge-reports blob-report/ --reporter html` (without the equals sign). Check the Playwright version for exact syntax.",`merge-reports only outputs JSON`,`--reporter=html must be specified in the config`],1,`Argument order and flag syntax for merge-reports is version-sensitive — check the docs for your exact Playwright version.`],[`E`,`I`,`What is wrong?
\`\`\`ts
test.describe.serial("flow", () => {
  test.describe.parallel("sub-flow", () => {
    // parallel inside serial
  });
});
\`\`\``,null,[`Nesting describe blocks is not supported`,`parallel inside serial is a conflict — the serial parent constrains all descendants to run on one worker in sequence, overriding the parallel child's intent. The parallel config has no effect.`,`parallel must come before serial`,`This is valid and each level applies independently`],1,`Serial is a ceiling constraint — descendants cannot escape it with parallel. Restructure to put parallel tests outside the serial describe.`],[`D`,`B`,`Adding more workers speeds up the test suite until around 8 workers, then times out. CPU and memory look fine. What else could be the bottleneck?`,null,[`Playwright caps workers at 8`,`The application server or database may be the bottleneck — too many concurrent workers overwhelm a shared backend. Check server response times and connection pool exhaustion. Consider using a test-specific backend that scales better.`,`headless browsers cannot run more than 8 in parallel`,`fullyParallel is needed above 8 workers`],1,`Backend saturation is a common wall — more Playwright workers doesn't help if the server is queueing requests.`],[`D`,`I`,`Sharded CI runs sometimes produce different total test counts across shards. What could cause this?`,null,[`Playwright randomly assigns tests to shards`,`A test file generates dynamic test cases (e.g. using test.each with a runtime array) — Playwright shards based on static discovery, and dynamic tests may not be discovered the same way. Fix: use static test data for shard-stable distribution.`,`Shard index is 0-based causing off-by-one`,`The number of shards must equal the number of test files`],1,`Dynamic test generation (runtime test.each data) can make shard distribution unstable — prefer static test parameterisation.`],[`D`,`B`,'`merge-reports` fails with "no blob reports found". What is the likely cause?',null,[`merge-reports requires an internet connection`,"The blob-report/ directory either doesn't exist or is empty — check that the sharded test runs used `--reporter=blob` and that the CI artifact upload step included the blob-report/ directory",`The blob format changed in a Playwright update`,`merge-reports must be run on the same machine as the tests`],1,`Missing blob files mean the reporter wasn't configured correctly or the CI artifact step didn't collect the directory.`],[`D`,`A`,`Worker 2 consistently fails a test that passes on workers 1, 3, and 4. The test interacts with shared test fixtures. What should you investigate?`,null,[`Worker 2 uses a different browser`,`A worker-scoped fixture may be leaking state between tests — if a fixture creates a resource once per worker and doesn't clean it up properly, earlier tests on worker 2 pollute later ones. Add cleanup and use a fresh resource per test for stateful fixtures.`,`Worker 2 has a different network route`,`Playwright assigns harder tests to higher-numbered workers`],1,`Worker-scoped fixture cleanup bugs cause intermittent per-worker failures — verify teardown runs even on test failure using try/finally.`],[`C`,`B`,`Complete to set a per-test timeout override.
\`\`\`ts
test("long test", async ({ page }) => {
  test.______( 120_000);
  await page.goto("/slow-page");
});
\`\`\``,null,[`setTimeout`,`setLimit`,`timeout`,`slow`],0,`test.setTimeout(ms) overrides the timeout for the currently running test.`],[`C`,`A`,"Complete to read the worker index for data isolation.\n```ts\nconst workerIdx = ______;\nconst dbName = `test_db_${workerIdx}`;\n```",null,[`process.env.PLAYWRIGHT_WORKER_INDEX`,`process.env.WORKER_ID`,`test.info().workerIndex`,`global.workerIndex`],2,`testInfo.workerIndex is the fixture-based way; process.env.PLAYWRIGHT_WORKER_INDEX is the env-based way — both are valid. testInfo.workerIndex is preferred inside tests.`],[`C`,`B`,"Complete to limit the test run to stop after 3 failures.\n```ts\n// playwright.config.ts\n______: 3,\n```",null,[`maxFailures`,`stopAfter`,`failAt`,`earlyStop`],0,`maxFailures: 3 stops the run once 3 tests have failed.`],[`C`,`I`,"Complete to run tests from the second of three shards.\n```sh\nnpx playwright test ______\n```",null,[`--shard=2/3`,`--shard 2/3`,`--partition=2/3`,`--split=2:3`],0,`--shard=2/3 is the exact CLI syntax — X/Y where X is the 1-based shard index.`],[`L`,`B`,"Why is `fullyParallel: true` not the default in Playwright?",null,[`It is slower than the default`,`Some tests depend on ordering or shared state — enabling full parallelism by default would break those tests. Playwright defaults to per-file parallelism (tests within a file run sequentially) to be safe for code written without explicit isolation.`,`Playwright cannot detect parallel-safe tests automatically`,`The default workers value is 1`],1,`Safe defaults: file-level parallelism is conservative and works for most code. fullyParallel is opt-in for teams that have ensured test isolation.`],[`L`,`I`,"What is the difference between `workers: 4` and `fullyParallel: true`?",null,[`They are the same setting`,`workers: N controls HOW MANY concurrent workers run. fullyParallel: true controls WHAT runs in parallel — with it, individual tests run across workers; without it, files run across workers but tests within a file are sequential.`,`fullyParallel enables 4× more workers`,`workers controls browsers; fullyParallel controls tests`],1,`Two orthogonal dimensions: worker count (how many) and parallelism mode (what granularity). Combine both for maximum throughput.`],[`L`,`I`,"When is `describe.serial()` the right choice vs. splitting into separate test files?",null,[`They are always interchangeable`,`describe.serial() is right when the tests share context and state within the same file (e.g. a wizard flow). Separate files are right when the tests are genuinely independent topics. Using describe.serial across the entire file for independent tests is an anti-pattern.`,`describe.serial is always slower than separate files`,`Separate files always run in parallel; describe.serial never does`],1,`Match the tool to the coupling — serial for genuinely ordered coupled steps, separate files for independent topics.`],[`L`,`A`,`Why does sharding beat simply adding more workers on a single machine?`,null,[`Sharding is always faster even on a single machine`,`A single machine has a fixed ceiling on CPU cores and memory — sharding spreads the load across multiple machines, multiplying total parallelism beyond what one machine can support. Sharding + workers together give the highest throughput.`,`Sharding avoids the need for blob reports`,`Workers are limited to 8; sharding removes this cap`],1,`Horizontal scale (more machines via sharding) multiplies vertical scale (workers per machine) — the two are complementary.`],[`L`,`I`,"What happens to the test run if `globalSetup` throws an error?",null,[`Playwright ignores globalSetup errors and starts the tests`,`The entire test run aborts — no tests run. globalSetup failures are fatal because the preconditions for the suite are unmet.`,`Only the first worker is affected`,`Tests are retried without globalSetup`],1,`globalSetup failures abort the run — fix the setup issue before any test results can be trusted.`],[`R`,`B`,`You want to run smoke tests quickly (10 tests) and the full suite (500 tests) on the same CI config. How do you structure this?`,null,[`Maintain two separate playwright.config.ts files`,"Tag smoke tests with @smoke and run: `npx playwright test --grep @smoke` for fast feedback and without --grep for the full suite — same config, different invocation",`Duplicate the test files into a smoke/ directory`,`Use projects with testMatch: to separate smoke and full`],1,`Tag-based filtering (@smoke, @regression) is the clean way to select test subsets from one config.`],[`R`,`I`,`After adding parallelism, some database tests fail with "deadlock detected". How do you isolate them?`,null,[`Run all tests serially`,`Move the database tests into a describe.serial() block OR into a separate project with workers: 1 — either approach ensures only one test accesses the shared database at a time`,`Add retries: 3 to the database tests`,`Use transactions to avoid deadlocks`],1,`Serial execution is the correct isolation for tests that share a global resource like a database without per-test isolation.`],[`R`,`A`,`You want a CI pipeline where: 1) unit tests run first, 2) E2E tests run in parallel across 4 shards, 3) a report is published after all shards complete. How do you structure this?`,null,[`Run everything sequentially in one stage`,`Three pipeline stages: stage 1 (unit tests, fail fast), stage 2 (4 parallel shard jobs with --shard=N/4 --reporter=blob, upload blob-report/ artifacts), stage 3 (download all artifacts, run merge-reports --reporter=html, publish report)`,`Use a single Playwright config with all stages combined`,`Add a depends-on: unit-tests line to the Playwright config`],1,`Multi-stage CI with artifact-based report merging is the canonical parallel Playwright CI pattern.`],[`S`,`B`,`Adding fullyParallel breaks 20 tests that were accidentally sharing module-level state. How do you identify which tests are affected?`,null,[`Run with --timeout=1 to force failures early`,`Run the test suite with fullyParallel: true and workers: 2 — the failures reveal tests that depend on sequential execution or shared module state. Fix them by moving state into fixtures or using unique identifiers per test.`,`Use --reporter=list to see ordering`,`Sort the test files alphabetically to identify the ordering dependency`],1,`Two workers with fullyParallel is the minimum configuration that exposes parallelism bugs — binary search workers upward to find the concurrency level that causes failures.`],[`S`,`I`,`Your 4-shard CI run takes 15 minutes. Each shard runs for 13-15 minutes while one shard finishes in 3 minutes (uneven distribution). How do you rebalance?`,null,[`Increase shard count to 8`,`Playwright shards by file alphabetically — reorganize large spec files into smaller ones, or use --shard with a different total count. Alternatively use a CI matrix with dynamic test splitting based on historical timing data.`,`Add workers to the slow shards`,`Use blob reporter to detect the slow shard`],1,`Shard imbalance is caused by large test files — splitting them evens the distribution, or use a timing-aware split tool.`],[`S`,`A`,`You need to run the same test suite against 5 environments (dev, staging, eu-prod, us-prod, asia-prod) in CI. How do you structure this efficiently?`,null,[`Maintain 5 separate Playwright configs`,`One config with a BASE_URL environment variable; CI runs 5 parallel pipeline jobs, each setting BASE_URL to its environment and running the suite (with sharding per job if needed). The blob reporter aggregates per-environment results.`,`Run environments sequentially in one job`,`Use projects with one project per environment and workers: 1`],1,`Environment-as-a-variable scales cleanly — one config, N parallel CI jobs with different env vars, optional sharding within each job for speed.`],[`L`,`A`,"How does increasing Playwright's `workers` count improve total suite runtime, and what is the practical limit?",null,[`More workers run more test files concurrently, reducing wall-clock time roughly up to the number of available CPU cores — beyond that, additional workers cause resource contention and can slow the suite down`,`Workers have no effect on runtime; they only affect memory usage`,`Doubling workers always exactly halves runtime with no upper limit`,`Workers only affect the reporter output formatting, not execution speed`],0,`Parallelism helps until you saturate the machine's actual hardware resources — past that point, more workers just compete for the same CPU/memory.`],[`O`,`I`,"What does sharding (`--shard=2/4`) do differently from simply increasing `workers`?",`// playwright.config.ts
export default defineConfig({
  projects: [{ name: 'shard-1', testDir: './tests' }],
  shard: { current: 2, total: 4 },
});`,[`Sharding splits the ENTIRE test suite across multiple separate machines/CI jobs (each with its own workers), enabling parallelism beyond a single machine's CPU limit — workers only parallelize within one machine/process`,`Sharding and workers are two names for the exact same mechanism`,`Sharding runs the same tests multiple times for flake detection`,`Sharding only works for visual regression tests`],0,`Workers parallelize within a single process; sharding parallelizes across multiple processes/machines — the two are typically combined.`],[`R`,`I`,`Your CI pipeline runs 2,000 tests taking 40 minutes on a single runner. You want to cut wall-clock time significantly without reducing coverage. What is the most effective first step?`,null,[`Split the suite into multiple shards run in parallel CI jobs (e.g., 4 shards x 4 workers each), merging the HTML reports afterward`,`Rewrite every test to remove all await calls`,`Reduce the number of assertions per test to speed up execution`,`Switch from Chromium to Firefox, which is claimed to always run faster`],0,`Sharding across parallel CI jobs is the standard way to scale test suite runtime down once a single machine's worker count is already maximized.`],[`D`,`I`,"After enabling `fullyParallel: true` and increasing workers, several tests that used to pass now fail intermittently with data conflicts (e.g., two tests both create a user with the same email). What is the underlying issue?",null,[`The tests share mutable external state (like a test database) and were written assuming serial execution — full parallelism requires each test to use isolated data (unique emails/IDs, or a fresh DB per worker)`,`fullyParallel is a purely cosmetic reporter setting with no effect on execution order`,`Workers share the same BrowserContext by default, causing state leaks`,`The fix is to disable retries, since retries are causing the conflict`],0,`True parallel execution exposes any hidden assumption of shared state/order — tests must generate their own unique test data to be safely parallelizable.`],[`O`,`A`,`What does this configuration do?`,`test.describe.configure({ mode: 'parallel' });
test.describe('independent checks', () => {
  test('check A', async ({ page }) => { /* ... */ });
  test('check B', async ({ page }) => { /* ... */ });
});`,[`It runs the tests WITHIN this describe block in parallel across multiple workers, overriding the file's default (which runs tests in one file serially unless fullyParallel is globally enabled)`,`It runs the two tests simultaneously within a single shared page/context`,`mode: "parallel" has no effect unless fullyParallel is also set to false globally`,`It parallelizes the individual STEPS within a single test, not separate tests`],0,`describe.configure({ mode: 'parallel' }) is a scoped override — useful for marking a specific group of genuinely independent tests as parallelizable without changing the global fullyParallel setting for the whole suite.`],[`L`,`A`,"Why can liberal use of `retries` mask real, reproducible bugs instead of just smoothing over genuine flakiness?",null,[`If a test fails due to an actual race condition or timing bug in the APPLICATION (not just test flakiness), retries can make it 'pass' often enough that the underlying bug goes unnoticed and unfixed, even though the retry count itself is a signal worth monitoring`,`Retries always re-run against a completely different, randomly chosen browser, hiding the bug by accident`,`Retries have no downside; more retries is strictly always better`,`Retried tests are automatically excluded from the HTML report, hiding all information about them`],0,`Retries are a tool for tolerating occasional infra/timing flakiness — but a HIGH retry rate on a specific test is itself valuable signal that something (possibly a real app bug) deserves investigation, not just suppression.`],[`D`,`A`,"A test file declares a module-level `let counter = 0` incremented in each test, expecting values 1, 2, 3 across three tests — but results are inconsistent when the suite runs with multiple workers. What is the issue?",`let counter = 0;
test('first', async () => { counter++; expect(counter).toBe(1); });
test('second', async () => { counter++; expect(counter).toBe(2); });
test('third', async () => { counter++; expect(counter).toBe(3); });`,[`Each WORKER process gets its own separate module instance (and thus its own separate counter) — if these three tests get distributed across different workers, each starts counting from 0 independently, breaking the assumed shared sequential state`,`Module-level variables are read-only in Playwright test files, so counter++ silently fails`,`The bug is that let should be replaced with var for cross-worker sharing`,`Workers share memory directly, so this should always work regardless of distribution`],0,`Worker processes are separate OS processes — they do NOT share memory, so any module-level mutable state is really per-worker state, not global truth. Tests should never depend on shared in-memory state across files/workers.`],[`R`,`I`,`You suspect a specific test is flaky (fails roughly 1 in 20 runs) but cannot reproduce it on demand. What Playwright CLI feature helps track this down?`,null,["`npx playwright test my-flaky.spec.ts --repeat-each=30` — runs the same test 30 times in a row, dramatically increasing the odds of reproducing an intermittent failure and observing it under a debugger/trace",`There is no way to intentionally repeat a single test multiple times in Playwright`,`Set retries to 30, which is functionally identical to --repeat-each`,"Flaky tests can only be diagnosed by manually re-running `npx playwright test` by hand as many times as patience allows"],0,`--repeat-each is purpose-built for flake-hunting — it is fundamentally different from retries (which only re-runs AFTER a failure); this forces many runs regardless of pass/fail to gather statistical evidence.`],[`O`,`A`,`What does this project dependency configuration achieve?`,`export default defineConfig({
  projects: [
    { name: 'setup', testMatch: /global\\.setup\\.ts/ },
    { name: 'chromium', use: { ...devices['Desktop Chrome'] }, dependencies: ['setup'] },
  ],
});`,[`The 'setup' project (which might seed test data or generate an auth file) always runs to completion BEFORE the 'chromium' project starts — expressing an explicit ordering dependency between projects, unlike independent projects which have no guaranteed relative order`,`dependencies has no real effect; both projects always run fully in parallel regardless`,`It merges the two projects into a single combined test run with shared state`,`The setup project only runs if the chromium project fails first`],0,`The dependencies array is specifically for expressing "this project must finish first" — commonly used for a dedicated setup project that other test projects rely on.`],[`L`,`I`,`Why might a CI pipeline choose to shard a suite across 4 separate machines (2 workers each) rather than running it on 1 machine with 8 workers, even if that machine technically has 8 CPU cores?`,null,[`Sharding across separate machines avoids resource contention beyond a single machine's memory/IO limits, can reduce wall-clock time further with dedicated hardware per shard, and provides fault isolation — if one machine has an environment issue, only its shard is affected, not the whole run`,`8 workers on a single machine is always strictly faster than 4 machines with 2 workers each, with no exceptions`,`Sharding across machines is only possible if each machine runs a different operating system`,`CI systems cannot run more than 4 workers on a single machine due to a hard technical limit`],0,`Beyond raw CPU count, memory bandwidth, disk I/O, and network contention are real constraints — splitting across machines also adds resilience against a single bad runner instance.`],[`S`,`I`,`Two tests running in parallel both write to the same test user's profile in a shared staging database, causing one test's write to overwrite the other's expected state. What is the most robust general fix?`,null,[`Ensure each test creates and operates on its OWN uniquely-generated data (e.g., a fresh user per test via a setup API call) instead of sharing one fixed test user/record across concurrently running tests`,`Force the entire suite to run serially (single worker) permanently, sacrificing all parallelism to avoid the conflict`,`Add random sleep delays before each write, hoping timing coincidentally avoids collisions`,`Have both tests write to the same record but wrap each write in a try/catch that silently ignores conflicts`],0,`Per-test data isolation (unique users/records generated per test) is the general, scalable fix — sacrificing all parallelism is a blunt workaround that gives up most of the speed benefit tests are meant to provide.`],[`L`,`A`,"What is the practical difference between `testInfo.workerIndex` and `testInfo.parallelIndex`?",null,[`They are exactly the same value under two different names`,`workerIndex is a unique, ever-increasing number assigned to each worker PROCESS and is never reused for the life of the test run (a restarted worker after a crash gets a brand-new, higher index); parallelIndex is bounded between 0 and workers-1 and IS reused — it identifies "which of the N concurrent slots" a worker currently occupies, making it suitable for things like assigning a fixed port per concurrent slot`,`parallelIndex only exists when sharding is enabled`,`workerIndex resets to 0 at the start of every test file`],1,`workerIndex tracks worker PROCESS identity over the life of the run (never reused, even across restarts); parallelIndex tracks which of the currently-concurrent SLOTS (0..workers-1) is in use, which is what you want for resource allocation like ports.`],[`L`,`I`,"Why does running Playwright Test in `--debug` mode force the suite down to a single worker, regardless of the configured `workers` value?",null,[`Debug mode has no effect on worker count`,`Interactive step-by-step debugging (pausing execution, using the Playwright Inspector) fundamentally requires a single, focused execution flow — running multiple tests concurrently across workers while trying to step through one of them interactively would be incoherent, so Playwright forces workers to 1 in this mode`,`Debug mode is only available when workers is already set to 1 in the config`,`--debug mode disables all fixtures, which is why it needs only one worker`],1,`Interactive debugging assumes one linear, pausable flow of execution — running many tests concurrently across workers would make that step-by-step model meaningless, so Playwright serializes execution in debug mode.`],[`L`,`A`,'Why can enabling `trace: "on"` (rather than "on-first-retry" or "retain-on-failure") for every test become a meaningful bottleneck specifically as `workers` increases?',null,[`Trace recording only affects a single worker regardless of how many are configured`,`Every worker independently records and writes a full trace (screenshots, DOM snapshots, network activity) for every test to disk; more concurrent workers means more simultaneous disk I/O and CPU spent on trace serialization, which can genuinely limit how much speedup additional workers actually deliver — "on-first-retry" avoids this cost for the (usually large majority of) tests that pass on the first try`,`Trace files are stored in memory only and never touch disk`,`Increasing workers automatically disables tracing to prevent this issue`],1,`Tracing has a real per-test CPU/disk cost — recording traces for every test on every worker compounds that cost with concurrency, which is why more selective trace policies exist.`],[`L`,`I`,"Why might a team deliberately prefix console/log output with `` `[w${testInfo.workerIndex}]` `` when running with many workers?",null,[`Playwright requires log lines to be tagged with the worker index or it discards them`,`With multiple workers running concurrently, their console output interleaves in CI logs; without an explicit per-worker prefix, it becomes very difficult to tell which log line came from which test/worker when debugging a failure from raw CI output alone`,`Log prefixing is required for the HTML reporter to function`,`Worker index prefixes make tests run measurably faster`],1,`Interleaved output from multiple concurrent workers is a common CI debugging headache — a worker-index prefix is a cheap, effective way to disentangle which lines belong to which concurrent run.`],[`L`,`A`,"Why is `testInfo.outputPath()` a safer way to generate per-test temp files or downloads than a fixed path like `/tmp/download.pdf`, specifically under parallel execution?",null,[`outputPath() and a fixed path behave identically`,`outputPath() generates a path scoped to the specific test (and retry attempt), automatically namespaced so that two tests running concurrently in different workers never write to the same file at the same time; a single fixed path shared across all tests/workers would cause concurrent tests to overwrite or corrupt each other's files`,`outputPath() only works when workers: 1 is configured`,`Fixed paths are actually safer because they are simpler to reason about`],1,`A hardcoded shared path is a classic parallel-execution bug — any two concurrently-running tests using it will collide; testInfo.outputPath() exists specifically to give each test its own safe, unique location.`],[`L`,`I`,`Why does running each worker's tests against its own isolated backend (e.g. a per-worker Docker container or database schema) sometimes make more sense than sharing one backend across all workers?`,null,[`Isolated per-worker backends are always slower and therefore never worth it`,`A shared backend forces every worker's tests to coexist with every other worker's side effects (data creation, deletion, rate limits) at the SAME time, which is a common source of parallel test flakiness; giving each worker its own backend instance removes that entire class of cross-worker interference at the cost of the resources needed to run multiple backend instances`,`Playwright requires a separate backend per worker or it refuses to run tests in parallel`,`Per-worker backends are only relevant when using sharding, not workers`],1,`Per-worker backend isolation trades infrastructure cost for eliminating an entire category of cross-worker data-collision flakiness — a common tradeoff for larger, database-heavy suites.`],[`L`,`A`,"If step 2 of a `test.describe.serial()` block fails, does Playwright automatically retry the WHOLE block from step 1, or just the failed step?",null,[`Only the failed step is retried; earlier passing steps are never re-run`,`When a test inside a serial block fails and retries are enabled, Playwright re-runs the ENTIRE remaining sequence starting from the failed test onward (not from step 1, but the block's remaining tests are still bound by serial ordering) — and by default, once a test in a serial block fails, Playwright SKIPS the rest of that block rather than continuing, unless a retry is available for the failed test itself`,`Serial blocks cannot be retried under any circumstances`,`Retries apply per-file, never per describe block`],1,`A failure partway through a serial block, by default, causes Playwright to skip the remaining tests in that block (since they likely depend on the failed step's state) — the failing test itself may still be retried per the normal retries setting.`],[`L`,`B`,"Can the `workers` count be set differently for two different `projects` entries in the same `playwright.config.ts`?",null,[`Yes, each project can independently declare its own worker pool size`,"No — `workers` is a single, TOP-LEVEL setting for the entire test run; it is not configurable per-project. All projects share the same overall worker budget, though how tests are distributed among workers still accounts for project boundaries",`Workers can only be set per-project, never globally`,`Setting workers per-project is possible only when sharding is also enabled`],1,`workers is a run-wide setting, not something individual project entries in the config array can override — a common misconception when first working with a multi-project (multi-browser) config.`],[`L`,`I`,`Why might a team create a dedicated, low-parallelism "quarantine" project specifically for known-flaky tests, separate from the main fully-parallel suite?`,null,[`Quarantining flaky tests is not possible in Playwright's project system`,`Isolating known-flaky tests into their own project (often with fewer workers or forced serial execution, and possibly more retries) prevents their instability from consuming shared worker slots needed by the reliable majority of the suite, and keeps CI signal from the main suite clean while the flaky tests are investigated and fixed separately`,`Quarantine projects automatically fix flaky tests without further investigation`,`Flaky tests must always be deleted rather than quarantined`],1,`A separate, lower-priority lane for known-flaky tests protects the signal quality of the main suite while giving the flaky tests room to be investigated without blocking everything else.`],[`L`,`A`,`Why is the choice between "more shards on more CI machines" and "more workers on fewer, larger machines" often a cost question as much as a technical one?`,null,[`CI billing is never affected by how parallelism is achieved`,`Many CI providers bill per machine-minute or per concurrent runner; spinning up more shards means paying for more concurrent machines (even if each does less work), while fewer, larger machines with more workers concentrates the cost differently — the fastest wall-clock option is not always the cheapest, and teams often have to balance speed against the resulting CI bill`,`Sharding is always free regardless of how many machines are used`,`Workers and shards always cost exactly the same regardless of configuration`],1,`Wall-clock speed and CI cost do not always move together — more concurrent machines (shards) can finish faster but often cost more, so the "best" configuration depends on which tradeoff the team prioritizes.`],[`L`,`A`,"A suite passes reliably at `workers: 1` but 2 specific tests fail whenever `workers` is 4 or higher. What kind of bug does this pattern typically indicate?",null,[`A Playwright bug specific to worker counts above 4`,`An order/isolation dependency bug — the two failing tests likely share some state (a database record, a fixed test account, a global counter) that only collides when they happen to run CONCURRENTLY in different workers; at workers:1 they never overlap in time, masking the underlying issue`,`Tests always fail more at higher worker counts due to CPU throttling`,`This pattern indicates a hardware issue with the CI machine, unrelated to the test code`],1,`The classic signature of a hidden concurrency/isolation bug is "passes serially, fails in parallel" — something the two tests share is only actually contended when they run at the same time.`],[`L`,`I`,"Why should browser binaries typically be installed once (e.g. via `npx playwright install` as a separate CI step) rather than relying on each worker process to install them independently?",null,[`Each worker process installing its own browsers in parallel is the recommended, fastest approach`,`Browser binaries are large downloads shared by ALL workers in a run; installing them once up front (before workers start) avoids N workers redundantly re-downloading/re-installing the same binaries simultaneously, which wastes bandwidth, disk I/O, and time, and can even corrupt a shared install directory if writes race`,`Browser installation happens automatically inside each worker with no configuration needed`,`Workers do not use browser binaries; only the main test process does`],1,`Browser binaries are a shared, one-time cost — treating installation as a separate, prior CI step (rather than something each worker redundantly repeats) avoids wasted time and potential install-directory race conditions.`],[`L`,`I`,"What does `test.describe.configure({ retries: 2 })` scope its retry override to, compared to setting `retries: 2` in `playwright.config.ts`?",null,[`They are identical; both apply retries to every test in the entire run`,`The describe-level configure() call applies its retry override ONLY to the tests inside that specific describe block, leaving the rest of the suite governed by whatever the global config (or other describe blocks) specify — useful for giving a known-more-flaky group of tests extra retries without inflating retries suite-wide`,`describe.configure() can only reduce retries, never increase them`,`Retries set via describe.configure() apply globally once used anywhere in the file`],1,`describe.configure() is a scoped override — it changes settings only for the tests inside that block, letting different parts of a suite have different retry policies without one global setting for everything.`],[`L`,`A`,"Why does `expect.poll()`'s own timeout need to be considered alongside the test-level timeout when running under aggressive worker/CPU contention?",null,[`expect.poll() timeouts are completely independent of CPU load and never affected by contention`,`Under heavy CPU contention from many concurrent workers, the actual wall-clock time for the polled condition to become true (or even for each poll callback to execute) can stretch out — if the expect.poll() timeout is set tight relative to the overall test timeout, contention-induced slowness can cause spurious poll timeouts that would not occur on a less-loaded machine`,`expect.poll() cannot be used inside tests that run under parallel workers`,`Increasing workers automatically increases every expect.poll() timeout proportionally`],1,`Anything time-based (like expect.poll intervals/timeouts) is sensitive to how loaded the machine actually is — heavy worker contention can slow down real wall-clock execution enough to trip timeouts that were fine on a quieter machine.`],[`L`,`A`,"Why does Playwright default `workers` to roughly half of the available CPU cores instead of using all of them?",null,[`Half-core defaults are an arbitrary historical accident with no rationale`,`Each worker runs a full browser process (which itself uses multiple threads/processes internally for rendering, GPU, networking, etc.), so a worker consumes meaningfully more than "one core's worth" of resources; defaulting to half the logical cores leaves headroom for the browser processes themselves rather than assuming each worker only needs one core`,`Using all CPU cores for workers is impossible due to an OS-level Node.js restriction`,`Half-core defaults exist purely to reduce electricity costs for CI providers`],1,`A worker is not just a thin test-runner thread — it drives a full browser process with its own multi-process/multi-threaded overhead, which is why the default assumes each worker needs more than a single core's share.`],[`L`,`A`,`Why can a moderate worker count against a real staging backend produce HTTP 429 (rate limited) responses that never appear when running the same suite against a local mock server?`,null,[`429 responses are always a Playwright-side bug, never a real backend behavior`,`A shared staging backend often enforces real-world rate limits (per IP, per API key, or globally) that a local mock server simply does not implement; higher worker counts mean more concurrent requests hitting the same real, rate-limited backend, which can trigger throttling that a mock (with no such limits) never would`,`Local mock servers always run faster than staging, which is unrelated to rate limiting`,`429 responses only occur when sharding, not when only increasing workers`],1,`Rate limiting is a real production/staging backend concern that many local mocks simply do not simulate — parallelism surfaces this gap by generating more simultaneous real requests than a lower-concurrency run would.`],[`L`,`I`,"Why might one enormous spec file with 200 tests cause uneven shard distribution, even though `--shard` tries to balance the total number of tests per shard?",null,[`Shard balancing accounts for file size in bytes, not test count, so this cannot happen`,`If shard balancing groups tests by FILE (a common constraint, since some file-level state may need to stay together) and one file contains a disproportionate share of all tests, that file's tests may end up concentrated on one or a few shards rather than evenly spread, unbalancing the shards' total runtimes even if the raw test COUNT is technically balanced`,`Playwright automatically splits any file exceeding 50 tests across multiple shards`,`Sharding always ignores file boundaries entirely, so this scenario cannot occur`],1,`A large outlier file can skew shard balance if the sharding granularity keeps a file's tests together — splitting an oversized file into several smaller ones is a common fix for this kind of imbalance.`],[`L`,`I`,"What is a downside of configuring a CI matrix's sharded jobs with `fail-fast: true` (cancel all other shards as soon as one shard fails)?",null,[`fail-fast has no meaningful downside and should always be enabled`,`While it saves compute by not continuing after the first failure, it means a single failing test in one shard prevents you from seeing the FULL picture of what else might be broken across the other shards in that same run — useful for saving cost, but it trades away complete failure visibility in one CI run`,`fail-fast only applies to sharded runs, never regular parallel runs`,`fail-fast automatically retries the failing shard before cancelling others`],1,`fail-fast optimizes for compute savings at the cost of full-picture visibility — you learn about only the first failure per run instead of everything that is currently broken.`],[`L`,`A`,'Why is there no single, unified "merged trace timeline" across all shards, even after running `merge-reports` to combine their HTML reports?',null,[`merge-reports does produce one single combined trace spanning all shards automatically`,`Each individual TEST still produces its own separate trace file (scoped to that one test's execution); merge-reports combines the reporting metadata (pass/fail, timings, links to each test's own trace) into one HTML report, but it does not — and conceptually could not — merge multiple, unrelated tests' independent traces into a single combined timeline`,`Trace files do not exist when sharding is used`,`merge-reports deletes all individual trace files and replaces them with a single global one`],1,`A trace is inherently scoped to one test's execution — merge-reports unifies the REPORT (which test passed/failed, links to artifacts) across shards, but each test's trace remains its own separate artifact.`],[`L`,`I`,"Why might a team deliberately run their visual-regression (screenshot comparison) tests with `workers: 1` even while the rest of the suite uses full parallelism?",null,[`Visual regression tests cannot run under Playwright's parallel execution model at all`,`Heavy CPU/GPU contention from many concurrent workers rendering pages simultaneously can subtly affect rendering timing, font rasterization, or animation completion, occasionally producing tiny pixel differences that make screenshot comparisons flaky; running these specific tests with less (or no) contention improves rendering consistency and reduces false-positive visual diffs`,`workers: 1 makes screenshots render at a higher resolution`,`Visual regression tests are always faster with more workers, so this would be counterproductive`],1,`Rendering-sensitive tests can be more susceptible to resource-contention-induced timing/rendering variance — reducing concurrency for just that subset can improve visual diff stability at some cost to speed.`],[`L`,`B`,"When a config defines multiple `projects` for cross-browser testing (chromium, firefox, webkit), does each project get its own separate pool of `workers`, or do they all share one global pool?",null,[`Each project gets an independent, additional pool of workers on top of the global setting`,"All projects share ONE global worker pool sized by the top-level `workers` setting — tests from chromium, firefox, and webkit projects all compete for the same fixed number of concurrent worker slots, rather than each browser getting its own separate allocation",`Only the first project listed gets any workers; the rest run serially`,`Cross-browser projects always run strictly one browser at a time regardless of workers`],1,`workers is a single run-wide budget shared across every project in the config — adding more browser projects does not multiply the available concurrency, it just adds more work competing for the same pool.`],[`L`,`I`,"What happens to Playwright's `workers` setting when `test.only()` is used in one file within an otherwise fully-parallel suite?",null,[`test.only() forces the entire run down to workers: 1`,`test.only() only changes WHICH tests are selected to run (skipping everything else) — it has no special effect on the configured worker count itself; if only a handful of tests remain selected, they may simply not need many workers to run since there are fewer of them, but the workers setting itself is unchanged`,`test.only() disables workers entirely, running that one test synchronously outside the worker pool`,`test.only() automatically increases workers to run the selected test faster`],1,`test.only() is purely a test-selection mechanism — it filters which tests run, and does not itself alter parallelism configuration; fewer selected tests may simply need fewer workers in practice.`],[`L`,`A`,"Why does a very long-running `globalSetup` function delay the START of every worker, even in a `fullyParallel: true` configuration with a high `workers` count?",null,[`globalSetup runs concurrently with worker startup, so it never delays anything`,`globalSetup is a single, one-time, synchronous gate that runs to completion BEFORE Playwright spawns any worker processes at all — no amount of worker/parallelism configuration changes this; a slow globalSetup is an unavoidable serial bottleneck at the very start of every run, regardless of how parallel the rest of the suite is`,`globalSetup only affects the first worker; the rest start immediately`,`fullyParallel: true causes globalSetup to be skipped entirely`],1,`globalSetup is inherently a single serial gate before any parallelism begins — its runtime is a flat tax added to the front of every run, unaffected by how many workers or how parallel the rest of the config is.`],[`L`,`I`,`Why can a single unusually slow "full smoke path" test (e.g. taking 3 minutes while every other test takes 5 seconds) become the effective bottleneck of an otherwise fast, highly-parallel suite?`,null,[`A slow test in one worker has no effect on total suite time as long as other workers are still running`,`Total suite wall-clock time is bounded by the LAST worker to finish; if every other test completes quickly but one worker is stuck running a 3-minute outlier test, the whole run cannot report as complete until that worker finishes — even a fully-parallel, well-configured suite is only as fast as its longest individual test's tail latency`,`Slow tests are automatically moved to their own dedicated worker to avoid this issue`,`Playwright automatically kills any test that runs longer than the suite's average test duration`],1,`Parallel suite runtime is gated by the slowest straggler, not the average — one long outlier test can dominate total wall-clock time even when everything else finishes almost instantly.`],[`O`,`A`,`What problem does this fixture solve for tests that need a locally-bound port?`,`export const test = base.extend<{ port: number }>({
  port: async ({}, use, testInfo) => {
    await use(4000 + testInfo.parallelIndex);
  },
});`,[`It assigns every test the exact same port, relying on Playwright to queue requests`,`It derives a unique, non-colliding port number for each concurrently-running test SLOT using parallelIndex (which is bounded between 0 and workers-1 and safely reused across slots), so tests that need to bind a local server/mock never fight over the same port when running concurrently`,`It assigns a port based on workerIndex, which grows unbounded over a long run and would eventually exceed valid port ranges`,`testInfo.parallelIndex is only available inside globalSetup, not in fixtures`],0,`parallelIndex is specifically bounded (0..workers-1), making it ideal for deriving a small, stable set of non-colliding resource identifiers like ports — unlike workerIndex, which grows unbounded over a long run.`],[`O`,`I`,'What does declaring this fixture with `{ scope: "worker", auto: true }` achieve?',`export const test = base.extend({
  featureFlags: [async ({}, use) => {
    const flags = await loadFlagsForWorker();
    await use(flags);
  }, { scope: 'worker', auto: true }],
});`,[`The fixture only runs if a test explicitly destructures { featureFlags }`,`Combining worker scope (created once per worker, shared by all its tests) with auto: true (runs automatically without any test needing to request it) means every test in that worker transparently benefits from feature flags being loaded exactly once per worker, with zero per-test opt-in required`,`auto: true forces the fixture to re-run before every single test despite worker scope`,`scope: "worker" and auto: true cannot be combined in the same fixture`],1,`auto + worker scope together create a transparent, amortized, cross-cutting fixture — set up once per worker, silently applied to every test in it, with no test needing to explicitly request it.`],[`O`,`I`,`What does this project configuration achieve for isolating known-flaky tests?`,`projects: [
  { name: 'stable', testIgnore: /\\.flaky\\.spec\\.ts$/, fullyParallel: true },
  {
    name: 'quarantine',
    testMatch: /\\.flaky\\.spec\\.ts$/,
    fullyParallel: false,
    retries: 3,
  },
],`,[`It runs every test twice, once in each project, doubling coverage`,`It splits the suite into a fully-parallel "stable" project (excluding files named *.flaky.spec.ts) and a separate, serialized "quarantine" project (only those flaky files, with extra retries) — isolating unreliable tests so they cannot consume shared worker capacity or destabilize the main suite's signal`,`testIgnore and testMatch cannot both be used across different projects in one config`,`fullyParallel: false in the quarantine project causes those tests to be skipped entirely`],1,`Splitting known-flaky tests into their own project (with different parallelism/retry settings) is a common way to keep quarantined tests running without letting their instability affect the main suite's reliability or speed.`],[`O`,`A`,"What issue does this globalSetup introduce for a `fullyParallel: true`, high-`workers` configuration?",`export default async function globalSetup() {
  await seedTenThousandProducts(); // takes ~4 minutes, runs synchronously, blocks completion
}`,[`fullyParallel: true automatically parallelizes globalSetup itself, so this is not a problem`,`Regardless of how many workers are configured, globalSetup is a single serial gate that must fully complete before ANY worker starts — a 4-minute synchronous seed adds a flat 4-minute tax to the front of every single run, no matter how parallel or how many workers the rest of the config uses`,`This code will run once per worker, multiplying the seeding time by the worker count`,`globalSetup timing has no effect on total suite runtime since it runs in a separate process`],1,`globalSetup's runtime is always a flat, unavoidable serial cost added before any parallelism kicks in — a slow one-time seed step directly inflates every run's total time regardless of workers/fullyParallel.`],[`O`,`I`,"What will happen when this test file runs in a suite with `fullyParallel: true` and 8 configured workers?",`test('critical smoke path', async ({ page }) => { /* ... */ });
test.only('debugging this one', async ({ page }) => { /* ... */ });
test('another test', async ({ page }) => { /* ... */ });`,[`All 3 tests run, distributed across up to 8 workers as usual`,`test.only() restricts execution to ONLY the "debugging this one" test across the ENTIRE run (not just this file) — the other two tests here (and every other test in the suite) are skipped; the 8-worker configuration is irrelevant since there is now only one test to actually run`,`test.only() only skips tests within the same describe block, not the whole file`,`This configuration will throw an error because test.only() cannot be combined with fullyParallel`],1,`test.only() is suite-wide, not file-scoped — it is easy to forget that leaving one in a file suppresses every OTHER test across the entire run, not merely the rest of that one file.`],[`O`,`A`,`What does this pattern accomplish for keeping expect.poll() reliable under variable machine load?`,`await expect.poll(async () => {
  const res = await page.request.get('/api/job-status');
  return (await res.json()).status;
}, {
  timeout: 30_000,
  intervals: [1_000, 2_000, 5_000],
}).toBe('complete');`,[`It polls at a fixed 1-second interval forever until the test-level timeout is hit`,`It uses an increasing back-off interval schedule (1s, then 2s, then settling at 5s) combined with a generous overall 30-second timeout, reducing the number of redundant requests made while still giving the condition ample wall-clock time to become true even under contention from other concurrently-running workers`,`intervals can only accept a single fixed number, not an array`,`expect.poll() ignores the timeout option and always uses Playwright's global test timeout`],1,`A backing-off poll interval combined with a generous overall timeout is a robust pattern for conditions whose actual completion time can vary under load — it avoids hammering the endpoint while still tolerating slower-than-usual completion.`],[`O`,`I`,`What does this CI matrix configuration achieve (pseudocode)?`,`strategy:
  fail-fast: false
  matrix:
    shardIndex: [1, 2, 3, 4]
steps:
  - run: npx playwright test --shard=\${{ matrix.shardIndex }}/4 --reporter=blob`,[`It runs the full suite 4 separate times, once per shard, with no actual splitting`,`It launches 4 parallel CI jobs, each running its own 1/4 slice of the total suite (via --shard) and writing a blob report; fail-fast: false ensures that if one shard fails, the other 3 continue running to completion rather than being cancelled, giving full visibility into all failures across the whole run`,`fail-fast: false disables sharding entirely, running the whole suite on one job`,`Blob reports from these 4 jobs will merge automatically without any further steps`],1,`fail-fast: false is the choice that trades some wasted compute (continuing shards even after one fails) for complete failure visibility across every shard in a single CI run.`],[`O`,`A`,`What does this fixture do to reduce database contention across parallel workers?`,`export const test = base.extend<{}, { dbSchema: string }>({
  dbSchema: [async ({}, use, workerInfo) => {
    const schema = \`test_worker_\${workerInfo.workerIndex}\`;
    await createSchema(schema);
    await use(schema);
    await dropSchema(schema);
  }, { scope: 'worker' }],
});`,[`It creates one shared schema for all workers, ensuring consistency`,`It gives each WORKER its own dedicated, uniquely-named database schema (derived from workerIndex), created once at worker start and torn down at worker end — tests running in different workers never contend for the same rows/tables, eliminating a whole class of parallel-execution data collisions`,`workerInfo.workerIndex is not accessible from within a fixture`,`This fixture creates a new schema before every single test, not once per worker`],1,`A worker-scoped fixture that creates a schema keyed by workerIndex is a standard pattern for giving each concurrently-running worker fully isolated data, removing cross-worker contention entirely.`],[`O`,`I`,`What behavior does nesting a serial block INSIDE a describe that also has independent, unrelated tests achieve?`,`test.describe('checkout area', () => {
  test('shows promo banner', async ({ page }) => { /* independent */ });
  test.describe.serial('multi-step purchase', () => {
    test('add to cart', async ({ page }) => { /* ... */ });
    test('checkout', async ({ page }) => { /* ... */ });
  });
});`,[`All tests in "checkout area" become serial because one nested block is serial`,`"shows promo banner" remains independently schedulable like any other test (potentially running concurrently with other files/tests), while "add to cart" and "checkout" are bound together in strict sequential order on a single worker — the serial constraint only applies to the nested block, not the whole outer describe`,`The independent test will fail to compile since it cannot coexist with a serial block in the same describe`,`This configuration is invalid; serial blocks cannot be nested inside a plain describe`],1,`A serial block's ordering constraint is scoped to itself — sibling tests/describes at the same level outside it are unaffected and remain independently parallelizable.`],[`O`,`A`,`What does splitting the suite this way accomplish for shard balance?`,`// Before: one file with 400 tests, uneven shard split
// After: split into 8 files of 50 tests each, same total test count
// tests/checkout-01.spec.ts ... tests/checkout-08.spec.ts`,[`It changes the total test count, which is why shards become more balanced`,`If sharding groups work at the file level, splitting one oversized file into several smaller files gives the shard-balancing algorithm finer-grained units to distribute, making it far more likely that each of the N shards ends up with a similar total number of tests (and therefore similar runtime) instead of one shard being saddled with a disproportionately large single file`,`Splitting files has no effect on shard distribution since shards are always based on raw test count regardless of file grouping`,`This change will cause tests to run twice, once in each file`],1,`Finer-grained file boundaries give a file-level shard balancer more flexibility to spread tests evenly — one giant file is a rigid, hard-to-balance unit compared to several smaller ones covering the same tests.`],[`O`,`I`,`What debugging technique does this invocation combine?`,`npx playwright test tests/checkout.spec.ts --workers=1 --repeat-each=20`,[`It runs 20 different browsers simultaneously against the same test`,`It removes concurrency entirely (workers=1) while repeating the SAME test 20 times in a row — a focused way to reproduce an intermittent failure without the added variable of cross-worker interference, isolating whether the flakiness is inherent to the test itself versus caused by parallel execution`,`--repeat-each is incompatible with --workers and this command will error`,`It runs the test suite 20 times in parallel across 20 separate workers`],1,`Combining workers=1 with repeat-each isolates a single test's inherent flakiness from any parallel-execution-induced flakiness — a useful first diagnostic split when hunting an intermittent failure.`],[`O`,`A`,`What does this worker-scoped fixture provide, and what should its teardown ensure?`,`export const test = base.extend({
  redisContainer: [async ({}, use, workerInfo) => {
    const container = await startRedisContainer({ port: 7000 + workerInfo.workerIndex });
    await use(container);
    await container.stop();
  }, { scope: 'worker' }],
});`,[`A single shared Redis instance for all workers, avoiding port conflicts by design`,`An isolated Redis container per worker, bound to a unique port derived from workerIndex so multiple concurrently-running workers never try to bind the same port — its teardown must reliably stop the container even if a test in that worker fails, or containers could leak and accumulate across runs`,`Redis containers cannot be started from within a Playwright fixture`,`workerInfo.workerIndex changes on every test, so this would start a new container per test, not per worker`],1,`Per-worker infrastructure (like a container) needs a worker-unique identifier (like workerIndex) to avoid port/resource collisions, and its teardown needs to be robust to test failures so resources do not leak across the run.`],[`O`,`I`,"What does adding `trace: 'on-first-retry'` to this config balance, compared to `trace: 'on'`, under high parallelism?",`use: {
  trace: 'on-first-retry',
},
retries: 1,`,[`It records a trace for every single test on every worker, same as trace: "on"`,`It avoids the disk I/O and CPU cost of tracing tests that pass on their first attempt (the large majority in a healthy suite), only paying that cost for tests that actually fail and get retried — a meaningful savings when many workers would otherwise be writing full traces simultaneously`,`on-first-retry disables tracing entirely unless retries is set to at least 2`,`It only works when workers: 1 is configured`],1,`"on-first-retry" specifically targets the cost of tracing at the tests that actually need debugging (failures), avoiding the overhead of tracing every passing test across every concurrent worker.`],[`O`,`A`,`What race condition does this module-level caching pattern introduce under parallel workers?`,`// tests/helpers/expensive.ts
let cachedResult: number | undefined;
export function getExpensiveComputation() {
  if (cachedResult === undefined) {
    cachedResult = computeSomethingSlow();
  }
  return cachedResult;
}`,[`This pattern is perfectly safe and effective across all workers`,"Since each worker is a separate Node.js process with its own copy of this module, `cachedResult` is NOT actually shared across workers — the intended cross-test caching benefit only applies WITHIN a single worker's tests, and the computation is redundantly repeated once per worker rather than once for the whole run, contrary to what the code might suggest at a glance",`computeSomethingSlow() will be called concurrently by multiple tests in the same worker, causing a real race condition on cachedResult`,`Module-level variables in Node.js are shared via the file system automatically`],1,`Module-level state is per-PROCESS, and each worker is its own process — this cache only helps within one worker, silently repeating the "expensive" computation once per worker rather than truly once per run.`],[`O`,`I`,`What does explicitly resetting mode here achieve within an otherwise serial file?`,`test.describe.serial('onboarding flow', () => {
  test('step 1', async ({ page }) => { /* ... */ });
  test.describe.configure({ mode: 'default' });
  test.describe('unrelated checks', () => {
    test('check A', async ({ page }) => { /* independent */ });
    test('check B', async ({ page }) => { /* independent */ });
  });
});`,[`configure({ mode: "default" }) has no effect once a parent block is already serial`,`It attempts to reset the nested "unrelated checks" block back to Playwright's normal (non-serial) scheduling — however, since the OUTER describe.serial() constrains everything nested within it to run in order on one worker regardless of inner configuration, this reset does not actually free those inner tests from the outer serial constraint`,`This configuration will cause a runtime error because mode cannot be changed mid-file`,`It reverses the parent describe.serial(), making "step 1" run in parallel with the rest`],1,`A serial ancestor is a hard outer constraint — attempting to loosen scheduling for a nested block does not escape a stricter ancestor's ordering requirement.`],[`O`,`A`,`What diagnostic purpose does this conditional skip serve?`,`test('run extended diagnostics', async ({ page }, testInfo) => {
  test.skip(testInfo.parallelIndex !== 0, 'Only run on the first parallel slot to avoid duplicating expensive diagnostics');
  await collectDetailedPerformanceMetrics(page);
});`,[`It ensures the test never runs under any configuration`,`It uses parallelIndex to designate exactly ONE of the currently-running concurrent slots (slot 0) as a "canary" that performs additional expensive diagnostic work, while every other concurrently-running slot skips it — avoiding redundantly repeating costly diagnostics across every worker`,`parallelIndex is not accessible from within a test body, only from fixtures`,`This pattern requires sharding to be enabled to function correctly`],1,`Since parallelIndex is bounded and stable across the run, checking for a specific value (like 0) is a simple way to designate exactly one concurrent slot for extra, non-duplicated work.`],[`O`,`I`,"What does adding `dependencies: ['seed']` to this project achieve when combined with `fullyParallel: true`?",`projects: [
  { name: 'seed', testMatch: /seed\\.setup\\.ts/ },
  {
    name: 'e2e',
    fullyParallel: true,
    dependencies: ['seed'],
  },
],`,[`fullyParallel: true cancels out the ordering guarantee from dependencies, so seed and e2e tests may interleave unpredictably`,`The "seed" project is guaranteed to run to completion BEFORE any test in the "e2e" project starts, regardless of how parallel the "e2e" project itself is once it begins — dependencies enforces ordering BETWEEN projects, while fullyParallel only affects scheduling WITHIN the dependent project once it is allowed to start`,`dependencies and fullyParallel cannot be used in the same project entry`,`This configuration causes the seed project to also run with full parallelism`],1,`dependencies establishes a hard ordering gate between projects; fullyParallel is an orthogonal setting about how tests WITHIN an already-started project are scheduled — the two compose without conflict.`],[`O`,`A`,`What does adding this fixture accomplish for downloads under parallel execution?`,`export const test = base.extend({
  downloadDir: async ({}, use, testInfo) => {
    const dir = testInfo.outputPath('downloads');
    await fs.promises.mkdir(dir, { recursive: true });
    await use(dir);
  },
});
// test:
const download = await page.waitForEvent('download');
await download.saveAs(path.join(downloadDir, download.suggestedFilename()));`,[`It saves every test's downloads to the exact same shared folder for easy comparison`,`It derives a per-test, uniquely-namespaced download directory via testInfo.outputPath(), ensuring that concurrently-running tests (potentially in different workers) never write downloaded files to the same path and risk overwriting or reading each other's files`,`outputPath() always returns a path inside the system temp directory regardless of the test`,`This fixture prevents downloads from being tested at all under fullyParallel`],1,`testInfo.outputPath() automatically scopes a path to the specific test, which is exactly the kind of per-test isolation needed for any file (like a download) that concurrently-running tests could otherwise collide on.`],[`R`,`A`,`A 4-shard CI run has one shard consistently taking 3x longer than the others due to one enormous spec file. Beyond simply splitting that file, what tooling approach can produce a more evenly-balanced split going forward?`,null,[`Manually guess-and-check file groupings until shard times look roughly even`,`Use a timing-aware sharding strategy: capture historical per-test or per-file duration data from previous runs (many CI systems and community tools support this) and use it to assign files/tests to shards based on actual measured runtime rather than raw file/test count, so shards are balanced by TIME rather than by count`,`Increase the shard count until the imbalance becomes negligible by brute force`,`Disable sharding entirely and rely solely on increasing workers on a single larger machine`],1,`Count-based balancing assumes every test/file takes similar time, which is often false — a timing-aware split (using real historical duration data) produces genuinely balanced shards instead of just balanced counts.`],[`R`,`A`,`Your suite needs to run identically against both PostgreSQL and MySQL backends in CI to verify database-agnostic behavior. How do you structure this without doubling your test-writing effort?`,null,[`Duplicate every test file, once hardcoded for Postgres and once for MySQL`,"Keep a single set of test files parameterized by a `DB_BACKEND` environment variable (or a Playwright project per backend using `use` to pass the variable), and run two parallel CI jobs — one per backend — each pointing the SAME suite at its respective database via configuration, optionally sharding within each job for additional speed",`Only test against one database and assume the other behaves identically without verification`,`Run both databases simultaneously against the same test run using a single shared connection`],1,`The environment-as-a-variable pattern (one suite, config-driven target) scales to any number of backend variants without duplicating test code — each variant just becomes another parallel CI job.`],[`R`,`A`,`Your monorepo has 5 separate frontend apps, each with its own Playwright suite, currently all running sequentially in one long CI pipeline. How do you restructure this for speed using parallelism concepts?`,null,[`Merge all 5 apps' tests into one giant Playwright config to simplify CI`,`Run each app's suite as an independent, parallel CI job (a natural "shard" boundary along app lines rather than arbitrary test-count splits), each with its own appropriate workers setting based on that app's suite size — apps that change independently can also be conditionally skipped in CI when unaffected by a given commit`,`Increase workers on the single sequential pipeline without changing its structure`,`Run all 5 apps' suites in a single worker to guarantee no cross-app interference`],1,`App boundaries in a monorepo are often a natural, meaningful sharding axis — parallelizing along real project lines (rather than just splitting an undifferentiated test count) also enables per-app CI optimizations like change-based skipping.`],[`R`,`I`,`Your suite's runtime jumped from 5 minutes to 20 minutes after a routine dependency bump, and it is unclear whether the regression is in test code, the app, or infrastructure. What is a good first diagnostic step using parallelism controls?`,null,[`Immediately roll back the dependency bump without further investigation`,"Run the suite with `--workers=1` and compare per-test timings against a known-good baseline — if individual test durations are now dramatically slower even in isolation, the regression is likely in the app or a dependency's runtime behavior; if tests are individually fast but the aggregate is slow, investigate contention/parallelism-related causes (resource exhaustion, backend saturation) instead",`Increase workers further to see if it masks the slowdown`,`Delete the slowest 20% of tests to bring the total time back down`],1,`Isolating with workers=1 separates "are individual tests actually slower" from "is something about running many of them concurrently now worse" — a useful first fork in a performance regression investigation.`],[`R`,`A`,`Your team wants fast feedback on every push (a few minutes) but also wants full regression coverage (which takes 45 minutes) without slowing down every commit. How do you structure this using tagging and scheduling?`,null,[`Run the full 45-minute suite on every single push regardless of feedback speed goals`,'Tag a small, representative "smoke"/"canary" subset of tests (`--grep @smoke`) to run on every push for fast feedback, and schedule the full suite (optionally sharded for speed) to run on a slower cadence — nightly, or pre-merge on a release branch — giving both fast iteration and eventual full coverage',`Run the full suite but only report the first failure found to save time`,`Disable coverage checks on pushes entirely and only run tests once a week`],1,`Splitting fast feedback (small tagged smoke subset, every push) from full coverage (larger suite, less frequent cadence) is the standard way to balance developer iteration speed against regression coverage completeness.`],[`R`,`A`,"Your legacy suite of 800 tests was written assuming serial, file-by-file execution and would break badly if `fullyParallel: true` were flipped on all at once. How do you migrate toward full parallelism safely?",null,[`Flip fullyParallel: true globally in one commit and fix whatever breaks afterward`,`Enable fullyParallel incrementally, e.g. per-project or per-directory using test.describe.configure({ mode: "parallel" }) on files/directories you have already audited for isolation, gradually expanding coverage as more of the suite is verified safe for concurrent execution, rather than a single risky global flip`,`Rewrite the entire suite from scratch before considering any parallelism changes`,`fullyParallel cannot be adopted incrementally; it is all-or-nothing by design`],1,`A large legacy suite benefits from an incremental, audited rollout of parallelism (directory by directory or project by project) rather than a risky one-shot global flip that could destabilize the whole suite at once.`],[`R`,`I`,`A third-party payment sandbox your suite depends on enforces a strict rate limit (10 requests/second) that your fully-parallel, high-worker suite regularly exceeds, causing spurious test failures. What is a reasonable fix?`,null,[`Ignore the rate limit and add retries to paper over the resulting failures`,`Introduce a shared, worker-aware throttling mechanism for calls to that specific sandbox (e.g. a semaphore/queue fixture, or simply reducing the number of workers/a dedicated lower-parallelism project specifically for payment-related tests) so the aggregate request rate across all concurrent tests respects the sandbox's real limit`,`Increase workers further since more requests will simply queue up automatically`,`Switch payment providers entirely to avoid any rate limiting`],1,`A real external rate limit is a hard constraint parallel test execution must respect — either throttle the aggregate request rate explicitly or reduce concurrency specifically for the tests hitting that limited resource.`],[`R`,`A`,'Your CI runs Playwright inside Docker containers, and you notice significantly fewer "effective" workers finishing useful work compared to running the same `workers` count on bare metal with the same core count. What should you investigate?',null,[`Docker always halves Playwright's performance with no way to mitigate it`,"Check the container's configured CPU/memory limits (cgroups) — a container might report the host's full core count to Node.js while actually being throttled to a fraction of that CPU allocation by the orchestrator, meaning the configured `workers` count assumes more real compute than the container is actually allowed to use; align `workers` with the container's ACTUAL allocated resources, not the host's",`Docker containers cannot run browser processes at all, so this scenario is impossible`,`This is always caused by a network misconfiguration between containers`],1,"A containerized environment can report a CPU core count to Node.js that does not reflect its actual cgroup-enforced compute allocation — sizing `workers` off the wrong number leads to oversubscription and worse-than-expected throughput."],[`R`,`I`,`You want to avoid the cost of creating a fresh, expensive authenticated browser context for every single test, while still keeping tests properly isolated from each other. How do worker-scoped fixtures help here specifically?`,null,[`Worker-scoped fixtures cannot help with this; only test-scoped fixtures provide isolation`,`A worker-scoped fixture can create ONE browser context per worker (amortizing the expensive setup across every test that worker runs), while each individual TEST still gets its own fresh Page within that shared context (or, if page-level isolation is insufficient, the fixture can still open a new page per test from the shared context) — balancing setup cost against isolation needs more precisely than either extreme (context-per-test or one-context-for-everything)`,`Only a global (not worker) scope achieves any real cost savings`,`Reusing a browser context across tests always provides identical isolation to reusing a browser context per test`],1,`Worker scope sits between "recreate everything per test" (safest, slowest) and "share everything across the whole run" (fastest, least isolated) — a good middle ground for amortizing genuinely expensive setup while still giving each test its own page.`],[`S`,`A`,"A suite of 300 tests passes cleanly at `workers: 1` but 2 specific tests fail consistently at `workers: 4`. How do you systematically isolate the offending pair without reading all 300 tests?",null,[`Randomly delete tests until the failure stops occurring`,"Use `--grep` (or a binary-search approach with test.describe groupings) combined with a moderate worker count to progressively narrow down which subset of tests reproduces the failure, or check whether the two known-failing tests share an obvious resource (fixed test account, fixed file path, fixed port) by inspecting just their source rather than the whole suite; once identified, verify the hypothesis by running just those two together at workers: 2",`Increase workers even further until the failure becomes so frequent it is easy to spot`,`Rewrite the entire suite to avoid any possibility of shared state, without first diagnosing the actual two tests`],1,`A targeted narrowing approach (grep/bisection, or inspecting the specific failing tests for obvious shared resources) is far more efficient than either brute-force deletion or a blind full rewrite.`],[`S`,`I`,`Your CI bill increased noticeably after switching from 2 shards to 8 shards, even though the total number of workers across all shards combined stayed roughly the same. Why might this happen?`,null,[`Sharding itself has no cost implications; only worker count matters for billing`,`Many CI providers charge per concurrently-running MACHINE/runner (or per-minute per machine), not purely by total worker-seconds — going from 2 machines to 8 machines means 8 separate billed runners spinning up (each with its own fixed overhead: checkout, dependency install, browser install) even if the SUM of workers is unchanged, and that per-machine fixed overhead is now paid 8 times instead of 2`,`More shards always cost strictly less because each shard finishes faster`,`This can only happen if fullyParallel was also disabled at the same time`],1,`Per-machine fixed costs (checkout, install steps, runner spin-up) get paid once per shard — more shards means paying that fixed overhead more times, even when the total useful compute (worker-seconds) is unchanged.`],[`S`,`A`,`A worker-scoped database connection-pool fixture is used across a whole project, and partway through a long CI run, tests start failing with "too many connections" errors that were not present earlier in the same run. What is the likely design flaw?`,null,[`The database itself has a bug unrelated to the test suite`,`The connection-pool fixture likely opens new connections per test (or per some unit smaller than intended) without properly releasing/closing prior ones, so connections accumulate over the course of a long-running worker until the pool or the database's max-connections limit is exhausted — the fixture's cleanup (closing connections) needs auditing to ensure it actually runs after every test, not just at worker teardown`,`This is expected behavior and simply requires restarting the database mid-run`,`Worker-scoped fixtures cannot use database connections at all`],1,`A leak that only manifests partway through a long run (not immediately) is a classic sign of gradual resource accumulation — check that the fixture's per-test cleanup (not just its final worker-teardown) actually releases the resource it acquires.`],[`S`,`I`,"A `test.describe.serial()` block contains one unusually slow test (2 minutes) followed by two fast ones (a few seconds each). The whole FILE containing this block seems to take much longer than expected relative to other files. Why, and is this expected?",null,[`This indicates a Playwright bug specific to serial blocks`,`This is expected: since the serial block runs its tests in strict sequence on a SINGLE worker, that worker cannot start the two fast tests until the 2-minute test finishes — the block's total time is the SUM of its tests' durations rather than the max, unlike independent tests that could otherwise run concurrently across different workers`,`Serial blocks should always be faster than independent tests since they avoid context-switching overhead`,`The fast tests inside the serial block are secretly running in parallel with the slow one despite the serial declaration`],1,`A serial block trades away potential concurrency for ordering guarantees — its total time is necessarily additive (sum of its tests) since they cannot overlap, unlike independent tests that could run on separate workers simultaneously.`],[`S`,`A`,'Leadership wants a "zero flaky tests" dashboard, so the team has been steadily increasing `retries` whenever a test occasionally fails. Six months later, a real intermittent race condition in the APPLICATION has been quietly passing CI the whole time thanks to retries. How do you fix the governance problem, not just this one bug?',null,[`Keep increasing retries further since it has worked so far`,`Track and surface RETRY COUNTS (not just final pass/fail) as a first-class CI metric — a test that only passes on retry 2 or 3 consistently is a strong signal of a real, reproducible problem (app or test) that deserves investigation, even though the dashboard currently shows it as "passing"; treat a rising retry rate as an alert condition, not something retries should silently absorb forever`,`Disable retries entirely across the whole suite immediately with no transition plan`,`Retries are purely a test-infrastructure concern and can never mask real application bugs`],1,`The governance fix is making retry RATE visible as its own signal (not just hidden inside a binary pass/fail), so a real, reproducible issue does not get to hide indefinitely behind "eventually passes."`],[`S`,`I`,`All your parallel workers share a single Redis instance for test data, and tests intermittently fail because one test's key gets overwritten or deleted by another test running concurrently in a different worker. What is the general fix?`,null,[`Reduce workers to 1 permanently to eliminate all concurrency`,"Namespace every key by a per-test or per-worker unique identifier (e.g. prefixing keys with `test:${testInfo.testId}:` or `worker:${workerIndex}:`) so concurrently-running tests never read or write the same literal key, rather than relying on a single flat, shared keyspace",`Add a global lock around every Redis operation used by any test`,`Switch to a different in-memory store, since Redis itself is the root cause`],1,`The general fix for any shared-resource collision under parallelism is namespacing/isolating the resource per test or per worker — the same principle that applies to database schemas, ports, and file paths applies to Redis keys too.`],[`S`,`I`,`New hires' 4-core laptops run the suite noticeably slower than CI's 16-core machines, even though both are configured with \`workers: "50%"\`. Beyond hardware being different, is there anything to actually fix?`,null,[`This is expected and requires no configuration change: 50% of 4 cores (2 workers) versus 50% of 16 cores (8 workers) naturally produces very different wall-clock times for the same total test count — if fast local iteration matters for new hires, consider giving local runs a lower total test count (e.g. via tags) rather than trying to force parity with CI's much larger worker budget`,`Percentage-based workers should produce identical wall-clock times on any machine`,`This indicates a bug in Playwright's percentage-based worker calculation`,`The fix is to force workers: 8 locally regardless of the laptop's actual core count`],0,`A percentage of very different absolute core counts naturally yields very different absolute worker counts and therefore very different wall-clock times — the more actionable fix for local dev experience is often a smaller, tagged local subset rather than chasing CI's exact parallelism.`],[`S`,`A`,`After increasing shard count from 4 to 10, overall wall-clock time barely improved, even though CI now spins up 10 machines. Investigation shows one particular spec file alone accounts for 60% of total test duration. What is the real fix?`,null,[`Increase shard count even further to 20, expecting proportional improvement`,`Splitting shard count further cannot help once one oversized file (or a small handful of very slow tests) dominates total duration and cannot itself be divided further by shard count alone — the fix is to break UP that specific slow file/tests (or optimize their actual runtime) so the workload itself becomes divisible across more shards, rather than adding more shards to an already-lumpy distribution`,`This is expected and cannot be improved further with any restructuring`,`Switch to workers instead of shards, which will automatically fix the imbalance`],1,`Sharding can only spread out granularity that already exists — a single dominant slow unit (file or test) needs to be broken apart or sped up directly before adding more shards can help at all.`],[`S`,`A`,"Adding a `test.describe.serial()` block to a file that also contains many fullyParallel-eligible tests seems to have made the WHOLE FILE noticeably slower than expected, not just the serial block itself. What should you check?",null,[`describe.serial() always makes an entire file run at workers: 1 as a side effect`,`Depending on how Playwright schedules work, all tests within one FILE may end up assigned to run on the same worker in some configurations (particularly without fullyParallel at the project level), meaning the serial block's sequential tests could end up blocking the SAME worker's other, otherwise-independent tests in that file from running concurrently on a different worker — check whether fullyParallel is actually enabled for that project, and consider moving the serial block to its own file if it is holding up unrelated tests`,`This is unrelated to describe.serial() and must be caused by a different config change`,`Serial blocks are always executed on a dedicated worker separate from the rest of the file`],1,`Without fullyParallel, an entire file's tests share one worker by default — a serial block's sequential tests occupying that worker can end up delaying unrelated tests in the SAME file that would otherwise have run concurrently elsewhere.`],[`S`,`I`,`Migrating from one large, always-warm self-hosted CI runner to ephemeral cloud runners (spun up fresh per job) changes which kinds of flakiness show up in a parallel Playwright suite. What is different, and what should you check for?`,null,[`Nothing meaningfully changes; ephemeral and persistent runners behave identically for test execution`,`Ephemeral runners start cold every time (no warm OS/browser/dependency caches, no lingering state from a previous run) and may have different, less predictable available CPU/memory depending on the cloud provider's current capacity — tests or fixtures that implicitly relied on a "warm," consistently-provisioned self-hosted machine (e.g. assuming a browser binary is already cached, or assuming a certain baseline resource availability) can newly surface as flaky under ephemeral, colder, more variable runners`,`Ephemeral runners always have MORE available resources than persistent self-hosted machines`,`This change can only affect sharding behavior, never worker-level parallelism`],1,`A move to ephemeral infrastructure removes implicit assumptions (warm caches, consistent resource availability) that a long-lived self-hosted runner may have quietly been providing — those assumptions can become new sources of flakiness once removed.`],[`D`,`A`,`Worker 3 out of 4 consistently finishes in a fraction of the time the others take, reporting zero failures, in a suite that should be evenly distributed. What should you check first?`,null,[`Assume worker 3 is simply on faster hardware and move on`,`Check whether worker 3 was actually assigned any meaningful share of tests at all — an uneven or buggy test distribution (e.g. due to a testMatch/testIgnore misconfiguration, or a project filter accidentally excluding files from that worker's share) can result in a worker finishing suspiciously fast simply because it had very little real work to do, not because it is faster`,`Zero failures on one worker always indicates a monitoring bug`,`This is normal variance and never worth investigating further`],1,`A worker finishing implausibly fast with zero failures is a common sign that it was not actually given a fair (or any) share of real work, rather than evidence that everything on it is healthy.`],[`D`,`A`,'"Target page, context or browser has been closed" errors start appearing across multiple unrelated tests only when `workers` is increased past a certain threshold on a given CI machine. What is the likely root cause?',null,[`This error only ever indicates a bug in the test's own code, never an environment issue`,`At high enough worker counts on a resource-constrained machine, the OS or the browser processes themselves can be killed or fail to start due to memory/CPU exhaustion (e.g. an OOM killer terminating a browser process) — the resulting "closed" errors are a symptom of resource starvation from over-provisioning workers relative to the machine's actual capacity, not a logic bug in the tests`,`This error only ever occurs when sharding, never with workers alone`,`Increasing workers can never cause browser or context closures under any circumstances`],1,`A "closed" error appearing broadly across many unrelated tests, correlated specifically with worker count, points toward resource exhaustion (memory/CPU) causing browser processes to be killed, rather than a bug in any individual test.`],[`D`,`I`,`A merged HTML report from a 4-shard sharded CI run shows several failed tests but is missing the trace file link for some of them, showing only "trace not found". What should you investigate?`,null,[`This indicates a Playwright bug in merge-reports that cannot be worked around`,`Verify the CI pipeline's artifact-upload step actually uploaded each shard's full trace/blob output directory (not just a subset), and that the merge step downloaded ALL shards' artifacts before running merge-reports — a partial or missing artifact upload/download for even one shard results in that shard's failing tests showing up in the merged report without their accompanying trace files`,`trace files are never included in blob reports and must be uploaded completely separately by design`,`Only the first shard's traces are ever included in a merged report`],1,`Missing traces in an otherwise-successful merge usually point to an incomplete artifact pipeline (upload or download step) for one or more shards, rather than a fundamental limitation of merge-reports itself.`],[`D`,`A`,"A worker-scoped fixture's teardown code (after `use()`) never seems to run when a CI job is stopped early due to `maxFailures` being reached. What is happening, and does it matter?",null,[`This is a bug that must always be fixed regardless of context`,`When a run is aborted early (e.g. via maxFailures or a manual cancellation), Playwright may not get the chance to gracefully run every worker fixture's teardown before the whole process is torn down — whether this matters depends on what the teardown was responsible for: ephemeral CI containers get destroyed regardless (no real leak), but external, longer-lived resources (a cloud database, a container on a shared host) may need a more robust cleanup mechanism, like an external timeout/expiry, that does not depend solely on graceful fixture teardown`,`Worker fixture teardown always runs even when the process is killed abruptly`,`maxFailures cannot be used together with worker-scoped fixtures`],1,`Graceful teardown assumes a graceful shutdown — an abrupt stop (maxFailures, cancellation, crash) can skip it, so genuinely external, persistent resources need a cleanup strategy that does not solely rely on fixture teardown running.`],[`D`,`A`,"Increasing `workers` from 8 to 16 on the same CI machine makes the suite finish SLOWER (not just fail to improve, but measurably worse) in total wall-clock time. What negative-returns phenomenon is this, and what should you check?",null,[`This can never happen; more workers always help or at worst have no effect`,`This is a classic sign of resource oversubscription — beyond some point, additional workers compete so heavily for the SAME finite CPU/memory/disk I/O that the overhead of context-switching and contention outweighs any parallelism benefit, making the machine slower overall than a lower, better-matched worker count; check CPU/memory utilization and consider reducing workers or moving to sharding across more machines instead`,`This is always caused by a network issue unrelated to worker count`,`Playwright silently caps effective parallelism at 8 regardless of the workers setting`],1,`Parallelism has a sweet spot tied to actual machine capacity — pushing worker count well past what the hardware can support leads to contention overhead that can make total runtime WORSE, not just fail to improve.`],[`L`,`I`,`What is the smallest unit of work that Playwright distributes across parallel workers?`,null,["An individual `await` expression inside a test","A single TEST (a `test(...)` block) — Playwright does not split the internal steps of one test across multiple workers; everything inside one test function always executes sequentially, in order, on a single worker, from start to finish","A single `test.step()` call",`A single assertion (expect() call)`],1,`Parallelism in Playwright happens BETWEEN tests (or files), never within a single test — the steps inside one test are always sequential on whichever worker was assigned that test.`],[`L`,`A`,"How does `test.describe.serial()` differ in SCOPE from simply leaving `fullyParallel` unset (false) for the whole project?",null,[`They are exactly the same and one is redundant given the other`,`Without fullyParallel, an entire FILE's tests run sequentially on one worker by default, but different files still run concurrently across workers; describe.serial() is a much narrower, explicit tool that forces just the tests inside that specific block to run in order on one worker, regardless of the file-level or project-level parallelism setting otherwise in effect`,`describe.serial() applies to the whole project, while fullyParallel only applies to one file`,`fullyParallel: false disables workers entirely, while describe.serial() does not`],1,`The default (non-fullyParallel) serialization happens at the FILE level as a blanket default; describe.serial() is a targeted, explicit override that works at the describe-block level regardless of what the broader default is.`],[`L`,`A`,"If a worker process crashes mid-run (e.g. due to a browser crash), what happens to its `workerIndex`, and why does this matter for anything keyed off that index?",null,[`Playwright reuses the exact same workerIndex for the replacement worker`,"Playwright spawns a replacement worker process with a NEW, higher workerIndex — the crashed worker's index is retired and never reused for the rest of the run, which matters if anything (like a per-worker resource name or log file) was keyed off workerIndex assuming a fixed, small set of indices equal to the configured worker count; after a crash, indices can exceed the configured `workers` value",`The entire test run aborts immediately whenever any worker crashes`,`Crashed workers are simply skipped, and their assigned tests are marked as passed by default`],1,`workerIndex is a monotonically increasing PROCESS identity, not a fixed slot number — a crash-and-restart means indices can grow beyond what you might expect from just the workers count, unlike the always-bounded parallelIndex.`],[`L`,`A`,"Why might a suite become I/O-bound (rather than CPU-bound) at high worker counts specifically when `video` or `trace` recording is enabled for every test?",null,[`Video and trace recording only consume network bandwidth, never disk I/O`,`Every worker independently writes video frames and/or trace data to disk for every test it runs; with many workers running concurrently, the aggregate disk write throughput required can exceed what the underlying storage (especially on CI, where disks may be network-backed or otherwise slower) can sustain, making disk I/O — not CPU — the actual bottleneck limiting further speedup from more workers`,`Video/trace files are written directly to memory and never touch disk until the run ends`,`Increasing workers automatically reduces the video/trace recording frequency to compensate`],1,`Beyond a certain worker count, the constraint shifts from "not enough CPU" to "not enough disk throughput" when every worker is simultaneously writing rich artifacts like video and trace data — a different bottleneck requiring a different fix (disable/limit artifacts, or use faster storage).`],[`L`,`I`,`Does sharding a suite across more CI machines reduce the TOTAL amount of CPU-time (CPU-seconds) consumed to run the whole suite?`,null,[`Yes, sharding magically reduces the total work that needs to be done`,`No — sharding redistributes the SAME total amount of work (and therefore roughly the same total CPU-seconds) across more machines running concurrently, which reduces WALL-CLOCK time but does not reduce the underlying total compute consumed; this is why more shards can increase CI cost even while decreasing how long you wait for results`,`Sharding always halves total CPU-time regardless of how many shards are used`,`Sharding only affects reporting output, not actual test execution`],1,`Sharding is about spreading unchanged total work across more concurrent machines to cut WALL-CLOCK time — it does not shrink the actual amount of computation the suite requires, which is why it can cost more even as it finishes faster.`],[`L`,`I`,"Why might a team deliberately place a small group of unusually timing-sensitive tests (e.g. precise animation-timing assertions) into their own project with `workers: 2` instead of leaving them fully parallel with everything else?",null,[`Timing-sensitive tests cannot be run under any parallel configuration`,`Reducing contention for that specific subset of tests reduces the chance that CPU scheduling variance under heavy concurrent load introduces just enough timing jitter to make a precise, timing-sensitive assertion flaky — trading some speed for more consistent, reliable results specifically where timing precision matters most`,`Setting workers: 2 for one project automatically increases the timeout for that project's tests`,`This has no effect since workers is a global-only setting shared by every project`],1,`Timing-sensitive assertions are more exposed to the small scheduling jitter that heavy concurrent contention introduces — giving that specific subset less contention (lower workers) is a targeted way to improve their reliability.`],[`L`,`A`,`Why does a worker-scoped fixture that seeds shared setup data need to be written defensively (idempotently) rather than assuming it always runs exactly once at a clean starting point?`,null,[`Worker-scoped fixtures are guaranteed to run exactly once with no exceptions, so defensive coding is unnecessary`,`A worker can be replaced after a crash (getting a fresh instance of the fixture), or a retried test might re-trigger certain setup paths depending on fixture scope interactions — writing setup to check "has this already been done" (e.g. checking if a schema/record already exists before creating it) avoids errors or duplicate data if the setup logic ever runs more than once in practice`,`Idempotent setup is only relevant for test-scoped fixtures, never worker-scoped ones`,`Defensive/idempotent fixtures always run slower and should be avoided for performance`],1,`Real-world parallel execution includes edge cases (crashes, restarts) where "runs exactly once, cleanly" is not a fully safe assumption — idempotent setup logic tolerates being invoked more than once without breaking.`],[`L`,`I`,`What is the difference between a suite failing due to file-descriptor exhaustion versus memory exhaustion under high worker counts, in terms of how each typically presents?`,null,[`They present identically and cannot be distinguished`,`File-descriptor exhaustion typically shows up as errors like "too many open files" or failures to open new sockets/processes once the OS-level limit (ulimit) is hit, often fixable by raising the limit or reducing concurrent file/socket usage; memory exhaustion typically shows up as processes being killed (e.g. by an OOM killer) or allocation failures, usually fixable by reducing workers or increasing available RAM — different root causes needing different fixes despite both being triggered by "too many workers"`,`Both failure modes are always fixed by the exact same configuration change`,`Neither failure mode is related to worker count in any way`],1,`"Too many workers" can manifest as two genuinely different resource ceilings — file descriptors (OS-level open-handle limits) versus memory (RAM) — and distinguishing which one you actually hit determines whether the fix is a ulimit change or a worker-count/RAM change.`],[`L`,`B`,"Why is `--workers=1` often the very first thing to try when a previously-reliable suite suddenly becomes flaky after enabling more parallelism?",null,[`--workers=1 fixes the underlying bug permanently and is a valid long-term solution`,`Forcing workers down to 1 removes concurrency as a variable entirely — if the flakiness disappears at workers=1, that strongly implicates a parallel-execution issue (shared state, resource contention) rather than a bug inherent to any individual test's own logic, giving a fast, cheap first signal about where to focus further investigation`,`--workers=1 makes Playwright skip all assertions, which is why it appears more stable`,`This has no diagnostic value since workers=1 changes nothing about how tests execute`],1,`Toggling a single variable (concurrency) off is a cheap, fast way to confirm or rule out an entire category of cause (parallel-execution issues) before investigating individual tests in detail.`],[`L`,`A`,"When `maxFailures` is configured and a suite is sharded across 4 independent CI jobs, does hitting `maxFailures` on shard 2 stop shards 1, 3, and 4 as well?",null,[`Yes — maxFailures is a global, cross-shard circuit breaker that stops every shard immediately`,`No — each shard normally runs as its own independent Playwright process/CI job, so maxFailures is evaluated PER SHARD; shard 2 hitting its failure limit and aborting has no direct effect on the separate, independently-running processes for shards 1, 3, and 4 unless the CI pipeline itself is separately configured (e.g. via fail-fast) to cancel siblings on one job's failure`,`maxFailures cannot be used at all in a sharded configuration`,`Sharded runs automatically share a single maxFailures counter across all shards by default`],1,`maxFailures operates within the scope of one Playwright process — in a sharded setup that means per-shard, not globally; cross-shard cancellation on failure is a separate CI-level concern (like a fail-fast matrix setting), not something maxFailures itself provides.`],[`L`,`A`,"Why can CI runs occasionally show inconsistent performance for the exact same `workers` configuration and the exact same test suite, run back to back?",null,[`Playwright introduces intentional random delays between runs for security reasons`,`On shared/virtualized CI infrastructure, "noisy neighbor" effects (other tenants' workloads competing for the same underlying physical CPU/disk/network) can cause the actual available performance to vary between runs even with identical configuration and code — a form of environmental variance outside the test suite's or Playwright's control`,`Identical configuration and code always produce identical timing on any infrastructure`,`This can only happen if fullyParallel was toggled between runs`],1,`Shared cloud/CI infrastructure is not a performance-isolated environment — variance from other tenants' workloads on the same physical hardware is a real, external source of run-to-run timing differences.`],[`L`,`I`,"Why does Playwright's UI mode (`--ui`) or watch-style local development workflow typically favor a much smaller effective parallelism than a full CI run?",null,[`UI mode is technically incapable of running more than one worker under any circumstances`,`A fast, interactive local dev loop generally benefits more from quick, focused feedback on the specific test(s) currently being worked on rather than maximum throughput across an entire large suite — local workflows often intentionally scope down to a subset of tests (or accept lower parallelism) in exchange for a snappier, more interactive experience, unlike CI which optimizes for total suite throughput`,`UI mode disables all fixtures, requiring workers: 1 as a technical necessity`,`Watch mode and CI mode always use identical parallelism settings by design`],1,`Local interactive development and CI batch execution optimize for different things — fast, focused iteration on a few tests versus maximum throughput across the whole suite — which naturally leads to different practical parallelism choices.`],[`L`,`A`,"Does increasing `workers` reduce the TOTAL number of browser process launches Playwright performs over the course of a full suite run?",null,[`Yes, more workers means fewer total browser launches since work is consolidated`,`No — the total number of browser CONTEXTS (and the underlying browser process work) is still roughly tied to the total number of tests (each test typically gets a fresh context, and workers reuse a browser instance across the tests they run); more workers changes HOW MANY of those are happening CONCURRENTLY at once, not the total amount of browser-related work the suite does overall`,`Workers and browser launches are entirely unrelated concepts`,`Each worker launches exactly one browser process per configured worker, regardless of test count`],1,`workers controls concurrency (how many things happen AT ONCE), not the total volume of work (how many contexts/tests exist in total) — those are two different axes.`],[`L`,`A`,"When a failed test is retried under `fullyParallel: true`, is the retry guaranteed to run on the SAME worker (and therefore the same workerIndex) as the original failed attempt?",null,[`Yes, retries always execute on the exact same worker process as the original attempt`,`No — a retry is simply another attempt at running that test, and Playwright's scheduler is free to assign it to whichever worker becomes available next, which may or may not be the same worker/process as the original failed attempt; code that assumes retry-and-original always share a workerIndex (e.g. for some caching optimization) could be making an incorrect assumption`,`Retries always run with workers forced to 1 regardless of the global setting`,`Retried tests skip worker assignment entirely and run in the main process`],1,`A retry is scheduled like any other unit of work — there is no guarantee it lands on the same worker process as the original attempt, which matters if any retry-related logic assumes worker-level continuity across attempts.`],[`L`,`A`,`What does it mean to say a fully-parallel suite is "only as parallel-safe as its least parallel-safe test"?`,null,[`This is not true; one problematic test can never affect the reliability of any other, unrelated test`,`A single test with a hidden shared-state dependency (e.g. mutating a fixed global record, or relying on execution order) can introduce intermittent cross-test failures whenever it happens to run concurrently with whatever it conflicts with — meaning the overall RELIABILITY of enabling full parallelism for the whole suite is gated by whether every individual test has actually been made safe for concurrent execution, not just most of them`,`This refers to the slowest test setting a floor on total suite runtime, not a safety concern`,`Parallel-safety only matters for tests inside the same describe block, never across files`],1,`Full-suite parallel safety is not an average — a single unsafe test (with a hidden dependency on shared state or ordering) can introduce real, intermittent failures whenever it happens to overlap with whatever it conflicts with, regardless of how well-isolated every other test is.`],[`L`,`A`,'Why might a team run an occasional "chaos" CI job that deliberately shuffles test execution order (or runs with `--repeat-each` at a high worker count) before trusting `fullyParallel: true` for a large legacy suite?',null,[`Shuffled/repeated runs are purely for entertainment and provide no real signal`,`Deliberately varying execution order and repeating runs under real concurrency is a practical way to surface hidden order-dependency or shared-state bugs that a suite's NORMAL, consistent execution order might never happen to expose — giving more confidence that enabling full parallelism broadly will not introduce hidden intermittent failures`,`This technique can only be used after fullyParallel is already fully enabled, never before`,`Shuffling test order requires disabling all fixtures for the run`],1,`A suite's usual, consistent run order can accidentally hide order-dependency bugs simply by never exercising the "bad" orderings — deliberately varying order/concurrency is a way to actively hunt for those hidden landmines before fully committing to parallel execution.`],[`O`,`A`,"What does logging `testInfo.retry` here help distinguish?",`test('flaky-looking checkout flow', async ({ page }, testInfo) => {
  console.log(\`[w\${testInfo.workerIndex}] attempt #\${testInfo.retry}\`);
  await page.goto('/checkout');
  // ...
});`,[`testInfo.retry always equals 0 and provides no useful information`,`testInfo.retry reports which attempt number this is (0 for the first try, 1 for the first retry, etc.) — combined with the worker index, this makes it possible to tell from CI logs alone whether a given execution was an original attempt or a retry, and on which worker it ran, which is valuable context when diagnosing intermittent failures`,`testInfo.retry can only be accessed from a fixture, never directly inside a test function`,`Logging testInfo.retry automatically increases the configured retries value`],1,`testInfo.retry combined with workerIndex gives you exactly the context needed to distinguish "first attempt on worker 2" from "retry attempt on worker 5" when reading raw, interleaved CI log output.`],[`O`,`I`,`What does this idempotent setup pattern protect against?`,`export const test = base.extend({
  seededSchema: [async ({}, use, workerInfo) => {
    const schema = \`w_\${workerInfo.workerIndex}\`;
    const exists = await schemaExists(schema);
    if (!exists) {
      await createSchema(schema);
      await seedBaseData(schema);
    }
    await use(schema);
  }, { scope: 'worker' }],
});`,[`It protects against the schema ever being created more than once, even across separate test RUNS`,`It protects against duplicate-creation errors or double-seeding if this worker-scoped fixture setup were ever invoked more than once for the same schema name within unusual circumstances (like a worker restart reusing infrastructure) by checking existence first — a defensive habit for setup logic that is assumed to run "once per worker" but should tolerate not being perfectly guaranteed to`,`schemaExists() is unnecessary since worker-scoped fixtures can mathematically never run twice`,`This pattern will cause every test in the worker to re-seed data before each individual test`],1,`Checking existence before creating is a simple, defensive habit that makes worker setup tolerant of being invoked more than once, rather than assuming a guarantee that may not always hold in every edge case.`],[`O`,`I`,`What is the effect of this reporter configuration for a suite that needs both live CI console feedback and shard-mergeable output?`,`export default defineConfig({
  reporter: process.env.CI ? [['line'], ['blob']] : [['list']],
});`,[`Only the last reporter in the array is ever actually used`,`In CI, it uses BOTH the "line" reporter (compact, streaming console output useful for watching a live CI log) and the "blob" reporter (binary artifact for later merging across shards) simultaneously — combining a good live-feedback format with a format that supports later cross-shard report merging; locally, it just uses the more detailed "list" reporter`,`Multiple reporters cannot be combined in a single array`,`blob and line reporters produce conflicting output and cannot coexist`],1,`Playwright supports multiple simultaneous reporters — pairing a human-readable live reporter with a machine-mergeable one (blob) for sharded CI is a common, practical combination.`],[`O`,`A`,`What does having each worker write its own manifest file, later consumed by globalTeardown, accomplish?`,`// worker fixture teardown:
await fs.promises.writeFile(\`./.tmp/worker-\${workerInfo.workerIndex}.json\`, JSON.stringify({ containerId }));

// global-teardown.ts
export default async function globalTeardown() {
  const files = await fs.promises.readdir('./.tmp');
  for (const f of files) {
    const { containerId } = JSON.parse(await fs.promises.readFile(\`./.tmp/\${f}\`, 'utf8'));
    await stopContainer(containerId);
  }
}`,[`It has no purpose since worker fixtures can directly communicate with globalTeardown in memory`,`Since globalTeardown runs in a separate process from the individual workers (which may have already exited), it cannot rely on in-memory state from those workers — writing each worker's resource identifiers to a file lets globalTeardown discover and clean up EVERY worker's resources (like containers) after the fact, even though it has no direct in-memory connection to any of them`,`This pattern only works if all workers run inside the same process`,`JSON.stringify cannot be used to persist data between a worker and globalTeardown`],1,`globalTeardown runs in a separate process with no shared memory with the workers that already exited — a manifest file (or similar durable record) is how it discovers what needs cleaning up from each worker's independent lifecycle.`],[`O`,`A`,`What does wrapping the request in retry/backoff logic accomplish for this fixture used by many concurrent workers?`,`async function getWithBackoff(request, url, attempt = 0) {
  const res = await request.get(url);
  if (res.status() === 429 && attempt < 3) {
    await new Promise(r => setTimeout(r, 500 * 2 ** attempt));
    return getWithBackoff(request, url, attempt + 1);
  }
  return res;
}`,[`It guarantees the request will never fail under any circumstances`,`It absorbs occasional 429 (rate-limited) responses — likely to occur more often as worker count increases and more concurrent requests hit a rate-limited backend — by waiting with exponential backoff and retrying a bounded number of times, rather than letting every rate-limit response immediately fail the test`,`Exponential backoff makes every request slower regardless of whether rate limiting occurs`,`This pattern eliminates the need for reducing worker count against rate-limited backends`],1,`Backoff-and-retry at the request level is a targeted way to absorb the INCREASED chance of hitting a real rate limit that comes with more concurrent workers, without necessarily having to reduce parallelism everywhere.`],[`O`,`I`,`What configuration mistake does this project setup contain?`,`projects: [
  { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  { name: 'chromium', use: { ...devices['Desktop Chrome'], viewport: { width: 1920, height: 1080 } } },
],`,[`Two projects are not allowed to use the same browser engine`,`Both projects share the identical name "chromium" — Playwright requires distinct project names, and duplicate names can cause ambiguity in CLI filtering (--project=chromium) and reporting, where it becomes unclear which "chromium" project a given result belongs to; each project should have a unique, descriptive name (e.g. "chromium-desktop", "chromium-1080p")`,`This configuration doubles the effective workers available to chromium tests`,`viewport cannot be overridden within a devices spread`],1,`Distinct, descriptive project names are important not just for config clarity but for CLI filtering and report readability — duplicate names create ambiguity about which configuration produced which result.`],[`O`,`A`,`What does deriving the video output path this way prevent under parallel execution?`,`use: {
  video: {
    mode: 'retain-on-failure',
  },
},
// Playwright automatically scopes each test's video file under testInfo.outputDir,
// e.g. test-results/checkout-flow-should-charge-card-chromium/video.webm`,[`Every test writes its video to one single shared video.webm file, requiring manual renaming after the run`,`Because Playwright automatically namespaces each test's output (including video) under a directory derived from that specific test's title/project, concurrently-running tests across different workers never write their video files to the same path, avoiding the overwrite/corruption that a single shared, hardcoded output path would cause`,`video: { mode: "retain-on-failure" } disables parallel execution for tests using video`,`Video files are always stored in memory and never actually written to test-results/`],1,`Playwright's automatic per-test output directory naming is what keeps concurrently-running tests' artifacts (video included) from colliding — the same principle behind testInfo.outputPath() for custom files.`],[`O`,`I`,"What does branching on `testInfo.project.name` accomplish in this fixture?",`export const test = base.extend({
  apiTimeout: async ({}, use, testInfo) => {
    const timeout = testInfo.project.name === 'slow-3g-emulation' ? 15_000 : 5_000;
    await use(timeout);
  },
});`,[`testInfo.project.name is not accessible from within a fixture`,`It lets a single shared fixture provide a DIFFERENT timeout value depending on which project (e.g. a throttled-network emulation project versus a normal one) the currently-running test belongs to, without needing separate fixture implementations per project`,`This fixture changes the actual project configuration at runtime`,`project.name always returns "default" regardless of the projects array`],1,`testInfo carries context about which project produced the current test run, letting one shared fixture adapt its behavior (like a timeout) to different projects' known characteristics (e.g. a deliberately slow network emulation).`],[`R`,`I`,`Your organization runs the same large suite across dozens of teams and wants known-flaky tests automatically routed to a quarantine project without every team manually maintaining a flaky-test list. How could this be automated?`,null,[`Have every team manually tag their own flaky tests and hope for consistency`,"Track historical pass/fail/retry data per test (from CI reporting or a test-analytics tool) and periodically regenerate a `testIgnore`/`testMatch` list (or a shared tag) for the quarantine project based on measured flakiness rates over recent runs — automating what would otherwise be manual, inconsistent tagging across many teams",`Quarantine projects cannot be populated programmatically under any circumstances`,`Disable retries organization-wide instead of building any quarantine mechanism`],1,`Using actual historical CI data (rather than manual, per-team tagging) to drive which tests land in a quarantine project scales much better across many teams and stays accurate as flakiness patterns change over time.`],[`O`,`A`,"Unlike `workers`, can `retries` be set differently per project in the same config?",`projects: [
  { name: 'chromium', retries: 0 },
  { name: 'flaky-legacy-browser', retries: 3 },
],`,[`No — retries, like workers, is a single top-level setting shared by every project`,"Yes — unlike `workers` (which is strictly global), `retries` CAN be overridden per project, letting a known-less-stable project (e.g. an older/flakier browser target) get extra retries while a reliable project stays at 0, without affecting the shared worker pool or any other project's retry behavior",`retries can only be set globally or per-test, never per-project`,`Setting retries per project disables sharding for that project`],1,`workers and retries are both parallelism-adjacent settings, but only retries supports per-project granularity — workers remains a single run-wide budget regardless of project.`],[`O`,`I`,`What race condition does this pattern introduce when multiple parallel workers use it?`,`// tests/helpers/shared-state.json is read and rewritten by multiple tests
const data = JSON.parse(fs.readFileSync('shared-state.json', 'utf8'));
data.counter += 1;
fs.writeFileSync('shared-state.json', JSON.stringify(data));`,[`This pattern is safe because Node.js file operations are always atomic across processes`,`Two tests in different workers can both read the file at the SAME counter value, each increment their own in-memory copy, and then write back — whichever write happens last overwrites the other, silently losing an increment (a classic read-modify-write race); a database with atomic increments, or a per-worker/per-test unique file, avoids this`,`JSON.parse and JSON.stringify automatically serialize concurrent access`,`This code can only fail if workers is set higher than 8`],1,`A shared file used as read-modify-write state is a classic concurrency hazard — without file locking or an atomic operation, concurrent writers can silently clobber each other's updates.`],[`O`,`A`,`What is the flaw in this attempt to build a cross-test "lock" to serialize access to a resource?`,`// Intended as a mutex shared across all tests
const locks = new Map<string, boolean>();
export async function acquireLock(key: string) {
  while (locks.get(key)) {
    await new Promise(r => setTimeout(r, 50));
  }
  locks.set(key, true);
}`,[`setTimeout cannot be used inside an exported async function`,`This Map only lives inside the memory of ONE worker process — tests running in a DIFFERENT worker have their own separate copy of this module (and therefore their own separate, empty Map), so this "lock" provides no actual cross-worker mutual exclusion at all; it only coincidentally serializes access among tests that happen to land in the same worker`,`This code correctly implements a mutex that works across all workers`,`Map.get() and Map.set() are asynchronous and require awaiting`],1,`A common trap: in-process data structures like a Map look like shared global state, but each worker process has its own independent copy — genuine cross-worker coordination needs an external mechanism (file, DB, or a dedicated coordination service), not an in-memory JS object.`],[`O`,`I`,"What does combining `--grep` and `--grep-invert` like this achieve when run alongside the main sharded suite?",`# main suite (sharded):
npx playwright test --grep-invert @quarantine --shard=1/4

# separate quarantine job (not sharded):
npx playwright test --grep @quarantine`,[`Both commands run the exact same set of tests, just with different reporters`,`The main sharded run explicitly EXCLUDES anything tagged @quarantine (keeping the stable, shardable suite clean and focused), while a separate, dedicated job runs ONLY the @quarantine-tagged tests — cleanly partitioning "reliable, shardable work" from "known-flaky work under its own separate handling"`,`--grep-invert disables sharding entirely wherever it is used`,`@quarantine is a reserved Playwright keyword with special built-in behavior`],1,`Combining an inverted grep on the main run with a matching grep on a separate job is a simple way to cleanly split "everything except the flaky stuff" from "just the flaky stuff" without needing separate config files.`],[`O`,`I`,"What does calling `testInfo.attach()` here provide, and how is it kept safe under parallel execution?",`test('checkout produces a valid receipt', async ({ page }, testInfo) => {
  const receipt = await page.locator('#receipt').innerText();
  await testInfo.attach('receipt-text', { body: receipt, contentType: 'text/plain' });
});`,[`It writes the attachment to a single shared file that every test appends to`,`It attaches a custom artifact (here, plain text) to THIS specific test's result — Playwright automatically scopes and stores each test's attachments separately (keyed to that test's own result/report entry), so concurrently-running tests in different workers never collide on where their attachments are stored`,`testInfo.attach() only works when workers: 1 is configured`,`Attachments created this way are shared globally across the entire test run as one combined report section`],1,`Like other per-test artifacts (screenshots, traces, videos), attachments are automatically scoped to the specific test that created them — no manual namespacing needed to avoid collisions under parallel execution.`],[`O`,`A`,`What does this configuration achieve, combining per-project settings with a single shared worker pool?`,`export default defineConfig({
  workers: 6,
  projects: [
    { name: 'chromium-fast', retries: 0, timeout: 15_000 },
    { name: 'webkit-legacy', retries: 2, timeout: 45_000 },
  ],
});`,[`Each project gets its own separate pool of 6 workers, for 12 total`,`Both projects' tests compete for the SAME shared pool of 6 workers overall, but each project independently controls its own retries and timeout — letting a known-slower or less-stable browser target (webkit-legacy) get more patience without affecting the faster, more reliable project's settings or inflating the total worker budget`,`timeout cannot be set per-project, only globally`,`This configuration is invalid because two projects cannot have different timeout values`],1,`workers is shared across all projects (a single budget), while retries and timeout are per-project settings — this config uses that combination to give different browser targets different tolerances without duplicating the worker pool.`],[`O`,`A`,`What would be needed to build a custom tool that assigns tests to shards based on ACTUAL historical duration rather than raw count, and what role does a custom reporter play?`,`class TimingReporter {
  onTestEnd(test, result) {
    appendToLog({ title: test.titlePath().join(' > '), duration: result.duration });
  }
}
export default TimingReporter;`,[`Playwright automatically builds this timing dataset with no custom reporter needed`,`A custom reporter like this one can capture per-test durations from real runs into a persistent log; a separate tool then reads that historical timing data across many runs and uses it to assign tests/files to shards so each shard's TOTAL estimated duration is balanced, rather than just balancing raw test counts — this is exactly the kind of building block "timing-aware sharding" tooling is built from`,`Custom reporters cannot access per-test duration information`,`onTestEnd is called once for the entire suite, not once per test`],1,`A custom reporter is precisely how you would capture the real, per-test timing data needed to later build shard assignments based on actual duration rather than a naive count-based split.`],[`O`,`I`,"What is the key difference this comment is pointing at, between Playwright's test workers and Node.js `worker_threads`?",`// NOTE: Playwright test workers are separate OS PROCESSES (like child_process.fork),
// NOT Node.js worker_threads — they do not share memory or a heap with each other or the main process.`,[`The comment is incorrect; Playwright workers are actually worker_threads that share memory`,`This distinction matters because worker_threads CAN share memory (via SharedArrayBuffer or similar), which might tempt someone to assume Playwright workers could too — but since Playwright workers are independent processes, any in-memory JS object (Map, plain variable, cache) is NOT actually shared between them, which is the root cause of several "why isn't my shared state working across workers" bugs`,`Worker processes and worker_threads are functionally identical in every relevant way for Playwright users`,`This distinction only matters when using more than 16 workers`],1,`The confusion between "process" and "thread" isolation is exactly why in-memory sharing tricks that might work with worker_threads (shared memory) silently fail to provide any real sharing between Playwright's process-based workers.`],[`O`,`A`,"What does setting `fullyParallel: true` only inside one project's `use`-adjacent project-level setting (not globally) achieve in this config?",`export default defineConfig({
  projects: [
    { name: 'independent-checks', fullyParallel: true },
    { name: 'legacy-sequential', fullyParallel: false },
  ],
});`,[`fullyParallel cannot be set at the project level, only globally`,`fullyParallel can be overridden per project, so tests within "independent-checks" run with test-level parallelism (each test schedulable independently across workers), while tests within "legacy-sequential" fall back to the default file-level parallelism (serialized within each file) — letting a suite mix migration-in-progress code with already-verified fully-parallel-safe code in the same run`,`Setting fullyParallel per project silently disables sharding for that project only`,`Both projects will always behave identically since workers is shared globally`],1,`fullyParallel is one of the settings that CAN be scoped per project, which is exactly what supports the incremental, project-by-project migration path toward full parallelism for a large legacy suite.`]]};export{t};